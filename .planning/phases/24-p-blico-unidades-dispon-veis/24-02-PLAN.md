---
phase: 24-publico-unidades-disponiveis
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/components/features/UnidadesPublicas.js
  - src/components/features/UnidadePublicaCard.js
  - src/components/features/UnidadeDetailSheet.js
autonomous: false
requirements: [PUB-01, PUB-02, PUB-03, PUB-04, PUB-05]
must_haves:
  truths:
    - "A listagem pública tem abas por edifício com contadores + aba Todos (PUB-01)"
    - "Visitante ordena a lista por relevância / menor valor / maior valor / maior área via pills (PUB-02)"
    - "Cards exibem imagem de capa, área, valor (ou 'Consulte o Proprietário') e status Disponível (PUB-03)"
    - "No desktop os cards aparecem em grid responsivo dentro de container maxWidth 1100 (PUB-03)"
    - "A ficha bottom-sheet mostra imagem real, descrição, área, valor mensal, valor/m² e refs LOC+REF (PUB-04)"
    - "D-15: 'Simular Aluguel' mantém lógica existente: setRemovingId(uid) → 700ms → setRemovedIds + fechar sheet; animação opacity-0 transition-opacity duration-700 preservada (rUnitOut deferred) (PUB-05)"
  artifacts:
    - path: "src/components/features/UnidadesPublicas.js"
      provides: "Tela principal com sort state, fotoSrcs map, grid desktop, sort control bar"
      contains: "sortUnits"
    - path: "src/components/features/UnidadePublicaCard.js"
      provides: "Card com imagem de capa 116px + overlay + StatusBadge absoluto"
      contains: "fotoSrc"
    - path: "src/components/features/UnidadeDetailSheet.js"
      provides: "Sheet com imagem real, valor/m², refs, CTA [>] Simular Aluguel"
      contains: "Simular Aluguel"
  key_links:
    - from: "src/components/features/UnidadesPublicas.js"
      to: "src/components/features/UnidadePublicaCard.js"
      via: "prop fotoSrc"
      pattern: "fotoSrc="
    - from: "src/components/features/UnidadesPublicas.js"
      to: "src/components/features/UnidadeDetailSheet.js"
      via: "props fotoSrc + simulating"
      pattern: "fotoSrc="
---

<objective>
Evoluir os três componentes da listagem pública in-place (D-01/D-02/D-03) para cobrir PUB-01 a PUB-05: abas com contadores (já existentes) + sort control novo, cards com imagem de capa em grid responsivo, ficha bottom-sheet com imagem real + valor/m² + refs, e botão "[>] Simular Aluguel". Consome `foto_signed_url` que a RPC do Plano 24-01 agora retorna.

Purpose: entrega a fatia visível demonstrável da Phase 24 — a página pública /unidades com variante A do design.
Output: três arquivos `.js` evoluídos sem renomear nem criar componentes novos (exceto helpers puros inline ou em utils).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-p-blico-unidades-dispon-veis/24-CONTEXT.md
@.planning/phases/24-p-blico-unidades-dispon-veis/24-RESEARCH.md
@.planning/phases/24-p-blico-unidades-dispon-veis/24-UI-SPEC.md
@.planning/design/js/public.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: UnidadesPublicas.js — sort state, sortUnits, fotoSrcs map, sort control bar, grid desktop</name>
  <files>src/components/features/UnidadesPublicas.js</files>
  <read_first>
    - src/components/features/UnidadesPublicas.js (estado atual: 168L, tabs/realtime/removedIds/removingId/simularAluguel/edificioById/shortenName — NÃO alterar esses)
    - src/lib/queries-client.js §getUnidadesDisponiveis (linha 77-83 — espalha ...u; foto_signed_url e foto_url já fluem do RPC após Plano 24-01)
    - .planning/design/js/public.jsx (linhas 12-17 sortUnits; 134-143 sort bar UI; 173 grid desktop; 177 footer)
    - .planning/phases/24-p-blico-unidades-dispon-veis/24-UI-SPEC.md §State Map + §Layout Specification (estrutura flex-column da página)
    - .planning/phases/24-p-blico-unidades-dispon-veis/24-RESEARCH.md §Pitfall 3 (guard de null em sortUnits)
  </read_first>
  <action>
    Evoluir `UnidadesPublicas.js` in-place (D-01). NÃO renomear, NÃO criar arquivo novo.

    Adicionar states novos: `sort` (default `'rel'`) e `fotoSrcs` (default `{}`). Mover/adicionar `simulating` como state no PAI (default `false`) — controle unificado com `simularAluguel()` conforme UI-SPEC §Interaction Contract.

    Adicionar constante `SORTS` (array de 4 objetos `{ id, label }`): `rel`/'Relevância', `valor_asc`/'Menor valor', `valor_desc`/'Maior valor', `area_desc`/'Maior área' (D-08, copywriting do UI-SPEC).

    Adicionar função pura `sortUnits(list, sort)` portada de public.jsx:12-17 COM guard de null (Pitfall 3): copiar a lista; para `valor_asc` ordenar por `(x.valor_mensal ?? Infinity) - (y.valor_mensal ?? Infinity)`; para `valor_desc` por `(y.valor_mensal ?? -Infinity) - (x.valor_mensal ?? -Infinity)`; para `area_desc` por `(y.area_m2 ?? 0) - (x.area_m2 ?? 0)`; `rel` = retorna a lista sem ordenar (ordem original do RPC). Pode ficar inline no arquivo ou em src/lib/utils.js (Claude's Discretion).

    No `useEffect` de load, após `setUnidades(u ?? [])`, construir o mapa `fotoSrcs` de forma SÍNCRONA (sem await extra — as URLs já vêm do RPC após Plano 24-01): `Object.fromEntries((u ?? []).map(x => [x.id, x.foto_signed_url ?? x.foto_url ?? '/Detalhe_Arquitetonico.png']))` e `setFotoSrcs(...)`. Reconstruir também quando o realtime chama `load()` novamente.

    Adicionar a derivação `sorted = sortUnits(filtered, sort)` e renderizar `sorted` em vez de `filtered` na lista. Manter `filtered.length` (ou `sorted.length`, equivalentes) na contagem.

    Substituir a barra de contagem atual (linha ~114-121, que mostra count + "SYNC · data") por uma barra `flex justify-between items-center` (D-07): à esquerda a contagem `{N} {UNIDADE|UNIDADES}` (manter lógica singular/plural existente); à direita o sort control = label "Ordenar" (className r-label / fontSize 9-10 mono) + os 4 pills mapeados de `SORTS`. Pill estilo: `all:'unset', cursor:'pointer', flexShrink:0, padding:'5px 10px', font-mono 10px letterSpacing 0.5px`; ativo (`sort === s.id`): `border 1px var(--indigo)` + `background oklch(0.339 0.179 301.68 / 0.18)` + `color var(--fg-1)`; inativo: `border 1px var(--border-3)` + `transparent` + `color var(--fg-4)`. Container dos pills com `overflowX:auto` no mobile. Onclick `setSort(s.id)`. (Portar de public.jsx:134-143.)

    Substituir a lista vertical de cards (atual: `.map` direto) por um GRID responsivo (D-11): wrapper `<div style={{ maxWidth: 1100, margin: '0 auto', padding: 'var(--rd-gutter-m)' }}>` envolvendo um `<div className="grid grid-cols-1 md:grid-cols-[repeat(auto-fill,minmax(280px,1fr))] gap-3">`. Mover o footer "POWERED BY ROMMA · GRID.OS" para dentro desse wrapper de maxWidth (mantendo o `border-t`). O empty state permanece como está. Manter o container scrollável `flex-1 overflow-auto min-h-0` (D-12).

    Passar para cada `<UnidadePublicaCard>` a prop nova `fotoSrc={fotoSrcs[u.id] ?? '/Detalhe_Arquitetonico.png'}` (além das props atuais unidade/edificio/onSelect/isRemoving).

    Passar para `<UnidadeDetailSheet>` as props novas `fotoSrc={fotoSrcs[selected.id] ?? '/Detalhe_Arquitetonico.png'}` e `simulating={simulating}` (além de unidade/edificio/onClose/onSimular).

    Ajustar `simularAluguel(uid)` para setar `simulating` (true no início; false quando fechar a sheet e iniciar a animação de remoção). Manter o padrão setRemovingId → setTimeout 700ms → setRemovedIds + setSelected(null) (D-15). Manter timerRef e cleanup.

    NÃO alterar: canal realtime `public-unidades`, `shortenName`, `edificioById`, a lógica das abas e contadores (D-09/D-10), o header da página.
  </action>
  <verify>
    <automated>grep -q "sortUnits" src/components/features/UnidadesPublicas.js && grep -q "fotoSrcs" src/components/features/UnidadesPublicas.js && grep -q "minmax(280px,1fr)" src/components/features/UnidadesPublicas.js && grep -q "fotoSrc=" src/components/features/UnidadesPublicas.js && grep -q "Ordenar" src/components/features/UnidadesPublicas.js && npx next lint --file src/components/features/UnidadesPublicas.js 2>&1 | grep -vi "warning" | grep -qi "error" && echo LINT_ERROR || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Existe state `sort` (default 'rel'), `fotoSrcs` e `simulating` no componente
    - Existe `SORTS` com exatamente 4 entradas: rel, valor_asc, valor_desc, area_desc
    - `sortUnits` aplica guard `?? Infinity` para valor_asc e `?? -Infinity` para valor_desc
    - O mapa fotoSrcs é construído com fallback `?? '/Detalhe_Arquitetonico.png'` e prioriza `foto_signed_url`
    - A barra de contagem usa `flex justify-between` com label "Ordenar" + 4 pills; pill ativa usa `var(--indigo)` border + `oklch(0.339 0.179 301.68 / 0.18)` bg
    - O grid usa `grid-cols-1 md:grid-cols-[repeat(auto-fill,minmax(280px,1fr))] gap-3` dentro de wrapper `maxWidth: 1100`
    - `UnidadePublicaCard` recebe `fotoSrc` e `UnidadeDetailSheet` recebe `fotoSrc` + `simulating`
    - Canal realtime, shortenName, edificioById e abas permanecem inalterados
    - `npx next lint` no arquivo não reporta erro novo
  </acceptance_criteria>
  <done>UnidadesPublicas.js tem sort funcional, fotoSrcs resolvido do RPC, grid desktop responsivo e passa fotoSrc/simulating aos filhos, sem regredir realtime/abas.</done>
</task>

<task type="auto">
  <name>Task 2: UnidadePublicaCard.js — imagem de capa, overlay, StatusBadge absoluto, tokens tipográficos</name>
  <files>src/components/features/UnidadePublicaCard.js</files>
  <read_first>
    - src/components/features/UnidadePublicaCard.js (estado atual: 55L, layout em row, sem imagem; importa StatusBadge + fmtBRL, refOf)
    - .planning/design/js/public.jsx (linhas 21-46 PublicUnitCard — estrutura card vertical com imagem)
    - .planning/phases/24-p-blico-unidades-dispon-veis/24-UI-SPEC.md §Card Layout + §Image Contract (card image) + §Typography card-specific overrides
    - .planning/phases/24-p-blico-unidades-dispon-veis/24-RESEARCH.md §Pitfall 4 (fill precisa de container relative) + §Pitfall 6 (refOf vs REF)
  </read_first>
  <action>
    Evoluir `UnidadePublicaCard.js` in-place (D-02). Aceitar prop nova `fotoSrc` na assinatura: `({ unidade, edificio, onSelect, isRemoving, fotoSrc })`.

    Importar `Image` de `next/image`.

    Converter o card de row horizontal para card VERTICAL com imagem no topo (flex-col, padding 18px). Estrutura:

    1. Bloco de imagem no topo: container `style={{ position:'relative', height:116, overflow:'hidden', border:'1px solid var(--border-3)' }}`. Dentro:
       - `<Image fill alt="" src={fotoSrc} sizes="(min-width: 768px) 280px, 100vw" style={{ objectFit:'cover', filter:'grayscale(0.3) contrast(1.1) brightness(0.6)' }} />`
       - overlay `<div style={{ position:'absolute', inset:0, background:'var(--primary-hover)', opacity:0.12 }} />`
       - StatusBadge absoluto: `<div style={{ position:'absolute', top:10, right:10 }}><StatusBadge status={unidade.status} /></div>`
    2. Abaixo da imagem: nome da unidade com `.r-subhead` + `style={{ fontSize:17, letterSpacing:'-0.3px' }}` (override do UI-SPEC) e nome do edifício com `.r-meta` + `style={{ marginTop:4, letterSpacing:'1px' }}` (usar `shortenName`/`edificio.nome` — manter exibição do edifício atual).
    3. Footer do card (após divider `border-t border-border-2`): área à esquerda (`.r-meta`, formato `{area_m2}m²`) e valor à direita: se `valor_visivel` → `{fmtBRL(valor_mensal)}/mês` com `style={{ fontFamily:'var(--font-mono)', fontWeight:700, fontSize:13, letterSpacing:'0.5px', color:'var(--primary-hover)' }}`; se não → "Consulte o Proprietário" (`.r-meta`/font-mono caps). Manter o link "Detalhes →" OU integrar ao footer (Claude's Discretion — UI-SPEC mostra footer área|valor).

    REMOVER a StatusBadge do footer/topo antigo (agora ela vive absoluta sobre a imagem). REMOVER o `refOf(unidade)` do topo do card antigo (o card vertical do design não mostra ref no card — ref só aparece na sheet). Pode manter o import de refOf se ainda usado, senão remover o import não utilizado para não quebrar o lint.

    Manter a animação de remoção atual: `transition-opacity duration-700` + condicional `opacity-0` quando `isRemoving` (D-15). NÃO usar rUnitOut (deferred). Manter o card como botão clicável full-width (`onClick={() => onSelect(unidade)}`, `cursor:pointer`).
  </action>
  <verify>
    <automated>grep -q "fotoSrc" src/components/features/UnidadePublicaCard.js && grep -q "next/image" src/components/features/UnidadePublicaCard.js && grep -q "grayscale(0.3) contrast(1.1) brightness(0.6)" src/components/features/UnidadePublicaCard.js && grep -q "var(--primary-hover)" src/components/features/UnidadePublicaCard.js && grep -q "StatusBadge" src/components/features/UnidadePublicaCard.js && npx next lint --file src/components/features/UnidadePublicaCard.js 2>&1 | grep -vi "warning" | grep -qi "error" && echo LINT_ERROR || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - A assinatura do componente inclui `fotoSrc`
    - Importa `Image` de `next/image`
    - Tem um container `position: relative` com `height: 116` envolvendo `<Image fill>` (Pitfall 4)
    - O Image usa `filter: 'grayscale(0.3) contrast(1.1) brightness(0.6)'` e `sizes` definido
    - Overlay `var(--primary-hover)` em `opacity: 0.12` presente
    - StatusBadge posicionado absoluto `top:10 right:10` sobre a imagem
    - Nome usa `.r-subhead`, nome do edifício usa `.r-meta`
    - Valor visível usa `color: var(--primary-hover)`; valor oculto mostra "Consulte o Proprietário"
    - Animação de remoção `transition-opacity duration-700` + `opacity-0` quando isRemoving mantida
    - Sem imports não utilizados (`npx next lint` sem erro novo)
  </acceptance_criteria>
  <done>O card é vertical com imagem de capa 116px, overlay indigo, StatusBadge absoluto e tipografia em tokens, recebendo fotoSrc do pai.</done>
</task>

<task type="auto">
  <name>Task 3: UnidadeDetailSheet.js — imagem real, valor/m², refs LOC+REF, CTA [>] Simular Aluguel + simulating</name>
  <files>src/components/features/UnidadeDetailSheet.js</files>
  <read_first>
    - src/components/features/UnidadeDetailSheet.js (estado atual: 104L; imagem placeholder /Detalhe_Arquitetonico.png opacity-10; grade área/valor; botão "Tenho interesse →"; importa fmtBRL, refOf)
    - src/lib/utils.js (refOf retorna "UN-"+id[0:6]; fmtBRL formata BRL — o REF de display da sheet é "RM-2026-"+id[0:6] e é DIFERENTE de refOf, Pitfall 6)
    - .planning/design/js/public.jsx (linhas 48-82 UnitSheet — imagem, grade, valor/m², refs, CTA bracket)
    - .planning/phases/24-p-blico-unidades-dispon-veis/24-UI-SPEC.md §Sheet Layout + §Image Contract (sheet image) + §Copywriting Contract + §Animation Contract (rSheetUp)
  </read_first>
  <action>
    Evoluir `UnidadeDetailSheet.js` in-place (D-03). Aceitar props novas: `({ unidade, edificio, onClose, onSimular, fotoSrc, simulating })`.

    Imagem: substituir o `<Image src="/Detalhe_Arquitetonico.png" ... opacity-10 />` por imagem real. Container `style={{ position:'relative', height:160, marginBottom:18, overflow:'hidden', border:'1px solid var(--border-3)' }}` (D-13). Dentro: `<Image fill alt="" src={fotoSrc} sizes="520px" style={{ objectFit:'cover', filter:'grayscale(0.3) contrast(1.1) brightness(0.65)' }} />` + overlay `<div style={{ position:'absolute', inset:0, background:'var(--primary-hover)', opacity:0.16 }} />`. Sem StatusBadge na sheet.

    Após a grade 2 colunas (Área | Valor mensal), adicionar a row VALOR/M² condicional, exibida só quando `unidade.valor_visivel && unidade.area_m2 > 0` (guard contra divisão por zero — Don't Hand-Roll): container `style={{ border:'1px solid var(--border-3)', padding:'12px 16px', display:'flex', justifyContent:'space-between', alignItems:'center' }}`; à esquerda `<span className="r-label">Valor / m²</span>`; à direita `{fmtBRL(Math.round(unidade.valor_mensal / unidade.area_m2))}` seguido de `<span className="r-meta">/m²</span>`.

    Adicionar a row de REFS (D-13): container `flex justify-between`; à esquerda `<span className="r-meta">LOC: −23.561° S, 46.656° W</span>` (coordenadas estáticas do design); à direita `<span className="r-meta">REF: RM-2026-{unidade.id.slice(0,6).toUpperCase()}</span>`. Este REF é o de display público — NÃO confundir com `refOf()` do header (Pitfall 6). Manter `refOf(unidade)` no header como está.

    Botão CTA (D-14): substituir o texto "Tenho interesse →" por `simulating ? '[···] Processando' : '[>] Simular Aluguel'`. Adicionar `disabled={simulating}` ao botão. Manter estilo bracket: `bg-indigo font-mono font-bold text-[13px] text-fg-1 text-center tracking-[0.5px]`, `minHeight: 44`. onClick continua `onSimular(unidade.id)`. Trocar a fonte de font-body para font-mono no botão para casar com o estilo bracket dos forms de acesso. Atualizar/remover a nota "Demo · 'Tenho interesse'..." para refletir "Simular Aluguel" (ou remover).

    Aplicar animação de entrada `rSheetUp` ao painel interno (UI-SPEC §Animation Contract / Pitfall 5): adicionar `style={{ animation: 'rSheetUp 320ms var(--ease-crisp) both' }}` ao `<div>` painel da sheet (o que tem stopPropagation).

    NÃO alterar: o padrão backdrop click (`onClick={onClose}` no backdrop + `stopPropagation` no painel), o drag handle, o layout do header (ref + nome + edifício + botão ✕), a grade 2 colunas Área/Valor mensal existente.
  </action>
  <verify>
    <automated>grep -q "fotoSrc" src/components/features/UnidadeDetailSheet.js && grep -q "Simular Aluguel" src/components/features/UnidadeDetailSheet.js && grep -q "Processando" src/components/features/UnidadeDetailSheet.js && grep -q "Valor / m" src/components/features/UnidadeDetailSheet.js && grep -q "RM-2026-" src/components/features/UnidadeDetailSheet.js && grep -q "rSheetUp" src/components/features/UnidadeDetailSheet.js && grep -q "valor_visivel && unidade.area_m2" src/components/features/UnidadeDetailSheet.js && npx next lint --file src/components/features/UnidadeDetailSheet.js 2>&1 | grep -vi "warning" | grep -qi "error" && echo LINT_ERROR || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - A assinatura inclui `fotoSrc` e `simulating`
    - A imagem usa `src={fotoSrc}` com `filter: grayscale(0.3) contrast(1.1) brightness(0.65)` em container relative height 160 (não mais o placeholder opacity-10)
    - Overlay `var(--primary-hover)` em `opacity: 0.16`
    - A row "Valor / m²" só renderiza quando `unidade.valor_visivel && unidade.area_m2 > 0` e usa `Math.round(valor_mensal / area_m2)`
    - A row de refs mostra "LOC: ..." e "REF: RM-2026-{id[0:6] uppercase}"
    - `refOf(unidade)` permanece no header (não substituído pelo REF de display)
    - O botão CTA alterna entre "[>] Simular Aluguel" e "[···] Processando" com `disabled={simulating}`
    - O painel da sheet tem `animation: rSheetUp 320ms var(--ease-crisp) both`
    - Backdrop click, drag handle e header preservados
    - `npx next lint` sem erro novo
  </acceptance_criteria>
  <done>A sheet mostra imagem real, valor/m² guardado, refs LOC+REF, CTA bracket com loading e animação rSheetUp, sem regredir o backdrop/header.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — verificar PUB-01..PUB-05 ao vivo em /unidades (anon)</name>
  <action>Rodar npm run dev e verificar (humano) PUB-01..PUB-05 ao vivo em /unidades no contexto anon, confirmando que imagens de Storage carregam. Detalhes em <how-to-verify>.</action>
  <what-built>
    Os três componentes da listagem pública evoluídos in-place: abas+contadores (existentes) com sort control novo (4 pills), cards com imagem de capa em grid responsivo, ficha bottom-sheet com imagem real + valor/m² + refs + CTA "[>] Simular Aluguel", consumindo `foto_signed_url` do RPC (Plano 24-01).
  </what-built>
  <how-to-verify>
    1. Rodar `npm run dev` e abrir `http://localhost:3000/unidades` em uma janela anônima (contexto anon, sem sessão).
    2. PUB-01: confirmar as abas por edifício com contadores + aba "Todos". Clicar entre abas filtra os cards e os contadores batem.
    3. PUB-02: confirmar a barra com "{N} UNIDADES" à esquerda e "Ordenar" + 4 pills à direita. Clicar "Menor valor" / "Maior valor" / "Maior área" reordena os cards; "Relevância" volta à ordem original. Unidades sem valor (valor_visivel=false) vão para o fim em "Menor valor" (não embaralham).
    4. PUB-03: cards mostram imagem de capa (foto real para unidades com foto gravada; placeholder /Detalhe_Arquitetonico.png para as sem foto), área, valor ou "Consulte o Proprietário", e badge "Disponível" no canto superior direito da imagem. No desktop (largura ≥768px) os cards aparecem em grid de múltiplas colunas dentro de container centralizado (maxWidth 1100). No mobile, uma coluna.
    5. PUB-04: clicar um card abre a bottom-sheet com animação de baixo para cima; mostra imagem real, descrição, grade Área/Valor mensal, row "Valor / m²" (só quando o valor é visível), refs "LOC:" e "REF: RM-2026-...".
    6. PUB-05: clicar "[>] Simular Aluguel" → botão vira "[···] Processando" e desabilita → a sheet fecha → o card some da lista com fade → os contadores das abas decrementam.
    7. Confirmar no DevTools que as imagens de Storage carregam (sem erro 403/RLS no Network) — valida a signed URL do Plano 24-01.
  </how-to-verify>
  <resume-signal>Digite "approved" se PUB-01 a PUB-05 funcionam e as imagens carregam. Caso contrário, descreva o que falhou.</resume-signal>
</task>

</tasks>

<verification>
- /unidades em anon: abas+contadores (PUB-01), sort pills reordenando (PUB-02), cards com imagem+área+valor+status em grid responsivo (PUB-03), sheet com imagem+valor/m²+refs (PUB-04), Simular Aluguel removendo com animação (PUB-05).
- Imagens de Storage carregam via foto_signed_url sem erro RLS.
- `npx next lint` limpo nos três arquivos.
</verification>

<success_criteria>
- PUB-01..PUB-05 verificados ao vivo na página pública anon.
- Os três componentes evoluídos in-place (sem renomear, sem arquivos novos exceto helpers puros).
- Realtime, abas e backdrop preservados.
</success_criteria>

<output>
Create `.planning/phases/24-p-blico-unidades-dispon-veis/24-02-SUMMARY.md` when done.
</output>
