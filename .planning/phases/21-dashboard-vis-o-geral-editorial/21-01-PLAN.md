---
phase: 21-dashboard-vis-o-geral-editorial
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/queries-server.js
  - src/lib/fluxo.js
  - src/lib/fluxo.test.mjs
  - src/app/globals.css
  - e2e/dashboard-editorial.spec.js
autonomous: true
requirements: [DASH-05]
must_haves:
  truths:
    - "getParcelasFluxo() returns all parcelas (every status, every contract) with contrato_id, data_vencimento, data_pagamento, status"
    - "aggregateFluxo() produces a 6-month array (today-3 .. today+2) of {mes, key, recebido, previsto, peak} with recebido/previsto as 0-100 integers"
    - "The month with the highest recebido value has peak=true; when no paga parcelas exist, no month has peak=true"
    - "globals.css defines @keyframes rGrowY (scaleY 0->1) and disables .chart-bar animation under prefers-reduced-motion and print"
  artifacts:
    - path: "src/lib/fluxo.js"
      provides: "Pure cash-flow aggregation helpers (buildFluxoWindow, aggregateFluxo)"
      exports: ["buildFluxoWindow", "aggregateFluxo"]
    - path: "src/lib/queries-server.js"
      provides: "getParcelasFluxo() read query"
      contains: "export async function getParcelasFluxo"
    - path: "src/app/globals.css"
      provides: "rGrowY keyframe + chart-bar reduced-motion/print guards"
      contains: "@keyframes rGrowY"
    - path: "e2e/dashboard-editorial.spec.js"
      provides: "Wave-0 E2E scaffold for DASH-04/05/06"
  key_links:
    - from: "src/lib/fluxo.js"
      to: "parcela.data_pagamento / data_vencimento month bucket"
      via: "dateStr.slice(0,7)"
      pattern: "slice\\(0, ?7\\)"
---

<objective>
Build the data + animation + test foundation for the editorial dashboard, with zero changes to `page.js`. This plan delivers the three independent files that the page reorganization (Plan 02) consumes: the new server query `getParcelasFluxo()`, the pure aggregation module `src/lib/fluxo.js`, the `rGrowY` CSS keyframe with reduced-motion/print guards, and the Wave-0 E2E scaffold.

Purpose: Defines the contracts (query shape, `aggregateFluxo` output array shape, CSS class `chart-bar`) that Plan 02 wires into the Server Component — interface-first ordering so the page plan never has to guess signatures. Implements D-01, D-02, D-03, D-04, D-05, D-08.
Output: `src/lib/queries-server.js` (modified), `src/lib/fluxo.js` (new), `src/lib/fluxo.test.mjs` (new), `src/app/globals.css` (modified), `e2e/dashboard-editorial.spec.js` (new scaffold).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dashboard-vis-o-geral-editorial/21-RESEARCH.md
@.planning/phases/21-dashboard-vis-o-geral-editorial/21-PATTERNS.md
@.planning/phases/21-dashboard-vis-o-geral-editorial/21-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getParcelasFluxo() query (D-01)</name>
  <files>src/lib/queries-server.js</files>
  <read_first>
    - src/lib/queries-server.js (read the full file; copy the `createServer()` call pattern and `return data ?? []` null-safety from the existing `getParcelasByContratos` at line 53)
    - .planning/phases/21-dashboard-vis-o-geral-editorial/21-PATTERNS.md (section "src/lib/queries-server.js" lines 270-279 — exact target code)
  </read_first>
  <action>
    Add a new exported async function `getParcelasFluxo()` after the existing `getParcelasByContratos` function. It calls `await createServer()`, then `supabase.from('parcelas').select('id, contrato_id, data_vencimento, data_pagamento, status').order('data_vencimento', { ascending: true })`, and returns `data ?? []`. Per D-01: NO `.in('status', ...)` filter (all statuses needed — `paga` feeds recebido, all statuses feed previsto) and NO `.in('contrato_id', ...)` filter (parcelas from encerrado/cancelado contracts must contribute to historical recebido). No input parameter, no early-return guard. The `'server-only'` import and `createServer` import already exist at the top of the file — do not re-add them. Match the quote style of the neighboring functions in this file.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/queries-server.js','utf8'); const body=(s.split('getParcelasFluxo')[1]||'').split('export')[0]; if(!/export async function getParcelasFluxo\(\)/.test(s)) throw new Error('missing fn'); if(/\.in\(/.test(body)) throw new Error('must not filter with .in'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `getParcelasFluxo` is exported and async.
    - Its `.select()` lists exactly: id, contrato_id, data_vencimento, data_pagamento, status.
    - No `.in()` status or contrato_id filter inside the function body.
    - Returns `data ?? []`.
    - `npx eslint src/lib/queries-server.js` exits clean.
  </acceptance_criteria>
  <done>getParcelasFluxo() exists, selects the 4+id columns with no status/contract filter, returns a null-safe array.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create pure aggregation module src/lib/fluxo.js + unit test (D-02, D-03, D-04, D-05)</name>
  <files>src/lib/fluxo.js, src/lib/fluxo.test.mjs</files>
  <read_first>
    - .planning/phases/21-dashboard-vis-o-geral-editorial/21-PATTERNS.md (section "src/lib/fluxo.js" lines 305-372 — exact target implementation incl. SHORT_MONTHS, buildFluxoWindow, getValorParcela, bucketParcelas, normalizeFluxo, aggregateFluxo)
    - .planning/phases/21-dashboard-vis-o-geral-editorial/21-RESEARCH.md (section 2 "Cash-Flow Aggregation Algorithm" — step-by-step rationale + edge cases)
    - src/lib/utils.js (confirm the project's pure-helper export style: named exports, no 'server-only' guard)
  </read_first>
  <behavior>
    - buildFluxoWindow("2026-06-15") returns 6 entries with keys 2026-03..2026-08 and labels mar,abr,mai,jun,jul,ago.
    - buildFluxoWindow handles year rollover: "2026-02-10" yields first key 2025-11; "2026-12-31" yields last key 2027-02.
    - aggregateFluxo buckets a `paga` parcela by its data_pagamento month into recebido, deriving valor from unidade.valor_mensal via contrato.unidade_id (D-02).
    - aggregateFluxo buckets every parcela (all statuses) by data_vencimento month into previsto.
    - recebido/previsto are integers 0-100 (normalized to window max; maxVal clamped to minimum 1 to avoid divide-by-zero).
    - peak=true on the single month with the highest recebido; when no paga parcelas exist (peakRecebido===0) no month has peak=true.
    - Empty parcelas array yields 6 entries all with recebido:0, previsto:0, peak:false.
  </behavior>
  <action>
    Create `src/lib/fluxo.js` as a pure module (no imports, no 'server-only' guard) exporting `buildFluxoWindow(todayStr)` and `aggregateFluxo(parcelasFluxo, contratos, unidades, todayStr)`, plus internal non-exported helpers `getValorParcela`, `bucketParcelas`, `normalizeFluxo` and a module-level `SHORT_MONTHS` array (`jan fev mar abr mai jun jul ago set out nov dez`). Implement exactly per 21-PATTERNS.md lines 305-372. Critical: bucket month keys via `dateStr.slice(0, 7)` directly on the ISO string — never `new Date(dateStr)` (UTC off-by-one, per the anti-pattern table). Clamp `maxVal` with `Math.max(..., 1)`. peak guard is `peakRecebido > 0 && recebido[key] === peakRecebido`. Each returned entry has keys `mes`, `key`, `recebido`, `previsto`, `peak`.

    Create `src/lib/fluxo.test.mjs` — a plain Node test file using `node:test` and `node:assert/strict` (no Jest; project has none). Import the public functions from `./fluxo.js`. Cover the behaviors above with plain-object fixtures for parcelas/contratos/unidades: window length+labels, year rollover both directions, recebido bucketing by data_pagamento with valor derived from valor_mensal, previsto bucketing by data_vencimento across all statuses, normalization range 0-100, peak selection, no-peak when zero paga, and the empty-array case.
  </action>
  <verify>
    <automated>node --test src/lib/fluxo.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test src/lib/fluxo.test.mjs` reports all tests pass, 0 failures.
    - `src/lib/fluxo.js` exports `buildFluxoWindow` and `aggregateFluxo`; contains no top-level `import` and no `new Date(` in the bucketing path.
    - Month bucketing uses `.slice(0, 7)`.
    - `npx eslint src/lib/fluxo.js` exits clean.
  </acceptance_criteria>
  <done>fluxo.js produces the normalized 6-month array per spec and its Node test suite passes green.</done>
</task>

<task type="auto">
  <name>Task 3: Add rGrowY keyframe + chart-bar motion guards to globals.css (D-08) and scaffold E2E spec</name>
  <files>src/app/globals.css, e2e/dashboard-editorial.spec.js</files>
  <read_first>
    - src/app/globals.css lines 457-468 (existing v1.5 keyframes block: rGrow at line 463 uses scaleX; prefers-reduced-motion at 467 + @media print at 468 for .r-fade/.romma-page)
    - .planning/phases/21-dashboard-vis-o-geral-editorial/21-PATTERNS.md (section "src/app/globals.css" lines 406-416 — exact additions)
    - e2e/dashboard.spec.js (existing dashboard spec — copy login + waitForURL + .romma-desktop-only wait pattern, and imports of login/PROPRIETARIO from helpers/fixtures)
  </read_first>
  <action>
    In `src/app/globals.css`: (1) immediately after the existing `@keyframes rGrow` line (line 463) add `@keyframes rGrowY { from { transform: scaleY(0); } to { transform: scaleY(1); } }`. Keep `rGrow` unchanged (it powers the horizontal rBar loading bar — do not repurpose it). (2) After the existing `@media print { .r-fade, .romma-page { animation: none; } }` block (line 468) add two rules: `@media (prefers-reduced-motion: reduce) { .chart-bar { animation: none !important; } }` and `@media print { .chart-bar { animation: none !important; } }`. The `!important` is required to override the inline `animation:` style the chart bars carry (REFINO-05 contract).

    Create `e2e/dashboard-editorial.spec.js` as a Wave-0 scaffold covering DASH-04/05/06. Reuse the harness from `e2e/dashboard.spec.js`: `import { test, expect } from '@playwright/test'`, `import { login } from './helpers.js'`, `import { PROPRIETARIO } from './fixtures.js'`, viewport 1440x900, and a beforeEach that logs in and waits for `/dashboard` + `.romma-desktop-only`. Write three tests asserting the END state Plan 02/03 must reach (they WILL fail now — correct for a scaffold). Tag the describe `@editorial`:
      - DASH-04: within `.romma-desktop-only`, `getByText('Taxa de Ocupação')` is visible AND an element with text matching `/^\d+%$/` (occupancy numeral) is visible.
      - DASH-05: within `.romma-desktop-only`, the gold eyebrow `Previsão de Fluxo · 2026` is visible AND `[data-testid="cashflow-chart"]` (a hook Plan 02 will add to the desktop chart container) has exactly 6 direct child columns (assert `locator('[data-testid="cashflow-chart"] > div')` count toBe 6).
      - DASH-06: within `.romma-desktop-only`, assert quick-action `<a>` links exist with hrefs `/dashboard/unidades`, `/dashboard/locatarios`, `/dashboard/contratos`, and `/`.
    Do NOT delete or modify `e2e/dashboard.spec.js`.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/app/globals.css','utf8'); if(!/@keyframes rGrowY\s*\{[^}]*scaleY\(0\)/.test(c)) throw new Error('no rGrowY'); if(!/prefers-reduced-motion[\s\S]{0,60}\.chart-bar\s*\{\s*animation:\s*none\s*!important/.test(c)) throw new Error('no reduced-motion chart-bar'); if(!/@media print[\s\S]{0,80}\.chart-bar/.test(c)) throw new Error('no print chart-bar'); console.log('OK')" && npx playwright test e2e/dashboard-editorial.spec.js --list</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `@keyframes rGrowY` with `scaleY(0)`->`scaleY(1)`; existing `rGrow` (scaleX) is unchanged.
    - `.chart-bar { animation: none !important; }` appears under both `prefers-reduced-motion: reduce` and `@media print`.
    - `e2e/dashboard-editorial.spec.js` lists 3 tests via `--list` without import/syntax errors.
    - `e2e/dashboard.spec.js` is untouched.
  </acceptance_criteria>
  <done>rGrowY keyframe + chart-bar motion guards are in globals.css; the editorial E2E scaffold lists 3 tests (red until Plan 02/03 ship the UI).</done>
</task>

</tasks>

<verification>
- `node --test src/lib/fluxo.test.mjs` green.
- `getParcelasFluxo` present with no `.in()` filters.
- `@keyframes rGrowY` + `.chart-bar` reduced-motion/print rules present.
- `npx playwright test e2e/dashboard-editorial.spec.js --list` shows 3 tests (expected red until UI lands).
- `npx eslint src/lib/queries-server.js src/lib/fluxo.js` clean.
</verification>

<success_criteria>
- New read query, pure aggregation module (with passing unit tests), vertical-grow keyframe with motion guards, and a red E2E scaffold all exist — none of which touch `page.js`. Plan 02 can import `getParcelasFluxo`, `aggregateFluxo`, and use `rGrowY`/`chart-bar` without re-deriving any signature.
</success_criteria>

<output>
Create `.planning/phases/21-dashboard-vis-o-geral-editorial/21-01-SUMMARY.md` when done.
</output>
