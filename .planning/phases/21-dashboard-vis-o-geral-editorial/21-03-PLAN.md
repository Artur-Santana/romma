---
phase: 21-dashboard-vis-o-geral-editorial
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - src/app/dashboard/page.js
autonomous: false
requirements: [DASH-04, DASH-05]
must_haves:
  truths:
    - "Mobile dashboard shows a compact per-unit OccupancyBar below the 2x2 stats grid"
    - "Mobile dashboard shows a compact 6-bar CashFlowChart (height ~108px) in an indigo-eyebrowed panel"
    - "The existing 2x2 mobile stats, vencendo banner, contratos-recentes cards and 2x2 quick actions are preserved"
  artifacts:
    - path: "src/app/dashboard/page.js"
      provides: "Mobile editorial blocks: compact OccupancyBar + compact CashFlowChart"
      contains: "FLUXO · PREVISÃO 2026"
  key_links:
    - from: "mobile CashFlowChart"
      to: "fluxoData + CashFlowChart component"
      via: "shared fluxoData prop"
      pattern: "CashFlowChart fluxo=\\{fluxoData\\} height=\\{108\\}"
---

<objective>
Add the mobile (`md:hidden`) editorial blocks to `src/app/dashboard/page.js`: a compact per-unit OccupancyBar after the 2x2 stats grid, and a compact 6-bar CashFlowChart panel after the vencendo banner. This achieves mobile parity for the occupancy and cash-flow narrative (D-11) without omitting the chart on small screens. Reuses the `OccupancyBar`/`CashFlowChart` components and `fluxoData` produced in Plan 02.

Purpose: Completes the mobile half of DASH-04 (occupancy on mobile) and DASH-05 (cash-flow on mobile) per D-11. The desktop work and the shared components/data already exist from Plan 02 — this plan only inserts two mobile blocks.
Output: `src/app/dashboard/page.js` (modified).

Reorganize, do NOT rewrite. The existing mobile 2x2 stats (lines 328-356), vencendo banner mobile (358-372), contratos recentes mobile (374-402), and quick actions 2x2 mobile (404-428) are PRESERVED verbatim — only two new blocks are inserted between them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dashboard-vis-o-geral-editorial/21-PATTERNS.md
@.planning/phases/21-dashboard-vis-o-geral-editorial/21-UI-SPEC.md
@src/app/dashboard/page.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert compact mobile OccupancyBar + CashFlowChart blocks (D-11)</name>
  <files>src/app/dashboard/page.js</files>
  <read_first>
    - src/app/dashboard/page.js (post-Plan-02 state — the mobile section starting at the `Mobile layout` comment; the 2x2 stats grid, the vencendo banner mobile block, and the contratos-recentes-mobile block are the insertion anchors). Confirm `OccupancyBar` and `CashFlowChart` are already defined above `export default` (added in Plan 02) and `fluxoData` is in scope.
    - .planning/phases/21-dashboard-vis-o-geral-editorial/21-PATTERNS.md (lines 221-240 — mobile OccupancyBar + mobile CashFlowChart insertion JSX)
    - .planning/phases/21-dashboard-vis-o-geral-editorial/21-UI-SPEC.md ("Mobile Layout" lines 245-277 — placement order, mx-5 spacing, heights, eyebrow copy)
  </read_first>
  <action>
    Insert two new blocks inside the `md:hidden` mobile pane, reusing the already-defined `OccupancyBar`/`CashFlowChart` components and the in-scope `fluxoData`:

    (1) Mobile OccupancyBar — insert AFTER the second 2x2 stats grid (the MRR + Receita Esperada row that ends around line 356) and BEFORE the vencendo banner mobile block:
      `<div className="mx-5 mb-4"><OccupancyBar alugadas={alugadas} total={unidades.length} /></div>`
    Note: the OccupancyBar cells render at the component's default `height: 28`. The UI-SPEC calls for 20px cells on mobile. To honor that without forking the component, add an optional `cellHeight = 28` prop to `OccupancyBar` (in its definition above `export default`) and use it for the cell `height`; then call the mobile instance as `<OccupancyBar alugadas={alugadas} total={unidades.length} cellHeight={20} />`. Desktop call stays `<OccupancyBar alugadas={alugadas} total={unidades.length} />` (defaults to 28). This is a backward-compatible addition; do not change desktop behavior.

    (2) Mobile CashFlowChart — insert AFTER the vencendo banner mobile block and BEFORE the contratos-recentes-mobile block (around line 374):
      `<div className="mx-5 mb-6"><span className="eyebrow eyebrow--indigo mb-[10px]">FLUXO · PREVISÃO 2026</span><div className="bg-surface border border-border-3" style={{ padding: "16px 14px" }}><CashFlowChart fluxo={fluxoData} height={108} /></div></div>`
    Use the mobile eyebrow copy `FLUXO · PREVISÃO 2026` (indigo) per the copywriting contract — distinct from the desktop gold `Previsão de Fluxo · 2026`. Height is 108 (mobile) vs 130 (desktop).

    Do not add a `data-testid` to the mobile chart (the E2E scaffold targets the desktop chart). Preserve every existing mobile block; only these two blocks are added.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/dashboard/page.js','utf8'); const mob=s.split('Mobile layout')[1]||''; if(!mob.includes('FLUXO · PREVISÃO 2026')) throw new Error('no mobile fluxo eyebrow'); if(!/CashFlowChart fluxo=\{fluxoData\} height=\{108\}/.test(mob)) throw new Error('no mobile chart height 108'); if(!/cellHeight=\{20\}/.test(mob)) throw new Error('no mobile occupancy cellHeight 20'); if(!s.includes('cellHeight = 28')) throw new Error('OccupancyBar must accept cellHeight default 28'); if(s.includes(\"'use client'\")||s.includes('\"use client\"')) throw new Error('must stay Server Component'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Mobile pane contains a compact `<OccupancyBar ... cellHeight={20} />` after the 2x2 stats and before the vencendo banner.
    - `OccupancyBar` definition takes `cellHeight = 28` and applies it to the cell `height`; desktop call is unchanged (defaults to 28).
    - Mobile pane contains a `<CashFlowChart fluxo={fluxoData} height={108} />` in an `eyebrow--indigo` "FLUXO · PREVISÃO 2026" panel, placed after the vencendo banner and before contratos-recentes-mobile.
    - All four pre-existing mobile blocks (2x2 stats, vencendo banner, contratos cards, quick actions) still present.
    - No `'use client'`.
    - `npx eslint src/app/dashboard/page.js` clean.
  </acceptance_criteria>
  <done>Mobile shows a compact per-unit occupancy bar and a 108px 6-bar cash-flow chart; all prior mobile sections preserved; file stays a Server Component.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Mobile visual verification (375px)</name>
  <what-built>
    Mobile dashboard now shows a compact per-unit OccupancyBar below the 2x2 stats grid and a compact 6-bar CashFlowChart (height 108) in an indigo-eyebrowed "FLUXO · PREVISÃO 2026" panel below the vencendo banner. Existing mobile stats, banner, contratos cards and quick actions are preserved.
  </what-built>
  <action>Pause for the developer to visually verify the mobile editorial blocks at 375px in a browser. Run the dev server, open /dashboard in a 375px viewport, and walk through the how-to-verify steps. This is a human-verification gate — make no code changes; resume only after explicit approval.</action>
  <how-to-verify>
    1. With `npm run dev` running and logged in as proprietário, open `/dashboard` in a 375px-wide viewport (device toolbar / responsive mode).
    2. Confirm the 2x2 stats grid (Ocupação / Contratos, MRR / Receita Esperada) renders unchanged.
    3. Confirm a row of compact cells (one per unit, shorter than desktop) appears just below the stats — the occupancy bar.
    4. Confirm a panel with the indigo eyebrow "FLUXO · PREVISÃO 2026" shows a 6-bar chart at reduced height; bars grow upward on load and the peak month is gold (if any paid parcela exists).
    5. Confirm the page scrolls without horizontal overflow at 375px, and the contratos-recentes cards + 2x2 quick actions still appear below.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the mobile issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `npx eslint src/app/dashboard/page.js` clean; no `'use client'`.
- Mobile OccupancyBar (cellHeight 20) + mobile CashFlowChart (height 108, indigo eyebrow) inserted; all prior mobile blocks intact.
- 375px human visual verification passed.
- Full suite check: `npx playwright test e2e/dashboard.spec.js e2e/dashboard-editorial.spec.js` — editorial spec should now go green (DASH-04/05/06).
</verification>

<success_criteria>
- DASH-04 and DASH-05 reach mobile parity per D-11; the editorial E2E scaffold from Plan 01 passes once the full desktop+mobile reorganization is shipped.
</success_criteria>

<output>
Create `.planning/phases/21-dashboard-vis-o-geral-editorial/21-03-SUMMARY.md` when done.
</output>
