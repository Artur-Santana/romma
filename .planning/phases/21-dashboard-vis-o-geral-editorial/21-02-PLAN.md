---
phase: 21-dashboard-vis-o-geral-editorial
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/app/dashboard/page.js
autonomous: false
requirements: [DASH-04, DASH-05, DASH-06]
must_haves:
  truths:
    - "Desktop dashboard shows a hero panel with the occupancy % as a 56px numeral plus an OccupancyBar of one cell per unit (alugadas filled, disponiveis outlined)"
    - "Desktop dashboard shows a 6-bar CashFlowChart (ghost previsto behind, solid recebido in front, peak month in gold) fed by aggregateFluxo"
    - "The old 4-column metrics grid is gone; metrics 02/03/04 are stacked in the hero's right column; Ocupacao is NOT duplicated"
    - "Vencendo banner, recent-contratos table, parcelas panel and quick actions still render and navigate correctly"
    - "page.js remains a Server Component with no 'use client' boundary"
  artifacts:
    - path: "src/app/dashboard/page.js"
      provides: "Editorial Variant B desktop layout with OccupancyBar + CashFlowChart inline components"
      contains: "function CashFlowChart"
  key_links:
    - from: "src/app/dashboard/page.js"
      to: "aggregateFluxo (src/lib/fluxo.js)"
      via: "import { aggregateFluxo } from '@/lib/fluxo'"
      pattern: "aggregateFluxo\\("
    - from: "src/app/dashboard/page.js"
      to: "getParcelasFluxo (queries-server.js) in Promise.all"
      via: "Promise.all extension"
      pattern: "getParcelasFluxo"
    - from: "CashFlowChart solid bar"
      to: "rGrowY keyframe + chart-bar class"
      via: "inline animation + className"
      pattern: "rGrowY"
---

<objective>
Reorganize the DESKTOP (`.romma-desktop-only`) section of `src/app/dashboard/page.js` from the flat 4-column metrics grid into the editorial Variant B layout: a 1.55fr/1fr hero grid with an occupancy hero panel (56px numeral + per-unit OccupancyBar + CashFlowChart) on the left and stacked metrics 02/03/04 on the right. Adds the two inline render components (`OccupancyBar`, `CashFlowChart`) and wires the new `getParcelasFluxo()` query + `aggregateFluxo()` aggregation. All other desktop sections (vencendo banner, contratos recentes, parcelas, quick actions) are preserved verbatim.

Purpose: Delivers DASH-04 (occupancy hero + stacked metrics), DASH-05 (cash-flow chart), and the desktop half of DASH-06 (preserved recent-contratos table + parcelas + quick actions). Implements D-06, D-07, D-08, D-09, D-10. This is a REORGANIZATION of the existing file — read it first, preserve everything not explicitly changed.
Output: `src/app/dashboard/page.js` (modified).

Reorganize, do NOT rewrite. Preserve EXACTLY: the empty-state guard + setup wizard (lines 76-147), the error state (lines 40-46), all metric calculations (lines 48-65), the vencendo banner (189-209), the contratos-recentes table (214-258), the parcelas panel (260-293), and the quick-actions grid (296-319). The ENTIRE mobile section (lines 324-431) is untouched in THIS plan — Plan 03 handles mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dashboard-vis-o-geral-editorial/21-RESEARCH.md
@.planning/phases/21-dashboard-vis-o-geral-editorial/21-PATTERNS.md
@.planning/phases/21-dashboard-vis-o-geral-editorial/21-UI-SPEC.md
@src/app/dashboard/page.js
@src/lib/fluxo.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire new query + aggregation + inline OccupancyBar/CashFlowChart components</name>
  <files>src/app/dashboard/page.js</files>
  <read_first>
    - src/app/dashboard/page.js (full file — current imports lines 1-12, Promise.all lines 27-31, getInitials inline helper lines 16-19)
    - src/lib/fluxo.js (confirm exact export signature `aggregateFluxo(parcelasFluxo, contratos, unidades, todayStr)` and its returned entry shape {mes, key, recebido, previsto, peak})
    - src/lib/queries-server.js (confirm `getParcelasFluxo` is exported — created in Plan 01)
    - .planning/phases/21-dashboard-vis-o-geral-editorial/21-PATTERNS.md (lines 42-141 — import extension, inline component JSX, Promise.all replacement, todayStr derivation)
  </read_first>
  <action>
    (1) Extend the `@/lib/queries-server` import to add `getParcelasFluxo` to the existing named import list. Add a new import `import { aggregateFluxo } from "@/lib/fluxo"`.

    (2) Add `getParcelasFluxo()` into the existing `Promise.all` so the parallel fetch becomes `[unidades, contratos, locatarios, edificios, parcelasFluxo] = await Promise.all([getUnidades(), getContratos(), getLocatarios(), getEdificios(), getParcelasFluxo()])`. Declare `let parcelasFluxo = []` alongside the other `let` declarations at line 22. Keep `getParcelasByContratos(contratosAtivosIds)` sequential AFTER the Promise.all (it depends on `contratosAtivosIds` derived from contratos). Do NOT change the existing metric calculations (lines 48-65) — `totalPendente`/`mrr`/`vencendoContratos` still come from the existing `parcelas`/`contratos`/`unidades`.

    (3) After the metric calculations and BEFORE the `isEmpty` early-return is reached at render time (place the derivation right after line 65 / before `const isEmpty`), compute `const todayStr = new Date().toISOString().slice(0, 10)` and `const fluxoData = aggregateFluxo(parcelasFluxo, contratos, unidades, todayStr)`. (It is harmless if computed before the isEmpty return since aggregateFluxo is pure and cheap; the chart only renders in the non-empty branch.)

    (4) Add two plain JS function components ABOVE `export default async function Dashboard()` (same pattern as `getInitials`), copied verbatim from 21-PATTERNS.md lines 68-118:
      - `OccupancyBar({ alugadas, total })` — flex row gap 3, one cell per unit: `flex:1, height:28`, alugadas cells `background: var(--color-primary-hover)` no border, disponiveis cells `background: var(--surface-hi)`, `border: 1px solid var(--border-3)`.
      - `CashFlowChart({ fluxo, height = 132 })` — flex row align-items flex-end gap 10, height={height}; per column ghost bar (`position:absolute, bottom:0, width:62%, height:${f.previsto}%, background: var(--secondary), opacity:0.5`) rendered FIRST in DOM, then solid bar (`className="chart-bar"`, `position:relative, width:62%, height:${f.recebido}%`, `background: f.peak ? var(--highlight) : var(--color-primary-hover)`, `boxShadow: f.peak ? "0 0 6px 0 var(--highlight)" : "none"`, `transformOrigin:"bottom"`, `animation: rGrowY var(--dur-base) var(--ease-crisp)`, `animationDelay: ${i*60}ms`, `animationFillMode: "none"`). Month label `<span className="r-meta" style={{fontSize:9}}>{f.mes}</span>`. Use `var(--color-primary-hover)` (NOT `var(--primary-hover)`) per the anti-pattern table.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/dashboard/page.js','utf8'); for(const t of ['getParcelasFluxo','aggregateFluxo','function OccupancyBar','function CashFlowChart','rGrowY','var(--color-primary-hover)','animationFillMode']){ if(!s.includes(t)) throw new Error('missing: '+t);} if(s.includes(\"'use client'\")||s.includes('\"use client\"')) throw new Error('must stay Server Component'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `getParcelasFluxo` is part of the `Promise.all`; `parcelasFluxo` is declared and assigned.
    - `aggregateFluxo(parcelasFluxo, contratos, unidades, todayStr)` is called producing `fluxoData`.
    - `OccupancyBar` and `CashFlowChart` are plain functions above `export default`.
    - Solid bar uses `var(--color-primary-hover)` / `var(--highlight)`, `className="chart-bar"`, `animation: rGrowY ...`, `animationFillMode: "none"`; ghost bar renders before solid bar in JSX.
    - No `'use client'` anywhere.
    - `npx eslint src/app/dashboard/page.js` exits clean.
  </acceptance_criteria>
  <done>Query + aggregation wired; both inline render components defined; file is still a Server Component and lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: Replace desktop 4-col metrics grid with Variant B hero layout (D-06, D-07)</name>
  <files>src/app/dashboard/page.js</files>
  <read_first>
    - src/app/dashboard/page.js (the desktop metrics grid at lines 172-186 to be replaced; the header at 158-170 and vencendo banner at 188-209 stay as anchors)
    - .planning/phases/21-dashboard-vis-o-geral-editorial/21-PATTERNS.md (lines 164-218 — production-adapted hero grid JSX)
    - .planning/phases/21-dashboard-vis-o-geral-editorial/21-UI-SPEC.md ("Desktop Layout — Variant B" lines 197-241, "Color" + "Typography" tables, copywriting strings)
    - src/app/globals.css lines 419-433 (confirm `.r-metric`, `.r-data`, `.r-eyebrow`, `.r-eyebrow.gold`, `.r-panel` exist; note `.eyebrow--gold` does NOT exist)
  </read_first>
  <action>
    Replace ONLY the desktop metrics-grid block (current lines 172-186, the `<div style={{...repeat(4,1fr)}} className="border border-border-3 mb-12">` that maps over `metricas`) with the Variant B hero grid from 21-PATTERNS.md lines 166-218. Leave the header (158-170) and everything from the vencendo banner (188) downward unchanged.

    The hero grid is `<div style={{display:"grid", gridTemplateColumns:"1.55fr 1fr", gap:"var(--rd-block-sm)"}} className="mb-12">` containing:
      - LEFT hero panel: `<div className="bg-surface border border-border-3" style={{padding:"var(--rd-panel)"}}>` with:
        - top row (`flex justify-between items-start mb-[18px]`): left = eyebrow `Taxa de Ocupação` + a baseline-aligned row (`display:flex, alignItems:"baseline", gap:12`) of `<span className="r-metric" style={{fontSize:56}}>{pctOcupacao}%</span>` and `<span className="r-data" style={{color:"var(--fg-4)"}}>{alugadas}/{unidades.length} unidades</span>`; right = eyebrow `Disponíveis` + `<span className="r-metric" style={{fontSize:34, color:"var(--success)"}}>{disponiveis}</span>`.
        - `<OccupancyBar alugadas={alugadas} total={unidades.length} />`
        - divider `<div style={{height:1, background:"var(--border-3)", margin:"20px 0 18px"}} />`
        - cash-flow eyebrow row: `<span className="r-eyebrow gold">Previsão de Fluxo · 2026</span>` (use `r-eyebrow gold` — `.eyebrow--gold` does not exist in globals.css).
        - `<div data-testid="cashflow-chart"><CashFlowChart fluxo={fluxoData} height={130} /></div>` — wrap in a div carrying `data-testid="cashflow-chart"` (the E2E scaffold from Plan 01 asserts 6 child columns under this hook). Note: CashFlowChart's own root is the flex row, so its 6 column divs are the testid element's grandchildren — instead put `data-testid="cashflow-chart"` directly on CashFlowChart's outer flex `<div>` by passing it through, OR set the wrapper testid on the immediate parent and have the scaffold assert `[data-testid="cashflow-chart"] [style*="flex-direction: column"]` count 6. Simplest: add an optional `testId` prop to CashFlowChart and spread `data-testid={testId}` onto its root flex div, then call `<CashFlowChart fluxo={fluxoData} height={130} testId="cashflow-chart" />`. Its 6 column divs become direct children, matching the scaffold's `> div` count of 6.
      - RIGHT stacked column: `<div style={{display:"flex", flexDirection:"column", gap:"var(--rd-block-sm)"}}>` with the three metric cells per 21-PATTERNS.md lines 191-217: a 2-row bordered grid holding MetricCell 02 (MRR, sub `{ativos} contrato(s) ativo(s)`) and MetricCell 03 (Receita Esperada = `fmtBRL(totalPendente)`, sub `{parcelas.length} parcela(s) em aberto`), then a separate warning box MetricCell 04 (Vencendo em 7 dias = `vencendoContratos.length`, `bg-warning-bg`, warning-colored text). Reuse existing values `mrr`, `ativos`, `totalPendente`, `parcelas.length`, `vencendoContratos.length` (D-07 — no recalculation).

    Per Pitfall 4: Metric 01 (Ocupação) now lives ONLY as the 56px hero numeral — it must NOT also appear in the stacked right column or any leftover 4-col grid. You may keep the `metricas` array definition (lines 69-74) since the empty-state wizard at lines 87-95 still maps over it; only the NON-empty desktop render path stops using it for the main grid.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/dashboard/page.js','utf8'); const desk=s.split('Mobile layout')[0]; if(!s.includes('1.55fr 1fr')) throw new Error('no hero grid'); if(!s.includes('Taxa de Ocupação')) throw new Error('no occupancy eyebrow'); if(!s.includes('Previsão de Fluxo · 2026')) throw new Error('no fluxo eyebrow'); if(!s.includes('r-eyebrow gold')) throw new Error('must use r-eyebrow gold'); if(s.includes('eyebrow--gold')) throw new Error('eyebrow--gold class does not exist'); if(!s.includes('data-testid=\"cashflow-chart\"')||!s.includes('testId')) throw new Error('no cashflow testid'); if(/gridTemplateColumns:\s*\"repeat\(4, ?1fr\)\"[\s\S]*?metricas\.map/.test(desk)) throw new Error('old 4-col metrics grid still in desktop render'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Desktop render contains the `1.55fr 1fr` hero grid; the old `repeat(4,1fr)` + `metricas.map` metrics grid no longer appears in the non-empty desktop render path.
    - Copy strings present exactly: `Taxa de Ocupação`, `Disponíveis`, `Previsão de Fluxo · 2026`.
    - Cash-flow eyebrow uses class `r-eyebrow gold`; no `eyebrow--gold` anywhere.
    - 56px occupancy numeral via `.r-metric` + `fontSize:56`; disponiveis numeral colored `var(--success)`.
    - CashFlowChart rendered with `height={130}` and `testId="cashflow-chart"` so the chart root carries `data-testid="cashflow-chart"` with 6 direct child columns.
    - Stacked right column has MetricCell 02 (MRR), 03 (Receita Esperada), 04 (Vencendo, warning bg); Ocupação appears only as the hero numeral.
    - `npx eslint src/app/dashboard/page.js` clean.
  </acceptance_criteria>
  <done>Desktop hero (occupancy numeral + OccupancyBar + CashFlowChart) and stacked metrics replace the flat 4-col grid; Ocupação is not duplicated; all preserved sections still render.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of desktop editorial dashboard</name>
  <what-built>
    Desktop dashboard reorganized into Variant B: occupancy hero panel (56px % numeral + per-unit OccupancyBar) with the 6-bar CashFlowChart below a divider, and stacked metrics 02/03/04 on the right. Cash-flow chart shows ghost previsto bars behind solid recebido bars, with the peak month in gold.
  </what-built>
  <action>Pause for the developer to visually verify the desktop editorial layout in a browser. Run the dev server, open /dashboard at desktop width, and walk through the how-to-verify steps. This is a human-verification gate — make no code changes; resume only after explicit approval.</action>
  <how-to-verify>
    1. Run `npm run dev` and log in as the proprietário; open `/dashboard` on a desktop-width viewport (>=1024px).
    2. Confirm the hero panel shows the occupancy % as a large (~56px) numeral with `{alugadas}/{total} unidades` beside it, and a row of small cells (one per unit) — filled cells for alugadas, outlined cells for disponíveis.
    3. Confirm the "Disponíveis" count on the right of the hero panel is green.
    4. Confirm the cash-flow chart shows 6 columns with month labels (jan…dez abbreviations), ghost (faint) bars behind solid bars, and exactly one solid bar in gold with a soft glow (the peak month) — assuming there is at least one paid parcela in the data.
    5. Confirm the bars animate upward (grow from bottom) on load, not a left-to-right wipe.
    6. Confirm the right column shows MRR, Receita Esperada, and a warning-tinted "Vencendo em 7 dias" cell — and that Ocupação is NOT shown twice.
    7. Confirm the vencendo banner (if any contracts are expiring), the Contratos Recentes table, the Parcelas panel, and the 4 quick-action links all still render and the quick actions navigate to /dashboard/unidades, /dashboard/locatarios, /dashboard/contratos, and /.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the visual issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `npx eslint src/app/dashboard/page.js` clean; no `'use client'`.
- Hero grid `1.55fr 1fr` present; old 4-col metrics grid gone from non-empty desktop path.
- OccupancyBar + CashFlowChart inline components defined; chart uses rGrowY + chart-bar + animationFillMode:none.
- Human visual verification passed.
</verification>

<success_criteria>
- DASH-04 desktop (occupancy hero + per-unit bar + stacked metrics) and DASH-05 desktop (6-bar cash-flow chart with gold peak) are visible; DASH-06 desktop sections preserved and navigating correctly; page is still a Server Component.
</success_criteria>

<output>
Create `.planning/phases/21-dashboard-vis-o-geral-editorial/21-02-SUMMARY.md` when done.
</output>
