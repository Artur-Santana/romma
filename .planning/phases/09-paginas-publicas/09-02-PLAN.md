---
phase: 09-paginas-publicas
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/app/page.js
  - src/components/ui/Header.js
autonomous: true
requirements: [LP-01, LP-02, LP-03]
must_haves:
  truths:
    - "Visitante clica em 'ACESSAR DASHBOARD' na hero e chega em /login (D-01, LP-02)"
    - "Visitante clica em 'VER UNIDADES' na hero e chega em /unidades (D-02, LP-01)"
    - "Visitante clica em 'ACESSAR PAINEL' (SISTEMA.04) e chega em /login (D-04, LP-03)"
    - "Os dois CTAs 'COMEÇAR AGORA' do Header (desktop + mobile) navegam para /login (D-07, LP-03)"
    - "Nenhum botão ativo da landing/Header retorna 404"
    - "ACESSAR DASHBOARD tem classe de gradiente (CTA primário) e VER UNIDADES não tem gradiente (CTA secundário) — hierarquia visual correta (D-03)"
  artifacts:
    - path: "src/app/page.js"
      provides: "Hero CTAs e botão SISTEMA.04 como <Link> com href correto"
      contains: "href=\"/login\""
    - path: "src/components/ui/Header.js"
      provides: "CTAs COMEÇAR AGORA como <Link href=/login> (desktop + mobile)"
      contains: "COMEÇAR AGORA"
  key_links:
    - from: "src/app/page.js"
      to: "/login"
      via: "<Link href=/login> ACESSAR DASHBOARD (gradiente, CTA primário)"
      pattern: "href=\"/login\""
    - from: "src/app/page.js"
      to: "/unidades"
      via: "<Link href=/unidades> VER UNIDADES (CTA secundário)"
      pattern: "href=\"/unidades\""
    - from: "src/components/ui/Header.js"
      to: "/login"
      via: "<Link href=/login> COMEÇAR AGORA (2 instâncias)"
      pattern: "href=\"/login\""
---

<objective>
Converter os CTAs de navegação mortos da landing page e do Header em `<Link>` do Next.js com destinos corretos, e ajustar os labels conforme D-01/D-02/D-04/D-07. Fecha LP-01, LP-02 e LP-03.

Purpose: hoje a hero tem um `<button>` sem href ("INICIE GRATUITAMENTE"), o label do CTA secundário está errado ("VER PROJETOS"), o botão SISTEMA.04 tem typo e é morto ("ACESSE ANALITYCS"), e o Header tem dois "COMEÇAR AGORA" mortos. O avaliador que clica espera navegar — botões mortos equivalem a destino quebrado.
Output: page.js e Header.js com todos os CTAs ativos apontando para destinos reais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-paginas-publicas/09-CONTEXT.md
@.planning/phases/09-paginas-publicas/09-UI-SPEC.md
@.planning/phases/09-paginas-publicas/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.js — converter hero CTAs e botão SISTEMA.04 (D-01, D-02, D-04)</name>
  <files>src/app/page.js</files>
  <read_first>
    - src/app/page.js (estado atual; `Link` já importado na linha 1 — NÃO adicionar import duplicado)
    - .planning/phases/09-paginas-publicas/09-PATTERNS.md (seção page.js tem os className verbatim ANTES/DEPOIS de cada CTA)
    - .planning/phases/09-paginas-publicas/09-UI-SPEC.md (D-01/D-02/D-04 — copy e estilos a preservar)
  </read_first>
  <action>
    Três edições em src/app/page.js, preservando todos os className existentes:
    - D-01 (linha ~44-46): trocar `<button type="button">INICIE GRATUITAMENTE</button>` por `<Link href="/login">ACESSAR DASHBOARD</Link>`. Manter `py-4 px-10 bg-linear-45 from-primary to-primary-hover cursor-pointer` e adicionar `text-center` (necessário pois `<a>` é inline, diferente de `<button>`). Remover `type="button"`.
    - D-02 (linha ~47-49): manter o `<Link href="/unidades">` existente; trocar APENAS o label `VER PROJETOS` → `VER UNIDADES`. href e className intactos.
    - D-04 (linha ~146-148): trocar `<button type="button">ACESSE ANALITYCS</button>` por `<Link href="/login">ACESSAR PAINEL</Link>`. Manter `py-4 px-10 bg-linear-45 from-primary to-primary-hover font-headline-hanken font-semibold tracking-[0.2em] text-white text-sm cursor-pointer`, adicionar `text-center`. Corrige o typo "ANALITYCS".
    Não tocar em nenhum outro elemento da página. Hierarquia (D-03) já fica correta: "ACESSAR DASHBOARD" gradiente = primário, "VER UNIDADES" bg-background = secundário.
  </action>
  <verify>
    <automated>grep -v '^\s*//' src/app/page.js | grep -E "ACESSAR DASHBOARD|VER UNIDADES|ACESSAR PAINEL" && ! grep -qE "INICIE GRATUITAMENTE|VER PROJETOS|ACESSE ANALITYCS" src/app/page.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'href="/login"' src/app/page.js` retorna >= 2 (D-01 hero + D-04 SISTEMA.04)
    - `src/app/page.js` contém os textos exatos `ACESSAR DASHBOARD`, `VER UNIDADES`, `ACESSAR PAINEL`
    - `src/app/page.js` NÃO contém `INICIE GRATUITAMENTE`, `VER PROJETOS`, nem `ACESSE ANALITYCS`
    - Nenhuma linha nova de `import` de `Link` foi adicionada (já existe na linha 1)
    - Os três elementos convertidos são `<Link>` (não `<button>`) e mantêm os className de gradiente/estilo originais
  </acceptance_criteria>
  <done>Hero e SISTEMA.04 com CTAs ativos: LP-01, LP-02 e parte de LP-03 satisfeitos.</done>
</task>

<task type="auto">
  <name>Task 2: Header.js — converter os dois "COMEÇAR AGORA" em Link (D-07)</name>
  <files>src/components/ui/Header.js</files>
  <read_first>
    - src/components/ui/Header.js (estado atual; `Link` já importado na linha 1; dois `<button type="button">COMEÇAR AGORA</button>` — desktop linha ~48, mobile linha ~96)
    - .planning/phases/09-paginas-publicas/09-PATTERNS.md (seção Header.js — className verbatim ANTES/DEPOIS, desktop e mobile)
    - .planning/phases/09-paginas-publicas/09-UI-SPEC.md (D-07 — manter label, corrigir href)
  </read_first>
  <action>
    Duas edições em src/components/ui/Header.js, mantendo o label `COMEÇAR AGORA` inalterado:
    - Desktop (linha ~47-51): trocar `<button type="button" className="content-center text-white font-semibold tracking-4 bg-linear-45 from-primary to-primary-hover py-4 px-10 cursor-pointer">` por `<Link href="/login">` com o mesmo className. Remover `type="button"`.
    - Mobile (linha ~95-99): trocar `<button type="button" className="w-full text-white font-semibold tracking-4 bg-linear-45 from-primary to-primary-hover py-4 px-10 cursor-pointer">` por `<Link href="/login">` com o mesmo className + adicionar `text-center`.
    NÃO tocar nos links de nav `href="#"` (CONTRATOS, PORTAIS, DASHBOARD) — auditados na UI-SPEC como placeholders aceitáveis (permanecem na página, sem 404). NÃO adicionar import de Link.
  </action>
  <verify>
    <automated>test "$(grep -c '>COMEÇAR AGORA<\|COMEÇAR AGORA' src/components/ui/Header.js)" -ge 2 && ! grep -B2 'COMEÇAR AGORA' src/components/ui/Header.js | grep -q 'type="button"' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - As duas instâncias de "COMEÇAR AGORA" em `src/components/ui/Header.js` são `<Link href="/login">` (não `<button>`)
    - `grep -c 'COMEÇAR AGORA' src/components/ui/Header.js` retorna 2 (label inalterado)
    - Os links de nav `href="#"` (CONTRATOS, PORTAIS, DASHBOARD) permanecem inalterados
    - Nenhum `import` de Link adicionado (já existe na linha 1)
    - Os className de gradiente (`bg-linear-45 from-primary to-primary-hover py-4 px-10`) foram preservados em ambas as instâncias
  </acceptance_criteria>
  <done>Header CTAs ativos; LP-03 completo (todos os botões ativos têm destino real).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitante (não autenticado) → /login | `<Link href="/login">` é navegação client-side padrão; proxy.js já protege /dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Spoofing | <Link href="/login"> | accept | Navegação para tela de login pública; sem exposição de dados; auth real ocorre em /login |
| T-09-04 | Information Disclosure | landing page estática | accept | Conteúdo de marketing público; nenhum dado sensível renderizado |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote instalado nesta fase — sem superfície de supply-chain |
</threat_model>

<verification>
- `npx playwright test e2e/public-pages.spec.js -g "LP-01|LP-02|LP-03"` passa (estava RED antes deste plano)
- `npm run build` ou `next lint` não acusa erro de JSX nos arquivos editados
</verification>

<success_criteria>
- page.js: 3 CTAs convertidos/renomeados conforme D-01/D-02/D-04
- Header.js: 2 CTAs "COMEÇAR AGORA" convertidos conforme D-07
- LP-01, LP-02, LP-03 verdes na suite E2E
</success_criteria>

<output>
Create `.planning/phases/09-paginas-publicas/09-02-SUMMARY.md` when done
</output>
