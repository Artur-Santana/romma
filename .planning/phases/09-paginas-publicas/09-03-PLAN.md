---
phase: 09-paginas-publicas
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/features/UnidadePublicaCard.js
  - src/components/features/UnidadesPublicas.js
  - src/components/features/UnidadeDetailSheet.js
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03]
must_haves:
  truths:
    - "Card de unidade com valor não visível mostra 'Consulte o Proprietário' (D-05, PUB-01)"
    - "Card mostra nome, edifício, área m², preço/'Consulte o Proprietário' e badge 'Disponível' (PUB-01)"
    - "/unidades sem unidades disponíveis exibe empty state informativo (PUB-02)"
    - "Tab buttons têm tap target >= 44px em 375px (D-06, PUB-03)"
    - "Link '← Voltar' tem tap target >= 44px (PUB-03)"
    - "Botão ✕ e botões de ação do UnidadeDetailSheet têm tap target >= 44px (PUB-03, critério #6)"
    - "/unidades em 375px não tem overflow horizontal (PUB-03)"
  artifacts:
    - path: "src/components/features/UnidadePublicaCard.js"
      provides: "Texto de fallback 'Consulte o Proprietário'"
      contains: "Consulte o Proprietário"
    - path: "src/components/features/UnidadesPublicas.js"
      provides: "Tab buttons e link Voltar com tap target >= 44px"
      contains: "min-h-[44px]"
    - path: "src/components/features/UnidadeDetailSheet.js"
      provides: "Botão ✕ 44x44 e botões de ação com min-h-[44px]"
      contains: "min-h-[44px]"
  key_links:
    - from: "src/components/features/UnidadesPublicas.js"
      to: "filtered.length === 0 empty state"
      via: "render condicional empty state (PUB-02, já existente — preservar)"
      pattern: "Nenhuma unidade dispon"
    - from: "src/components/features/UnidadePublicaCard.js"
      to: "valor_visivel ? fmtBRL : 'Consulte o Proprietário'"
      via: "fallback de preço (D-05)"
      pattern: "Consulte o Proprietário"
---

<objective>
Aplicar as correções de copy e tap target às páginas públicas: trocar o fallback de preço do card (D-05), garantir tap targets ≥44px nos tab buttons (D-06), no link "← Voltar", e nos elementos do `UnidadeDetailSheet` (✕ e botões de ação). Verificar que o empty state (PUB-02) renderiza. Fecha PUB-01, PUB-02 e PUB-03.

Purpose: o card mostra "Valor sob consulta" (terminologia divergente do CLAUDE.md); os tab buttons (~30px) e o link Voltar (~15px) falham o mínimo de 44px; e o `UnidadeDetailSheet` — interação primária de /unidades (tap no card abre o sheet) — tem botão ✕ de 32×32px e botões de ação ~41px, violando o critério #6 ("todos os tap targets ≥44px"). Sem o fix do sheet, a suite PUB-03 ficaria verde sobre uma falha real.

Nota de cascade (load-bearing para PUB-03): tab buttons e botões do sheet usam `style={{ all: 'unset' }}` inline. Onde houver dúvida se `min-h-[44px]` (Tailwind) vence o `all: unset`, garantir a altura também via `minHeight: 44` no objeto `style` inline — caminho garantido independente da cascata. Verificar com o assert de altura do plano 01.
Output: 3 componentes de /unidades com copy e tap targets em conformidade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-paginas-publicas/09-CONTEXT.md
@.planning/phases/09-paginas-publicas/09-UI-SPEC.md
@.planning/phases/09-paginas-publicas/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: UnidadePublicaCard.js — fallback "Consulte o Proprietário" (D-05) + verificar campos PUB-01</name>
  <files>src/components/features/UnidadePublicaCard.js</files>
  <read_first>
    - src/components/features/UnidadePublicaCard.js (linha ~45 texto "Valor sob consulta"; card já exibe nome, edifício, área m², StatusBadge "Disponível")
    - .planning/phases/09-paginas-publicas/09-PATTERNS.md (seção UnidadePublicaCard.js — ANTES/DEPOIS exato; className a preservar)
    - .planning/phases/09-paginas-publicas/09-UI-SPEC.md (D-05; copy contract)
  </read_first>
  <action>
    Uma edição em src/components/features/UnidadePublicaCard.js:
    - D-05 (linha ~44-46): trocar o literal `Valor sob consulta` por `Consulte o Proprietário` dentro do `<span className="font-mono text-[11px] text-fg-3 tracking-[1px] uppercase">`. className intacto, apenas a string muda.
    Verificar (não modificar) que o card já renderiza os 5 campos PUB-01: nome da unidade, nome do edifício, área m², valor (ou o fallback), e o `StatusBadge` "Disponível". O card button usa `py-5` (~68px) — já atende ≥44px, NÃO modificar. NÃO tocar em UnidadeDetailSheet.js nesta task (seu texto já está correto — Task 3 cuida só dos tap targets do sheet).
  </action>
  <verify>
    <automated>grep -q "Consulte o Proprietário" src/components/features/UnidadePublicaCard.js && ! grep -q "Valor sob consulta" src/components/features/UnidadePublicaCard.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/features/UnidadePublicaCard.js` contém `Consulte o Proprietário` e NÃO contém `Valor sob consulta`
    - O className do span (`font-mono text-[11px] text-fg-3 tracking-[1px] uppercase`) permanece inalterado
    - O card button mantém `py-5` (não foi reduzido)
    - StatusBadge continua importado e renderizado (badge "Disponível" presente)
  </acceptance_criteria>
  <done>Fallback de preço alinhado à terminologia; PUB-01 completo (5 campos presentes).</done>
</task>

<task type="auto">
  <name>Task 2: UnidadesPublicas.js — tap targets dos tab buttons e link Voltar (D-06) + verificar empty state</name>
  <files>src/components/features/UnidadesPublicas.js</files>
  <read_first>
    - src/components/features/UnidadesPublicas.js (tab button linha ~100 com `px-3.5 py-2` e `style={{ all: 'unset', cursor: 'pointer', flexShrink: 0, boxSizing: 'border-box' }}`; link Voltar linha ~82 sem padding vertical; empty state linhas ~124-135)
    - .planning/phases/09-paginas-publicas/09-PATTERNS.md (seção UnidadesPublicas.js — className verbatim ANTES/DEPOIS)
    - .planning/phases/09-paginas-publicas/09-UI-SPEC.md (D-06 + fix adicional do link Voltar; matemática de tap target)
  </read_first>
  <action>
    Duas edições em src/components/features/UnidadesPublicas.js:
    - D-06 (tab button, linha ~100): no className, trocar `py-2` por `py-3 min-h-[44px]`. AMBAS as classes são obrigatórias — `py-3` sozinho dá ~38-40px, abaixo de 44px. Resultado: `px-3.5 py-3 min-h-[44px] inline-flex gap-2 font-body font-bold text-[10px] uppercase tracking-[0.5px] items-center border [...]`. Preservar o branch condicional isActive. ATENÇÃO À CASCATA: o tab button carrega `style={{ all: 'unset', ... }}` inline; como as classes de layout existentes (`inline-flex`, `gap-2`) já se aplicam hoje, `min-h-[44px]` deve aplicar igualmente. Se o assert de altura do plano 01 (PUB-03 tab) ainda falhar abaixo de 44px, adicionar `minHeight: 44` ao objeto `style` inline (caminho garantido independente da cascata) — preferir a classe Tailwind primeiro.
    - Link Voltar (linha ~82): adicionar `py-3 inline-flex items-center min-h-[44px]` ao className existente (`font-mono text-[11px] text-fg-4 tracking-[1px] uppercase hover:text-fg-2 transition-colors`). Manter href="/" e o texto "← Voltar".
    Verificar (NÃO modificar) que o empty state (linhas ~124-135) renderiza "Nenhuma unidade disponível" + corpo quando `filtered.length === 0` — PUB-02 já implementado. Confirmar que o tab row mantém `overflow-x-auto` para evitar overflow horizontal em 375px.
  </action>
  <verify>
    <automated>test "$(grep -c 'min-h-\[44px\]' src/components/features/UnidadesPublicas.js)" -ge 2 && grep -q "py-3" src/components/features/UnidadesPublicas.js && grep -q "Nenhuma unidade dispon" src/components/features/UnidadesPublicas.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'min-h-\[44px\]' src/components/features/UnidadesPublicas.js` retorna >= 2 (tab button + link Voltar)
    - O tab button className contém AMBOS `py-3` e `min-h-[44px]` (não apenas `py-3`)
    - O link Voltar contém `inline-flex items-center` além de `py-3 min-h-[44px]` (necessário para a altura aplicar a um `<a>` inline)
    - Se o teste PUB-03 do tab button (plano 01) reportar altura < 44px, o `style` inline do tab button contém `minHeight: 44` como garantia
    - O empty state ("Nenhuma unidade disponível") permanece presente e inalterado
  </acceptance_criteria>
  <done>Tab buttons e Voltar ≥44px (medidos, não só calculados); empty state verificado; PUB-02 confirmado.</done>
</task>

<task type="auto">
  <name>Task 3: UnidadeDetailSheet.js — tap targets do ✕ e botões de ação (PUB-03, critério #6)</name>
  <files>src/components/features/UnidadeDetailSheet.js</files>
  <read_first>
    - src/components/features/UnidadeDetailSheet.js (botão ✕ linha ~31 `width: 32, height: 32`; botões "Tenho interesse →" linha ~84 e "Fechar" linha ~91 com `py-[14px]` ≈ 41px)
    - .planning/phases/09-paginas-publicas/09-RESEARCH.md (Pitfall 3 + Assumption A2: dimensões atuais e fix recomendado)
    - .planning/phases/09-paginas-publicas/09-UI-SPEC.md (critério #6 — "todos os tap targets ≥44px")
  </read_first>
  <action>
    Três edições mínimas em src/components/features/UnidadeDetailSheet.js, preservando layout e estilo visual:
    - Botão ✕ (linha ~31): no objeto `style`, trocar `width: 32, height: 32` por `width: 44, height: 44`. Manter `all: 'unset', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0` e o className `border border-border-3 text-fg-3 text-[14px]`.
    - "Tenho interesse →" (linha ~84): adicionar `min-h-[44px]` ao className (atual `py-[14px] px-5 bg-indigo font-body font-bold text-[13px] text-fg-1 text-center tracking-[0.5px]`). Como o button usa `style={{ all: 'unset', display: 'block' }}`, garantir altura mínima via classe Tailwind `min-h-[44px]` (e, se `min-height` não aplicar por causa de `display: block`, adicionar `minHeight: 44` ao objeto style). Preferir a classe Tailwind; só usar o style inline se o teste de altura falhar.
    - "Fechar" (linha ~91): mesma adição de `min-h-[44px]` ao className.
    NÃO alterar o texto "Consulte o Proprietário" (linha ~59) — já correto. NÃO mudar a estrutura do overlay/sheet.
  </action>
  <verify>
    <automated>grep -q "width: 44, height: 44\|width: 44,\s*height: 44" src/components/features/UnidadeDetailSheet.js && test "$(grep -c 'min-h-\[44px\]\|min-height: 44\|minHeight: 44' src/components/features/UnidadeDetailSheet.js)" -ge 2 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - O botão ✕ em `src/components/features/UnidadeDetailSheet.js` tem `width: 44` e `height: 44` (não mais 32)
    - Os dois botões de ação ("Tenho interesse →", "Fechar") têm garantia de altura mínima 44px (via `min-h-[44px]` ou `minHeight: 44`)
    - O texto `Consulte o Proprietário` (linha ~59) permanece inalterado
    - A estrutura do overlay (`onClick={onClose}`, `stopPropagation`) permanece intacta
    - `npx playwright test e2e/public-pages.spec.js -g "PUB-03"` passa, incluindo o assert do sheet ✕ (height E width >= 44)
  </acceptance_criteria>
  <done>Todos os tap targets do sheet ≥44px; critério #6 satisfeito de fato (não apenas no gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase RLS → /unidades (público) | Dados de unidades vêm via anon key; RLS restringe a `status='disponivel'`. Esta fase não muda queries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Information Disclosure | UnidadePublicaCard / DetailSheet | accept | Renderiza só campos públicos (`valor_visivel` controla exibição do preço); RLS já filtra dados sensíveis |
| T-09-06 | Tampering | edições de className/string | accept | Mudanças puramente apresentacionais; nenhuma lógica de dados/auth alterada |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote instalado nesta fase — sem superfície de supply-chain |
</threat_model>

<verification>
- `npx playwright test e2e/public-pages.spec.js -g "PUB-01|PUB-02|PUB-03"` passa (estavam RED nos tap targets antes deste plano)
- Inspeção em 375px (Plano 04 audit) confirma sem overflow horizontal e todos os tap targets ≥44px
</verification>

<success_criteria>
- D-05 aplicado (card "Consulte o Proprietário")
- Tab buttons + link Voltar + sheet (✕ e 2 botões) todos ≥44px (medidos pelo teste, com fallback minHeight onde a cascata exigir)
- Empty state (PUB-02) verificado presente
- PUB-01, PUB-02, PUB-03 verdes na suite E2E
</success_criteria>

<output>
Create `.planning/phases/09-paginas-publicas/09-03-SUMMARY.md` when done
</output>
