---
phase: 09-paginas-publicas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/public-pages.spec.js
autonomous: true
requirements: [LP-01, LP-02, LP-03, PUB-01, PUB-02, PUB-03]
must_haves:
  truths:
    - "Suite e2e/public-pages.spec.js existe e cobre os 6 requisitos LP/PUB"
    - "Os testes de copy/href (LP-01, LP-02, D-04/LP-03 (ACESSAR PAINEL), D-05) estão RED antes da implementação"
    - "Os testes de tap target (PUB-03) e empty state (PUB-02) executam sem erro de framework"
  artifacts:
    - path: "e2e/public-pages.spec.js"
      provides: "Smoke + viewport + mock E2E para landing page e /unidades"
      contains: "test.describe"
      min_lines: 90
  key_links:
    - from: "e2e/public-pages.spec.js"
      to: "page.route('**/rest/v1/unidades**')"
      via: "Supabase REST intercept para empty state PUB-02 e card valor_visivel=false PUB-01"
      pattern: "page\\.route.*unidades"
    - from: "e2e/public-pages.spec.js"
      to: "getBoundingClientRect().height"
      via: "assert tap target >= 44px (tab buttons, Voltar, sheet ✕)"
      pattern: "getBoundingClientRect"
---

<objective>
Plano 01 (Wave 1): criar a suite E2E `e2e/public-pages.spec.js` com cenários RED para os 6 requisitos da fase (LP-01, LP-02, LP-03, PUB-01, PUB-02, PUB-03), antes de qualquer alteração de código-fonte. Os testes devem falhar agora (copy/href ainda errados, tap targets abaixo de 44px) e passar após os planos 02 e 03.

Purpose: a Regra de Nyquist exige um `<automated>` real por requisito. Esta fase não tem teste prévio para páginas públicas — sem esta suite (Plano 01, Wave 1), os gates de PUB-03/LP-* não teriam verificação automatizada.

Nota de seed (verificado 2026-06-06): `e2e/seed.mjs` tem exatamente uma unidade `disponivel` ("E2E-Sala Disponivel") com `valor_visivel: true`. Portanto a asserção "Consulte o Proprietário" (PUB-01) NÃO pode ser satisfeita pelo seed — deve usar `page.route` para mockar uma unidade disponível com `valor_visivel: false`. O badge "Disponível" e o clique no card (sheet ✕) podem usar o seed real (há 1 unidade disponível).
Output: `e2e/public-pages.spec.js` com describes por req-id.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-paginas-publicas/09-PATTERNS.md
@.planning/phases/09-paginas-publicas/09-RESEARCH.md
@e2e/dashboard-smoke.spec.js
@e2e/seed.mjs
@playwright.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar e2e/public-pages.spec.js — smoke LP-01/LP-02/LP-03 + card PUB-01</name>
  <files>e2e/public-pages.spec.js</files>
  <read_first>
    - e2e/dashboard-smoke.spec.js (análogo de estrutura: import, test.describe, getByRole, waitForURL — sem login para páginas públicas)
    - .planning/phases/09-paginas-publicas/09-PATTERNS.md (seção "e2e/public-pages.spec.js" tem os padrões de teste verbatim, incluindo page.route mock)
    - src/app/page.js (estado atual dos CTAs — linhas 45 "INICIE GRATUITAMENTE", 48 "VER PROJETOS", 147 "ACESSE ANALITYCS")
    - src/components/ui/Header.js (botões "COMEÇAR AGORA" em linhas 48 e 96 — atualmente type="button" sem href)
    - e2e/seed.mjs (estado do seed: 1 unidade disponivel com valor_visivel=true — confirma necessidade do mock para "Consulte o Proprietário")
  </read_first>
  <action>
    Criar o arquivo com `import { test, expect } from '@playwright/test'` (sem login — páginas públicas). Estruturar describes por req-id seguindo o análogo dashboard-smoke.spec.js.
    - LP-01: visitar `/`, localizar `page.getByRole('link', { name: /VER UNIDADES/i })`, clicar, `waitForURL('**/unidades')`. (RED agora: label é "VER PROJETOS".)
    - LP-02: visitar `/`, localizar `page.getByRole('link', { name: /ACESSAR DASHBOARD/i })`, clicar, `waitForURL('**/login')`. (RED agora: é `<button>` "INICIE GRATUITAMENTE" sem href.)
    - LP-03: visitar `/`, asseverar que `page.getByRole('link', { name: /ACESSAR PAINEL/i })` existe e tem href `/login` (corrige "ACESSE ANALITYCS"); e que os dois CTAs "COMEÇAR AGORA" do Header são links com href `/login` via `page.getByRole('link', { name: /COMEÇAR AGORA/i }).first()` checando `getAttribute('href')` === `/login`. (RED agora: ambos são `<button>`.)
    - PUB-01 "Consulte o Proprietário": MOCKAR via `page.route('**/rest/v1/unidades**', route => route.fulfill({ status: 200, body: JSON.stringify([{ id: '...', nome: 'Sala Mock', area_m2: 25, valor_mensal: 1000, valor_visivel: false, status: 'disponivel', edificio_id: '...' }]) }))` antes de `page.goto('/unidades')`. Asseverar `page.getByText('Consulte o Proprietário')` visível. (RED agora: card mostra "Valor sob consulta".) NÃO depender do seed para esta asserção — o seed não tem unidade disponível com valor_visivel=false.
    - PUB-01 badge "Disponível": visitar `/unidades` SEM mock (usa seed real, que tem 1 unidade disponível), asseverar `page.getByText(/Disponível/i).first()` visível. (Já verde — serve de regressão de PUB-01.)
    Usar timeouts de 10_000ms no padrão do projeto. Não duplicar o teste de navegação do link "← Voltar" (já coberto em dashboard-smoke.spec.js BUG-04).
  </action>
  <verify>
    <automated>npx playwright test e2e/public-pages.spec.js --list 2>&1 | grep -E "LP-01|LP-02|LP-03|PUB-01"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `e2e/public-pages.spec.js` existe e `npx playwright test e2e/public-pages.spec.js --list` lista pelo menos um teste para cada um de LP-01, LP-02, LP-03, PUB-01
    - O teste PUB-01 "Consulte o Proprietário" usa `page.route` com uma unidade `valor_visivel: false` (não depende do seed)
    - Rodar `npx playwright test e2e/public-pages.spec.js -g "LP-02"` resulta em FALHA (RED) porque "ACESSAR DASHBOARD" ainda não existe como link
    - Nenhum teste usa `login()` (páginas públicas não exigem autenticação)
    - O arquivo não contém um teste de navegação do link "← Voltar" (evita duplicar dashboard-smoke.spec.js)
  </acceptance_criteria>
  <done>Describes LP-01/LP-02/LP-03/PUB-01 criados; cenários de copy/href estão RED; "Consulte o Proprietário" via mock.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar testes PUB-02 (empty state) e PUB-03 (tap targets + overflow 375px)</name>
  <files>e2e/public-pages.spec.js</files>
  <read_first>
    - e2e/public-pages.spec.js (arquivo criado na Task 1 — adicionar describes, não recriar)
    - .planning/phases/09-paginas-publicas/09-PATTERNS.md (padrões de mock page.route, viewport 375px, getBoundingClientRect)
    - src/components/features/UnidadesPublicas.js (empty state linhas 124-135: "Nenhuma unidade disponível"; tab buttons linha 100 py-2; link Voltar linha 82)
    - src/components/features/UnidadeDetailSheet.js (botão ✕ linha 31 width:32 height:32; botões py-[14px] linhas 84 e 91)
    - e2e/seed.mjs (há 1 unidade disponível no seed — o teste do sheet ✕ pode clicar no card real, sem mock)
  </read_first>
  <action>
    Adicionar ao arquivo:
    - PUB-02: interceptar `page.route('**/rest/v1/unidades**', route => route.fulfill({ status: 200, body: JSON.stringify([]) }))`, visitar `/unidades`, asseverar `page.getByText('Nenhuma unidade disponível')` visível com timeout 10_000. (Deve passar já — empty state existe; serve de regressão.)
    - PUB-03 overflow: criar contexto com `viewport: { width: 375, height: 812 }`, visitar `/unidades`, asseverar `document.documentElement.scrollWidth <= document.documentElement.clientWidth` via `page.evaluate`. Fechar o contexto.
    - PUB-03 tap target tab: viewport 375px, visitar `/unidades`, localizar o tab "Todos" (ou primeiro tab button), asseverar `getBoundingClientRect().height >= 44`. (RED agora: py-2 ≈ 30px.)
    - PUB-03 tap target Voltar: viewport 375px, visitar `/unidades`, localizar `getByRole('link', { name: /Voltar/i })`, asseverar altura `>= 44`. (RED agora: sem padding vertical.)
    - PUB-03 tap target sheet ✕: viewport 375px, visitar `/unidades` (seed real tem 1 unidade disponível), aguardar e clicar no primeiro card para abrir o `UnidadeDetailSheet`, localizar o botão ✕ (`getByRole('button', { name: '✕' })` ou via texto), asseverar `getBoundingClientRect().height >= 44` E `width >= 44`. (RED agora: 32×32px — cobre o critério #6 "todos os tap targets ≥44px".) Antes de clicar, checar a contagem de cards renderizados; se for 0 (seed vazio em CI), usar `test.skip` condicional para não dar falso negativo.
  </action>
  <verify>
    <automated>npx playwright test e2e/public-pages.spec.js --list 2>&1 | grep -E "PUB-02|PUB-03"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/public-pages.spec.js --list` lista testes para PUB-02 e múltiplos PUB-03 (overflow, tab, Voltar, sheet ✕)
    - O teste PUB-03 do sheet ✕ asevera tanto `height >= 44` quanto `width >= 44` (não apenas altura)
    - Rodar `npx playwright test e2e/public-pages.spec.js -g "PUB-03"` resulta em pelo menos uma FALHA (RED) — tab buttons e/ou sheet ✕ abaixo de 44px
    - O teste de empty state usa `page.route` com body `[]` e não depende do estado real do banco
  </acceptance_criteria>
  <done>PUB-02 e PUB-03 (4 asserts) adicionados; tap targets RED, empty state verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → Supabase REST | Os testes interceptam (`page.route`) a resposta REST anon; nenhum dado real de produção é mutado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | e2e/public-pages.spec.js | accept | Testes leem apenas páginas públicas (RLS já restringe a `status='disponivel'`); nenhum segredo no spec |
| T-09-02 | Tampering | mock page.route | accept | Mock vive só no contexto de teste; não altera código de produção nem dados |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote instalado nesta fase — Playwright já presente. Sem superfície de supply-chain. |
</threat_model>

<verification>
- `npx playwright test e2e/public-pages.spec.js --list` lista todos os 6 grupos de req
- Os cenários de copy/href e tap target estão RED (provam que a implementação dos planos 02/03 é necessária)
- O empty state (PUB-02) passa via mock — serve de regressão
- "Consulte o Proprietário" (PUB-01) usa mock com valor_visivel=false — passável independente do seed
</verification>

<success_criteria>
- e2e/public-pages.spec.js existe com cobertura LP-01/02/03 + PUB-01/02/03
- Tap target do sheet ✕ tem assert dedicado (height E width ≥44) — fecha o critério #6
- Asserção "Consulte o Proprietário" usa mock (não depende do seed, que tem valor_visivel=true)
- Nenhuma duplicação do teste de navegação do link Voltar (BUG-04 já cobre)
</success_criteria>

<output>
Create `.planning/phases/09-paginas-publicas/09-01-SUMMARY.md` when done
</output>
