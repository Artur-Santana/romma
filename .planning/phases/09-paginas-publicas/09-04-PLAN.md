---
phase: 09-paginas-publicas
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified: ["e2e/public-pages.spec.js"]
autonomous: false
requirements: [AUDIT-01, FIX-01, LP-01, LP-02, LP-03, PUB-01, PUB-02, PUB-03]
must_haves:
  truths:
    - "A suite E2E completa passa (npx playwright test) — sem regressão das fases anteriores"
    - "e2e/public-pages.spec.js passa inteiramente (LP-01/02/03, PUB-01/02/03)"
    - "Landing page e /unidades foram auditadas visualmente em 375px e desktop (AUDIT-01)"
    - "Correções emergentes descobertas na auditoria são registradas (FIX-01)"
  artifacts:
    - path: ".planning/phases/09-paginas-publicas/09-04-SUMMARY.md"
      provides: "Resultado da auditoria e suite completa"
      contains: "AUDIT-01"
  key_links:
    - from: "e2e/public-pages.spec.js"
      to: "página real /unidades e /"
      via: "execução completa da suite após implementação"
      pattern: "public-pages"
---

<objective>
Wave 3: auditoria isolada das telas trabalhadas (AUDIT-01) e validação da suite E2E completa. Confirmar visualmente, em viewport 375px e desktop, que landing page e /unidades cumprem todos os critérios de sucesso da Fase 9. Registrar quaisquer correções emergentes (FIX-01).

Purpose: o ROADMAP marca AUDIT-01 e FIX-01 como cross-cutting obrigatórios em toda fase 8-14 — cada fase fecha com deep-dive isolado das telas trabalhadas antes de verificar. Os gates automatizados provam comportamento; o checkpoint humano confirma credibilidade visual para a banca.
Output: suite verde + auditoria documentada no SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-paginas-publicas/09-UI-SPEC.md
@.planning/phases/09-paginas-publicas/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rodar suite E2E completa — sem regressão</name>
  <files>e2e/public-pages.spec.js</files>
  <read_first>
    - e2e/public-pages.spec.js (suite criada/atualizada nos planos 01-03)
    - playwright.config.js (config do runner; global-setup/teardown)
    - .planning/phases/09-paginas-publicas/09-RESEARCH.md (Validation Architecture — comandos e sampling)
  </read_first>
  <action>
    Executar `npx playwright test e2e/public-pages.spec.js` e confirmar que todos os cenários LP-01/02/03 e PUB-01/02/03 passam (estavam RED no plano 01). Em seguida executar `npx playwright test` (suite completa) para garantir que as edições em page.js, Header.js, UnidadePublicaCard.js, UnidadesPublicas.js e UnidadeDetailSheet.js não quebraram nenhum teste existente (auth, crud, dashboard-smoke incluindo BUG-04). Se algum teste falhar, registrar o detalhe e corrigir a causa (FIX-01) — não relaxar o assert. Capturar o resumo de pass/fail para o SUMMARY.
  </action>
  <verify>
    <automated>npx playwright test e2e/public-pages.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/public-pages.spec.js` termina com 0 falhas
    - `npx playwright test` (suite completa) termina com 0 falhas — sem regressão em auth/crud/dashboard-smoke
    - O teste PUB-03 do sheet ✕ passa (height E width >= 44)
    - Qualquer falha emergente foi registrada e corrigida (não silenciada nem skipada)
  </acceptance_criteria>
  <done>Suite verde; nenhuma regressão; resultados capturados para o SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Auditoria visual das páginas públicas (AUDIT-01)</name>
  <what-built>
    Landing page (`/`) com CTAs ativos (ACESSAR DASHBOARD → /login, VER UNIDADES → /unidades, ACESSAR PAINEL → /login, COMEÇAR AGORA → /login no Header) e /unidades com card mostrando "Consulte o Proprietário", empty state, e tap targets ≥44px (tabs, Voltar, sheet ✕ e botões).
  </what-built>
  <how-to-verify>
    1. Rodar `npm run dev` e abrir `http://localhost:3000/`.
    2. DESKTOP — clicar em cada CTA e confirmar destino:
       - Hero "ACESSAR DASHBOARD" → /login (botão gradiente, primário)
       - Hero "VER UNIDADES" → /unidades (botão secundário)
       - SISTEMA.04 "ACESSAR PAINEL" → /login (sem typo "ANALITYCS")
       - Header "COMEÇAR AGORA" → /login
       - Nenhum botão ativo deve retornar 404 ou ficar inerte.
    3. MOBILE 375px (DevTools → iPhone SE) em `/unidades`:
       - Confirmar ausência de overflow horizontal (nenhuma scrollbar lateral no body).
       - Confirmar que tabs de edifício, link "← Voltar" são facilmente tocáveis (≥44px no inspetor de acessibilidade).
       - Tocar num card → abrir o UnidadeDetailSheet → confirmar que o botão ✕ e os botões "Tenho interesse"/"Fechar" têm área de toque confortável (≥44px).
       - Confirmar card mostra: nome, edifício, área m², preço ou "Consulte o Proprietário", badge "Disponível".
    4. Com banco em estado sem unidades disponíveis (ou via mock já testado), confirmar empty state "Nenhuma unidade disponível".
    5. Registrar qualquer ajuste fino necessário (FIX-01) antes de aprovar.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados (serão registrados como FIX-01)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auditoria humana → ambiente local | Verificação visual em dev server; nenhum dado de produção tocado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Repudiation | auditoria visual | accept | Resultado da auditoria registrado no SUMMARY (AUDIT-01) com data e veredito |
| T-09-08 | Information Disclosure | nav href="#" do Header (CONTRATOS/PORTAIS/DASHBOARD) | accept | Risco A1 documentado: placeholders aceitáveis para a banca (permanecem na página, sem 404); wiring completo é pós-TCC |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote instalado nesta fase — sem superfície de supply-chain |
</threat_model>

<verification>
- Suite completa `npx playwright test` verde
- Checkpoint humano aprovado (AUDIT-01) — landing + /unidades em desktop e 375px
- Riscos aceitos (A1: href="#" do Header) documentados no SUMMARY
</verification>

<success_criteria>
- Todos os 6 critérios de sucesso da Fase 9 (ROADMAP) confirmados
- AUDIT-01: deep-dive das telas trabalhadas concluído
- FIX-01: correções emergentes (se houver) registradas e incorporadas
</success_criteria>

<output>
Create `.planning/phases/09-paginas-publicas/09-04-SUMMARY.md` when done
</output>
