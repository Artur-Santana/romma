---
phase: 20-edif-cios-cards-drill-in
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/components/features/GestaoEdificios.js
autonomous: false
requirements: [EDIF-01, EDIF-02, EDIF-03]
must_haves:
  truths:
    - "Edifícios aparecem em grade de cards de 2 colunas (1 coluna no mobile), cada card com os 4 stats: nº de unidades, ocupação %, MRR (dourado), área total (D-01/D-02, EDIF-01)"
    - "Cada card mostra barra de ocupação contígua, alugadas-first, sem buracos, com legenda 'X alugada(s) · Y disponível(is)' (D-03/D-04, EDIF-02)"
    - "Botão 'Ver N unidade(s)' expande inline a lista de unidades do edifício; desabilitado quando N=0 (D-05, EDIF-03)"
    - "Cada linha de unidade na lista expandida é clicável e abre o UnifiedUnidadeModal em mode=edit com lockEdificio=true (D-06/D-07/D-08, EDIF-03)"
    - "Após onSaved, recarrega unidades+edifícios e recomputa stats; o accordion permanece aberto (não reseta expandidos)"
    - "CRUD de edifício (criar/editar/remover) preservado via Server Actions existentes (D-09)"
  artifacts:
    - path: "src/components/features/GestaoEdificios.js"
      provides: "Grade de cards 2-col com stats, barra de ocupação, accordion drill-in e reuso do modal travado"
      contains: "OccupationBar"
  key_links:
    - from: "src/components/features/GestaoEdificios.js"
      to: "getUnidades"
      via: "Promise.all em carregarDados"
      pattern: "Promise\\.all"
    - from: "src/components/features/GestaoEdificios.js"
      to: "UnifiedUnidadeModal"
      via: "drill-in row click com lockEdificio={true}"
      pattern: "lockEdificio"
    - from: "src/components/features/GestaoEdificios.js"
      to: "criarEdificio|editarEdificio|deletarEdificio"
      via: "Server Actions preservadas"
      pattern: "criarEdificio|editarEdificio|deletarEdificio"
---

<objective>
Reestruturar `src/components/features/GestaoEdificios.js` de lista vertical para grade de **cards de 2 colunas**, cada card com stats por edifício (nº unidades, ocupação %, MRR, área total), barra de ocupação contígua com legenda, e um accordion "Ver N unidade(s)" que expande as unidades do edifício — cada linha clicável abre o `UnifiedUnidadeModal` (Phase 19) em modo edição com o edifício travado (`lockEdificio=true` da Plan 01).

Purpose: Entregar EDIF-01, EDIF-02, EDIF-03. Preservar integralmente o CRUD de edifício existente (D-09) — esta fase reestrutura apresentação, não requisitos de CRUD.
Output: `GestaoEdificios.js` reestruturado; `e2e/crud-edificios.spec.js` (estendido na Plan 01) passa do RED ao GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-edif-cios-cards-drill-in/20-CONTEXT.md
@.planning/phases/20-edif-cios-cards-drill-in/20-RESEARCH.md
@.planning/phases/20-edif-cios-cards-drill-in/20-PATTERNS.md
@.planning/phases/20-edif-cios-cards-drill-in/20-UI-SPEC.md
@.planning/phases/20-edif-cios-cards-drill-in/20-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carregar unidades + derivar stats e barra de ocupação por edifício</name>
  <files>src/components/features/GestaoEdificios.js</files>
  <read_first>
    - src/components/features/GestaoEdificios.js (arquivo a modificar — state lines ~28-36, carregarEdificios lines ~42-44, useEffect ~46-52, handlers CRUD ~54-98, SkeletonEdificios ~12-25, error banners ~140-144 e ~160-164)
    - src/components/features/Unidades.js (analog canônico — Promise.all lines 91-112, fmtBRLk lines 62-71, stat derivation lines 114-124, card grid lines 321-326, metrics cell lines 199-251)
    - src/lib/queries-client.js (getEdificios e getUnidades — confirmar campos retornados: edificio_id, status, valor_mensal, area_m2)
    - .planning/phases/20-edif-cios-cards-drill-in/20-PATTERNS.md (código exato de carregarDados, computeStats, unidadesPorEdificio, OccupationBar)
    - .planning/phases/20-edif-cios-cards-drill-in/20-UI-SPEC.md (Stats Display Contract + Color contract da barra)
  </read_first>
  <action>
    Adicionar `getUnidades` ao import existente de `@/lib/queries-client` (per D-01 — nenhuma extensão de query; getUnidades já traz todos os campos necessários). Adicionar `import UnifiedUnidadeModal from "@/components/ui/UnifiedUnidadeModal"`.

    Adicionar novo state: `unidades` (default `[]`), `expandidos` (default `new Set()`) e `modalState` (default `null`). Preservar todo o state existente de CRUD (showForm, form, editandoId, formEdit, loading, erro, erroEdit).

    Substituir `carregarEdificios` por `carregarDados`, que faz `Promise.all([getEdificios(), getUnidades()])` e seta ambos os states com guard `?? []` (Pitfall 4). Atualizar o `useEffect` de mount e TODAS as chamadas de `carregarEdificios()` nos handlers CRUD para `carregarDados()`. CRÍTICO: `carregarDados` só atualiza `edificios` e `unidades` — NUNCA toca em `expandidos` (Pitfall 2: accordion deve sobreviver ao reload).

    Adicionar a função local `fmtBRLk(v)` (copiar verbatim de Unidades.js linhas 62-71 — não exportada; D-02, formatação de MRR).

    Adicionar a derivação `unidadesPorEdificio` (reduce agrupando por `edificio_id`) computada inline antes do render. Adicionar a função `computeStats(lista)` retornando `{ total, alugadas, disponiveis, ocupacaoPct, mrr, areaTotal }` per D-02: ocupacaoPct = `alugadas/total*100` arredondado (0 se total=0); mrr = soma `valor_mensal` das alugadas; areaTotal = soma `area_m2` de todas; alugadas = filter status === "alugada".

    Adicionar o componente local `OccupationBar({ alugadas, disponiveis })` (D-03/EDIF-02): quando total=0, renderiza uma barra neutra única `var(--border-3)`, altura 6px, largura 100%. Senão, um container flex altura 6px com segmentos CONDICIONAIS — `{alugadas > 0 && <div style={{ flex: alugadas, background: "var(--indigo)" }} />}` PRIMEIRO, depois `{disponiveis > 0 && <div style={{ flex: disponiveis, background: "var(--border-3)" }} />}`. Render condicional de cada segmento é obrigatório para não deixar buracos (Pitfall 1, EDIF-02 "sem buracos"). Sem border-radius.

    Esta task entrega a infra de dados/derivação/OccupationBar e mantém a tela compilando. O render completo de cards é a Task 2.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/features/GestaoEdificios.js','utf8'); const ok=/Promise\.all/.test(s)&&/function computeStats/.test(s)&&/function OccupationBar/.test(s)&&/getUnidades/.test(s)&&/fmtBRLk/.test(s); process.exit(ok?0:1)" && npx eslint src/components/features/GestaoEdificios.js</automated>
  </verify>
  <acceptance_criteria>
    - `carregarDados` usa `Promise.all([getEdificios(), getUnidades()])` com `?? []` e NÃO reseta `expandidos`.
    - `computeStats` implementa as fórmulas de D-02 (ocupacaoPct, mrr, areaTotal, alugadas, disponiveis, total).
    - `OccupationBar` renderiza segmentos condicionais (alugadas-first) e estado vazio neutro.
    - `fmtBRLk` presente como função local.
    - `npx eslint src/components/features/GestaoEdificios.js` sem erros.
  </acceptance_criteria>
  <done>Dados de unidades carregados em paralelo; stats e barra de ocupação derivados client-side; expandidos preservado no reload.</done>
</task>

<task type="auto">
  <name>Task 2: Renderizar grade de cards 2-col, accordion drill-in e reuso do modal travado</name>
  <files>src/components/features/GestaoEdificios.js</files>
  <read_first>
    - src/components/features/GestaoEdificios.js (mesmo arquivo, já com a infra da Task 1)
    - .planning/phases/20-edif-cios-cards-drill-in/20-PATTERNS.md (card grid, metrics cell, accordion toggle, unit row drill-in, modal invocation Phase 20 variant, SkeletonEdificios 2-col, inline edit dentro do card)
    - .planning/phases/20-edif-cios-cards-drill-in/20-UI-SPEC.md (Layout Contract EdificioCard, Interaction Contract, Copywriting Contract, Stats Display Contract)
    - src/components/features/Unidades.js (metrics cell e card grid de referência)
  </read_first>
  <action>
    Trocar o container de lista vertical por uma grade CSS: `display: grid; gridTemplateColumns: repeat(auto-fill, minmax(340px, 1fr)); gap: 16px; alignItems: start` (1 coluna no mobile naturalmente via minmax — Pitfall 5). Aplicar o page shell `romma-page r-fade` com paddings `var(--rd-page-y) var(--rd-gutter)` e paddingBottom 64 (UI-SPEC Page Layout Shell). Preservar o `PageHeader` (eyebrow "E.LIST · EDIFÍCIOS", título "Edifícios.", subtitle "N cadastrado(s)", CTA "Novo Edifício"/"Fechar").

    Para cada edifício no `.map`, renderizar um card (`border: 1px solid var(--border-3)`, `background: var(--surface)`, `padding: var(--rd-panel)` 20px). Computar `const lista = unidadesPorEdificio[edificio.id] ?? []` e `const stats = computeStats(lista)`. Estrutura do card (UI-SPEC EdificioCard Internal Layout):
    - Header row: nome do edifício (20px bold, fg-1) + endereço (13px fg-4) à esquerda; botões ghost "Editar" e "Remover" (danger) à direita. Preservar os handlers existentes `handleEditar`/`handleDeletar`.
    - Stats row: grid 4 colunas inline com `border: 1px solid var(--border-3)`, divisores `borderRight`, label `.r-label` (caps) + valor (20px). Células: [Unidades = stats.total], [Ocupação = `${stats.ocupacaoPct}%`], [MRR = `fmtBRLk(stats.mrr)` em `var(--highlight)` dourado label+valor], [Área total = `${stats.areaTotal} m²`].
    - `<OccupationBar alugadas={stats.alugadas} disponiveis={stats.disponiveis} />` seguido da legenda `.r-meta` fg-4 no formato `${stats.alugadas} alugada${stats.alugadas !== 1 ? "s" : ""} · ${stats.disponiveis} disponíve${stats.disponiveis !== 1 ? "is" : "l"}` (D-03; estado vazio "0 alugada(s) · 0 disponível(is)" D-04).
    - Botão "Ver N unidade(s)" (ghost, border-indigo) com label `Ver ${lista.length} unidade${lista.length !== 1 ? "s" : ""}`, `disabled` + opacity 0.4 + cursor not-allowed quando `lista.length === 0` (D-04/D-05). onClick chama `toggleExpandido(edificio.id)`.

    Adicionar a função `toggleExpandido(id)` que faz add/delete imutável num novo `Set` (PATTERNS accordion). Quando `expandidos.has(edificio.id)`, renderizar o painel accordion: `borderTop: 1px solid var(--border-3)`, e cada unidade de `lista` como uma linha clicável (a LINHA INTEIRA, não só um botão — D-06): `cursor: pointer`, padding `var(--rd-row-y) var(--rd-row-x)`, hover `var(--surface-hi)`, exibindo nome (13px 600 fg-1) + status pill (alugada/disponível com as cores do UI-SPEC) + área (`${u.area_m2} m²`). onClick → `setModalState({ unidade: u })`.

    Adaptar `SkeletonEdificios` para a mesma grade 2-col (PATTERNS skeleton). Preservar o inline edit form (branch `editandoId === edificio.id`) DENTRO da fronteira do card, sem spanning de colunas, com os campos Nome/Endereço e botões Salvar/Cancelar existentes. Preservar o painel "Novo Edifício" (`showForm`) como painel no topo e os error banners existentes.

    Renderizar o modal no fim do componente: `{modalState && (<UnifiedUnidadeModal mode="edit" initial={modalState.unidade} edificios={edificios} lockEdificio={true} onClose={() => setModalState(null)} onSaved={() => { carregarDados(); setModalState(null) }} />)}`. CRÍTICO: NÃO resetar `expandidos` no `onSaved` — accordion permanece aberto após salvar (Pitfall 2). Passar `lockEdificio={true}` (D-07) e `edificios` completo com `initial.edificio_id` já preenchido (D-08).

    Não alterar as Server Actions nem a assinatura dos handlers CRUD (D-09).
  </action>
  <verify>
    <automated>npx eslint src/components/features/GestaoEdificios.js && node -e "const s=require('fs').readFileSync('src/components/features/GestaoEdificios.js','utf8'); const ok=/minmax\(340px/.test(s)&&/lockEdificio=\{true\}/.test(s)&&/toggleExpandido/.test(s)&&/Ver \$\{/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Grade usa `repeat(auto-fill, minmax(340px, 1fr))` (2-col desktop, 1-col mobile).
    - Cada card mostra os 4 stats (Unidades, Ocupação %, MRR dourado, Área total).
    - Barra de ocupação + legenda no formato "X alugada(s) · Y disponível(is)" presentes.
    - Botão "Ver N unidade(s)" expande/recolhe via `toggleExpandido`; disabled quando N=0.
    - Linha de unidade clicável abre `UnifiedUnidadeModal` com `lockEdificio={true}`; `onSaved` recarrega sem resetar `expandidos`.
    - CRUD de edifício e Server Actions inalterados.
  </acceptance_criteria>
  <done>Tela de Edifícios renderiza cards 2-col com stats, barra de ocupação, accordion drill-in e modal travado; CRUD preservado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual da tela de Edifícios</name>
  <files>src/components/features/GestaoEdificios.js</files>
  <action>Checkpoint de verificação humana — nenhuma mudança de código. Confirmar visualmente o resultado das Tasks 1-2 conforme os passos em how-to-verify.</action>
  <what-built>
    GestaoEdificios.js reestruturado: cards 2-col com stats por edifício, barra de ocupação contígua (alugadas-first), accordion "Ver N unidade(s)" e drill-in que abre o UnifiedUnidadeModal com o edifício travado.
  </what-built>
  <how-to-verify>
    1. Rodar `npm run dev` e acessar `/dashboard/edificios` autenticado como proprietário.
    2. Confirmar que os edifícios aparecem em 2 colunas (desktop) e que cada card mostra os 4 stats.
    3. Num edifício com unidades de status misto: confirmar que a barra de ocupação é contígua, alugadas à esquerda, SEM buracos, e a legenda "X alugada(s) · Y disponível(is)" está correta.
    4. Clicar "Ver N unidade(s)" — a lista expande inline; clicar de novo recolhe. Botão desabilitado em edifício com 0 unidades.
    5. Clicar numa linha de unidade — o modal abre em edição com o select de Edifício DESABILITADO mostrando só o edifício corrente. Salvar — o accordion permanece aberto e os stats recomputam.
    6. Confirmar reduzindo a janela para ~375px: cards viram 1 coluna sem estourar.
  </how-to-verify>
  <verify>
    <human-check>Layout, contiguidade da barra (sem buracos), accordion e modal travado verificados visualmente.</human-check>
  </verify>
  <resume-signal>Digite "approved" ou descreva problemas visuais.</resume-signal>
  <done>Proprietário confirma que cards, barra de ocupação contígua, accordion e modal travado funcionam visualmente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action (editarUnidade) | Save do modal de unidade cruza para o backend; ownership já validado (authGuard + proprietario_id, Phase 15/19) |
| client → Server Action (edificios.js) | criar/editar/remover edifício — Server Actions preservadas com guards existentes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Tampering | `lockEdificio` UI no drill-in | accept | `lockEdificio` é UI-only; a Server Action `editarUnidade` revalida `proprietario_id` na cadeia de propriedade. Sem nova superfície de ataque. |
| T-20-03 | Information Disclosure | stats derivados client-side | accept | Stats (ocupação/MRR/área) computados no cliente a partir de `getUnidades()`, que já é RLS-filtrado por proprietário. Dataset não-autoritativo é aceitável para dashboard de proprietário único (TCC). |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote instalado nesta fase (RESEARCH "No New Packages Required"). N/A. |
</threat_model>

<verification>
- `npx playwright test e2e/crud-edificios.spec.js` verde (RED → GREEN vs. Plan 01).
- `npx playwright test` (suíte completa) verde antes de `/gsd-verify-work`.
- `npx eslint src/components/features/GestaoEdificios.js` sem erros.
</verification>

<success_criteria>
- EDIF-01: cards 2-col com 4 stats por edifício.
- EDIF-02: barra de ocupação contígua alugadas-first com legenda, sem buracos.
- EDIF-03: accordion "Ver N unidade(s)" + drill-in abrindo o modal travado.
- CRUD de edifício preservado (D-09).
</success_criteria>

<output>
Create `.planning/phases/20-edif-cios-cards-drill-in/20-02-SUMMARY.md` when done
</output>
