---
phase: 20-edif-cios-cards-drill-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/UnifiedUnidadeModal.js
  - e2e/crud-edificios.spec.js
autonomous: true
requirements: [EDIF-03]
must_haves:
  truths:
    - "Modal de unidade pode ser aberto com o edifício travado (FSelect desabilitado) sem quebrar o call-site existente de Unidades.js"
    - "Quando lockEdificio=true, o select de Edifício exibe apenas o edifício corrente e não permite trocar"
    - "e2e/crud-edificios.spec.js contém asserções para o novo layout de cards, stats, barra de ocupação, accordion, drill-in e lockEdificio (RED até a Plan 02 implementar a UI)"
  artifacts:
    - path: "src/components/ui/UnifiedUnidadeModal.js"
      provides: "Prop lockEdificio que desabilita o FSelect de edifício"
      contains: "lockEdificio"
    - path: "e2e/crud-edificios.spec.js"
      provides: "Asserções E2E para EDIF-01..03"
      contains: "Ver"
  key_links:
    - from: "src/components/ui/UnifiedUnidadeModal.js"
      to: "FSelect"
      via: "prop disabled={lockEdificio}"
      pattern: "disabled.*lockEdificio|lockEdificio.*disabled"
---

<objective>
Estender o `UnifiedUnidadeModal` (Phase 19) com uma prop booleana `lockEdificio` que desabilita o `FSelect` de edifício, e estender a suíte E2E `e2e/crud-edificios.spec.js` com asserções para todas as novas features da Phase 20 (cards, stats, barra de ocupação, accordion, drill-in, select travado).

Purpose: Habilitar o reuso do modal no drill-in (D-07/D-08) sem quebrar `Unidades.js`, e criar o test scaffold (Wave 0 / Nyquist) que a Plan 02 fará passar do RED ao GREEN.
Output: Modal com prop `lockEdificio` (default `false`); spec E2E estendido com asserções para EDIF-01..03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-edif-cios-cards-drill-in/20-CONTEXT.md
@.planning/phases/20-edif-cios-cards-drill-in/20-RESEARCH.md
@.planning/phases/20-edif-cios-cards-drill-in/20-PATTERNS.md
@.planning/phases/20-edif-cios-cards-drill-in/20-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar prop lockEdificio ao UnifiedUnidadeModal</name>
  <files>src/components/ui/UnifiedUnidadeModal.js</files>
  <read_first>
    - src/components/ui/UnifiedUnidadeModal.js (arquivo a modificar — assinatura na linha ~272, componente FSelect linhas ~40-67, campo Edifício linhas ~431-439)
    - src/components/features/Unidades.js (call-site canônico ~linha 361 — confirmar que NÃO passa lockEdificio, garantindo que o default false preserva o comportamento)
    - .planning/phases/20-edif-cios-cards-drill-in/20-PATTERNS.md (seção UnifiedUnidadeModal.js — código exato do FSelect e do campo Edifício)
  </read_first>
  <behavior>
    - Com lockEdificio omitido (default false): FSelect de edifício funciona exatamente como hoje — todas as opções listadas, troca permitida, onChange atualiza form.edificio_id. Call-site de Unidades.js inalterado.
    - Com lockEdificio=true: o <select> recebe disabled, exibe apenas a opção cujo id === form.edificio_id (D-08), e onChange não altera o form. Visual: opacity 0.5, cursor default no select.
  </behavior>
  <action>
    Estender a assinatura do componente default export de `{ mode, initial, edificios, onClose, onSaved }` para `{ mode, initial, edificios, onClose, onSaved, lockEdificio = false }` — default `false` é obrigatório para não quebrar o call-site de Unidades.js (per D-07).

    No componente local `FSelect`, adicionar o parâmetro `disabled` à destructuring de props e forwardá-lo ao `<select>` interno via atributo `disabled`. Ajustar o estilo inline do `<select>`: `cursor` passa a ser `default` quando disabled (senão `pointer`), e adicionar `opacity` `0.5` quando disabled (senão `1`). Não alterar o restante dos estilos nem a span do chevron.

    No `FormField label="Edifício"` (campo do edifício), passar `disabled={lockEdificio}` ao `FSelect`. Guardar o handler `onChange` com `!lockEdificio` antes de chamar `setForm` (per D-07). Filtrar a lista de options: quando `lockEdificio` for true, mapear `edificios.filter(ed => ed.id === form.edificio_id)`; senão mapear `edificios` completo (per D-08, evita o Pitfall 3 de exibir edifício errado).

    Não tocar em nenhum outro campo do formulário, no fluxo de foto de capa, nem na lógica de save. Esta é uma extensão mínima de API.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/ui/UnifiedUnidadeModal.js','utf8'); const ok = /lockEdificio\s*=\s*false/.test(s) && /disabled\s*=\s*\{\s*lockEdificio\s*\}/.test(s) && /edificios\.filter\(/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Assinatura do componente inclui `lockEdificio = false`.
    - `FSelect` aceita e forwarda `disabled` ao `<select>` interno.
    - O campo Edifício passa `disabled={lockEdificio}` e filtra options com `edificios.filter(...)` quando travado.
    - `npx eslint src/components/ui/UnifiedUnidadeModal.js` retorna sem erros.
    - O call-site em `Unidades.js` não foi alterado e continua funcionando (default false).
  </acceptance_criteria>
  <done>Modal abre com edifício travado quando lockEdificio=true; comportamento inalterado quando omitido.</done>
</task>

<task type="auto">
  <name>Task 2: Estender e2e/crud-edificios.spec.js com asserções da Phase 20 (test scaffold)</name>
  <files>e2e/crud-edificios.spec.js</files>
  <read_first>
    - e2e/crud-edificios.spec.js (arquivo a estender — testes atuais: criar/editar/deletar edifício; reusar o setup/login/navegação existente)
    - .planning/phases/20-edif-cios-cards-drill-in/20-VALIDATION.md (Wave 0 Requirements — lista exata de asserções a adicionar)
    - .planning/phases/20-edif-cios-cards-drill-in/20-UI-SPEC.md (Copywriting Contract + Layout Contract — strings e estrutura a assertar)
  </read_first>
  <action>
    Adicionar novos testes ao spec existente (reusar o bloco de login/navegação para /dashboard/edificios já presente no arquivo). Cobrir, em asserções Playwright:
    - EDIF-01: a área de edifícios renderiza em grade de cards (não lista vertical) e cada card exibe os 4 stats — labels "Unidades", "Ocupação", "MRR", "Área total" visíveis em pelo menos um card.
    - EDIF-02: a barra de ocupação está presente em um card com unidades, e a legenda no formato "X alugada(s) · Y disponível(is)" é visível (asserção por regex de texto, ex.: `/\d+ alugada.* · \d+ dispon/`).
    - EDIF-03 accordion: existe um botão com texto no padrão `/Ver \d+ unidade/`; clicar nele revela a lista de unidades do edifício (alguma linha de unidade fica visível); clicar de novo recolhe.
    - EDIF-03 drill-in: clicar numa linha de unidade abre o `UnifiedUnidadeModal` (asserção por um elemento/label do modal de edição de unidade, ex.: label "Edifício" ou o título do modal).
    - lockEdificio: dentro do modal aberto pelo drill-in, o `<select>` de Edifício está desabilitado (asserção `toBeDisabled()`).

    Como a UI ainda não existe (Plan 02 implementa), estes testes ESTARÃO RED até a Plan 02 — isto é esperado (Nyquist Wave 0). Usar seletores resilientes (getByText/getByRole com regex) alinhados ao Copywriting Contract do UI-SPEC. NÃO depender de IDs ou data-testid que ainda não existem; preferir texto visível especificado no UI-SPEC.

    Não remover nem alterar os testes de CRUD existentes (criar/editar/deletar) — apenas adicionar.
  </action>
  <verify>
    <automated>npx playwright test e2e/crud-edificios.spec.js --list 2>&1 | grep -v '^#' | grep -E -c 'Ver|ocupa|card|drill|lockEdificio|stats|accordion'</automated>
  </verify>
  <acceptance_criteria>
    - O spec lista (via `--list`) os novos testes para cards/stats, barra de ocupação, accordion e drill-in/lockEdificio.
    - Os testes de CRUD pré-existentes permanecem no arquivo (criar/editar/deletar).
    - `npx playwright test e2e/crud-edificios.spec.js --list` executa sem erro de parse.
    - Os novos testes referenciam strings do Copywriting Contract (ex.: "Ver N unidade(s)", legenda de ocupação).
  </acceptance_criteria>
  <done>Spec estendido lista as novas asserções de EDIF-01..03; testes de CRUD intactos; RED esperado até Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action (editarUnidade) | Save do modal de unidade cruza para o backend; ownership já validado por authGuard + proprietario_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | `lockEdificio` no FSelect | accept | `lockEdificio` é UI-only; mesmo se um usuário forjar uma requisição alterando `edificio_id`, a Server Action `editarUnidade` revalida `proprietario_id` na cadeia de propriedade (já securizado na Phase 15/19). Sem nova superfície de ataque. |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote instalado nesta fase (RESEARCH "No New Packages Required"). N/A. |
</threat_model>

<verification>
- `npx playwright test e2e/crud-edificios.spec.js --list` executa sem erro de parse.
- `npx eslint src/components/ui/UnifiedUnidadeModal.js` sem erros.
- Call-site de `Unidades.js` inalterado (default `false`).
</verification>

<success_criteria>
- `UnifiedUnidadeModal` aceita `lockEdificio` (default false) e desabilita/filtra o FSelect de edifício quando true.
- `e2e/crud-edificios.spec.js` estendido com asserções para EDIF-01..03 (RED esperado, será GREEN após Plan 02).
</success_criteria>

<output>
Create `.planning/phases/20-edif-cios-cards-drill-in/20-01-SUMMARY.md` when done
</output>
