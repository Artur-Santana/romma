---
phase: 04-polimento-visual-publico
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/LocatariosDesktop.js
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "Proprietário vê botão 'Editar' em cada linha de locatário ativo (não pendente) em LocatariosDesktop.js"
    - "Clicar em 'Editar' abre modal com campos: nome_razao_social, tipo (PF/PJ), documento, email, telefone — pré-preenchidos com dados do locatário"
    - "Salvar chama a Server Action editarLocatario(id, formEdit) e fecha o modal em caso de sucesso (status 200)"
    - "Erro de servidor exibido no modal usando erroMessage (português) da Server Action"
    - "LocatariosDesktop.js declara editandoId e formEdit como estado (confirmado por grep)"
  artifacts:
    - path: "src/components/features/LocatariosDesktop.js"
      provides: "Edição de locatário: estado + handlers + modal com form shadcn"
      contains: "editandoId"
  key_links:
    - from: "src/components/features/LocatariosDesktop.js"
      to: "src/actions/locatarios.js"
      via: "import editarLocatario + call em handleSalvarLocatario"
      pattern: "editarLocatario"
    - from: "LocatariosDesktop.js state"
      to: "modal de edição"
      via: "editandoId !== null → renderizar modal"
      pattern: "editandoId"
---

<objective>
Implementar edição de locatário em `src/components/features/LocatariosDesktop.js`.

**ALERTA DE ESCOPO — D-09 foi subestimado no CONTEXT.md:**
O CONTEXT.md afirma "só falta o botão trigger". O RESEARCH.md confirmou via grep (zero matches) que `editandoId`, `formEdit` e `handleEditarLocatario` NÃO existem em LocatariosDesktop.js. O escopo real é ~50-80 linhas novas: estado + handlers + modal completo copiando o padrão do modal de convite já existente no mesmo arquivo.

**MVP Deviation:** Este plano cobre o leftover D-09 (UAT Fase 3), não o slice de visitante (ver 04-01 para o slice).

Purpose: Locatário pode ser editado pelo Proprietário no dashboard — funcionalidade ausente desde a Fase 3 (defeito identificado em UAT).
Output: LocatariosDesktop.js com editandoId state + formEdit object + handleEditarLocatario + handleSalvarLocatario + modal de edição.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-polimento-visual-p-blico/04-CONTEXT.md
@.planning/phases/04-polimento-visual-p-blico/04-RESEARCH.md
@.planning/phases/04-polimento-visual-p-blico/04-PATTERNS.md
@.planning/phases/04-polimento-visual-p-blico/04-UI-SPEC.md

<interfaces>
<!-- Contratos extraídos de PATTERNS.md e da codebase. Leitura OBRIGATÓRIA antes de implementar. -->

De src/actions/locatarios.js — Server Action existente e correto (não alterar):
  editarLocatario(id, { nome_razao_social, tipo, documento, email, telefone })
    → Promise<{ status: 200 }> | Promise<{ status: 4xx|5xx, erroMessage: string }>
  Já faz: auth guard, UUID validation, whitelist de campos, supabaseAdmin update
  Import atual em LocatariosDesktop.js: { convidarLocatario, revogarConvite } de "@/actions/locatarios"
  → Adicionar editarLocatario ao import existente

Estado a adicionar (seguindo padrão de Unidades.js linhas 16, 29-36):
  const [editandoId, setEditandoId] = useState(null)
  const [formEdit, setFormEdit] = useState({
    nome_razao_social: '', tipo: 'pf', documento: '', email: '', telefone: ''
  })

Handlers a implementar (baseados em Unidades.js linhas 51-83):
  function handleEditarLocatario(locatario) {
    setFormEdit({ nome_razao_social: locatario.nome_razao_social, tipo: locatario.tipo,
      documento: locatario.documento, email: locatario.email, telefone: locatario.telefone })
    setEditandoId(locatario.id)
  }

  async function handleSalvarLocatario() {
    setLoading(true); setErro('')
    const { status, erroMessage } = await editarLocatario(editandoId, formEdit)
    setLoading(false)
    if (status === 200) { setEditandoId(null); setLocatarios(await getLocatarios() ?? []) }
    else { setErro(erroMessage ?? 'Erro ao salvar locatário.') }
  }

Botão trigger (linhas da tabela — inserir junto aos outros botões de ação):
  Exibir apenas quando !isPendente (locatário aceito)
  Button variant="ghost" size="sm" onClick={() => handleEditarLocatario(l)}
  className="font-mono text-[10px] text-fg-3 uppercase tracking-[0.5px] font-bold p-0 h-auto"
  Texto: "Editar"

Modal de edição (copiar estrutura do modal showInviteForm existente — linhas 168-262):
  Condição: editandoId !== null
  Heading: "Editar Locatário"
  Campos: nome_razao_social (Input text), tipo (Select: 'pf'/'pj'), documento (Input text), email (Input email), telefone (Input text)
  Botão salvar: "Salvar" → handleSalvarLocatario
  Botão cancelar: "Cancelar" → setEditandoId(null)
  Erro: erroMessage em text-danger-fg text-[11px] font-mono

shadcn disponíveis (já importados em LocatariosDesktop.js ou adicionar):
  Button, Input, Select, SelectContent, SelectItem, SelectTrigger, SelectValue

Field helper já existe em LocatariosDesktop.js (reutilizar):
  function Field({ label, required, children }) — ver linhas 267-276

Nota: 'erroMessage' (português) — nunca 'errorMessage'. Padrão de todo o projeto.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar estado de edição + handlers em LocatariosDesktop.js</name>
  <files>
    src/components/features/LocatariosDesktop.js
  </files>
  <read_first>
    - src/components/features/LocatariosDesktop.js — ler COMPLETO antes de qualquer edição; mapa de todas as linhas: useState existentes (~31-34), modal de convite (linhas ~168-262), linha da tabela com botões (~97-150), Field helper (~267-276)
    - src/components/features/Unidades.js — linhas 16 e 29-83 para padrão exato de editandoId + formEdit + handlers
    - src/actions/locatarios.js — ver assinatura de editarLocatario e imports atuais
    - .planning/phases/04-polimento-visual-p-blico/04-PATTERNS.md — seção `src/components/features/LocatariosDesktop.js` com o código exato a implementar
  </read_first>
  <action>
**ATENÇÃO:** Confirmar via leitura que editandoId e formEdit NÃO existem ainda em LocatariosDesktop.js antes de editar (conforme RESEARCH.md — zero matches em grep anterior).

**1. Adicionar editarLocatario ao import:**
Localizar o import de actions (linha ~7): `import { convidarLocatario, revogarConvite } from "@/actions/locatarios"`.
Adicionar `editarLocatario`: `import { convidarLocatario, revogarConvite, editarLocatario } from "@/actions/locatarios"`.

**2. Adicionar estado após os useState existentes (linha ~34):**
Inserir junto com os outros useState do componente:
```
const [editandoId, setEditandoId] = useState(null)
const [formEdit, setFormEdit] = useState({
  nome_razao_social: '', tipo: 'pf', documento: '', email: '', telefone: ''
})
```

**3. Adicionar handlers após os handlers existentes:**
Implementar `handleEditarLocatario(locatario)` e `async handleSalvarLocatario()` conforme o padrão exato de Unidades.js (linhas 51-83) adaptado para locatários. Ver seção de interfaces acima para o código exato. `erroMessage` (português) obrigatório.

**4. Adicionar botão "Editar" na linha da tabela:**
Localizar o botão de ação "VER →" ou similar por locatário ativo. Adicionar `Button` com "Editar" que chama `handleEditarLocatario(l)`. Exibir apenas quando `!isPendente` (locatário com convite aceito). Posicionar consistente com os outros botões de ação existentes na linha.

**5. Adicionar modal de edição após o modal de convite:**
Copiar a estrutura do modal `showInviteForm` (linhas ~168-262) e adaptar:
- Condição de exibição: `editandoId !== null`
- Heading: "Editar Locatário"
- Campos usando o Field helper existente: nome_razao_social (Input text), tipo (Select com opções 'pf'/'pj'), documento (Input text), email (Input email), telefone (Input text)
- Campos pré-preenchidos com `formEdit.{campo}`, onChange atualiza `setFormEdit({ ...formEdit, campo: valor })`
- Botão "Salvar" → `handleSalvarLocatario()` (disabled enquanto loading)
- Botão "Cancelar" → `setEditandoId(null)`
- Exibir `erro` de `erroMessage` em `text-danger-fg text-[11px] font-mono`
- Reutilizar classes de overlay e container do modal de convite existente para consistência visual

Não alterar nada no modal de convite existente nem em outras partes do componente além das adições listadas acima.
  </action>
  <verify>
    <automated>
      grep -c 'editandoId' src/components/features/LocatariosDesktop.js
      # esperado: >= 3 (declaração useState + uso no handler + condição do modal)
      grep -c 'formEdit' src/components/features/LocatariosDesktop.js
      # esperado: >= 3 (declaração + uso em handleEditar + campos do form)
      grep -c 'editarLocatario' src/components/features/LocatariosDesktop.js
      # esperado: >= 2 (import + call em handleSalvar)
      grep -c 'erroMessage' src/components/features/LocatariosDesktop.js
      # esperado: >= 1 (uso no handler de erro — nunca 'errorMessage')
      npm run lint 2>&amp;1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'editandoId' src/components/features/LocatariosDesktop.js` → >= 3
    - `grep -c 'formEdit' src/components/features/LocatariosDesktop.js` → >= 3
    - `grep -c 'editarLocatario' src/components/features/LocatariosDesktop.js` → >= 2 (import + call)
    - `grep -c 'erroMessage' src/components/features/LocatariosDesktop.js` → >= 1 (nunca 'errorMessage')
    - `grep -c "'Editar'" src/components/features/LocatariosDesktop.js` → >= 1 (botão trigger)
    - `grep -c "'Editar Locatário'" src/components/features/LocatariosDesktop.js` → >= 1 (heading do modal)
    - Modal com campos nome_razao_social, tipo, documento, email, telefone presente no arquivo
    - `npm run lint` não reporta erros para LocatariosDesktop.js
    - Modal de convite existente (`showInviteForm`) permanece inalterado (verificar grep do heading existente do modal de convite)
  </acceptance_criteria>
  <done>LocatariosDesktop.js com edição completa de locatário: editandoId + formEdit + handleEditarLocatario + handleSalvarLocatario + botão trigger + modal de edição com campos pré-preenchidos + tratamento de erro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Dashboard (autenticado) → editarLocatario | Server Action requer sessão de Proprietário ativo |
| formEdit (client) → Server Action | Dados do formulário trafegam via Server Action — não via fetch manual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | editarLocatario Server Action — campo whitelist | mitigate | Server Action já implementa whitelist explícita: aceita apenas `{ nome_razao_social, tipo, documento, email, telefone }`. Cliente DEVE replicar exatamente esses 5 campos em formEdit — não adicionar id ou usuario_id ao objeto formEdit passado ao handler. Validação UUID existente no server bloqueia tentativas de IDOR. |
| T-04-04-02 | Elevation of Privilege | editarLocatario — auth guard | accept | Server Action já tem authGuard local que verifica sessão + `is_proprietario` RPC. Sem mudança necessária no lado servidor. |
| T-04-04-03 | Information Disclosure | erro exibido no modal | accept | Mensagem de erro vem de erroMessage do Server Action (controlada pelo servidor). Sem stack trace ou dados de banco expostos ao cliente. |
</threat_model>

<verification>
Verificação completa do plano 04-04:

```bash
# Estado de edição implementado
grep -c 'editandoId' src/components/features/LocatariosDesktop.js
# esperado: >= 3

grep -c 'formEdit' src/components/features/LocatariosDesktop.js
# esperado: >= 3

# Server Action importada
grep -c 'editarLocatario' src/components/features/LocatariosDesktop.js
# esperado: >= 2

# Typo de erroMessage não introduzido
grep -c 'errorMessage' src/components/features/LocatariosDesktop.js
# esperado: 0 (a grafia correta é erroMessage)

# Lint
npm run lint
```
</verification>

<success_criteria>
1. LocatariosDesktop.js declara `const [editandoId, setEditandoId] = useState(null)` e `const [formEdit, setFormEdit] = useState({...})`
2. Botão "Editar" aparece em linhas de locatários com convite aceito (não pendentes)
3. Modal de edição exibe campos pré-preenchidos: nome_razao_social, tipo, documento, email, telefone
4. handleSalvarLocatario chama `editarLocatario(editandoId, formEdit)` e fecha modal em status 200
5. Erro de servidor exibido usando `erroMessage` (não `errorMessage`) da Server Action
6. Modal de convite existente não alterado
7. npm run lint passa sem novos erros
</success_criteria>

<output>
Criar `.planning/phases/04-polimento-visual-p-blico/04-04-SUMMARY.md` quando concluído.
</output>
