---
phase: 05-testes-e2e
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/app/dashboard/edificios/page.js
  - e2e/seed.mjs
  - e2e/global-teardown.js
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-04
tags:
  - testing
  - e2e
  - playwright
  - infrastructure

must_haves:
  truths:
    - "Rota /dashboard/edificios carrega o componente GestaoEdificios sem 404"
    - "Seed cria uma unidade chamada 'E2E-Sala Disponivel' com status disponivel e sem contrato"
    - "global-teardown remove edificios/unidades/locatarios cujos nomes começam com 'E2E-' e usuários auth com email começando em 'e2e-'"
  artifacts:
    - path: "src/app/dashboard/edificios/page.js"
      provides: "Rota dashboard para CRUD de Edifícios"
      contains: "GestaoEdificios"
    - path: "e2e/seed.mjs"
      provides: "Unidade extra 'E2E-Sala Disponivel' para TEST-04"
      contains: "E2E-Sala Disponivel"
    - path: "e2e/global-teardown.js"
      provides: "Limpeza por prefixo E2E- e emails e2e-"
      contains: "like('nome', 'E2E-%')"
  key_links:
    - from: "src/app/dashboard/edificios/page.js"
      to: "src/components/features/GestaoEdificios.js"
      via: "default import"
      pattern: "import GestaoEdificios"
    - from: "e2e/global-teardown.js"
      to: "supabase.auth.admin.listUsers"
      via: "filter por email começando em e2e-"
      pattern: "startsWith\\('e2e-'\\)"
---

<objective>
Wave 0 da Fase 5: criar a infraestrutura bloqueante que viabiliza os specs de TEST-01, TEST-02 e TEST-04. Sem esta wave, o spec de Edifícios não consegue navegar até `/dashboard/edificios` (rota não existe), TEST-04 não tem unidade disponível dedicada para testar (D-08) e o teardown não limpa os dados criados pelos specs (D-01, D-04).

Purpose: Habilitar todos os specs subsequentes a rodar de forma idempotente e isolada do seed principal, sem dependência de dados frágeis.
Output: Rota `/dashboard/edificios` + seed estendido com unidade `E2E-Sala Disponivel` + teardown estendido com limpeza por prefixo `E2E-` e emails `e2e-`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-testes-e2e/05-CONTEXT.md
@.planning/phases/05-testes-e2e/05-RESEARCH.md
@.planning/phases/05-testes-e2e/05-PATTERNS.md
@.planning/phases/05-testes-e2e/05-VALIDATION.md
@CLAUDE.md
@src/app/dashboard/unidades/page.js
@src/components/features/GestaoEdificios.js
@e2e/seed.mjs
@e2e/global-teardown.js

<interfaces>
<!-- Contratos chave que o executor precisa para esta wave. -->

src/app/dashboard/unidades/page.js (analog exato para o wrapper a criar):
- Server Component thin wrapper
- import default do componente em `@/components/features/...`
- export default function ...Page() retornando <Componente />

src/components/features/GestaoEdificios.js:
- export default function GestaoEdificios({})
- form com inputs placeholder "Nome do edificio" e "Endereço"
- button type="submit" com texto "Enviar"
- erro renderizado como `<p>ERRO!: {erro}</p>`
- usa EdificioCard que renderiza botões "Editar" e "Remover"

e2e/seed.mjs:
- usa `admin` (createClient com SUPABASE_ROLE_KEY)
- função seed() exportada
- após inserir 'Sala 101' (linhas 44-56), retorna `{ edificioId, unidadeId }`
- chamada por global-setup.js que persiste retorno em .e2e-state.json

e2e/global-teardown.js:
- export default async function globalTeardown()
- já limpa por usuario_id do locatario@test.romma.local e por ID em .e2e-state.json
- usa `admin` com SUPABASE_ROLE_KEY
- ordem de cascata FK obrigatória: parcelas → contratos → locatarios → unidades → edificios
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar rota dashboard de Edifícios</name>
  <files>src/app/dashboard/edificios/page.js</files>
  <read_first>
    - src/app/dashboard/unidades/page.js (analog exato — copiar estrutura linha-a-linha)
    - src/components/features/GestaoEdificios.js (para confirmar o nome do default export)
  </read_first>
  <action>Criar `src/app/dashboard/edificios/page.js` como Server Component thin wrapper seguindo exatamente o padrão de `src/app/dashboard/unidades/page.js`. Importar o default export `GestaoEdificios` do alias `@/components/features/GestaoEdificios` e renderizá-lo dentro de uma função exportada como default chamada `EdificiosPage`. Não adicionar `'use client'` (o componente filho já declara). Sem props, sem layout adicional — wrapper de 5 linhas idêntico em estrutura ao analog. Resolve o BLOQUEANTE identificado no RESEARCH (linha 162) e habilita os specs de Edifícios em TEST-01.</action>
  <verify>
    <automated>test -f src/app/dashboard/edificios/page.js && grep -q "import GestaoEdificios" src/app/dashboard/edificios/page.js && grep -q "export default function" src/app/dashboard/edificios/page.js</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/app/dashboard/edificios/page.js` existe
    - Contém `import GestaoEdificios from "@/components/features/GestaoEdificios"`
    - Exporta default uma função (assinatura `export default function EdificiosPage`)
    - Função retorna `<GestaoEdificios />`
    - Arquivo NÃO contém a string `'use client'`
    - `npm run build` (ou `npm run lint` se build for caro) passa sem erro de import
  </acceptance_criteria>
  <done>Rota `/dashboard/edificios` resolve em dev sem 404 e renderiza o formulário do componente `GestaoEdificios`.</done>
</task>

<task type="auto">
  <name>Task 2: Estender seed.mjs com unidade E2E-Sala Disponivel</name>
  <files>e2e/seed.mjs</files>
  <read_first>
    - e2e/seed.mjs (estado atual — entender ponto de inserção após a `Sala 101`)
    - .planning/phases/05-testes-e2e/05-PATTERNS.md (seção `e2e/seed.mjs (modificar)` — bloco exato de insert a adicionar)
    - .planning/phases/05-testes-e2e/05-RESEARCH.md (seção "Adicionar 'E2E-Sala Disponivel' ao seed.mjs")
  </read_first>
  <action>Após o bloco que insere `Sala 101` (linhas 44-56 do estado atual, terminando em `if (errUnidade) throw errUnidade`) e antes do bloco de limpeza/insert do locatário (linha 59 em diante), adicionar um novo insert na tabela `unidades` com os campos: `edificio_id: edificio.id`, `nome: 'E2E-Sala Disponivel'` (prefixo D-01 — resolve conflito D-08 conforme RESEARCH linha 32), `area_m2: 30`, `valor_mensal: 1500`, `valor_visivel: true`, `status: 'disponivel'`. Capturar resultado em const `unidadeE2E` com `.select().single()`, propagar erro via `if (errUniE2E) throw errUniE2E`. Esta unidade é dedicada ao TEST-04 e NÃO deve receber contrato no seed. Não modificar o return de `seed()` (teardown limpa via prefixo "E2E-%"). Manter convenção de aspas simples e indentação de 2 espaços do arquivo existente.</action>
  <verify>
    <automated>grep -q "E2E-Sala Disponivel" e2e/seed.mjs && grep -B2 -A8 "E2E-Sala Disponivel" e2e/seed.mjs | grep -q "status: 'disponivel'" && node -e "import('./e2e/seed.mjs').then(m => console.log(typeof m.seed))" 2>&1 | grep -q function</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/seed.mjs` contém literal `'E2E-Sala Disponivel'`
    - O insert para essa unidade tem `status: 'disponivel'` (verificável por grep no contexto adjacente)
    - O insert tem `valor_visivel: true`
    - O insert usa `edificio_id: edificio.id` (mesmo edificio do seed)
    - Arquivo importa sem erro (ESM valida com `node --check` ou import dinâmico)
    - Executar `node e2e/seed.mjs` (com `.env.test` apontando para localhost:54321 e Supabase rodando) cria a unidade e imprime `seed ok`
  </acceptance_criteria>
  <done>Após rodar o seed, a query `select id from unidades where nome = 'E2E-Sala Disponivel'` retorna exatamente 1 linha com `status = 'disponivel'`.</done>
</task>

<task type="auto">
  <name>Task 3: Estender global-teardown.js com limpeza por prefixo E2E- e emails e2e-</name>
  <files>e2e/global-teardown.js</files>
  <read_first>
    - e2e/global-teardown.js (estado atual — ponto de inserção é após linha 72, antes do fim da função)
    - .planning/phases/05-testes-e2e/05-PATTERNS.md (seção `e2e/global-teardown.js (modificar)` — bloco completo de extensão)
    - .planning/phases/05-testes-e2e/05-RESEARCH.md (seção "Ampliar global-teardown.js com limpeza E2E-")
  </read_first>
  <action>Adicionar três blocos novos ao final da função `globalTeardown` exportada como default, antes do fechamento da função, sem remover nem reordenar a lógica existente. Bloco A: buscar `edificios` via `.like('nome', 'E2E-%')`, coletar IDs, então em cascata FK obrigatória (parcelas → contratos → unidades → edificios) deletar tudo encadeado por `unidade_id` e `edificio_id`. Bloco B: buscar `locatarios` via `.like('nome_razao_social', 'E2E-%')`, coletar IDs, em cascata FK deletar parcelas e contratos do locatário antes do próprio locatario. Bloco C: chamar `admin.auth.admin.listUsers()`, filtrar usuários cujo `email` começa com `'e2e-'` (D-04) e chamar `admin.auth.admin.deleteUser(u.id)` para cada um em loop sequencial. Implementar tolerância: cada bloco verifica array vazio antes de tentar delete (`if (ids.length)` guard). Ordem dos blocos: A, B, C. Reusar a const `admin` já declarada no topo do arquivo. Manter o `existsSync(STATE_FILE)` + cleanup por ID intactos.</action>
  <verify>
    <automated>grep -q "like('nome', 'E2E-%')" e2e/global-teardown.js && grep -q "like('nome_razao_social', 'E2E-%')" e2e/global-teardown.js && grep -q "startsWith('e2e-')" e2e/global-teardown.js && grep -q "auth.admin.deleteUser" e2e/global-teardown.js && node --check e2e/global-teardown.js</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo contém `like('nome', 'E2E-%')` aplicado em `from('edificios')`
    - Arquivo contém `like('nome_razao_social', 'E2E-%')` aplicado em `from('locatarios')`
    - Arquivo contém `admin.auth.admin.listUsers()` seguido de filtro por `email?.startsWith('e2e-')`
    - Arquivo contém pelo menos uma chamada a `admin.auth.admin.deleteUser(`
    - Ordem de delete em cada cascata é: parcelas (`from('parcelas').delete()`) → contratos → unidades → edificios; verificável visualmente — não há `from('edificios').delete()` antes de `from('unidades').delete()` dentro do mesmo bloco
    - `node --check e2e/global-teardown.js` exit code 0
    - O bloco original (teardown por usuario_id do locatario@test.romma.local + STATE_FILE) permanece presente e funcional (grep `locatario@test.romma.local` ainda retorna match)
    - Inserir manualmente uma `edificios` row com `nome='E2E-Teardown Test'`, rodar `node -e "import('./e2e/global-teardown.js').then(m => m.default())"` e confirmar deleção (smoke local)
  </acceptance_criteria>
  <done>Após rodar a suíte E2E, queries `select count(*) from edificios where nome like 'E2E-%'` e `select count(*) from auth.users where email like 'e2e-%'` retornam 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → Supabase local | Service role key (SUPABASE_ROLE_KEY) usada em seed/teardown via .env.test |
| Dev workstation → Supabase produção | NUNCA atravessada: .env.test aponta para `http://127.0.0.1:54321`; seed.mjs tem guard que aborta se URL não contém "test"/"local"/"127.0.0.1" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | e2e/seed.mjs apontar para Supabase prod por engano | mitigate | Guard existente (linhas 142-145) verifica URL contém "test"/"local"/"127.0.0.1" antes de rodar; não modificar |
| T-05-02 | Information Disclosure | SUPABASE_ROLE_KEY em logs/CI | accept | Chave fica em .env.test (gitignored); rodada apenas em ambiente local |
| T-05-03 | Denial of Service | teardown deletar dados produtivos por filtro mal escrito | mitigate | Filtros usam prefixo literal "E2E-%" e "e2e-"; nomes normais do seed (`Sala 101`, `Edifício Teste E2E`, `Locatário Teste`) NÃO matcheiam o pattern |
| T-05-SC | Tampering | npm installs | accept | Esta wave não instala pacotes — sem mudança em package.json |
</threat_model>

<verification>
- `npm run lint` passa sem novos warnings/erros
- `node --check e2e/seed.mjs` e `node --check e2e/global-teardown.js` exit 0
- Rodar `node e2e/seed.mjs` cria a unidade `E2E-Sala Disponivel`
- Rodar suíte existente (`npx playwright test --grep "@smoke"`) ainda passa — extensão de seed/teardown não quebra specs antigos
- Navegar manualmente para `/dashboard/edificios` (logado como Proprietário) renderiza o formulário do `GestaoEdificios`
</verification>

<success_criteria>
1. Rota `/dashboard/edificios` resolve sem 404 e renderiza `GestaoEdificios`
2. Seed insere `E2E-Sala Disponivel` com `status = 'disponivel'`
3. Teardown remove todas as entidades com prefixo `E2E-` e usuários com email iniciando em `e2e-`
4. Suíte E2E existente (`@smoke`, portal.spec.js, dashboard.spec.js, auth-redirect.spec.js, auth-session.spec.js, server-actions.spec.js) continua verde
</success_criteria>

<output>
Criar `.planning/phases/05-testes-e2e/05-01-SUMMARY.md` ao concluir, registrando: artefatos criados, comandos executados para validar, e quaisquer ajustes nos seletores/IDs observados.
</output>
