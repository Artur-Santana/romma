---
phase: 05-testes-e2e
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - e2e/crud.spec.js
autonomous: true
requirements:
  - TEST-01
tags:
  - testing
  - e2e
  - playwright
  - crud

must_haves:
  truths:
    - "Spec roda criar/editar/deletar de Edifício end-to-end (Proprietário logado, UI real)"
    - "Spec roda criar/editar/deletar de Unidade incluindo shadcn Select de Edifício via click pattern"
    - "Spec roda convidar Locatário (email dinâmico e2e-${Date.now()}) e editar — confirma aparecimento na lista após submit"
    - "Spec roda criar Contrato (com gerarParcelas automático), cancelar via ConfirmDialog, e encerrar contrato com data_fim no passado (manipulada via supabaseAdmin)"
    - "Após encerrar/cancelar contrato, status da unidade volta a 'disponivel' (regra de negócio verificada)"
  artifacts:
    - path: "e2e/crud.spec.js"
      provides: "Cobertura completa de TEST-01 — CRUD Proprietário em 4 entidades"
      contains: "test.describe('TEST-01"
      min_lines: 200
  key_links:
    - from: "e2e/crud.spec.js"
      to: "/dashboard/edificios"
      via: "page.goto"
      pattern: "/dashboard/edificios"
    - from: "e2e/crud.spec.js"
      to: "shadcn Select Radix"
      via: "getByRole('combobox') + getByRole('option')"
      pattern: "getByRole\\('combobox'\\)"
    - from: "e2e/crud.spec.js"
      to: "ConfirmDialog Contratos"
      via: "waitFor text 'Cancelar contrato?'"
      pattern: "Cancelar contrato\\?"
---

<objective>
Slice vertical completo de TEST-01: um único spec `e2e/crud.spec.js` cobrindo CRUD do Proprietário em todas as 4 entidades (Edifícios, Unidades, Locatários, Contratos), incluindo validação da regra de negócio que devolve a unidade ao status `disponivel` ao encerrar/cancelar contrato.

User story (do roadmap): **As a** Proprietário, **I want to** ter uma suíte automatizada que valida criar/editar/remover Edifícios, Unidades, Locatários (convidar/editar) e Contratos (criar/encerrar/cancelar), **so that** eu posso refatorar e fazer deploy com confiança antes da banca.

Purpose: Garantir que toda a área administrativa do Proprietário tem regressão automatizada antes do deploy final (Fase 6). Sem este spec, TEST-01 não é coberto e mudanças posteriores podem quebrar fluxos críticos silenciosamente.
Output: `e2e/crud.spec.js` verde, organizado em 4 `test.describe` aninhados (um por entidade), todos rodando com viewport desktop 1440x900 e padrão `beforeEach: login + waitForURL`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-testes-e2e/05-CONTEXT.md
@.planning/phases/05-testes-e2e/05-RESEARCH.md
@.planning/phases/05-testes-e2e/05-PATTERNS.md
@.planning/phases/05-testes-e2e/05-01-SUMMARY.md
@CLAUDE.md
@e2e/dashboard.spec.js
@e2e/portal.spec.js
@e2e/helpers.js
@e2e/fixtures.js
@src/components/features/GestaoEdificios.js
@src/components/features/Unidades.js
@src/components/features/Locatarios.js
@src/components/features/Contratos.js

<interfaces>
<!-- Contratos extraídos dos componentes — usar exatamente. -->

e2e/helpers.js:
- export async function login(page, { email, password })
- preenche email/password e clica submit; CALLER chama page.waitForURL após

e2e/fixtures.js:
- export const PROPRIETARIO = { email: 'proprietario@test.romma.local', password: 'Test1234!' }

GestaoEdificios (rota: /dashboard/edificios):
- form com input placeholder "Nome do edificio", input placeholder "Endereço"
- button type="submit" texto "Enviar" (NÃO "Criar Edifício")
- itens listados via EdificioCard com botões "Editar" e "Remover"
- erro: <p>ERRO!: {erro}</p>

Unidades (rota: /dashboard/unidades) — shadcn Select para Edifício:
- botão "Nova Unidade" abre formulário
- combobox índice 0 = Edifício; options pelo nome do edificio
- inputs placeholder "Nome da unidade", "Área (m²)", "Valor mensal (R$)"
- botão submit "Criar Unidade"
- UnidadeCard com botões "Editar"/"Deletar"

Locatarios (rota: /dashboard/locatarios) — HTML nativo:
- input placeholder "Nome"
- <select> nativo para tipo pf/pj (page.selectOption funciona)
- input placeholder "Documento"
- input type="email"
- input placeholder "Telefone " (note: espaço extra no placeholder)
- button type="submit"
- após submit, locatário aparece na lista; sem toast de sucesso
- botão "Editar" por linha

Contratos (rota: /dashboard/contratos) — shadcn Select + ConfirmDialog:
- botão "Novo Contrato" abre formulário
- combobox 0 = Locatário, combobox 1 = Unidade disponível
- input[type="date"] [0] = data_inicio, [1] = data_fim
- botão "Criar Contrato"
- handleCriarContrato chama gerarParcelas AUTOMATICAMENTE (sem botão extra)
- tabela mostra contratos; botões "CANC" (ativo não-vencido) e "ENC" (ativo vencido)
- ConfirmDialog: aguardar texto "Cancelar contrato?" / "Encerrar contrato?" antes de clicar botão confirmador "Cancelar Contrato" / "Encerrar"

supabaseAdmin (para setar data_fim no passado em teste de encerrar):
- import { createClient } from '@supabase/supabase-js'
- import { config } from 'dotenv'; config({ path: '.env.test' })
- admin = createClient(NEXT_PUBLIC_SUPABASE_URL, SUPABASE_ROLE_KEY, { auth: { autoRefreshToken: false, persistSession: false } })

Padrão de assertion: await expect(locator).toBeVisible({ timeout: 10_000 }) — usar 15_000 quando envolve gerarParcelas (criar contrato)

Anti-pattern: page.getByRole('button', { name: 'Editar' }).click() sem âncora — SEMPRE ancorar via getByText('E2E-...').locator('..').getByRole('button', { name: 'Editar' })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spec CRUD Edifícios + Unidades</name>
  <files>e2e/crud.spec.js</files>
  <read_first>
    - e2e/dashboard.spec.js (analog principal de estrutura)
    - e2e/portal.spec.js (analog de describes aninhados)
    - .planning/phases/05-testes-e2e/05-RESEARCH.md (seções "Selector Map — Edifícios" e "Selector Map — Unidades")
    - .planning/phases/05-testes-e2e/05-PATTERNS.md (seção `e2e/crud.spec.js` — estrutura recomendada)
    - src/components/features/GestaoEdificios.js (confirmar botão submit "Enviar" e botões "Editar"/"Remover")
    - src/components/features/Unidades.js (confirmar shadcn Select e placeholders)
    - .planning/phases/05-testes-e2e/05-01-SUMMARY.md (confirmar que rota /dashboard/edificios existe)
  </read_first>
  <behavior>
    - Edifícios.criar: preencher "Nome do edificio"='E2E-Edifício Alpha' + "Endereço"='Rua E2E, 1' + clicar "Enviar" → texto 'E2E-Edifício Alpha' fica visível em 10s
    - Edifícios.editar: ancorar em 'E2E-Edifício Alpha', clicar "Editar", trocar nome para 'E2E-Edifício Alpha Editado', clicar "Salvar" → novo nome visível
    - Edifícios.deletar: ancorar em 'E2E-Edifício Alpha Editado', clicar "Remover" → toHaveCount(0)
    - Unidades.criar: clicar "Nova Unidade", clicar combobox[0], selecionar option 'Edifício Teste E2E' (do seed), preencher nome='E2E-Sala 301', área='50', valor='3000', clicar "Criar Unidade" → 'E2E-Sala 301' visível
    - Unidades.editar: ancorar em 'E2E-Sala 301', clicar "Editar", trocar para 'E2E-Sala 301 Editada', "Salvar" → novo nome visível
    - Unidades.deletar: ancorar em 'E2E-Sala 301 Editada', clicar "Deletar" → toHaveCount(0)
  </behavior>
  <action>Criar `e2e/crud.spec.js` com imports `{ test, expect }` de `@playwright/test`, `login` de `./helpers.js`, `PROPRIETARIO` de `./fixtures.js`. Envelope externo: `test.describe('TEST-01 — CRUD Proprietário', () => { test.use({ viewport: { width: 1440, height: 900 } }); ... })`. Aninhar dois `test.describe` internos por enquanto: `'Edifícios'` (com beforeEach que loga e navega para `/dashboard/edificios`) e `'Unidades'` (com beforeEach navegando para `/dashboard/unidades`). Cada describe contém 3 testes: criar, editar, deletar — todos usando prefixo `'E2E-'` (D-01) nos nomes para que o teardown limpe automaticamente. Para Unidades.criar, usar `page.getByRole('combobox').first().click()` seguido de `page.getByRole('option', { name: 'Edifício Teste E2E' }).click()` (D-08 — referência ao edificio do seed). Usar `getByText('E2E-...').locator('..').getByRole('button', { name: '...' })` para ancorar todos os cliques em botões de Editar/Remover/Deletar/Salvar (anti-pitfall 7 do RESEARCH). Todas as assertions usam `toBeVisible({ timeout: 10_000 })` ou `toHaveCount(0)`. NÃO usar `page.selectOption()` para o combobox de Edifício (shadcn Radix — pitfall 1). Confirmar via leitura prévia do componente que o submit de Edifícios é texto "Enviar" (não "Criar Edifício") e o submit de edição é "Salvar" (renderizado por EdificioCard).</action>
  <verify>
    <automated>npx playwright test e2e/crud.spec.js --grep "Edifícios|Unidades" --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `e2e/crud.spec.js` existe e exporta describes via test API do Playwright
    - Contém `test.describe('Edifícios'` e `test.describe('Unidades'`
    - Contém ao menos 6 testes (3 por entidade)
    - Nenhum uso de `page.selectOption(` apontando para o combobox de Edifício (pode usar para `<select>` nativo no futuro)
    - Cada test usa âncora `getByText('E2E-...').locator('..')` antes de cliques em Editar/Remover/Salvar/Deletar
    - `npx playwright test e2e/crud.spec.js --grep "Edifícios"` passa 100%
    - `npx playwright test e2e/crud.spec.js --grep "Unidades"` passa 100%
    - Após execução, `select count(*) from edificios where nome like 'E2E-%'` retorna 0 (teardown limpou)
  </acceptance_criteria>
  <done>6 testes (Edifícios criar/editar/deletar + Unidades criar/editar/deletar) passam em sequência.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Spec CRUD Locatários + Contratos (criar/cancelar/encerrar) com regra de unidade</name>
  <files>e2e/crud.spec.js</files>
  <read_first>
    - e2e/crud.spec.js (resultado da Task 1 — estrutura já criada)
    - .planning/phases/05-testes-e2e/05-RESEARCH.md (seções "Selector Map — Locatários", "Selector Map — Contratos", "Pitfall 2: Encerrar contrato")
    - src/components/features/Locatarios.js (confirmar placeholder "Telefone " com espaço extra)
    - src/components/features/Contratos.js (confirmar texto dos botões "CANC", "ENC" e textos do ConfirmDialog)
    - e2e/seed.mjs (entender que dataset base inclui Locatário Teste, Sala 101 alugada, e — após Task 01 — E2E-Sala Disponivel)
  </read_first>
  <behavior>
    - Locatários.convidar: preencher Nome='E2E-Locatário Teste', selectOption tipo='pf', Documento='12345678901', email=`e2e-${Date.now()}@test.romma.local`, Telefone='11999999999' (placeholder com espaço extra), clicar submit → 'E2E-Locatário Teste' aparece na lista em 10s (D-03 reinterpretado: confirmação implícita via lista)
    - Locatários.editar: ancorar 'E2E-Locatário Teste', "Editar", renomear para 'E2E-Locatário Editado', "Salvar" → novo nome visível
    - Contratos.criar: criar pré-requisitos via supabaseAdmin no beforeAll do describe (edifício 'E2E-Edifício Contratos', unidade 'E2E-Sala Contrato' status disponivel, locatário 'E2E-Locatário Contratos' com auth user e2e-contratos@test.romma.local). Via UI: "Novo Contrato", combobox[0]→ 'E2E-Locatário Contratos', combobox[1]→ 'E2E-Sala Contrato', data_inicio='2026-06-01', data_fim='2027-06-01', "Criar Contrato" → 'E2E-Locatário Contratos' visível em tabela em 15s (gerarParcelas roda automático)
    - Contratos.cancelar: identificar a linha do contrato recém-criado, clicar "CANC", aguardar 'Cancelar contrato?', clicar "Cancelar Contrato" → 'cancelado' visível em 10s. Verificar via supabaseAdmin que a unidade voltou a `status='disponivel'`.
    - Contratos.encerrar: criar segundo contrato via UI (mesmo padrão), depois usar supabaseAdmin para setar `data_fim` = ontem (pitfall 2), recarregar página, clicar "ENC", aguardar 'Encerrar contrato?', clicar "Encerrar" → 'encerrado' visível. Verificar via supabaseAdmin que a unidade voltou a `status='disponivel'`.
  </behavior>
  <action>Adicionar dois novos `test.describe` no mesmo `e2e/crud.spec.js`: `'Locatários'` (beforeEach loga + `page.goto('/dashboard/locatarios')`) e `'Contratos'` (beforeEach loga + `page.goto('/dashboard/contratos')`). Em Locatários: 2 testes (convidar via formulário HTML nativo com `page.selectOption('select', 'pf')` para o select nativo e email dinâmico `e2e-${Date.now()}@test.romma.local`, depois editar). Em Contratos: criar `test.beforeAll` que importa supabaseAdmin (createClient com SUPABASE_ROLE_KEY) e cria a tripla edificio+unidade+locatario com prefixo `E2E-` (cadeia FK do RESEARCH). Para o locatário do contrato, criar auth user via `admin.auth.admin.createUser` (email `e2e-contratos@test.romma.local`, password 'Test1234!', email_confirm: true) e inserir em `locatarios` referenciando o usuario_id. Adicionar `test.afterAll` que zera contratos/parcelas/unidade/edificio/locatario (defensivo — teardown global já limpa por prefixo, mas afterAll garante ordem dos próximos testes no spec). Os 3 testes de Contratos: (1) criar → assertion `getByText('E2E-Locatário Contratos').toBeVisible({ timeout: 15_000 })` + verificar via admin que unidade ficou `alugada`; (2) cancelar via ConfirmDialog usando ancoragem `getByText('E2E-Locatário Contratos').locator('..').getByRole('button', { name: 'CANC' })` + `getByText('Cancelar contrato?').waitFor({ timeout: 5_000 })` + `getByRole('button', { name: 'Cancelar Contrato' }).click()` + assertion `expect(page.getByText('cancelado')).toBeVisible({ timeout: 10_000 })` + assertion via admin que unidade voltou a `disponivel`; (3) encerrar: criar contrato novo via UI, usar `admin.from('contratos').update({ data_fim: ontemISO }).eq('id', contratoId)` (capturar id via select por unidade/locatario) seguindo pitfall 2, recarregar página, repetir padrão ConfirmDialog com 'Encerrar contrato?'/'Encerrar', assertion 'encerrado' + unidade voltou `disponivel`. NÃO duplicar a const `admin` se já criada no escopo do arquivo — declarar no top-level abaixo dos imports. Manter convenção de aspas simples e indentação 2 espaços.</action>
  <verify>
    <automated>npx playwright test e2e/crud.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/crud.spec.js` contém `test.describe('Locatários'` e `test.describe('Contratos'`
    - Pelo menos 2 testes de Locatários e 3 de Contratos (criar, cancelar, encerrar)
    - Total mínimo de 11 testes no spec (6 da Task 1 + 5 da Task 2)
    - Contém `page.selectOption('select', 'pf')` (select nativo do form de Locatário)
    - Contém pelo menos uma chamada `e2e-${Date.now()}@test.romma.local` (D-04)
    - Contém texto `'Cancelar contrato?'` e `'Encerrar contrato?'` em `waitFor` ou `getByText`
    - Contém atualização de `data_fim` via supabaseAdmin antes do teste de encerrar (regex `admin.from('contratos').update`)
    - Contém duas verificações via admin de que a unidade voltou a `status: 'disponivel'` após cancelar/encerrar (regex `unidades` + `disponivel`)
    - `npx playwright test e2e/crud.spec.js` passa 100% (todos os 11+ testes verdes)
    - Após execução completa, `select count(*) from auth.users where email like 'e2e-%'` retorna 0
  </acceptance_criteria>
  <done>Suíte `crud.spec.js` completa verde. TEST-01 é coberto end-to-end. Teardown limpa todos os artefatos prefixados E2E-/e2e-.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright runner → Next.js dev → Supabase local | Specs autenticam como Proprietário e exercitam Server Actions reais |
| Spec → Supabase admin API | Setup/teardown de dados via SUPABASE_ROLE_KEY (.env.test) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Spec deleta dados produtivos | mitigate | Todos os dados criados pelo spec usam prefixo `E2E-`; teardown filtra por `like('nome', 'E2E-%')`; .env.test aponta para 127.0.0.1:54321 |
| T-05-05 | Information Disclosure | Email dinâmico vaza timestamp | accept | Domínio `@test.romma.local` é inexistente; timestamp não é segredo |
| T-05-06 | Repudiation | Teste flake silencioso pode ocultar regressão | mitigate | Timeouts explícitos (10-15s) + assertions diretas em texto-chave; CI exigirá 100% verde antes de DEPL-03 |
| T-05-SC | Tampering | npm installs | accept | Esta wave não instala pacotes |
</threat_model>

<verification>
- **Pré-condição obrigatória:** `supabase functions serve` deve estar ativo antes de rodar este spec — `handleCriarContrato` chama `gerar-parcelas` Edge Function automaticamente ao criar contrato via UI (confirmado em Contratos.js). Sem a EF ativa, os testes de criar contrato falharão com erro de conexão (ECONNREFUSED em 127.0.0.1:54321/functions).
- `npx playwright test e2e/crud.spec.js` passa 100% (todos os 11+ testes)
- `npx playwright test` (suíte completa) continua verde
- Pós-execução: queries Supabase confirmam zero registros com prefixo `E2E-`/`e2e-`
- Reset manual entre runs não necessário (teardown idempotente)
</verification>

<success_criteria>
1. TEST-01 coberto: criar/editar/deletar de Edifícios, Unidades, Locatários (convidar/editar) e Contratos (criar/cancelar/encerrar) — todos via UI real
2. Regra de negócio validada: unidade volta para `disponivel` após cancelar OU encerrar contrato
3. Spec resiliente: roda múltiplas vezes consecutivas sem flakes (teardown limpa dependências)
4. Sem uso de `page.selectOption` em shadcn Select; sem botões clicados sem ancoragem
</success_criteria>

<output>
Criar `.planning/phases/05-testes-e2e/05-02-SUMMARY.md` ao concluir, registrando: número total de testes, tempo de execução, ajustes de seletores aplicados em relação ao RESEARCH (se houver), e comandos exatos validados.
</output>
