---
phase: 01-dashboard-completions
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/components/features/LocatariosDesktop.js
autonomous: true
requirements:
  - VIS-02

must_haves:
  truths:
    - "LocatariosDesktop.js não contém inline styles exceto gridTemplateColumns GRID"
    - "Botões REVOGAR e VER → usam shadcn Button variant ghost com text-danger-fg e text-fg-3"
    - "Modal de convite usa classes Tailwind (fixed inset-0 z-50)"
    - "Toggle PF/PJ usa cn() para estado ativo/inativo"
    - "Field helper usa classes Tailwind para label e gap"
    - "shadcn Input e Select usados no formulário de convite"
  artifacts:
    - path: "src/components/features/LocatariosDesktop.js"
      provides: "Componente de locatários migrado para Tailwind v4 + shadcn"
  key_links:
    - from: "src/components/features/LocatariosDesktop.js"
      to: "src/components/ui/button.jsx"
      via: "import { Button }"
      pattern: "import.*Button.*components/ui/button"
    - from: "src/components/features/LocatariosDesktop.js"
      to: "src/components/ui/input.jsx"
      via: "import { Input }"
      pattern: "import.*Input.*components/ui/input"
---

<objective>
Migrar LocatariosDesktop.js de inline styles para Tailwind v4 + shadcn. Executado em paralelo com Plan 03 (Parcelas.js) — sem overlap de arquivos.

Purpose: Entregar a tela de Locatários (/dashboard/locatarios) com visual Obsidian Blueprint consistente.
Output: LocatariosDesktop.js sem inline styles (exceto gridTemplateColumns GRID), modal de convite com Tailwind, formulário usando shadcn Input.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-dashboard-completions/01-CONTEXT.md
@.planning/phases/01-dashboard-completions/01-PATTERNS.md
@.planning/phases/01-dashboard-completions/01-02-SUMMARY.md
</context>

<interfaces>
De src/components/ui/button.jsx:
- Button({ variant, size, className, disabled, onClick, type, children })
- variant: "default" | "ghost" | "outline" | "destructive"

De src/components/ui/input.jsx:
- Input({ className, type, value, onChange, placeholder, required, ...props })
- Usar className para override de tokens: "bg-surface-hi border-border-3 text-fg-1 font-mono text-[13px] rounded-none"

De src/lib/utils.js:
- cn(...classes) — merge de classes Tailwind

Mapeamento de tokens para LocatariosDesktop.js:
- var(--surface)    → bg-surface
- var(--surface-hi) → bg-surface-hi
- var(--border-3)   → border-border-3
- var(--border-2)   → NÃO MAPEADO — usar [var(--border-2)] como arbitrary value
- var(--fg-1)       → text-fg-1
- var(--fg-2)       → text-fg-2
- var(--fg-3)       → text-fg-3
- var(--fg-4)       → text-fg-4
- var(--indigo)     → bg-indigo / text-indigo / border-indigo
- var(--danger)     → NÃO MAPEADO — usar text-danger-fg
- var(--success)    → text-success

Exceção justificada (manter como style={{}}):
- gridTemplateColumns: GRID = "1.8fr 0.5fr 1.2fr 1.2fr 0.8fr 1.4fr 80px" — sem equivalente Tailwind
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrar LocatariosDesktop.js para Tailwind v4 + shadcn</name>
  <files>src/components/features/LocatariosDesktop.js</files>
  <read_first>
    - src/components/features/LocatariosDesktop.js — ler integralmente: ver Field helper (~linhas 302-319), inputStyle, container de tabela (~linhas 74-79), linhas de tabela (~linhas 101-105), avatar (~linhas 107-116), botões de ação (~linhas 151-168), modal de convite (~linhas 199-204), toggle PF/PJ (~linhas 234-248)
    - .planning/phases/01-dashboard-completions/01-PATTERNS.md — seção "src/components/features/LocatariosDesktop.js"
    - src/components/ui/button.jsx — assinatura e variantes
    - src/components/ui/input.jsx — assinatura e como passar className de override
  </read_first>
  <action>
    Migrar LocatariosDesktop.js seguindo as receitas do PATTERNS.md para este componente. Percorrer o arquivo de cima para baixo:

    1. IMPORTS — adicionar: import { cn } from "@/lib/utils", import { Button } from "@/components/ui/button", import { Input } from "@/components/ui/input" (verificar se Select é necessário — se houver <select> nativo no arquivo, importar também)

    2. Field HELPER — substituir style={{}} por:
       - Container div: className="flex flex-col gap-1.5"
       - label: className="font-mono text-[10px] font-bold tracking-[1px] uppercase text-fg-4"
       - span de required (*): className="text-danger-fg ml-0.5"

    3. CONTAINER DE TABELA (~linhas 74-79):
       - Wrapper externo: className="bg-surface border border-border-3"
       - Header row: manter style={{ gridTemplateColumns: GRID }}, adicionar className="px-5 py-3 bg-[oklch(0.26_0_0)]"

    4. LINHAS DE TABELA (~linhas 101-105):
       - manter style={{ gridTemplateColumns: GRID }}, adicionar className={cn("px-5 py-4 items-center", i > 0 ? "border-t border-border-3" : "")}

    5. AVATAR DO LOCATÁRIO (~linhas 107-116):
       - className={cn("w-8 h-8 shrink-0 flex items-center justify-center border font-body font-bold text-[10px] tracking-[1px]", isPendente ? "bg-transparent border-[var(--border-2)] text-fg-4" : "bg-surface border-[var(--border-2)] text-fg-1")}
       - Nota: --border-2 não mapeado no @theme — usar arbitrary value [var(--border-2)]

    6. BOTÕES DE AÇÃO DE LINHA (~linhas 151-168):
       - REVOGAR: Button variant="ghost" size="sm" className="font-mono text-[10px] text-danger-fg uppercase tracking-[0.5px] font-bold p-0 h-auto"
       - VER →: Button variant="ghost" size="sm" className="font-mono text-[10px] text-fg-3 uppercase tracking-[0.5px] font-bold p-0 h-auto"
       - CRÍTICO: REVOGAR usa text-danger-fg (não text-danger — token não mapeado)

    7. MODAL DE CONVITE (~linhas 199-204):
       - Overlay: className="fixed inset-0 z-50 bg-[oklch(0_0_0/0.7)] flex items-center justify-center"
       - Container: className="bg-surface border border-[var(--border-2)] w-[480px] p-8 flex flex-col gap-6"

    8. TOGGLE PF/PJ (~linhas 234-248):
       - button (manter como <button> nativo com type="button"):
         className={cn("cursor-pointer py-2 px-5 font-mono font-bold text-[11px] tracking-[1px] uppercase border border-border-3", form.tipo === t ? "bg-indigo text-fg-1" : "bg-surface-hi text-fg-4")}

    9. INPUTS DO FORMULÁRIO DE CONVITE:
       - Substituir <input style={inputStyle}> por: <Input className="bg-surface-hi border-border-3 text-fg-1 font-mono text-[13px] rounded-none" />
       - Preservar value, onChange, required, placeholder existentes

    10. BOTÕES DO MODAL (Convidar, Cancelar):
        - Convidar: Button type="submit" className="bg-indigo text-fg-1 font-mono font-bold text-[12px] tracking-[1.2px] uppercase px-8 py-[14px] rounded-none" (ou variant="default" com override)
        - Cancelar: Button type="button" variant="ghost" onClick={fecharModal} className="text-fg-3 font-mono text-[12px]"

    11. QUAISQUER outros inline styles — migrar usando tokens do mapeamento.

    MANTER apenas style={{ gridTemplateColumns: GRID }} como única exceção justificada.
    Preservar toda a lógica de negócio: convite por email, toggle PF/PJ, ConfirmDialog para REVOGAR.
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma && grep -c "style={{" src/components/features/LocatariosDesktop.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "style={{" src/components/features/LocatariosDesktop.js retorna 1 (somente gridTemplateColumns)
    - grep "gridTemplateColumns: GRID" src/components/features/LocatariosDesktop.js confirma o único style={{}}} correto
    - grep "var(--danger)\b" src/components/features/LocatariosDesktop.js retorna 0
    - grep "text-danger-fg" src/components/features/LocatariosDesktop.js retorna pelo menos 1 linha
    - grep "inputStyle" src/components/features/LocatariosDesktop.js retorna 0 (variável inputStyle eliminada)
    - grep "Button" src/components/features/LocatariosDesktop.js retorna pelo menos 3 linhas
    - npm run build sem erros
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → Server Action | Formulário de convite chama Server Action de invite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Formulário de convite (inviteUserByEmail) | accept | Server Action já valida autenticação com authGuard(). Migração de estilo não altera fluxo de autorização. |
</threat_model>

<verification>
- grep -c "style={{" src/components/features/LocatariosDesktop.js — 1
- grep "gridTemplateColumns: GRID" — confirma exceção justificada
- grep "var(--danger)\b" — 0
- grep "inputStyle" — 0
- npm run build — sem erros
</verification>

<success_criteria>
1. LocatariosDesktop.js com exatamente 1 inline style (gridTemplateColumns: GRID)
2. Modal de convite com overlay e container em Tailwind
3. Botão REVOGAR usa text-danger-fg (não text-danger)
4. Toggle PF/PJ usa cn() para estado ativo/inativo com bg-indigo
5. Inputs do formulário usando shadcn Input
6. Funcionalidade de convite preservada
7. npm run build sem erros
</success_criteria>

<output>
Criar `.planning/phases/01-dashboard-completions/01-04-SUMMARY.md` quando concluído
</output>
