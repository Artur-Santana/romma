---
phase: 01-dashboard-completions
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/components/features/Parcelas.js
autonomous: true
requirements:
  - VIS-02

must_haves:
  truths:
    - "Parcelas.js não contém nenhum inline style exceto gridTemplateColumns COL"
    - "Botão 'Marcar Paga' usa shadcn Button variant outline com tokens Tailwind"
    - "Botão '← Contratos' usa shadcn Button variant outline"
    - "Texto de status 'vencida' usa text-danger-fg (não text-danger)"
    - "HeaderCell usa classes Tailwind (não inline styles)"
  artifacts:
    - path: "src/components/features/Parcelas.js"
      provides: "Componente de parcelas migrado para Tailwind v4 + shadcn"
  key_links:
    - from: "src/components/features/Parcelas.js"
      to: "src/components/ui/button.jsx"
      via: "import { Button }"
      pattern: "import.*Button.*components/ui/button"
---

<objective>
Migrar Parcelas.js de inline styles para Tailwind v4 + shadcn Button. Este é o componente mais simples (184 linhas, sem formulário complexo) e serve para validar o padrão de migração antes dos componentes maiores.

Purpose: Entregar a tela de Parcelas (/dashboard/contratos/[id]) com visual Obsidian Blueprint consistente.
Output: Parcelas.js sem inline styles (exceto gridTemplateColumns), usando shadcn Button e tokens Tailwind.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-dashboard-completions/01-CONTEXT.md
@.planning/phases/01-dashboard-completions/01-PATTERNS.md
@.planning/phases/01-dashboard-completions/01-02-SUMMARY.md
</context>

<interfaces>
De src/components/ui/button.jsx (após shadcn add button):
- Button({ variant, size, className, disabled, onClick, type, children })
- variant: "default" | "ghost" | "outline" | "destructive" | "secondary" | "link"
- size: "default" | "sm" | "lg" | "icon"

De src/lib/utils.js:
- cn(...classes) — merge de classes Tailwind

Mapeamento de tokens para Parcelas.js:
- var(--surface)    → bg-surface
- var(--border-3)   → border-border-3
- var(--fg-1)       → text-fg-1
- var(--fg-3)       → text-fg-3
- var(--fg-4)       → text-fg-4
- var(--fg-5)       → text-fg-5
- var(--success)    → text-success / border-success
- var(--danger)     → NÃO MAPEADO — usar text-danger-fg
- var(--warning)    → text-warning

Exceção justificada (manter como style={{}}):
- gridTemplateColumns: COL = "60px 1fr 1fr 1fr 1.2fr 120px" — sem equivalente Tailwind
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrar Parcelas.js para Tailwind v4 + shadcn Button</name>
  <files>src/components/features/Parcelas.js</files>
  <read_first>
    - src/components/features/Parcelas.js — ler integralmente: ver HeaderCell (~linhas 13-24), container de tabela (~linhas 106-107), linhas de tabela (~linhas 122-130), status condicional (~linhas 143-148), botão "Marcar Paga" (~linhas 163-176), botão "← Contratos" (~linhas 70-80)
    - src/app/login/page.js — referência canônica de cn() e classes Tailwind
    - .planning/phases/01-dashboard-completions/01-PATTERNS.md — seção "src/components/features/Parcelas.js"
    - src/components/ui/button.jsx — ver assinatura e variantes disponíveis
  </read_first>
  <action>
    Migrar Parcelas.js seguindo as receitas do PATTERNS.md para este componente. Percorrer o arquivo de cima para baixo:

    1. IMPORTS — adicionar ao topo: import { cn } from "@/lib/utils" e import { Button } from "@/components/ui/button" (se ainda não importados)

    2. HeaderCell (~linhas 13-24) — substituir style={{}} por:
       className="px-5 py-3 font-mono text-[10px] font-bold tracking-[1.4px] uppercase text-fg-4"

    3. CONTAINER DE TABELA (~linhas 106-107):
       - Wrapper externo: className="border border-border-3 bg-surface mb-8"
       - Header row: manter style={{ gridTemplateColumns: COL }} como única exceção, adicionar className="bg-[oklch(0.26_0_0)] border-b border-border-3"

    4. LINHAS DE TABELA (~linhas 122-130):
       - manter style={{ gridTemplateColumns: COL }}, adicionar: className={cn("items-center", i > 0 ? "border-t border-border-3" : "")}

    5. TEXTO CONDICIONAL POR STATUS (~linhas 143-148):
       - CRÍTICO: parcela.status === "vencida" usa var(--danger) — substituir por text-danger-fg (não text-danger)
       - className={cn("font-mono text-[11px]", parcela.status === "vencida" ? "text-danger-fg" : "text-fg-3")}

    6. BOTÃO "Marcar Paga" (~linhas 163-176):
       - Substituir button com style={{}} por:
       - Button variant="outline" size="sm" className="border-success text-success font-mono text-[10px] uppercase tracking-[1px] font-bold rounded-none h-auto py-2 px-4"

    7. BOTÃO "← Contratos" (~linhas 70-80):
       - Substituir button com style={{}} por:
       - Button variant="outline" onClick={() => router.push("/dashboard/contratos")} className="border-border-3 bg-transparent text-fg-3 font-mono text-[10px] uppercase tracking-[1.2px] font-bold rounded-none mb-10 h-auto py-[10px] px-5"

    8. QUAISQUER outros inline styles restantes — migrar usando tokens do mapeamento acima.

    REGRA: se o inline style usa var(--danger), SEMPRE substituir por text-danger-fg (não text-danger — token não mapeado no @theme inline). Para backgrounds de danger usar bg-[var(--danger-bg)] como arbitrary value.

    MANTER apenas style={{ gridTemplateColumns: COL }} — esta é a única exceção justificada.

    Após migração: verificar que a funcionalidade de "Marcar Paga" ainda funciona (onClick preservado, disabled state preservado se existir).
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma && grep -c "style={{" src/components/features/Parcelas.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "style={{" src/components/features/Parcelas.js retorna 1 (somente gridTemplateColumns)
    - grep "gridTemplateColumns: COL" src/components/features/Parcelas.js retorna a linha com COL (confirma que o único style={{}} é o correto)
    - grep "var(--danger)\b" src/components/features/Parcelas.js retorna 0 (token não mapeado eliminado)
    - grep "text-danger-fg" src/components/features/Parcelas.js retorna pelo menos 1 linha
    - grep "inputStyle\|actionBtnStyle" src/components/features/Parcelas.js retorna 0
    - grep "Button" src/components/features/Parcelas.js retorna pelo menos 2 linhas (shadcn Button em uso)
    - npm run build sem erros
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → Supabase | Parcelas.js usa queries-client.js — RLS ativo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Ação "Marcar Paga" | accept | Ação já existe e tem validação de auth/RLS. Migração de estilo não altera lógica de autorização. |
</threat_model>

<verification>
- grep -c "style={{" src/components/features/Parcelas.js — deve retornar 1
- grep "gridTemplateColumns: COL" src/components/features/Parcelas.js — confirma que o único style={{}} é gridTemplateColumns
- grep "var(--danger)\b" src/components/features/Parcelas.js — 0 (token unmapped eliminado)
- npm run build — sem erros
</verification>

<success_criteria>
1. Parcelas.js com exatamente 1 inline style restante (gridTemplateColumns: COL)
2. Botões "Marcar Paga" e "← Contratos" usando shadcn Button
3. Status "vencida" usa text-danger-fg (não text-danger)
4. Funcionalidade de marcar parcela como paga preservada
5. npm run build sem erros
</success_criteria>

<output>
Criar `.planning/phases/01-dashboard-completions/01-03-SUMMARY.md` quando concluído
</output>
