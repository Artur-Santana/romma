---
phase: 01-dashboard-completions
plan: 08
type: execute
wave: 5
depends_on:
  - 01-05
  - 01-06
files_modified:
  - src/components/ui/ConfirmDialog.js
  - src/components/ui/MobileNav.js
  - src/components/ui/OwnerSidebar.js
autonomous: true
requirements:
  - VIS-02

must_haves:
  truths:
    - "ConfirmDialog.js sem inline styles — bordas dinâmicas (danger/indigo) migradas para cn() condicional"
    - "MobileNav.js sem inline styles (MobileTopBar e MobileBottomNav)"
    - "OwnerSidebar.js sem inline styles"
  artifacts:
    - path: "src/components/ui/ConfirmDialog.js"
      provides: "Modal de confirmação migrado para Tailwind v4 com cn() condicional"
    - path: "src/components/ui/MobileNav.js"
      provides: "Navegação mobile migrada para Tailwind v4"
    - path: "src/components/ui/OwnerSidebar.js"
      provides: "Sidebar de proprietário migrada para Tailwind v4"
  key_links:
    - from: "src/components/ui/ConfirmDialog.js"
      to: "src/lib/utils.js"
      via: "import { cn }"
      pattern: "import.*cn.*utils"
---

<objective>
Migrar os três componentes de UI shell de navegação/modal (ConfirmDialog, MobileNav, OwnerSidebar) de inline styles para Tailwind v4. Executado em paralelo com Plan 07 — sem overlap de arquivos.

Purpose: Completar a cobertura D-06 para todos os componentes UI shell com inline styles usados no dashboard.
Output: Todos os três componentes sem inline styles (exceto casos dinâmicos migrados para cn() condicional).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-dashboard-completions/01-CONTEXT.md
@.planning/phases/01-dashboard-completions/01-PATTERNS.md
@.planning/phases/01-dashboard-completions/01-05-SUMMARY.md
</context>

<interfaces>
De src/lib/utils.js:
- cn(...classes) — merge de classes Tailwind

Mapeamento de tokens (mesmos do projeto):
- var(--background)  → bg-background
- var(--border-1)    → border-border-1 (verificar se mapeado em @theme — se não, usar [var(--border-1)])
- var(--border-2)    → border-[var(--border-2)] (arbitrary value — não mapeado no @theme inline)
- var(--border-3)    → border-border-3
- var(--fg-1)        → text-fg-1
- var(--fg-2)        → text-fg-2
- var(--fg-3)        → text-fg-3
- var(--fg-4)        → text-fg-4
- var(--indigo)      → text-indigo / bg-indigo / border-indigo
- var(--danger)      → NÃO MAPEADO diretamente — usar text-danger-fg para texto, border-danger-fg para borda
- var(--danger-bg)   → bg-[var(--danger-bg)] (arbitrary value)
- var(--success)     → text-success

Casos dinâmicos em ConfirmDialog.js (migrar com cn() condicional):
- `accentColor` = danger ? var(--danger) : var(--indigo) → usar cn(danger ? "border-danger-fg" : "border-indigo")
- `accentBg` = danger ? var(--danger-bg) : var(--indigo) → usar cn(danger ? "bg-[var(--danger-bg)]" : "bg-indigo")
- Botão confirm color: danger ? var(--danger) : var(--fg-1) → cn(danger ? "text-danger-fg" : "text-fg-1")
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrar ConfirmDialog.js para Tailwind v4 com cn() condicional</name>
  <files>src/components/ui/ConfirmDialog.js</files>
  <read_first>
    - src/components/ui/ConfirmDialog.js — ler integralmente (108 linhas). Identificar: overlay div, modal container (com border accentColor dinâmico), h2 title, body div, botões Cancelar e Confirmar (com bg/color dinâmicos)
    - src/lib/utils.js — confirmar assinatura cn()
  </read_first>
  <action>
    Migrar ConfirmDialog.js de cima para baixo. Adicionar import { cn } from "@/lib/utils":

    1. OVERLAY: style={{ position:"fixed", inset:0, background:"oklch(0 0 0 / 0.7)", display:"flex", alignItems:"center", justifyContent:"center", zIndex:100 }} → className="fixed inset-0 bg-[oklch(0_0_0/0.7)] flex items-center justify-center z-[100]"

    2. MODAL CONTAINER: style={{ width:480, background:"var(--background)", border:`1px solid ${accentColor}`, padding:32, display:"flex", flexDirection:"column", gap:20 }} →
       className={cn("w-[480px] bg-background border flex flex-col gap-5 p-8", danger ? "border-danger-fg" : "border-indigo")}

    3. TITLE div: style={{ fontFamily:"var(--font-display-arch)", fontWeight:700, fontSize:28, letterSpacing:-1.2, color:"var(--fg-1)", lineHeight:1.1 }} → className="font-display font-bold text-[28px] tracking-[-1.2px] text-fg-1 leading-[1.1]"

    4. BODY div: style={{ fontSize:14, lineHeight:1.5, color:"var(--fg-2)" }} → className="text-[14px] leading-[1.5] text-fg-2"

    5. BOTÕES wrapper: style={{ display:"flex", gap:12 }} → className="flex gap-3"

    6. BOTÃO CANCELAR: style={{ flex:1, border:"1px solid var(--border-3)", background:"transparent", padding:"14px 0", fontFamily:"var(--font-body)", fontWeight:700, fontSize:12, letterSpacing:1.2, textTransform:"uppercase", color:"var(--fg-2)", cursor:"pointer" }} → className="flex-1 border border-border-3 bg-transparent py-[14px] font-body font-bold text-[12px] tracking-[1.2px] uppercase text-fg-2 cursor-pointer"

    7. BOTÃO CONFIRMAR: style com accentBg e color dinâmicos → className={cn("flex-1 border-none py-[14px] font-body font-bold text-[12px] tracking-[1.2px] uppercase cursor-pointer", danger ? "bg-[var(--danger-bg)] text-danger-fg" : "bg-indigo text-fg-1")}

    Preservar toda a lógica: props exatos (open, title, body, confirmLabel, cancelLabel, danger, onConfirm, onCancel), stopPropagation no click do modal, return null quando !open.
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma && grep -c "style={{" src/components/ui/ConfirmDialog.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "style={{" src/components/ui/ConfirmDialog.js retorna 0
    - npm run build sem erros
    - ConfirmDialog ainda usado sem erros nos componentes que o importam (Contratos.js, LocatariosDesktop.js)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrar MobileNav.js e OwnerSidebar.js para Tailwind v4</name>
  <files>src/components/ui/MobileNav.js, src/components/ui/OwnerSidebar.js</files>
  <read_first>
    - src/components/ui/MobileNav.js — ler integralmente (132 linhas). Identificar: MobileTopBar e MobileBottomNav. Notar --border-2 (arbitrary value) e tokens de fg/bg.
    - src/components/ui/OwnerSidebar.js — ler integralmente (160 linhas). Identificar: wrapper container, wordmark, nav links (isActive condicional), badge counter, logout.
  </read_first>
  <action>
    MOBILENAV.JS:
    Percorrer MobileTopBar de cima para baixo. Migrar cada style={{}} por classes Tailwind:
    - Usar border-[var(--border-2)] para --border-2 (não mapeado no @theme)
    - Usar cn() para condicionais (ex: isActive no MobileBottomNav)
    - Adicionar import { cn } from "@/lib/utils" se necessário

    OWNERSIDEBAR.JS:
    Percorrer de cima para baixo. Migrar cada style={{}} por classes Tailwind:
    - Wrapper: w-64 bg-background border-r border-border-1 flex flex-col h-full shrink-0
    - Wordmark section: px-8 py-8 pb-6
    - ROMMA heading: font-display font-bold text-[28px] tracking-[-1px] text-fg-1 mt-2
    - Nav link: usar cn() para estado isActive (ex: bg-surface-hi ou border-r border-indigo para ativo)
    - Se --border-1 não mapeado no @theme, usar border-[var(--border-1)]
    - Preservar toda a lógica: pathname check, badges, logout, supabase.auth.signOut()

    Em ambos os arquivos: após migração, confirmar que nenhum style={{}} contém propriedades que têm token Tailwind disponível.
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma && grep -c "style={{" src/components/ui/MobileNav.js src/components/ui/OwnerSidebar.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "style={{" retorna 0 para MobileNav.js e 0 para OwnerSidebar.js
    - npm run build sem erros
    - Dashboard layout funciona corretamente (OwnerSidebar e MobileNav renderizam)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → Supabase Auth | OwnerSidebar.js chama supabase.auth.signOut() e getUser() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | OwnerSidebar logout (signOut) | accept | Migração de estilo não altera lógica de auth. signOut() já usa supabase-browser client com sessão local. |
| T-08-02 | Tampering | ConfirmDialog onConfirm callback | accept | Callbacks são props — executors (Contratos, Locatarios) já têm auth guard via Server Action. Migração não altera fluxo. |
</threat_model>

<verification>
- grep -c "style={{" src/components/ui/ConfirmDialog.js — 0
- grep -c "style={{" src/components/ui/MobileNav.js — 0
- grep -c "style={{" src/components/ui/OwnerSidebar.js — 0
- npm run build — sem erros
</verification>

<success_criteria>
1. ConfirmDialog.js sem inline styles — bordas danger/indigo condicionais via cn()
2. MobileNav.js sem inline styles — MobileTopBar e MobileBottomNav completamente migrados
3. OwnerSidebar.js sem inline styles — nav, wordmark, logout migrados
4. npm run build sem erros
5. Dashboard layout funcional (sidebar + mobile nav renderizam corretamente)
</success_criteria>

<output>
Criar `.planning/phases/01-dashboard-completions/01-08-SUMMARY.md` quando concluído
</output>
