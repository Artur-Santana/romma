---
phase: 01-dashboard-completions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/button.jsx
  - src/components/ui/input.jsx
  - src/components/ui/select.jsx
  - tests/dashboard.spec.js
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - VIS-02

must_haves:
  truths:
    - "Componentes shadcn button, input e select estão disponíveis em src/components/ui/"
    - "Tests E2E falham por razão semântica (tiles exibem conteúdo errado), não por erro de configuração"
    - "playwright.config.js aponta para baseURL correta e testa dashboard"
  artifacts:
    - path: "src/components/ui/button.jsx"
      provides: "shadcn Button component"
    - path: "src/components/ui/input.jsx"
      provides: "shadcn Input component"
    - path: "src/components/ui/select.jsx"
      provides: "shadcn Select component"
    - path: "tests/dashboard.spec.js"
      provides: "Testes E2E para DASH-01, DASH-02, DASH-03"
  key_links:
    - from: "tests/dashboard.spec.js"
      to: "src/app/dashboard/page.js"
      via: "Playwright baseURL /dashboard"
      pattern: "page.goto.*dashboard"
---

## Phase Goal

**As a** Proprietário, **I want to** ver valores financeiros reais (MRR e receita esperada) e alertas de contratos vencendo em um dashboard visualmente consistente, **so that** eu possa monitorar meu negócio de aluguéis em um único lugar.

> Derivado do ROADMAP.md Goal — não gerado via `/gsd mvp-phase 1`. A estrutura "As a / I want / so that" foi inferida do texto original.

<objective>
Pré-condições para toda a migração da Fase 1: instalar os três primitivos shadcn necessários e criar os testes E2E que falharão (RED) enquanto os tiles ainda exibem dados incorretos.

Purpose: Garantir que as ferramentas de UI estejam disponíveis antes de qualquer migração, e que os critérios de aceitação de DASH-01/02/03 sejam verificáveis automaticamente.
Output: button.jsx, input.jsx, select.jsx em src/components/ui/ + tests/dashboard.spec.js com 3 testes failing por razão semântica.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-dashboard-completions/01-CONTEXT.md
@.planning/phases/01-dashboard-completions/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar primitivos shadcn (button, input, select)</name>
  <files>src/components/ui/button.jsx, src/components/ui/input.jsx, src/components/ui/select.jsx</files>
  <read_first>
    - src/components.json — confirmar style: radix-lyra, tsx: false, cssVariables: true
    - src/app/globals.css — confirmar --radius: 0 (garante corners sharp nos componentes instalados)
    - src/app/login/page.js — ver como Button já é usado no projeto (importação e variantes)
  </read_first>
  <action>
    Executar os três comandos de instalação shadcn em sequência:

    npx shadcn@latest add button
    npx shadcn@latest add input
    npx shadcn@latest add select

    Aceitar prompts padrão (não sobrescrever globals.css). Após instalar, verificar que os arquivos foram criados em src/components/ui/ com extensão .jsx (shadcn gera .jsx mesmo com tsx:false no projeto JS).

    NÃO instalar: table, badge, card — fora do escopo desta fase per RESEARCH.md.

    Confirmar que --radius: 0 em globals.css é respeitado (os componentes shadcn lêem essa variável automaticamente — não adicionar rounded-none manualmente neste passo).
  </action>
  <verify>
    <automated>ls /home/artursantana/Code/romma/src/components/ui/button.jsx /home/artursantana/Code/romma/src/components/ui/input.jsx /home/artursantana/Code/romma/src/components/ui/select.jsx</automated>
  </verify>
  <acceptance_criteria>Os três arquivos existem em src/components/ui/. Nenhum erro de instalação.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Criar testes E2E para DASH-01, DASH-02, DASH-03 (RED)</name>
  <files>tests/dashboard.spec.js</files>
  <read_first>
    - playwright.config.js — ver baseURL atual e configuração de autenticação
    - src/app/dashboard/page.js — ver o HTML atual dos tiles (labels e valores) para escrever seletores corretos
    - .planning/phases/01-dashboard-completions/01-RESEARCH.md — seção "Status atual dos tiles" e "Wave 0 Gaps"
  </read_first>
  <action>
    Criar tests/dashboard.spec.js com três testes marcados com @smoke. O arquivo deve usar a autenticação existente do playwright.config.js (storageState ou cookies de sessão se já configurado, caso contrário pular auth e usar apenas asserções de conteúdo acessível sem login — verificar o config primeiro).

    Estrutura dos testes:

    Teste DASH-01: Navegar para /dashboard. Verificar que o tile 02 exibe o texto "MRR" como label (não "Contratos Ativos"). Verificar que o valor principal do tile 02 contém "R$" (formato monetário, não número inteiro de contagem). Este teste DEVE FALHAR no estado atual porque o tile mostra "Contratos Ativos" e exibe a contagem numérica.

    Teste DASH-02: Navegar para /dashboard. Verificar que o tile 03 exibe o texto "Receita Esperada" como label (não "Parcelas Pendentes"). Verificar que o valor principal do tile 03 contém "R$" (não um número de contagem pura). Este teste DEVE FALHAR no estado atual.

    Teste DASH-03: Navegar para /dashboard. Verificar que se a página contém o texto "ATENÇÃO · CONTRATOS A VENCER", o elemento existe com classe bg-warning-bg ou background de warning. Se não há contratos vencendo, o teste verifica que o banner NÃO aparece (o alerta só aparece quando há contratos). Estruturar como: if (await page.locator('[data-testid="expiry-banner"]').isVisible()) { verificar texto }. Este teste PASSA no estado atual (DASH-03 já funciona).

    Usar getByText para localizar labels de tile. Não usar seletores por índice DOM — os tiles mudam de posição entre mobile/desktop. Usar seletores que combinem label + vizinhança próxima.

    NÃO configurar autenticação do zero — se playwright.config.js não tem storageState, adicionar skip condicional ou usar page.goto com nota de que login manual é necessário para ambiente sem auth configurado.
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma && npx playwright test tests/dashboard.spec.js --grep @smoke --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo tests/dashboard.spec.js existe com 3 testes @smoke
    - Teste DASH-01 falha porque tile mostra "Contratos Ativos" (não "MRR") — falha semântica esperada, não erro de config
    - Teste DASH-02 falha porque tile mostra "Parcelas Pendentes" (não "Receita Esperada") — falha semântica esperada
    - Teste DASH-03 passa (ou skip se não há contratos vencendo no ambiente)
    - Nenhum teste falha com "browser not found" ou "connection refused" — apenas falhas semânticas
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → npm registry | shadcn CLI busca componentes do registry oficial shadcn.dev |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npx shadcn@latest add (3 installs) | accept | shadcn é um projeto oficial Radix/Vercel, amplamente auditado. Sem pacotes novos no package.json — shadcn apenas copia código fonte local. |
</threat_model>

<verification>
- ls src/components/ui/button.jsx src/components/ui/input.jsx src/components/ui/select.jsx — todos existem
- npx playwright test tests/dashboard.spec.js --grep @smoke — DASH-01 e DASH-02 falham (semântico), DASH-03 passa
- grep -c "style={{" tests/dashboard.spec.js — 0 (arquivo de teste não usa inline styles)
</verification>

<success_criteria>
1. Três primitivos shadcn instalados e importáveis nos feature components
2. tests/dashboard.spec.js com 3 testes @smoke funcionando contra o app
3. DASH-01 e DASH-02 falham por razão semântica correta (label errado), não por erro de config
4. DASH-03 passa (alerta já funciona) ou skip condicional documentado
</success_criteria>

<output>
Criar `.planning/phases/01-dashboard-completions/01-01-SUMMARY.md` quando concluído
</output>
