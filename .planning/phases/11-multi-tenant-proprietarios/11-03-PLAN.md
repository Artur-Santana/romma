---
phase: 11-multi-tenant-proprietarios
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - .planning/phases/11-multi-tenant-proprietarios/11-VERIFICATION.md
autonomous: false
requirements: [MT-01, MT-02, MT-03, MT-04]
must_haves:
  truths:
    - "Proprietário A não vê edifícios/locatários/contratos/parcelas de Proprietário B"
    - "Visitante anon em /unidades vê unidades disponíveis com nome do edifício"
    - "Portal do Locatário exibe contrato ativo (com unidade) e histórico de parcelas"
    - "criarEdificio grava proprietario_id correto no banco"
    - "Signup em /signup cria Proprietário sem guard de instância única (MT-04)"
  artifacts:
    - path: ".planning/phases/11-multi-tenant-proprietarios/11-VERIFICATION.md"
      provides: "Relatório de verificação end-to-end do isolamento multi-tenant"
      contains: "Proprietário"
  key_links:
    - from: "Proprietário B login"
      to: "RLS SELECT"
      via: "auth.uid() = proprietario_id"
      pattern: "isolamento"
---

<objective>
Verificar de ponta a ponta que o isolamento multi-tenant funciona: dois Proprietários distintos não veem dados um do outro, a página pública `/unidades` continua acessível a anon (com nome do edifício), o portal do Locatário continua funcional, e o signup aberto cria múltiplos Proprietários sem guard de instância única (MT-04). Registrar resultados em `11-VERIFICATION.md`.

Purpose: Os critérios de sucesso da fase exigem prova empírica de isolamento — não apenas que a migration aplicou. Esta é a auditoria AUDIT-01 da fase.

Output: `.planning/phases/11-multi-tenant-proprietarios/11-VERIFICATION.md` com cada cenário marcado PASS/FAIL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-multi-tenant-proprietarios/11-CONTEXT.md
@.planning/phases/11-multi-tenant-proprietarios/11-RESEARCH.md
@src/lib/queries-server.js
@src/lib/queries-client.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificação automatizada — schema, seed, RLS, MT-04, build</name>
  <read_first>
    - supabase/migrations/20260521000000_multi_tenant_proprietario_id.sql (migration aplicada)
    - .planning/phases/11-multi-tenant-proprietarios/11-RESEARCH.md (seção 4 — critérios)
    - src/actions/edificios.js (proprietario_id no insert — confirmar Plan 2 aplicada)
  </read_first>
  <files>.planning/phases/11-multi-tenant-proprietarios/11-VERIFICATION.md</files>
  <action>
    Executar e registrar em `11-VERIFICATION.md`:

    1. Confirmar que MT-04 já está satisfeito (guard de instância única removido na Phase 10): `grep -rn "Instância já configurada\|instancia.*configurada\|count.*proprietarios" src/actions/auth.js src/app/signup` — não deve existir guard que bloqueie o segundo signup. Referenciar o commit b9537f8 ("remover guard de instância única").

    2. Confirmar seed via query de diagnóstico no banco remoto: `SELECT COUNT(*) FROM edificios WHERE proprietario_id IS NULL` = 0 e `SELECT COUNT(*) FROM locatarios WHERE proprietario_id IS NULL` = 0.

    3. Confirmar contagem de policies: `SELECT tablename, cmd, COUNT(*) FROM pg_policies WHERE schemaname='public' AND tablename IN ('edificios','unidades','locatarios','contratos','parcelas') GROUP BY tablename, cmd` — confirmar presença de policies SELECT anon em edificios e unidades.

    4. Confirmar que o insert de criarEdificio inclui proprietario_id: `grep -q "proprietario_id: user.id" src/actions/edificios.js`.

    5. Build não quebra: `npm run build` (ou `npx next build`) completa sem erro.

    6. Testes E2E existentes não quebram: `npx playwright test` — registrar resultado (se ambiente de teste exigir servidor rodando, registrar como "executar manualmente" e anotar).
  </action>
  <verify>
    <automated>test -f .planning/phases/11-multi-tenant-proprietarios/11-VERIFICATION.md && grep -q "MT-04" .planning/phases/11-multi-tenant-proprietarios/11-VERIFICATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `11-VERIFICATION.md` existe e registra cada um dos 6 itens com resultado
    - MT-04: nenhum guard de instância única encontrado em src/actions/auth.js / src/app/signup
    - Seed: ambas as queries de NULL retornam 0
    - criarEdificio contém `proprietario_id: user.id`
    - `npm run build` completa sem erro
  </acceptance_criteria>
  <done>Verificação automatizada registrada em 11-VERIFICATION.md com PASS/FAIL por item.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação humana do isolamento multi-tenant (AUDIT-01)</name>
  <action>Checkpoint de verificação humana — o executor pausa e o usuário valida os 4 cenários descritos em <how-to-verify>. Registrar o resultado de cada cenário em 11-VERIFICATION.md.</action>
  <what-built>
    Isolamento multi-tenant via RLS: cada Proprietário vê só seus dados; página pública /unidades e portal do Locatário preservados.
  </what-built>
  <how-to-verify>
    1. ISOLAMENTO ENTRE PROPRIETÁRIOS:
       - Faça login com o Proprietário de seed (existente). Confirme que vê seus edifícios/unidades/contratos/parcelas/locatários no dashboard.
       - Crie um segundo Proprietário via /signup (novo email + senha). Confirme e faça login com ele.
       - Com o Proprietário B logado, confirme que o dashboard está VAZIO (nenhum edifício/locatário/contrato de A aparece). Crie um edifício como B.
       - Volte ao Proprietário A: confirme que o edifício criado por B NÃO aparece para A.

    2. PÁGINA PÚBLICA /unidades (sem login):
       - Abra /unidades em uma aba anônima (sem sessão). Confirme que as unidades disponíveis aparecem COM o nome do edifício em cada card (não "null"/vazio) e preço ou "Consulte o Proprietário".

    3. PORTAL DO LOCATÁRIO:
       - Faça login com uma conta de Locatário existente. Confirme que /portal exibe o contrato ativo (com nome da unidade e valor) e o histórico de parcelas. Confirme que o Locatário NÃO acessa o dashboard de Proprietário.

    4. CRIAÇÃO GRAVA proprietario_id:
       - No Supabase Studio, rode `SELECT nome, proprietario_id FROM edificios ORDER BY created_at DESC LIMIT 3` e confirme que o edifício criado por B tem `proprietario_id` = userId de B.
  </how-to-verify>
  <resume-signal>Digite "approved" se todos os 4 cenários passaram, ou descreva o que falhou (qual Proprietário viu o quê).</resume-signal>
</task>

</tasks>

<verification>
- Isolamento A↔B confirmado visualmente
- /unidades anon com nome do edifício
- Portal do Locatário funcional
- proprietario_id gravado corretamente em novos inserts
- MT-04 confirmado (signup aberto)
</verification>

<success_criteria>
- MT-01, MT-02, MT-03, MT-04 verificados end-to-end e registrados em 11-VERIFICATION.md
- Critérios de sucesso 1-4 da Phase 11 (ROADMAP) confirmados
</success_criteria>

<output>
Create `.planning/phases/11-multi-tenant-proprietarios/11-03-SUMMARY.md` when done
</output>
