---
phase: 11-multi-tenant-proprietarios
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/edificios.js
  - src/actions/locatarios.js
autonomous: true
requirements: [UX-01, THEME-01, THEME-02]
gap_closure: true

must_haves:
  truths:
    - "Proprietário A não consegue editar edifícios pertencentes a Proprietário B"
    - "Proprietário A não consegue deletar edifícios pertencentes a Proprietário B"
    - "Proprietário A não consegue editar locatários pertencentes a Proprietário B"
    - "Proprietário A não consegue deletar locatários pertencentes a Proprietário B"
    - "Proprietário A não consegue revogar convites de locatários pertencentes a Proprietário B"
  artifacts:
    - path: "src/actions/edificios.js"
      provides: "editarEdificio e deletarEdificio com filtro proprietario_id"
      contains: ".eq('proprietario_id', user.id)"
    - path: "src/actions/locatarios.js"
      provides: "editarLocatario, deletarLocatario e revogarConvite com filtro proprietario_id"
      contains: ".eq('proprietario_id', user.id)"
  key_links:
    - from: "editarEdificio"
      to: "supabaseAdmin.from('edificios').update()"
      via: ".eq('proprietario_id', user.id)"
      pattern: "eq\\('proprietario_id', user\\.id\\)"
    - from: "deletarEdificio"
      to: "supabaseAdmin.from('edificios').delete()"
      via: ".eq('proprietario_id', user.id)"
      pattern: "eq\\('proprietario_id', user\\.id\\)"
    - from: "editarLocatario"
      to: "supabaseAdmin.from('locatarios').update()"
      via: ".eq('proprietario_id', user.id)"
      pattern: "eq\\('proprietario_id', user\\.id\\)"
    - from: "deletarLocatario"
      to: "supabaseAdmin.from('locatarios') select e delete"
      via: ".eq('proprietario_id', user.id)"
      pattern: "eq\\('proprietario_id', user\\.id\\)"
    - from: "revogarConvite"
      to: "supabaseAdmin.from('locatarios') select e delete"
      via: ".eq('proprietario_id', user.id)"
      pattern: "eq\\('proprietario_id', user\\.id\\)"
---

<objective>
Fechar o gap BLOCKER de IDOR nas Server Actions de escrita: editarEdificio, deletarEdificio (edificios.js) e editarLocatario, deletarLocatario, revogarConvite (locatarios.js) executam via supabaseAdmin mas filtram apenas por `.eq('id', id)`, sem escopar ao proprietario_id do usuário autenticado. Qualquer Proprietário autenticado consegue mutar registros de outro Proprietário.

Purpose: Aplicar o mesmo princípio de D-09 e D-10 (creates já escopam a proprietario_id: user.id) às operações de update e delete.

Output: 5 funções corrigidas em 2 arquivos — supabaseAdmin apenas age em linhas onde proprietario_id = user.id autenticado.
</objective>

<execution_context>
@/home/artursantana/.claude/get-shit-done/workflows/execute-plan.md
@/home/artursantana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-multi-tenant-proprietarios/11-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar filtro proprietario_id em editarEdificio e deletarEdificio</name>
  <files>src/actions/edificios.js</files>

  <read_first>
    src/actions/edificios.js — ler o arquivo completo antes de qualquer edição. Prestar atenção em:
    - Linha 9-15: authGuard() já retorna { user } mas editarEdificio (linha 31) e deletarEdificio (linha 44) ignoram o user via `const { err } = await authGuard()`.
    - Linha 39: update chain termina em `.eq('id', id)` sem escopo de proprietario_id.
    - Linha 49: delete chain termina em `.eq('id', id)` sem escopo de proprietario_id.
  </read_first>

  <action>
    Em `editarEdificio` (linha 31): trocar `const { err } = await authGuard()` por `const { err, user } = await authGuard()`. Na linha 39, encadear `.eq('proprietario_id', user.id)` após `.eq('id', id)`:

    Antes: `.update({ nome: nome.trim(), endereco: endereco.trim() }).eq('id', id)`
    Depois: `.update({ nome: nome.trim(), endereco: endereco.trim() }).eq('id', id).eq('proprietario_id', user.id)`

    Em `deletarEdificio` (linha 44): trocar `const { err } = await authGuard()` por `const { err, user } = await authGuard()`. Na linha 49, encadear `.eq('proprietario_id', user.id)` após `.eq('id', id)`:

    Antes: `.delete().eq('id', id)`
    Depois: `.delete().eq('id', id).eq('proprietario_id', user.id)`

    NÃO alterar criarEdificio, authGuard, imports, ou qualquer outra parte do arquivo.
    NÃO trocar supabaseAdmin por outro cliente — o fix é no filtro, não no cliente.
  </action>

  <verify>
    <automated>
      grep -n "proprietario_id" src/actions/edificios.js
    </automated>
    Resultado esperado: 3 ocorrências — 1 no criarEdificio (já existente, linha ~25) + 1 no editarEdificio (nova) + 1 no deletarEdificio (nova).
  </verify>

  <acceptance_criteria>
    - `grep -c "proprietario_id" src/actions/edificios.js` retorna 3
    - `grep "editarEdificio" -A 15 src/actions/edificios.js` mostra `const { err, user } = await authGuard()` (não `const { err }`)
    - `grep "deletarEdificio" -A 10 src/actions/edificios.js` mostra `const { err, user } = await authGuard()` (não `const { err }`)
    - `grep "\.eq('proprietario_id', user\.id)" src/actions/edificios.js` retorna 2 linhas (uma no update, uma no delete)
    - `grep "\.update(" src/actions/edificios.js` contém `.eq('id', id).eq('proprietario_id', user.id)` na mesma cadeia
    - `grep "\.delete()" src/actions/edificios.js` contém `.eq('id', id).eq('proprietario_id', user.id)` na mesma cadeia
  </acceptance_criteria>

  <done>editarEdificio e deletarEdificio só atuam em edifícios cujo proprietario_id = user.id autenticado. Tentativa de mutar edifício alheio resulta em 0 rows afetadas (não em erro 500).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adicionar filtro proprietario_id em editarLocatario, deletarLocatario e revogarConvite</name>
  <files>src/actions/locatarios.js</files>

  <read_first>
    src/actions/locatarios.js — ler o arquivo completo antes de qualquer edição. Prestar atenção em:
    - Linha 66: update em locatarios usa `.eq('id', id)` sem proprietario_id — user já está disponível na linha 61.
    - Linha 80: select de ownership check usa `.eq('id', id)` sem proprietario_id.
    - Linha 83: delete usa `.eq('id', id)` sem proprietario_id.
    - Linha 98-99: select em revogarConvite usa `.eq('id', id)` sem proprietario_id.
    - Linha 108: delete em revogarConvite usa `.eq('id', id)` sem proprietario_id.
  </read_first>

  <action>
    **editarLocatario (linha 66):**
    Antes: `.update({ nome_razao_social, tipo, documento, email, telefone }).eq('id', id)`
    Depois: `.update({ nome_razao_social, tipo, documento, email, telefone }).eq('id', id).eq('proprietario_id', user.id)`

    **deletarLocatario — select de ownership (linha 80):**
    Antes: `.from('locatarios').select('usuario_id').eq('id', id).single()`
    Depois: `.from('locatarios').select('usuario_id').eq('id', id).eq('proprietario_id', user.id).single()`

    **deletarLocatario — delete (linha 83):**
    Antes: `.from('locatarios').delete().eq('id', id)`
    Depois: `.from('locatarios').delete().eq('id', id).eq('proprietario_id', user.id)`

    **revogarConvite — select (linha 98-99):**
    Antes: `.from('locatarios').select('usuario_id, status_convite').eq('id', id).single()`
    Depois: `.from('locatarios').select('usuario_id, status_convite').eq('id', id).eq('proprietario_id', user.id).single()`

    **revogarConvite — delete (linha 108):**
    Antes: `.from('locatarios').delete().eq('id', id)`
    Depois: `.from('locatarios').delete().eq('id', id).eq('proprietario_id', user.id)`

    NÃO alterar convidarLocatario (já tem proprietario_id no insert, per D-09/D-10).
    NÃO trocar supabaseAdmin por outro cliente.
    NÃO alterar a lógica de autenticação (getUser + isProprietario) existente.
    NÃO alterar supabaseAdmin.auth.admin.deleteUser — essa chamada usa usuario_id, não proprietario_id, e é correta.
  </action>

  <verify>
    <automated>
      grep -n "proprietario_id" src/actions/locatarios.js
    </automated>
    Resultado esperado: pelo menos 6 ocorrências — 1 no convidarLocatario (já existente, linha ~35) + 5 novas (editarLocatario, 2x deletarLocatario, 2x revogarConvite).
  </verify>

  <acceptance_criteria>
    - `grep -c "proprietario_id" src/actions/locatarios.js` retorna 6
    - `grep "editarLocatario" -A 15 src/actions/locatarios.js` mostra `.eq('proprietario_id', user.id)` na cadeia do update
    - `grep "deletarLocatario" -A 25 src/actions/locatarios.js` mostra `.eq('proprietario_id', user.id)` duas vezes (no select e no delete)
    - `grep "revogarConvite" -A 25 src/actions/locatarios.js` mostra `.eq('proprietario_id', user.id)` duas vezes (no select e no delete)
    - `grep "\.eq('proprietario_id', user\.id)" src/actions/locatarios.js` retorna 5 linhas
    - O arquivo ainda contém `supabaseAdmin.auth.admin.deleteUser` sem alteração (não deve ter sido removido)
  </acceptance_criteria>

  <done>editarLocatario, deletarLocatario e revogarConvite só atuam em locatários cujo proprietario_id = user.id autenticado. Tentativa de mutar locatário alheio resulta em 404 (select retorna null) ou 0 rows afetadas, sem vazar dados do outro Proprietário.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | Input id vem do cliente — não confiável, deve ser validado |
| Server Action → supabaseAdmin | supabaseAdmin ignora RLS — o escopo de proprietario_id deve ser feito na query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering | editarEdificio / deletarEdificio | mitigate | Adicionar `.eq('proprietario_id', user.id)` para que supabaseAdmin só mute linhas do proprietário autenticado |
| T-11-04-02 | Tampering | editarLocatario | mitigate | Adicionar `.eq('proprietario_id', user.id)` ao update chain |
| T-11-04-03 | Tampering | deletarLocatario | mitigate | Adicionar `.eq('proprietario_id', user.id)` ao select de ownership check E ao delete |
| T-11-04-04 | Tampering | revogarConvite | mitigate | Adicionar `.eq('proprietario_id', user.id)` ao select E ao delete |
| T-11-04-05 | Information Disclosure | deletarLocatario / revogarConvite select | mitigate | Com proprietario_id no select, retorna null para id alheio — 404 sem vazar dado |
</threat_model>

<verification>
Após execução, verificar as duas correções com:

```bash
# edificios.js — deve retornar 3
grep -c "proprietario_id" src/actions/edificios.js

# locatarios.js — deve retornar 6
grep -c "proprietario_id" src/actions/locatarios.js

# Todas as ocorrências de filtro por proprietario_id (deve ser 7 no total entre os dois arquivos)
grep -rn "eq('proprietario_id', user.id)" src/actions/
```
</verification>

<success_criteria>
- Ambos os arquivos contêm `.eq('proprietario_id', user.id)` em todas as operações de escrita (update/delete) via supabaseAdmin
- editarEdificio e deletarEdificio: `const { err, user } = await authGuard()` (user não é mais descartado)
- editarLocatario: filtro adicionado ao update
- deletarLocatario: filtro adicionado ao select de ownership check e ao delete
- revogarConvite: filtro adicionado ao select e ao delete
- criarEdificio e convidarLocatario: inalterados (já corretos per D-09/D-10)
- Nenhum supabaseAdmin foi substituído por outro cliente
</success_criteria>

<output>
Criar `.planning/phases/11-multi-tenant-proprietarios/11-04-SUMMARY.md` quando concluído.
</output>
