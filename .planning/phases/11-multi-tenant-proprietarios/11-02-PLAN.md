---
phase: 11-multi-tenant-proprietarios
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/actions/edificios.js
  - src/actions/locatarios.js
autonomous: true
requirements: [MT-03]
must_haves:
  truths:
    - "criarEdificio insere proprietario_id = id do usuário autenticado"
    - "convidarLocatario insere proprietario_id = id do Proprietário logado"
    - "authGuard() retorna o objeto user para uso no insert"
  artifacts:
    - path: "src/actions/edificios.js"
      provides: "criarEdificio com proprietario_id; authGuard retornando user"
      contains: "proprietario_id"
    - path: "src/actions/locatarios.js"
      provides: "convidarLocatario com proprietario_id no insert de locatarios"
      contains: "proprietario_id"
  key_links:
    - from: "criarEdificio"
      to: "edificios.proprietario_id"
      via: "insert payload com user.id"
      pattern: "proprietario_id: user\\.id"
    - from: "convidarLocatario"
      to: "locatarios.proprietario_id"
      via: "insert payload com user.id"
      pattern: "proprietario_id: user\\.id"
---

<objective>
Atualizar as Server Actions `criarEdificio` e `convidarLocatario` para gravar `proprietario_id = <id do Proprietário autenticado>` no payload de insert. Ajustar `authGuard()` em `edificios.js` para expor o `user`.

Purpose: Como todas as mutations usam `supabaseAdmin` (service role) que bypassa RLS, o `proprietario_id` correto NÃO é preenchido automaticamente — precisa ser explícito no payload (atende MT-03). Sem isso, novos edifícios/locatários ficariam órfãos ou violariam o NOT NULL da Plan 1.

Output: `src/actions/edificios.js` e `src/actions/locatarios.js` atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-multi-tenant-proprietarios/11-CONTEXT.md
@.planning/phases/11-multi-tenant-proprietarios/11-RESEARCH.md
@src/actions/edificios.js
@src/actions/locatarios.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: authGuard retorna user + criarEdificio insere proprietario_id (D-09)</name>
  <read_first>
    - src/actions/edificios.js (authGuard linha 9-15 retorna {} ou {err}; criarEdificio linha 17-28)
    - .planning/phases/11-multi-tenant-proprietarios/11-RESEARCH.md (seção 2.3 — authGuard fix)
  </read_first>
  <files>src/actions/edificios.js</files>
  <action>
    Em `src/actions/edificios.js`:

    1. Alterar `authGuard()` (linha 9-15) para retornar o `user` no caso de sucesso: trocar `return {}` por `return { user }`. O `user` já está disponível na desestruturação `const { data: { user } } = await supabase.auth.getUser()` (linha 11). Os retornos de erro (`{ err: ... }`) permanecem inalterados.

    2. Em `criarEdificio` (linha 17), trocar `const { err } = await authGuard()` por `const { err, user } = await authGuard()`.

    3. No insert (linha 25), adicionar `proprietario_id: user.id` ao objeto: `supabaseAdmin.from('edificios').insert({ nome: nome.trim(), endereco: endereco.trim(), proprietario_id: user.id })`.

    NÃO alterar `editarEdificio` nem `deletarEdificio` — estão fora do escopo das decisões D-01..D-13 (hardening de ownership na escrita foi explicitamente marcado como fora de escopo em 11-RESEARCH.md seção 5 item 4). Manter o destructure `const { err } = await authGuard()` nessas duas funções (elas não usam `user`).
  </action>
  <verify>
    <automated>grep -q "return { user }" src/actions/edificios.js && grep -q "proprietario_id: user.id" src/actions/edificios.js && node -e "require('@babel/core') || 0" 2>/dev/null; npx eslint src/actions/edificios.js --no-eslintrc --parser-options=ecmaVersion:2022,sourceType:module 2>/dev/null; grep -q "proprietario_id: user.id" src/actions/edificios.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `authGuard()` contém `return { user }` no caminho de sucesso (não `return {}`)
    - `criarEdificio` desestrutura `const { err, user } = await authGuard()`
    - O insert de `edificios` contém `proprietario_id: user.id`
    - `editarEdificio` e `deletarEdificio` permanecem inalterados (ainda `const { err } = await authGuard()`)
  </acceptance_criteria>
  <done>edificios.js: authGuard expõe user; criarEdificio grava proprietario_id; editar/deletar intactos.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: convidarLocatario insere proprietario_id (D-10)</name>
  <read_first>
    - src/actions/locatarios.js (convidarLocatario linha 11-56; user já disponível na linha 24)
    - .planning/phases/11-multi-tenant-proprietarios/11-RESEARCH.md (seção 2.3 — convidarLocatario)
  </read_first>
  <files>src/actions/locatarios.js</files>
  <action>
    Em `src/actions/locatarios.js`, função `convidarLocatario`: o `user` (o Proprietário logado) já está disponível na linha 24 via `const { data: { user } } = await supabase.auth.getUser()`. No insert de `locatarios` (linha 31-38), adicionar o campo `proprietario_id: user.id` ao objeto inserido — ao lado dos campos existentes `usuario_id`, `nome_razao_social`, `email`, `telefone`, `documento`, `tipo`.

    Atenção à distinção: `usuario_id: data.user.id` é o ID do NOVO usuário convidado (o Locatário, retornado por `inviteUserByEmail`). `proprietario_id: user.id` é o ID do Proprietário logado que está criando o convite. São dois IDs diferentes — não confundir.

    NÃO alterar `editarLocatario`, `deletarLocatario` nem `revogarConvite` — fora de escopo.
  </action>
  <verify>
    <automated>grep -q "proprietario_id: user.id" src/actions/locatarios.js && grep -q "usuario_id:data.user.id\|usuario_id: data.user.id" src/actions/locatarios.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - O insert de `locatarios` em `convidarLocatario` contém `proprietario_id: user.id`
    - O campo `usuario_id: data.user.id` (Locatário convidado) permanece presente e distinto de `proprietario_id`
    - `editarLocatario`, `deletarLocatario`, `revogarConvite` permanecem inalterados
  </acceptance_criteria>
  <done>convidarLocatario grava proprietario_id = user.id (Proprietário logado) no insert de locatarios.</done>
</task>

</tasks>

<verification>
- `criarEdificio` grava `proprietario_id: user.id`
- `convidarLocatario` grava `proprietario_id: user.id`
- `authGuard()` retorna `{ user }`
- Nenhuma outra action alterada
</verification>

<success_criteria>
- MT-03: criarEdificio e convidarLocatario incluem proprietario_id = userId nas inserções
</success_criteria>

<output>
Create `.planning/phases/11-multi-tenant-proprietarios/11-02-SUMMARY.md` when done
</output>
