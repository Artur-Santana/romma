---
phase: 14-anima-es-feedback
plan: 03
type: execute
wave: 1
depends_on: ["14-00"]
files_modified:
  - src/components/features/LocatariosDesktop.js
  - src/components/features/Locatarios.js
autonomous: true
requirements: [ANIM-02, ANIM-03]
user_setup: []

must_haves:
  truths:
    - "Revoking a Locatário's access fades the row out (~200ms) before it leaves the list"
    - "A toast confirms a successful acesso revogado"
    - "On revoke error the row is NOT left invisible-but-present (removingIds rollback)"
  artifacts:
    - path: "src/components/features/LocatariosDesktop.js"
      provides: "removingIds exit animation + 'Acesso revogado' toast (re-fetch variant)"
      contains: "removingIds"
    - path: "src/components/features/Locatarios.js"
      provides: "removingIds exit animation + toast (legacy mobile component, currently unmounted)"
      contains: "removingIds"
  key_links:
    - from: "src/components/features/LocatariosDesktop.js"
      to: "sonner"
      via: "import toast from sonner"
      pattern: "from \"sonner\""
---

<objective>
Wire exit animation (ANIM-02) + "Acesso revogado" toast (ANIM-03) into LocatariosDesktop.js (the mounted component) and apply the same pattern to Locatarios.js (legacy mobile, per D-05), both using the RE-FETCH-AFTER-TIMEOUT variant.

Purpose: revogarConvite and deletarLocatario are DELETE actions — the row leaves the DB, so re-fetch inside the setTimeout is safe. LocatariosDesktop is the route-mounted component; Locatarios.js is included per D-05 but is currently unmounted. Files do not overlap with any other Wave 1 plan.
Output: LocatariosDesktop.js and Locatarios.js with removingIds animation + toast.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-anima-es-feedback/14-CONTEXT.md
@.planning/phases/14-anima-es-feedback/14-RESEARCH.md
@.planning/phases/14-anima-es-feedback/14-PATTERNS.md
@.planning/phases/14-anima-es-feedback/14-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exit animation + toast in LocatariosDesktop (mounted, re-fetch variant)</name>
  <files>src/components/features/LocatariosDesktop.js</files>
  <behavior>
    - handleRevogar: adds id to removingIds before await; on status!==200 sets erro + rolls back removingIds + returns; on status===200 fires toast.success("Acesso revogado") then after 200ms re-fetches getLocatarios() and clears removingId
    - The locatário row fades out (opacity 0 + scale 0.97) over 200ms before re-fetch
    - On error the row is not left invisible-but-present
  </behavior>
  <read_first>
    - src/components/features/LocatariosDesktop.js (FULL read — handler ~91-99, map ~133-200 with row div style={{ display: "grid", gridTemplateColumns: GRID }})
    - .planning/phases/14-anima-es-feedback/14-PATTERNS.md (LocatariosDesktop.js section lines 300-367 — handler before/after, map style merge)
    - .planning/phases/14-anima-es-feedback/14-RESEARCH.md (Pitfalls 2 & 4)
    - .planning/phases/14-anima-es-feedback/14-UI-SPEC.md (Exit-Animation Contract values)
  </read_first>
  <action>Add the sonner toast import (toast function only). Add `const [removingIds, setRemovingIds] = useState(new Set())` near the existing useState declarations. Rewrite handleRevogar: set removingIds before the await; on `status !== 200` call setErro with the existing fallback string AND roll back removingIds, then return; on success call `toast.success("Acesso revogado")`, then inside `setTimeout(..., 200)` run `getLocatarios().then(l => setLocatarios(l ?? []))` and clear the removingId. In the map row div (existing style={{ display: "grid", gridTemplateColumns: GRID }}), derive `const isRemoving = removingIds.has(l.id)` and MERGE opacity/transform/transition into the existing style object (opacity isRemoving ? 0 : 1, transform isRemoving ? "scale(0.97)" : "scale(1)", transition "opacity 200ms ease, transform 200ms ease") — single merged style prop, keep the existing className. Mutate the Set immutably. Do NOT add error toasts.</action>
  <verify>
    <automated>grep -q 'from "sonner"' src/components/features/LocatariosDesktop.js && grep -q 'removingIds' src/components/features/LocatariosDesktop.js && grep -q '"Acesso revogado"' src/components/features/LocatariosDesktop.js && grep -q 'scale(0.97)' src/components/features/LocatariosDesktop.js && grep -q 'setTimeout' src/components/features/LocatariosDesktop.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - LocatariosDesktop.js imports the toast function from "sonner" (not Toaster)
    - `const [removingIds, setRemovingIds] = useState(new Set())` present
    - handleRevogar sets removingIds before await; on status!==200 → setErro + rollback + return; on success → toast.success("Acesso revogado") then setTimeout(...,200) with getLocatarios() re-fetch and removingId clear
    - Row div merges opacity/transform/transition into the existing display:grid style object (single style prop)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Revoking access fades the row out, toasts "Acesso revogado", then re-fetches; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Exit animation + toast in Locatarios (legacy mobile, per D-05)</name>
  <files>src/components/features/Locatarios.js</files>
  <read_first>
    - src/components/features/Locatarios.js (FULL read — handler ~66-71, map ~112-168, note setter is `setlocatarios` lowercase l)
    - .planning/phases/14-anima-es-feedback/14-PATTERNS.md (Locatarios.js section lines 371-434 — MOUNT WARNING + handler before/after + map style, preserve setlocatarios spelling)
    - .planning/phases/14-anima-es-feedback/14-UI-SPEC.md (Exit-Animation Contract values)
  </read_first>
  <action>NOTE (per PATTERNS MOUNT WARNING): Locatarios.js is a legacy mobile component NOT imported by any route (/dashboard/locatarios mounts LocatariosDesktop). Apply the changes per D-05 anyway; the changes will have no visible effect until the component is routed. Do NOT attempt to wire it to a route (out of scope). Add the sonner toast import (toast function only). Add `const [removingIds, setRemovingIds] = useState(new Set())`. Rewrite handleDeletarLocatario: set removingIds before the await; on `status !== 200` roll back removingIds and return; on success call `toast.success("Acesso revogado")`, then inside `setTimeout(..., 200)` run `getLocatarios().then(l => setlocatarios(l ?? []))` — PRESERVE the lowercase `setlocatarios` setter exactly (existing inconsistency; renaming breaks the component) — and clear the removingId. In the map, derive `const isRemoving = removingIds.has(locatario.id)` and add the opacity/transform/transition style to the row `<div key={locatario.id}>`. Mutate the Set immutably.</action>
  <verify>
    <automated>grep -q 'from "sonner"' src/components/features/Locatarios.js && grep -q 'removingIds' src/components/features/Locatarios.js && grep -q '"Acesso revogado"' src/components/features/Locatarios.js && grep -q 'setlocatarios' src/components/features/Locatarios.js && grep -q 'scale(0.97)' src/components/features/Locatarios.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Locatarios.js imports the toast function from "sonner"
    - `const [removingIds, setRemovingIds] = useState(new Set())` present
    - handleDeletarLocatario sets removingIds before await; on status!==200 → rollback + return; on success → toast.success("Acesso revogado") then setTimeout(...,200) re-fetch using the lowercase setlocatarios setter
    - Lowercase setlocatarios setter is preserved (NOT renamed)
    - Row div has opacity/transform/transition style driven by isRemoving
    - npm run build exits 0
  </acceptance_criteria>
  <done>Locatarios.js carries the same animation + toast pattern with setlocatarios preserved; build green. (No visible effect until the component is routed — documented per D-05.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. Both edits touch Client Component local state, toasts, and inline styles only. The revogarConvite and deletarLocatario Server Actions are unchanged; no new endpoint, query, auth, or user-input parsing is introduced (RESEARCH Security Domain: V2-V6 all N/A).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-SC | Tampering | sonner package (installed in 14-00) | mitigate | sonner APPROVED in RESEARCH Package Legitimacy Audit. No new install in this plan; no blocking-human checkpoint. |
| T-14-03-I | Information Disclosure | toast.success string | accept | Static literal ("Acesso revogado") — no PII, no user input, no locatário data leaked. |
</threat_model>

<verification>
- npm run build green.
- Source asserts: sonner imports, removingIds, "Acesso revogado", scale(0.97) style, setlocatarios preserved.
- Manual (ANIM-02, 200ms timing): in /dashboard/locatarios, revogar a locatário → row fades (opacity + scale) ~200ms before removal.
- Phase-gate spec tests/toast-feedback.spec.js (from 14-00) covers the revogar toast flow owned here — run at phase level.
- Locatarios.js changes have no visible effect until routed (legacy mobile, unmounted) — expected.
</verification>

<success_criteria>
- ANIM-02: revogar acesso animates the row out.
- ANIM-03: "Acesso revogado" toast fires on success (LocatariosDesktop).
- D-05: Locatarios.js carries the same pattern (unmounted, documented).
- D-09: revoke/delete error rolls back removingIds.
</success_criteria>

<output>
Create `.planning/phases/14-anima-es-feedback/14-03-SUMMARY.md` when done.
</output>
