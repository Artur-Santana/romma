---
phase: 14-anima-es-feedback
plan: 02
type: execute
wave: 1
depends_on: ["14-00"]
files_modified:
  - src/components/features/Unidades.js
  - src/components/features/Parcelas.js
autonomous: true
requirements: [ANIM-02, ANIM-03]
user_setup: []

must_haves:
  truths:
    - "Deleting a unidade fades the card out (~200ms) before it leaves the list"
    - "A toast confirms a successful unidade removal"
    - "A toast confirms a successful parcela marcada como paga (no exit animation — item stays with new status)"
    - "On delete error the unidade card is NOT left invisible-but-present (removingIds rollback)"
  artifacts:
    - path: "src/components/features/Unidades.js"
      provides: "removingIds exit animation (wrapper div) + toast on delete (re-fetch variant)"
      contains: "removingIds"
    - path: "src/components/features/Parcelas.js"
      provides: "toast.success on marcarComoPaga, no exit animation"
      contains: "Parcela marcada como paga"
  key_links:
    - from: "src/components/features/Unidades.js"
      to: "UnidadeCard"
      via: "wrapper div owns key + animation style (UnidadeCard untouched)"
      pattern: "removingIds.has"
---

<objective>
Wire exit animation (ANIM-02) + toast (ANIM-03) into Unidades.js using the RE-FETCH-AFTER-TIMEOUT variant, and add the toast-only flow (ANIM-03) to Parcelas.js.

Purpose: Both are DELETE/UPDATE flows distinct from Contratos. Unidades needs a wrapper div because UnidadeCard is opaque. Parcelas is toast-only (status change in place, no removal). Files do not overlap with any other Wave 1 plan.
Output: Unidades.js with removingIds + wrapper-div animation + "Unidade removida" toast; Parcelas.js with "Parcela marcada como paga" toast.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-anima-es-feedback/14-CONTEXT.md
@.planning/phases/14-anima-es-feedback/14-RESEARCH.md
@.planning/phases/14-anima-es-feedback/14-PATTERNS.md
@.planning/phases/14-anima-es-feedback/14-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exit animation + toast in Unidades (wrapper div, re-fetch variant)</name>
  <files>src/components/features/Unidades.js</files>
  <behavior>
    - handleDeletarUnidade: adds id to removingIds before await; on status!==200 sets erroDelete + rolls back removingIds + returns; on status===200 fires toast.success("Unidade removida") then after 200ms re-fetches getUnidades() and clears removingId
    - The unidade card fades out (opacity 0 + scale 0.97) over 200ms before the row is re-fetched away
    - On error the card is not left invisible-but-present
  </behavior>
  <read_first>
    - src/components/features/Unidades.js (FULL read — handler ~83-92, map ~283-297 rendering UnidadeCard directly; erroDelete/erroEdit state)
    - .planning/phases/14-anima-es-feedback/14-PATTERNS.md (Unidades.js section lines 209-296 — wrapper-div requirement, handler before/after, key moves to wrapper, UnidadeCard untouched)
    - .planning/phases/14-anima-es-feedback/14-RESEARCH.md (Pitfalls 2 & 4 — re-fetch inside setTimeout; rollback on error)
    - .planning/phases/14-anima-es-feedback/14-UI-SPEC.md (Exit-Animation Contract values)
  </read_first>
  <action>Add the sonner toast import (toast function only). Add `const [removingIds, setRemovingIds] = useState(new Set())` near the existing useState declarations. Rewrite handleDeletarUnidade: set removingIds before the await (new Set([...prev, id])); on `result.status !== 200` call `setErroDelete(result.erroMessage)` AND roll back removingIds, then return; on success call `toast.success("Unidade removida")` (D-08 message — confirmed, resolves Q1), then inside `setTimeout(..., 200)` run `getUnidades().then(u => setUnidades(u ?? []))` and clear the removingId. In the map, wrap each UnidadeCard in a `<div>` that owns `key={unidade.id}` and the animation style (derive `const isRemoving = removingIds.has(unidade.id)`; opacity isRemoving ? 0 : 1, transform isRemoving ? "scale(0.97)" : "scale(1)", transition "opacity 200ms ease, transform 200ms ease"); move the key from UnidadeCard to the wrapper. Do NOT modify UnidadeCard.js — it is opaque and unchanged. Preserve the existing erroDelete/erroEdit separation (BUG-02 fix). Mutate the Set immutably.</action>
  <verify>
    <automated>grep -q 'from "sonner"' src/components/features/Unidades.js && grep -q 'removingIds' src/components/features/Unidades.js && grep -q '"Unidade removida"' src/components/features/Unidades.js && grep -q 'scale(0.97)' src/components/features/Unidades.js && grep -q 'setTimeout' src/components/features/Unidades.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Unidades.js imports the toast function from "sonner" (not Toaster)
    - `const [removingIds, setRemovingIds] = useState(new Set())` present
    - handleDeletarUnidade sets removingIds before await; on status!==200 → setErroDelete + rollback + return; on success → toast.success("Unidade removida") then setTimeout(...,200) with getUnidades() re-fetch and removingId clear
    - Map wraps UnidadeCard in a div carrying key={unidade.id} and the opacity/transform/transition style; UnidadeCard.js is NOT modified
    - npm run build exits 0
  </acceptance_criteria>
  <done>Deleting a unidade fades the card out, toasts "Unidade removida", then re-fetches; build green; UnidadeCard untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Toast on marcarComoPaga in Parcelas (no animation)</name>
  <files>src/components/features/Parcelas.js</files>
  <read_first>
    - src/components/features/Parcelas.js (handler ~49-57)
    - .planning/phases/14-anima-es-feedback/14-PATTERNS.md (Parcelas.js section lines 438-474 — add one line, no removingIds, no setTimeout)
    - .planning/phases/14-anima-es-feedback/14-CONTEXT.md (D-04 — parcelas: toast only, no exit animation)
  </read_first>
  <action>Add the sonner toast import (toast function only). In marcarComoPaga, add `toast.success("Parcela marcada como paga")` inside the existing `result.status === 200` branch, immediately before/after the existing setParcelas re-fetch (the re-fetch stays immediate — the item changes status in place, it does NOT leave the list). Do NOT add removingIds, setTimeout, or any exit animation (D-04). Do NOT add error toasts.</action>
  <verify>
    <automated>grep -q 'from "sonner"' src/components/features/Parcelas.js && grep -q '"Parcela marcada como paga"' src/components/features/Parcelas.js && ! grep -q 'removingIds' src/components/features/Parcelas.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Parcelas.js imports the toast function from "sonner"
    - marcarComoPaga fires toast.success("Parcela marcada como paga") only inside the status===200 branch
    - NO removingIds and NO setTimeout/exit-animation added to Parcelas.js
    - npm run build exits 0
  </acceptance_criteria>
  <done>Marking a parcela paga shows the toast; the item stays in the list with updated status; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. Both edits touch Client Component local state, toasts, and inline styles only. The deletarUnidade and marcarParcelaComoPaga Server Actions are unchanged; no new endpoint, query, auth, or user-input parsing is introduced (RESEARCH Security Domain: V2-V6 all N/A).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-SC | Tampering | sonner package (installed in 14-00) | mitigate | sonner APPROVED in RESEARCH Package Legitimacy Audit. No new install in this plan; no blocking-human checkpoint. |
| T-14-02-I | Information Disclosure | toast.success strings | accept | Static literals ("Unidade removida", "Parcela marcada como paga") — no PII, no user input, no record data. |
</threat_model>

<verification>
- npm run build green.
- Source asserts: sonner imports, removingIds (Unidades only), 2 toast strings, wrapper-div animation, no removingIds in Parcelas.
- Manual (ANIM-02, 200ms timing): delete a unidade → card fades (opacity + scale) ~200ms before removal.
- Phase-gate spec tests/toast-feedback.spec.js (from 14-00) covers the unidade + parcela toast flows owned here — run at phase level.
</verification>

<success_criteria>
- ANIM-02: deleting a unidade animates the card out.
- ANIM-03: "Unidade removida" and "Parcela marcada como paga" toasts fire on success.
- D-04: Parcelas has toast only, no animation.
- D-09: Unidades delete error rolls back removingIds.
</success_criteria>

<output>
Create `.planning/phases/14-anima-es-feedback/14-02-SUMMARY.md` when done.
</output>
