---
phase: 14-anima-es-feedback
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/app/layout.js
  - tests/toast-feedback.spec.js
autonomous: true
requirements: [ANIM-03]
user_setup: []

must_haves:
  truths:
    - "sonner is installed and listed in package.json dependencies"
    - "A single <Toaster> is mounted in the root layout, covering every page"
    - "A failing E2E spec exists asserting toast.success appears after the 5 main actions"
  artifacts:
    - path: "src/app/layout.js"
      provides: "Single Toaster mount point with UI-SPEC props/CSS-var theming"
      contains: "Toaster"
    - path: "tests/toast-feedback.spec.js"
      provides: "RED E2E smoke covering ANIM-03 toast flows"
      contains: "toast-feedback"
    - path: "package.json"
      provides: "sonner dependency"
      contains: "sonner"
  key_links:
    - from: "src/app/layout.js"
      to: "sonner"
      via: "import { Toaster } from \"sonner\""
      pattern: "from \"sonner\""
---

<objective>
Install `sonner` (D-01) and mount a single `<Toaster>` in the root layout with the exact UI-SPEC theming, then create the RED E2E spec that the rest of the phase will turn green.

Purpose: Establishes the foundation every Wave 1 plan depends on — without the Toaster mounted and the `sonner` package present, no `toast.success()` call renders. The RED spec defines ANIM-03 acceptance up front (Nyquist Wave 0 gap from VALIDATION.md).
Output: `sonner` in package.json, `<Toaster>` in `src/app/layout.js`, `tests/toast-feedback.spec.js` (failing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-anima-es-feedback/14-CONTEXT.md
@.planning/phases/14-anima-es-feedback/14-RESEARCH.md
@.planning/phases/14-anima-es-feedback/14-UI-SPEC.md
@.planning/phases/14-anima-es-feedback/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install sonner</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (confirm current dependencies; no sonner present)
    - .planning/phases/14-anima-es-feedback/14-RESEARCH.md (Standard Stack + Package Legitimacy Audit — sonner 2.0.7, approved, maintainer Emil Kowalski)
  </read_first>
  <action>Run `npm install sonner`. sonner is APPROVED in the Package Legitimacy Audit (RESEARCH.md: maintainer Emil Kowalski, ~3M weekly downloads, 10k+ stars, `'use client'` internal) — no human verification checkpoint required. Do NOT add any other dependency; D-02 forbids framer-motion / tw-animate-css / any extra animation lib.</action>
  <verify>
    <automated>node -e "process.exit(require('./package.json').dependencies.sonner ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains a "sonner" entry
    - No new dependency other than sonner added (grep package.json diff — no framer-motion, no tw-animate addition)
  </acceptance_criteria>
  <done>`sonner` appears in package.json dependencies and `node_modules/sonner` exists.</done>
</task>

<task type="auto">
  <name>Task 2: Mount Toaster in root layout (D-01)</name>
  <files>src/app/layout.js</files>
  <read_first>
    - src/app/layout.js (current state — Server Component, no `'use client'`, SpeedInsights imported but NOT rendered)
    - .planning/phases/14-anima-es-feedback/14-PATTERNS.md (layout.js section, lines 34-80 — exact Toaster props + CSS-var style object)
    - .planning/phases/14-anima-es-feedback/14-UI-SPEC.md (Toast Contract — Toaster Component Props table, mobileOffset rationale)
  </read_first>
  <action>Add `import { Toaster } from "sonner"` at the top of layout.js. Render exactly ONE `<Toaster>` as the last child inside `<body>`, after `{children}`, with these exact props from UI-SPEC: `theme="dark"`, `richColors`, `position="bottom-right"`, `mobileOffset={{ bottom: "80px" }}`, and the inline `style` object mapping sonner CSS vars to project tokens: `--normal-bg: var(--surface)`, `--normal-text: var(--fg-1)`, `--normal-border: var(--border-2)`, `--success-bg: var(--surface)`, `--success-text: var(--fg-1)`, `--success-border: var(--success)`, `--border-radius: "0px"`. Keep layout.js a Server Component — do NOT add `'use client'` (sonner has `'use client'` internally, same pattern as SpeedInsights). Do NOT start rendering `<SpeedInsights />` — it is imported but intentionally absent from the body; leave that unchanged. The `<Toaster>` mounts ONLY here — feature components import only `{ toast }`, never `{ Toaster }`.</action>
  <verify>
    <automated>grep -q 'from "sonner"' src/app/layout.js && grep -q 'Toaster' src/app/layout.js && grep -q 'mobileOffset' src/app/layout.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/layout.js contains `import { Toaster } from "sonner"`
    - `<Toaster` is rendered inside `<body>` after `{children}` with `theme="dark"`, `richColors`, `position="bottom-right"`, `mobileOffset={{ bottom: "80px" }}` and the `--border-radius: "0px"` + `--success-border: var(--success)` style overrides
    - layout.js has NO `'use client'` directive
    - `<SpeedInsights />` is still NOT rendered (no new render added)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Layout builds with a single themed Toaster mounted; no other layout behavior changed.</done>
</task>

<task type="auto">
  <name>Task 3: RED E2E spec for ANIM-03 toast flows</name>
  <files>tests/toast-feedback.spec.js</files>
  <read_first>
    - tests/ (read 1-2 existing *.spec.js for project Playwright conventions: login helper, selectors, baseURL)
    - playwright.config.js (baseURL, projects)
    - .planning/phases/14-anima-es-feedback/14-VALIDATION.md (Wave 0 Requirements — spec covers ANIM-03 across all 5 actions)
    - .planning/phases/14-anima-es-feedback/14-UI-SPEC.md (Toast Messages table — exact strings)
  </read_first>
  <action>Create `tests/toast-feedback.spec.js` as a Playwright E2E smoke covering ANIM-03. Follow existing spec conventions (reuse the project's login/auth setup pattern from a sibling spec). Assert that after each main success action a sonner toast with the exact D-08 string is visible. The 5 toast strings to assert: "Contrato criado", "Contrato encerrado" OR "Contrato cancelado", "Unidade removida", "Acesso revogado", "Parcela marcada como paga". Locate toasts via sonner's rendered region — assert `page.getByText("<exact string>")` becomes visible after the triggering action. This spec is the PHASE GATE — it covers flows owned by multiple Wave 1 plans and is EXPECTED TO FAIL now (handlers not yet wired). Do not stub it to pass. Mark it clearly as the ANIM-03 acceptance spec in a top-of-file comment. If full UI driving for an action is impractical in this stub, leave a `test.fixme()` or failing assertion for that flow so the spec is genuinely RED — do not silently skip ANIM-03 coverage.</action>
  <verify>
    <automated>test -f tests/toast-feedback.spec.js && grep -q "Parcela marcada como paga" tests/toast-feedback.spec.js && npx playwright test tests/toast-feedback.spec.js --project=chromium; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - tests/toast-feedback.spec.js exists
    - File references all 5 D-08 toast strings as assertions
    - `npx playwright test tests/toast-feedback.spec.js` exits NON-zero (RED — expected, handlers not yet implemented)
  </acceptance_criteria>
  <done>The spec file exists, asserts the 5 toast messages, and fails because no handler fires toasts yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Third-party package code enters the build |

This phase is purely client-side UI (toast state + CSS). No new endpoints, Server Actions, queries, auth, or user-input handling are introduced. `toast.success()` receives string literals only — no user input crosses any boundary (RESEARCH Security Domain: V2-V6 all N/A).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-SC | Tampering | `npm install sonner` | mitigate | sonner verified APPROVED in RESEARCH Package Legitimacy Audit — public maintainer (Emil Kowalski), ~3M weekly downloads, 10k+ stars, source repo confirmed. Not [ASSUMED]/[SUS]/[SLOP] → no blocking-human checkpoint required. |
</threat_model>

<verification>
- `node_modules/sonner` present; sonner in package.json.
- `npm run build` green with Toaster mounted.
- `npx playwright test tests/toast-feedback.spec.js` is RED (foundation in place, handlers pending in Wave 1).
</verification>

<success_criteria>
- sonner installed (ANIM-03 foundation).
- Single themed `<Toaster>` mounted in root layout with UI-SPEC props.
- RED ANIM-03 spec exists and fails for the right reason.
</success_criteria>

<output>
Create `.planning/phases/14-anima-es-feedback/14-00-SUMMARY.md` when done.
</output>
