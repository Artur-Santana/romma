---
phase: 14-anima-es-feedback
plan: 01
type: execute
wave: 1
depends_on: ["14-00"]
files_modified:
  - src/components/features/Contratos.js
autonomous: true
requirements: [ANIM-01, ANIM-03]
user_setup: []

must_haves:
  truths:
    - "Cancelling or encerrando a contrato fades the item out (~200ms) before it leaves the list"
    - "A toast confirms each successful criar/encerrar/cancelar contrato"
    - "An encerrado/cancelado contrato does not reappear in the main listing on reload (only ativo shown)"
    - "On action error the item is NOT left invisible-but-present (removingIds rollback)"
  artifacts:
    - path: "src/components/features/Contratos.js"
      provides: "removingIds exit animation + toasts + ativo-only main listing (optimistic filter)"
      contains: "removingIds"
  key_links:
    - from: "src/components/features/Contratos.js"
      to: "sonner"
      via: "import toast from sonner"
      pattern: "from \"sonner\""
    - from: "confirmarCancelamento / confirmarEncerramento"
      to: "contratos state"
      via: "optimistic filter, NO getContratos() re-fetch"
      pattern: "filter\\(c"
---

<objective>
Wire exit animations (ANIM-01) and success toasts (ANIM-03) into Contratos.js using the OPTIMISTIC FILTER variant, and enforce D-07 (main listing shows only status === 'ativo').

Purpose: Contratos is the only UPDATE-based variant — getContratos() returns all statuses, so re-fetching after cancelar/encerrar would resurrect the item. This plan owns all three Contratos toast flows plus the D-07 reconciliation, the phase's one real landmine.
Output: Contratos.js with removingIds Set state, 3 toasts, optimistic filter, and an ativo-only main listing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-anima-es-feedback/14-CONTEXT.md
@.planning/phases/14-anima-es-feedback/14-RESEARCH.md
@.planning/phases/14-anima-es-feedback/14-PATTERNS.md
@.planning/phases/14-anima-es-feedback/14-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exit animation + toasts + optimistic filter in Contratos handlers</name>
  <files>src/components/features/Contratos.js</files>
  <behavior>
    - confirmarCancelamento: adds id to removingIds before await; on status!==200 rolls back removingIds and returns; on status===200 fires toast.success("Contrato cancelado") then after 200ms optimistic-filters the contrato out of state, re-fetches unidades, clears removingId
    - confirmarEncerramento: identical shape, toast.success("Contrato encerrado")
    - handleCriarContrato: fires toast.success("Contrato criado") inside the status===200 branch (no animation — item enters, not leaves)
    - On error neither handler leaves an item with opacity 0 stuck in the list
  </behavior>
  <read_first>
    - src/components/features/Contratos.js (FULL read — current handlers ~137-155, handleCriarContrato ~90-111, map ~314-399, and any existing ativos/encerrados derivation ~157)
    - .planning/phases/14-anima-es-feedback/14-PATTERNS.md (Contratos.js section, lines 84-205 — exact before/after for both confirm handlers, handleCriarContrato toast placement, map style merge, D-07 derived-variable approach)
    - .planning/phases/14-anima-es-feedback/14-RESEARCH.md (Pitfall 1 — never re-fetch getContratos after cancelar/encerrar)
  </read_first>
  <action>Add the sonner toast import (toast function only — never import Toaster). Add `const [removingIds, setRemovingIds] = useState(new Set())` near the other useState declarations. Rewrite confirmarCancelamento and confirmarEncerramento per the PATTERNS before/after: set removingIds before the await; on `result.status !== 200` call `setErro(result.erroMessage)` AND roll back removingIds (new Set(prev) minus id), then return; on success call `toast.success("Contrato cancelado")` / `toast.success("Contrato encerrado")` immediately, then inside `setTimeout(..., 200)` run `setContratos(prev => prev.filter(c => c.id !== contrato.id))` (OPTIMISTIC — do NOT call getContratos()), re-fetch unidades only via `getUnidades().then(u => setUnidades(u ?? []))`, and clear the removingId. Use the erroMessage spelling. In handleCriarContrato, add `toast.success("Contrato criado")` inside the existing status === 200 success branch (no animation). Mutate the Set immutably — new Set([...prev, id]) to add, new Set(prev) + .delete(id) to remove. Do NOT add error toasts (deferred per CONTEXT).</action>
  <verify>
    <automated>grep -q 'from "sonner"' src/components/features/Contratos.js && grep -q 'removingIds' src/components/features/Contratos.js && grep -q '"Contrato criado"' src/components/features/Contratos.js && grep -q '"Contrato encerrado"' src/components/features/Contratos.js && grep -q '"Contrato cancelado"' src/components/features/Contratos.js && grep -Eq 'filter\(c ?=>' src/components/features/Contratos.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Contratos.js imports the toast function from "sonner" and does NOT import Toaster
    - `const [removingIds, setRemovingIds] = useState(new Set())` present
    - confirmarCancelamento awaits cancelarContrato, on status!==200 does setErro + removingIds rollback + return; on success fires toast.success("Contrato cancelado") then setTimeout(...,200) with setContratos(prev => prev.filter(...)) and getUnidades() re-fetch
    - confirmarEncerramento mirrors it with toast.success("Contrato encerrado")
    - handleCriarContrato fires toast.success("Contrato criado") only inside the status===200 branch
    - NO getContratos() call inside either cancelar/encerrar setTimeout success path
    - npm run build exits 0
  </acceptance_criteria>
  <done>Both confirm handlers animate-then-optimistic-remove with toasts; criar fires its toast; build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Conditional exit style on row + D-07 ativo-only main listing</name>
  <files>src/components/features/Contratos.js</files>
  <read_first>
    - src/components/features/Contratos.js (the map at ~314-399 and the existing ativos/encerrados derivation at ~157 — read BOTH; reconcile, do not duplicate)
    - .planning/phases/14-anima-es-feedback/14-PATTERNS.md (Contratos.js map style merge lines 172-205; D-07 derived-variable note line 199)
    - .planning/phases/14-anima-es-feedback/14-UI-SPEC.md (Exit-Animation Contract — opacity/transform/transition values)
    - .planning/phases/14-anima-es-feedback/14-CONTEXT.md (D-07 — filter applies to MAIN listing only; "não remove dados do banco")
  </read_first>
  <action>In the main contratos map row div (currently style={COL_STYLE}), derive `const isRemoving = removingIds.has(contrato.id)` and MERGE into the existing style object: opacity isRemoving ? 0 : 1, transform isRemoving ? "scale(0.97)" : "scale(1)", transition "opacity 200ms ease, transform 200ms ease". Keep the existing className/COL_STYLE spread — single merged style prop, not a second one. For D-07: the MAIN listing must show only status === 'ativo'. FIRST inspect the existing derivation near line 157 — if an ativos/active-filtered variable already exists, drive the main map from it rather than adding a duplicate filter. Otherwise add `const contratosAtivos = contratos.filter(c => c.status === "ativo")` and map over it. CRITICAL (D-07 scope): if a separate "Arquivo"/encerrados history section exists ("histórico imutável"), leave it untouched — it must keep showing encerrado/cancelado contratos. D-07 filters the main active listing only; it does not delete data or hide the history view. Per CLAUDE.md use inline styles + CSS vars (not Tailwind) for the feature row.</action>
  <verify>
    <automated>grep -q 'isRemoving' src/components/features/Contratos.js && grep -q 'scale(0.97)' src/components/features/Contratos.js && grep -q 'opacity 200ms' src/components/features/Contratos.js && grep -Eq 'status === "ativo"|ativos' src/components/features/Contratos.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Row div derives isRemoving from removingIds.has(contrato.id) and merges opacity/transform/transition into the existing style object (single style prop)
    - transition string is "opacity 200ms ease, transform 200ms ease"; transform removing value is scale(0.97)
    - Main listing renders only status === 'ativo' contratos (derived variable or reused existing ativos)
    - Any existing encerrados/Arquivo history section is unchanged (still shows non-ativo statuses)
    - npm run build exits 0
  </acceptance_criteria>
  <done>The active row animates on removal and the main listing shows only ativo contratos; the history section is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan only edits a Client Component's local state, toasts, and inline styles. No new Server Action, query, endpoint, auth, or user-input parsing is introduced — the underlying cancelarContrato/encerrarContrato/criarContrato actions are unchanged (RESEARCH Security Domain: V2-V6 all N/A).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-SC | Tampering | sonner package (installed in 14-00) | mitigate | sonner APPROVED in RESEARCH Package Legitimacy Audit (public maintainer, ~3M weekly downloads, 10k+ stars, source repo verified). No new install in this plan; no blocking-human checkpoint. |
| T-14-01-I | Information Disclosure | toast.success strings | accept | Toast messages are static literals ("Contrato criado/encerrado/cancelado") — no PII, no user input, no contrato data leaked. |
</threat_model>

<verification>
- npm run build green.
- Source asserts: sonner import, removingIds, 3 toast strings, optimistic filter, isRemoving style, ativo-only listing.
- Manual (ANIM-01, 200ms timing): in the dashboard, encerrar/cancelar an ativo contrato → item fades (opacity + scale) over ~200ms before leaving; reload → it does not reappear in the main listing.
- Phase-gate spec tests/toast-feedback.spec.js (from 14-00) covers the criar/encerrar/cancelar toast flows owned here — run at phase level, not as this plan's gate.
</verification>

<success_criteria>
- ANIM-01: cancelar/encerrar fade the item out ~200ms via optimistic filter.
- ANIM-03: criar/encerrar/cancelar each fire the locked toast.
- D-07: main listing shows only ativo; history (if any) untouched; no reappear on reload.
- D-09: error path rolls back removingIds.
</success_criteria>

<output>
Create `.planning/phases/14-anima-es-feedback/14-01-SUMMARY.md` when done.
</output>
