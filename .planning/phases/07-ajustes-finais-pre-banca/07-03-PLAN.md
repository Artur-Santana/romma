---
phase: 07-ajustes-finais-pre-banca
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/skeleton.js
  - src/app/dashboard/loading.js
  - src/app/dashboard/locatarios/loading.js
  - src/components/features/Unidades.js
  - src/components/features/Contratos.js
  - src/components/features/portal/PortalDashboard.js
autonomous: false
requirements: [UX-02]
must_haves:
  truths:
    - "A aba Visão Geral (/dashboard) exibe skeleton durante o carregamento server-side"
    - "A aba Locatários (/dashboard/locatarios) exibe skeleton durante o carregamento server-side"
    - "A aba Unidades exibe skeleton durante o carregamento inicial dos dados"
    - "A aba Contratos exibe skeleton durante o carregamento inicial dos dados"
    - "O portal do Locatário exibe skeleton no lugar do texto 'Carregando...'"
  artifacts:
    - path: "src/components/ui/skeleton.js"
      provides: "Componente Skeleton do shadcn/ui"
      contains: "Skeleton"
    - path: "src/app/dashboard/loading.js"
      provides: "Suspense boundary com skeleton para Visão Geral"
    - path: "src/app/dashboard/locatarios/loading.js"
      provides: "Suspense boundary com skeleton para Locatários"
  key_links:
    - from: "src/components/features/Unidades.js"
      to: "loadingInicial state"
      via: "guard de render que mostra skeleton no primeiro fetch"
      pattern: "loadingInicial"
    - from: "src/components/features/portal/PortalDashboard.js"
      to: "src/components/ui/skeleton.js"
      via: "import Skeleton substituindo texto Carregando"
      pattern: "Skeleton"
---

<objective>
Adicionar skeleton loading em todas as 4 abas do dashboard (Visão Geral, Unidades, Contratos, Locatários) e no portal do Locatário (UX-02). Cada superfície usa a estratégia adequada à sua arquitetura: `loading.js` (Suspense) para as abas Server Component, flag `loadingInicial` para os Client Components que só usam `loading` em mutations, e substituição direta no PortalDashboard que já tem `loading` inicial.

Slice vertical: instalar o primitivo Skeleton → aplicar por superfície → validar visualmente. Após este plano, nenhuma aba mostra tela em branco ou texto "Carregando..." durante o fetch — todas exibem skeleton estruturado.

Purpose: Carregamento sem feedback visual (tela em branco / texto cru) prejudica a percepção de qualidade na demo da banca.
Output: `src/components/ui/skeleton.js` (instalado via shadcn CLI), 2 `loading.js`, e skeleton em Unidades/Contratos/PortalDashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ajustes-finais-pre-banca/07-CONTEXT.md
@.planning/phases/07-ajustes-finais-pre-banca/07-RESEARCH.md
@.planning/phases/07-ajustes-finais-pre-banca/07-PATTERNS.md
@.planning/phases/07-ajustes-finais-pre-banca/07-UI-SPEC.md
@src/components/features/Unidades.js
@src/components/features/Contratos.js
@src/components/features/portal/PortalDashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar Skeleton (shadcn) + loading.js das abas Server Component</name>
  <read_first>
    - components.json (config shadcn: preset radix-lyra, baseColor mauve, registry oficial)
    - src/app/dashboard/page.js (layout visual da Visão Geral — eyebrow, título display, grid de 4 KPI cards com `border border-border-3`, bloco de lista; o skeleton deve espelhar essa estrutura)
    - src/app/dashboard/locatarios/page.js (delega para LocatariosDesktop — layout de tabela)
    - .planning/phases/07-ajustes-finais-pre-banca/07-UI-SPEC.md (seções "Skeleton Loading 1. /dashboard" e "2. /dashboard/locatarios": proporções exatas e regra `rounded-none`)
    - src/app/globals.css (tokens `--muted`, `--surface-hi`, `--border-3`, `--background`)
  </read_first>
  <action>
    (A) **Instalar Skeleton (D-08):** rodar `npx shadcn@latest add skeleton` — gera `src/components/ui/skeleton.js` via CLI oficial (sem novo pacote npm; registry oficial conforme 07-UI-SPEC.md Registry Safety). Se o CLI gerar `.tsx`, ajustar para `.js` (projeto é JS sem TS).

    (B) **`src/app/dashboard/loading.js`** (NEW — UX-02, D-09): componente default export `Loading()` que espelha o layout de `dashboard/page.js`. Importar `Skeleton` de `@/components/ui/skeleton`. Estrutura per 07-UI-SPEC.md seção 1: container `romma-page p-12 bg-background min-h-full`; eyebrow `Skeleton h-[10px] w-32 mb-2 rounded-none`; título `Skeleton h-12 w-64 mb-12 rounded-none`; grid de 4 KPI cards (`grid grid-cols-4 ... border border-border-3 mb-12`), cada card `p-7` com label `Skeleton h-[10px] w-24 mb-3 rounded-none` e valor `Skeleton h-12 w-32 rounded-none`; bloco de lista `Skeleton h-64 w-full rounded-none mt-4`. TODOS os Skeleton com `rounded-none` (sistema usa `--radius: 0`; shadcn aplica `rounded-md` por padrão — sobrescrever).

    (C) **`src/app/dashboard/locatarios/loading.js`** (NEW — UX-02, D-09): copiar a estrutura do loading.js acima, ajustando proporções para o layout de Locatários per 07-UI-SPEC.md seção 2: eyebrow `Skeleton h-[10px] w-24 mb-2 rounded-none`; título `Skeleton h-9 w-48 mb-8 rounded-none`; tabela `Skeleton h-64 w-full rounded-none`. Todos `rounded-none`.
  </action>
  <verify>
    <automated>test -f src/components/ui/skeleton.js && test -f src/app/dashboard/loading.js && test -f src/app/dashboard/locatarios/loading.js && grep -c "rounded-none" src/app/dashboard/loading.js && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/skeleton.js` existe e exporta `Skeleton`
    - `src/app/dashboard/loading.js` existe, default export, importa Skeleton, contém grid de 4 cards
    - `src/app/dashboard/locatarios/loading.js` existe, default export, importa Skeleton
    - `grep -c "rounded-none" src/app/dashboard/loading.js` retorna ≥ 4 (todos os Skeleton sobrescrevem o rounded padrão)
    - `npm run build` sem erros
    - Nenhum pacote novo no package.json (apenas arquivo local gerado pelo CLI)
  </acceptance_criteria>
  <done>Skeleton instalado e abas Server Component (Visão Geral, Locatários) com loading.js.</done>
</task>

<task type="auto">
  <name>Task 2: Skeleton de initial load em Unidades.js e Contratos.js</name>
  <read_first>
    - src/components/features/Unidades.js (linha 19: `const [loading, setLoading] = useState(false)` — só muda em mutations; `carregarDados` linhas 38-41 sem setLoading; useEffect que chama carregarDados)
    - src/components/features/Contratos.js (linha 43: `const [loading, setLoading] = useState(false)` — só mutations; função `carregar` no useEffect linhas 45-54)
    - .planning/phases/07-ajustes-finais-pre-banca/07-UI-SPEC.md (seções "3. /dashboard/unidades" e "4. /dashboard/contratos": estrutura dos skeletons)
    - .planning/phases/07-ajustes-finais-pre-banca/07-PATTERNS.md (Pitfall 4 / ALT-04: NÃO reutilizar o `loading` existente — adicionar `loadingInicial` separado)
  </read_first>
  <action>
    Em ambos os arquivos, adicionar skeleton de carregamento inicial via flag SEPARADA (D-10, mas com a correção do ALT-04/Pitfall 4: o `loading` existente é `useState(false)` e só fica `true` em mutations — NÃO reutilizar).

    (A) **`src/components/features/Unidades.js`** (modify): adicionar `import { Skeleton } from "@/components/ui/skeleton"`. Adicionar estado `const [loadingInicial, setLoadingInicial] = useState(true)` (após a linha 19). Na função `carregarDados`, após `setUnidades(...)`, adicionar `setLoadingInicial(false)`. Antes do `return` principal, adicionar guard `if (loadingInicial) return <SkeletonUnidades />`. Definir `SkeletonUnidades` como função local que espelha o grid de cards de unidade per 07-UI-SPEC.md seção 3: grid de 3 cards placeholder, cada um `Skeleton h-48 w-full rounded-none` (header/imagem) + `Skeleton h-4 w-3/4 mt-3 rounded-none` (nome) + `Skeleton h-3 w-1/2 mt-2 rounded-none` (descrição); container `romma-page`. Todos `rounded-none`.

    (B) **`src/components/features/Contratos.js`** (modify): mesma estratégia. `import { Skeleton } from "@/components/ui/skeleton"`. `const [loadingInicial, setLoadingInicial] = useState(true)` (após linha 43). Na função `carregar`, após os `set...`, adicionar `setLoadingInicial(false)`. Guard `if (loadingInicial) return <SkeletonContratos />` antes do return principal. `SkeletonContratos` local per 07-UI-SPEC.md seção 4: header de tabela simulado `Skeleton h-[10px] w-full rounded-none mb-2` (repetir ~5×) + linhas `Skeleton h-8 w-full rounded-none mt-1` (repetir ~4×). Todos `rounded-none`. NÃO mexer no `loading` de mutations existente.
  </action>
  <verify>
    <automated>grep -c "loadingInicial" src/components/features/Unidades.js && grep -c "loadingInicial" src/components/features/Contratos.js && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "loadingInicial" src/components/features/Unidades.js` retorna ≥ 3 (declaração + set + guard)
    - `grep -c "loadingInicial" src/components/features/Contratos.js` retorna ≥ 3
    - Ambos os arquivos importam `Skeleton` de `@/components/ui/skeleton`
    - O `loading` de mutações existente NÃO foi alterado em nenhum dos dois (continua `useState(false)`)
    - Os skeletons usam `rounded-none`
    - `npm run build` sem erros
  </acceptance_criteria>
  <done>Unidades e Contratos exibem skeleton no carregamento inicial via flag loadingInicial separada.</done>
</task>

<task type="auto">
  <name>Task 3: Skeleton no PortalDashboard (substituição direta)</name>
  <read_first>
    - src/components/features/portal/PortalDashboard.js (linha 16: `loading` já é `useState(true)` — NÃO modificar; linha 55: bloco `loading ? <div ...>Carregando...</div>` a substituir; imports nas linhas 1-8)
    - .planning/phases/07-ajustes-finais-pre-banca/07-UI-SPEC.md (seção "5. Portal do Locatário": estrutura exata do skeleton)
  </read_first>
  <action>
    Modificar `src/components/features/portal/PortalDashboard.js` (D-09, D-10 — aplicação direta, pois o `loading` inicial já é `useState(true)`):
    - Adicionar `import { Skeleton } from "@/components/ui/skeleton"` após os imports existentes (linha 8).
    - Substituir o ramo `loading ? <div className="mt-8 font-mono text-[12px] text-fg-4">Carregando...</div>` (linha 55) por um bloco `<div className="mt-8 flex flex-col gap-4">` contendo, per 07-UI-SPEC.md seção 5: `Skeleton h-32 w-full rounded-none` (placeholder do ContratoCard), seguido de 4× `Skeleton h-8 w-full rounded-none` (cabeçalho + 3 linhas da tabela de parcelas). NÃO alterar o estado `loading` (linha 16) nem os ramos `erro`/`!contrato`/conteúdo. Todos os Skeleton com `rounded-none`.
  </action>
  <verify>
    <automated>grep -c "Skeleton" src/components/features/portal/PortalDashboard.js && grep -c "Carregando..." src/components/features/portal/PortalDashboard.js && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Skeleton" src/components/features/portal/PortalDashboard.js` retorna ≥ 2 (import + uso)
    - `grep -c "Carregando..." src/components/features/portal/PortalDashboard.js` retorna 0 (texto cru removido)
    - `const [loading, setLoading] = useState(true)` permanece inalterado (linha 16)
    - Skeleton usa `rounded-none`
    - `npm run build` sem erros
  </acceptance_criteria>
  <done>PortalDashboard exibe skeleton em vez de "Carregando...".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificação visual dos skeletons nas 5 superfícies</name>
  <what-built>
    Skeleton loading aplicado nas 4 abas do dashboard (Visão Geral, Unidades, Contratos, Locatários) e no portal do Locatário. Cada um com a estratégia adequada (loading.js para SSR, loadingInicial para client, substituição direta no portal).
  </what-built>
  <how-to-verify>
    1. Rodar `npm run dev` e logar como proprietário.
    2. Navegar entre as 4 abas (preferencialmente com throttling de rede no DevTools para ver o skeleton): /dashboard (Visão Geral), /dashboard/unidades, /dashboard/contratos, /dashboard/locatarios. Confirmar que aparece skeleton estruturado (não tela em branco, não texto "Carregando...") durante o carregamento e que o conteúdo real substitui o skeleton ao terminar.
    3. Logar como locatário (ou abrir /portal/dashboard autenticado) e confirmar que o skeleton aparece no lugar do antigo "Carregando..." e some quando o contrato carrega.
    4. Confirmar visualmente que os skeletons têm cantos retos (`rounded-none`) — consistente com o sistema dark sharp-corner.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva problemas visuais por superfície.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | UX-02 é puramente apresentacional (skeleton de loading) — não introduz novo fluxo de dados, entrada de usuário ou controle de acesso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Information Disclosure | loading.js / skeletons | accept | Skeletons não renderizam dados reais — apenas formas placeholder; nenhum vazamento possível |
| T-07-SC | Tampering | shadcn skeleton install | mitigate | `npx shadcn@latest add skeleton` usa registry oficial do shadcn (07-UI-SPEC.md Registry Safety: não requer gate); gera arquivo local, nenhum pacote npm novo no package.json |
</threat_model>

<verification>
- `npm run build` sem erros após cada task
- Checkpoint visual (Task 4) confirma skeleton nas 5 superfícies
- `grep` confirma `loadingInicial` em Unidades/Contratos e ausência de "Carregando..." no PortalDashboard
</verification>

<success_criteria>
- Todas as 4 abas do dashboard + portal do Locatário exibem skeleton loading durante carregamento de dados (UX-02, critério 3 da fase)
- Nenhuma superfície exibe tela em branco ou texto cru "Carregando..." durante o fetch
- Skeletons consistentes com o sistema visual (rounded-none, tokens do projeto)
</success_criteria>

<output>
Create `.planning/phases/07-ajustes-finais-pre-banca/07-03-SUMMARY.md` when done
</output>
