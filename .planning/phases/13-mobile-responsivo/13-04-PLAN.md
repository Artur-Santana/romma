---
phase: 13-mobile-responsivo
plan: 04
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - src/components/features/portal/PortalDashboard.js
  - src/components/features/portal/ParcelsTable.js
  - src/components/features/portal/ContratoCard.js
autonomous: true
requirements: [UX-04]

must_haves:
  truths:
    - "O Portal do Locatário em 375px não tem overflow horizontal de página"
    - "PortalDashboard usa padding e tipografia responsivos (px-4 sm:px-12, text-[28px] sm:text-[48px])"
    - "ParcelsTable rola horizontalmente dentro de wrapper overflow-x-auto sem empurrar a página"
    - "ContratoCard exibe os blocos de info sem overflow em 375px"
  artifacts:
    - path: "src/components/features/portal/PortalDashboard.js"
      provides: "Portal com padding e h1 responsivos"
      contains: "px-4 sm:px-12"
    - path: "src/components/features/portal/ParcelsTable.js"
      provides: "Tabela de parcelas do portal com overflow-x e min-w-[480px]"
      contains: "overflow-x-auto"
  key_links:
    - from: "src/components/features/portal/ParcelsTable.js"
      to: "viewport mobile 375px"
      via: "overflow-x-auto wrapper + min-w-[480px]"
      pattern: "overflow-x-auto"
---

<objective>
Implementar UX-04 (D-05): tornar o Portal do Locatário usável em 375px sem overflow horizontal. Aplicar padding/tipografia responsivos em PortalDashboard.js, wrapper `overflow-x-auto` + `min-w-[480px]` em ParcelsTable.js, e verificar/corrigir ContratoCard.js. Portal usa Tailwind — fixes em classes responsivas.

Purpose: Tornar o spec UX-04 (Wave 0) verde. Independente do shell do dashboard — só precisa do spec de 13-01.
Output: 3 arquivos do portal modificados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-mobile-responsivo/13-RESEARCH.md
@.planning/phases/13-mobile-responsivo/13-PATTERNS.md
@.planning/phases/13-mobile-responsivo/13-UI-SPEC.md
@src/components/features/portal/PortalDashboard.js
@src/components/features/portal/ParcelsTable.js
@src/components/features/portal/ContratoCard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Padding/tipografia responsivos em PortalDashboard.js + overflow em ParcelsTable.js + verificar ContratoCard.js</name>
  <files>src/components/features/portal/PortalDashboard.js, src/components/features/portal/ParcelsTable.js, src/components/features/portal/ContratoCard.js</files>
  <read_first>
    - src/components/features/portal/PortalDashboard.js — wrapper ~42 (`px-12 pt-12 pb-20`); h1 ~47 (`text-[48px]`)
    - src/components/features/portal/ParcelsTable.js — section ~6; container ~8 (`mt-4 border border-border-3 bg-surface`); grid `grid-cols-[60px_1fr_1fr_1.2fr]`
    - src/components/features/portal/ContratoCard.js — linha ~8 (`grid grid-cols-2 gap-6 mt-4`) com 4 blocos de info
    - .planning/phases/13-mobile-responsivo/13-PATTERNS.md §PortalDashboard.js, §ParcelsTable.js, §ContratoCard.js — antes/depois exato
    - .planning/phases/13-mobile-responsivo/13-CONTEXT.md D-05 — especificação dos fixes do portal
  </read_first>
  <action>
    PortalDashboard.js (D-05, PATTERNS §PortalDashboard.js):
    - Wrapper (~42): `<div className="romma-page bg-background min-h-full px-12 pt-12 pb-20">` → `<div className="romma-page bg-background min-h-full px-4 sm:px-12 pt-6 sm:pt-12 pb-20">`.
    - h1 (~47): `text-[48px]` → `text-[28px] sm:text-[48px]` (manter o resto: `font-display font-bold leading-none tracking-[-2.4px] text-fg-1 m-0`).

    ParcelsTable.js (D-05, PATTERNS §ParcelsTable.js):
    - Container (~8): trocar `<div className="mt-4 border border-border-3 bg-surface">` por um wrapper de overflow contendo o container com min-width: `<div className="mt-4 overflow-x-auto"><div className="border border-border-3 bg-surface min-w-[480px]">` — e fechar a `</div>` extra do wrapper no fim da section. Todo o conteúdo interno (header grid + linhas) permanece inalterado.

    ContratoCard.js (verificar e corrigir se necessário — PATTERNS §ContratoCard.js, A2):
    - Inspecionar o `grid grid-cols-2 gap-6 mt-4` (~8) com 4 blocos de info em 375px. Em 375px cada coluna ~175px. Se algum bloco de info transbordar (texto longo de unidade/valor causando overflow horizontal de página), aplicar `grid-cols-1 sm:grid-cols-2`. Se não houver overflow, deixar como está e registrar no SUMMARY que A2 foi verificada sem mudança necessária.
  </action>
  <verify>
    <automated>npx eslint src/components/features/portal/PortalDashboard.js src/components/features/portal/ParcelsTable.js src/components/features/portal/ContratoCard.js && grep -q 'px-4 sm:px-12' src/components/features/portal/PortalDashboard.js && grep -q 'text-\[28px\] sm:text-\[48px\]' src/components/features/portal/PortalDashboard.js && grep -q 'overflow-x-auto' src/components/features/portal/ParcelsTable.js && grep -q 'min-w-\[480px\]' src/components/features/portal/ParcelsTable.js</automated>
  </verify>
  <acceptance_criteria>
    - `PortalDashboard.js` wrapper usa `px-4 sm:px-12 pt-6 sm:pt-12` (não mais `px-12 pt-12` isolado)
    - `PortalDashboard.js` h1 usa `text-[28px] sm:text-[48px]`
    - `ParcelsTable.js` contém `overflow-x-auto` e `min-w-[480px]` no container interno
    - `ContratoCard.js`: ou usa `grid-cols-1 sm:grid-cols-2`, ou o SUMMARY documenta que A2 foi verificada em 375px sem overflow (decisão registrada)
    - ESLint passa nos 3 arquivos
  </acceptance_criteria>
  <done>Portal do Locatário em 375px exibe contrato ativo e histórico de parcelas sem overflow horizontal de página; tabela de parcelas rola dentro do wrapper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhum novo | Mudanças de className Tailwind em componentes do portal — sem novos inputs, sem novas queries, RLS do portal inalterado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Information Disclosure | portal/PortalDashboard.js, ParcelsTable.js | accept | Mudanças puramente de layout responsivo; o portal continua servindo apenas os dados do Locatário autenticado (RLS + auth guard inalterados) |
| T-13-SC | Tampering | npm installs | accept | Nenhum pacote novo. Apenas mudanças de className Tailwind |
</threat_model>

<verification>
- Spec UX-04 passa: `npx playwright test --config=playwright.validation.config.js --grep "UX-04" --project=chromium`
- `scrollWidth <= 375` em /portal/dashboard
</verification>

<success_criteria>
- Portal sem overflow horizontal em 375px
- PortalDashboard com padding e h1 responsivos
- ParcelsTable com overflow-x-auto + min-w-[480px]
- ContratoCard verificado (fix aplicado ou ausência de overflow documentada)
- Spec UX-04 verde
</success_criteria>

<output>
Create `.planning/phases/13-mobile-responsivo/13-04-SUMMARY.md` when done
</output>
