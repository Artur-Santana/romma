---
phase: 13-mobile-responsivo
plan: 02
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - src/components/ui/DashboardShell.js
  - src/app/dashboard/layout.js
  - src/app/dashboard/page.js
  - src/app/globals.css
  - src/components/ui/MobileNav.js
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "Em 375px o dashboard exibe MobileTopBar + MobileBottomNav e a sidebar fica oculta (display:none)"
    - "A rota /dashboard não tem chrome mobile duplicado (top bar / bottom nav apenas uma vez)"
    - "MobileTopBar e MobileBottomNav expõem data-testid='mobile-top-bar' e 'mobile-bottom-nav'"
    - "dashboard/layout.js permanece Server Component (sem 'use client') e delega ao DashboardShell"
  artifacts:
    - path: "src/components/ui/DashboardShell.js"
      provides: "Client Component que renderiza chrome desktop (TopStrip+sidebar) e mobile (MobileTopBar+MobileBottomNav) via CSS render-both"
      contains: "use client"
      min_lines: 40
    - path: "src/components/ui/MobileNav.js"
      provides: "data-testid em MobileTopBar e MobileBottomNav"
      contains: "data-testid=\"mobile-top-bar\""
  key_links:
    - from: "src/app/dashboard/layout.js"
      to: "src/components/ui/DashboardShell.js"
      via: "import DashboardShell from '@/components/ui/DashboardShell'"
      pattern: "DashboardShell"
    - from: "src/components/ui/DashboardShell.js"
      to: "src/components/ui/MobileNav.js"
      via: "import { MobileTopBar, MobileBottomNav }"
      pattern: "MobileTopBar.*MobileBottomNav"
---

<objective>
Implementar UX-02 (D-01, D-02, D-03): criar o Client Component `DashboardShell` que provê o chrome do dashboard — desktop (TopStrip + OwnerSidebar + main) e mobile (MobileTopBar + main + MobileBottomNav) — via CSS render-both. Fazer `dashboard/layout.js` delegar ao shell, remover APENAS o chrome mobile manual de `dashboard/page.js` (preservando as classes de visibilidade do conteúdo), adicionar `!important` à regra mobile de `.romma-desktop-only` em globals.css, e adicionar os `data-testid` em MobileNav.js.

Purpose: Tornar o spec UX-02 (Wave 0) verde sem chrome duplicado e sem vazamento da sidebar em 375px.
Output: 1 componente novo + 4 arquivos modificados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-mobile-responsivo/13-RESEARCH.md
@.planning/phases/13-mobile-responsivo/13-PATTERNS.md
@.planning/phases/13-mobile-responsivo/13-UI-SPEC.md
@.planning/phases/13-mobile-responsivo/13-CONTEXT.md
@src/app/dashboard/layout.js
@src/app/dashboard/page.js
@src/components/ui/MobileNav.js
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar DashboardShell + delegar layout.js + adicionar data-testid em MobileNav.js + fix globals.css</name>
  <files>src/components/ui/DashboardShell.js, src/app/dashboard/layout.js, src/app/globals.css, src/components/ui/MobileNav.js</files>
  <read_first>
    - src/app/dashboard/layout.js — estrutura atual (Server Component, TopStrip + romma-sidebar-wrapper + main) que vira o bloco desktop do shell
    - src/components/ui/MobileNav.js — API de MobileTopBar (props title, subtitle, onBack, onMenu, right) e MobileBottomNav (props items[{href,label,code}]); detecta ativo via usePathname
    - src/components/ui/TopStrip.js + src/components/ui/OwnerSidebar.js — usados no caminho desktop
    - src/app/globals.css (linhas ~355-362) — .romma-desktop-only, .romma-mobile-only, media query 768px, .romma-sidebar-wrapper { display: none !important }
    - .planning/phases/13-mobile-responsivo/13-PATTERNS.md §DashboardShell, §layout.js, §globals.css, §MobileNav.js — código de referência completo
  </read_first>
  <action>
    Criar `src/components/ui/DashboardShell.js` como Client Component (primeira linha `"use client"`). Imports: `usePathname, useRouter` de "next/navigation"; `MobileTopBar, MobileBottomNav` de "@/components/ui/MobileNav"; `TopStrip` de "@/components/ui/TopStrip"; `OwnerSidebar` de "@/components/ui/OwnerSidebar" (per D-01).

    Constantes no topo do módulo:
    - `NAV_ITEMS` (D-02): `[{ href: "/dashboard", label: "Início", code: "OVW" }, { href: "/dashboard/unidades", label: "Unidades", code: "UNI" }, { href: "/dashboard/contratos", label: "Contratos", code: "CTR" }, { href: "/dashboard/locatarios", label: "Locatários", code: "LOC" }]`
    - `ROUTE_TITLES` (D-03): `{ "/dashboard": "Dashboard", "/dashboard/unidades": "Unidades", "/dashboard/contratos": "Contratos", "/dashboard/locatarios": "Locatários" }`

    Dentro do componente `DashboardShell({ children })`: `const pathname = usePathname()`; `const router = useRouter()`. Per D-03: `const isParcelasRoute = pathname.startsWith("/dashboard/contratos/") && pathname !== "/dashboard/contratos"`; `const title = isParcelasRoute ? "Parcelas" : (ROUTE_TITLES[pathname] ?? "Dashboard")`; `const onBack = isParcelasRoute ? () => router.back() : undefined`.

    Render — dois blocos irmãos (CSS render-both, abordagem recomendada da Claude's Discretion D-01). IMPORTANTE: render-both significa que `{children}` é renderizado DENTRO de ambos os blocos (desktop e mobile). O CSS esconde o bloco inadequado por viewport. O shell controla a visibilidade do CHROME; as classes de visibilidade do CONTEÚDO (dentro de page.js) continuam sendo responsabilidade da própria page.js (ver Task 2).
    - Bloco desktop: `<div className="romma-desktop-only" style={{ display: "flex", flexDirection: "column", height: "100vh" }}>` contendo TopStrip, depois `<div style={{ display: "flex", height: "calc(100vh - 24px)" }}>` com `<div className="romma-sidebar-wrapper"><OwnerSidebar badges={{}} /></div>` e `<main style={{ flex: 1, overflow: "auto", background: "var(--background)" }}>` envolvendo `<div style={{ maxWidth: "1570px", margin: "0 auto", padding: "0 24px" }}>{children}</div>`. (replicar estrutura atual de layout.js exatamente).
    - Bloco mobile: `<div className="romma-mobile-only"><MobileTopBar title={title} onBack={onBack} /><main style={{ flex: 1, overflow: "auto", background: "var(--background)" }}>{children}</main><MobileBottomNav items={NAV_ITEMS} /></div>`.

    Modificar `src/app/dashboard/layout.js`: substituir todo o corpo do retorno por `return <DashboardShell>{children}</DashboardShell>`, importando `DashboardShell from "@/components/ui/DashboardShell"`. NÃO adicionar `"use client"` — layout permanece Server Component (D-01). Remover imports não mais usados (TopStrip, OwnerSidebar) se ficarem órfãos.

    Modificar `src/components/ui/MobileNav.js`: adicionar `data-testid="mobile-top-bar"` no div externo de MobileTopBar (linha ~9) e `data-testid="mobile-bottom-nav"` no div externo de MobileBottomNav (linha ~46). Apenas esses dois atributos — nenhuma mudança de lógica.

    Modificar `src/app/globals.css`: na media query `@media (max-width: 768px)`, mudar `.romma-desktop-only { display: none; }` para `.romma-desktop-only { display: none !important; }` (per RESEARCH §Pitfall 5 — necessário para vencer o inline `style={{ display: "flex" }}` do bloco desktop do shell, E para garantir que o CONTEÚDO `romma-desktop-only` dentro de page.js suma quando renderizado dentro do bloco mobile do shell — ver Task 2).
  </action>
  <verify>
    <automated>npx eslint src/components/ui/DashboardShell.js src/app/dashboard/layout.js src/components/ui/MobileNav.js && grep -q 'display: none !important' src/app/globals.css && grep -q 'data-testid="mobile-top-bar"' src/components/ui/MobileNav.js && grep -q 'data-testid="mobile-bottom-nav"' src/components/ui/MobileNav.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/DashboardShell.js` existe, primeira linha é `"use client"`, e contém `MobileTopBar`, `MobileBottomNav`, `NAV_ITEMS` com os 4 itens D-02 e `ROUTE_TITLES` com as 4 rotas D-03
    - DashboardShell contém `isParcelasRoute` e `router.back()` para a rota /contratos/[id] (D-03)
    - `src/app/dashboard/layout.js` NÃO contém `"use client"` e contém `<DashboardShell>{children}</DashboardShell>`
    - `src/components/ui/MobileNav.js` contém `data-testid="mobile-top-bar"` e `data-testid="mobile-bottom-nav"`
    - `src/app/globals.css` contém `.romma-desktop-only { display: none !important; }` dentro da media query 768px (grep: `display: none !important`)
    - ESLint passa nos 3 arquivos JS
  </acceptance_criteria>
  <done>O shell renderiza chrome desktop e mobile; layout delega ao shell mantendo-se Server Component; MobileNav expõe os data-testid; globals.css garante que tanto o bloco desktop do shell quanto o conteúdo romma-desktop-only de page.js sumam em mobile.</done>
</task>

<task type="auto">
  <name>Task 2: Remover APENAS o chrome mobile manual de dashboard/page.js, preservando as classes de visibilidade do conteúdo</name>
  <files>src/app/dashboard/page.js</files>
  <read_first>
    - src/app/dashboard/page.js — LER O ARQUIVO INTEIRO antes de editar. Estrutura confirmada: conteúdo desktop gated por `<div className="romma-desktop-only">` (linhas ~87 e ~165); conteúdo mobile gated por `<div className="flex flex-col h-screen md:hidden">` com MobileTopBar/MobileBottomNav próprios (linhas ~138-154 setup-state e ~335-442 normal-state)
    - .planning/phases/13-mobile-responsivo/13-RESEARCH.md §Pitfall 1 — descrição do double chrome
    - .planning/phases/13-mobile-responsivo/13-PATTERNS.md §dashboard/page.js — referência (NOTA: a recomendação do PATTERNS de remover `romma-desktop-only` está ERRADA para shell render-both; seguir o action abaixo, não o PATTERNS nesse ponto)
  </read_first>
  <action>
    CONTEXTO CRÍTICO: o DashboardShell (Task 1) usa render-both — `{children}` (= o output de page.js) é renderizado DENTRO de ambos os blocos do shell (desktop e mobile). O CSS esconde o bloco do shell inadequado por viewport. Como page.js é renderizado dentro do bloco mobile do shell quando em 375px, qualquer conteúdo de page.js que perca sua classe de ocultação mobile vai aparecer indevidamente em mobile, somado ao conteúdo mobile, causando overflow horizontal (UX-03 falha em /dashboard).

    Portanto: remover APENAS o chrome (as barras de navegação manuais e o `h-screen` do wrapper mobile). PRESERVAR todas as classes de visibilidade do conteúdo (`romma-desktop-only` no conteúdo desktop, `md:hidden` no conteúdo mobile).

    1. Bloco setup-state mobile (~138-154): remover o `<MobileTopBar ...>` (linha ~139) e o `<MobileBottomNav ...>` (linha ~153) internos; trocar o wrapper `<div className="flex flex-col h-screen md:hidden">` por `<div className="flex flex-col md:hidden">` (remover apenas `h-screen`, MANTER `md:hidden`). Manter o `<div className="romma-mobile-pane flex-1 overflow-auto p-5">` e seu conteúdo intactos.

    2. Bloco normal-state mobile (~335-442): remover `<MobileTopBar ...>` (~336) e `<MobileBottomNav ...>` (~442); trocar `<div className="flex flex-col h-screen md:hidden">` por `<div className="flex flex-col md:hidden">` (remover `h-screen`, MANTER `md:hidden`). Manter o `romma-mobile-pane` e todo o conteúdo (stats cards 2x2, contratos mobile, quick actions) intactos.

    3. Conteúdo desktop (~87/88 e ~165/166): NÃO remover `<div className="romma-desktop-only">`. MANTER essa classe — é o que faz o conteúdo desktop sumir quando page.js é renderizado dentro do bloco mobile do shell (graças ao `display: none !important` adicionado em globals.css na Task 1). Não tocar nesses wrappers.

    Remover o import de `MobileTopBar`/`MobileBottomNav` de page.js (linha ~13) — fica órfão após remover as barras. Os DOIS conjuntos de conteúdo (desktop `romma-desktop-only` vs mobile `md:hidden`) DEVEM ser preservados com suas classes de visibilidade intactas.
  </action>
  <verify>
    <automated>npx eslint src/app/dashboard/page.js && test "$(grep -v '^[[:space:]]*//' src/app/dashboard/page.js | grep -c 'MobileBottomNav')" = "0" && test "$(grep -c 'h-screen md:hidden' src/app/dashboard/page.js)" = "0" && test "$(grep -c 'romma-desktop-only' src/app/dashboard/page.js)" = "2"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/dashboard/page.js`: nenhuma linha de JSX (excluindo comentários) contém `MobileBottomNav` ou `MobileTopBar` — barras de chrome removidas
    - Não contém mais `h-screen md:hidden` (grep count = 0) — `h-screen` removido dos wrappers mobile
    - AINDA contém `romma-desktop-only` exatamente 2 vezes (conteúdo desktop preservado com sua classe de ocultação mobile) — grep count = 2
    - AINDA contém `md:hidden` em pelo menos os 2 wrappers de conteúdo mobile (conteúdo mobile preservado)
    - AINDA contém `romma-mobile-pane` (conteúdo mobile interno preservado) e `romma-page` (conteúdo desktop interno preservado)
    - Import de `MobileTopBar, MobileBottomNav` removido de page.js (linha ~13)
    - ESLint passa
  </acceptance_criteria>
  <done>A rota /dashboard em 375px exibe apenas o chrome do DashboardShell (uma top bar, uma bottom nav). O conteúdo desktop (romma-desktop-only) suma em mobile via globals.css !important; o conteúdo mobile (md:hidden) aparece. Sem double chrome e sem vazamento do conteúdo desktop em mobile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhum novo | Mudança puramente de layout/CSS — auth em layout.js e RLS não alterados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Information Disclosure | DashboardShell (Client Component) | accept | Shell renderiza apenas chrome; não busca nem expõe dados. children continuam Server Components com auth herdada de layout.js (inalterada) |
| T-13-03 | Elevation of Privilege | dashboard/layout.js | accept | Guard de auth do layout não é tocado; delegação ao shell não altera controle de acesso |
| T-13-SC | Tampering | npm installs | accept | Nenhum pacote novo. Todas as deps (next/navigation, componentes UI) já presentes |
</threat_model>

<verification>
- Spec UX-02 passa: `npx playwright test --config=playwright.validation.config.js --grep "UX-02" --project=chromium`
- `.romma-sidebar-wrapper` oculto, `[data-testid="mobile-top-bar"]` e `[data-testid="mobile-bottom-nav"]` visíveis em 375px
- Sem chrome duplicado em /dashboard mobile; conteúdo desktop não vaza em mobile
- IMPORTANTE — page.js é tocado APENAS neste plano; verificar a própria superfície UX-03 de /dashboard antes de fechar: `npx playwright test --config=playwright.validation.config.js --grep "UX-03" --project=chromium` DEVE mostrar o teste de `/dashboard` (scrollWidth <= 375) verde. Se /dashboard falhar UX-03 aqui, o bug está em page.js (este plano) — NÃO em 13-03, que não toca page.js. Demais abas (unidades/contratos/locatarios) podem permanecer RED até 13-03.
</verification>

<success_criteria>
- DashboardShell criado e em uso via layout.js (Server Component preservado)
- data-testid presentes em MobileNav.js
- globals.css com !important na regra mobile de .romma-desktop-only
- Double chrome eliminado de dashboard/page.js, classes de visibilidade de conteúdo preservadas
- Spec UX-02 verde
</success_criteria>

<output>
Create `.planning/phases/13-mobile-responsivo/13-02-SUMMARY.md` when done
</output>
