---
phase: 13-mobile-responsivo
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - src/components/features/Contratos.js
  - src/components/features/LocatariosDesktop.js
  - src/components/features/Unidades.js
  - src/components/features/Parcelas.js
autonomous: true
requirements: [UX-03]

must_haves:
  truths:
    - "As 4 abas do dashboard (Início/Unidades, Contratos, Locatários, Parcelas) não têm overflow horizontal em 375px"
    - "A aba Locatários fica visível em mobile (romma-desktop-only removido de LocatariosDesktop.js)"
    - "Tabelas em grid rolam horizontalmente dentro de container overflow-x:auto, sem empurrar a página"
    - "Botões de ação (Editar/REVOGAR/VER, Pagar) têm altura tocável >=44px em mobile"
  artifacts:
    - path: "src/components/features/LocatariosDesktop.js"
      provides: "Aba Locatários visível e usável em mobile"
      contains: "overflowX"
    - path: "src/components/features/Contratos.js"
      provides: "Tabela de contratos com overflow-x e minWidth 680px"
      contains: "minWidth"
    - path: "src/components/features/Parcelas.js"
      provides: "Tabela de parcelas com overflow-x e minWidth 580px; heading responsivo"
      contains: "minWidth"
  key_links:
    - from: "src/components/features/LocatariosDesktop.js"
      to: "viewport mobile 375px"
      via: "remoção de romma-desktop-only + overflow-x wrapper"
      pattern: "overflowX"
---

<objective>
Implementar UX-03 (D-04): tornar as 4 abas do dashboard usáveis em 375px sem overflow horizontal de página. Aplicar o fix de overflow em tabelas CSS grid (wrapper `overflow-x:auto` + `minWidth` no grid interno), remover `romma-desktop-only` de LocatariosDesktop.js (desbloqueio crítico), colapsar forms `grid-cols-2` → `grid-cols-1 sm:grid-cols-2`, padding responsivo, heading responsivo e tap targets ≥44px.

Purpose: Tornar o spec UX-03 (Wave 0, 4 abas) verde. Depende do shell de 13-02 — a assertion de scrollWidth só passa se a sidebar não vazar.
Output: 4 arquivos de feature modificados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-mobile-responsivo/13-RESEARCH.md
@.planning/phases/13-mobile-responsivo/13-PATTERNS.md
@.planning/phases/13-mobile-responsivo/13-UI-SPEC.md
@src/components/features/Contratos.js
@src/components/features/LocatariosDesktop.js
@src/components/features/Unidades.js
@src/components/features/Parcelas.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix overflow + romma-desktop-only + tap targets em LocatariosDesktop.js e Contratos.js</name>
  <files>src/components/features/LocatariosDesktop.js, src/components/features/Contratos.js</files>
  <read_first>
    - src/components/features/LocatariosDesktop.js — linha 104 root `className="romma-desktop-only romma-page p-12..."`; GRID linha ~101; container tabela linha ~113; botões de ação Editar/REVOGAR/VER com `p-0 h-auto`; form de convite com grid-cols-2
    - src/components/features/Contratos.js — COL linhas ~46-47 (`"116px 1.6fr 1.6fr 1fr 1fr 1.2fr 96px"`); container tabela linha ~296; form linha ~188 (grid-cols-2); padding linhas ~18 e ~174
    - src/components/features/Parcelas.js (linha ~69) — referência de botão com tap target correto `py-[10px] px-5`
    - .planning/phases/13-mobile-responsivo/13-PATTERNS.md §LocatariosDesktop.js e §Contratos.js — antes/depois exato
    - .planning/phases/13-mobile-responsivo/13-RESEARCH.md §Pitfall 2, 3, 4 — romma-desktop-only, min-width, tap targets
  </read_first>
  <action>
    LocatariosDesktop.js (RESEARCH §Pitfall 2/3/4):
    - Linha 104: remover `romma-desktop-only` e tornar padding responsivo. De `<div className="romma-desktop-only romma-page p-12 bg-background min-h-full">` para `<div className="romma-page px-4 sm:px-12 pt-6 sm:pt-12 pb-20 bg-background min-h-full">`.
    - Container da tabela (~113): envolver `<div className="bg-surface border border-border-3">` num wrapper `<div style={{ overflowX: "auto" }}>` e adicionar `style={{ minWidth: "700px" }}` ao div da tabela. minWidth vai no grid interno, NÃO no wrapper (§Pitfall 3).
    - Botões de ação (Editar/REVOGAR/VER) com `p-0 h-auto`: substituir `p-0 h-auto` por `py-[10px] px-3` (mínimo de altura tocável ≥44px, §Pitfall 4).
    - Form de convite: trocar qualquer `grid-cols-2` por `grid-cols-1 sm:grid-cols-2`.
    - NÃO editar `Locatarios.js` (twin legado não usado na rota /dashboard/locatarios — §Pitfall 2 nota).

    Contratos.js (PATTERNS §Contratos.js):
    - Container da tabela (~296): envolver `<div className="border border-border-3 bg-surface mb-8">` num wrapper `<div style={{ overflowX: "auto" }}>` e adicionar `style={{ minWidth: "680px" }}` ao div da tabela.
    - Form (~188): `grid grid-cols-2 gap-4 mb-4` → `grid grid-cols-1 sm:grid-cols-2 gap-4 mb-4`.
    - Padding (linhas ~18 SkeletonContratos e ~174 render principal): `px-12 pt-12 pb-20` → `px-4 sm:px-12 pt-6 sm:pt-12 pb-20`.
  </action>
  <verify>
    <automated>npx eslint src/components/features/LocatariosDesktop.js src/components/features/Contratos.js && test "$(grep -v '^[[:space:]]*//' src/components/features/LocatariosDesktop.js | grep -c 'romma-desktop-only')" = "0" && grep -q 'minWidth: "700px"' src/components/features/LocatariosDesktop.js && grep -q 'minWidth: "680px"' src/components/features/Contratos.js</automated>
  </verify>
  <acceptance_criteria>
    - `LocatariosDesktop.js` NÃO contém mais `romma-desktop-only` (grep count = 0)
    - `LocatariosDesktop.js` contém `overflowX: "auto"` e `minWidth: "700px"`
    - `LocatariosDesktop.js` não contém mais `p-0 h-auto` nos botões de ação (substituído por `py-[10px] px-3`)
    - `Contratos.js` contém `overflowX: "auto"` e `minWidth: "680px"`
    - `Contratos.js` form usa `grid-cols-1 sm:grid-cols-2`
    - Ambos contêm padding responsivo `px-4 sm:px-12`
    - `Locatarios.js` (twin legado) permanece intocado — `git diff --quiet src/components/features/Locatarios.js`
    - ESLint passa nos 2 arquivos
  </acceptance_criteria>
  <done>Aba Locatários visível e rolável em 375px com botões tocáveis; aba Contratos sem overflow de página, com tabela rolável e form colapsado.</done>
</task>

<task type="auto">
  <name>Task 2: Fix overflow + heading responsivo em Parcelas.js e padding/form em Unidades.js</name>
  <files>src/components/features/Parcelas.js, src/components/features/Unidades.js</files>
  <read_first>
    - src/components/features/Parcelas.js — COL linhas ~11-12 (`"72px 1fr 1fr 1fr 1.2fr 120px"`); container tabela ~97; heading ~77 (`text-[48px]`); padding ~63; botão "Pagar" (verificar altura)
    - src/components/features/Unidades.js — padding linhas ~16 (SkeletonUnidades) e ~138; form ~150 (grid-cols-2); usa UnidadeCard (cards, não tabela grid — sem fix de overflow na lista)
    - .planning/phases/13-mobile-responsivo/13-PATTERNS.md §Parcelas.js e §Unidades.js — antes/depois exato
  </read_first>
  <action>
    Parcelas.js (PATTERNS §Parcelas.js):
    - Container da tabela (~97): envolver `<div className="border border-border-3 bg-surface mb-8">` num wrapper `<div style={{ overflowX: "auto" }}>` e adicionar `style={{ minWidth: "580px" }}` ao div da tabela.
    - Heading (~77): `text-[48px]` → `text-[28px] sm:text-[48px]` (manter o resto da classe).
    - Padding (~63): `px-12 pt-12 pb-20` → `px-4 sm:px-12 pt-6 sm:pt-12 pb-20`.
    - Botão "Pagar": se altura < 44px (ex.: `p-0 h-auto` ou `sm`), garantir `py-[10px] px-5` mínimo (§Pitfall 4). Se já usa `py-[10px]`, manter.

    Unidades.js (PATTERNS §Unidades.js):
    - Padding (linhas ~16 e ~138): `p-12` → `px-4 sm:px-12 pt-6 sm:pt-12 pb-20`.
    - Form (~150): `grid grid-cols-2 gap-4 mb-4` → `grid grid-cols-1 sm:grid-cols-2 gap-4 mb-4`.
    - Lista usa UnidadeCard (layout de cards) — NÃO precisa de fix de overflow na lista. Apenas padding + form.
  </action>
  <verify>
    <automated>npx eslint src/components/features/Parcelas.js src/components/features/Unidades.js && grep -q 'minWidth: "580px"' src/components/features/Parcelas.js && grep -q 'text-\[28px\] sm:text-\[48px\]' src/components/features/Parcelas.js && grep -q 'grid-cols-1 sm:grid-cols-2' src/components/features/Unidades.js</automated>
  </verify>
  <acceptance_criteria>
    - `Parcelas.js` contém `overflowX: "auto"` e `minWidth: "580px"`
    - `Parcelas.js` heading usa `text-[28px] sm:text-[48px]`
    - `Parcelas.js` padding usa `px-4 sm:px-12`
    - `Unidades.js` padding usa `px-4 sm:px-12` (não mais `p-12` isolado nos dois locais)
    - `Unidades.js` form usa `grid-cols-1 sm:grid-cols-2`
    - ESLint passa nos 2 arquivos
  </acceptance_criteria>
  <done>Aba Parcelas (/contratos/[id]) sem overflow de página, tabela rolável, heading responsivo; aba Unidades com padding responsivo e form colapsado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhum novo | Mudanças de CSS/className em componentes feature existentes — sem novos inputs, sem novas queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Information Disclosure | LocatariosDesktop.js (romma-desktop-only removido) | accept | Remover a classe apenas expõe a aba em mobile para o mesmo usuário autenticado; nenhum dado novo é exposto a quem já não tinha acesso. Auth/RLS inalterados |
| T-13-SC | Tampering | npm installs | accept | Nenhum pacote novo. Apenas mudanças de className/style inline |
</threat_model>

<verification>
- Spec UX-03 passa nas 4 abas: `npx playwright test --config=playwright.validation.config.js --grep "UX-03" --project=chromium`
- `scrollWidth <= 375` em /dashboard, /dashboard/unidades, /dashboard/contratos, /dashboard/locatarios
- Verificação manual (13-VALIDATION §Manual-Only): tap targets ≥44px em DevTools 375px
</verification>

<success_criteria>
- 4 abas sem overflow horizontal de página em 375px
- LocatariosDesktop visível em mobile (romma-desktop-only removido)
- Tabelas com overflow-x + minWidth corretos (680/700/580px)
- Forms colapsados, padding e heading responsivos, tap targets ≥44px
- Spec UX-03 verde
</success_criteria>

<output>
Create `.planning/phases/13-mobile-responsivo/13-03-SUMMARY.md` when done
</output>
