---
phase: 13-mobile-responsivo
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - e2e/mobile-responsive.spec.js
autonomous: true
requirements: [UX-02, UX-03, UX-04]

must_haves:
  truths:
    - "e2e/mobile-responsive.spec.js existe e roda no viewport 375x812"
    - "Os testes UX-02, UX-03 e UX-04 falham (RED) antes de qualquer implementação"
    - "Os specs usam seletores estáveis ([data-testid] e .romma-sidebar-wrapper) que as fases seguintes farão passar"
  artifacts:
    - path: "e2e/mobile-responsive.spec.js"
      provides: "Specs E2E RED para UX-02/03/04 em viewport 375px"
      contains: "viewport: { width: 375"
  key_links:
    - from: "e2e/mobile-responsive.spec.js"
      to: "e2e/helpers.js + e2e/fixtures.js"
      via: "import { login } / import { PROPRIETARIO, LOCATARIO }"
      pattern: "import.*from './helpers.js'"
---

<objective>
Criar o spec E2E Playwright `e2e/mobile-responsive.spec.js` cobrindo UX-02, UX-03 e UX-04 em viewport 375px. Este é o gate Wave 0 (Nyquist): os testes DEVEM falhar (RED) agora, e as fases seguintes (13-02, 13-03, 13-04) os tornarão verdes.

Purpose: Estabelecer o critério objetivo de "pronto" antes de qualquer mudança de UI, evitando que a fase feche sem cobertura automatizada.
Output: Um único arquivo de spec novo. Nenhum código de produção é tocado nesta fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-mobile-responsivo/13-RESEARCH.md
@.planning/phases/13-mobile-responsivo/13-PATTERNS.md
@.planning/phases/13-mobile-responsivo/13-VALIDATION.md
@e2e/tema.spec.js
@e2e/portal.spec.js
@e2e/crud.spec.js
@e2e/helpers.js
@e2e/fixtures.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar e2e/mobile-responsive.spec.js com specs RED para UX-02/03/04</name>
  <files>e2e/mobile-responsive.spec.js</files>
  <read_first>
    - e2e/tema.spec.js — padrão describe + beforeEach(login PROPRIETARIO) + waitForURL('**/dashboard')
    - e2e/portal.spec.js — padrão login LOCATARIO + waitForURL('**/portal/dashboard', timeout 15_000)
    - e2e/crud.spec.js (linha ~33) — padrão test.use({ viewport: { width, height } }) por describe
    - e2e/helpers.js — assinatura `login(page, { email, password })`; o caller chama waitForURL depois
    - e2e/fixtures.js — exports PROPRIETARIO e LOCATARIO
    - .planning/phases/13-mobile-responsivo/13-PATTERNS.md §`e2e/mobile-responsive.spec.js` — estrutura completa do spec
    - .planning/phases/13-mobile-responsivo/13-RESEARCH.md §Validation Architecture — assertions concretas por requirement
  </read_first>
  <action>
    Criar `e2e/mobile-responsive.spec.js` importando `{ test, expect }` de '@playwright/test', `{ login }` de './helpers.js' e `{ PROPRIETARIO, LOCATARIO }` de './fixtures.js'. Header de comentário com as três linhas UX-02/UX-03/UX-04 (formato do header de tema.spec.js).

    Definir o viewport 375 POR describe via `test.use({ viewport: { width: 375, height: 812 } })` — NÃO editar playwright.validation.config.js (config compartilhada de validação, runtime Desktop Chrome). O viewport vive no spec.

    Estrutura — três describes, cada um com seu próprio `test.use({ viewport })`:

    1. describe "UX-02 — Dashboard shell mobile":
       - beforeEach: `await login(page, PROPRIETARIO)` então `await page.waitForURL('**/dashboard', { timeout: 10_000 })`.
       - test "UX-02 — sidebar oculta; MobileTopBar e MobileBottomNav visíveis": navegar `/dashboard`, `await page.waitForLoadState('networkidle')`. Assertions: `await expect(page.locator('.romma-sidebar-wrapper')).toBeHidden()`; `await expect(page.locator('[data-testid="mobile-top-bar"]')).toBeVisible()`; `await expect(page.locator('[data-testid="mobile-bottom-nav"]')).toBeVisible()`.

    2. describe "UX-03 — Abas do dashboard sem overflow":
       - beforeEach: login PROPRIETARIO + waitForURL '**/dashboard'.
       - Um test por aba navegando para `/dashboard`, `/dashboard/unidades`, `/dashboard/contratos`, `/dashboard/locatarios`. Cada test: `await page.goto(rota)`, `await page.waitForLoadState('networkidle')`, depois `const scrollWidth = await page.evaluate(() => document.documentElement.scrollWidth)` e `expect(scrollWidth).toBeLessThanOrEqual(375)`. Nomear os tests "UX-03 — {rota} sem overflow horizontal".

    3. describe "UX-04 — Portal mobile":
       - beforeEach: `await login(page, LOCATARIO)` então `await page.waitForURL('**/portal/dashboard', { timeout: 15_000 })`.
       - test "UX-04 — portal sem overflow horizontal": `await page.waitForLoadState('networkidle')`, `const scrollWidth = await page.evaluate(() => document.documentElement.scrollWidth)`, `expect(scrollWidth).toBeLessThanOrEqual(375)`.

    NÃO adicionar `data-testid` em MobileNav.js nesta fase — esses atributos não existem ainda, e é exatamente por isso que UX-02 deve falhar (RED). Adicioná-los é trabalho do plano 13-02. NÃO criar DashboardShell nem tocar qualquer arquivo de produção.
  </action>
  <verify>
    <automated>npx playwright test --config=playwright.validation.config.js --grep "UX-02|UX-03|UX-04" --project=chromium 2>&1 | grep -qE "(failed|expect)" && echo "RED confirmado" || echo "FALHA: esperava testes vermelhos"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `e2e/mobile-responsive.spec.js` existe (`test -f e2e/mobile-responsive.spec.js`)
    - Contém exatamente três blocos `test.describe(` com `UX-02`, `UX-03` e `UX-04` nos rótulos
    - Cada describe contém `test.use({ viewport: { width: 375, height: 812 } })` (grep: `viewport: { width: 375`)
    - Contém `page.locator('[data-testid="mobile-top-bar"]')` e `page.locator('[data-testid="mobile-bottom-nav"]')`
    - Contém `expect(scrollWidth).toBeLessThanOrEqual(375)` em pelo menos os 4 tests de abas + 1 do portal
    - `playwright.validation.config.js` permanece byte-idêntico (não editado) — `git diff --quiet playwright.validation.config.js`
    - A suite UX-02/03/04 roda e produz falhas (RED) — saída do comando de verify imprime "RED confirmado"
  </acceptance_criteria>
  <done>O spec existe, roda em 375px, e os testes UX-02/03/04 falham por estado de produção ainda não implementado (sem data-testid, sem shell mobile, com overflow). Nenhum arquivo de produção alterado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhum novo | Fase de teste E2E — não introduz superfície de ataque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | e2e/mobile-responsive.spec.js | accept | Arquivo de teste; sem dados sensíveis, sem credenciais hardcoded (usa fixtures de teste já existentes) |
| T-13-SC | Tampering | npm installs | accept | Nenhum pacote novo instalado nesta fase — Playwright já presente (^1.60.0). Sem novos pacotes a auditar |
</threat_model>

<verification>
- `npx playwright test --config=playwright.validation.config.js --grep "UX-02|UX-03|UX-04"` roda e retorna falhas (RED) — comportamento esperado nesta fase
- `git diff --quiet playwright.validation.config.js` (config não tocado)
- Nenhum arquivo em `src/` modificado
</verification>

<success_criteria>
- e2e/mobile-responsive.spec.js criado com 3 describes (UX-02, UX-03, UX-04), viewport 375px por describe
- Suite RED confirmada: testes falham antes da implementação
- Config de validação intocado; nenhum código de produção alterado
</success_criteria>

<output>
Create `.planning/phases/13-mobile-responsivo/13-01-SUMMARY.md` when done
</output>
