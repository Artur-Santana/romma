---
phase: 23-locat-rios-busca-m-scaras
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/locatarios.js
  - e2e/server-actions.spec.js
autonomous: true
requirements: [LOC-03, LOC-04]
must_haves:
  truths:
    - "Proprietário consegue reenviar convite para um locatário pendente (status 200)"
    - "Reenviar convite para locatário já aceito retorna erro de negócio (status 400)"
    - "Reenviar convite de locatário de outro proprietário retorna 404 (IDOR fechado)"
    - "Editar locatário não sobrescreve tipo/documento — só nome, e-mail, telefone persistem"
  artifacts:
    - path: "src/actions/locatarios.js"
      provides: "Server Action reenviarConvite + editarLocatario com whitelist"
      contains: "export async function reenviarConvite"
  key_links:
    - from: "src/actions/locatarios.js (reenviarConvite)"
      to: "supabaseAdmin.auth.admin.inviteUserByEmail"
      via: "admin invite após guard de pendente"
      pattern: "inviteUserByEmail"
    - from: "src/actions/locatarios.js (reenviarConvite)"
      to: "locatarios table"
      via: "select com .eq('proprietario_id', user.id)"
      pattern: "proprietario_id"
---

<objective>
Adicionar a Server Action `reenviarConvite` em `src/actions/locatarios.js` (LOC-04) e ajustar `editarLocatario` para fazer whitelist apenas de `nome_razao_social, email, telefone` — sem sobrescrever `tipo`/`documento` (LOC-03, per D-09 e D-10).

Purpose: A camada de Server Actions é a base que o componente de Locatários (Plano 02) consome. Isolada em arquivo próprio, sem sobreposição com o componente, permite execução e verificação independentes na Wave 1.
Output: `reenviarConvite` exportada e funcional; `editarLocatario` corrigida para não regredir `tipo`/`documento`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-locat-rios-busca-m-scaras/23-CONTEXT.md
@.planning/phases/23-locat-rios-busca-m-scaras/23-RESEARCH.md
@.planning/phases/23-locat-rios-busca-m-scaras/23-PATTERNS.md
@src/actions/locatarios.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar Server Action reenviarConvite</name>
  <files>src/actions/locatarios.js</files>
  <read_first>
    - src/actions/locatarios.js (estado atual — reusar UUID_RE da linha 7, padrão de authGuard inline de revogarConvite linhas 92-100, padrão de retorno { status, erroMessage })
    - .planning/phases/23-locat-rios-busca-m-scaras/23-CONTEXT.md (D-10 — lógica exata da SA)
    - .planning/phases/23-locat-rios-busca-m-scaras/23-RESEARCH.md (Pattern 5 + Pitfall 4 — guard de proprietario_id obrigatório)
  </read_first>
  <action>
    Exportar nova função async `reenviarConvite(id)` em src/actions/locatarios.js (após `revogarConvite`), seguindo exatamente o padrão de guard inline já usado em `revogarConvite` (linhas 92-100). Sequência obrigatória, na ordem (per D-10):
    1. `const supabase = await createServer()`; `const { data: { user } } = await supabase.auth.getUser()`.
    2. Guards de auth idênticos a revogarConvite: se `!user` → `{ status: 401, erroMessage: 'Não autenticado.' }`; se `!await isProprietario(supabase)` → `{ status: 403, erroMessage: 'Sem permissão.' }`; se `!UUID_RE.test(id)` → `{ status: 400, erroMessage: 'ID inválido.' }` (reusar o UUID_RE já declarado na linha 7 do arquivo, NÃO redeclarar).
    3. Buscar o locatário via `supabaseAdmin.from('locatarios').select('email, status_convite').eq('id', id).eq('proprietario_id', user.id).single()` — o `.eq('proprietario_id', user.id)` é obrigatório para fechar IDOR (Pitfall 4). Se `fetchErr || !loc` → `{ status: 404, erroMessage: 'Locatário não encontrado.' }`.
    4. Guard de estado: se `loc.status_convite !== 'pendente'` → `{ status: 400, erroMessage: 'Convite já foi aceito.' }`.
    5. Ler `process.env.SITE_URL`; se ausente → `{ status: 500, erroMessage: 'Configuração de servidor inválida.' }`.
    6. Chamar `supabaseAdmin.auth.admin.inviteUserByEmail(loc.email, { redirectTo: `${siteUrl}/auth/confirm` })` (mesmo método e redirectTo que `convidarLocatario` usa na linha 27-29). Se `error` → `{ status: 500, erroMessage: error.message }`.
    7. Sucesso → `{ status: 200 }`.
    Manter a grafia `erroMessage` (não `errorMessage`). Não criar arquivo de lib separado — a SA fica no arquivo existente.
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma &amp;&amp; grep -q "export async function reenviarConvite" src/actions/locatarios.js &amp;&amp; grep -q "inviteUserByEmail" src/actions/locatarios.js &amp;&amp; npx eslint src/actions/locatarios.js</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/locatarios.js contém `export async function reenviarConvite(id)`
    - O corpo de reenviarConvite contém `.eq('proprietario_id', user.id)` na query de select do locatário
    - O corpo contém o guard `loc.status_convite !== 'pendente'` retornando status 400
    - O corpo chama `supabaseAdmin.auth.admin.inviteUserByEmail(loc.email, { redirectTo: ... })`
    - `npx eslint src/actions/locatarios.js` sai com código 0
    - reenviarConvite NÃO redeclara `const UUID_RE` (reusa a constante do topo do arquivo)
  </acceptance_criteria>
  <done>reenviarConvite exportada, com guard de auth, guard de proprietario_id (IDOR), guard de pendente, e chamada de inviteUserByEmail; retorna { status: 200 } ou { status: 4xx|5xx, erroMessage }.</done>
</task>

<task type="auto">
  <name>Task 2: Whitelist de campos em editarLocatario</name>
  <files>src/actions/locatarios.js</files>
  <read_first>
    - src/actions/locatarios.js (linhas 59-69 — função editarLocatario atual; linha 65 desestrutura tipo/documento, linha 66 os envia no .update())
    - .planning/phases/23-locat-rios-busca-m-scaras/23-CONTEXT.md (D-09 — editar só nome/email/telefone)
    - .planning/phases/23-locat-rios-busca-m-scaras/23-RESEARCH.md (Pattern 6 + Pitfall 3)
  </read_first>
  <action>
    Ajustar `editarLocatario(id, form)` (linhas 59-69) per D-09 para fazer whitelist apenas dos campos editáveis pós-convite. Substituir a linha de desestruturação `const { nome_razao_social, tipo, documento, email, telefone } = form` por `const { nome_razao_social, email, telefone } = form` (remover `tipo` e `documento`). Substituir o objeto passado a `.update(...)` de `{ nome_razao_social, tipo, documento, email, telefone }` por `{ nome_razao_social, email, telefone }`. Manter intactos: os guards de auth (linhas 60-64), o `.eq('id', id).eq('proprietario_id', user.id)`, e o tratamento de erro/retorno. Não alterar nenhuma outra SA do arquivo.
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma &amp;&amp; grep -A2 "export async function editarLocatario" src/actions/locatarios.js | grep -v '^#' | grep -c "documento" | grep -qx 0 &amp;&amp; npx eslint src/actions/locatarios.js</automated>
  </verify>
  <acceptance_criteria>
    - A função editarLocatario NÃO contém a substring `documento` (nem na desestruturação nem no .update())
    - A função editarLocatario NÃO contém `tipo` na desestruturação do form nem no objeto do .update()
    - O .update() de editarLocatario contém exatamente `{ nome_razao_social, email, telefone }`
    - O `.eq('proprietario_id', user.id)` permanece presente em editarLocatario
    - `npx eslint src/actions/locatarios.js` sai com código 0
  </acceptance_criteria>
  <done>editarLocatario só desestrutura e atualiza nome_razao_social, email, telefone; tipo e documento não são mais tocados, eliminando a regressão de sobrescrita.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client component → Server Action | `id` (UUID) e `form` chegam do browser, não confiáveis |
| Server Action → Supabase Admin (service role) | reenviarConvite/editarLocatario usam supabaseAdmin que bypassa RLS — guard manual obrigatório |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Elevation of Privilege | reenviarConvite(id) | mitigate | `.eq('proprietario_id', user.id)` no select força que só o dono possa reenviar; locatário de outro proprietário → 404 (Pitfall 4 / V4 ASVS) |
| T-23-02 | Tampering | reenviarConvite(id) | mitigate | guard `status_convite !== 'pendente'` impede reenvio de convite já aceito antes de chamar inviteUserByEmail |
| T-23-03 | Spoofing | reenviarConvite/editarLocatario | mitigate | `getUser()` + `isProprietario(supabase)` antes de qualquer escrita (V2 ASVS) |
| T-23-04 | Tampering | editarLocatario(id, form) | mitigate | whitelist `{ nome_razao_social, email, telefone }` impede o cliente sobrescrever tipo/documento via form malicioso |
| T-23-05 | Input validation | reenviarConvite(id) | mitigate | `UUID_RE.test(id)` rejeita IDs malformados antes de qualquer query (V5 ASVS) |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote novo nesta fase (RESEARCH §Package Legitimacy Audit: não aplicável) |
</threat_model>

<verification>
- `npx eslint src/actions/locatarios.js` limpo
- `reenviarConvite` exportada com guard de proprietario_id + guard de pendente
- `editarLocatario` sem `tipo`/`documento`
- Nenhuma outra SA (convidarLocatario, deletarLocatario, revogarConvite) alterada
</verification>

<success_criteria>
- LOC-04: SA `reenviarConvite` existe, fecha IDOR via proprietario_id, bloqueia reenvio de aceitos, invoca inviteUserByEmail
- LOC-03 (backend): `editarLocatario` faz whitelist nome/email/telefone, não regride tipo/documento
- ESLint limpo, sem pacotes novos
</success_criteria>

<output>
Create `.planning/phases/23-locat-rios-busca-m-scaras/23-01-SUMMARY.md` when done
</output>
