---
phase: 23-locat-rios-busca-m-scaras
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/components/features/LocatariosDesktop.js
  - src/components/features/Locatarios.js
autonomous: false
requirements: [LOC-01, LOC-02, LOC-03, LOC-05, LOC-06]
must_haves:
  truths:
    - "Proprietário busca locatário por nome, e-mail ou documento e vê contador de resultados"
    - "No modal de convite, trocar PF↔PJ re-formata o documento sem digit-jumble; o valor é armazenado só com dígitos"
    - "Editar locatário pelo modal (só nome, e-mail, telefone com máscara) persiste"
    - "Reenviar convite mostra '✓ Reenviado' por 2200ms sem reload da lista"
    - "Revogar acesso abre ConfirmDialog antes de chamar a SA"
    - "No mobile, cada locatário expõe ações (Reenviar/Revogar para pendentes; Editar para ativos) sem hover-reveal"
  artifacts:
    - path: "src/components/features/LocatariosDesktop.js"
      provides: "Componente único desktop (cards) + mobile (rows) com busca, máscaras, reenviar, ConfirmDialog"
      contains: "romma-mobile-only"
      min_lines: 400
  key_links:
    - from: "src/components/features/LocatariosDesktop.js (handleReenviar)"
      to: "reenviarConvite (Server Action)"
      via: "import de @/actions/locatarios"
      pattern: "reenviarConvite"
    - from: "src/components/features/LocatariosDesktop.js (revogar)"
      to: "ConfirmDialog"
      via: "estado confirmRevogarId + componente ConfirmDialog"
      pattern: "ConfirmDialog"
    - from: "src/app/dashboard/locatarios/page.js"
      to: "LocatariosDesktop"
      via: "import default (único componente)"
      pattern: "LocatariosDesktop"
---

<objective>
Evoluir `src/components/features/LocatariosDesktop.js` in-place para o contrato visual da Phase 23: busca client-side (LOC-01), máscaras CPF/CNPJ/telefone com re-formatação ao trocar PF/PJ (LOC-02), modal de edição com whitelist de campos e máscara de telefone (LOC-03), revogar com ConfirmDialog (LOC-05), reenviar com feedback "✓ Reenviado", e layout duplo desktop (grade de cards) + mobile (rows com ações sempre visíveis, LOC-06). Remover o legado `Locatarios.js` ao final (per D-01).

Purpose: Entrega a fatia visível/interativa da fase. Consome `reenviarConvite` e a `editarLocatario` corrigida do Plano 01.
Output: `LocatariosDesktop.js` evoluído como componente único desktop+mobile; `Locatarios.js` removido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-locat-rios-busca-m-scaras/23-CONTEXT.md
@.planning/phases/23-locat-rios-busca-m-scaras/23-RESEARCH.md
@.planning/phases/23-locat-rios-busca-m-scaras/23-PATTERNS.md
@.planning/phases/23-locat-rios-busca-m-scaras/23-UI-SPEC.md
@src/components/features/LocatariosDesktop.js
@src/components/features/Contratos.js
@src/components/ui/ConfirmDialog.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Máscaras, busca, estado e handlers (lógica)</name>
  <files>src/components/features/LocatariosDesktop.js</files>
  <read_first>
    - src/components/features/LocatariosDesktop.js (estado linhas 30-44, handlers 46-107, fmtDoc 19-23, getInitials 25-28, segmented PF/PJ 369-383)
    - .planning/phases/23-locat-rios-busca-m-scaras/23-RESEARCH.md (Pattern 3 máscaras, Pattern 4 resent, Pitfall 1 maskPhone 10vs11, Pitfall 2 abrir edit com maskPhone, Pitfall 5 resent Set de UUID string)
    - .planning/phases/23-locat-rios-busca-m-scaras/23-PATTERNS.md (funções de máscara D-06, strip antes da SA D-08, abertura do modal de edição com maskPhone)
    - .planning/phases/23-locat-rios-busca-m-scaras/23-CONTEXT.md (D-05 a D-13)
  </read_first>
  <action>
    Adicionar a lógica que o layout (Task 2) consumirá, mantendo o padrão de form-state objeto único e funções nomeadas.

    Funções puras inline (declarar no topo do módulo, junto de fmtDoc/getInitials, per D-06):
    - onlyDigits(s) → retorna s.replace(/\D/g, "") (ou "" se falsy).
    - maskCPF(v) → formata até 11 dígitos como 000.000.000-00 (branches por comprimento conforme Pattern 3 do RESEARCH).
    - maskCNPJ(v) → formata até 14 dígitos como 00.000.000/0000-00.
    - maskDocumento(tipo, v) → tipo === "pf" ? maskCPF(onlyDigits(v)) : maskCNPJ(onlyDigits(v)).
    - maskPhone(v) → slice para 11 dígitos ANTES do branch (Pitfall 1); 10 dígitos → (11) 9999-9999, 11 → (11) 99999-9999.
    - Manter fmtDoc e getInitials existentes.

    Imports: adicionar reenviarConvite ao import de @/actions/locatarios (já importa convidarLocatario, revogarConvite, editarLocatario); adicionar import default ConfirmDialog de @/components/ui/ConfirmDialog.

    Estado novo (per PATTERNS.md):
    - const [q, setQ] = useState("").
    - const [resent, setResent] = useState(new Set()).
    - const [confirmRevogarId, setConfirmRevogarId] = useState(null).
    - Derivar (sem useState): const view = locatarios.filter(l => !q || (l.nome_razao_social + " " + l.email + " " + l.documento).toLowerCase().includes(q.toLowerCase())) (D-05).

    Ajustes em handlers:
    - handleConvidar: strip antes de chamar a SA — convidarLocatario(form.email, form.nome_razao_social, onlyDigits(form.documento), onlyDigits(form.telefone), form.tipo) (D-08).
    - handleEditarLocatario(locatario): inicializar formEdit apenas com { nome_razao_social, email, telefone: maskPhone(locatario.telefone ?? "") } — remover tipo e documento (D-09 + Pitfall 2).
    - handleSalvarLocatario: chamar editarLocatario(editandoId, { nome_razao_social: formEdit.nome_razao_social, email: formEdit.email, telefone: onlyDigits(formEdit.telefone) }) (strip antes de salvar).
    - Novo handleReenviar(id) (Pattern 4): chama reenviarConvite(id); se status !== 200 → toast.error(erroMessage ?? "Erro ao reenviar convite.") e return; senão setResent(s => new Set([...s, id])) e setTimeout(() => setResent(s => { const n = new Set(s); n.delete(id); return n }), 2200). O Set guarda l.id (UUID string), não o objeto (Pitfall 5). NÃO chamar getLocatarios após reenvio.
    - Atualizar o formEdit inicial (linha 39-41) para { nome_razao_social: "", email: "", telefone: "" } (sem tipo/documento).
    - Manter handleRevogar(id) como está (animação removingIds + getLocatarios); será disparado pelo ConfirmDialog (Task 2).
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma &amp;&amp; grep -q "function maskCPF" src/components/features/LocatariosDesktop.js &amp;&amp; grep -q "function maskCNPJ" src/components/features/LocatariosDesktop.js &amp;&amp; grep -q "function maskPhone" src/components/features/LocatariosDesktop.js &amp;&amp; grep -q "reenviarConvite" src/components/features/LocatariosDesktop.js &amp;&amp; grep -q "const view = locatarios.filter" src/components/features/LocatariosDesktop.js &amp;&amp; npx eslint src/components/features/LocatariosDesktop.js</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo declara function onlyDigits, function maskCPF, function maskCNPJ, function maskDocumento, function maskPhone
    - maskPhone faz slice para 11 dígitos antes de ramificar (não estoura com 10 vs 11)
    - O import de @/actions/locatarios inclui reenviarConvite
    - O arquivo importa ConfirmDialog de @/components/ui/ConfirmDialog
    - Existe estado q, resent (new Set()), confirmRevogarId
    - Existe a derivação const view = locatarios.filter(...) usando nome_razao_social, email e documento
    - handleConvidar passa onlyDigits(form.documento) e onlyDigits(form.telefone) à SA
    - handleSalvarLocatario chama editarLocatario com objeto contendo apenas nome_razao_social, email e telefone (telefone via onlyDigits)
    - handleReenviar NÃO chama getLocatarios
    - npx eslint src/components/features/LocatariosDesktop.js sai com código 0
  </acceptance_criteria>
  <done>Funções de máscara, busca derivada, estado (q/resent/confirmRevogarId), strip antes das SAs, handleReenviar e formEdit whitelisted estão implementados; ESLint limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Layout duplo (cards desktop + rows mobile), segmented PF/PJ com máscara, ConfirmDialog, campo de busca</name>
  <files>src/components/features/LocatariosDesktop.js, src/components/features/Locatarios.js</files>
  <read_first>
    - src/components/features/LocatariosDesktop.js (tabela atual a substituir linhas 120-221; callout 223-235; modais 237-429; Field 434-443; segmented PF/PJ 369-383)
    - .planning/phases/23-locat-rios-busca-m-scaras/23-UI-SPEC.md (Card desktop, Row mobile, Campo de busca, Modais, Copywriting Contract — textos exatos, Avatar lógica de cor)
    - .planning/phases/23-locat-rios-busca-m-scaras/23-PATTERNS.md (layout desktop/mobile de Contratos.js, Avatar inline, campo de busca, ghost buttons, ConfirmDialog)
    - src/components/features/Contratos.js (padrão romma-desktop-only/romma-mobile-only e ghost buttons all:unset)
    - src/components/ui/ConfirmDialog.js (props: open, title, body, confirmLabel, danger, onConfirm, onCancel)
  </read_first>
  <action>
    Substituir o bloco de tabela atual (linhas 120-221, o div com overflowX:auto e seu conteúdo) por: campo de busca + grade de cards desktop + lista de rows mobile + ConfirmDialog. Manter PageHeader, callout de convite e os dois modais (com ajustes abaixo).

    Avatar inline: declarar componente function Avatar({ nome, pendente, size = 40 }) que renderiza um quadrado size×size (flexShrink:0, sem border-radius), border var(--border-2), background var(--surface) quando aceito / transparent quando pendente, cor var(--fg-1) (aceito) / var(--fg-4) (pendente, dim), fontFamily var(--font-mono), fontSize size === 40 ? 14 : 11, fontWeight 700, conteúdo getInitials(nome) (D-04).

    Campo de busca (após PageHeader, antes dos layouts) — per UI-SPEC §Campo de busca: container marginBottom var(--rd-block-sm, 16px), ícone ⌕ absoluto à esquerda (cor var(--fg-5), fontSize 11, pointerEvents none), input com all:unset, width 100%, padding 9px 12px 9px 30px, fontSize 13, fontFamily var(--font-body), background var(--surface-hi), border 1px solid var(--border-3), placeholder exato "Buscar por nome, e-mail ou documento...", value={q} / onChange={e => setQ(e.target.value)}. Abaixo, quando q não vazio, span className="r-meta" com texto {view.length} resultado(s).

    Desktop (romma-desktop-only): div com display:grid; gridTemplateColumns: repeat(auto-fill, minmax(300px, 1fr)); gap:12px (D-02). Iterar view.map((l, i) => ...). Empty-state busca: quando view.length === 0, célula gridColumn "1 / -1" com texto "Nenhum locatário encontrado." (mono 12px, fg-4, centralizado). Cada card (per UI-SPEC §Card desktop): border 1px solid var(--border-3), background var(--surface), padding var(--rd-panel, 20px), flex coluna gap 12, animação rFade var(--dur-base, 240ms) var(--ease-crisp) both com animationDelay ${i*30}ms, e opacity/scale 220ms via removingIds.has(l.id). Conteúdo: header (Avatar size=40 + nome em .r-subhead truncado + linha .r-meta com {l.tipo?.toUpperCase()} · {fmtDoc(l.tipo, l.documento)}); email em .r-meta truncado; footer com borderTop 1px solid var(--border-3); paddingTop 12 contendo à esquerda StatusBadge (pendente_convite/aceito) + contador {ativosCount}/{cs.length} contrato(s) em .r-meta, e à direita as ações.

    Derivação por card (D-13): const cs = contratos.filter(c => c.locatario_id === l.id); const ativosCount = cs.filter(c => c.status === "ativo").length.

    Ações (ghost buttons style all:unset, cursor:pointer, fontFamily var(--font-mono), fontSize 10, fontWeight 700, letterSpacing 0.5px, textTransform uppercase, padding 4px 0):
    - Se pendente: botão Reenviar (cor resent.has(l.id) ? var(--success) : var(--indigo), texto resent.has(l.id) ? "✓ Reenviado" : "Reenviar" desktop / "Reenviar convite" mobile, onClick handleReenviar(l.id)) + botão Revogar (cor var(--danger-fg), texto "Revogar", onClick setConfirmRevogarId(l.id)).
    - Se aceito: botão Editar (cor var(--fg-3), onClick handleEditarLocatario(l)). Remover o antigo botão "VER →" (não há rota de detalhe nesta variante) e o useRouter se ficar sem uso.

    Mobile (romma-mobile-only): container .r-panel. Empty-state quando view.length === 0: "Nenhum locatário encontrado." (mono 12px, fg-4, centralizado). Iterar view.map((l, i) => ...): cada row com padding 12px var(--rd-row-x, 16px); borderTop i>0 ? "1px solid var(--border-3)" : "none"; display:flex; flexDirection:column; gap:10, opacity 220ms via removingIds. Header da row: Avatar size=32 + nome .r-subhead (14px) + .r-meta tipo·documento + StatusBadge à direita. Footer da row (SEMPRE visível, sem hover — LOC-06): borderTop 1px solid var(--border-3); paddingTop 10; display:flex; gap:8. Pendente → Reenviar (esq) + spacer flex + Revogar (dir, setConfirmRevogarId(l.id)). Aceito → contador {ativosCount}/{cs.length} contrato(s) em .r-meta (esq) + spacer + Editar (dir).

    ConfirmDialog (D-12): renderizar no JSX (próximo aos modais) com open={confirmRevogarId !== null}, title "Revogar acesso?", body usando locatarios.find(l => l.id === confirmRevogarId)?.nome_razao_social na frase "O convite/acesso de {nome} será revogado. Esta ação não pode ser desfeita.", confirmLabel "Revogar Acesso", danger true, onCancel={() => setConfirmRevogarId(null)}, onConfirm={() => { handleRevogar(confirmRevogarId); setConfirmRevogarId(null) }}.

    Modal Convidar — segmented PF/PJ (linhas 369-383): no onClick de cada botão de tipo, trocar setForm({ ...form, tipo: t }) por setForm({ ...form, tipo: t, documento: maskDocumento(t, form.documento) }) (D-07, evita digit-jumble). No input de documento, value={form.documento} e onChange={e => setForm({ ...form, documento: maskDocumento(form.tipo, e.target.value) })}. No input de telefone, onChange={e => setForm({ ...form, telefone: maskPhone(e.target.value) })}. Placeholders exatos: CPF 000.000.000-00, CNPJ 00.000.000/0000-00, telefone (11) 99999-9999.

    Modal Editar (linhas 237-332): remover os Fields de Tipo (Select, linhas 262-275) e de Documento (linhas 277-286) — não editáveis pós-convite (D-09). Manter apenas Nome/Razão Social, Email, Telefone. No input de telefone, onChange={e => setFormEdit({ ...formEdit, telefone: maskPhone(e.target.value) })} (o valor já chega formatado por handleEditarLocatario). Remover Select dos imports se não for mais usado em nenhum lugar do arquivo.

    Após o layout estar completo, remover src/components/features/Locatarios.js (legado, page.js já importa LocatariosDesktop — D-01). A remoção é feita no comando de verify.
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma &amp;&amp; rm -f src/components/features/Locatarios.js &amp;&amp; grep -q "romma-mobile-only" src/components/features/LocatariosDesktop.js &amp;&amp; grep -q "minmax(300px, 1fr)" src/components/features/LocatariosDesktop.js &amp;&amp; grep -q "ConfirmDialog" src/components/features/LocatariosDesktop.js &amp;&amp; grep -q "maskDocumento(t, form.documento)" src/components/features/LocatariosDesktop.js &amp;&amp; grep -q "Buscar por nome, e-mail ou documento" src/components/features/LocatariosDesktop.js &amp;&amp; test ! -f src/components/features/Locatarios.js &amp;&amp; npx eslint src/components/features/LocatariosDesktop.js</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/LocatariosDesktop.js contém romma-desktop-only e romma-mobile-only
    - Contém a string de grid minmax(300px, 1fr)
    - Contém o placeholder exato "Buscar por nome, e-mail ou documento..."
    - Contém Avatar inline (function Avatar) usado em desktop (size 40) e mobile (size 32)
    - Contém ConfirmDialog renderizado com open={confirmRevogarId !== null}
    - O segmented PF/PJ chama maskDocumento(t, form.documento) no onClick
    - O modal Editar NÃO contém Field de Tipo (Select) nem de Documento
    - O footer da row mobile não usa :hover para revelar ações (ações no JSX estático do footer)
    - src/components/features/Locatarios.js NÃO existe mais
    - npx eslint src/components/features/LocatariosDesktop.js sai com código 0
  </acceptance_criteria>
  <done>Layout duplo (cards desktop minmax 300px + rows mobile com ações sempre visíveis), busca com contador, Avatar inline, segmented com re-máscara, modal editar sem tipo/documento, ConfirmDialog para revogar; Locatarios.js removido; ESLint limpo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual e interativa (desktop + mobile 375px)</name>
  <what-built>
    LocatariosDesktop.js evoluído: grade de cards no desktop, rows com ações expostas no mobile, busca por nome/e-mail/documento, máscaras CPF/CNPJ/telefone com re-formatação ao trocar PF/PJ, reenviar com feedback "✓ Reenviado", revogar com ConfirmDialog, editar (só nome/e-mail/telefone).
  </what-built>
  <how-to-verify>
    1. Rodar `npm run dev` e acessar `/dashboard/locatarios` autenticado como proprietário.
    2. DESKTOP: confirmar grade de cards (não tabela), cada card com avatar+nome+tipo/documento formatado+email+badge+contador "N/M contrato(s)".
    3. Digitar no campo de busca um trecho de nome, depois um e-mail, depois dígitos de documento — a grade filtra e aparece "{N} resultado(s)".
    4. Abrir "Convidar Locatário": digitar 11 dígitos no documento com PF (vê 000.000.000-00), trocar para PJ — os dígitos re-formatam para padrão CNPJ sem sumir (sem digit-jumble). Telefone formata (11) 99999-9999.
    5. Em um locatário pendente, clicar Reenviar — botão vira "✓ Reenviado" em verde por ~2s, sem reload da lista.
    6. Clicar Revogar em um pendente — abre ConfirmDialog "Revogar acesso?"; cancelar não remove; confirmar remove com animação.
    7. Em um locatário aceito, clicar Editar — modal mostra só Nome, Email, Telefone (telefone com máscara). Salvar persiste. Tipo/documento NÃO aparecem.
    8. MOBILE (DevTools 375px): cada locatário aparece como row com ações (Reenviar/Revogar ou contador/Editar) sempre visíveis no footer, sem precisar de hover.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário (browser) → componente client | input de busca, documento, telefone digitados — não confiáveis |
| componente client → Server Action | reenviarConvite/editarLocatario/revogarConvite recebem dados do browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Tampering | handleConvidar / handleSalvarLocatario | mitigate | onlyDigits() strip de documento e telefone no handler antes de chamar a SA; a SA do Plano 01 valida DOCUMENTO_RE como segunda linha |
| T-23-07 | Information Disclosure | supabaseAdmin em client | accept | reenviarConvite/editarLocatario/revogarConvite são Server Actions; o componente client só as invoca — supabaseAdmin nunca é importado no client (RESEARCH anti-pattern) |
| T-23-08 | Elevation of Privilege | revogar/reenviar via UI | mitigate | as SAs (Plano 01 / existentes) fazem o guard de proprietario_id; o frontend não confia no client para autorização |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote novo nesta fase (RESEARCH §Package Legitimacy Audit: não aplicável) |
</threat_model>

<verification>
- ESLint limpo em LocatariosDesktop.js
- Layout duplo desktop/mobile presente; Locatarios.js removido
- Máscaras, busca, ConfirmDialog, reenviar implementados conforme UI-SPEC
- Checkpoint humano valida visual/interação desktop + mobile 375px
</verification>

<success_criteria>
- LOC-01: busca client-side por nome/e-mail/documento + contador de resultados
- LOC-02: convite com segmented PF/PJ, máscara CPF/CNPJ re-formatando ao trocar tipo, telefone com máscara, armazena só dígitos
- LOC-03: editar só nome/e-mail/telefone com máscara, persiste
- LOC-05: revogar abre ConfirmDialog antes da SA
- LOC-06: ações expostas no mobile sem hover-reveal
</success_criteria>

<output>
Create `.planning/phases/23-locat-rios-busca-m-scaras/23-02-SUMMARY.md` when done
</output>
