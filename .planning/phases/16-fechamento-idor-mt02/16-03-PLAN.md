---
phase: 16-fechamento-idor-mt02
plan: "03"
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - test/unit/actions/unidades.test.js
  - test/unit/actions/contratos.test.js
  - test/unit/actions/parcelas.test.js
autonomous: true
requirements: [MT-03]
must_haves:
  truths:
    - "Each of the four fixed Actions has unit coverage: happy (owner) + validation error + cross-tenant block (D-04)"
    - "Each cross-tenant test asserts the mutation was NOT called and the ownership filter used proprietario_id = user.id (D-06)"
    - "npx vitest run exits 0 across the full suite including the new cases (D-07)"
    - "npx playwright test --list parses with exit 0 (D-07)"
  artifacts:
    - path: "test/unit/actions/parcelas.test.js"
      provides: "marcarParcelaComoPaga 4-hop ownership coverage (happy + validation + cross-tenant + D-08)"
      contains: "proprietario_id"
    - path: "test/unit/actions/unidades.test.js"
      provides: "criarUnidade ownership coverage appended to existing editar/deletar specs"
      contains: "criarUnidade"
    - path: "test/unit/actions/contratos.test.js"
      provides: "criarContrato + editarContrato ownership coverage appended to existing cancelar/encerrar specs"
      contains: "criarContrato"
  key_links:
    - from: "test cross-tenant cases"
      to: "mockAdmin.eq('proprietario_id', mockUser.id)"
      via: "D-08-style assertion on the edificios ownership query"
      pattern: "toHaveBeenCalledWith\\('proprietario_id', mockUser.id\\)"
---

<objective>
Prove the four owner pre-checks from Plans 16-01/16-02 with unit tests that mirror EXACTLY the 15-04 pattern (D-04, D-05): extend `unidades.test.js` with a `criarUnidade` block, extend `contratos.test.js` with `criarContrato` + `editarContrato` blocks, and create a new `parcelas.test.js` covering `marcarParcelaComoPaga`. Each Action gets happy (owner) + validation error + cross-tenant block, plus a D-08-style assertion that the ownership query filtered on `proprietario_id = user.id` (D-06). Final gate: `npx vitest run` exit 0 and `npx playwright test --list` exit 0 (D-07).

Purpose: lock the IDOR fixes against regression and satisfy MT-03 success criteria 5 and 6.
Output: three test files updated/created, full unit suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-fechamento-idor-mt02/16-CONTEXT.md
@.planning/phases/15-testes/15-04-SUMMARY.md
@test/helpers/supabaseMock.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend unidades.test.js (criarUnidade) + contratos.test.js (criarContrato, editarContrato)</name>
  <files>test/unit/actions/unidades.test.js, test/unit/actions/contratos.test.js</files>
  <read_first>
    - test/unit/actions/unidades.test.js — existing editarUnidade/deletarUnidade describe blocks; reuse the exact vi.hoisted+require header, mock wiring, and beforeEach (lines 1–42). The import line 31 must be extended to include `criarUnidade`.
    - test/unit/actions/contratos.test.js — existing cancelar/encerrar blocks + the supabaseJWT stub (lines 34–36) and the setupOwnerSingles/setupCrossTenantSingles/setupMutationThenables helpers. Import line 38 must add `criarContrato, editarContrato`.
    - test/helpers/supabaseMock.js — single() drives `.single()` fetches via mockResolvedValueOnce; thenable `then` drives awaited mutation chains (insert/update) via mockImplementationOnce.
    - src/actions/unidades.js + src/actions/contratos.js (as fixed by 16-01) — confirm exact erroMessages: criarUnidade cross-tenant 'Edifício não encontrado.', criarContrato 'Unidade não encontrada.', editarContrato 'Contrato não encontrado.'.
  </read_first>
  <action>
    In unidades.test.js: add `criarUnidade` to the import. Add a `describe('criarUnidade')` block with its own beforeEach (resetAll + mockGetUser + mockIsProprietario, copy from existing blocks). Use a valid form including `edificio_id: <valid UUID>`. Cases: (1) happy — single() returns the owned edificio row once, then an insert thenable returns `{ error: null }` → `{ status: 200 }`; (2) cross-tenant — single() returns `{ data: null, error: null }` for the edificios fetch → `{ status: 404, erroMessage: 'Edifício não encontrado.' }` and assert `mockAdmin.insert` not called; (3) validation — form with invalid edificio_id UUID → `{ status: 400, erroMessage: 'Edifício inválido.' }`; (4) auth guard — null user → status 401; (5) D-08 — happy setup, assert `mockAdmin.eq` called with `('proprietario_id', mockUser.id)`.

    In contratos.test.js: add `criarContrato, editarContrato` to the import. Add a `describe('criarContrato')` block: happy uses two single() resolves (unidade→edificio_id, edificio→row) then an insert thenable `{ data: { id }, error: null }` and the unidade-status update thenable `{ error: null }` → `{ status: 200, data: { id } }` (set the insert chain via `then` mockImplementationOnce sequence, mirroring setupMutationThenables); cross-tenant → second single() returns null → `{ status: 404, erroMessage: 'Unidade não encontrada.' }`, assert `mockAdmin.insert` not called; validation invalid unidade_id → `{ status: 400, erroMessage: 'Unidade inválida.' }`; auth null → 401; D-08 assert `('proprietario_id', mockUser.id)`. Add a `describe('editarContrato')` block mirroring the existing cancelarContrato block (3 single() ownership resolves via setupOwnerSingles, then one update thenable): happy → `{ status: 200 }`; cross-tenant via setupCrossTenantSingles → `{ status: 404, erroMessage: 'Contrato não encontrado.' }`, assert update not called; validation invalid id → 400; auth null → 401; D-08 assert `('proprietario_id', mockUser.id)`.

    Append-only edits — do not alter existing describe blocks. Reuse existing helpers in contratos.test.js where the thenable count matches; for criarContrato (1 single-step insert + 1 update) define a local thenable setup if the existing helper's 3-step sequence does not fit.
  </action>
  <verify>
    <automated>npx vitest run test/unit/actions/unidades.test.js test/unit/actions/contratos.test.js</automated>
  </verify>
  <acceptance_criteria>
    - unidades.test.js contains a `describe('criarUnidade')` with happy/cross-tenant/validation/auth/D-08 cases; cross-tenant asserts `insert` not called.
    - contratos.test.js contains `describe('criarContrato')` and `describe('editarContrato')` blocks with the same five-case shape each.
    - Every cross-tenant/D-08 case asserts `mockAdmin.eq` called with `('proprietario_id', mockUser.id)`.
    - `npx vitest run test/unit/actions/unidades.test.js test/unit/actions/contratos.test.js` exits 0; existing cases still pass.
  </acceptance_criteria>
  <done>criarUnidade, criarContrato, editarContrato each have owner happy + validation + cross-tenant + D-08 coverage; both files green; existing tests untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create parcelas.test.js (marcarParcelaComoPaga 4-hop) + full suite gate</name>
  <files>test/unit/actions/parcelas.test.js</files>
  <read_first>
    - test/unit/actions/contratos.test.js — closest analog for chained .single() ownership (setupOwnerSingles is 3 hops; parcelas needs 4). Copy the vi.hoisted+require header, the three vi.mock blocks (supabase-server, auth, supabaseAdmin). parcelas.js does NOT import supabaseJWT, so the supabaseJWT stub is NOT needed here.
    - src/actions/parcelas.js (as fixed by 16-02) — confirm marcarParcelaComoPaga's 4-hop order (parcela→contrato→unidade→edificio) and cross-tenant erroMessage 'Parcela não encontrada.'; the final update is a thenable chain (update().eq().in()).
    - test/helpers/supabaseMock.js — single() via mockResolvedValueOnce for the 4 hops; then via mockImplementationOnce for the final update.
  </read_first>
  <action>
    Create test/unit/actions/parcelas.test.js mirroring contratos.test.js structure: same vi.hoisted+require header (mockUser, mockIsProprietario, mockGetUser, mockAdmin, configureResult, resetAll), the three vi.mock blocks (supabase-server, auth, supabaseAdmin) — omit the supabaseJWT stub. Import `marcarParcelaComoPaga` from `@/actions/parcelas`. Define a `setupOwnerSingles4` helper queuing four mockResolvedValueOnce: parcela→`{ contrato_id }`, contrato→`{ unidade_id }`, unidade→`{ edificio_id }`, edificio→`{ id }` (owner row). Define a cross-tenant variant where the fourth single() returns `{ data: null, error: null }`. Define a thenable setup for the final update returning `{ error: null }`.

    Cases in `describe('marcarParcelaComoPaga')` with beforeEach (resetAll + mockGetUser + mockIsProprietario): (1) happy — setupOwnerSingles4 + update thenable → `{ status: 200 }`; (2) cross-tenant — fourth single() null → `{ status: 404, erroMessage: 'Parcela não encontrada.' }`, assert `mockAdmin.update` not called; (3) validation — `'not-a-uuid'` → `{ status: 400, erroMessage: 'ID inválido.' }`; (4) auth guard — null user → status 401; (5) D-08 — happy setup, assert `mockAdmin.eq` called with `('proprietario_id', mockUser.id)`.

    Then run the full unit suite and the Playwright parse gate as the closing verification (D-07): `npx vitest run` (all five test files) must exit 0; `npx playwright test --list` must exit 0.
  </action>
  <verify>
    <automated>npx vitest run && npx playwright test --list >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - test/unit/actions/parcelas.test.js exists with a `describe('marcarParcelaComoPaga')` covering happy/cross-tenant/validation/auth/D-08.
    - Cross-tenant case asserts `mockAdmin.update` not called; D-08 asserts `mockAdmin.eq` called with `('proprietario_id', mockUser.id)`.
    - No supabaseJWT stub present (parcelas.js does not import it).
    - `npx vitest run` exits 0 across all five action test files (D-07).
    - `npx playwright test --list` exits 0 (D-07).
  </acceptance_criteria>
  <done>parcelas.test.js proves the 4-hop owner pre-check (happy + validation + cross-tenant + D-08); full vitest suite exits 0; playwright --list parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → production Actions | Tests are the regression boundary that keeps T-16-01 closed; a weakened cross-tenant assertion would silently reopen the IDOR. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Elevation of Privilege (IDOR) | regression coverage for all 4 fixed Actions | mitigate | Each Action has a cross-tenant test asserting the mutation is NOT called AND `mockAdmin.eq('proprietario_id', mockUser.id)` (D-06/D-08) — a regression that drops the owner filter fails CI. |
| T-16-03 | Tampering | test mock fidelity | accept | Mocks model `.single()` and thenable chains per supabaseMock.js; behavior also exercised live by E2E in CI. |

No package installs in this plan — no T-16-SC supply-chain surface.
</threat_model>

<verification>
- `npx vitest run test/unit/actions/unidades.test.js test/unit/actions/contratos.test.js` exits 0.
- `npx vitest run` (full suite, all five action files) exits 0 (D-07).
- `npx playwright test --list` exits 0 (D-07).
- Each new cross-tenant test asserts mutation-not-called + `proprietario_id`/`mockUser.id` filter (D-06/D-08).
</verification>

<success_criteria>
- criarUnidade, criarContrato, editarContrato, marcarParcelaComoPaga each covered by happy + validation + cross-tenant tests mirroring 15-04 (D-04).
- D-08-style `proprietario_id = user.id` assertion present per Action (D-06).
- Full `npx vitest run` exit 0; `npx playwright test --list` exit 0 (D-07).
</success_criteria>

<output>
Create `.planning/phases/16-fechamento-idor-mt02/16-03-SUMMARY.md` when done.
</output>
