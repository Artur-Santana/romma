---
phase: 16-fechamento-idor-mt02
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/parcelas.js
autonomous: true
requirements: [MT-03]
must_haves:
  truths:
    - "marcarParcelaComoPaga rejects (404) a parcela whose contrato→unidade→edificio is not owned by the authenticated Proprietário before marking paid"
    - "authGuard in parcelas.js returns { user } so the Action can scope by owner"
    - "Legitimate owner mark-as-paid still returns { status: 200 }"
  artifacts:
    - path: "src/actions/parcelas.js"
      provides: "authGuard returning { user } + 4-hop ownership pre-check in marcarParcelaComoPaga"
      contains: "proprietario_id"
  key_links:
    - from: "src/actions/parcelas.js marcarParcelaComoPaga"
      to: "edificios.proprietario_id"
      via: "4-hop fetch parcela→contrato→unidade→edificio before update"
      pattern: "proprietario_id.*user\\.id"
---

<objective>
Close the deepest open IDOR write vector (MT-03): `marcarParcelaComoPaga` (parcelas.js, line 17) uses `supabaseAdmin` (bypasses RLS) and is guarded only by `authGuard`. Unlike unidades.js/contratos.js, parcelas.js `authGuard` STILL returns `{}` today (the same bug 15-02 fixed elsewhere) — fix it to `return { user }` and add a 4-hop ownership pre-check (parcela→contrato→unidade→edificio→proprietario_id) before marking the parcela paid (D-01, D-02).

Purpose: any authenticated Proprietário can currently mark ANY parcela paid by id, including another tenant's — a real multi-tenant IDOR.
Output: src/actions/parcelas.js with `authGuard` returning `{ user }`, the caller updated to `const { err, user } = await authGuard()`, and the 4-hop owner pre-check returning 404 on cross-tenant, preserving the `{ status, erroMessage }` contract (D-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-fechamento-idor-mt02/16-CONTEXT.md
@.planning/phases/15-testes/15-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: authGuard returns { user } + 4-hop ownership pre-check in marcarParcelaComoPaga</name>
  <files>src/actions/parcelas.js</files>
  <read_first>
    - src/actions/parcelas.js — `authGuard` (line 9) returns `{}` on success (the bug); `marcarParcelaComoPaga` (line 17) is the target, currently `const { err } = await authGuard()`.
    - src/actions/contratos.js — `cancelarContrato`/`encerrarContrato` show the 3-hop chain (contrato→unidade→edificio); marcarParcelaComoPaga needs one more hop in front (parcela→contrato).
    - .planning/phases/15-testes/15-02-SUMMARY.md — authGuard `{ user }` change + fetch-then-verify chain, 404 on cross-tenant.
  </read_first>
  <behavior>
    - authGuard success path returns { user } (was {}); 401/403 error branches unchanged.
    - Owner case: parcela→contrato→unidade→edificio chain resolves to an owned edificio → update proceeds → { status: 200 }.
    - Cross-tenant case: edificios ownership query returns no row → { status: 404, erroMessage: 'Parcela não encontrada.' }, parcelas update NOT called.
    - Validation unchanged: invalid UUID → { status: 400, erroMessage: 'ID inválido.' } before the chain.
  </behavior>
  <action>
    Change `authGuard` in parcelas.js to `return { user }` on the success path (mirror unidades.js line 15), leaving the 401 and 403 branches untouched. Change `marcarParcelaComoPaga` to `const { err, user } = await authGuard()`.

    After the UUID validation and BEFORE the `supabaseAdmin.from('parcelas').update(...)` call, add a 4-hop ownership pre-check, each step a `.single()` fetch returning `{ status: 404, erroMessage: 'Parcela não encontrada.' }` if it errors or returns no row:
    1. `supabaseAdmin.from('parcelas').select('contrato_id').eq('id', id).single()` → contrato_id
    2. `supabaseAdmin.from('contratos').select('unidade_id').eq('id', parcela.contrato_id).single()` → unidade_id
    3. `supabaseAdmin.from('unidades').select('edificio_id').eq('id', contrato.unidade_id).single()` → edificio_id
    4. `supabaseAdmin.from('edificios').select('id').eq('id', unidade.edificio_id).eq('proprietario_id', user.id).single()` → owner verification
    Only after step 4 returns a row, run the existing update (status='paga', data_pagamento, `.in('status', ['pendente','vencida'])`). Keep the update and its error handling unchanged. Use distinct local variable/error names per hop to avoid shadowing, consistent with contratos.js naming.
  </action>
  <verify>
    <automated>grep -q "return { user }" src/actions/parcelas.js && grep -q "eq('proprietario_id', user.id)" src/actions/parcelas.js && npx eslint src/actions/parcelas.js</automated>
  </verify>
  <acceptance_criteria>
    - `authGuard` in parcelas.js returns `{ user }` on success (grep `return { user }` matches).
    - `marcarParcelaComoPaga` destructures `{ err, user }`.
    - A 4-hop chain ending in `.eq('proprietario_id', user.id).single()` exists before the update.
    - Cross-tenant / missing parcela → `{ status: 404, erroMessage: 'Parcela não encontrada.' }` with no update.
    - `npx eslint src/actions/parcelas.js` clean; owner happy path returns `{ status: 200 }`.
  </acceptance_criteria>
  <done>marcarParcelaComoPaga refuses a non-owned parcela with 404 before marking paid; authGuard returns { user }; owner path returns 200; ESLint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated Proprietário → marcarParcelaComoPaga | A logged-in Proprietário submits a parcela `id` belonging to another tenant's contrato. |
| Server Action → supabaseAdmin | service-role client bypasses RLS; before this fix authGuard returned `{}`, so no `user.id` was even available to scope by. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Elevation of Privilege (IDOR) | marcarParcelaComoPaga (parcelas.js) | mitigate | authGuard returns `{ user }`; 4-hop fetch parcela→contrato→unidade→edificio scoped by `.eq('proprietario_id', user.id)` before update; 404 if absent. |
| T-16-02 | Information Disclosure | 404 vs 403 response | accept | Return 404 (not 403) on cross-tenant — matches 15-02 convention; does not confirm existence of another tenant's parcela. |

No package installs in this plan — no T-16-SC supply-chain surface.
</threat_model>

<verification>
- `grep "return { user }" src/actions/parcelas.js` matches; `grep "eq('proprietario_id', user.id)" src/actions/parcelas.js` matches.
- `npx eslint src/actions/parcelas.js` — clean.
- Owner/cross-tenant behavior proven with mocks in Plan 16-03 (Wave 2).
</verification>

<success_criteria>
- authGuard in parcelas.js returns `{ user }`; marcarParcelaComoPaga uses it.
- 4-hop owner-scoped pre-check (`.eq('proprietario_id', user.id)`) runs before the parcela update.
- Cross-tenant → 404 'Parcela não encontrada.'; owner → 200; `{ status, erroMessage }` contract preserved (D-03).
</success_criteria>

<output>
Create `.planning/phases/16-fechamento-idor-mt02/16-02-SUMMARY.md` when done.
</output>
