---
phase: 16-fechamento-idor-mt02
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/unidades.js
  - src/actions/contratos.js
autonomous: true
requirements: [MT-03]
must_haves:
  truths:
    - "criarUnidade rejects (404) an edificio_id not owned by the authenticated Proprietário before any insert"
    - "criarContrato rejects (404) a unidade_id whose edificio is not owned by the authenticated Proprietário before any insert"
    - "editarContrato rejects (404) a contrato not owned by the authenticated Proprietário before any update"
    - "Legitimate owner operations still return { status: 200 } unchanged"
  artifacts:
    - path: "src/actions/unidades.js"
      provides: "criarUnidade ownership pre-check (edificio_id → proprietario_id)"
      contains: "proprietario_id"
    - path: "src/actions/contratos.js"
      provides: "criarContrato + editarContrato ownership pre-checks"
      contains: "proprietario_id"
  key_links:
    - from: "src/actions/unidades.js criarUnidade"
      to: "edificios.proprietario_id"
      via: "supabaseAdmin fetch-then-verify before insert"
      pattern: "proprietario_id.*user\\.id"
    - from: "src/actions/contratos.js criarContrato/editarContrato"
      to: "edificios.proprietario_id"
      via: "fetch chain unidade→edificio (criar) / contrato→unidade→edificio (editar)"
      pattern: "proprietario_id.*user\\.id"
---

<objective>
Close three of the four open IDOR write vectors (MT-03) by adding owner pre-checks that mirror EXACTLY the pattern already shipped in 15-02:
- `criarUnidade` (unidades.js) — verify `form.edificio_id` belongs to the caller BEFORE insert (D-01).
- `criarContrato` (contratos.js) — verify `form.unidade_id` ownership via unidade→edificio→proprietario_id BEFORE insert (D-01).
- `editarContrato` (contratos.js) — 3-hop ownership pre-check (contrato→unidade→edificio→proprietario_id) BEFORE update, identical to `cancelarContrato`/`encerrarContrato` (D-01).

Purpose: `supabaseAdmin` bypasses RLS; these three Actions are guarded only by `authGuard` (auth + is_proprietario), not by tenant — a real IDOR in multi-tenant. authGuard in both files ALREADY returns `{ user }` (15-02), so no authGuard change is needed here.
Output: src/actions/unidades.js and src/actions/contratos.js with owner-scoped pre-checks, preserving the `{ status: 200 }` / `{ status, erroMessage }` contract (D-03), cross-tenant → 404.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-fechamento-idor-mt02/16-CONTEXT.md
@.planning/phases/15-testes/15-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scope criarUnidade by owner (edificio_id → proprietario_id)</name>
  <files>src/actions/unidades.js</files>
  <read_first>
    - src/actions/unidades.js — `criarUnidade` (line 18) is the target; `editarUnidade`/`deletarUnidade` (lines 37–81) already contain the exact 2-step ownership pre-check to mirror. authGuard (line 10) already returns `{ user }`.
    - .planning/phases/15-testes/15-02-SUMMARY.md — fetch-then-verify ownership pattern (404 on cross-tenant).
  </read_first>
  <behavior>
    - Owner case: form.edificio_id belongs to user → insert proceeds → { status: 200 }.
    - Cross-tenant case: edificios ownership query returns no row → { status: 404, erroMessage: 'Edifício não encontrado.' }, insert NOT called.
    - Validation unchanged: missing nome/invalid edificio_id UUID → existing 400s, returned before the ownership fetch.
  </behavior>
  <action>
    In `criarUnidade`, change the authGuard destructure from `const { err } = await authGuard()` to `const { err, user } = await authGuard()`. After the existing form validation (the block ending with the STATUS_UNIDADE check) and BEFORE the `supabaseAdmin.from('unidades').insert(...)` call, add a single ownership pre-check on edificios: fetch `supabaseAdmin.from('edificios').select('id').eq('id', edificio_id).eq('proprietario_id', user.id).single()`. If the query errors or returns no row, return `{ status: 404, erroMessage: 'Edifício não encontrado.' }` before any insert. This is a 1-hop check (form already carries edificio_id) — do not over-build into a join. Mirror the second step of editarUnidade exactly, including the `.eq('proprietario_id', user.id)` filter. Keep the insert and its error handling unchanged.
  </action>
  <verify>
    <automated>grep -n "eq('proprietario_id', user.id)" src/actions/unidades.js | grep -q . && node -e "require('@babel/core')||0" 2>/dev/null; node --check src/actions/unidades.js 2>/dev/null || npx eslint src/actions/unidades.js</automated>
  </verify>
  <acceptance_criteria>
    - `criarUnidade` destructures `{ err, user }` from authGuard.
    - An edificios ownership fetch with `.eq('proprietario_id', user.id).single()` exists between validation and insert.
    - Cross-tenant / missing edificio returns `{ status: 404, erroMessage: 'Edifício não encontrado.' }` with no insert.
    - `npx eslint src/actions/unidades.js` clean; `{ status: 200 }` returned on owner happy path.
  </acceptance_criteria>
  <done>criarUnidade refuses a non-owned edificio_id with 404 before insert; owner insert still returns 200; ESLint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scope criarContrato + editarContrato by owner</name>
  <files>src/actions/contratos.js</files>
  <read_first>
    - src/actions/contratos.js — `criarContrato` (line 19) and `editarContrato` (line 46) are the targets; `cancelarContrato`/`encerrarContrato` (lines 58–141) already contain the exact 3-hop chain (contrato→unidade→edificio→proprietario_id) to mirror for editarContrato. authGuard (line 11) already returns `{ user }`.
    - .planning/phases/15-testes/15-02-SUMMARY.md — 3-hop ownership chain, 404 on cross-tenant.
  </read_first>
  <behavior>
    - criarContrato owner case: unidade_id's edificio belongs to user → insert + unidade status update proceed → { status: 200, data: { id } }.
    - criarContrato cross-tenant: edificios ownership returns no row → { status: 404, erroMessage: 'Unidade não encontrada.' }, contratos insert NOT called.
    - editarContrato owner case: 3-hop chain resolves to an owned edificio → update proceeds → { status: 200 }.
    - editarContrato cross-tenant: edificios ownership returns no row → { status: 404, erroMessage: 'Contrato não encontrado.' }, update NOT called.
    - Existing validation 400s for both Actions returned before ownership fetches.
  </behavior>
  <action>
    Change both `criarContrato` and `editarContrato` authGuard destructures to `const { err, user } = await authGuard()`.

    In `criarContrato`, after the existing form validation (the STATUS_CONTRATO check) and BEFORE the `contratos.insert(...)`, add a 2-hop ownership pre-check on the form's unidade_id: fetch `supabaseAdmin.from('unidades').select('edificio_id').eq('id', unidade_id).single()` (return `{ status: 404, erroMessage: 'Unidade não encontrada.' }` if error or no row), then `supabaseAdmin.from('edificios').select('id').eq('id', unidade.edificio_id).eq('proprietario_id', user.id).single()` (return the same 404 if error or no row). Place both fetches before the insert. Leave the insert, the unidade status='alugada' update, and the return shape `{ status: 200, data: { id } }` unchanged.

    In `editarContrato`, after the UUID and STATUS_CONTRATO validation and BEFORE the `contratos.update(...)`, add the full 3-hop chain exactly as in `cancelarContrato`: fetch contrato.unidade_id by id, then unidade.edificio_id, then edificios filtered by `.eq('proprietario_id', user.id)`. Return `{ status: 404, erroMessage: 'Contrato não encontrado.' }` at any failed hop, before the update. Keep the update and its error handling unchanged.
  </action>
  <verify>
    <automated>test "$(grep -c "eq('proprietario_id', user.id)" src/actions/contratos.js)" -ge 4 && npx eslint src/actions/contratos.js</automated>
  </verify>
  <acceptance_criteria>
    - Both `criarContrato` and `editarContrato` destructure `{ err, user }` from authGuard.
    - `grep -c "eq('proprietario_id', user.id)" src/actions/contratos.js` is >= 4 (criar + editar + the two pre-existing cancelar/encerrar checks).
    - criarContrato cross-tenant → `{ status: 404, erroMessage: 'Unidade não encontrada.' }` with no insert.
    - editarContrato cross-tenant → `{ status: 404, erroMessage: 'Contrato não encontrado.' }` with no update.
    - `npx eslint src/actions/contratos.js` clean; owner happy paths unchanged.
  </acceptance_criteria>
  <done>criarContrato and editarContrato both refuse non-owned targets with 404 before mutating; owner paths unchanged; ESLint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated Proprietário → Server Action | A logged-in Proprietário (passes authGuard) submits an arbitrary `edificio_id`/`unidade_id`/contrato `id` belonging to another tenant. |
| Server Action → supabaseAdmin | service-role client bypasses RLS; nothing but explicit pre-checks scopes writes to the caller's tenant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Elevation of Privilege (IDOR) | criarUnidade (unidades.js) | mitigate | Fetch edificios by `id` AND `.eq('proprietario_id', user.id)` before insert; 404 if absent. |
| T-16-01 | Elevation of Privilege (IDOR) | criarContrato (contratos.js) | mitigate | 2-hop fetch unidade→edificio scoped by `proprietario_id` before insert; 404 if absent. |
| T-16-01 | Elevation of Privilege (IDOR) | editarContrato (contratos.js) | mitigate | 3-hop fetch contrato→unidade→edificio scoped by `proprietario_id` before update; 404 if absent. |
| T-16-02 | Information Disclosure | 404 vs 403 response | accept | Return 404 (not 403) on cross-tenant — matches 15-02 convention, avoids confirming row existence to a non-owner. |

No package installs in this plan — no T-16-SC supply-chain surface.
</threat_model>

<verification>
- `npx eslint src/actions/unidades.js src/actions/contratos.js` — clean.
- `grep -c "eq('proprietario_id', user.id)" src/actions/contratos.js` >= 4; present in unidades.js for criarUnidade.
- Full unit suite gate runs in Plan 16-03 (Wave 2), which proves owner/cross-tenant behavior with mocks.
</verification>

<success_criteria>
- criarUnidade, criarContrato, editarContrato each perform an owner-scoped pre-check (`.eq('proprietario_id', user.id)`) before their mutation.
- Cross-tenant attempts return 404 with the established erroMessage; owner happy paths return 200 unchanged.
- Return contract `{ status, erroMessage }` preserved (D-03).
</success_criteria>

<output>
Create `.planning/phases/16-fechamento-idor-mt02/16-01-SUMMARY.md` when done.
</output>
