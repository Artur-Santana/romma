---
phase: 22
slug: contratos-parcelas-renovacao
status: draft
shadcn_initialized: true
preset: radix-lyra / mauve / remixicon
created: 2026-06-16
---

# Phase 22 — UI Design Contract

> Visual and interaction contract for Phase 22: Contratos & Parcelas — Renovação.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (components.json found — `radix-lyra` style) |
| Preset | `radix-lyra`, base color `mauve`, cssVariables enabled |
| Component library | Radix UI (via shadcn) |
| Icon library | remixicon |
| Font | Space Grotesk (body + mono + display alias) |
| Radius | 0 — sharp corners enforced across all elements |

Source: `components.json` + `npx shadcn info` (verified 2026-06-16).

---

## Spacing Scale

This phase uses the project-wide density token system (`--rd-*`) defined in `src/app/globals.css` lines 396–404. These are **existing design system tokens — not new values introduced by this phase**.

The 8-point scale new values introduced by this phase map to:

| Token | Value | CSS Var | Usage |
|-------|-------|---------|-------|
| xs | 4px | — | Icon gaps, tight inline spacing |
| sm | 8px | — | Compact badge + button padding |
| md | 16px | `--rd-block-sm`, `--rd-row-x` | Default element spacing, row horizontal padding |
| xl | 24px | `--rd-block` | Gap between major blocks (grade-resumo ↔ resumo financeiro ↔ timeline) |
| 2xl | 32px | `--rd-gutter` | Desktop horizontal gutter |

**Inherited design system tokens (not new introductions):**

The following token values do not fall on the 8-point grid but are **pre-existing CSS variables defined in `src/app/globals.css` lines 396–404**. This phase inherits and uses them as-is — they are not being specified or introduced here:

| CSS Var | Value | Defined In | Usage in this phase |
|---------|-------|------------|---------------------|
| `--rd-panel` | 20px | globals.css:401 (existing DS token) | Panel padding — timeline container, arquivo section |
| `--rd-cell` | 20px | globals.css:402 (existing DS token) | Cell padding — grade-resumo cells, resumo financeiro cells |
| `--rd-gutter-m` | 20px | globals.css:397 (existing DS token) | Mobile horizontal gutter |
| `--rd-page-y` | 28px | globals.css:398 (existing DS token) | Page top padding |
| `--rd-row-y` | 12px | globals.css:403 (existing DS token) | Row vertical padding — arquivo rows, timeline rows |

These token values were established in a prior phase (v1.5 design refinement). The checker should treat all `--rd-*` references as passthrough to existing globals, not as new spacing decisions.

Additional exceptions within this phase (fixed pixel values, not new tokens):

- Card grid gap: 12px (matches `console3.jsx:96` — between `sm` and `md`, design canonical value; existing pattern)
- Timeline connector line: `minHeight: 28px` (matches connector height between parcela items)
- Progress bar height (contract): 4px
- Progress bar cell height (parcelas): 6px — creates half-step between the 11px label visual context and the standard 16px row padding; maintains visual rhythm at sub-unit scale where full 8px increments would produce oversized cells relative to 6px height
- Timeline dot: 12px × 12px square (never circle — Obsidian Blueprint rule)
- Touch targets for "✓ Registrar" ghost button: `padding: 5px 9px` — minimum asymmetric pad for a tertiary text-only action; this is not a primary CTA, so the standard 8px/16px padding would visually over-emphasize it relative to its tertiary role in the timeline

Source: RESEARCH.md Standard Stack / Tokens CSS section + globals.css lines 396–404 (VERIFIED).

---

## Typography

All sizes are declared via `--rt-*` tokens in `src/app/globals.css` lines 385–393. This phase uses exactly **4 type sizes** covering 3 distinct information layers (financial emphasis, section structure, and tabular/body data) across 2 screens.

| Role | CSS Class | Size | Family | Weight | Line Height | Usage |
|------|-----------|------|--------|--------|-------------|-------|
| Financial value / emphasis | `.r-section` + inline `font-size: 24px` | 24px | Space Grotesk (display) | 700 | 1.05 | Resumo financeiro values (totalContrato, totalPago, Em aberto, Inadimplência) |
| Subhead / section heading | `.r-subhead` | 16px (`--rt-subhead`) | Space Grotesk (body) | 600 | 1.2 | Locatário name on card, "Parcela NN" label in timeline, grade-resumo values, "Cronograma de Parcelas" section heading |
| Body / data | `.r-body` / `.r-data` | 14px (`--rt-body` / `--rt-data`) | Space Grotesk (body/mono) | 400 | 1.5 / — | Modal body text, dates, countdown text, daysLeft, modal preview "Novo término", data cells in timeline (.r-meta row) |
| Label / meta | `.r-label` / `.r-meta` | 11px (`--rt-label`) | Space Grotesk (mono) | 700 / 400 | — | Column headers (grade-resumo, resumo financeiro), "RENOVAÇÃO" eyebrow, IDs, sub-captions, "N resultado(s)", "Venc · data", "Pago · data" in timeline |

**Consolidation rationale:** 20px (previously used for grade-resumo values) is replaced by 16px `.r-subhead` — the same token used for similar section-level values in prior phases (console3.jsx pattern). 10px (previously `.r-meta`) is absorbed into 11px `.r-label`/`.r-meta` tokens; the visual difference at these sizes is imperceptible in this dark-surface context and consolidation preserves the DS token boundary.

Declared font weights for this phase: **400 (regular)** and **600/700 (semibold/bold)**. No other weights permitted.

Source: globals.css lines 419–431 (VERIFIED). RESEARCH.md Standard Stack / Classes de utilidade (VERIFIED).

---

## Color

The project is dark-only. All values are OKLCH. Source: `src/app/globals.css` (VERIFIED 2026-06-16).

| Role | Token | Approx. Value | Usage |
|------|-------|---------------|-------|
| Dominant (60%) | `--background` / `--ds-background` | oklch(0.239 0 0) — near-black | Page background |
| Secondary (30%) | `--surface` | oklch(0.182 0 0) — dark surface | Card backgrounds, grade-resumo cells, r-panel, arquivo list |
| Secondary elevated | `--surface-hi` | oklch(0.218 0 0) | Progress bar empty segments, row hover state, section headers |
| Accent (10%) | `--indigo` / `--primary` | oklch(0.54 0.178 301.8) — indigo | Modal header eyebrow only, active toggle "Vencendo" border |

Accent reserved for: modal "RENOVAÇÃO" eyebrow label ONLY, and the "Vencendo" toggle button active border. No other elements use `--indigo`.

Semantic colors (used for status signaling, not accent):

| Token | Approx. Value | Reserved For |
|-------|---------------|-------------|
| `--warning` | oklch(0.769 0.165 70.08) — amber | Card border when `isExpiring`, countdown text when ≤7 days, progress bar fill when expiring, "Vencendo" toggle badge color, parcela status "pendente" |
| `--warning-bg` | oklch(0.21 0.02 70) | Background of active "Vencendo" toggle button |
| `--success` | oklch(0.696 0.17 162.5) — green | "Total recebido" value + label, parcela status "paga" (dot + progress cell), "✓ Registrar" button text + border |
| `--highlight` | oklch(0.7245 0.0998 82.35) — gold | "Em aberto" value + label only |
| `--danger-fg` | oklch(0.826 0.073 22.5) — light red text | "Inadimplência" value + label when vencidas > 0, vencimento date text for overdue parcelas |
| `--danger-bg2` | oklch(0.27 0.13 27.38) — dark red bg | "Inadimplência" cell background when vencidas > 0 |
| `--border-3` | oklch(0.387 0 0 / 0.4) — subtle border | Card borders (non-expiring), grade-resumo cell borders, timeline connector line, r-panel border |
| `--fg-5` | (bg + 0.148) — very dim | Timeline dot border for "futura" parcelas |
| `--fg-3` | (bg + 0.45) | Body text, r-body, "Em aberto" subtext |
| `--fg-4` | (bg + 0.317) | r-label, r-meta, r-eyebrow default |
| `--primary-hover` | oklch(from primary l+0.25 c h) | Progress bar fill (contract) when not expiring |

Source: CONTEXT.md decisions D-03, D-04, D-08, D-09, D-11, D-12 + RESEARCH.md Token table (VERIFIED against globals.css).

---

## Component Inventory

All components are already installed. No new shadcn add commands required.

| Component | Source | Usage in Phase 22 |
|-----------|--------|-------------------|
| `Button` (shadcn) | `@/components/ui/button` | "Renovar" primary button in Parcelas header, modal "Confirmar" button |
| `Input` (shadcn) | `@/components/ui/input` | Custom months field in renewal modal (`type="number"`, `min=1`, `max=36`) |
| `ConfirmDialog` (local) | existing in Contratos.js | Cancelar/encerrar confirmation (reuse, no change) |
| `StatusBadge` (local) | existing, supports "vencendo" | All contract and parcela status badges |
| `PageHeader` (local) | existing in Contratos.js | Header for both screens (reuse) |
| `toast` (sonner) | already integrated | Success/error feedback on all mutations |
| `.r-panel` class | globals.css | Timeline container, arquivo section container |
| `.r-rowlink` class | globals.css | Archive rows, mobile contract rows |
| `.r-ghostbtn` class | globals.css | "Ver →" links, "✓ Registrar" button |
| `.r-fade` class | globals.css | Card entrance animation (apply with `animationDelay: i * 30ms`) |

Source: CONTEXT.md code_context section + RESEARCH.md Don't Hand-Roll table (VERIFIED).

---

## Layout Contract

### Contratos.js — Desktop (≥sm breakpoint)

```
romma-page wrapper (rFade entrance)
  PageHeader ("Contratos")
  ├─ Search bar + "Vencendo · N" toggle button  (above grid)
  ├─ "N resultado(s)" meta line  (shown when filter active)
  └─ Cards grid: display:grid; grid-template-columns: repeat(auto-fill, minmax(330px, 1fr)); gap: 12px
       Card (per contract):
         ├─ Top row: StatusBadge + "X dias" countdown  (right-aligned)
         ├─ Locatário nome  (.r-subhead, 16px)
         ├─ Unidade + Edifício abreviado  (.r-meta, 11px)
         ├─ Progress bar: 4px height, --surface-hi bg, --primary-hover (or --warning) fill
         ├─ Dates: data_inicio → data_fim  (.r-data, 14px)
         └─ Bottom row: fmtBRL(valor_mensal) | "Ver →" | "Cancelar"
       Card border: --warning when isExpiring, --border-3 otherwise
       Card bg: --surface; padding: var(--rd-panel)  [existing DS token: 20px]
  └─ Arquivo callout button  (below grid; text: "Ver Arquivo (N) →" / "⌃ Ocultar Arquivo")
       Expanded arquivo: .r-panel list
         Row per contract: ID · locatário · unidade+edifício+datas · badge · "Ver →"
         Opacity: 0.78 on each row
```

### Contratos.js — Mobile (<sm breakpoint)

```
romma-page wrapper
  PageHeader
  ├─ Search bar + "Vencendo · N" toggle  (stacked, full-width)
  └─ .r-panel list (rows, no cards)
       Row: locatário + unidade/datas abreviadas + badge (clicável → detalhe)
  └─ Arquivo callout + expanded rows
```

### Parcelas.js — Desktop + Mobile

```
romma-page wrapper
  PageHeader (contrato info) + "Renovar" button (ao lado do badge) + StatusBadge
  ├─ Grade-resumo: 5 colunas desktop / 2×3 grid mobile
  │    Cells: Unidade | Edifício | Valor mensal | Início | Término
  │    Border: --border-3 on grid container + between cells
  │    Cell bg: --surface; padding: var(--rd-cell)  [existing DS token: 20px]
  │    Label: .r-label (11px, uppercase); Value: .r-subhead (16px, 600)
  ├─ Resumo financeiro: 4 colunas (2×2 em mobile)
  │    Cells: Valor do contrato | Total recebido | Em aberto | Inadimplência
  │    Inadimplência cell: bg --danger-bg2 + text --danger-fg when vencidas > 0
  │    Value: font-display 700 24px; Label: .r-label (11px); Sub: .r-meta (11px, 400)
  ├─ Section "Cronograma de Parcelas" (.r-subhead, 16px)
  ├─ Progress bar segmentada: N células × 6px height, gap 3px; cor por status
  └─ Timeline vertical: .r-panel padding var(--rd-panel)  [existing DS token: 20px]
       Per parcela:
         ├─ Left col: 12×12 square dot (color by status; transparent + fg-5 border if futura)
         │            + vertical connector 1px × minHeight 28px (--border-3) until last item
         └─ Right col: "Parcela NN" (.r-subhead, 16px) + StatusBadge + "✓ Registrar" ghost btn
                       .r-meta row (11px): "Venc · data" | "Pago · data_ou_—" | fmtBRL(valor_mensal)
```

### Modal de Renovação

```
Dialog (shadcn, position fixed inset-0 mobile)
  Header: .r-eyebrow.indigo "RENOVAÇÃO"  + título "Renovar Contrato"
  Body:
    "Término atual: DD/MM/AAAA"  (.r-body, 14px)
    Grid 3 colunas: [+6 meses] [+12 meses] [+24 meses]  (Button variant=outline)
    Input personalizado: Input[type=number min=1 max=36]  (label: "Meses personalizados")
    Preview: "Novo término: DD/MM/AAAA"  (.r-data, 14px, cor --highlight)
  Footer:
    [Cancelar]  (Button variant=ghost)
    [Confirmar]  (Button variant=default, loading state during SA call)
```

Source: CONTEXT.md decisions D-01, D-02, D-08, D-09, D-11, D-12, D-14, D-15 + console3.jsx references.

---

## Interaction Contract

| Interaction | Trigger | Response | State Change |
|-------------|---------|----------|-------------|
| Search contracts | type in search input | Filter cards/rows client-side via `nameOf()` | `q` state |
| Toggle "Vencendo" filter | click toggle button | Show only contracts with `isExpiring=true`; button gets `--warning-bg` bg | `onlyVencendo` state |
| Clear filter | click toggle again or clear search | Show all ativos | reset filter state |
| Open arquivo | click "Ver Arquivo →" callout | Expand section below; button text changes | `showArquivo: true` |
| Close arquivo | click "⌃ Ocultar Arquivo" | Collapse section | `showArquivo: false` |
| Card entrance | on mount (list loaded) | `.r-fade` animation with `animationDelay: i * 30ms` per card | — |
| Card cancel | click "Cancelar" | Open ConfirmDialog → on confirm: fade-out animation 220ms → SA `cancelarContrato` → re-fetch | `removingIds` Set |
| Open renewal modal | click "Renovar" in Parcelas header | Open Dialog `showRenew: true` | modal state |
| Select quick months | click +6/+12/+24 | Highlight selected; update preview "Novo término: DD/MM/AAAA"; clear custom input | `renewMonths` state |
| Custom months input | type number 1–36 | Update preview; deselect quick buttons | `renewMonths` state |
| Confirm renewal | click "Confirmar" | Button shows loading state; close modal only after SA 200; toast; re-fetch | async flow |
| Register payment | click "✓ Registrar" on parcela | SA `marcarParcelaComoPaga`; re-fetch parcelas; resumo financeiro recalculates via React state | `parcelas` state |
| Payment toast | on SA 200 | `toast.success("Pagamento registrado · DD/MM/AAAA")` | — |
| Renewal toast | on SA 200 | `toast.success("Contrato renovado até DD/MM/AAAA")` | — |
| Error | on SA non-200 | `toast.error(result.erroMessage)` — modal stays open | — |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — renewal | "Renovar" |
| Renewal modal title | "Renovar Contrato" |
| Renewal modal eyebrow | "RENOVAÇÃO" |
| Renewal modal current term | "Término atual: {DD/MM/AAAA}" |
| Renewal modal preview | "Novo término: {DD/MM/AAAA}" |
| Renewal confirm button | "Confirmar" |
| Renewal confirm loading | "Confirmando…" |
| Renewal success toast | "Contrato renovado até {DD/MM/AAAA}" |
| Payment register button | "✓ Registrar" |
| Payment success toast | "Pagamento registrado · {DD/MM/AAAA}" |
| Filter toggle label | "Vencendo · {N}" |
| Filter results meta | "{N} resultado(s)" |
| Arquivo closed callout | "Ver Arquivo ({N}) →" |
| Arquivo open callout | "⌃ Ocultar Arquivo" |
| Empty state — no contracts | "Nenhum contrato encontrado." |
| Empty state — no results after filter | "Nenhum contrato corresponde à busca." |
| Empty state — no parcelas | "Nenhuma parcela encontrada." |
| Error — generic SA failure | "{erroMessage da SA}" (surfaced via toast.error) |
| Cancelar contract confirmation | "Esta ação encerrará o contrato. O histórico será preservado no arquivo." |
| Cancelar confirm button | "Cancelar Contrato" |
| Countdown — singular | "1 dia" |
| Countdown — plural | "{N} dias" |
| Countdown — expired | "Encerrado" |
| Parcela number format | "Parcela {00}" (zero-padded 2 digits) |
| Parcela date — not paid | "Pago · —" |
| Grade-resumo labels | "Unidade" · "Edifício" · "Valor mensal" · "Início" · "Término" |
| Resumo financeiro labels | "Valor do contrato" · "Total recebido" · "Em aberto" · "Inadimplência" |
| Resumo sub-captions | "{N} parcelas" · "{N} pagas" · "{N} parcelas" · "{N} vencida(s)" |
| Section heading | "Cronograma de Parcelas" |
| Progress bar label | "{N}/{total} pagas" (right-aligned above bar) |

Source: CONTEXT.md specifics section + console3.jsx lines 260–387 + established project terminology (CLAUDE.md).

Note: All labels use Portuguese. "Cancelar" for the destructive action (maps to SA that changes status to `cancelado`). No synonym substitution.

---

## Motion Contract

| Animation | Keyframe | Duration | Easing | Trigger |
|-----------|----------|----------|--------|---------|
| Card entrance | `rFade` (translateY 8px → 0) | `--dur-base` (220ms) | `--ease-crisp` | On list mount, staggered `i * 30ms` delay |
| Card removal (cancel) | opacity 0 + scale(0.98) | 220ms | ease | `removingIds.has(c.id)` |
| Row hover | background transition | `--dur-fast` (120ms) | `--ease-crisp` | hover on `.r-rowlink` |
| Ghost button hover | color + border-color | `--dur-fast` (120ms) | — | hover on `.r-ghostbtn` |
| Page entrance | `rFade` via `.romma-page` | 320ms | `--ease-crisp` | Page mount |
| Reduced motion | animation: none | — | — | `@media (prefers-reduced-motion: reduce)` |
| Print | animation: none | — | — | `@media print` |

Source: globals.css lines 458–469 (VERIFIED). CONTEXT.md discretion — "apply rFade if already in globals.css" → confirmed present.

---

## Form State Contract

Modal de renovação state object (single useState, not per-field):

```javascript
const [renew, setRenew] = useState({ meses: 0, custom: '' })
```

- `meses`: selected quick option (6, 12, or 24) or parsed value from custom field
- `custom`: raw string from Input (allows empty while typing)
- Preview calculated inline: `previewNovoTermino(renew.meses)` — pure function, no extra state
- Loading state: `const [renovando, setRenovando] = useState(false)` — separate boolean for button state

Source: RESEARCH.md Anti-Patterns + CLAUDE.md form state convention.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Input (already installed) | not required |
| Third-party | none | not applicable |

No third-party registries declared for this phase. Vetting gate: not required.

Source: RESEARCH.md "Sem instalação de pacotes" + components.json `"registries": {}`.

---

## Accessibility Notes

- All interactive elements must have accessible labels. "✓ Registrar" ghost button: `aria-label="Registrar pagamento da parcela {numero}"`.
- ConfirmDialog already handles focus trap and Esc key (shadcn Dialog).
- Countdown text: use `<span>` with visible text — no icon-only countdown.
- Progress bar (contract): add `role="progressbar"` with `aria-valuenow={pctElapsed}` `aria-valuemin={0}` `aria-valuemax={100}`.
- Progress cells (parcelas): decorative — `aria-hidden="true"`, status communicated via timeline text.
- Color is never the sole differentiator — status is always communicated via both color AND text (StatusBadge label).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
