---
phase: 22-contratos-parcelas-renova-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/Contratos.js
autonomous: true
requirements: [CONTR-01, CONTR-02, CONTR-03, CONTR-04, CONTR-05]
must_haves:
  truths:
    - "Proprietário vê contratos ativos como cards no desktop (grid auto-fill 330px) per D-01"
    - "Cada card mostra countdown de dias e barra de progresso início→término per D-02, D-03, D-04"
    - "Busca por locatário/unidade filtra cards client-side per D-05"
    - "Toggle 'Vencendo · N' filtra apenas contratos expirando per D-05"
    - "Mobile mostra lista de rows clicáveis (sem cards) per D-02"
    - "Toggle inline 'Ver Arquivo' expande lista de encerrados+cancelados (opacidade 0.78) per D-06, D-07"
    - "Formulário de novo contrato continua mostrando valor da unidade per CONTR-03 (preservado)"
  artifacts:
    - path: "src/components/features/Contratos.js"
      provides: "Cards desktop + busca + filtro vencendo + countdown + progresso + arquivo toggle"
      contains: "repeat(auto-fill, minmax(330px, 1fr))"
  key_links:
    - from: "src/components/features/Contratos.js"
      to: "getContratos join (locatarios + unidades)"
      via: "nameOf(c) busca client-side"
      pattern: "nameOf"
    - from: "search/filter state (q, onlyVencendo, showArquivo)"
      to: "view derivada"
      via: "filter sobre contratosAtivos"
      pattern: "onlyVencendo"
---

<objective>
Redesenhar `src/components/features/Contratos.js` da tabela atual para o layout variante B do design: cards no desktop, busca inline, filtro "Vencendo", countdown de dias, barra de progresso início→término, e arquivo toggleável de encerrados. Mobile mantém lista de rows clicáveis. Cobre CONTR-01 a CONTR-05.

Purpose: Entregar o "board de cards" do ROADMAP — o Proprietário busca/filtra contratos, vê o status temporal (countdown + progresso) e acessa o histórico de encerrados sem sair da tela.
Output: `Contratos.js` reescrito com cards desktop, rows mobile, busca/filtro client-side e arquivo inline. Formulário de novo contrato (CONTR-03) preservado intacto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-contratos-parcelas-renova-o/22-CONTEXT.md
@.planning/phases/22-contratos-parcelas-renova-o/22-PATTERNS.md
@.planning/phases/22-contratos-parcelas-renova-o/22-UI-SPEC.md
@.planning/phases/22-contratos-parcelas-renova-o/22-VALIDATION.md
@src/components/features/Contratos.js
@.planning/design/js/console3.jsx

# Design canônico: console3.jsx linhas 29-218 (ContratosScreen variante B).
# Tokens de produção em src/app/globals.css (--warning, --border-3, --surface, --surface-hi, --primary-hover, --warning-bg, --indigo, --rd-panel, --dur-base, --ease-crisp).
</context>

<critical_facts>
- `getContratos()` (queries-client.js:21) retorna `locatarios(nome_razao_social)` + `unidades(nome)` no join — MAS NÃO retorna `valor_mensal` nem `edificio_id`.
- Portanto, no card, derive `valor_mensal` e `edificio` casando `contrato.unidade_id` com o array `unidades` (já buscado via `getUnidades()`) e `edificios` (já buscado via `getEdificios()`). Padrão idêntico ao código atual em Contratos.js:340-341.
- `isExpiring(c)` já existe (Contratos.js:41-45). Reusar. Ao redesenhar, corrigir para usar `new Date(c.data_fim + 'T12:00:00')` (Pitfall 4) — evita erro de 1 dia por UTC shift.
- `daysLeft(c) = Math.ceil((new Date(c.data_fim + 'T12:00:00') - new Date()) / 86400000)`.
- `pctElapsed(c) = Math.max(4, Math.min(100, Math.round(((n - a) / (b - a)) * 100)))` com a/b/n parseados com T12:00:00.
- `nameOf(c) = (c.locatarios?.nome_razao_social ?? "") + " " + (c.unidades?.nome ?? "")`.
- `arquivo = contratos.filter(c => c.status !== "ativo")` inclui encerrado + cancelado (D-07).
- Tokens: `--indigo` SÓ no border ativo do toggle "Vencendo" (e na callout aberta do arquivo). Font weights só 400 e 700 (sem 600). Ponto/quadrado: bordas sharp (radius 0).
- NÃO usar Tailwind para grid/borders de layout — usar inline styles + CSS vars. Tailwind apenas para spacing/typography utilities.
- Preservar TODO o formulário de novo contrato (Contratos.js:207-317), `ConfirmDialog`, `askCancelar`/`askEncerrar`/`confirmarCancelamento`/`confirmarEncerramento`, `removingIds`, `SkeletonContratos`, `PageHeader`.
</critical_facts>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar state, helpers e listas derivadas de busca/filtro/arquivo</name>
  <files>src/components/features/Contratos.js</files>
  <action>
    Implementar a camada de estado e derivações para CONTR-01, CONTR-02, CONTR-04 sem ainda mudar o render principal.
    1. No import de `@/lib/utils`, adicionar `fmtBRL` (já exportado de utils.js): `import { fmtData, fmtBRL, cn } from "@/lib/utils"`.
    2. Adicionar três novos state vars junto aos existentes (após linha 71): `const [q, setQ] = useState("")`, `const [onlyVencendo, setOnlyVencendo] = useState(false)`, `const [showArquivo, setShowArquivo] = useState(false)`.
    3. Corrigir `isExpiring` (linha 43) para parsear `new Date(c.data_fim + 'T12:00:00')` — evita UTC shift (Pitfall 4). Manter a lógica `diff >= 0 && diff <= 7`.
    4. Adicionar helpers no escopo do módulo (junto a `isExpiring`): `daysLeft(c) = Math.ceil((new Date(c.data_fim + 'T12:00:00') - new Date()) / 86400000)`; `pctElapsed(c)` com `a = new Date(c.data_inicio + 'T12:00:00')`, `b = new Date(c.data_fim + 'T12:00:00')`, `n = new Date()`, retornando `Math.max(4, Math.min(100, Math.round(((n - a) / (b - a)) * 100)))`; `nameOf(c) = (c.locatarios?.nome_razao_social ?? "") + " " + (c.unidades?.nome ?? "")`.
    5. No corpo do componente, junto a `contratosAtivos` (linha 182), adicionar derivações: `const vencendoCount = contratosAtivos.filter(isExpiring).length`; `const arquivo = contratos.filter(c => c.status !== "ativo")` (D-07: encerrado + cancelado); `const view = contratosAtivos.filter(c => { if (onlyVencendo && !isExpiring(c)) return false; if (q && !nameOf(c).toLowerCase().includes(q.toLowerCase())) return false; return true })`.
    Não remover ainda a tabela existente nesta task — apenas adicionar a infraestrutura. O build deve continuar passando.
  </action>
  <verify>
    <automated>npx next build --no-lint 2>&1 | grep -qiE "Compiled|Generating static" && echo BUILD_OK</automated>
  </verify>
  <done>State `q`/`onlyVencendo`/`showArquivo` adicionados; helpers `daysLeft`/`pctElapsed`/`nameOf` definidos com T12:00:00; `isExpiring` corrigido; `view`/`arquivo`/`vencendoCount` derivados. Build passa.</done>
</task>

<task type="auto">
  <name>Task 2: Substituir tabela por cards desktop + rows mobile + barra de busca/filtro</name>
  <files>src/components/features/Contratos.js</files>
  <action>
    Substituir o bloco da tabela (Contratos.js:319-431, o `<div style={{ overflowX: "auto" }}>` envolvendo a grid `COL_STYLE`) por: (a) barra de busca + toggle Vencendo, (b) cards grid desktop, (c) rows mobile. Remover as constantes `COL`/`COL_STYLE`/`HeaderCell` que se tornam órfãs.
    Seguir EXATAMENTE os snippets de PATTERNS.md (Contratos.js section) e a layout/copy contract de UI-SPEC.md.
    (a) BARRA (acima dos cards): `<Input>` (q) com placeholder "Buscar por locatário ou unidade...", classes `bg-surface-hi border-border-3 text-fg-1 font-body text-[13px] rounded-none pl-8`. Toggle button (onlyVencendo) com inline style: border `var(--warning)` quando ativo senão `var(--border-3)`, background `var(--warning-bg)` quando ativo senão transparent, cor `var(--warning)`/`var(--fg-4)`, texto "Vencendo · {vencendoCount}". Quando `q || onlyVencendo`, mostrar `<span>` "{view.length} resultado(s)" (font-mono 11px text-fg-4).
    (b) CARDS DESKTOP: `<div className="romma-desktop-only" style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(330px, 1fr))", gap: 12 }}>`. Empty state quando `view.length === 0`: "Nenhum contrato corresponde à busca." (gridColumn "1 / -1"). Por card (map sobre `view`): derivar `const uni = unidades.find(u => u.id === contrato.unidade_id) ?? contrato.unidades`, `const edi = edificios.find(e => e.id === uni?.edificio_id)`, `const exp = isExpiring(contrato)`, `const pct = pctElapsed(contrato)`, `const isRemoving = removingIds.has(contrato.id)`. Card div: border `1px solid ${exp ? "var(--warning)" : "var(--border-3)"}`, background `var(--surface)`, padding `var(--rd-panel)`, flex column gap 14, opacity/transform de remoção (220ms). Animação de entrada: `animation: "rFade var(--dur-base) var(--ease-crisp) both"`, `animationDelay: \`${i * 30}ms\`` (D-C / Motion Contract). Conteúdo: topo = StatusBadge (`status={exp ? "vencendo" : contrato.status}`) + countdown alinhado à direita "{daysLeft} dias → {fmtData(data_fim)}" cor `exp ? var(--warning) : var(--fg-4)` (singular "1 dia"; se daysLeft < 0 mostrar "Encerrado" per Copywriting Contract); nome locatário `.r-subhead` (16px, peso 700); unidade + edifício abreviado `edi.nome.replace(/Edifício\s*/i, "")` em font-mono 11px text-fg-4; barra de progresso (height 4px, bg `var(--surface-hi)`, fill width `${pct}%` bg `exp ? var(--warning) : var(--primary-hover)`, com `role="progressbar" aria-valuenow={pct} aria-valuemin={0} aria-valuemax={100}`); datas `fmtData(data_inicio)` → `fmtData(data_fim)` font-mono 14px; rodapé = `fmtBRL(uni?.valor_mensal)` + botão "Ver →" (router.push detalhe) + "Cancelar"/"Encerrar" (reusar `askCancelar`/`askEncerrar` conforme `vencido` — manter a lógica `vencido = data_fim < getTodayLocal()` existente).
    (c) ROWS MOBILE: `<div className="romma-mobile-only border border-border-3 bg-surface mb-8">` mapeando `view`; cada row clicável (router.push detalhe), borderTop quando i>0, mostrando nome locatário + StatusBadge + "{uni?.nome} · {fmtData(inicio)}→{fmtData(fim)}".
    Preservar o callout de arquivo existente (linhas 433-445) sem alterar nesta task (será na Task 3).
  </action>
  <verify>
    <automated>npx next build --no-lint 2>&1 | grep -qiE "Compiled|Generating static" && echo BUILD_OK</automated>
  </verify>
  <done>Tabela removida; barra de busca + toggle Vencendo + cards grid desktop (330px auto-fill) + rows mobile renderizam. `COL`/`COL_STYLE`/`HeaderCell` removidos. Cards mostram countdown, progresso, valor, ações. Build passa.</done>
</task>

<task type="auto">
  <name>Task 3: Ativar arquivo de encerrados (toggle inline)</name>
  <files>src/components/features/Contratos.js</files>
  <action>
    Converter o callout desabilitado de arquivo (Contratos.js:434-445) em toggle funcional (D-06, D-B) e adicionar a lista expansível abaixo dele. Seguir o snippet "Archive toggle" de PATTERNS.md.
    1. Substituir o `<Button disabled ...>` por `<button onClick={() => setShowArquivo(v => !v)}>` com inline style (all:unset, cursor pointer, font-mono 11px peso 700, letterSpacing 1px uppercase, cor `showArquivo ? "var(--indigo)" : "var(--fg-3)"`). Texto dinâmico: `showArquivo ? "⌃ Ocultar Arquivo" : \`Ver Arquivo (${arquivo.length}) →\``.
    2. Abaixo do callout, render condicional `{showArquivo && (...)}`: eyebrow "Arquivo · Contratos Encerrados" (classe `eyebrow eyebrow--indigo`), depois `<div className="border border-border-3 bg-surface">` mapeando `arquivo`. Cada row: derivar `loc`/`uni`/`edi` (mesmo padrão dos cards), `borderTop` quando i>0, `opacity: 0.78`. Esquerda: ID arquivado `ARQ_{String(i+1).padStart(3,"0")}` (font-mono) + nome locatário + "unidade · edifício · datas". Direita: `<StatusBadge status={c.status} />` + botão "Ver →" (router.push detalhe).
    3. Quando `arquivo.length === 0`, mostrar dentro do bloco expandido "Nenhum contrato arquivado." (font-mono 12px text-fg-4, py-10 text-center).
  </action>
  <verify>
    <automated>npx next build --no-lint 2>&1 | grep -qiE "Compiled|Generating static" && echo BUILD_OK</automated>
  </verify>
  <done>Callout vira botão funcional com texto dinâmico; clicar alterna `showArquivo`; lista de encerrados+cancelados expande com opacidade 0.78, ID arquivado, badge e "Ver →". Build passa.</done>
</task>

</tasks>

<verification>
- `npx next build --no-lint` → zero erros após cada task.
- Manual (após wave, browser): tela Contratos abre → cards visíveis no desktop; busca filtra por locatário/unidade; toggle "Vencendo" filtra apenas expirando; arquivo expande ao clicar; formulário de novo contrato continua mostrando valor da unidade selecionada.
- Conferir contra `.planning/design/screenshots/desktop/07-contratos.png` e `mobile/07-contratos.png`.
</verification>

<success_criteria>
- CONTR-01: Busca client-side + toggle "Vencendo · N" funcionais sobre `contratosAtivos`.
- CONTR-02: Cada card mostra `daysLeft` countdown + barra `pctElapsed` (4%–100%).
- CONTR-03: Formulário de novo contrato preservado, valor da unidade ainda visível.
- CONTR-04: Cancelar/encerrar continuam exigindo ConfirmDialog e preservam no arquivo.
- CONTR-05: Toggle inline abre arquivo de encerrados+cancelados com contagem.
- Desktop = cards (330px auto-fill); mobile = rows clicáveis.
- Datas parseadas com T12:00:00 (sem erro de 1 dia).
</success_criteria>

<output>
Create `.planning/phases/22-contratos-parcelas-renova-o/22-01-SUMMARY.md` when done.
</output>
