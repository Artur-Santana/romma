---
phase: 22-contratos-parcelas-renova-o
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/components/features/Parcelas.js
autonomous: true
requirements: [PARC-01, PARC-02, PARC-03]
must_haves:
  truths:
    - "Detalhe do contrato exibe grade-resumo (5 cols desktop / 2×3 mobile) per D-08"
    - "Resumo financeiro (4 cols) mostra valor do contrato, recebido, em aberto, inadimplência per D-09, D-10"
    - "Inadimplência fica vermelha quando há parcelas vencidas per D-09"
    - "Parcelas renderizam em timeline vertical (ponto quadrado 12×12 + linha) per D-12"
    - "Barra de progresso segmentada (1 célula = 1 parcela) colorida por status per D-11"
    - "Registrar pagamento atualiza resumo financeiro ao vivo + toast per D-13"
  artifacts:
    - path: "src/components/features/Parcelas.js"
      provides: "Grade-resumo + resumo financeiro + barra progresso + timeline vertical + registrar pagamento"
      contains: "Cronograma de Parcelas"
  key_links:
    - from: "parcelas state"
      to: "resumo financeiro derivado"
      via: "parcelas.filter(status) × unidade.valor_mensal recalculado no render"
      pattern: "valor_mensal"
    - from: "marcarComoPaga"
      to: "setParcelas(re-fetch)"
      via: "React state recalcula resumo automaticamente"
      pattern: "getParcelasByContrato"
---

<objective>
Redesenhar `src/components/features/Parcelas.js` da tabela atual para o layout variante B: grade-resumo (5 colunas), resumo financeiro derivado (4 colunas), barra de progresso segmentada e timeline vertical de parcelas, mantendo o registrar pagamento com atualização ao vivo do resumo. Cobre PARC-01, PARC-02, PARC-03.

Purpose: Transformar a tela de detalhe num painel financeiro narrativo — o Proprietário lê o estado do contrato (valor, recebido, em aberto, inadimplência) e a posição temporal das parcelas de relance, e registra pagamentos com feedback imediato.
Output: `Parcelas.js` reescrito com grade-resumo, resumo financeiro recalculado por React state, barra de células e timeline vertical. Botão "Renovar" e modal serão adicionados no Plan 03 (Wave 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-contratos-parcelas-renova-o/22-CONTEXT.md
@.planning/phases/22-contratos-parcelas-renova-o/22-PATTERNS.md
@.planning/phases/22-contratos-parcelas-renova-o/22-UI-SPEC.md
@.planning/phases/22-contratos-parcelas-renova-o/22-VALIDATION.md
@src/components/features/Parcelas.js
@.planning/design/js/console3.jsx

# Design canônico: console3.jsx linhas 220-387 (ContratoDetailScreen variante B).
# Tokens: --success, --danger-fg, --danger-bg2, --warning, --highlight, --surface, --surface-hi, --border-3, --fg-5, --rd-cell, --rd-panel, --rd-block.
</context>

<critical_facts>
- `getParcelasByContrato()` retorna `id, numero, data_fechamento, data_vencimento, data_pagamento, status` — NÃO tem coluna de valor.
- D-10: valor de cada parcela = `unidade.valor_mensal` (já no state `unidade` carregado em `carregar()`). Todos os cálculos financeiros usam esse valor derivado.
- `carregar()` (Parcelas.js:31-48) já busca parcelas + contrato + locatario + unidade. NÃO mudar a busca. Mas `getUnidades()` retorna `valor_mensal`? Verificar: a query `getUnidades` deve trazer `valor_mensal` e `edificio_id`. Se `unidade.edificios?.nome` não existir no objeto, derivar o edifício adicionando `getEdificios()` ao `Promise.all` do `carregar()` e casando `unidade.edificio_id`. CONFIRMAR ao implementar — se `unidade` não tiver `valor_mensal`, ajustar `carregar()` para garantir.
- Derivações financeiras DEVEM ser expressões inline no corpo do componente (não em useEffect, não fora do componente) — assim React recalcula automaticamente após `setParcelas` (Pitfall 2 / D-13).
  - `valor = unidade?.valor_mensal ?? 0`
  - `pagas = parcelas.filter(p => p.status === "paga")`
  - `vencidas = parcelas.filter(p => p.status === "vencida")`
  - `emAberto = parcelas.filter(p => p.status === "pendente" || p.status === "vencida" || p.status === "futura")`
  - `totalContrato = parcelas.length * valor`, `totalPago = pagas.length * valor`, `totalEmAberto = emAberto.length * valor`, `totalInadimplencia = vencidas.length * valor`
- Timeline: ponto 12×12 QUADRADO (radius 0, regra Obsidian Blueprint). Futura: `background: transparent` + `border: 1px solid var(--fg-5)`. Outros: `background: cor_status`. Linha vertical `width:1, background var(--border-3), minHeight 28` até o penúltimo item.
- `marcarComoPaga` já existe (Parcelas.js:50-59) e re-fetcha. Manter; toast deve ser "Pagamento registrado · {fmtData(hoje)}" per Copywriting Contract (atualizar a string atual "Parcela marcada como paga").
- Font weights só 400 e 700. `--indigo` NÃO aparece nesta tela (só na barra de progresso? não — indigo é reservado ao modal de renovação no Plan 03 e ao eyebrow). Eyebrows usam classe `eyebrow eyebrow--indigo` já existente — manter.
- NÃO usar Tailwind para grid/borders de layout — inline styles + CSS vars.
- O botão "Renovar" e o modal NÃO entram neste plan (Plan 03). Não adicionar `renovarContrato` import aqui.
</critical_facts>

<tasks>

<task type="auto">
  <name>Task 1: Grade-resumo + resumo financeiro derivado</name>
  <files>src/components/features/Parcelas.js</files>
  <action>
    Adicionar grade-resumo e resumo financeiro acima da área de parcelas, e instalar as derivações financeiras inline. Seguir os snippets "Grade-resumo" e "Resumo financeiro" de PATTERNS.md e os Code Examples de RESEARCH.md.
    1. Adicionar `import { fmtBRL } from "@/lib/utils"` (junto ao import existente de utils — `import { cn, fmtData, fmtBRL } from "@/lib/utils"`).
    2. Em `carregar()`, garantir que `unidade` tenha `valor_mensal` e dados do edifício. Adicionar `getEdificios` ao import de queries-client e ao `Promise.all`; após resolver `unidade`, guardar também o edifício casando `unidade.edificio_id` (novo state `const [edificio, setEdificio] = useState(null)`), OU usar `unidade.edificios?.nome` se já vier no join. Implementar o caminho que funciona com a query real.
    3. No corpo do componente (substituindo `pagas`/`pendentes` das linhas 61-62 por derivações completas inline): calcular `valor`, `pagas`, `vencidas`, `emAberto`, `totalContrato`, `totalPago`, `totalEmAberto`, `totalInadimplencia` conforme critical_facts. Manter `pagas`/`pendentes` counts se ainda usados no header.
    4. GRADE-RESUMO (render condicional `contrato && unidade`): grid 5 colunas desktop (`gridTemplateColumns: "repeat(5, 1fr)"`), border `var(--border-3)`, marginBottom `var(--rd-block, 32px)`. Células: Unidade, Edifício, Valor mensal `fmtBRL(unidade.valor_mensal)`, Início `fmtData(contrato.data_inicio)`, Término `fmtData(contrato.data_fim)`. Cada célula: padding 14px 16px, background `var(--surface)`, borderRight quando i<4. Label `.r-label` (11px uppercase tracking, mb-2); valor font-display peso 700 16px (`.r-subhead`). Mobile: aplicar `gridTemplateColumns: "1fr 1fr"` via classe responsiva + ajustar bordas (borderRight só em células pares, borderTop quando i>=2). Implementar 2×3 mobile via wrapper className `romma-mobile-only`/`romma-desktop-only` com dois grids, OU classes Tailwind responsivas no gridTemplateColumns.
    5. RESUMO FINANCEIRO (4 colunas): grid `repeat(4, 1fr)`, border `var(--border-3)`, marginBottom 20. Array `metrics`: "Valor do contrato"/`fmtBRL(totalContrato)`/`{parcelas.length} parcelas`; "Total recebido"/`fmtBRL(totalPago)`/`{pagas.length} pagas`/ok:true; "Em aberto"/`fmtBRL(totalEmAberto)`/`{emAberto.length} parcelas`/gold:true; "Inadimplência"/(`vencidas.length > 0 ? fmtBRL(totalInadimplencia) : "R$0"`)/`{vencidas.length} vencida(s)`/danger:`vencidas.length > 0`. Célula danger: background `var(--danger-bg2)`. Label cor por flag (danger→`--danger-fg`, gold→`--highlight`, ok→`--success`, default→`--fg-4`). Valor font-display peso 700 24px tracking -1px, cor pela mesma lógica. Sub-caption `.r-meta` mt-1.
  </action>
  <verify>
    <automated>npx next build --no-lint 2>&1 | grep -qiE "Compiled|Generating static" && echo BUILD_OK</automated>
  </verify>
  <done>Grade-resumo (5 cols / 2×3 mobile) e resumo financeiro (4 cols) renderizam com valores derivados de `unidade.valor_mensal`. Inadimplência fica `--danger-bg2`/`--danger-fg` quando há vencidas. Derivações são inline (recalculam no re-render). Build passa.</done>
</task>

<task type="auto">
  <name>Task 2: Barra de progresso segmentada + timeline vertical (substituir tabela)</name>
  <files>src/components/features/Parcelas.js</files>
  <action>
    Substituir o bloco da tabela (Parcelas.js:98-163, `<div style={{ overflowX: "auto" }}>` envolvendo `gridStyle`) por: cabeçalho de seção + barra de progresso segmentada + timeline vertical. Remover `COL`/`gridStyle`/`HeaderCell` órfãos. Seguir snippets "Progress bar of parcelas" e "Timeline vertical" de PATTERNS.md.
    1. CABEÇALHO DE SEÇÃO: linha flex justify-between — esquerda: eyebrow "SISTEMA.PARCELAS" (`eyebrow eyebrow--indigo`) + `<h2>` "Cronograma de Parcelas" (font-display peso 700 18px, `.r-subhead`); direita: "{pagas.length}/{parcelas.length} pagas" (font-mono 11px text-fg-4, right-aligned, label da Progress bar).
    2. BARRA SEGMENTADA: `<div style={{ display: "flex", gap: 3, marginBottom: 24 }} aria-hidden="true">` mapeando `parcelas`; cada célula `flex:1, height:6`, background por status: paga→`var(--success)`, vencida→`var(--danger)`, pendente→`var(--warning)`, futura→`var(--surface-hi)`.
    3. TIMELINE: `<div className="border border-border-3 bg-surface p-6">` (ou `.r-panel` com padding `var(--rd-panel)`). Empty state quando `parcelas.length === 0`: "Nenhuma parcela encontrada." Map sobre `parcelas`: `const col = paga→--success, vencida→--danger, pendente→--warning, else→--fg-5`. Cada item: `<div style={{ display: "flex", gap: 16 }}>`. Coluna esquerda (flex column align center): `<span>` ponto 12×12 (flexShrink 0, marginTop 3, `background: status==="futura" ? "transparent" : col`, `border: status==="futura" ? "1px solid var(--fg-5)" : "none"`, SEM border-radius); linha vertical `{i < parcelas.length - 1 && <span style={{ flex:1, width:1, background:"var(--border-3)", minHeight:28 }} />}`. Coluna direita (flex:1, paddingBottom `i < len-1 ? 18 : 0`): linha flex justify-between — "Parcela {String(numero).padStart(2,"0")}" (font-display peso 700 15-16px, `.r-subhead`) + (à direita) botão "✓ Registrar" quando `pendente|vencida` + `<StatusBadge status={status} />`. Botão "✓ Registrar": inline style all:unset cursor pointer, font-mono 10px peso 700, cor `var(--success)`, uppercase letterSpacing 0.5px, border `1px solid color-mix(in oklch, var(--success) 40%, transparent)`, padding 5px 9px, `aria-label={\`Registrar pagamento da parcela ${numero}\`}`, `onClick={() => marcarComoPaga(parcela)}`. Meta row `.r-meta` (font-mono 11px, flex gap 14, flex-wrap, mt-1): "Venc · {fmtData(data_vencimento)}" (cor da data: vencida→`--danger-fg` senão `--fg-3`); "Pago · {data_pagamento ? fmtData : "—"}" (cor: pago→`--success` senão `--fg-5`); "{fmtBRL(unidade?.valor_mensal)}" (cor `--fg-2`).
    4. Remover constantes órfãs `COL`, `gridStyle`, `HeaderCell`.
  </action>
  <verify>
    <automated>npx next build --no-lint 2>&1 | grep -qiE "Compiled|Generating static" && echo BUILD_OK</automated>
  </verify>
  <done>Tabela removida. Barra de progresso segmentada (células 6px coloridas por status) + timeline vertical (ponto quadrado 12×12 + linha) renderizam todas as parcelas. Botão "✓ Registrar" aparece em pendente/vencida. `COL`/`gridStyle`/`HeaderCell` removidos. Build passa.</done>
</task>

<task type="auto">
  <name>Task 3: Atualização ao vivo do resumo + toast no registrar pagamento</name>
  <files>src/components/features/Parcelas.js</files>
  <action>
    Garantir que registrar pagamento atualize o resumo financeiro ao vivo (D-13, PARC-03). A lógica já existe parcialmente em `marcarComoPaga` (linhas 50-59).
    1. Confirmar que `marcarComoPaga` faz `setParcelas(await getParcelasByContrato(contratoId) ?? [])` após sucesso — como as derivações financeiras (Task 1) são inline no render, o resumo recalcula automaticamente. Nenhuma query extra ou setState manual do resumo é necessário (Pitfall 2).
    2. Atualizar o toast de sucesso para o copy do contrato: `toast.success(\`Pagamento registrado · ${fmtData(getTodayLocal())}\`)` (ou usar a data de hoje formatada). Definir/reusar um helper de "hoje" local (padrão `getTodayLocal()` existe em Contratos.js — replicar pequeno helper ou usar `new Date()`). Manter o tratamento de erro existente (`setErro(result.erroMessage)`).
    3. Validar visualmente (no done) que após registrar: a parcela vira "paga" na timeline, a barra de progresso atualiza a célula, e os números "Total recebido"/"Em aberto"/"Inadimplência" recalculam sem reload.
  </action>
  <verify>
    <automated>npx next build --no-lint 2>&1 | grep -qiE "Compiled|Generating static" && echo BUILD_OK</automated>
  </verify>
  <done>Registrar pagamento → `setParcelas` re-fetcha → resumo financeiro, barra e timeline recalculam ao vivo via React state. Toast "Pagamento registrado · DD/MM/AAAA". Build passa.</done>
</task>

</tasks>

<verification>
- `npx next build --no-lint` → zero erros após cada task.
- Manual (browser): `/dashboard/contratos/[id]` abre → grade-resumo + resumo financeiro visíveis; timeline vertical renderiza todas as parcelas; barra colorida por status; registrar pagamento → toast + resumo atualizado ao vivo.
- Conferir contra `.planning/design/screenshots/desktop/08-contrato-parcelas.png` e `mobile/08-contrato-parcelas.png`.
</verification>

<success_criteria>
- PARC-01: Grade-resumo (5 cols / 2×3 mobile) + resumo financeiro (4 cols) com valores derivados de `unidade.valor_mensal`.
- PARC-02: Parcelas em timeline vertical (ponto quadrado 12×12 + linha) + barra de progresso segmentada (1 célula = 1 parcela).
- PARC-03: Registrar pagamento marca paga, atualiza resumo financeiro ao vivo (React state, sem reload) e mostra toast.
- Inadimplência em vermelho quando há vencidas.
- Derivações inline (recalculam no re-render). Datas exibidas via fmtData.
</success_criteria>

<output>
Create `.planning/phases/22-contratos-parcelas-renova-o/22-02-SUMMARY.md` when done.
</output>
