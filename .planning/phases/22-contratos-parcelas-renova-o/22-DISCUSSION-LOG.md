# Discussion Log — Phase 22: Contratos & Parcelas — Renovação

**Mode:** `--auto` (fully autonomous — all decisions auto-selected)
**Date:** 2026-06-15

---

## Areas Selected

`[--auto] Selected all gray areas: layout-contratos, busca-filtro, arquivo-encerrados, timeline-parcelas, renovacao-sa`

---

## Area 1: Layout da tela de Contratos

**Q:** Cards (variante B do design) vs. manter tabela grid existente?

**→ Selected:** Cards desktop (grid auto-fill minmax 330px) + rows mobile (recommended default)

**Why:** Design canônico `console3.jsx` implementa cards. ROADMAP §22 diz "board de cards". A tabela existente serve de base para mobile. Conversão para cards no desktop segue o alvo de fidelidade alta definido em `.planning/design/README.md`.

**Auto log:**
```
[auto] Layout contratos — Q: "Cards vs tabela no desktop?" → Selected: "Cards desktop + rows mobile" (recommended default)
```

---

## Area 2: Busca e filtro Vencendo

**Q:** Client-side (sobre lista carregada) vs. query server-side com parâmetros?

**→ Selected:** Client-side sobre lista carregada (recommended default)

**Why:** Padrão do codebase (ver Unidades.js, LocatariosDesktop.js — todos filtram client-side). Dataset de contratos é pequeno para um TCC. `nameOf(c)` concatena join já existente em `getContratos()`.

**Auto log:**
```
[auto] Busca/filtro — Q: "Client-side vs server-side?" → Selected: "Client-side" (recommended default)
```

---

## Area 3: Arquivo de encerrados

**Q:** Toggle inline na mesma tela vs. nova rota `/dashboard/contratos/arquivo`?

**→ Selected:** Toggle inline (recommended default)

**Why:** O callout "Ver Arquivo →" já existe em Contratos.js:434 como disabled. Conversão em toggle: mínima mudança, sem nova rota, sem nova page.js. Alinha com `console3.jsx:179-218` que usa `setShowArquivo`.

**Auto log:**
```
[auto] Arquivo encerrados — Q: "Toggle inline vs nova rota?" → Selected: "Toggle inline" (recommended default)
```

---

## Area 4: Timeline vertical de parcelas

**Q:** Substituir tabela existente por timeline vs. adicionar timeline ao lado?

**→ Selected:** Substituir tabela por timeline vertical (recommended default)

**Why:** PARC-02 é explícito: "timeline vertical". Design `console3.jsx:300-327` (variante B) implementa timeline pura sem tabela. Combinar os dois criaria redundância visual.

**Auto log:**
```
[auto] Timeline parcelas — Q: "Redesign completo vs tabela + timeline?" → Selected: "Substituir por timeline vertical" (recommended default)
```

---

## Area 5: SA renovarContrato

**Q:** Reusar Edge Function `gerar-parcelas` vs. nova Server Action com lógica inline?

**→ Selected:** Nova Server Action `renovarContrato` com lógica de INSERT inline (recommended default)

**Why:** ROADMAP SC-5 é explícito: "append de novas parcelas futuras via Server Action, sem re-chamar a Edge Function gerar-parcelas". Edge Function tem latência adicional e invocação via JWT. SA direta com `supabaseAdmin` é mais simples e segura para o TCC.

**Auto log:**
```
[auto] Renovação SA — Q: "Edge Function vs Server Action?" → Selected: "Nova SA renovarContrato" (recommended default)
```

---

## Claude's Discretion Items

Itens não perguntados ao usuário — decididos pelo implementador:

1. Numeração das novas parcelas: `MAX(numero) + 1..N` via SQL LIMIT 1 ORDER BY numero DESC.
2. Callout existente: adicionar `onClick` ao botão existente (não substituir o elemento).
3. Animação de entrada dos cards: aplicar `rFade` se token existir em globals.css; skip caso contrário.
4. Campo custom de meses no modal: `<input type="number" min={1} max={36} />`.

---

## Deferred Ideas

- "Expandir contrato" (somar unidade): explicitamente cortado — Out of Scope.
- Reajuste IGP-M: explicitamente removido pelo cliente.

---

*Generated by discuss-phase --auto · Phase 22*
