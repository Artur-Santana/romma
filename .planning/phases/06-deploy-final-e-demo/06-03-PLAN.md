---
phase: 06-deploy-final-e-demo
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified: [.gitignore, DEMO.md, demo-cheat-sheet.html]
autonomous: false
requirements: [DEPL-02, DEMO-01]

must_haves:
  truths:
    - "O fluxo completo de convite de Locatário foi testado em produção de ponta a ponta"
    - "O email de convite chega e o link redireciona corretamente para /dashboard em romma-alpha.vercel.app"
    - "DEMO.md está listado no .gitignore e nunca é commitado"
    - "DEMO.md contém sequência de ações, pontos de destaque técnico, tempo por seção e fallback de Realtime"
    - "Existe um cheat sheet imprimível de 1 página A4 com passos numerados e fallbacks"
  artifacts:
    - path: ".gitignore"
      provides: "Exclusão de DEMO.md do versionamento"
      contains: "DEMO.md"
    - path: "DEMO.md"
      provides: "Roteiro completo de demonstração da banca"
      min_lines: 40
    - path: "demo-cheat-sheet.html"
      provides: "Cheat sheet imprimível de 1 página A4"
      min_lines: 30
  key_links:
    - from: "convite em produção"
      to: "romma-alpha.vercel.app/dashboard"
      via: "redirectTo do inviteUserByEmail com SITE_URL"
      pattern: "romma-alpha"
---

<objective>
Validar o fluxo completo de convite de Locatário em produção (DEPL-02) e produzir o roteiro de demonstração da banca em formato duplo (DEMO-01, D-05): DEMO.md (gitignored) e cheat sheet imprimível de 1 página A4.

Purpose: O fluxo de convite nunca foi testado em prod (D-02) e a banca em 18/06/2026 precisa de um roteiro ensaiado com fallback documentado para a limitação conhecida do Realtime (D-07). DEMO.md contém dados sensíveis de apresentação e não pode ser commitado (D-05).
Output: Confirmação de convite funcional em prod; .gitignore atualizado; DEMO.md e demo-cheat-sheet.html criados (DEMO.md nunca entra em git).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deploy-final-e-demo/06-CONTEXT.md
@src/actions/locatarios.js
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar DEMO.md ao .gitignore (DEMO-01, D-05)</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (estado atual — seção #docs nas linhas 54-58)
  </read_first>
  <action>
    Adicionar a linha `DEMO.md` à seção `#docs` do .gitignore (após `/docs/plans`, linha 58), seguindo a convenção de agrupamento por seção (06-PATTERNS.md). Esta tarefa DEVE ser executada e o .gitignore commitado ANTES de qualquer DEMO.md ser criado (Task 3), para garantir que o arquivo sensível nunca seja rastreado pelo git. DEMO.md contém dados de banca sensíveis (D-05) — tratar vazamento como modo de falha.
  </action>
  <verify>
    <automated>grep -v '^#' .gitignore | grep -c "^DEMO.md$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' .gitignore | grep -c "^DEMO.md$"` retorna 1
    - A linha está dentro/após a seção #docs
  </acceptance_criteria>
  <done>DEMO.md consta no .gitignore na seção #docs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validar fluxo de convite de Locatário em produção (DEPL-02, D-02)</name>
  <read_first>
    - src/actions/locatarios.js (convidarLocatario — fluxo de convite sob teste, usa SITE_URL no redirectTo)
    - .env.example (variáveis que devem estar setadas em prod por 06-01)
  </read_first>
  <what-built>
    06-01 configurou SITE_URL=https://romma-alpha.vercel.app, a Redirect URL no Supabase Auth e APP_URL. O fluxo de convite usa src/actions/locatarios.js:convidarLocatario() → inviteUserByEmail com redirectTo `${SITE_URL}/dashboard`. O envio de email e o clique no link não têm CLI — exigem verificação humana ponta a ponta em produção.
  </what-built>
  <action>Pausar para o usuário executar manualmente o fluxo de convite em produção (convidar locatário → receber email → clicar link → confirmar redirect para romma-alpha.vercel.app/dashboard), conforme os passos em how-to-verify. Nenhuma automação possível — envio de email e clique no link exigem ação humana.</action>
  <how-to-verify>
    1. Acessar https://romma-alpha.vercel.app/dashboard logado como Proprietário
    2. Convidar um Locatário com um email real acessível (ex: email pessoal de teste)
    3. Confirmar que a Server Action retorna status 200 (sem erroMessage)
    4. Abrir a caixa de entrada do email convidado — confirmar que o email de convite chegou
    5. Clicar no link do convite — confirmar que abre em romma-alpha.vercel.app (NÃO localhost) e leva ao fluxo de definição de senha e depois a /dashboard
    6. Confirmar que o registro do locatário existe na tabela locatarios (via Supabase Dashboard ou painel)
  </how-to-verify>
  <acceptance_criteria>
    - Server Action convidarLocatario retorna status 200 (sem erroMessage)
    - Email de convite recebido na caixa de entrada do email convidado
    - Link do convite abre em romma-alpha.vercel.app (NÃO localhost) e leva ao fluxo de senha → /dashboard
    - Registro do locatário existe na tabela locatarios
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" se o convite chegou e o redirect funcionou em produção, ou descreva o erro (email não chegou / redirect foi para localhost / erro 4xx-5xx).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Criar DEMO.md e demo-cheat-sheet.html (DEMO-01, D-05, D-06, D-07)</name>
  <files>DEMO.md, demo-cheat-sheet.html</files>
  <read_first>
    - .gitignore (confirmar que DEMO.md já está ignorado — Task 1 deve ter rodado antes)
    - .planning/phases/06-deploy-final-e-demo/06-CONTEXT.md (D-05, D-06, D-07 — conteúdo do roteiro e fallback)
    - .planning/PROJECT.md (§Key Decisions — limitação Realtime)
  </read_first>
  <action>
    Confirmar primeiro que DEMO.md está no .gitignore (Task 1). NUNCA executar `git add DEMO.md` — o arquivo é sensível e gitignored (D-05).

    Criar DEMO.md (roteiro completo, D-06) com: (1) sequência numerada de ações da demo — LP/listagem pública → login Proprietário → dashboard (MRR, Receita Esperada, alerta DASH-03) → portal do Locatário (login via convite, contrato ativo, parcelas) → criação ao vivo de contrato mostrando unidade sumindo de /unidades (D-04 ação ao vivo); (2) pontos de destaque técnico para mencionar à banca (RLS, Realtime, Edge Function de geração de parcelas, paleta Obsidian Blueprint); (3) tempo estimado por seção; (4) fallback de Realtime (D-07): se o card não sumir de /unidades após criar contrato, fazer refresh manual (F5) e explicar verbalmente — "RLS do Supabase filtra eventos UPDATE de disponível→alugada; workaround conhecida, solução seria webhook ou polling". Incluir credenciais/dados da base de demo populada por 06-02 (URLs, emails de teste, valores esperados) para consulta rápida.

    Criar demo-cheat-sheet.html (D-05) — versão imprimível que cabe em 1 página A4: passos numerados condensados, tempos por seção, fallbacks visíveis em destaque. Usar CSS com @media print e @page size A4, fonte legível, layout compacto de 1 página. Este arquivo deve conter APENAS passos do roteiro, tempos e fallbacks — NUNCA credenciais, emails de teste ou dados sensíveis (esses ficam só em DEMO.md, gitignored). Sem dados sensíveis, demo-cheat-sheet.html pode ser commitado com segurança.
  </action>
  <verify>
    <automated>test -f DEMO.md && test -f demo-cheat-sheet.html && grep -qi "fallback" DEMO.md && grep -qi "realtime\|disponível→alugada\|RLS" DEMO.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - DEMO.md existe e contém: sequência numerada de ações, seção de pontos de destaque técnico, tempos estimados, e fallback de Realtime mencionando RLS e refresh manual (D-06, D-07)
    - demo-cheat-sheet.html existe, contém CSS de impressão (@media print ou @page A4) e passos numerados
    - `git status --porcelain DEMO.md` NÃO mostra DEMO.md como rastreado/staged (continua ignorado)
  </acceptance_criteria>
  <done>DEMO.md (roteiro completo, gitignored) e demo-cheat-sheet.html (1 página A4 imprimível) existem com sequência, destaques, tempos e fallback de Realtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo git → mundo | DEMO.md com dados sensíveis de banca não pode vazar para o histórico git |
| Convite prod → email externo | Link de convite redireciona usuário externo para o dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information disclosure | DEMO.md commitado por engano | mitigate | Task 1 adiciona DEMO.md ao .gitignore ANTES de Task 3 criar o arquivo; Task 3 proíbe `git add DEMO.md` e o acceptance_criteria verifica que não está rastreado |
| T-06-07 | Spoofing | redirect do link de convite | mitigate | Validado em Task 2 que o redirect vai para romma-alpha.vercel.app (não localhost/domínio arbitrário), dependente da Redirect URL restrita de 06-01 |
| T-06-08 | Information disclosure | credenciais no demo-cheat-sheet.html | mitigate | Action instrui a não incluir credenciais sensíveis no HTML imprimível, ou adicioná-lo ao .gitignore se incluir |
</threat_model>

<verification>
- Checkpoint humano confirma convite funcional ponta a ponta em prod (DEPL-02)
- DEMO.md e demo-cheat-sheet.html existem com conteúdo D-06/D-07
- DEMO.md não rastreado pelo git
</verification>

<success_criteria>
- DEPL-02: fluxo completo de convite de Locatário testado e funcional em produção
- DEMO-01: roteiro de demo em formato duplo (DEMO.md + cheat sheet imprimível) com sequência, destaques, tempos e fallback de Realtime
</success_criteria>

<output>
Create `.planning/phases/06-deploy-final-e-demo/06-03-SUMMARY.md` when done
</output>
