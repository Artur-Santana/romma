---
phase: 18
slug: acesso-login-cadastro-redefinir
status: draft
shadcn_initialized: true
preset: obsidian-blueprint
created: 2026-06-13
---

# Phase 18 — UI Design Contract
## Acesso: Login / Cadastro / Redefinir

> Visual and interaction contract for Phase 18 auth screen redesign.
> Generated by gsd-ui-researcher. Source of truth: `.planning/design/js/auth.jsx` (Variante A).
> All decisions locked from CONTEXT.md unless noted otherwise.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui (v4.2.0) + custom CSS |
| Preset | Obsidian Blueprint — `--ds-*` source tokens in `globals.css` |
| Component library | radix-ui (via shadcn) |
| Icon library | Inline SVG only (no external icon lib in auth screens) |
| Font — body/mono | Space Grotesk (`var(--font-body)`, `var(--font-mono)`) |
| Font — display | Space Grotesk architectural alias (`var(--font-display)` → `var(--font-display-arch)`) |
| Radius | 0 (sharp corners everywhere — `--radius: 0`) |

Source: `src/app/globals.css`, `components.json`. No new design system init required.

---

## Layout Architecture

### Variante A — Split-Panel (LOCKED — source: CONTEXT.md + auth.jsx)

```
┌──────────────────────────────────────────────────────────────────┐
│ TopStrip  h-7  bg rgba(18,18,18,0.95)  border-b border-2        │
├────────────────────────────┬─────────────────────────────────────┤
│  AuthAside  (1.05fr)       │  Form panel  (1fr)                  │
│  photo + corner brackets   │  r-scroll  overflowY auto           │
│  gradient overlay          │  flex center  padding 40px 56px     │
│  CONSOLE ROMMA wordmark    │  maxW 408px form card               │
│  headline + body copy      │  gap-between-fields: 24px           │
│  hidden on mobile          │  (always visible)                   │
├────────────────────────────┴─────────────────────────────────────┤
│ BottomMeta  py-2.5  px-5  border-t border-1  flex space-between │
└──────────────────────────────────────────────────────────────────┘
```

**Breakpoint:** `lg` (≥1024px) → 2-column grid (`grid-cols-[1.05fr_1fr]`).
Below `lg` → single column, AuthAside hidden (`hidden lg:block`), form fills viewport.
Mobile target: 375px — form padding 32px horizontal, 22px vertical.

### Shared Components (extract to `src/components/auth/`)

| Component | Responsibility |
|-----------|----------------|
| `AuthFrame` | Full-height shell: TopStrip + grid main + BottomMeta |
| `AuthAside` | Left panel: image, gradient, CornerBrackets, wordmark, headline |
| `CornerBrackets` | Four L-shaped gold bracket decorations positioned absolute |
| `TopStrip` | 28px top bar — mono text, status dot |
| `AuthField` | Underline input with mono label, ref code, focus/error states |
| `AuthBanner` | Success/danger/warning inline banner |
| `SubmitButton` | Bracket-style button with 3-state machine |

---

## Spacing Scale

Tokens consumed from Phase 17 `--rd-*`. Auth screens map to these values:

| Token | Value | Auth Usage |
|-------|-------|-----------|
| `--rd-gutter-m` | 20px | Mobile form horizontal padding |
| `--rd-panel` | 20px | AuthAside internal insets (top: 36px exception) |
| `--rd-block` | 24px | Gap between form field groups |
| `--rd-block-sm` | 16px | Gap between refLabel and adjacent elements |
| `--rd-row-y` | 12px | CornerBracket offset from panel edges |
| `--rd-cell` | 20px | AuthField vertical padding base |

**Auth-specific spacing (exact from `auth.jsx`):**

| Location | Value | Source |
|----------|-------|--------|
| TopStrip height | 28px | `auth.jsx:8` |
| TopStrip padding x | 20px | `auth.jsx:8` |
| Form panel padding desktop | 40px 56px | `auth.jsx:35` |
| Form panel padding mobile | 32px 22px | `auth.jsx:35` |
| Form card max-width | 408px | `auth.jsx:163` |
| Gap between form sections | 28px (login/reset), 24px (signup) | `auth.jsx:167,230` |
| Gap between individual fields | 24px (login), 20px (signup) | `auth.jsx:175,242` |
| AuthField input padding | **12px** top/bottom, 56px right, 0 left | `auth.jsx:100` (adjusted to 4-grid: 13→12) |
| AuthField label margin-bottom | 8px | `auth.jsx:91` |
| AuthAside wordmark top inset | 36px from edge | `auth.jsx:56` |
| AuthAside copy bottom inset | 56px from bottom | `auth.jsx:60` |
| CornerBrackets corner offset | **16px** from each edge | `auth.jsx:75` (adjusted to 4-grid: 18→16) |
| CornerBracket bracket size | 22×22px | `auth.jsx:75` |
| BottomMeta padding | 10px 20px | `auth.jsx:41` (10px = deliberate thin strip) |
| Status badge margin-bottom | 44px desktop / 28px mobile | `auth.jsx:164` |

**8-point exceptions (auth-specific, do not replicate in other phases):**
- 28px gap (login/reset form sections) — between-section spacing
- 44px status badge top spacing (desktop) — touch-target region
- 56px AuthAside copyblock bottom inset — deep bottom anchor
- 10px BottomMeta vertical padding — deliberate thin meta strip (not a content area)

---

## Typography

### Typography Scale Rationale

Auth screens use a **4-tier bounded scale** mapped to Phase 17 `--rt-*` tokens. The auth pages require an oversized display heading that exceeds `--rt-title` (32px) — this is declared as a single **auth-display** role with responsive stepping (one role, multiple viewport sizes), not as independent sizes. All pixel values within a tier are responsive steps of the same semantic role.

**4-tier scale:**

| Tier | Role | Token Anchor | Pixel Range | Weights |
|------|------|-------------|-------------|---------|
| 1 — Display | Auth headlines + Aside headline | beyond `--rt-title` (32px) — auth-display oversized | 34–52px (responsive steps) | 700 |
| 2 — Body | Input text + body copy | `--rt-subhead` (16px) / `--rt-body` (14px) | 14–16px | 400 |
| 3 — Label | Field labels + banner codes + button text | `--rt-label` (11px) | 11–12px | 700 |
| 4 — Meta | Ref codes, TopStrip, BottomMeta, status | `--rt-meta` (10px) | 10px | 400 |

**Weight contract: exactly 2 weights — 400 (regular) and 700 (bold). No 600 in auth screens.**

---

### Tier 1 — Display (auth-display role, responsive stepping)

Auth headlines are an intentional oversize beyond the dashboard `--rt-title` (32px), justified by the full-viewport auth context. All variants are the SAME display role at different responsive steps:

| Context | Desktop | Mobile | Weight | Font | Line-height | Letter-spacing |
|---------|---------|--------|--------|------|------------|----------------|
| Login / Reset headline | 50px | 38px | 700 | `var(--font-display)` | 1 | -2.4px |
| Signup headline (secondary step) | 46px | 34px | 700 | `var(--font-display)` | 1 | -2.4px |
| Aside headline (largest step) | 52px | — (aside hidden on mobile) | 700 | `var(--font-display)` | 1 | -2.6px |

These 5 pixel values (50, 46, 38, 34, 52) are **responsive steps of the single auth-display role** — not independent type sizes. The signup variant is a secondary step of the same role (slightly reduced to accommodate more fields). The aside headline is the largest step of the same role. Class: `headline` inline style.

---

### Tier 2 — Body

Mapped to `--rt-subhead` (16px) and `--rt-body` (14px).

| Usage | Token | Size | Weight | Font | Line-height | Color |
|-------|-------|------|--------|------|------------|-------|
| Field input text | `--rt-subhead` | 16px | 400 | `var(--font-body)` | — | `var(--fg-1)` |
| Aside body copy | `--rt-body` | 14px | 400 | `var(--font-body)` | 1.5 | `var(--fg-2)` |

Classes: `.r-subhead` / `.r-body`.

---

### Tier 3 — Label

Standardized at `--rt-label` (11px). Elements doing the same label job use the same size. The 12px bracket text (SubmitButton) is the only step within this tier — justified as a button-bracket affordance requiring slightly larger mono for legibility at the button's optical weight.

| Usage | Token | Size | Weight | Font | Letter-spacing | Color | Class |
|-------|-------|------|--------|------|----------------|-------|-------|
| Field label (idle) | `--rt-label` | 11px | 700 | `var(--font-mono)` | 1.5px | `var(--fg-4)` | `.r-label` |
| Field label (focused) | `--rt-label` | 11px | 700 | `var(--font-mono)` | 1.5px | `var(--primary-hover)` | `.r-label` variant |
| Section eyebrow | `--rt-label` | 11px | 700 | `var(--font-body)` | 3px | `var(--primary-hover)` | inline |
| Banner code (ALLCAPS) | `--rt-label` | 11px | 700 | `var(--font-body)` | 1.5px | semantic color | `.r-label` |
| Submit button label | `--rt-label` | 11px | 700 | `var(--font-body)` | 2px | `var(--fg-1)` | inline UPPERCASE |
| Cross-screen link | `--rt-label` | 11px | 400 | `var(--font-mono)` | — | `var(--fg-4)` | inline |
| Password toggle | `--rt-label` | 11px | 400 | `var(--font-mono)` | 1px | `var(--fg-4)` | inline UPPERCASE |
| Submit button bracket | label tier | 12px | 400 | `var(--font-mono)` | 1px | `var(--fg-1)` | inline (bracket step) |
| Banner body | label tier | 12px | 400 | `var(--font-mono)` | — | `var(--fg-2)` | inline (banner body step, line-height 1.45) |

The 12px values (button bracket, banner body) are the label tier's secondary step — same tier, slightly larger for optical balance within their containers.

---

### Tier 4 — Meta

Mapped to `--rt-meta` (10px).

| Usage | Token | Size | Weight | Font | Letter-spacing | Color | Class |
|-------|-------|------|--------|------|----------------|-------|-------|
| Field ref code | `--rt-meta` | 10px | 400 | `var(--font-mono)` | — | `var(--fg-5)` | `.r-meta` |
| TopStrip text | `--rt-meta` | 10px | 400 | `var(--font-mono)` | 0.5px | `var(--fg-4)` | `.r-meta` |
| Status badge | `--rt-meta` | 10px | 400 | `var(--font-mono)` | 0.5px | `var(--fg-4)` | `.r-meta` |
| BottomMeta | `--rt-meta` | 10px | 400 | `var(--font-mono)` | 0.5px | `var(--fg-5)` | `.r-meta` |
| Password policy hint | `--rt-meta` | 10px | 400 | `var(--font-mono)` | — | `var(--fg-5)` | `.r-meta` |

---

## Color

Obsidian Blueprint palette. All tokens from `globals.css`.

| Role | Token | Resolved Value | Usage |
|------|-------|---------------|-------|
| Dominant surface (60%) | `var(--background)` | `oklch(0.2393 0 0)` ≈ #3D3D3D | Full-screen background, TopStrip, BottomMeta |
| Secondary surface (30%) | `var(--surface)` | `oklch(0.182 0 0)` ≈ #2E2E2E | AuthAside image underlay, form scroll area |
| Gold accent (10%) | `var(--highlight)` = `var(--color-highlight)` | `oklch(0.7245 0.0998 82.35)` ≈ #C9A84C | CornerBrackets, eyebrow text, headline gold period, active field underline via `--primary-hover` |
| Indigo primary | `var(--primary)` | `oklch(0.339 0.1793 301.68)` | Submit button bg (idle), field underline active on login |
| Primary hover (gold alias) | `var(--primary-hover)` = `oklch(from var(--primary) calc(l + 0.25) c h)` | lighter indigo-violet | Wordmark eyebrow "CONSOLE", aside eyebrow line, field label active |
| Destructive | `var(--danger-fg)` | `oklch(0.826 0.073 22.5)` ≈ #D9614A | Error banner border, icon, label |
| Success semantic | `var(--success)` | `oklch(0.696 0.17 162.5)` ≈ #34C78A | Success banner border, icon, submit button bg on success state |

**Accent (gold `--highlight`) is reserved exclusively for:**
- CornerBracket decorations (opacity 0.7)
- EyebrowRail rule line (`w-6 h-px`)
- EyebrowRail text
- Headline gold period `.` character
- AuthAside wordmark "CONSOLE" tag

**Gold does NOT apply to:** field underlines (those use `--primary`), submit button, banners.

### Image Treatment (AuthAside)
`filter: grayscale(0.3) contrast(1.1) brightness(0.62)` — exact values from `auth.jsx:52`.
Gradient overlay: `linear-gradient(180deg, rgba(0,0,0,0.25) 0%, rgba(0,0,0,0.88) 100%)`.

---

## Component Specs

### CornerBrackets

Four absolute-positioned `<span>` elements. Each 22×22px. Border color `var(--highlight)` at opacity 0.7. Border width 1px. Positioned **16px** from each corner edge. Each uses only 2 border sides:
- Top-left: `border-top border-left`
- Top-right: `border-top border-right`
- Bottom-left: `border-bottom border-left`
- Bottom-right: `border-bottom border-right`

### AuthField (underline input style)

```
[ LABEL                      REF_CODE ]   ← mono 11px / 10px, mb-2
──────────────────────────────────────    ← 1px bottom border
  input text 16px                [extra] ← padding 12px 56px 12px 0
```

States:
| State | Label color | Border | Box-shadow |
|-------|------------|--------|-----------|
| Idle | `var(--fg-4)` | `var(--border-2)` (rgba white 10%) | none |
| Focused | `var(--primary-hover)` | `var(--primary-hover)` | `0 1px 0 0 var(--primary-hover)` |
| Error | `var(--fg-4)` | `var(--danger-fg)` | none |

Transition: `border-color var(--dur-fast), box-shadow var(--dur-fast)`.

Password toggle extra: absolute right-0, top 50% -translate-y-1/2. Mono 11px, `var(--fg-4)`. Labels: `EXIBIR` / `OCULTAR` (uppercase, 1px letter-spacing). `style={{ all: "unset", cursor: "pointer" }}`.

### AuthBanner

```
[ MARK ] [ CODE_LABEL (11px bold uppercase) ]
         [ body text 12px mono fg-2         ]
```

`border-left: 2px solid {semantic-color}`.
Mark icon: 16×16px box, `border: 1px solid {semantic-color}`, centered character.

| Tone | bg | border/mark/code | mark char |
|------|----|-----------------|-----------|
| success | `rgba(16,185,129,0.13)` | `var(--success)` | `✓` |
| danger | `rgba(147,0,10,0.20)` | `var(--danger-fg)` | `!` |
| warning | `var(--warning-bg)` | `var(--warning)` | `·` |

Padding: 12px 14px. Gap between mark and text: 12px.

### SubmitButton (Bracket State Machine)

3-state button: `idle → loading → success`.

| State | Left bracket | Center label | Right | bg | shadow |
|-------|-------------|-------------|-------|-----|--------|
| idle | `[>]` | uppercase text | `ENTER` | `var(--primary)` | `0 0 16px 0 var(--primary-glow)` |
| loading | `[···]` | uppercase text | _(empty)_ | `var(--primary)` | none |
| success | `[OK]` | uppercase text | `200` | `var(--success)` | none |

- Padding: 17px 22px (`auth.jsx:133`). (Non-multiple of 4 — deliberate button-height spec from handoff; padding-y 16px would compress the button below its designed optical weight.)
- `style={{ all: "unset" }}` reset, full width, flex row space-between.
- `disabled` when `isLoad`.
- Loading progress bar: `position:absolute bottom-0 left-0 h-[2px] w-[40%] bg-[var(--chart-1)] animation: rBar 1s linear infinite`.
- Transition: `background var(--dur-base), box-shadow var(--dur-base)`.

### Custom Checkbox (Login — "Manter sessão")

16×16px box. `border: 1px solid`. On checked: `border-primary bg-primary`. Off: `border-[rgba(255,255,255,0.25)] bg-transparent`. SVG checkmark 10×10 (stroke white 1.5px strokeLinecap square).
Role: `role="checkbox" aria-checked tabIndex=0`. Keyboard: Space toggles.
Label: mono 11px fg-4 tracking-[1px] uppercase. "MANTER SESSÃO ATIVA".

### Status Badge (top-right of each form)

```
[r-dot] GATEWAY_NAME · ONLINE
```

`.r-dot` = 6×6px realtime pulse (existing class). `.r-meta` for text. Aligned flex-end top of form area, `mb-11` (44px desktop) / `mb-7` (28px mobile) before heading.

---

## Screen-by-Screen Copy Contract

### Screen 1: Login (`/login`)

| Element | Copy |
|---------|------|
| TopStrip left | `INTEGRATED_SYSTEM_NODE: 0X449F` |
| TopStrip right (desktop) | `GRID.OS.ALPHA` · `[r-dot] ONLINE` |
| Status badge | `AUTH_GATEWAY · ONLINE` |
| Eyebrow | `Identificação do Operador` |
| Headline | `ACESSO AO`  `SISTEMA`**`.`** |
| Field 1 label | `E-MAIL` `REF_U_AUTH_01` |
| Field 2 label | `SENHA` `REF_U_AUTH_02` |
| Password toggle idle | `EXIBIR` |
| Password toggle shown | `OCULTAR` |
| Checkbox label | `MANTER SESSÃO ATIVA` |
| Forgot link | `Esqueci minha senha` |
| Submit idle | `[>]` — `ACESSAR SISTEMA` — `ENTER` |
| Submit loading | `[···]` — `AUTENTICANDO` — _(empty)_ |
| Submit success | `[OK]` — `ACESSO CONCEDIDO` — `200` |
| Error banner code | `ERRO_AUTH · 401` |
| Error banner body | `Credenciais inválidas. Verifique e-mail e senha e tente novamente.` |
| Bottom of form | `Sem conta?` [link] `Configurar sistema` |
| BottomMeta left | `ROMMA © 2026 · CONSOLE v2.4.1` |
| BottomMeta right (desktop) | `SESSION_ID: 0XFF8A-2310 // TLS 1.3` |

### Screen 2: Cadastro (`/signup`)

| Element | Copy |
|---------|------|
| TopStrip left | `INTEGRATED_SYSTEM_NODE: 0X449F` |
| TopStrip right | `GRID.OS.ALPHA` · `[r-dot] ONLINE` |
| Status badge | `SIGNUP_GATEWAY · ONLINE` |
| Eyebrow | `Configuração Inicial` |
| Headline | `CONFIGURAR`  `SISTEMA`**`.`** |
| Field 1 label | `NOME` `REF_U_INIT_01` |
| Field 2 label | `SOBRENOME` `REF_U_INIT_02` |
| Field 3 label | `E-MAIL` `REF_U_INIT_03` |
| Field 4 label | `TELEFONE` `REF_U_INIT_04` |
| Field 5 label | `SENHA` `REF_U_INIT_05` |
| Field 6 label | `CONFIRMAR SENHA` `REF_U_INIT_06` |
| Password hint | `Mínimo 6 caracteres, 1 letra maiúscula e 1 número.` |
| Submit idle | `[>]` — `CONFIGURAR SISTEMA` — `ENTER` |
| Submit loading | `[···]` — `CONFIGURANDO` — _(empty)_ |
| Success banner code | `VERIFIQUE SEU E-MAIL · 200` |
| Success banner body | `Enviamos um link de ativação. Confirme para liberar o console.` |
| Error banner code | `ERRO_VALIDAÇÃO` |
| Error banner body | _(dynamic — see validation section)_ |
| Bottom of form | `Já tem conta?` [link] `Acessar sistema` |

**Field layout:** Nome + Sobrenome in 2-column grid (`grid-cols-[1fr_1fr] gap-4`). All other fields full-width.

### Screen 3: Redefinir Senha — Part A: Request (`/auth/reset-password` — email step)

Note: existing `/auth/reset-password` handles two sub-flows. The "request" (email) sub-flow is currently inlined in login. In this redesign it becomes a dedicated screen at `/auth/reset-password` using a query param to distinguish flows. See Sub-flow B below.

**Request email flow:**

| Element | Copy |
|---------|------|
| TopStrip left | `INTEGRATED_SYSTEM_NODE: 0X449F` |
| Back button (top-left) | `← LOGIN` |
| Status badge | `RECOVERY · ONLINE` |
| Eyebrow | `Recuperação de Acesso` |
| Headline | `REDEFINIR`  `SENHA`**`.`** |
| Sub-heading | `Informe o e-mail da conta. Enviaremos um link seguro para criar uma nova senha.` |
| Field label | `E-MAIL` `REF_U_RST_01` |
| Submit idle | `[>]` — `ENVIAR LINK` — `ENTER` |
| Submit loading | `[···]` — `ENVIANDO` — _(empty)_ |
| Success banner code | `E-MAIL_ENVIADO · 200` |
| Success banner body | `Verifique sua caixa de entrada para redefinir a senha.` |

**Define new password sub-flow (existing `/auth/reset-password?type=recovery`):**

| Element | Copy |
|---------|------|
| Eyebrow | `Recuperação de Acesso · Nova Senha` |
| Headline | `DEFINIR`  `NOVA SENHA`**`.`** |
| Field 1 label | `NOVA SENHA` |
| Field 2 label | `CONFIRMAR SENHA` |
| Password hint | `Mínimo 6 caracteres, 1 letra maiúscula e 1 número.` |
| Submit idle | `[>]` — `DEFINIR SENHA` — `ENTER` |
| Submit loading | `[···]` — `DEFININDO` — _(empty)_ |
| Success banner code | `SENHA_DEFINIDA · 200` |
| Success banner body | `Senha definida com sucesso. Redirecionando...` |
| Error — token inválido | `ERRO_AUTH · TOKEN_INVÁLIDO` / `Link expirado ou inválido. Solicite um novo link de redefinição.` |
| Error — senha curta | `ERRO · SENHA_INVÁLIDA` / `A senha não atende aos requisitos mínimos.` |
| Error — senhas divergentes | `ERRO · SENHAS_DIVERGENTES` / `As senhas não coincidem. Verifique e tente novamente.` |
| Error — supabase | `ERRO_AUTH · 500` / `Ocorreu um erro ao definir a senha. Tente novamente.` |

---

## Validation Contract

### Login
| Rule | Error trigger | Message |
|------|--------------|---------|
| Auth failed | Supabase error on signIn | Banner `ERRO_AUTH · 401` + body |

### Cadastro (client-side, blocks submit)
| Rule | Check | Error message |
|------|-------|--------------|
| Nome obrigatório | `!nome.trim()` | `Informe nome e sobrenome.` |
| Sobrenome obrigatório | `!sobrenome.trim()` | `Informe nome e sobrenome.` |
| E-mail válido | `/\S+@\S+\.\S+/.test(email)` | `Informe um e-mail válido.` |
| Telefone ≥10 dígitos | `telefone.replace(/\D/g,"").length < 10` | `Informe um telefone válido (com DDD).` |
| Senha ≥6 chars | `senha.length < 6` | `A senha deve ter no mínimo 6 caracteres, 1 letra maiúscula e 1 número.` |
| Senha ≥1 maiúscula | `!/[A-Z]/.test(senha)` | `A senha deve ter no mínimo 6 caracteres, 1 letra maiúscula e 1 número.` |
| Senha ≥1 número | `!/[0-9]/.test(senha)` | `A senha deve ter no mínimo 6 caracteres, 1 letra maiúscula e 1 número.` |
| Senhas coincidem | `senha !== confirmarSenha` | `As senhas não coincidem.` |

### Redefinir (define new password — same password policy)
| Rule | Check | Error |
|------|-------|-------|
| Senha ≥6 chars | same | `ERRO · SENHA_INVÁLIDA` banner |
| Senha ≥1 maiúscula | same | `ERRO · SENHA_INVÁLIDA` banner |
| Senha ≥1 número | same | `ERRO · SENHA_INVÁLIDA` banner |
| Senhas coincidem | same | `ERRO · SENHAS_DIVERGENTES` banner |

**Password policy hint placement:** A static text line immediately below the senha field, always visible (not only on error):
```
Mínimo 6 caracteres, 1 letra maiúscula e 1 número.
```
Style: `.r-meta` (10px mono fg-5). Appears below `SENHA` field on signup and below `NOVA SENHA` on reset.

---

## Phone Mask Contract

Format: `(99) 99999-9999` (11 digits = mobile) or `(99) 9999-9999` (10 digits = landline).
Masking: hand-rolled, no library. Strip non-digits on input, re-apply mask on display.
DB storage: digits only (no parentheses, spaces, or hyphens).
Validation threshold: `≥10 digits` unmasked.

```js
function maskPhone(value) {
  const digits = value.replace(/\D/g, "").slice(0, 11)
  if (digits.length <= 10) {
    return digits.replace(/(\d{2})(\d{4})(\d{0,4})/, "($1) $2-$3").replace(/-$/, "")
  }
  return digits.replace(/(\d{2})(\d{5})(\d{0,4})/, "($1) $2-$3").replace(/-$/, "")
}
```

---

## AuthAside Content (Left Panel)

| Element | Spec |
|---------|------|
| Image src | `/hero-building.png` (next/image, `fill`, `objectFit:cover`) |
| Image filter | `grayscale(0.3) contrast(1.1) brightness(0.62)` |
| Gradient | `linear-gradient(180deg, rgba(0,0,0,0.25) 0%, rgba(0,0,0,0.88) 100%)` |
| Border right | `1px solid var(--border-2)` |
| Corner brackets | CornerBrackets component, gold 0.7 opacity |
| Wordmark position | `top:36px left:36px right:36px` |
| Wordmark — tag | `CONSOLE` mono 11px `var(--primary-hover)` tracking-[2px] |
| Wordmark — name | `ROMMA` display 26px weight 700 `var(--fg-1)` tracking-[-0.5px] |
| Eyebrow area | `bottom:56px left:36px right:36px` |
| Eyebrow rule | `w-6 h-px bg-[var(--primary-hover)]` |
| Eyebrow text | `ESPAÇOS COMERCIAIS` body 11px 700 tracking-[3px] uppercase `var(--primary-hover)` |
| Headline | `CONTROLE INABALÁVEL DE CADA`**`.`**`ATIVO` — display 52px 700 tracking-[-2.6px] `var(--fg-1)`. Gold period: `var(--primary-hover)` |
| Body copy | `Gerencie contratos, locatários e parcelas em um único sistema integrado.` `.r-body` maxW 360px `var(--fg-2)` mt-4 |

---

## TopStrip & BottomMeta (Shared)

### TopStrip
- Height: 28px (`h-7`). `flexShrink: 0`. z-index: 2.
- bg: `rgba(18,18,18,0.95)`. `border-bottom: 1px solid var(--border-2)`.
- Left: `INTEGRATED_SYSTEM_NODE: 0X449F` — mono 10px fg-4 tracking-[0.5px].
- Right: `GRID.OS.ALPHA` (desktop only) + `[r-dot] ONLINE` — same style.

### BottomMeta
- `flexShrink: 0`. `border-top: 1px solid var(--border-1)`. padding: 10px 20px.
- Left: `ROMMA © 2026 · CONSOLE v2.4.1` — mono 10px fg-5 tracking-[0.5px].
- Right (desktop only): `SESSION_ID: 0XFF8A-2310 // TLS 1.3` — same style.

---

## Motion Contract

| Element | Animation | Duration | Timing |
|---------|-----------|---------|--------|
| Page entrance | `.r-fade` (translateY 8px → 0) | `var(--dur-base)` = 220ms | `var(--ease-crisp)` |
| Submit button bg change | CSS transition | `var(--dur-base)` = 220ms | — |
| Submit button glow | CSS transition | `var(--dur-base)` = 220ms | — |
| Loading progress bar | `rBar` keyframe (translateX -100% → 260%) | 1s linear infinite | — |
| Field underline/label | CSS transition | `var(--dur-fast)` = 120ms | — |
| Checkbox fill | CSS transition | 150ms | — |
| Banner appearance | no animation (static render — always visible when truthy) | — | — |

`prefers-reduced-motion`: `.r-fade { animation: none }` (already in globals.css).
`@media print`: `.r-fade { animation: none }` (already in globals.css).

---

## State Machine Summary

### Login page states
```
empty → loading → success (→ redirect)
                → error (banner)
empty → reset_loading → reset_sent (banner)
                      → error (banner)
```

### Signup page states
```
empty → loading → sent (success banner, form hidden)
               → error (erroLocal set, banner)
```
On `sent`: hide form fields and SubmitButton. Show success banner. Keep cross-screen link.

### Reset — email request states
```
idle → loading → sent (success banner, field+button hidden)
```

### Reset — new password states
```
idle → loading → success (banner, redirect after 1500ms)
              → error (erro code set, banner shown)
```

---

## Interaction Details

### "Esqueci minha senha" (Login)
- Button type="button". Navigates to `/auth/reset-password`.
- Previous behavior (inline reset on login page) is removed — dedicated screen instead.

### "Sem conta?" / "Já tem conta?" cross-links
- Mono 11px fg-4. Link span: `var(--primary-hover)` underline offset 3px.
- Login → `/signup`. Signup → `/login`.

### Back button (Reset screen)
- Top-left. `← LOGIN`. Mono 11px fg-4 tracking-[0.5px]. `style={{ all: "unset" }}`. Navigates to `/login`.

### Form scroll on overflow
- Form panel uses `.r-scroll` class for scrollbar styling.
- `overflowY: auto` on form panel. No overflow on outer shell.

---

## Copywriting Contract (Template Required Fields)

| Element | Copy |
|---------|------|
| Primary CTA — Login | `ACESSAR SISTEMA` |
| Primary CTA — Cadastro | `CONFIGURAR SISTEMA` |
| Primary CTA — Redefinir | `ENVIAR LINK` / `DEFINIR SENHA` |
| Empty state | Not applicable — auth screens have no empty state |
| Success state — Cadastro | `VERIFIQUE SEU E-MAIL · 200` / `Enviamos um link de ativação. Confirme para liberar o console.` |
| Success state — Redefinir | `E-MAIL_ENVIADO · 200` / `Verifique sua caixa de entrada para redefinir a senha.` |
| Error — auth | `ERRO_AUTH · 401` / `Credenciais inválidas. Verifique e-mail e senha e tente novamente.` |
| Error — validation | `ERRO_VALIDAÇÃO` / _(dynamic per field rule above)_ |
| Destructive actions | None in this phase |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Existing components only — no new shadcn components added in this phase | not required |
| Third-party | None | not applicable |

No new shadcn components are added. All auth UI is custom JSX using `style={{}}` inline patterns + existing `.r-*` classes. No third-party registries declared.

---

## Implementation Notes for Executor

1. **File structure:** Extract shared components to `src/components/auth/AuthFrame.js`, `AuthAside.js`, `CornerBrackets.js`, `AuthField.js`, `AuthBanner.js`, `SubmitButton.js`. All `'use client'`.

2. **Reuse existing:** `TopStrip` and `LeftPanel` exist in `login/page.js` and `signup/page.js` — replace both with the canonical `AuthFrame`/`AuthAside` extracting into the shared directory.

3. **Form state:** Single `useState` object pattern (`const [form, setForm] = useState({...})`). Named reset function.

4. **Action extension:** `cadastrarProprietario` in `src/actions/auth.js` must accept `{ email, senha, nome, sobrenome, telefone }` and pass `nome`/`sobrenome`/`telefone` via `options.data` in `signUp`.

5. **Auth confirm route:** `src/app/auth/confirm/route.js` `tentarRegistrarProprietario` reads `data.user.user_metadata` to persist `nome`/`sobrenome`/`telefone` to `proprietarios` table.

6. **Reset flow:** `/auth/reset-password` must handle two sub-flows:
   - No token params → email request form (new design)
   - Token present (recovery session) → new password form (existing logic, redesigned)

7. **`supabase-browser`** client for all client-side auth calls.

8. **`h-screen overflow-hidden`** on root shell — same as existing login.

9. **Signup fields refLabel sequence:** REF_U_INIT_01 through REF_U_INIT_06 (nome, sobrenome, email, telefone, senha, confirmar-senha).

10. **CornerBrackets offset:** 16px from each panel edge (not 18px — aligned to 4-point grid).

11. **AuthField padding:** 12px top/bottom (not 13px — aligned to 4-point grid). Visually imperceptible difference from handoff prototype.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
