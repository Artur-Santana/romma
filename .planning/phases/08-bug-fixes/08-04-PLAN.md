---
phase: 08-bug-fixes
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/auth/confirm/route.js
autonomous: true
requirements: [BUG-03]
must_haves:
  truths:
    - "Após aceitar o convite (verifyOtp type=invite bem-sucedido), locatarios.status_convite vira 'aceito' para o usuario_id"
    - "O card do Locatário no dashboard exibe 'Convite aceito' (não 'Convite pendente') após o aceite"
    - "O caminho code/exchangeCodeForSession também atualiza status_convite (idempotente)"
  artifacts:
    - path: "src/app/auth/confirm/route.js"
      provides: "UPDATE locatarios SET status_convite='aceito' via supabaseAdmin após verificação de invite"
      contains: "status_convite"
  key_links:
    - from: "src/app/auth/confirm/route.js"
      to: "locatarios.status_convite"
      via: "supabaseAdmin.update por usuario_id"
      pattern: "status_convite: 'aceito'"
---

<objective>
Corrigir BUG-03 (causa raiz mais crítica): `/auth/confirm/route.js` chama verifyOtp e redireciona ao portal, mas NUNCA atualiza `locatarios.status_convite` de 'pendente' → 'aceito' (D-08). Todos os locatários permanecem 'pendente' no DB, fazendo o badge exibir estado incorreto e o botão REVOGAR aparecer para locatários ativos. Fix: após a verificação bem-sucedida do invite, usar `supabaseAdmin` para executar o UPDATE (D-09).

Purpose: Badge de convite reflete o estado real; BUG-01 fica consistente (locatários ativos deixam de mostrar REVOGAR, conforme D-10).
Output: route.js com UPDATE de status_convite após aceite, em ambos os caminhos de invite (token_hash e code).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bug-fixes/08-RESEARCH.md
@.planning/phases/08-bug-fixes/08-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: UPDATE status_convite após aceite de invite em ambos os caminhos (D-08, D-09, D-10)</name>
  <read_first>
    - src/app/auth/confirm/route.js (estado atual COMPLETO: imports linhas 1-2; bloco token_hash linhas 12-23 desestrutura só `{ error }` de verifyOtp e nunca atualiza status_convite; bloco code linhas 25-32 usa exchangeCodeForSession; ambos redirecionam para /portal/dashboard sem tocar locatarios)
    - src/lib/supabaseAdmin.js (export default supabaseAdmin com 'server-only' — seguro em route handlers)
    - src/actions/locatarios.js (padrão de UPDATE via supabaseAdmin: `supabaseAdmin.from('locatarios').update({ ... }).eq('id', id)` linha 65 e `.eq('usuario_id', ...)` no insert linha 33; coluna usuario_id é a FK para auth)
    - .planning/phases/08-bug-fixes/08-PATTERNS.md (seção route.js — import a adicionar, mudança de desestruturação, bloco de update)
    - .planning/phases/08-bug-fixes/08-RESEARCH.md (Pitfall 2 — verifyOtp pode retornar data.user; usar `const { data, error }`. Assumption A2 — se data.user for null, fallback por email)
  </read_first>
  <files>src/app/auth/confirm/route.js</files>
  <action>
    Em `src/app/auth/confirm/route.js`:

    (1) Adicionar import: `import supabaseAdmin from "@/lib/supabaseAdmin"` junto aos imports existentes (linhas 1-2). Usar o path exato `@/lib/supabaseAdmin` (RESEARCH anti-pattern: não usar path alternativo).

    (2) Caminho token_hash (linhas 12-23): trocar a desestruturação `const { error } = await supabase.auth.verifyOtp({ type, token_hash })` por `const { data, error } = await supabase.auth.verifyOtp({ type, token_hash })`. Após o guard de erro (que redireciona em caso de error), e ANTES dos redirects de recovery/portal, inserir: se `type === 'invite'` e `data?.user`, executar `await supabaseAdmin.from('locatarios').update({ status_convite: 'aceito' }).eq('usuario_id', data.user.id)`. Fallback (Discretion + Assumption A2): se `data?.user` for null mas o verifyOtp não deu erro, NÃO travar o fluxo — registrar via console e seguir o redirect (a transição pode ser feita manualmente; não regredir o redirect que já funciona).

    (3) Caminho code (linhas 25-32): `exchangeCodeForSession` é o segundo ponto de entrada de invite (template customizado com {{ .Code }}). Após a troca bem-sucedida e antes do redirect para /portal/dashboard, buscar o usuário autenticado via `const { data: { user } } = await supabase.auth.getUser()` e, se houver user, executar o MESMO update: `await supabaseAdmin.from('locatarios').update({ status_convite: 'aceito' }).eq('usuario_id', user.id)`. O UPDATE é idempotente (escopado por usuario_id em linha existente; rodar para um locatário já 'aceito' ou para um proprietário que não tem linha em locatarios não causa dano — 0 rows afetadas). Como o caminho code não tem `type`, aplicar o update incondicionalmente após o exchange (qualquer aceite que chegue por code é de convite neste sistema).

    (4) Não alterar a lógica de redirect existente nem o tratamento de erro. JavaScript puro (sem TypeScript). Manter a variável `erro`/`error` conforme o arquivo já usa (o arquivo usa `error` em inglês — manter para não quebrar; não renomear).
  </action>
  <verify>
    <automated>grep -q "import supabaseAdmin" src/app/auth/confirm/route.js && grep -q "status_convite: 'aceito'" src/app/auth/confirm/route.js && grep -q "const { data, error } = await supabase.auth.verifyOtp" src/app/auth/confirm/route.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Existe `import supabaseAdmin from "@/lib/supabaseAdmin"`
    - verifyOtp é desestruturado como `const { data, error }` (não só `{ error }`)
    - No caminho token_hash, quando `type === 'invite'` e `data?.user`, executa `supabaseAdmin.from('locatarios').update({ status_convite: 'aceito' }).eq('usuario_id', data.user.id)`
    - No caminho code, após exchangeCodeForSession bem-sucedido, executa o mesmo update usando o user de `supabase.auth.getUser()`
    - O update usa coluna `usuario_id` (não `id`) no `.eq()` — é a FK para auth.users
    - Os redirects existentes (recovery → /auth/reset-password; sucesso → /portal/dashboard; erro → /login?error=invite_invalid) permanecem inalterados
    - O cenário BUG-03 de e2e/auth-confirm.spec.js (Plan 01) passa OU o SUMMARY registra explicitamente o resultado (token real funcionou / fallback manual)
  </acceptance_criteria>
  <done>status_convite vira 'aceito' após aceite de convite em ambos os caminhos; badge do dashboard reflete o estado real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador (link de invite) → route handler /auth/confirm | token_hash/code não confiáveis cruzam para o servidor; validados por verifyOtp/exchangeCodeForSession antes de qualquer UPDATE |
| route handler → DB (locatarios) | UPDATE com service role (supabaseAdmin) — bypass de RLS intencional para escrita server-side pós-autenticação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | UPDATE status_convite via service role | mitigate | UPDATE só ocorre APÓS verifyOtp/exchangeCodeForSession bem-sucedido; escopado por usuario_id do user autenticado — não aceita id arbitrário do cliente |
| T-08-08 | Spoofing | aceitar invite de terceiro | mitigate | usuario_id vem de `data.user`/`getUser()` (sessão verificada), não de parâmetro de query |
| T-08-09 | Elevation of Privilege | supabaseAdmin em route handler | accept | supabaseAdmin tem 'server-only'; route handler é server-side; bypass de RLS necessário pois locatário ainda não tem sessão com RLS sobre própria linha |
</threat_model>

<verification>
- route.js importa supabaseAdmin e faz update status_convite='aceito' em ambos os caminhos
- verifyOtp desestruturado como { data, error }
- `npx playwright test e2e/auth-confirm.spec.js --project=chromium` — cenário de aceite verde, OU SUMMARY documenta o resultado do token real
</verification>

<success_criteria>
ROADMAP Success Criteria 3 atendido: card de Locatário exibe "Convite aceito"/"Convite pendente" conforme estado real. Consistência com BUG-01 (D-10): locatários ativos não mostram mais REVOGAR.
</success_criteria>

<output>
Create `.planning/phases/08-bug-fixes/08-04-SUMMARY.md` when done. Registrar explicitamente se o teste de token real (Plan 01 Task 2) passou ou caiu no fallback manual.
</output>
