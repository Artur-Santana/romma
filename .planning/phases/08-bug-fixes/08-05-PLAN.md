---
phase: 08-bug-fixes
plan: 05
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-04"]
files_modified: []
autonomous: false
requirements: [BUG-01, BUG-02, BUG-03, BUG-04]
must_haves:
  truths:
    - "Os 4 fixes são confirmados visualmente/funcionalmente em uma passada de auditoria isolada das telas trabalhadas"
    - "A suite E2E completa passa verde antes do fechamento da fase"
  artifacts: []
  key_links: []
---

<objective>
Wave 3: deep-dive de auditoria isolada das telas trabalhadas (AUDIT-01) e verificação humana visual dos 4 fixes antes de fechar a fase. As plans 02/03/04 entregam código com testes automatizados; este checkpoint confirma o comportamento visual/funcional que os testes E2E não capturam plenamente (renderização de erro inline, flip do badge, navegação do link). Também roda a suite completa como phase gate.

Purpose: Satisfazer AUDIT-01 (deep-dive por tela) e dar confirmação visual antes de /gsd-verify-work.
Output: Confirmação humana + suite E2E verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-bug-fixes/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: rodar suite E2E completa (phase gate)</name>
  <read_first>
    - playwright.config.js (config da suite)
    - .planning/phases/08-bug-fixes/08-RESEARCH.md (Validation Architecture — comandos de suite)
  </read_first>
  <files>(nenhum — apenas execução de testes)</files>
  <action>
    Rodar a suite E2E completa para o projeto chromium: `npx playwright test --project=chromium`. Todos os cenários — incluindo os novos da Plan 01 (revogar, split de erro, ← Voltar, status_convite) — devem passar. Se algum cenário de BUG-03 estiver marcado como verificação manual (fallback da Plan 01/04), registrar isso e NÃO contá-lo como falha de gate, mas sinalizar para a verificação humana da Task 2.
  </action>
  <verify>
    <automated>npx playwright test --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --project=chromium` termina sem falhas (exit 0), exceto cenários explicitamente marcados como verificação manual de BUG-03
    - Os cenários de BUG-01 (revogar + erro inline), BUG-02 (split de erro) e BUG-04 (← Voltar) passam
  </acceptance_criteria>
  <done>Suite E2E verde (descontados itens de verificação manual documentados).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: auditoria visual dos 4 fixes (AUDIT-01)</name>
  <action>Verificação humana — sem alterações de código. O executor apresenta os passos de how-to-verify ao usuário e aguarda o resume-signal. Trata qualquer problema reportado como gap/FIX-01.</action>
  <what-built>
    Os 4 bug fixes da fase 08: revogar acesso com erro inline (BUG-01), separação de erro delete/edit em unidades (BUG-02), atualização de status de convite (BUG-03), link ← Voltar em /unidades (BUG-04).
  </what-built>
  <how-to-verify>
    Rodar a app em dev (`npm run dev`) e verificar, tela a tela:

    1. BUG-04 (mais rápido): abrir /unidades sem login. Confirmar que no topo, no lugar onde dizia "Unidades Disponíveis", há agora um link "← Voltar" (mono, uppercase, cinza). Clicar → deve navegar para a home /. O RealtimeDot continua à direita.

    2. BUG-02: logar como Proprietário → /dashboard/unidades. Tentar deletar uma unidade que tem contrato (a 'Sala 101' seedada, status alugada). Confirmar que a mensagem de erro aparece num bloco ACIMA da lista de cards (barra vermelha à esquerda), NÃO dentro de um card. Depois clicar "Editar" em OUTRA unidade — confirmar que a mensagem de erro de delete NÃO aparece dentro do card de edição.

    3. BUG-01: em /dashboard/locatarios. Para um locatário pendente COM contrato vinculado, clicar REVOGAR → confirmar que aparece a mensagem inline "Locatário tem contratos vinculados — encerre-os antes de revogar." na tabela (logo abaixo do header), e NÃO um popup/alert do browser. Para um locatário pendente SEM contrato, REVOGAR → a linha some da lista. Abrir o modal "Convidar Locatário" logo após um erro de revogar — confirmar que o erro de revogar NÃO aparece dentro do modal de convite.

    4. BUG-03: aceitar um convite real (ou usar o fluxo de invite). Após o aceite e login do locatário, voltar ao dashboard como Proprietário → /dashboard/locatarios. Confirmar que o badge desse locatário mostra "Convite aceito" (verde) e o botão REVOGAR NÃO aparece mais para ele (mostra Editar / VER → em vez disso).
  </how-to-verify>
  <resume-signal>Digite "approved" se os 4 fixes se comportam como descrito, ou descreva os problemas encontrados (serão tratados como gaps/FIX-01).</resume-signal>
</task>

</tasks>

<verification>
- Suite E2E verde (descontados itens de verificação manual de BUG-03)
- Confirmação humana dos 4 comportamentos visuais
</verification>

<success_criteria>
Todos os 4 ROADMAP Success Criteria confirmados visual e automaticamente. AUDIT-01 satisfeito por deep-dive tela a tela. Quaisquer correções emergentes registradas como FIX-01.
</success_criteria>

<output>
Create `.planning/phases/08-bug-fixes/08-05-SUMMARY.md` when done. Registrar resultado da auditoria e qualquer FIX-01 emergente.
</output>
