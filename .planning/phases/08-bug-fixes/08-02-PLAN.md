---
phase: 08-bug-fixes
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/actions/locatarios.js
  - src/components/features/LocatariosDesktop.js
autonomous: true
requirements: [BUG-01]
must_haves:
  truths:
    - "Proprietário clica REVOGAR em locatário pendente sem contrato e a ação completa — o locatário some da lista"
    - "Proprietário clica REVOGAR em locatário pendente COM contrato vinculado e vê erro descritivo inline na tabela (não alert)"
    - "Abrir o modal de convite após um erro de revogar não exibe o erro stale dentro do modal"
  artifacts:
    - path: "src/actions/locatarios.js"
      provides: "revogarConvite com checagem de FK em contratos antes do delete"
      contains: "contratos vinculados"
    - path: "src/components/features/LocatariosDesktop.js"
      provides: "Erro de revogar renderizado inline na tabela via setErro (sem alert)"
      contains: "border-t border-border-3"
  key_links:
    - from: "src/components/features/LocatariosDesktop.js"
      to: "revogarConvite"
      via: "handleRevogar → setErro inline"
      pattern: "setErro\\(erroMessage"
    - from: "src/actions/locatarios.js"
      to: "contratos"
      via: "count check por locatario_id"
      pattern: "from\\('contratos'\\)"
---

<objective>
Corrigir BUG-01: revogar acesso de Locatário falha por FK violation (locatário com contrato vinculado) e o erro é exibido via alert() bloqueante. Fix em duas camadas: (1) a Server Action `revogarConvite` checa se há contratos vinculados antes do delete e retorna erro descritivo (D-01, D-02); (2) o componente troca alert() por erro inline na tabela (D-03).

Purpose: Demo pode revogar convites sem erro confuso; avaliador vê mensagem clara quando há contrato vinculado.
Output: revogarConvite com FK-check; LocatariosDesktop com erro inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bug-fixes/08-RESEARCH.md
@.planning/phases/08-bug-fixes/08-PATTERNS.md
@.planning/phases/08-bug-fixes/08-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: revogarConvite checa contratos vinculados antes do delete (D-01, D-02)</name>
  <read_first>
    - src/actions/locatarios.js (função revogarConvite linhas 91-106 — estado atual: guards de auth/UUID/status_convite, depois delete direto sem checar FK)
    - src/lib/queries-client.js (padrão countRegistros: `.select('*', { count: 'exact', head: true }).eq(...)`)
    - .planning/phases/08-bug-fixes/08-PATTERNS.md (seção locatarios.js — bloco "Fix a inserir")
    - .planning/phases/08-bug-fixes/08-RESEARCH.md (Common Pitfalls 3 — FK em contratos.locatario_id é REFERENCES sem ON DELETE CASCADE; checar QUALQUER contrato, sem filtro de status)
  </read_first>
  <files>src/actions/locatarios.js</files>
  <action>
    Em `src/actions/locatarios.js`, na função `revogarConvite`, inserir uma checagem de contratos vinculados IMEDIATAMENTE após o guard `if (loc.status_convite !== 'pendente')` (linha 100) e ANTES do `supabaseAdmin.from('locatarios').delete()` (linha 101).

    A checagem usa supabaseAdmin sobre a tabela `contratos`, contando linhas com `locatario_id` igual ao `id` do locatário, SEM filtro de status (a FK `contratos.locatario_id REFERENCES locatarios(id)` não tem ON DELETE CASCADE — qualquer contrato, ativo/encerrado/cancelado, bloqueia o delete; verificado em RESEARCH Common Pitfalls 3). Forma: `.from('contratos').select('*', { count: 'exact', head: true }).eq('locatario_id', id)`.

    Tratamento: se houver erro na query de count, retornar `{ status: 500, erroMessage: countErr.message }`. Se `count > 0`, retornar exatamente `{ status: 400, erroMessage: 'Locatário tem contratos vinculados — encerre-os antes de revogar.' }` (D-02 — copy exato). Manter o padrão de retorno do projeto `{ status, erroMessage }` (erroMessage, não errorMessage). Não alterar os guards existentes nem o delete subsequente.
  </action>
  <verify>
    <automated>grep -q "contratos vinculados — encerre-os antes de revogar" src/actions/locatarios.js && grep -q "from('contratos')" src/actions/locatarios.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - revogarConvite contém uma query `.from('contratos').select('*', { count: 'exact', head: true }).eq('locatario_id', id)`
    - A checagem aparece DEPOIS do guard `status_convite !== 'pendente'` e ANTES do `delete()` em locatarios
    - Quando count > 0 retorna `{ status: 400, erroMessage: 'Locatário tem contratos vinculados — encerre-os antes de revogar.' }` (string idêntica)
    - Nenhum filtro `.eq('status', ...)` na query de contratos (qualquer contrato bloqueia)
    - Retorno usa chave `erroMessage` (não `errorMessage`)
  </acceptance_criteria>
  <done>revogarConvite retorna erro descritivo quando o locatário tem contratos vinculados; sem FK violation crua.</done>
</task>

<task type="auto">
  <name>Task 2: erro de revogar inline na tabela + limpeza de stale error (D-03)</name>
  <read_first>
    - src/components/features/LocatariosDesktop.js (handleRevogar linha 91-98 usa alert(); estado `erro` único linha 34; header da tabela termina linha 118; modal de convite abre via setShowInviteForm(true) linhas 108 e 205 SEM limpar erro; bloco {erro && ...} no modal de convite linha 379)
    - .planning/phases/08-bug-fixes/08-UI-SPEC.md (BUG-01: erro inline `font-mono text-[11px] text-danger-fg` abaixo do header da tabela; sem confirmação no clique)
    - .planning/phases/08-bug-fixes/08-PATTERNS.md (seção LocatariosDesktop.js — bloco JSX de erro e limpeza de erro)
    - .planning/phases/08-bug-fixes/08-RESEARCH.md (Pitfall 1 — estado `erro` compartilhado; reusar `erro`, não criar erroRevogar)
  </read_first>
  <files>src/components/features/LocatariosDesktop.js</files>
  <action>
    Em `src/components/features/LocatariosDesktop.js`:

    (1) Em `handleRevogar` (linhas 91-98): adicionar `setErro("")` no início da função (antes da chamada a revogarConvite). Substituir `alert(erroMessage ?? "Erro ao revogar convite.")` por `setErro(erroMessage ?? "Erro ao revogar convite.")` (D-03). Reusar o estado `erro` existente (linha 34) — NÃO criar estado novo erroRevogar (RESEARCH anti-pattern).

    (2) Inserir um bloco JSX de erro inline na tabela, logo APÓS o div do header da tabela (após linha 118, antes da renderização das rows linha 121). Bloco: `{erro && (<div className="px-5 py-2 font-mono text-[11px] text-danger-fg border-t border-border-3">{erro}</div>)}`. Classes conforme UI-SPEC.

    (3) REGRESSÃO CRÍTICA: como `erro` é compartilhado com os modais (bloco {erro && ...} dentro do modal de convite linha 379 e do modal de edição linha 281), um erro de revogar ficaria visível ao abrir o modal de convite. Adicionar `setErro("")` no onClick que abre o modal de convite — ambos os pontos: o CTA do PageHeader (linha ~108, `onClick: () => setShowInviteForm(true)`) e o botão "Convidar →" do callout (linha ~205, `onClick={() => setShowInviteForm(true)}`). Trocar para `() => { setErro(""); setShowInviteForm(true) }`. O modal de edição já limpa erro em handleEditarLocatario (linha 62), não precisa de mudança.

    Não adicionar confirmação (confirm dialog) — UI-SPEC diz que a ação executa diretamente ao clicar (comportamento intencional pré-existente).
  </action>
  <verify>
    <automated>! grep -q "alert(" src/components/features/LocatariosDesktop.js && grep -q "px-5 py-2 font-mono text-\[11px\] text-danger-fg border-t border-border-3" src/components/features/LocatariosDesktop.js && npx playwright test e2e/crud.spec.js -g "BUG-01" --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "alert(" src/components/features/LocatariosDesktop.js` retorna 0 (alert removido)
    - handleRevogar chama `setErro` no ramo de falha e `setErro("")` no início
    - Existe bloco JSX com a className completa exata `px-5 py-2 font-mono text-[11px] text-danger-fg border-t border-border-3` (não apenas `border-t border-border-3`, que já existe no divisor de linhas) posicionado após o header da tabela e antes das rows
    - Ambos os onClick que chamam `setShowInviteForm(true)` também chamam `setErro("")` (limpeza de stale error antes de abrir o modal de convite)
    - Nenhum estado novo `erroRevogar` foi criado — reusa `erro`
    - Os cenários BUG-01 de e2e/crud.spec.js (Plan 01) passam: `npx playwright test e2e/crud.spec.js -g "BUG-01" --project=chromium` (NÃO rodar o arquivo inteiro — os cenários BUG-02 são corrigidos pela Plan 03, paralela)
  </acceptance_criteria>
  <done>Erro de revogar aparece inline na tabela; alert removido; sem vazamento de erro stale para o modal de convite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (LocatariosDesktop) → Server Action (revogarConvite) | Input `id` (UUID) cruza para a server action; validado por UUID_RE e guard isProprietario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Elevation of Privilege | revogarConvite | mitigate | Guard `isProprietario(supabase)` já presente (linha 95) — não remover |
| T-08-03 | Information Disclosure | erro de FK exposto ao cliente | mitigate | Retornar mensagem amigável fixa ("Locatário tem contratos vinculados…"), não o erro raw do Postgres, quando count > 0 |
| T-08-04 | Input Validation | id na action | mitigate | UUID_RE já valida `id` (linha 96) — manter |
</threat_model>

<verification>
- `grep -c "alert(" src/components/features/LocatariosDesktop.js` == 0
- revogarConvite tem checagem de contratos com a copy exata de D-02
- `npx playwright test e2e/crud.spec.js -g "BUG-01" --project=chromium` verde (apenas cenários BUG-01; não rodar o arquivo inteiro)
</verification>

<success_criteria>
Success Criteria 1 do ROADMAP atendido: Proprietário revoga acesso de locatário pendente sem contrato e ele some da lista; locatário com contrato mostra erro descritivo inline (não alert).
</success_criteria>

<output>
Create `.planning/phases/08-bug-fixes/08-02-SUMMARY.md` when done.
</output>
