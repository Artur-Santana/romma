---
phase: 08-bug-fixes
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/features/Unidades.js
  - src/components/features/UnidadesPublicas.js
autonomous: true
requirements: [BUG-02, BUG-04]
must_haves:
  truths:
    - "Erro ao deletar unidade aparece no nível da lista (acima dos cards), não dentro do form de edição"
    - "Erro ao salvar edição aparece dentro do UnidadeCard em edição, não no nível da lista"
    - "Abrir form de edição após erro de delete não mostra o erro de delete dentro do card"
    - "Visitante em /unidades clica '← Voltar' e navega para a home /"
  artifacts:
    - path: "src/components/features/Unidades.js"
      provides: "Estados erroDelete e erroEdit separados; erroDelete no nível da lista, erroEdit via prop"
      contains: "erroDelete"
    - path: "src/components/features/UnidadesPublicas.js"
      provides: "Link ← Voltar para / no header"
      contains: "← Voltar"
  key_links:
    - from: "src/components/features/Unidades.js"
      to: "UnidadeCard"
      via: "prop erro={erroEdit}"
      pattern: "erro=\\{erroEdit\\}"
    - from: "src/components/features/UnidadesPublicas.js"
      to: "/"
      via: "next/link Link href"
      pattern: "href=\"/\""
---

<objective>
Corrigir dois bugs em arquivos disjuntos da Plan 02:
- BUG-02: Unidades.js usa estado `erro` único (linha 35) para delete E edit; um erro de delete vaza para dentro do UnidadeCard em edição via prop `erro`. Fix: separar em `erroDelete` (nível lista) e `erroEdit` (prop do card) (D-04 a D-07).
- BUG-04: /unidades não tem link de volta à home. Fix: trocar o `<span>` "Unidades Disponíveis" do header por `<Link href="/">← Voltar</Link>` (D-11 a D-13).

Purpose: Edição de unidade não exibe erro de FK que pertence ao delete; visitante consegue voltar à home a partir de /unidades.
Output: Unidades.js com split de erro; UnidadesPublicas.js com link de volta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bug-fixes/08-PATTERNS.md
@.planning/phases/08-bug-fixes/08-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: split de estado de erro em Unidades.js (D-04 a D-07)</name>
  <read_first>
    - src/components/features/Unidades.js (estado único `erro` linha 35; handleDeletarUnidade linhas 81-89; handleSalvarUnidade linhas 91-101; handleEditarUnidade linhas 69-79; insertUnidade linhas 112-126 usa erro do form de criação; bloco {erro && ...} do form de criação linhas 251-255; prop erro={erro} no UnidadeCard linha 286; div da lista linha 271)
    - src/components/ui/UnidadeCard.js (recebe prop `erro` e renderiza no estado isEditing — NÃO MODIFICAR este arquivo, só consumir a prop correta)
    - .planning/phases/08-bug-fixes/08-PATTERNS.md (seção Unidades.js — substituição de estado, limpeza por handler, prop erro={erroEdit}, render de erroDelete)
    - .planning/phases/08-bug-fixes/08-UI-SPEC.md (BUG-02 — slot de erro de delete `bg-[var(--danger-bg2)] border-l-2 border-l-danger-fg px-4 py-3 font-mono text-[13px] text-danger-fg mb-4` acima da lista)
  </read_first>
  <files>src/components/features/Unidades.js</files>
  <action>
    Em `src/components/features/Unidades.js`:

    (1) ATENÇÃO ao escopo do estado `erro` atual: ele é usado em TRÊS fluxos — criar (insertUnidade, bloco JSX do form linhas 251-255), deletar (handleDeletarUnidade) e editar (handleSalvarUnidade, via prop erro={erro} no card). A decisão D-04/D-05/D-06 separa apenas delete e edit. Manter o erro do form de CRIAÇÃO funcionando: substituir o estado único `erro` (linha 35) por DOIS estados: `const [erroDelete, setErroDelete] = useState(null)` e `const [erroEdit, setErroEdit] = useState(null)`. O bloco de erro do form de criação (linhas 251-255) atualmente lê `erro` — repontá-lo para `erroEdit` é incorreto (criação ≠ edição). Como `insertUnidade` é um fluxo de criação que compartilhava o mesmo estado, repontar o erro de criação para `erroEdit` mantém a semântica "erro de formulário de unidade" (criar e editar são ambos form de unidade) e evita um terceiro estado. Usar `erroEdit` para insertUnidade (linhas 115, 123) e para handleSalvarUnidade; usar `erroDelete` para handleDeletarUnidade.

    (2) Limpeza (D-07): no início de `handleDeletarUnidade` chamar `setErroDelete(null); setErroEdit(null)`. No início de `handleSalvarUnidade` chamar `setErroDelete(null); setErroEdit(null)`. No início de `handleEditarUnidade` chamar `setErroDelete(null)` (Discretion — previne erro de delete stale ao abrir card de edição). No início de `insertUnidade` chamar `setErroDelete(null); setErroEdit(null)`.

    (3) Atribuição: handleDeletarUnidade ramo de falha → `setErroDelete(result.erroMessage)`; handleSalvarUnidade ramo de falha → `setErroEdit(result.erroMessage)`; insertUnidade ramo de falha → `setErroEdit(result.erroMessage)`. Remover as chamadas `setErro(null)` dos ramos de sucesso (substituídas pela limpeza no início).

    (4) Render de erroDelete: inserir IMEDIATAMENTE ACIMA do `<div className="flex flex-col gap-0 border border-border-3 bg-surface">` (linha 271) o bloco: `{erroDelete && (<div className="bg-[var(--danger-bg2)] border-l-2 border-l-danger-fg px-4 py-3 font-mono text-[13px] text-danger-fg mb-4">{erroDelete}</div>)}`.

    (5) Render do bloco de erro do form de criação (linhas 251-255): trocar `{erro && ...}` por `{erroEdit && ...}` (mesmo bloco, lendo erroEdit). Manter as classes existentes.

    (6) Prop do UnidadeCard (linha 286): trocar `erro={erro}` por `erro={erroEdit}` (D-05/D-06: nunca passar erroDelete para o card). NÃO modificar UnidadeCard.js.
  </action>
  <verify>
    <automated>grep -q "erroDelete" src/components/features/Unidades.js && grep -q "erro={erroEdit}" src/components/features/Unidades.js && ! grep -qE "\[erro, setErro\]" src/components/features/Unidades.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Existem dois estados: `const [erroDelete, setErroDelete]` e `const [erroEdit, setErroEdit]`
    - O estado único `const [erro, setErro] = useState(null)` foi removido (grep `\[erro, setErro\]` retorna 0)
    - handleDeletarUnidade seta `setErroDelete(result.erroMessage)` no ramo de falha
    - handleSalvarUnidade seta `setErroEdit(result.erroMessage)` no ramo de falha
    - UnidadeCard recebe `erro={erroEdit}` (nunca erroDelete)
    - O bloco erroDelete usa classes `bg-[var(--danger-bg2)] border-l-2 border-l-danger-fg px-4 py-3 font-mono text-[13px] text-danger-fg mb-4` e está acima do div da lista
    - handleDeletarUnidade, handleSalvarUnidade, handleEditarUnidade e insertUnidade limpam erros no início (setErroDelete(null) / setErroEdit(null) conforme D-07)
    - UnidadeCard.js NÃO foi modificado
    - Os cenários BUG-02 de e2e/crud.spec.js (Plan 01) passam: `npx playwright test e2e/crud.spec.js -g "BUG-02" --project=chromium` (NÃO rodar o arquivo inteiro — os cenários BUG-01 são corrigidos pela Plan 02, paralela)
  </acceptance_criteria>
  <done>Erro de delete renderiza no nível da lista; erro de edit/criação no card/form; sem vazamento entre fluxos.</done>
</task>

<task type="auto">
  <name>Task 2: link ← Voltar em /unidades (D-11, D-12, D-13)</name>
  <read_first>
    - src/components/features/UnidadesPublicas.js (imports linhas 1-8 — SEM import de next/link; header linhas 79-89; `<span className="font-mono text-[11px] text-fg-5 tracking-[1px] uppercase">Unidades Disponíveis</span>` linhas 81-83; `<RealtimeDot />` linha 84; `<h1>` "Unidades\nDisponíveis." linhas 86-88 permanece)
    - src/components/ui/Header.js (padrão de import e uso de next/link Link)
    - .planning/phases/08-bug-fixes/08-UI-SPEC.md (BUG-04 — classes exatas, copy "← Voltar", não usar .eyebrow, href="/")
    - .planning/phases/08-bug-fixes/08-PATTERNS.md (seção UnidadesPublicas.js — import e substituição)
  </read_first>
  <files>src/components/features/UnidadesPublicas.js</files>
  <action>
    Em `src/components/features/UnidadesPublicas.js`:

    (1) Adicionar o import `import Link from 'next/link'` junto aos imports existentes (linhas 1-8). O arquivo ainda não importa next/link.

    (2) No header (`<div className="flex justify-between items-center">` linha 80), substituir o `<span>` "Unidades Disponíveis" (linhas 81-83) por: `<Link href="/" className="font-mono text-[11px] text-fg-4 tracking-[1px] uppercase hover:text-fg-2 transition-colors">← Voltar</Link>` (D-11, D-12 — copy exato "← Voltar", classes exatas da UI-SPEC, text-fg-4 não text-fg-5, href="/" home pública). NÃO usar a classe `.eyebrow` (display:block quebra o layout inline — UI-SPEC).

    (3) `<RealtimeDot />` (linha 84) permanece sem alteração no lado direito do mesmo flex row (D-13). O `<h1>` "Unidades\nDisponíveis." (linhas 86-88) permanece — o título da página não depende do span removido.
  </action>
  <verify>
    <automated>grep -q "import Link from 'next/link'" src/components/features/UnidadesPublicas.js && grep -q "← Voltar" src/components/features/UnidadesPublicas.js && grep -q 'href="/"' src/components/features/UnidadesPublicas.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Existe `import Link from 'next/link'` no topo do arquivo
    - O `<span>` "Unidades Disponíveis" foi removido do header (grep "Unidades Disponíveis" não retorna o span no flex justify-between — o texto pode permanecer no h1)
    - Existe `<Link href="/" className="font-mono text-[11px] text-fg-4 tracking-[1px] uppercase hover:text-fg-2 transition-colors">← Voltar</Link>`
    - Copy é exatamente "← Voltar" e href é exatamente "/"
    - A classe `.eyebrow` NÃO é usada no link
    - `<RealtimeDot />` permanece no header
    - O cenário BUG-04 de e2e/dashboard-smoke.spec.js (Plan 01) passa: `npx playwright test e2e/dashboard-smoke.spec.js --project=chromium`
  </acceptance_criteria>
  <done>Visitante em /unidades vê e clica "← Voltar" e navega para /.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitante → /unidades (público) | Página pública sem auth; o link de navegação não cruza dado sensível |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | mensagem de erro de delete exposta ao cliente | accept | erroDelete exibe `result.erroMessage` da action; a action de unidades já controla o que retorna — sem PII; aceitar |
| T-08-06 | (n/a) | BUG-04 link puramente client-side de navegação | accept | Sem fronteira de confiança nova; navegação para rota pública / |
</threat_model>

<verification>
- Unidades.js: dois estados separados, prop erro={erroEdit}, bloco erroDelete acima da lista
- UnidadesPublicas.js: import Link, "← Voltar", href="/"
- `npx playwright test e2e/crud.spec.js -g "BUG-02" --project=chromium` verde (apenas BUG-02) e `npx playwright test e2e/dashboard-smoke.spec.js --project=chromium` verde
</verification>

<success_criteria>
ROADMAP Success Criteria 2 (erro de edit separado do de delete) e 4 (link de volta funcional) atendidos.
</success_criteria>

<output>
Create `.planning/phases/08-bug-fixes/08-03-SUMMARY.md` when done.
</output>
