---
phase: 08-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/crud.spec.js
  - e2e/dashboard-smoke.spec.js
  - e2e/auth-confirm.spec.js
autonomous: true
requirements: [BUG-01, BUG-02, BUG-03, BUG-04]
must_haves:
  truths:
    - "Existe um cenário E2E que falha ANTES do fix de BUG-01 (revogar) e passa depois"
    - "Existe um cenário E2E que falha ANTES do fix de BUG-04 (← Voltar) e passa depois"
    - "Existe um teste que verifica status_convite='aceito' após aceite de convite (BUG-03), usando token real via admin generateLink"
    - "Existe cobertura que distingue erro de delete (nível lista) de erro de edit (modal/card) em Unidades (BUG-02)"
  artifacts:
    - path: "e2e/crud.spec.js"
      provides: "Cenários RED para BUG-01 (revogar) e BUG-02 (split de erro delete/edit)"
      contains: "REVOGAR"
    - path: "e2e/dashboard-smoke.spec.js"
      provides: "Cenário RED para BUG-04 (link ← Voltar em /unidades navega para /)"
      contains: "Voltar"
    - path: "e2e/auth-confirm.spec.js"
      provides: "Cenário para BUG-03 (status_convite vira 'aceito' após verifyOtp com token real)"
      contains: "status_convite"
  key_links:
    - from: "e2e/auth-confirm.spec.js"
      to: "admin.auth.admin.generateLink"
      via: "geração de token_hash real de invite"
      pattern: "generateLink"
---

<objective>
Wave 0: criar os cenários de teste que provam (por falha) que cada um dos 4 bugs existe ANTES dos fixes. Sem esta wave, os testes E2E existentes passam independentemente do fix (RESEARCH "Wave 0 Gaps" confirma que crud.spec.js não exercita revogar nem split de erro, e auth-confirm.spec.js só testa redirects de erro). Isto evita verificação falso-verde nas plans de fix (02/03/04).

Purpose: Garantir que cada `<verify>` das plans de fix tenha um teste que estava RED antes do fix.
Output: 3 spec files com cenários novos. Os cenários de BUG-01/02/04 DEVEM falhar quando rodados contra o código atual (sem fix). O cenário de BUG-03 valida a transição de status via token real de invite gerado pela admin API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bug-fixes/08-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cenários RED para BUG-01 (revogar) e BUG-02 (split de erro) em crud.spec.js</name>
  <read_first>
    - e2e/crud.spec.js (estado atual — entender estrutura de describe/test, login, seletores)
    - e2e/seed.mjs (entender que o locatário seedado tem usuario_id, status_convite default 'pendente', e um contrato ativo vinculado)
    - e2e/helpers.js (função login)
    - e2e/fixtures.js (PROPRIETARIO)
    - src/components/features/LocatariosDesktop.js (seletor do botão REVOGAR: texto "REVOGAR", visível só quando status_convite === 'pendente')
    - src/components/features/Unidades.js (handleDeletarUnidade / handleSalvarUnidade, prop erro={erro} compartilhada — o bug que o teste deve flagrar)
  </read_first>
  <files>e2e/crud.spec.js</files>
  <action>
    Adicionar dois novos blocos de teste a e2e/crud.spec.js.

    BUG-01 (revogar) — o locatário seedado tem status_convite='pendente' E um contrato ativo vinculado (seed.mjs cria contrato com locatario_id deste locatário). Como o seed liga um contrato, o botão REVOGAR NÃO aparece para ele (só aparece se pendente). Para um cenário determinístico de revogar bem-sucedido, o teste deve, no beforeEach ou setup, inserir via admin client (mesmo padrão de seed.mjs com SUPABASE_ROLE_KEY) um locatário pendente SEM contrato vinculado. Cenário: como Proprietário, navegar /dashboard/locatarios, localizar a linha do locatário pendente-sem-contrato, clicar "REVOGAR", e assertar que a linha some da lista (locatário não mais presente após re-fetch). Este cenário valida BUG-01 happy path. Segundo cenário BUG-01 (erro inline): para o locatário pendente-COM-contrato, clicar REVOGAR e assertar que aparece a mensagem inline "Locatário tem contratos vinculados — encerre-os antes de revogar." na tabela (NÃO um alert do browser). Este cenário estará RED no código atual (usa alert(), não erro inline, e a action ainda não checa FK).

    BUG-02 (split de erro delete vs edit) — cenário que prova que um erro de delete não vaza para o form de edição. Navegar /dashboard/unidades. A unidade seedada 'Sala 101' tem status 'alugada' com contrato ativo (FK bloqueia delete). Clicar deletar nessa unidade → assertar que mensagem de erro aparece no NÍVEL DA LISTA (acima dos cards, bloco `bg-[var(--danger-bg2)]`). Depois abrir o form de edição de OUTRA unidade (a unidade 'E2E-Sala Disponivel') e assertar que a mensagem de erro de delete NÃO aparece dentro do card em edição. No código atual (estado `erro` único) este cenário estará RED porque o mesmo erro vaza para o card via prop `erro={erro}`.

    OBRIGATÓRIO — títulos dos testes (para scoping por -g nas plans de fix): os títulos dos cenários de revogar DEVEM conter o literal "BUG-01" e os de split de erro delete/edit DEVEM conter o literal "BUG-02". Ex.: `test('BUG-01 — revogar locatário pendente sem contrato remove a linha', ...)` e `test('BUG-02 — erro de delete não vaza para o form de edição', ...)`. As plans 02 e 03 rodam `-g "BUG-01"` / `-g "BUG-02"` sobre este mesmo arquivo — sem esses literais nos títulos, o scoping não casa.

    OBRIGATÓRIO — fixture de revogar sucesso: o locatário pendente-SEM-contrato precisa de um usuário de auth REAL, pois `revogarConvite` chama `supabaseAdmin.auth.admin.deleteUser(usuario_id)`. Criar o usuário via o padrão `upsertUser` de e2e/seed.mjs (admin.auth.admin.createUser com email_confirm:true) e usar o `id` retornado como `usuario_id` na linha de locatarios. NÃO fabricar um usuario_id aleatório — o deleteUser falharia e o teste quebraria pelo motivo errado.

    Use seletores robustos: getByText para mensagens exatas, getByRole/locator por estrutura. Não usar timeouts arbitrários — usar expect com auto-wait do Playwright.
  </action>
  <verify>
    <automated>npx playwright test e2e/crud.spec.js --project=chromium --list | grep -E 'BUG-01' && npx playwright test e2e/crud.spec.js --project=chromium --list | grep -E 'BUG-02'</automated>
  </verify>
  <acceptance_criteria>
    - `--list` mostra título(s) contendo o literal "BUG-01" (cenários de revogar) E título(s) contendo o literal "BUG-02" (cenários de split de erro delete/edit)
    - O cenário de erro inline de revogar referencia a string exata "Locatário tem contratos vinculados — encerre-os antes de revogar."
    - Rodar o cenário de erro inline de revogar contra o código ATUAL (sem fix BUG-01) resulta em FALHA (RED) — pois o código usa alert() e a action não checa FK
    - O cenário de BUG-02 abre o form de edição de uma unidade diferente da que falhou no delete e assere ausência do erro de delete dentro do card
    - Setup do locatário pendente-sem-contrato usa admin client com SUPABASE_ROLE_KEY (mesmo padrão de e2e/seed.mjs), cria um usuário de auth REAL via createUser (email_confirm:true) e usa o id retornado como usuario_id; não cria FK de contrato
  </acceptance_criteria>
  <done>crud.spec.js contém cenários RED para BUG-01 (revogar sucesso + erro inline FK) e BUG-02 (erro delete não vaza para edit).</done>
</task>

<task type="auto">
  <name>Task 2: Cenário RED para BUG-04 (← Voltar) em dashboard-smoke.spec.js e cenário BUG-03 em auth-confirm.spec.js</name>
  <read_first>
    - e2e/dashboard-smoke.spec.js (estrutura atual — describe, login no beforeEach)
    - e2e/auth-confirm.spec.js (estrutura atual — apenas 2 testes de redirect de erro)
    - e2e/seed.mjs (locatário seedado com usuario_id e status_convite='pendente')
    - src/components/features/UnidadesPublicas.js (header linhas 79-89: <span>"Unidades Disponíveis"</span> a ser trocado por <Link href="/">← Voltar</Link>)
    - src/app/auth/confirm/route.js (rota que precisa fazer UPDATE status_convite após verifyOtp)
  </read_first>
  <files>e2e/dashboard-smoke.spec.js, e2e/auth-confirm.spec.js</files>
  <action>
    BUG-04 (← Voltar) — adicionar a dashboard-smoke.spec.js (ou criar um describe de páginas públicas no mesmo arquivo) um teste que: navega para /unidades (página pública, SEM login), localiza o link com texto "← Voltar" (ou "Voltar"), clica, e assere que a URL final é a home "/" (page.waitForURL para "/"). No código atual há apenas um <span> sem link, então este cenário estará RED (link "Voltar" não existe).

    BUG-03 (status_convite='aceito' após aceite) — adicionar a auth-confirm.spec.js um teste que valida a transição de status usando um token de invite REAL gerado pela admin API. Estratégia: usar admin client (createClient com SUPABASE_ROLE_KEY, padrão de seed.mjs) para (1) garantir um usuário+linha locatarios com status_convite='pendente'; (2) chamar `admin.auth.admin.generateLink({ type: 'invite', email })` que retorna `properties.hashed_token` (o token_hash real); (3) navegar para `/auth/confirm?token_hash=<hashed_token>&type=invite`; (4) após o redirect, consultar via admin client `locatarios.status_convite` para o usuario_id e assertar que virou 'aceito'. No código atual (route.js não faz o UPDATE) este cenário estará RED — o status permanece 'pendente'.

    Se generateLink não estiver disponível na versão da admin API ou o token não verificar em ambiente de teste, o cenário deve falhar de forma explícita (não silenciosa) e o executor deve registrar no SUMMARY que BUG-03 precisa de verificação manual (fallback Nyquist). NÃO mascarar com um teste que passa trivialmente.
  </action>
  <verify>
    <automated>npx playwright test e2e/dashboard-smoke.spec.js e2e/auth-confirm.spec.js --project=chromium --list | grep -iE 'voltar|status_convite|aceito'</automated>
  </verify>
  <acceptance_criteria>
    - `--list` mostra um cenário de dashboard-smoke citando "Voltar" e um de auth-confirm citando status_convite/aceito
    - O cenário BUG-04 navega para "/unidades" sem login e assere waitForURL para "/"
    - O cenário BUG-03 usa `admin.auth.admin.generateLink({ type: 'invite', ... })` e lê `properties.hashed_token`
    - O cenário BUG-03 consulta `locatarios.status_convite` via admin client após o redirect e assere igualdade a 'aceito'
    - Rodar BUG-04 contra código atual (span sem link) resulta em FALHA (RED)
    - Rodar BUG-03 contra código atual (route.js sem UPDATE) resulta em FALHA (RED) OU registro explícito de necessidade de verificação manual — nunca passa trivialmente
  </acceptance_criteria>
  <done>dashboard-smoke.spec.js tem cenário RED de ← Voltar; auth-confirm.spec.js tem cenário de transição status_convite via token real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → Supabase admin | Specs usam SUPABASE_ROLE_KEY (service role) para seed/assert — bypass de RLS intencional, apenas em ambiente de teste |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | SUPABASE_ROLE_KEY em specs | accept | Chave já usada em seed.mjs; guard em seed.mjs aborta se URL não for de teste — manter padrão |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo instalado nesta fase (RESEARCH Package Legitimacy Audit vazio) |
</threat_model>

<verification>
- Os 3 spec files contêm os cenários novos listados via `--list`
- Cenários BUG-01(erro inline)/BUG-02/BUG-04 estão RED contra o código atual (provam o bug)
- Cenário BUG-03 usa token real via admin API
</verification>

<success_criteria>
Wave 0 completa: cada bug tem um cenário de teste que falha (ou registra necessidade manual) antes do fix. As plans 02/03/04 podem referenciar estes cenários em seus `<verify>` sabendo que não são falso-verdes.
</success_criteria>

<output>
Create `.planning/phases/08-bug-fixes/08-01-SUMMARY.md` when done. Registrar explicitamente o status de cada cenário (RED confirmado / token real funcionou / fallback manual) — especialmente para BUG-03.
</output>
