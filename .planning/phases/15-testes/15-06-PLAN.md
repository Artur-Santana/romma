---
phase: 15-testes
plan: 06
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - .github/workflows/e2e.yml
autonomous: true
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "A separate `unit` job runs `npm run test:unit` on pull_request, parallel to the existing `e2e` job"
    - "The unit job needs no Supabase CLI, no .env.test, no Playwright install"
    - "The workflow file is valid YAML and both jobs trigger on the same pull_request event (so unit failure blocks merge via branch protection)"
  artifacts:
    - path: ".github/workflows/e2e.yml"
      provides: "Separate unit CI job alongside e2e (D-03)"
      contains: "test:unit"
  key_links:
    - from: ".github/workflows/e2e.yml (unit job)"
      to: "package.json test:unit script"
      via: "run: npm run test:unit"
      pattern: "test:unit"
---

<objective>
D-03: add a SEPARATE `unit` job to the existing GitHub Actions workflow, parallel to the `e2e` job (no `needs:` dependency), so unit-test failures block merge. The unit job is lightweight — no Supabase CLI, no local DB, no Playwright browsers — because all Supabase/auth calls are mocked. Depends on 15-01 (the `test:unit` script must exist).

Purpose: Make TEST-01 unit failures a required, fast merge gate without slowing or coupling to the heavier E2E job.
Output: Updated `.github/workflows/e2e.yml` with two parallel jobs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-testes/15-RESEARCH.md
@.planning/phases/15-testes/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parallel unit job to e2e.yml</name>
  <read_first>
    - .github/workflows/e2e.yml (full — existing single `e2e` job, on.pull_request trigger, checkout + setup-node + npm ci pattern)
    - .planning/phases/15-testes/15-PATTERNS.md (.github/workflows/e2e.yml section lines 515-563 — exact unit job block + placement + merge-blocking note)
    - .planning/phases/15-testes/15-RESEARCH.md (GitHub Actions: Unit Job section — no Supabase/Playwright, parallel is fine, blocks merge via required checks)
  </read_first>
  <files>.github/workflows/e2e.yml</files>
  <action>Add a new `unit` job inside the existing `jobs:` block, alongside `e2e` (do NOT modify the e2e job; do NOT add a `needs:` — parallel per D-03 and RESEARCH). The unit job: `runs-on: ubuntu-latest`, `timeout-minutes: 5`; steps = `actions/checkout@v4`, `actions/setup-node@v4` with `node-version: '20'` and `cache: 'npm'`, `npm ci`, then `run: npm run test:unit`. No `supabase/setup-cli`, no `supabase start`, no `.env.test` creation, no `playwright install` — the unit job exercises only mocked code (RESEARCH). The job inherits the existing `on.pull_request: branches:[main]` trigger, so a failing unit job blocks merge through branch protection (no extra config needed). Optionally rename the workflow `name:` to reflect both suites (e.g., "Tests") — discretionary, keep `e2e.yml` filename. Preserve existing YAML indentation (2-space).</action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/e2e.yml','utf8'); if(!/^\s{2}unit:/m.test(y)) throw 'no unit job'; if(!/npm run test:unit/.test(y)) throw 'unit job does not run test:unit'; if(/unit:[\s\S]*?supabase start/.test(y.split('e2e:')[0]||'')) throw 'unit job should not start supabase';" && (command -v yamllint >/dev/null && yamllint -d relaxed .github/workflows/e2e.yml || python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/e2e.yml'))")</automated>
  </verify>
  <done>e2e.yml contains a `unit:` job that runs `npm run test:unit` with no Supabase/Playwright steps, parallel to `e2e:`, and the file parses as valid YAML.</done>
  <acceptance_criteria>
    The unit job exists, is independent (no `needs`), runs only checkout + node + npm ci + test:unit, and the YAML is valid. Both jobs share the pull_request trigger.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR branch → main merge gate | CI required checks gate code entering main |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-CI | Tampering (unverified code reaching main) | merge gate | mitigate | Separate `unit` job on pull_request makes TEST-01 a required, merge-blocking check parallel to e2e |
</threat_model>

<verification>
- `.github/workflows/e2e.yml` parses as valid YAML
- `unit` job present, runs `npm run test:unit`, no Supabase/Playwright steps
- Both `unit` and `e2e` jobs under the same `on.pull_request` trigger
</verification>

<success_criteria>
The CI workflow has a separate, fast, parallel `unit` job that runs the unit suite on every PR to main and blocks merge on failure — fulfilling D-03 and the merge-gate portion of TEST-01/TEST-02.
</success_criteria>

<output>
Create `.planning/phases/15-testes/15-06-SUMMARY.md` when done.
</output>
