---
phase: 15-testes
plan: 03
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - test/unit/actions/auth.test.js
  - test/unit/actions/locatarios.test.js
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "cadastrarProprietario unit tests pass for happy path, missing-field 400, and signUp error pass-through"
    - "revogarConvite unit tests pass for happy path, invalid-UUID 400, unauthenticated 401, and D-08 proprietario_id filter"
    - "npx vitest run test/unit/actions/auth.test.js test/unit/actions/locatarios.test.js exits 0"
  artifacts:
    - path: "test/unit/actions/auth.test.js"
      provides: "cadastrarProprietario coverage (D-06, D-07)"
      contains: "cadastrarProprietario"
    - path: "test/unit/actions/locatarios.test.js"
      provides: "revogarConvite coverage incl. D-08 multi-tenant assertion"
      contains: "proprietario_id"
  key_links:
    - from: "test/unit/actions/locatarios.test.js"
      to: "test/unit/helpers/supabaseMock.js"
      via: "import createSupabaseMock"
      pattern: "createSupabaseMock"
---

<objective>
Unit-test the two Action files that need NO production-code prerequisite: `auth.js` (cadastrarProprietario — no `proprietario_id`, signup flow) and `locatarios.js` (revogarConvite — already correctly filters `.eq('proprietario_id', user.id)` from Phase 11). Covers D-06 + D-07 (happy + validation error + auth guard, ~3 cases each) and D-08 for revogarConvite. Depends only on the Wave-0 infra (15-01); does NOT depend on the IDOR fix (15-02), so it runs in parallel.

Purpose: Lock in the contract of the two Actions that are already correct, proving the mock factory works end to end.
Output: Two passing unit spec files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-testes/15-PATTERNS.md
@.planning/phases/15-testes/15-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit spec for cadastrarProprietario (auth.js)</name>
  <read_first>
    - src/actions/auth.js (full file — imports @supabase/ssr + next/headers; validation line 8-9; signUp lines 33-39; error pass-through lines 41-43; success line 45)
    - test/unit/helpers/supabaseMock.js (from 15-01 — note: NOT used here; auth.js uses @supabase/ssr not supabaseAdmin)
    - .planning/phases/15-testes/15-PATTERNS.md (auth.test.js section lines 183-252 — vi.hoisted mock setup + 3 test cases)
    - .planning/phases/15-testes/15-RESEARCH.md (Pattern 3 auth.js; Pitfall 1 hoisting; Pitfall 5 different mock targets; Pitfall 7 AUTH-02 is DB-side)
  </read_first>
  <files>test/unit/actions/auth.test.js</files>
  <behavior>
    - cadastrarProprietario({ email:'a@b.com', senha:'pass123' }) with mocked signUp → { error: null } resolves to { status: 200 }
    - cadastrarProprietario({ email:'', senha:'' }) → { status: 400 } with truthy erroMessage (no signUp call)
    - cadastrarProprietario(valid) when mocked signUp returns { error: { status: 422, message: 'Email rate limit exceeded' } } → { status: 422, erroMessage: 'Email rate limit exceeded' }
  </behavior>
  <action>Create `test/unit/actions/auth.test.js`. Mock targets are `next/headers` (cookies) and `@supabase/ssr` (createServerClient) — NOT supabaseAdmin (this IS the D-04 total-chain mock for the signup flow) (auth.js does not import it; Pitfall 5). Use `vi.hoisted()` to declare `mockSignUp` so it is assigned before the hoisted `vi.mock` factory runs (Pitfall 1). `vi.mock('next/headers')` returns `cookies` as a `vi.fn().mockResolvedValue({ getAll: vi.fn().mockReturnValue([]), set: vi.fn() })`. `vi.mock('@supabase/ssr')` returns `createServerClient: vi.fn(() => ({ auth: { signUp: mockSignUp } }))`. Import `cadastrarProprietario` from `@/actions/auth` AFTER the mocks. Write the three `it` cases from the behavior block; `beforeEach` calls `vi.clearAllMocks()`. Assert exact `erroMessage` spelling (project canon — not errorMessage). Do NOT write a test asserting an "Instância já configurada" guard — that guard does not exist in auth.js JS (Pitfall 7; covered E2E in 15-05). Use 2-space indent, globals (no vitest import needed beyond what factories require, but importing `vi` is fine).</action>
  <verify>
    <automated>npx vitest run test/unit/actions/auth.test.js</automated>
  </verify>
  <done>`npx vitest run test/unit/actions/auth.test.js` reports 3 passing tests, 0 failing.</done>
  <acceptance_criteria>
    All three cases green. signUp is mocked (no real network). Validation case asserts status 400 + truthy erroMessage. Error case asserts status 422 + exact message string.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit spec for revogarConvite (locatarios.js) incl. D-08</name>
  <read_first>
    - src/actions/locatarios.js (revogarConvite lines 92-114 — inline createServer() auth, UUID check, fetch .eq('id').eq('proprietario_id', user.id).single(), pendente check, contratos count, delete .eq('id').eq('proprietario_id', user.id), auth.admin.deleteUser)
    - test/unit/helpers/supabaseMock.js (from 15-01 — createSupabaseMock factory)
    - .planning/phases/15-testes/15-PATTERNS.md (locatarios.test.js section lines 256-351 — mock setup with vi.hoisted, createSupabaseMock wiring, 4 test cases incl. D-08; authGuard difference table lines 744-753)
    - .planning/phases/15-testes/15-RESEARCH.md (locatarios.js mock setup; D-08 spy assertion)
  </read_first>
  <files>test/unit/actions/locatarios.test.js</files>
  <behavior>
    - revogarConvite(validUuid) with authenticated owner, mocked fetch returning a pendente locatario with 0 contratos, delete + deleteUser succeeding → { status: 200 }
    - revogarConvite('not-a-uuid') → { status: 400, erroMessage: 'ID inválido.' }
    - revogarConvite(validUuid) when getUser returns { user: null } → { status: 401 }
    - D-08: after a successful revogarConvite(validUuid), mockAdmin.eq was called with ('proprietario_id', mockUser.id)
  </behavior>
  <action>Create `test/unit/actions/locatarios.test.js`. Use `vi.hoisted()` for `mockUser` (object with `id`), `mockIsProprietario` (vi.fn), `mockGetUser` (vi.fn). `vi.mock('@/lib/supabase-server')` → `createServer: vi.fn().mockResolvedValue({ auth: { getUser: mockGetUser } })`. `vi.mock('@/lib/auth')` → `isProprietario: mockIsProprietario`. Import `createSupabaseMock` from `../helpers/supabaseMock.js`, destructure `{ mockAdmin, configureResult, resetAll }`, then `vi.mock('@/lib/supabaseAdmin', () => ({ default: mockAdmin }))`. Import `revogarConvite` from `@/actions/locatarios` after mocks. In `beforeEach`: `resetAll()`, `mockGetUser.mockResolvedValue({ data: { user: mockUser } })`, `mockIsProprietario.mockResolvedValue(true)`. For the happy path, configure the chain to return a `single()` of `{ data: { usuario_id: 'u-id', status_convite: 'pendente' }, error: null }` (use `configureResult` and/or `mockAdmin.single.mockResolvedValueOnce`), and ensure the contratos count query and delete resolve without error and `mockAdmin.auth.admin.deleteUser` resolves `{ error: null }`. The contratos count uses `.select('*', { count:'exact', head:true }).eq('locatario_id', id)` then awaits the builder — make the awaited result expose `count: 0` (configure the thenable `_resolve` to `{ count: 0, error: null }` for that step via configureResult before the count, or use mockImplementationOnce on the relevant terminal). Write the 4 `it` cases from the behavior block. For D-08, assert `expect(mockAdmin.eq).toHaveBeenCalledWith('proprietario_id', mockUser.id)`. Use 2-space indent (NOT the 4-space style of the source file — project canon for new files).</action>
  <verify>
    <automated>npx vitest run test/unit/actions/locatarios.test.js</automated>
  </verify>
  <done>`npx vitest run test/unit/actions/locatarios.test.js` reports 4 passing tests, 0 failing, including the D-08 `proprietario_id` assertion.</done>
  <acceptance_criteria>
    All four cases green. D-08 case proves `.eq('proprietario_id', mockUser.id)` is invoked. Auth case proves 401 when no user. UUID case proves exact 400 contract.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → mocked Supabase | All Supabase + auth calls are mocked; no real DB or network crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Elevation of Privilege (IDOR) | revogarConvite | mitigate | D-08 assertion verifies `.eq('proprietario_id', user.id)` is enforced (regression guard for the Phase 11 fix) |
| T-15-AC | Spoofing (auth bypass) | revogarConvite, cadastrarProprietario | mitigate | Unit cases assert 401 (no user) and 400 (missing fields) paths fire before mutation |
| T-15-IV | Tampering (UUID injection) | revogarConvite | mitigate | Invalid-UUID case asserts the 400 reject path |
</threat_model>

<verification>
- `npx vitest run test/unit/actions/auth.test.js test/unit/actions/locatarios.test.js` exits 0 (7 tests pass)
- D-08 assertion present in locatarios.test.js
</verification>

<success_criteria>
auth.js and locatarios.js have passing unit coverage for happy + validation + auth-guard cases per D-07, and revogarConvite proves the D-08 multi-tenant filter, all running on the 15-01 mock factory.
</success_criteria>

<output>
Create `.planning/phases/15-testes/15-03-SUMMARY.md` when done.
</output>
