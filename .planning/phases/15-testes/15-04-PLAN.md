---
phase: 15-testes
plan: 04
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - test/unit/actions/unidades.test.js
  - test/unit/actions/contratos.test.js
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "editarUnidade and deletarUnidade unit tests pass for happy / 400 invalid-UUID / 401 unauthenticated / 404 cross-tenant"
    - "cancelarContrato and encerrarContrato unit tests pass for happy / 400 / 401 / 404 cross-tenant"
    - "D-08: cross-tenant cases prove the edificios.proprietario_id ownership filter rejects non-owned rows"
    - "npx vitest run test/unit/actions/unidades.test.js test/unit/actions/contratos.test.js exits 0"
  artifacts:
    - path: "test/unit/actions/unidades.test.js"
      provides: "editarUnidade + deletarUnidade coverage incl. D-08 ownership"
      contains: "proprietario_id"
    - path: "test/unit/actions/contratos.test.js"
      provides: "cancelarContrato + encerrarContrato coverage incl. D-08 ownership"
      contains: "proprietario_id"
  key_links:
    - from: "test/unit/actions/unidades.test.js"
      to: "test/unit/helpers/supabaseMock.js"
      via: "import createSupabaseMock"
      pattern: "createSupabaseMock"
    - from: "test/unit/actions/contratos.test.js"
      to: "src/actions/contratos.js (ownership pre-check from 15-02)"
      via: "asserts proprietario_id filter on edificios fetch"
      pattern: "proprietario_id"
---

<objective>
Unit-test the two Action files that REQUIRED the IDOR fix first: `unidades.js` (editarUnidade, deletarUnidade) and `contratos.js` (cancelarContrato, encerrarContrato). Covers D-06 + D-07 (happy + validation + auth guard) and the D-08 multi-tenant assertion now made possible by the ownership pre-check added in 15-02. Depends on BOTH 15-01 (infra) and 15-02 (ownership scoping) — assertions here would be impossible against the pre-fix code.

Purpose: Prove the four highest-risk mutation Actions enforce ownership and the standard return contract.
Output: Two passing unit spec files completing TEST-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-testes/15-PATTERNS.md
@.planning/phases/15-testes/15-RESEARCH.md
@.planning/phases/15-testes/15-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit spec for editarUnidade + deletarUnidade (unidades.js)</name>
  <read_first>
    - src/actions/unidades.js (post-15-02 state — authGuard returns { user }; editarUnidade/deletarUnidade ownership pre-check via edificio_id → edificios.proprietario_id)
    - .planning/phases/15-testes/15-02-SUMMARY.md (exact shape of the ownership pre-check added — which queries run in what order)
    - test/unit/helpers/supabaseMock.js (createSupabaseMock factory)
    - .planning/phases/15-testes/15-PATTERNS.md (unidades.test.js section lines 355-417; authGuard difference table)
    - .planning/phases/15-testes/15-RESEARCH.md (Pattern 3 + Critical Finding option 2 flow)
  </read_first>
  <files>test/unit/actions/unidades.test.js</files>
  <behavior>
    - editarUnidade(validUuid, validForm) — owner: unidade fetch returns edificio_id, edificios fetch returns a row for mockUser.id, update succeeds → { status: 200 }
    - editarUnidade(validUuid, validForm) — cross-tenant: edificios ownership fetch returns no row → { status: 404 }, update never reached
    - editarUnidade('not-uuid', validForm) → { status: 400, erroMessage: 'ID inválido.' }
    - editarUnidade(validUuid, validForm) when getUser returns null user → { status: 401 }
    - deletarUnidade(validUuid) — owner happy path → { status: 200 }
    - deletarUnidade(validUuid) — cross-tenant → { status: 404 }
    - D-08: cross-tenant cases assert mockAdmin.eq was called with ('proprietario_id', mockUser.id) on the edificios ownership query
  </behavior>
  <action>Create `test/unit/actions/unidades.test.js`. Mock targets per the authGuard difference table (D-04 total-chain mock): `@/lib/supabase-server` (createServer → getUser via hoisted mockGetUser), `@/lib/auth` (isProprietario via hoisted mockIsProprietario), and `@/lib/supabaseAdmin` via `createSupabaseMock()` (`{ mockAdmin, configureResult, resetAll }`), with `vi.mock('@/lib/supabaseAdmin', () => ({ default: mockAdmin }))`. Use `vi.hoisted()` for `mockUser`, `mockGetUser`, `mockIsProprietario`. `beforeEach`: `resetAll()`, set getUser to return `{ data: { user: mockUser } }`, isProprietario true. The ownership pre-check issues TWO sequential `.single()` resolutions (unidades fetch then edificios fetch) — use `mockAdmin.single.mockResolvedValueOnce(...)` to sequence them: owner path → first single `{ data:{ edificio_id:'e-id' }, error:null }`, second single `{ data:{ id:'e-id' }, error:null }`, then the update thenable resolves `{ error:null }`; cross-tenant path → second single `{ data:null, error:null }` (or error) to trigger 404. Write the cases from the behavior block (3 per action per D-07, plus the cross-tenant D-08 case). For D-08 assert `expect(mockAdmin.eq).toHaveBeenCalledWith('proprietario_id', mockUser.id)`. Use 2-space indent, single quotes acceptable. validForm example: `{ nome:'Sala Teste', area_m2:50, valor_mensal:1000, status:'disponivel' }`.</action>
  <verify>
    <automated>npx vitest run test/unit/actions/unidades.test.js</automated>
  </verify>
  <done>`npx vitest run test/unit/actions/unidades.test.js` reports all tests passing (≥6), including cross-tenant 404 + D-08 proprietario_id assertion; no failures.</done>
  <acceptance_criteria>
    Owner paths 200, cross-tenant paths 404 with no mutation, invalid-UUID 400, unauthenticated 401, and the ownership query filtered by `('proprietario_id', mockUser.id)` are all asserted and green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit spec for cancelarContrato + encerrarContrato (contratos.js)</name>
  <read_first>
    - src/actions/contratos.js (post-15-02 state — authGuard returns { user }; cancelar/encerrar ownership pre-check via unidade_id → edificio_id → edificios.proprietario_id; then update contrato, update unidade, delete parcelas futuras)
    - .planning/phases/15-testes/15-02-SUMMARY.md (exact ownership-check query order added)
    - test/unit/helpers/supabaseMock.js (createSupabaseMock factory)
    - .planning/phases/15-testes/15-PATTERNS.md (contratos.test.js section lines 421-481 — multi-step chain + mockImplementationOnce guidance)
  </read_first>
  <files>test/unit/actions/contratos.test.js</files>
  <behavior>
    - cancelarContrato(validUuid) — owner: contrato fetch → unidade_id; unidade fetch → edificio_id; edificios fetch → row for mockUser.id; subsequent updates/delete succeed → { status: 200 }
    - cancelarContrato(validUuid) — cross-tenant: edificios ownership fetch returns no row → { status: 404, erroMessage: 'Contrato não encontrado.' }, contrato status update never issued
    - cancelarContrato('not-uuid') → { status: 400, erroMessage: 'ID inválido.' }
    - cancelarContrato(validUuid) with null user → { status: 401 }
    - encerrarContrato: owner happy → { status: 200 }; cross-tenant → { status: 404 }
    - D-08: cross-tenant cases assert the edificios query filtered ('proprietario_id', mockUser.id)
  </behavior>
  <action>Create `test/unit/actions/contratos.test.js`. Mock targets: `@/lib/supabase-server`, `@/lib/auth`, `@/lib/supabaseAdmin` (via createSupabaseMock). Do NOT mock `@/lib/supabaseJWT` — it is only used by gerarParcelas, which is not under test. Use `vi.hoisted()` for `mockUser`/`mockGetUser`/`mockIsProprietario`; `beforeEach` resets and sets authenticated owner defaults. The cancelar/encerrar flow now issues THREE sequential `.single()` calls in the ownership pre-check (contrato → unidade → edificio) before the status update — sequence them with `mockAdmin.single.mockResolvedValueOnce`: owner path → `{data:{unidade_id:'u-id'},error:null}`, `{data:{edificio_id:'e-id'},error:null}`, `{data:{id:'e-id'},error:null}`, then the update/delete thenables resolve `{error:null}`; cross-tenant path → third single `{data:null,error:null}` to trigger 404. Write 3 cases per action per D-07 plus the cross-tenant D-08 case. Assert `expect(mockAdmin.eq).toHaveBeenCalledWith('proprietario_id', mockUser.id)` for D-08. Note: `.eq` is called many times across the chain — `toHaveBeenCalledWith` (any-call match) is sufficient; do not over-constrain with Nth. 2-space indent.</action>
  <verify>
    <automated>npx vitest run test/unit/actions/contratos.test.js</automated>
  </verify>
  <done>`npx vitest run test/unit/actions/contratos.test.js` reports all tests passing (≥6), including cross-tenant 404 + D-08 assertion; no failures.</done>
  <acceptance_criteria>
    Owner 200, cross-tenant 404 (no contrato status update), invalid-UUID 400, unauthenticated 401, and ownership filter by `('proprietario_id', mockUser.id)` all asserted and green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run full unit suite green</name>
  <read_first>
    - package.json (test:unit script from 15-01)
  </read_first>
  <files></files>
  <action>Run the complete unit suite to confirm all four spec files (auth, locatarios, unidades, contratos) pass together with no cross-file mock leakage. This is the phase-level unit gate before CI wiring and verification.</action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <done>`npm run test:unit` (→ `vitest run`) exits 0 with all unit specs passing across the 4 Action files.</done>
  <acceptance_criteria>
    Full suite green; no flaky/hanging behavior (non-watch mode confirmed).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → mocked Supabase | All Supabase + auth calls mocked; no real DB crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Elevation of Privilege (IDOR) | editarUnidade, deletarUnidade, cancelarContrato, encerrarContrato | mitigate | Cross-tenant unit cases assert 404 + the `('proprietario_id', user.id)` ownership filter — regression lock for the 15-02 fix |
| T-15-AC | Spoofing (auth bypass) | all four Actions | mitigate | 401 cases assert authGuard fires before any fetch/mutation |
| T-15-IV | Tampering (UUID injection) | all four Actions | mitigate | Invalid-UUID 400 cases assert the reject path |
</threat_model>

<verification>
- `npm run test:unit` exits 0 (all 4 spec files pass)
- D-08 assertions present in both unidades.test.js and contratos.test.js
</verification>

<success_criteria>
All four critical Actions of D-06 have passing unit coverage for happy + validation + auth-guard (D-07) and prove the D-08 ownership filter rejects cross-tenant rows — completing TEST-01.
</success_criteria>

<output>
Create `.planning/phases/15-testes/15-04-SUMMARY.md` when done.
</output>
