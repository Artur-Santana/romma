---
phase: 15-testes
plan: 05
type: execute
wave: 0
depends_on: []
files_modified:
  - e2e/signup.spec.js
  - e2e/mobile-responsive.spec.js
  - e2e/crud.spec.js
  - e2e/crud-edificios.spec.js
  - e2e/crud-unidades.spec.js
  - e2e/crud-contratos.spec.js
  - e2e/crud-locatarios.spec.js
  - e2e/toast-feedback.spec.js
  - e2e/toast-contratos.spec.js
  - e2e/toast-unidades.spec.js
  - e2e/toast-locatarios.spec.js
  - e2e/toast-parcelas.spec.js
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "A second signup attempt while an instance is already configured surfaces a rejection (AUTH-02 gap filled)"
    - "At least one complete interactive user journey passes at 375px (not just static overflow checks)"
    - "crud.spec.js is split into 4 domain files; toast-feedback.spec.js is split into 4 domain files; no test is lost"
    - "npx playwright test passes with no failures after the split + new specs"
  artifacts:
    - path: "e2e/crud-edificios.spec.js"
      provides: "Edifícios CRUD split from crud.spec.js (D-10)"
    - path: "e2e/toast-contratos.spec.js"
      provides: "Contrato toast split from toast-feedback.spec.js (D-10)"
    - path: "e2e/mobile-responsive.spec.js"
      provides: "Adds at least one complete 375px interactive journey (D-09)"
      contains: "375"
  key_links:
    - from: "e2e/crud-*.spec.js"
      to: "e2e/helpers.js + e2e/fixtures.js"
      via: "import login, PROPRIETARIO"
      pattern: "PROPRIETARIO"
---

<objective>
TEST-02: audit-and-gap-fill the E2E suite (D-09) and split the two monolith specs by domain (D-10). Two concrete gaps from RESEARCH: (1) AUTH-02 — second signup blocked is NOT covered (only AUTH-01 happy path exists); (2) mobile 375px coverage is static overflow-only, not a complete interactive journey. Plus: split `crud.spec.js` (27KB) and `toast-feedback.spec.js` (17KB) into per-domain files. Do NOT rewrite specs that already pass (D-09) — move and scope only.

Purpose: Close the documented E2E gaps and make failures localizable, satisfying TEST-02 without churning passing tests.
Output: 8 split spec files (2 monoliths removed), an AUTH-02 spec, and a complete mobile journey.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-testes/15-RESEARCH.md
@.planning/phases/15-testes/15-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AUTH-02 second-signup spec + complete mobile 375px journey</name>
  <read_first>
    - e2e/signup.spec.js (full — AUTH-01 happy path, removeTestUser helper, admin client, /signup form selectors)
    - e2e/mobile-responsive.spec.js (full — UX-02/03/04 describes, login + viewport 375 pattern)
    - e2e/helpers.js + e2e/fixtures.js (login(page, PROPRIETARIO) pattern; seeded proprietario@test.romma.local)
    - .planning/phases/15-testes/15-RESEARCH.md (E2E Gap Analysis table; Open Question 2 mock-vs-real; Pitfall 7 AUTH-02 guard is DB-side; Assumption A4 message source)
  </read_first>
  <files>e2e/signup.spec.js, e2e/mobile-responsive.spec.js</files>
  <action>In `e2e/signup.spec.js`, add an AUTH-02 test inside the existing `Phase 10 — Signup Proprietário` describe (serial mode already set). Per RESEARCH Open Question 2, test the REAL constraint: the seeded `proprietario@test.romma.local` already exists (instance configured), so navigate to `/signup`, fill a DIFFERENT email plus password, submit, and assert the UI surfaces a rejection. Assert visible text matching `/instância já configurada|já configurada|já existe/i` OR (fallback per A4) assert the `email_sent` "verifique seu email" banner does NOT appear and the page stays on /signup. Use a tolerant regex / `expect.poll` so the assertion holds whether the message comes from custom JS or a Supabase error body. Do not delete the seeded proprietário.

In `e2e/mobile-responsive.spec.js`, add a new describe `Mobile 375px — jornada completa (TEST-02)` with `test.use({ viewport: { width: 375, height: 812 } })`. Implement ONE complete interactive journey (not overflow-only): login as PROPRIETARIO, waitForURL dashboard, navigate to `/dashboard/unidades`, wait for networkidle, interact with a real element (tap a `[data-testid="mobile-bottom-nav"]` item OR open the first list item's detail via the `VER →` button already used at line 67), and assert a concrete post-interaction state (URL changed or a panel/row became visible). Reuse selectors already proven in this file. The point is to prove the UI is OPERABLE at 375px.</action>
  <verify>
    <automated>npx playwright test e2e/signup.spec.js e2e/mobile-responsive.spec.js</automated>
  </verify>
  <done>signup.spec.js has a passing AUTH-02 test; mobile-responsive.spec.js has a passing complete-journey test at 375px; both files green under `npx playwright test`.</done>
  <acceptance_criteria>
    AUTH-02 asserts a rejection on second signup (no email_sent state). Mobile journey performs at least one real interaction and asserts a resulting state change at 375px. Both green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Split crud.spec.js into 4 per-domain files (D-10)</name>
  <read_first>
    - e2e/crud.spec.js (full — header lines 18-31, viewport test.use line 33, the four test.describe blocks: Edifícios, Unidades, Contratos, Locatários)
    - .planning/phases/15-testes/15-PATTERNS.md (crud-*.spec.js section lines 567-607 — header verbatim, per-domain describe, split mapping, viewport preservation)
    - e2e/helpers.js + e2e/fixtures.js (shared imports)
  </read_first>
  <files>e2e/crud.spec.js, e2e/crud-edificios.spec.js, e2e/crud-unidades.spec.js, e2e/crud-contratos.spec.js, e2e/crud-locatarios.spec.js</files>
  <action>Create four new files, one per domain, each with the verbatim shared header from crud.spec.js (imports `test, expect`, `createClient`, `config({path:'.env.test'})`, `login`, `PROPRIETARIO`, and the `admin` client block). Move each `test.describe('Edifícios'|'Unidades'|'Contratos'|'Locatários', ...)` block into its matching file unchanged, applying `test.use({ viewport: { width: 1440, height: 900 } })` at the top-level describe of each new file to preserve crud.spec.js line 33. Move any beforeAll/afterAll fixtures a domain block depends on into that domain's file, scoped to only what it needs (Contratos/Locatários keep their fixtures; Edifícios/Unidades use beforeEach login). Then DELETE `e2e/crud.spec.js` — its content now fully lives in the 4 new files, no test lost. Do NOT add Playwright projects (D-10 defers them). `playwright.config.js` uses `fullyParallel:false, workers:1`, so file ordering and global seed/teardown are unaffected by the split.</action>
  <verify>
    <automated>test ! -f e2e/crud.spec.js; for f in edificios unidades contratos locatarios; do test -f e2e/crud-$f.spec.js || exit 1; done; npx playwright test e2e/crud-edificios.spec.js e2e/crud-unidades.spec.js e2e/crud-contratos.spec.js e2e/crud-locatarios.spec.js</automated>
  </verify>
  <done>crud.spec.js no longer exists; four crud-*.spec.js files exist; all four pass under `npx playwright test`; no Playwright projects added.</done>
  <acceptance_criteria>
    Every test from the original crud.spec.js is present in exactly one split file and passes. No net loss of test count.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Split toast-feedback.spec.js into 4 per-domain files (D-10)</name>
  <read_first>
    - e2e/toast-feedback.spec.js (full — header lines 19-31, module-scope shared `let` fixture vars, the single beforeAll creating edificio→unidade→locatario→contrato, the per-action toast tests)
    - .planning/phases/15-testes/15-PATTERNS.md (toast-*.spec.js section lines 611-671 — header verbatim, beforeAll fixture pattern, scoped vars, split mapping, idempotent cleanup)
    - .planning/phases/15-testes/15-RESEARCH.md (Pitfall 6 — intertwined beforeAll fixtures; D-10 split strategy table)
  </read_first>
  <files>e2e/toast-feedback.spec.js, e2e/toast-contratos.spec.js, e2e/toast-unidades.spec.js, e2e/toast-locatarios.spec.js, e2e/toast-parcelas.spec.js</files>
  <action>Create four new files per the split mapping: `toast-contratos.spec.js` (Contrato criado/cancelado/encerrado — needs edificio+unidade+locatario+contrato fixtures), `toast-unidades.spec.js` (Unidade removida — edificio+unidade only), `toast-locatarios.spec.js` (Acesso revogado — locatario fixture), `toast-parcelas.spec.js` (Parcela marcada como paga — contrato+parcela fixture). Each file: verbatim shared header + admin client; its OWN module-scoped `let` vars (do NOT share across files); its OWN beforeAll that creates only the fixtures that domain needs, preserving the idempotent stale-cleanup block from the original scoped to that domain's fixture names; its OWN afterAll cleaning in reverse dependency order. Apply the original `test.use({ viewport: { width: 1440, height: 900 } })` per file. Move the matching tests unchanged. Then DELETE `e2e/toast-feedback.spec.js`. Key gotcha (Pitfall 6): the intertwined single beforeAll is the main work — split fixture creation per domain; the tests themselves are straightforward moves. workers:1 + fullyParallel:false means no race conditions.</action>
  <verify>
    <automated>test ! -f e2e/toast-feedback.spec.js; for f in contratos unidades locatarios parcelas; do test -f e2e/toast-$f.spec.js || exit 1; done; npx playwright test e2e/toast-contratos.spec.js e2e/toast-unidades.spec.js e2e/toast-locatarios.spec.js e2e/toast-parcelas.spec.js</automated>
  </verify>
  <done>toast-feedback.spec.js no longer exists; four toast-*.spec.js files exist with self-contained fixtures; all pass under `npx playwright test`.</done>
  <acceptance_criteria>
    Each of the 5 original toast assertions lives in exactly one split file with its own scoped fixtures and passes. No shared module state across files.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Run full E2E suite green</name>
  <read_first>
    - playwright.config.js (testDir ./e2e, workers:1, fullyParallel:false, global setup/teardown)
  </read_first>
  <files></files>
  <action>Run the entire Playwright suite to confirm the splits lost no tests and the two new specs (AUTH-02, mobile journey) pass alongside everything else. This is the phase-level E2E gate for TEST-02 success criterion 3 (`npx playwright test` passes in CI).</action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <done>`npm run test:e2e` (→ `playwright test`) passes with no failures; the suite includes the 8 split files + AUTH-02 + mobile journey.</done>
  <acceptance_criteria>
    Full E2E suite green. Total passing test count is at least the pre-split count plus the 2 new tests.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor → /signup | Untrusted second-signup attempt crosses the instance-uniqueness guard |
| authenticated Proprietário → dashboard at 375px | Real interactive session exercised end to end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Spoofing / abuse (unauthorized account creation) | /signup second attempt | mitigate | AUTH-02 E2E asserts the second signup is rejected (instance-uniqueness guard holds) |
| T-15-RG | Tampering (test-coverage regression from refactor) | crud / toast split | accept | D-09 mandates move-not-rewrite; Task 4 full-suite run is the regression guard ensuring no test lost |
</threat_model>

<verification>
- `npm run test:e2e` exits 0
- `e2e/crud.spec.js` and `e2e/toast-feedback.spec.js` no longer exist; 8 split files present
- AUTH-02 + mobile-journey tests present and passing
</verification>

<success_criteria>
The E2E suite covers AUTH-02 (second signup blocked) and at least one complete interactive 375px journey, the two monolith specs are split by domain with no test lost, and `npx playwright test` passes — satisfying TEST-02.
</success_criteria>

<output>
Create `.planning/phases/15-testes/15-05-SUMMARY.md` when done.
</output>
