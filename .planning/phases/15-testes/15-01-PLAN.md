---
phase: 15-testes
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.mjs
  - test/helpers/server-only-stub.js
  - test/helpers/supabaseMock.js
  - package.json
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "npx vitest run executes with zero specs and exits 0 (config valid, node env)"
    - "A test file can import createSupabaseMock from test/helpers/supabaseMock.js and await a mocked chain that resolves with a configured {data, error}"
    - "npm run test:unit invokes vitest in run mode (not watch — does not hang)"
  artifacts:
    - path: "vitest.config.mjs"
      provides: "Vitest config — node environment, @/ alias, server-only stub alias"
      contains: "environment"
    - path: "test/helpers/supabaseMock.js"
      provides: "Shared chainable thenable Supabase mock builder (D-05)"
      exports: ["createSupabaseMock"]
    - path: "test/helpers/server-only-stub.js"
      provides: "Empty module stub so import 'server-only' does not throw in node env"
    - path: "package.json"
      provides: "test:unit script = vitest run"
      contains: "test:unit"
  key_links:
    - from: "vitest.config.mjs"
      to: "test/helpers/server-only-stub.js"
      via: "resolve.alias server-only"
      pattern: "server-only"
    - from: "test/helpers/supabaseMock.js"
      to: "vitest vi.fn"
      via: "import { vi } from 'vitest'"
      pattern: "createSupabaseMock"
---

<objective>
Stand up Vitest unit-test infrastructure for the first time in this repo (D-01, D-02, D-05): install the framework, create `vitest.config.mjs` (node env, `@/` alias, `server-only` stub), the shared chainable Supabase mock factory at `test/helpers/supabaseMock.js`, and the `test:unit` npm script.

Purpose: Every TEST-01 unit spec (plans 03, 04) and the CI unit job (plan 07) depend on this scaffold. Nothing to assert yet — this is pure infra so downstream specs have a working runner and a DRY mock builder.
Output: `vitest.config.mjs`, `test/helpers/server-only-stub.js`, `test/helpers/supabaseMock.js`, updated `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-testes/15-RESEARCH.md
@.planning/phases/15-testes/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest + create config and server-only stub</name>
  <read_first>
    - jsconfig.json (the `@/*` → `./src/*` path mapping the alias must replicate)
    - .planning/phases/15-testes/15-PATTERNS.md (section "vitest.config.js" lines 35-75 — config shape; section "server-only-stub.js" lines 79-88)
    - src/lib/supabaseAdmin.js line 2 (the `import 'server-only'` side-effect the stub neutralizes)
    - package.json (devDependencies + engines node >=20)
  </read_first>
  <files>vitest.config.mjs, test/helpers/server-only-stub.js, package.json</files>
  <action>
    Run `npm install -D vitest@^4.1.8` (D-01; slopcheck [SUS] verdict is a confirmed false positive per RESEARCH Package Legitimacy Audit — vitest is maintained by the Vite core team, no postinstall, safe). Do NOT install vite-tsconfig-paths, jsdom, happy-dom, or @vitejs/plugin-react — RESEARCH "What NOT to install" rules these out for a node-env Action test suite.

    Create `vitest.config.mjs` (NOT `.js` — D-01 mandates `vitest.config.mjs`) importing `defineConfig` from `vitest/config` and `path`. Set `test.environment` to `'node'`, `test.globals` to `true`, and `test.include` to `['test/unit/**/*.test.js']`. Under `resolve.alias`, map `'@'` to `path.resolve(__dirname, './src')` (mirrors jsconfig `@/*`) and map `'server-only'` to `path.resolve(__dirname, 'test/helpers/server-only-stub.js')` (Pitfall 4 — the real `server-only` throws on evaluation outside RSC).

    Create `test/helpers/server-only-stub.js` exporting an empty default object (`export default {}`).

    Do NOT add the `test:unit` script yet — that is Task 2 (avoids re-editing package.json mid-task; npm install already touches it for the devDependency).
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | grep -qiE 'no test files|0 passed|passed' && node -e "require('fs').accessSync('vitest.config.mjs')" && node -e "require('fs').accessSync('test/helpers/server-only-stub.js')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with "no test files found" (or equivalent) — runner boots without crashing on config or alias resolution.
    - `vitest.config.mjs` exists with `environment: 'node'`, `globals: true`, `@` alias, and `server-only` alias pointing at the stub.
    - `vitest` is present in package.json devDependencies; vite-tsconfig-paths / jsdom / happy-dom / @vitejs/plugin-react are NOT added.
  </acceptance_criteria>
  <done>npx vitest run boots cleanly (zero specs, exit 0); config + stub files exist; vitest installed as devDependency.</done>
</task>

<task type="auto">
  <name>Task 2: Shared chainable Supabase mock factory + test:unit script</name>
  <read_first>
    - .planning/phases/15-testes/15-PATTERNS.md (section "supabaseMock.js" lines 92-179 — the full factory pattern incl. thenable builder and resetAll; "package.json" lines 485-511)
    - .planning/phases/15-testes/15-RESEARCH.md (Pattern 2 lines 199-279 — thenable rationale; Pitfall 2 lines 480-483; Pitfall 3 lines 485-487)
    - src/actions/locatarios.js lines 79-110 (real chain: .from().select().eq().eq().single() and .from().delete().eq().eq(); auth.admin.deleteUser)
    - package.json scripts block
  </read_first>
  <files>test/helpers/supabaseMock.js, package.json</files>
  <action>
    Create `test/helpers/supabaseMock.js` (D-05 — single shared factory, path matches CONTEXT D-05 example). Export a named function `createSupabaseMock()` returning `{ mockAdmin, configureResult, resetAll }`.

    The `mockAdmin` builder object must expose `vi.fn()` methods: `from`, `select`, `insert`, `update`, `delete`, `eq`, `single`, plus a nested `auth.admin.deleteUser`, `auth.admin.inviteUserByEmail`, and `auth.signUp` (all `vi.fn()`). Wire `from/select/insert/update/delete/eq` to `mockReturnValue(builder)` so chaining works. Make the builder THENABLE by adding a `then(resolve)` method that returns `Promise.resolve(_resolve).then(resolve)` where `_resolve` is a closure-held `{ data, error }` — this is mandatory because Actions await the full chain inline (e.g. `await supabaseAdmin.from('unidades').delete().eq('id', id)`, unidades.js:60). `.single()` is terminal: `mockImplementation(() => Promise.resolve(_resolve))`.

    Provide `configureResult(result)` to set `_resolve` and re-point `.single()` at it, and `resetAll()` to call `vi.clearAllMocks()`, reset `_resolve` to `{ data: null, error: null }`, and re-establish all `mockReturnValue(builder)` wirings (clearAllMocks wipes the return values, so they MUST be re-applied — without this, post-reset chains break). Import `{ vi }` from `'vitest'` at top. Use 2-space indentation per project canon (do NOT copy locatarios.js 4-space style).

    Then edit `package.json` scripts: add `"test:unit": "vitest run"` and `"test:unit:watch": "vitest"` after `"test:e2e:ui"`. The run-mode form is REQUIRED — bare `vitest` starts watch mode and hangs CI (Pitfall 3).
  </action>
  <verify>
    <automated>node --experimental-vm-modules -e "import('./test/helpers/supabaseMock.js').then(async m => { const { mockAdmin, configureResult } = m.createSupabaseMock(); configureResult({ data: { ok: 1 }, error: null }); const r = await mockAdmin.from('x').select('*').eq('id','1'); if (!r.data || r.data.ok !== 1) { console.error('thenable chain failed', r); process.exit(1); } console.log('OK'); }).catch(e => { console.error(e); process.exit(1); })" && node -e "if(require('./package.json').scripts['test:unit']!=='vitest run'){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - Importing `createSupabaseMock`, calling `configureResult({data:{ok:1},error:null})`, then `await mockAdmin.from('x').select('*').eq('id','1')` resolves to `{data:{ok:1},error:null}` (thenable builder works end to end).
    - `mockAdmin.auth.admin.deleteUser`, `mockAdmin.auth.admin.inviteUserByEmail`, `mockAdmin.auth.signUp` are vi.fn mocks.
    - `package.json` has `"test:unit": "vitest run"` (exact) and `"test:unit:watch": "vitest"`.
  </acceptance_criteria>
  <done>Factory resolves a chained awaited call to the configured result; auth.admin namespace present; test:unit script = `vitest run`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local devDeps | Installing vitest crosses a supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-SC | Tampering | `npm install -D vitest` | mitigate | RESEARCH Package Legitimacy Audit ran slopcheck 2026-06-12; vitest [SUS] is a confirmed false positive (parent-project naming, Vite core team, no postinstall). Approved with CITED Next.js docs. No [ASSUMED]/[SUS]/[SLOP] package requires a blocking human checkpoint. |
| T-15-INFRA | Information Disclosure | `server-only` stub in test env | accept | Stub only active under Vitest config alias; never bundled into app build. No production impact. |
</threat_model>

<verification>
- `npx vitest run` exits 0 (boots, no specs yet).
- Mock factory thenable chain resolves to configured `{data,error}`.
- `npm run test:unit` runs in non-watch mode.
</verification>

<success_criteria>
Vitest installed and configured (node env, `@/` + `server-only` aliases); shared `createSupabaseMock` factory works as a thenable chainable builder; `test:unit` script present in run mode. Downstream unit specs can be authored against this scaffold.
</success_criteria>

<output>
Create `.planning/phases/15-testes/15-01-SUMMARY.md` when done.
</output>
