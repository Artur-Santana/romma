---
phase: 25-portal-do-locat-rio-pix-recibo
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - src/components/features/portal/VencimentoDestaque.js
  - src/components/features/portal/ContratoCard.js
  - src/components/features/portal/PortalDashboard.js
autonomous: true
requirements:
  - PORT-04
  - PORT-05
must_haves:
  truths:
    - "VencimentoDestaque exibe valor BRL 32px bold, parcela X/N, dias restantes com cor condicional (verde/warning/danger), botão PAGAR AGORA"
    - "VencimentoDestaque fica oculto quando não há parcela pendente/vencida"
    - "ContratoCard exibe barra de progresso (track + fill indigo) e linha 'X pagas · N total · Z% adimplente'"
    - "PortalDashboard busca todas as parcelas (incluindo futura) via getTodasParcelasPortal"
    - "PortalDashboard renderiza VencimentoDestaque acima de ContratoCard com estado de modal PIX gerenciado localmente"
    - "Clique em PAGAR AGORA abre PixModal com a parcela correta injetada"
  artifacts:
    - path: "src/components/features/portal/VencimentoDestaque.js"
      provides: "Componente novo — destaque próximo vencimento (PORT-04)"
      contains: "export default function VencimentoDestaque"
    - path: "src/components/features/portal/ContratoCard.js"
      provides: "Extensão com progresso row (barra + stats)"
      contains: "role=\"progressbar\""
    - path: "src/components/features/portal/PortalDashboard.js"
      provides: "Estado todasParcelas + pixModal state + VencimentoDestaque renderizado"
      contains: "getTodasParcelasPortal"
  key_links:
    - from: "PortalDashboard.js"
      to: "VencimentoDestaque"
      via: "onPagar prop — abre modal: setPixModal({ open: true, parcela })"
      pattern: "setPixModal"
    - from: "PortalDashboard.js"
      to: "getTodasParcelasPortal"
      via: "useEffect fetchData — substitui getParcelasPortal para contadores de progresso"
      pattern: "getTodasParcelasPortal"
---

<objective>
Construir a camada de destaque (VencimentoDestaque) e progresso (ContratoCard + barra) do portal — PORT-04 + parte do PORT-05 (botão PAGAR AGORA que abre PixModal).

Purpose: O Locatário precisa ver o próximo vencimento em destaque e o progresso do contrato (D-08). O botão "PAGAR AGORA" é a entrada do fluxo de pagamento — prepara o estado para o PixModal criado no Plan 03.

Output:
- `VencimentoDestaque.js` novo (destaque próximo vencimento)
- `ContratoCard.js` estendido (barra de progresso)
- `PortalDashboard.js` estendido (todasParcelas state, pixModal state, VencimentoDestaque renderizado, PixModal importado)
</objective>

<execution_context>
@/home/artursantana/.claude/get-shit-done/workflows/execute-plan.md
@/home/artursantana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-portal-do-locat-rio-pix-recibo/25-CONTEXT.md
@.planning/phases/25-portal-do-locat-rio-pix-recibo/25-UI-SPEC.md
@.planning/phases/25-portal-do-locat-rio-pix-recibo/25-PATTERNS.md
@.planning/phases/25-portal-do-locat-rio-pix-recibo/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar VencimentoDestaque.js + estender ContratoCard.js</name>
  <files>src/components/features/portal/VencimentoDestaque.js, src/components/features/portal/ContratoCard.js</files>

  <read_first>
    - src/components/features/portal/ContratoCard.js — arquivo completo; notar imports (fmtBRL, fmtData, StatusBadge), estrutura da section (border border-border-3 bg-surface p-7), grid grid-cols-1 sm:grid-cols-2 gap-6 mt-4, onde inserir a barra de progresso (após o </div> de fechamento do grid, antes do </section>)
    - src/components/features/portal/PortalDashboard.js — arquivo completo; notar imports existentes, padrão de classe (eyebrow eyebrow--indigo, font-display font-bold, font-mono text-[11px])
    - .planning/phases/25-portal-do-locat-rio-pix-recibo/25-UI-SPEC.md — Surface 1 (VencimentoDestaque classes exatas) e Surface 2 (ContratoCard barra de progresso classes)
    - .planning/phases/25-portal-do-locat-rio-pix-recibo/25-PATTERNS.md — seções "VencimentoDestaque.js (NEW)" e "ContratoCard.js — add progresso row"
  </read_first>

  <behavior>
    - VencimentoDestaque recebe { parcelas, contrato, onPagar }
    - Quando não há parcela pendente/vencida: retorna null (componente oculto)
    - Quando há próxima pagável: exibe valor BRL 32px (contrato.unidades?.valor_mensal), parcela X/N, dias restantes
    - diasRestantes < 0 → text-danger-fg + "X dias em atraso"; diasRestantes <= 7 → text-warning; senão → text-fg-3
    - Botão "[>] PAGAR AGORA" chama onPagar(proximaPagavel)
    - ContratoCard recebe { contrato, parcelas = [] }
    - Quando parcelas.length > 0: exibe barra de progresso (track bg-border-3 + fill bg-indigo, 6px) com role="progressbar" aria-valuenow={pct}
    - Stats row: "X pagas · N total · Z% adimplente" — Z em text-highlight se < 100%, text-success se 100%
  </behavior>

  <action>
    **Criar `src/components/features/portal/VencimentoDestaque.js`** (arquivo novo, `'use client'` NÃO necessário — sem hooks, derivação pura de props):

    Imports: `import { fmtBRL, cn } from "@/lib/utils"`

    Lógica de derivação (sem useEffect — cálculo síncrono sobre prop parcelas):
    - Filtrar pagaveis: parcelas onde status === 'pendente' || status === 'vencida'
    - Sort por data_vencimento ascending
    - proximaPagavel = pagaveis[0] ?? null
    - Se !proximaPagavel: return null
    - totalParcelas = parcelas.length
    - diasRestantes = Math.ceil((new Date(proximaPagavel.data_vencimento) - new Date()) / 86400000)

    Estrutura JSX (classes exatas de UI-SPEC Surface 1):
    - Container: `<section className="mt-8 border-l-4 border-l-highlight bg-surface p-6 flex flex-col gap-3">`
    - Eyebrow: `<span className="eyebrow eyebrow--indigo">PRÓXIMO VENCIMENTO</span>`
    - Valor BRL: `<span className="font-display font-bold text-[32px] leading-none tracking-[-1.6px] text-fg-1">{fmtBRL(contrato.unidades?.valor_mensal)}</span>`
    - Subtítulo: `<span className={cn("font-mono text-[11px]", diasRestantes < 0 ? "text-danger-fg" : diasRestantes <= 7 ? "text-warning" : "text-fg-3")}>Parcela {proximaPagavel.numero}/{totalParcelas} · {diasRestantes < 0 ? `${Math.abs(diasRestantes)} dias em atraso` : `${diasRestantes} dias restantes`}</span>`
    - Botão "[>] PAGAR AGORA": style={{ all: 'unset', cursor: 'pointer' }}, className="bg-indigo text-fg-1 font-mono font-bold text-[11px] tracking-[1.5px] uppercase px-5 py-3 hover:opacity-90 transition-opacity w-full sm:w-auto mt-2 cursor-pointer", onClick={() => onPagar(proximaPagavel)}, aria-label="Pagar próxima parcela"

    **Estender `src/components/features/portal/ContratoCard.js`**:

    1. Adicionar `cn` aos imports existentes: `import { fmtBRL, fmtData, cn } from "@/lib/utils"`
    2. Alterar assinatura da função: `export default function ContratoCard({ contrato, parcelas = [] })`
    3. Adicionar bloco de progresso dentro de `<section>`, após o `</div>` de fechamento do grid (antes de `</section>`):

       Calcular inline dentro de JSX (IIFE ou variáveis antes do return):
       - `const total = parcelas.length`
       - `const pagas = parcelas.filter(p => p.status === 'paga').length`
       - `const pct = total > 0 ? Math.round((pagas / total) * 100) : 0`

       Condicional: `{parcelas.length > 0 && (<div className="mt-4">...)}`

       Barra: `<div className="w-full bg-border-3" style={{ height: 6 }} role="progressbar" aria-valuenow={pct} aria-valuemin={0} aria-valuemax={100} aria-label={\`Progresso do contrato: ${pagas} parcelas pagas de ${total}\`}><div className="bg-indigo h-full origin-left" style={{ width: pct + '%' }} /></div>`

       Stats row: `<div className="flex gap-4 mt-2"><span className="font-mono text-[11px] text-fg-4">{pagas} pagas · {total} total</span><span className={cn("font-mono text-[11px]", pct === 100 ? "text-success" : "text-highlight")}>· {pct}% adimplente</span></div>`
  </action>

  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>

  <done>
    `src/components/features/portal/VencimentoDestaque.js` existe e exporta `default function VencimentoDestaque`. `src/components/features/portal/ContratoCard.js` contém `role="progressbar"` e aceita prop `parcelas`. Suite Vitest continua green (npx vitest run — exit 0).
  </done>
</task>

<task type="auto">
  <name>Task 2: Estender PortalDashboard.js — todasParcelas state + PixModal state + wiring</name>
  <files>src/components/features/portal/PortalDashboard.js</files>

  <read_first>
    - src/components/features/portal/PortalDashboard.js — arquivo completo; identificar: linha de imports de queries (linha 5), useState declarations (linhas 13–17), fetchData (linhas 20–37), render tree (linhas 41–76), posição exata onde VencimentoDestaque deve ser inserido (linha 69–70, acima de ContratoCard)
    - .planning/phases/25-portal-do-locat-rio-pix-recibo/25-PATTERNS.md — seção "PortalDashboard.js — extend state and render tree" completa (state extension, fetchData extension, render tree extension, toast import)
    - .planning/phases/25-portal-do-locat-rio-pix-recibo/25-UI-SPEC.md — IC-01 (fluxo "Pagar Agora") e IC-07 (re-fetch após pagamento)
    - src/components/features/portal/VencimentoDestaque.js — confirmar que existe (criado na Task 1)
  </read_first>

  <action>
    Modificar `src/components/features/portal/PortalDashboard.js`:

    **1. Imports — adicionar:**
    - `getTodasParcelasPortal` ao import existente de queries: `import { getLocatarioByUserId, getContratoAtivoByLocatario, getParcelasPortal, getTodasParcelasPortal } from "@/lib/queries-client"`
    - Novos componentes: `import VencimentoDestaque from "./VencimentoDestaque"` e `import PixModal from "./PixModal"` (PixModal será criado no Plan 03; o import deve existir já para que o wiring esteja pronto)
    - Toast: `import { toast } from "sonner"`

    **2. Estado — adicionar após a declaração `const [parcelas, setParcelas] = useState([])`:**
    - `const [todasParcelas, setTodasParcelas] = useState([])`
    - `const [pixModal, setPixModal] = useState({ open: false, parcela: null })`

    **3. fetchData — adicionar após `setParcelas(parc ?? [])`:**
    ```
    const todasParc = await getTodasParcelasPortal(ct.id)
    setTodasParcelas(todasParc ?? [])
    ```

    **4. Função refetchParcelas para uso após pagamento confirmado:**
    ```
    async function refetchParcelas() {
      if (!contrato) return
      const todasParc = await getTodasParcelasPortal(contrato.id)
      setTodasParcelas(todasParc ?? [])
    }
    ```
    Declarar como função nomeada dentro do componente (não dentro do useEffect).

    **5. Render tree — alterar o bloco `<>` do happy-path (onde contrato existe):**
    - Adicionar `<VencimentoDestaque parcelas={todasParcelas} contrato={contrato} onPagar={(parcela) => setPixModal({ open: true, parcela })} />` ANTES do `<div className="mt-8"><ContratoCard .../>` existente
    - Atualizar `<ContratoCard contrato={contrato} />` para `<ContratoCard contrato={contrato} parcelas={todasParcelas} />`
    - Atualizar `<ParcelsTable parcelas={parcelas} />` para `<ParcelsTable parcelas={todasParcelas} locatario={locatario} contrato={contrato} onPagar={(parcela) => setPixModal({ open: true, parcela })} />`
    - Adicionar `<PixModal open={pixModal.open} parcela={pixModal.parcela} contrato={contrato} onClose={() => setPixModal({ open: false, parcela: null })} onSucesso={async () => { await refetchParcelas(); toast.success("Pagamento registrado") }} />` após ParcelsTable

    **NOTA sobre PixModal import:** O arquivo `src/components/features/portal/PixModal.js` será criado no Plan 03. O import pode causar erro de build até que o Plan 03 execute. Para evitar isso, criar um stub mínimo `src/components/features/portal/PixModal.js` que exporta um componente vazio: `export default function PixModal() { return null }`. O Plan 03 vai substituir este stub pelo componente completo.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <done>
    `src/components/features/portal/PortalDashboard.js` contém `getTodasParcelasPortal`, `todasParcelas`, `pixModal`, `VencimentoDestaque`, `PixModal`. `npm run build` termina com exit 0 (sem crash SSR, sem import errors). Portal renderiza VencimentoDestaque (visível quando há parcela pendente/vencida) e ContratoCard com progresso.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → Server Action | onPagar passa parcela.id para confirmarPagamentoLocatario (Plan 01) — ID vem de query RLS do locatário |
| RLS via supabase-browser | getTodasParcelasPortal usa cliente anon com RLS — locatário só vê parcelas do próprio contrato |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06 | Information Disclosure | getTodasParcelasPortal (RLS) | mitigate | Query usa supabase-browser (anon key + RLS) — locatário autenticado só acessa parcelas do próprio contrato via RLS policies existentes |
| T-25-07 | Tampering | VencimentoDestaque — cálculo de proximaPagavel | accept | Cálculo é client-side display-only; a validação real de ownership ocorre no Server Action (Plan 01); manipulação de UI não bypassa a guard |
| T-25-08 | Information Disclosure | PortalDashboard — dados de contrato/locatario no state | accept | State é client-side do próprio usuário; dados carregados via RLS que já garante isolamento |
| T-25-SC | Tampering | npm install (nenhum novo pacote) | accept | Nenhum pacote novo neste plano; jsPDF já no package.json (RESEARCH.md slopcheck OK) |
</threat_model>

<verification>
`npm run build` exit 0 — portal compila sem erros SSR.

Portal do Locatário renderiza `VencimentoDestaque` quando há parcela pendente/vencida, exibindo valor BRL 32px, parcela X/N, dias restantes com cor correta.

`ContratoCard` exibe barra de progresso com `role="progressbar"`.

`PortalDashboard` usa `getTodasParcelasPortal` (inclui futura) para contadores de progresso.

Clicar "PAGAR AGORA" muda `pixModal.open` para true (verificar via React DevTools ou log temporário).

Suite Vitest continua green (npx vitest run).
</verification>

<success_criteria>
- VencimentoDestaque.js existe, exporta VencimentoDestaque, retorna null quando sem pagável
- ContratoCard.js tem role="progressbar" e aceita prop parcelas
- PortalDashboard.js usa getTodasParcelasPortal, tem pixModal state, importa VencimentoDestaque e PixModal
- `npm run build` exit 0
- `npx vitest run` exit 0 (sem regressão)
</success_criteria>

<output>
Criar `.planning/phases/25-portal-do-locat-rio-pix-recibo/25-02-SUMMARY.md` quando concluído.
</output>
