---
phase: 25-portal-do-locat-rio-pix-recibo
plan: 04
type: execute
wave: 3
depends_on:
  - 25-02
  - 25-03
files_modified:
  - src/components/features/portal/ParcelsTable.js
autonomous: true
requirements:
  - PORT-04
  - PORT-05
  - PORT-07
must_haves:
  truths:
    - "ParcelsTable exibe 5ª coluna 'Ação' no header e em todas as rows"
    - "Rows com status pendente/vencida exibem '[>] PAGAR' que chama onPagar(parcela)"
    - "Rows com status paga exibem '[↓] RECIBO' que dispara download do PDF"
    - "Rows com status futura exibem '—' na coluna Ação"
    - "PDF baixado contém: valor mensal, parcela X/N, locatário, unidade, data_fechamento, data_vencimento, data_pagamento, forma PIX, código de autenticação"
    - "PDF gerado via import dinâmico — npm run build exit 0 (sem crash SSR)"
    - "Código de autenticação é determinístico (mesma parcela.id + data_pagamento → mesmo código)"
  artifacts:
    - path: "src/components/features/portal/ParcelsTable.js"
      provides: "Tabela de parcelas com coluna Ação — pagar/recibo/placeholder"
      contains: "handleBaixarRecibo"
  key_links:
    - from: "ParcelsTable.js handleBaixarRecibo"
      to: "await import('jspdf')"
      via: "import dinâmico client-side — evita crash SSR"
      pattern: "await import\\('jspdf'\\)"
    - from: "ParcelsTable.js"
      to: "onPagar prop"
      via: "botão [>] PAGAR → onPagar(parcela) → PixModal em PortalDashboard"
      pattern: "onPagar\\(parcela\\)"
---

<objective>
Estender `ParcelsTable.js` com a coluna "Ação" (botões Pagar / Recibo / placeholder), incluindo a geração de recibo PDF client-side via jsPDF com import dinâmico (PORT-05 row-level + PORT-07).

Purpose: Fecha o loop PORT-04 (histórico completo com ação por parcela), PORT-05 (entrada Pagar do histórico), e PORT-07 (comprovante PDF das parcelas pagas). O import dinâmico de jsPDF é crítico para evitar crash SSR em Vercel — testado via `npm run build`.

Output:
- `ParcelsTable.js` estendido com 5ª coluna, `handleBaixarRecibo` com jsPDF dinâmico, `gerarCodigoAuth`
</objective>

<execution_context>
@/home/artursantana/.claude/get-shit-done/workflows/execute-plan.md
@/home/artursantana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-portal-do-locat-rio-pix-recibo/25-CONTEXT.md
@.planning/phases/25-portal-do-locat-rio-pix-recibo/25-UI-SPEC.md
@.planning/phases/25-portal-do-locat-rio-pix-recibo/25-PATTERNS.md
@.planning/phases/25-portal-do-locat-rio-pix-recibo/25-02-SUMMARY.md
@.planning/phases/25-portal-do-locat-rio-pix-recibo/25-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender ParcelsTable.js — coluna Ação + handleBaixarRecibo (jsPDF dinâmico)</name>
  <files>src/components/features/portal/ParcelsTable.js</files>

  <read_first>
    - src/components/features/portal/ParcelsTable.js — arquivo completo; notar: grid grid-cols-[60px_1fr_1fr_1.2fr] (linha ~10 e ~25), min-w-[480px] (linha ~9), header row divs (linhas ~11–14), row divs e cells (linhas ~21–48), import de StatusBadge e { fmtData, cn }
    - src/components/features/portal/PortalDashboard.js — confirmar props passadas para ParcelsTable: parcelas, locatario, contrato, onPagar
    - .planning/phases/25-portal-do-locat-rio-pix-recibo/25-UI-SPEC.md — Surface 3 (coluna Ação classes exatas) e Surface 5 (layout PDF y-positions completos)
    - .planning/phases/25-portal-do-locat-rio-pix-recibo/25-PATTERNS.md — seção "ParcelsTable.js — add Ação column" (grid change, header cell, row action cell, handleBaixarRecibo, codigoAuth btoa)
    - .planning/phases/25-portal-do-locat-rio-pix-recibo/25-RESEARCH.md — Pitfall 1 (defensive import jsPDF: mod.jsPDF ?? mod.default?.jsPDF ?? mod.default) e Pattern 2 (import dinâmico)
  </read_first>

  <behavior>
    - ParcelsTable aceita props: `{ parcelas, locatario, contrato, onPagar }`
    - Header row tem 5ª célula "AÇÃO" com classe font-mono text-[10px] font-bold tracking-[1.4px] uppercase text-fg-4
    - Grid row tem 5ª célula com: button "[>] PAGAR" para pendente/vencida; button "[↓] RECIBO" para paga; "—" para futura
    - Clicar "[>] PAGAR" → onPagar(parcela)
    - Clicar "[↓] RECIBO" → handleBaixarRecibo(parcela, locatario, contrato, parcelas.length)
    - handleBaixarRecibo usa `await import('jspdf')` com defensive: `mod.jsPDF ?? mod.default?.jsPDF ?? mod.default`
    - PDF salvo como `recibo-parcela-XX.pdf` (numero com padStart 2)
    - gerarCodigoAuth usa btoa approach (D-05): `btoa(parcelaId.slice(0,8) + (dataPagamento ?? '')).replace(/[^A-Z0-9]/gi,'').toUpperCase().slice(0,8)`
    - Falha no PDF → setErroPDF com mensagem (não crash, não toast — mensagem inline)
    - 'use client' NECESSÁRIO pois ParcelsTable agora usa useState para erroPDF e handleBaixarRecibo usa eventos
  </behavior>

  <action>
    Modificar `src/components/features/portal/ParcelsTable.js` com as seguintes alterações:

    **1. Adicionar `'use client'` no topo do arquivo** (ParcelsTable agora usa useState):
    ```
    'use client'
    ```

    **2. Adicionar import de useState:**
    `import { useState } from "react"`
    (junto com StatusBadge e { fmtData, cn } já existentes)

    **3. Alterar assinatura:**
    `export default function ParcelsTable({ parcelas, locatario, contrato, onPagar })`

    **4. Adicionar estado dentro do componente (antes do return):**
    `const [erroPDF, setErroPDF] = useState(null)`

    **5. Adicionar função `gerarCodigoAuth` (dentro do componente ou como módulo-level helper):**
    ```
    function gerarCodigoAuth(parcelaId, dataPagamento) {
      return btoa(parcelaId.slice(0, 8) + (dataPagamento ?? ''))
        .replace(/[^A-Z0-9]/gi, '')
        .toUpperCase()
        .slice(0, 8)
    }
    ```

    **6. Adicionar função `handleBaixarRecibo` (dentro do componente, após gerarCodigoAuth):**
    ```
    async function handleBaixarRecibo(parcela, totalParcelas) {
      try {
        const mod = await import('jspdf')
        const JsPDF = mod.jsPDF ?? mod.default?.jsPDF ?? mod.default
        if (!JsPDF) throw new Error('jsPDF não carregou')
        const doc = new JsPDF()
        const codigoAuth = gerarCodigoAuth(parcela.id, parcela.data_pagamento)

        // Header (UI-SPEC Surface 5 y-positions)
        doc.setFont('helvetica', 'bold')
        doc.setFontSize(14)
        doc.text('ROMMA — COMPROVANTE DE PAGAMENTO', 105, 15, { align: 'center' })
        doc.line(20, 22, 190, 22)

        // Dados da parcela
        doc.setFontSize(10)
        doc.text('DADOS DA PARCELA', 20, 32)
        doc.setFont('helvetica', 'normal')
        doc.text(`Locatário:    ${locatario?.nome_razao_social ?? '—'}`, 20, 40)
        doc.text(`Unidade:      ${contrato?.unidades?.nome ?? '—'}`, 20, 48)
        doc.text(`Parcela:      ${parcela.numero} de ${totalParcelas}`, 20, 56)
        const valorFormatado = contrato?.unidades?.valor_mensal
          ? new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(contrato.unidades.valor_mensal / 100)
          : '—'
        doc.text(`Valor mensal: ${valorFormatado}`, 20, 64)

        // Datas
        doc.setFont('helvetica', 'bold')
        doc.text('DATAS', 20, 76)
        doc.setFont('helvetica', 'normal')
        const fmtPDF = (d) => d ? new Date(d + 'T12:00:00').toLocaleDateString('pt-BR') : '—'
        doc.text(`Fechamento:   ${fmtPDF(parcela.data_fechamento)}`, 20, 84)
        doc.text(`Vencimento:   ${fmtPDF(parcela.data_vencimento)}`, 20, 92)
        doc.text(`Pagamento:    ${fmtPDF(parcela.data_pagamento)}`, 20, 100)

        // Pagamento
        doc.setFont('helvetica', 'bold')
        doc.text('PAGAMENTO', 20, 112)
        doc.setFont('helvetica', 'normal')
        doc.text('Forma:              PIX', 20, 120)
        doc.text(`Código de auth.:    ${codigoAuth}`, 20, 128)

        // Footer
        doc.line(20, 145, 190, 145)
        doc.setFontSize(8)
        doc.setTextColor(120, 120, 120)
        doc.text('Este documento é gerado automaticamente pelo sistema Romma.', 20, 152)
        doc.text('Código de autenticação identifica unicamente este comprovante.', 20, 158)

        doc.save(`recibo-parcela-${String(parcela.numero).padStart(2, '0')}.pdf`)
        setErroPDF(null)
      } catch (e) {
        setErroPDF('Não foi possível gerar o comprovante. Tente novamente.')
      }
    }
    ```

    **NOTA sobre valor_mensal:** O schema armazena valor_mensal. Verificar se está em centavos (inteiro) ou em reais (decimal) consultando queries existentes do projeto. Se `fmtBRL` já existe em `@/lib/utils`, usar `fmtBRL(contrato?.unidades?.valor_mensal)` em vez de Intl.NumberFormat manual.

    **7. Alterar grid classes:**
    - Container inner: `min-w-[480px]` → `min-w-[600px]`
    - Header div: `grid-cols-[60px_1fr_1fr_1.2fr]` → `grid-cols-[60px_1fr_1fr_1fr_1.4fr]`
    - Rows div: `grid grid-cols-[60px_1fr_1fr_1.2fr]` → `grid grid-cols-[60px_1fr_1fr_1fr_1.4fr]` (em AMBOS — header e rows)

    **8. Adicionar header cell "AÇÃO" após o header cell de "Status":**
    ```
    <div className="px-5 py-3 font-mono text-[10px] font-bold tracking-[1.4px] uppercase text-fg-4">Ação</div>
    ```

    **9. Adicionar action cell nas rows, após a cell de StatusBadge:**
    ```
    <div className="px-5 py-[14px]">
      {(parcela.status === 'pendente' || parcela.status === 'vencida') && (
        <button
          style={{ all: 'unset', cursor: 'pointer' }}
          className="font-mono text-[10px] font-bold tracking-[1px] uppercase text-indigo hover:opacity-70 transition-opacity"
          aria-label={`Pagar parcela ${String(parcela.numero).padStart(2, '0')}`}
          onClick={() => onPagar(parcela)}
        >
          [&gt;] PAGAR
        </button>
      )}
      {parcela.status === 'paga' && (
        <button
          style={{ all: 'unset', cursor: 'pointer' }}
          className="font-mono text-[10px] font-bold tracking-[1px] uppercase text-fg-4 hover:text-fg-2 transition-colors"
          aria-label={`Baixar comprovante da parcela ${String(parcela.numero).padStart(2, '0')}`}
          onClick={() => handleBaixarRecibo(parcela, parcelas.length)}
        >
          [↓] RECIBO
        </button>
      )}
      {(parcela.status === 'futura' || (!['pendente','vencida','paga'].includes(parcela.status))) && (
        <span className="font-mono text-[10px] text-fg-5">—</span>
      )}
    </div>
    ```

    **10. Adicionar exibição de erroPDF inline** (abaixo do container da tabela, antes do fechamento de `<section>`):
    ```
    {erroPDF && (
      <p className="mt-2 font-mono text-[11px] text-danger-fg">{erroPDF}</p>
    )}
    ```
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <done>
    `src/components/features/portal/ParcelsTable.js` tem `'use client'`, aceita props `{ parcelas, locatario, contrato, onPagar }`, exibe 5ª coluna "Ação", contém `handleBaixarRecibo` com `await import('jspdf')` e defensive destructuring, contém `gerarCodigoAuth` com btoa approach. `npm run build` exit 0 (sem crash SSR — import dinâmico no handler evita execução em SSR).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    ParcelsTable com coluna Ação — botão [>] PAGAR nas pendente/vencida, botão [↓] RECIBO nas pagas, geração de PDF com jsPDF.
  </what-built>
  <how-to-verify>
    1. Iniciar servidor: `npm run dev`
    2. Fazer login como Locatário em http://localhost:3000/portal/dashboard
    3. Verificar que o histórico de parcelas tem coluna "AÇÃO":
       - Parcelas pendentes/vencidas: botão "[>] PAGAR" visível, indigo
       - Parcelas pagas: botão "[↓] RECIBO" visível, fg-4
       - Parcelas futuras: "—" na coluna
    4. Clicar "[↓] RECIBO" em uma parcela paga → PDF deve baixar automaticamente com nome "recibo-parcela-XX.pdf"
    5. Abrir o PDF e verificar que contém:
       - Nome do locatário
       - Nome da unidade
       - Número da parcela e total
       - Valor mensal
       - Data de fechamento, vencimento e pagamento
       - "Forma: PIX"
       - "Código de auth.: XXXXXXXX" (8 caracteres uppercase)
    6. Verificar no browser DevTools (Network) que NÃO há request HTTP para gerar o PDF — tudo client-side
    7. Verificar que `npm run build` termina com exit 0 (SSR safety)
  </how-to-verify>
  <resume-signal>Digite "aprovado" se o PDF baixa corretamente com todos os campos. Descreva qualquer campo ausente ou erro para correção.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → jsPDF | Import dinâmico client-side; dados do PDF vêm de state RLS-protegido |
| Client Component → onPagar callback | parcela.id passado a PixModal → confirmarPagamentoLocatario (guardado no Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-12 | Information Disclosure | PDF com dados do locatário (client-side) | accept | Dados carregados via RLS (locatário só vê seu contrato); PDF não é enviado a servidor; não há vazamento cross-tenant |
| T-25-13 | Tampering | gerarCodigoAuth — uso como "autenticação" | accept | Código é display-only para TCC (identificador reproduzível, não criptograficamente seguro — PORT-F1 pós-banca); D-05 documenta essa decisão |
| T-25-14 | Denial of Service | jsPDF import falha em runtime | mitigate | try/catch em handleBaixarRecibo — falha exibe erroPDF inline, não crasha a página |
| T-25-15 | Tampering | import('jspdf') em SSR crasharia em Vercel | mitigate | Import APENAS dentro de handleBaixarRecibo (handler de clique) — nunca no topo do módulo; verificado com npm run build |
| T-25-SC | Tampering | npm install (nenhum novo pacote) | accept | jsPDF já instalado (package.json, RESEARCH.md slopcheck [OK]) |
</threat_model>

<verification>
`npm run build` exit 0 — confirmação crítica de que jsPDF não crashou o SSR.

`npx vitest run` exit 0 — suite completa green, sem regressão.

PDF baixado contém todos os 7 campos obrigatórios de PORT-07: valor, parcela X/N, locatário, unidade, datas (fechamento/vencimento/pagamento), forma PIX, código de autenticação.

Código de autenticação é reproduzível: mesma parcela.id + data_pagamento → mesmo código.

ParcelsTable coluna Ação: pendente/vencida → [>] PAGAR; paga → [↓] RECIBO; futura → —.
</verification>

<success_criteria>
- ParcelsTable.js tem 'use client', aceita { parcelas, locatario, contrato, onPagar }
- ParcelsTable.js contém handleBaixarRecibo com `await import('jspdf')` e defensive destructuring
- ParcelsTable.js contém gerarCodigoAuth com btoa approach (determinístico)
- PDF baixado verificado manualmente com todos os campos PORT-07
- `npm run build` exit 0 (CRÍTICO — SSR safety do jsPDF)
- `npx vitest run` exit 0
</success_criteria>

<output>
Criar `.planning/phases/25-portal-do-locat-rio-pix-recibo/25-04-SUMMARY.md` quando concluído.
</output>
