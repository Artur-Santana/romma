---
phase: 25-portal-do-locat-rio-pix-recibo
plan: 03
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - src/components/features/portal/PixModal.js
  - public/pix-qr.png
autonomous: true
requirements:
  - PORT-05
  - PORT-06
must_haves:
  truths:
    - "PixModal exibe QR estático (/pix-qr.png, 200px) centralizado"
    - "PixModal exibe código PIX copia-e-cola com botão '[>] COPIAR CÓDIGO' → '[✓] COPIADO' (2s reset)"
    - "PixModal exibe nota obrigatória de demo: 'Este é um ambiente de demonstração. O pagamento real não é processado...'"
    - "Clicar '[✓] CONFIRMAR' chama confirmarPagamentoLocatario(parcela.id) e fecha modal em sucesso"
    - "Loading state exibe '[···] CONFIRMANDO' e desabilita ambos os botões"
    - "Erro do server action exibe mensagem inline; botões re-habilitados"
    - "Escape fecha modal; click no backdrop NÃO fecha"
    - "public/pix-qr.png existe (arquivo PNG válido)"
  artifacts:
    - path: "src/components/features/portal/PixModal.js"
      provides: "Modal PIX completo — QR, copia-e-cola, nota demo, confirmar pagamento"
      contains: "export default function PixModal"
    - path: "public/pix-qr.png"
      provides: "QR code estático único (D-01)"
  key_links:
    - from: "PixModal.js"
      to: "confirmarPagamentoLocatario"
      via: "import from '@/actions/parcelas'"
      pattern: "confirmarPagamentoLocatario"
    - from: "PixModal.js"
      to: "navigator.clipboard.writeText"
      via: "handleCopiar try/catch"
      pattern: "navigator\\.clipboard\\.writeText"
---

<objective>
Criar o componente `PixModal.js` completo e o asset estático `public/pix-qr.png` — PORT-05 (modal PIX) + complemento do PORT-06 (chamada a confirmarPagamentoLocatario da UI).

Purpose: O PixModal é o ponto de confirmação de pagamento do Locatário. Integra QR estático (D-01), cópia de código PIX, nota de demo obrigatória (PORT-05), e chama `confirmarPagamentoLocatario` (implementada no Plan 01, D-02) para persistir o pagamento.

Output:
- `src/components/features/portal/PixModal.js` (substitui stub criado pelo Plan 02)
- `public/pix-qr.png` (QR estático único)
</objective>

<execution_context>
@/home/artursantana/.claude/get-shit-done/workflows/execute-plan.md
@/home/artursantana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-portal-do-locat-rio-pix-recibo/25-CONTEXT.md
@.planning/phases/25-portal-do-locat-rio-pix-recibo/25-UI-SPEC.md
@.planning/phases/25-portal-do-locat-rio-pix-recibo/25-PATTERNS.md
@.planning/phases/25-portal-do-locat-rio-pix-recibo/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar public/pix-qr.png + PixModal.js completo</name>
  <files>public/pix-qr.png, src/components/features/portal/PixModal.js</files>

  <read_first>
    - src/components/features/portal/PortalDashboard.js — confirmar como PixModal é chamado: props open, parcela, contrato, onClose, onSucesso
    - src/components/ui/ConfirmDialog.js — padrão de modal: romma-modal-backdrop, stopPropagation, estrutura de botões, guard `if (!open) return null`
    - .planning/phases/25-portal-do-locat-rio-pix-recibo/25-UI-SPEC.md — Surface 4 (PixModal estrutura JSX completa, state machine, mobile constraints, animation rSheetUp)
    - .planning/phases/25-portal-do-locat-rio-pix-recibo/25-PATTERNS.md — seção "PixModal.js (NEW)": PIX_CODE_CONST, handleConfirmar, handleCopiar, IC-03 error feedback, IC-04 Escape key pattern
    - src/components/features/Contratos.js — padrão de chamada a Server Action com loading state (linhas ~145–155) para confirmar convenção
    - public/ — verificar se public/hero-building.png ou similar existe para confirmar que public/ é o local correto de assets estáticos
  </read_first>

  <action>
    **Criar `public/pix-qr.png`:**
    Gerar um arquivo PNG placeholder de QR code para demo (qualquer QR código estático). Uma forma simples é criar um PNG mínimo via comando bash. O conteúdo exato do QR não importa para o TCC — é demonstrativo. O arquivo DEVE ser um PNG válido em `public/pix-qr.png` para que `<img src="/pix-qr.png">` funcione. Usar um QR code de placeholder gerado com `npx qrcode <valor>` ou copiar um PNG simples existente no projeto. Se não houver ferramenta disponível, criar o arquivo com ImageMagick: `convert -size 200x200 xc:white -fill black -draw "rectangle 10,10 190,190" public/pix-qr.png` (fallback: qualquer PNG válido de 200x200 serve para demo).

    **Criar `src/components/features/portal/PixModal.js`** (substitui o stub da Plan 02):

    Diretiva no topo: `'use client'`

    Imports:
    ```
    import { useState, useEffect } from "react"
    import { cn } from "@/lib/utils"
    import { fmtBRL, fmtData } from "@/lib/utils"
    import { confirmarPagamentoLocatario } from "@/actions/parcelas"
    ```

    Constante no nível do módulo (D-01 — string fixa, não por-proprietário):
    ```
    const PIX_CODE_CONST = "00020126580014br.gov.bcb.pix0136a1b2c3d4-e5f6-7890-abcd-ef1234567890520400005303986540510.005802BR5911ROMMA DEMO6009SAO PAULO62070503***6304ABCD"
    ```
    (String PIX BR Code de demonstração — não é um BR Code real válido, apenas demonstrativo)

    **Assinatura:** `export default function PixModal({ open, parcela, contrato, onClose, onSucesso })`

    **Guard:** `if (!open || !parcela) return null`

    **Estado** (objeto único, per convenção do projeto):
    `const [modal, setModal] = useState({ loading: false, erro: null, copiado: false, copiouErro: false })`

    **Escape key** (per IC-04):
    ```
    useEffect(() => {
      function onKey(e) {
        if (e.key === 'Escape' && !modal.loading) onClose()
      }
      document.addEventListener('keydown', onKey)
      return () => document.removeEventListener('keydown', onKey)
    }, [modal.loading, onClose])
    ```

    **handleCopiar** (per IC-03 — failure feedback, não crash):
    ```
    async function handleCopiar() {
      try {
        await navigator.clipboard.writeText(PIX_CODE_CONST)
        setModal(m => ({ ...m, copiado: true, copiouErro: false }))
        setTimeout(() => setModal(m => ({ ...m, copiado: false })), 2000)
      } catch {
        setModal(m => ({ ...m, copiouErro: true, copiado: false }))
        setTimeout(() => setModal(m => ({ ...m, copiouErro: false })), 2000)
      }
    }
    ```

    **handleConfirmar**:
    ```
    async function handleConfirmar() {
      setModal(m => ({ ...m, loading: true, erro: null }))
      const res = await confirmarPagamentoLocatario(parcela.id)
      if (res.status === 200) {
        setModal({ loading: false, erro: null, copiado: false, copiouErro: false })
        onClose()
        onSucesso()
      } else {
        setModal(m => ({ ...m, loading: false, erro: res.erroMessage ?? "Erro ao confirmar pagamento. Tente novamente." }))
      }
    }
    ```

    **handleFechar** (reset estado + chamar onClose):
    ```
    function handleFechar() {
      setModal({ loading: false, erro: null, copiado: false, copiouErro: false })
      onClose()
    }
    ```

    **JSX estrutura** (per UI-SPEC Surface 4):
    ```
    <div className="romma-modal-backdrop z-[100]" aria-modal="true" role="dialog">
      <div onClick={e => e.stopPropagation()} className="bg-surface border border-border-3 w-full max-w-md p-7 flex flex-col gap-5 mx-4">

        {/* Header */}
        <div className="flex flex-col gap-1">
          <span className="eyebrow eyebrow--indigo">PAGAMENTO VIA PIX</span>
          <span className="font-display font-bold text-[32px] leading-none tracking-[-1.6px] text-fg-1">
            {fmtBRL(contrato?.unidades?.valor_mensal)}
          </span>
          <span className="font-mono text-[11px] text-fg-3">
            Parcela {parcela.numero} · Vencimento: {fmtData(parcela.data_vencimento)}
          </span>
        </div>

        {/* QR Code */}
        <div className="flex justify-center">
          <img
            src="/pix-qr.png"
            alt="QR Code PIX estático"
            className="w-[160px] sm:w-[200px] h-auto block"
          />
        </div>

        {/* Código copia-e-cola */}
        <div className="flex flex-col gap-2">
          <span className="font-mono text-[10px] font-bold tracking-[1.4px] uppercase text-fg-4">CÓDIGO PIX</span>
          <div className="bg-background border border-border-3 px-4 py-3">
            <span className="font-mono text-[11px] text-fg-3 break-all select-all">{PIX_CODE_CONST}</span>
          </div>
          <button
            style={{ all: 'unset', cursor: 'pointer' }}
            className={cn(
              "font-mono text-[11px] font-bold tracking-[1px] uppercase px-4 py-2 border transition-colors w-fit",
              modal.copiado
                ? "border-success text-success"
                : modal.copiouErro
                ? "border-danger-fg text-danger-fg"
                : "border-border-3 text-fg-3 hover:text-fg-1 hover:border-fg-3"
            )}
            onClick={handleCopiar}
            disabled={modal.loading}
          >
            {modal.copiado ? "[✓] COPIADO" : modal.copiouErro ? "[!] ERRO AO COPIAR" : "[>] COPIAR CÓDIGO"}
          </button>
        </div>

        {/* Nota de demo — OBRIGATÓRIO PORT-05 */}
        <p className="font-mono text-[11px] text-fg-4 italic border-l-2 border-warning pl-3">
          Este é um ambiente de demonstração. O pagamento real não é processado. Clique em confirmar para registrar o pagamento.
        </p>

        {/* Erro inline */}
        {modal.erro && (
          <span className="font-mono text-[11px] text-danger-fg">{modal.erro}</span>
        )}

        {/* Actions */}
        <div className="flex flex-col sm:flex-row gap-3 justify-end">
          <button
            style={{ all: 'unset', cursor: 'pointer' }}
            className="font-mono text-[11px] text-fg-4 border border-border-3 px-5 py-3 hover:text-fg-2 hover:border-fg-4 transition-colors text-center"
            onClick={handleFechar}
            disabled={modal.loading}
          >
            CANCELAR
          </button>
          <button
            style={{ all: 'unset', cursor: 'pointer' }}
            className={cn(
              "bg-indigo font-mono font-bold text-[11px] tracking-[1.5px] uppercase px-5 py-3 text-fg-1 text-center transition-opacity",
              modal.loading ? "opacity-60 cursor-not-allowed" : "hover:opacity-90 cursor-pointer"
            )}
            onClick={handleConfirmar}
            disabled={modal.loading}
          >
            {modal.loading ? "[···] CONFIRMANDO" : "[✓] CONFIRMAR"}
          </button>
        </div>

      </div>
    </div>
    ```
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <done>
    `src/components/features/portal/PixModal.js` existe, não é mais o stub vazio. Contém `confirmarPagamentoLocatario`, nota de demo obrigatória ("Este é um ambiente de demonstração"), PIX_CODE_CONST, handleCopiar com try/catch, handleConfirmar com loading state. `public/pix-qr.png` existe como arquivo PNG válido. `npm run build` exit 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    PixModal completo integrado ao portal. Planos 01 + 02 + 03 foram executados: guard 3-hop testado, destaque de vencimento visível no portal, modal PIX funcional com QR estático.
  </what-built>
  <how-to-verify>
    1. Iniciar servidor de desenvolvimento: `npm run dev`
    2. Fazer login como Locatário em http://localhost:3000/portal/dashboard
    3. Verificar que VencimentoDestaque aparece com valor BRL em 32px bold e botão "[>] PAGAR AGORA" (se houver parcela pendente/vencida)
    4. Verificar que ContratoCard mostra barra de progresso + "X pagas · N total · Z% adimplente"
    5. Clicar "[>] PAGAR AGORA" → modal PIX deve abrir com:
       - QR code visível (pode ser placeholder/simples)
       - Código PIX copia-e-cola
       - Nota: "Este é um ambiente de demonstração. O pagamento real não é processado..."
       - Botão "[>] COPIAR CÓDIGO" e "[✓] CONFIRMAR"
    6. Clicar "[✓] CONFIRMAR" → loading "[···] CONFIRMANDO" → modal fecha → toast "Pagamento registrado" → parcela muda para "paga" no histórico
    7. Pressionar Escape → modal fecha
    8. Verificar que clicar no backdrop escuro não fecha o modal
  </how-to-verify>
  <resume-signal>Digite "aprovado" se o fluxo completo funciona. Descreva qualquer problema encontrado para que seja corrigido antes de prosseguir para o Plan 04.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → Server Action | handleConfirmar passa parcela.id para confirmarPagamentoLocatario |
| Browser → Clipboard API | navigator.clipboard.writeText — requer contexto seguro (HTTPS/localhost) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Information Disclosure | PIX_CODE_CONST exposto no bundle JS | accept | String de demo, não é chave secreta; nenhuma informação sensível real |
| T-25-10 | Tampering | parcela.id passado a confirmarPagamentoLocatario | mitigate | Guard 3-hop no server action (Plan 01) valida ownership; UI não tem bypass |
| T-25-11 | Spoofing | Clipboard API sem HTTPS | mitigate | try/catch em handleCopiar — falha silenciosa com feedback "[!] ERRO AO COPIAR"; Vercel garante HTTPS em produção |
| T-25-SC | Tampering | npm install (nenhum novo pacote) | accept | Nenhum pacote novo neste plano |
</threat_model>

<verification>
`npm run build` exit 0 — PixModal compila sem crash SSR (componente é client-side, `'use client'` declarado).

PixModal renderiza QR image, código copia-e-cola, nota de demo obrigatória ("Este é um ambiente de demonstração..."), botões Cancelar e Confirmar.

Clicar "CONFIRMAR" chama `confirmarPagamentoLocatario` e exibe loading state.

Escape fecha modal; click no backdrop não fecha.

`npx vitest run` exit 0 (sem regressão).
</verification>

<success_criteria>
- PixModal.js existe, tem 'use client', exporta PixModal, importa confirmarPagamentoLocatario
- PixModal.js contém PIX_CODE_CONST, nota de demo obrigatória, handleCopiar com try/catch, handleConfirmar com loading
- public/pix-qr.png existe como PNG válido
- `npm run build` exit 0
- Fluxo "Pagar Agora" → modal → confirmar → parcela paga aprovado no checkpoint visual
</success_criteria>

<output>
Criar `.planning/phases/25-portal-do-locat-rio-pix-recibo/25-03-SUMMARY.md` quando concluído.
</output>
