---
phase: 17-funda-o-tokens-mobile-modal-fixes-infra
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/components/ui/DashboardShell.js
  - src/app/portal/layout.js
  - src/components/features/UnidadesPublicas.js
  - src/components/features/LocatariosDesktop.js
  - src/components/ui/ConfirmDialog.js
autonomous: true
requirements: [REFINO-03, REFINO-04]
must_haves:
  truths:
    - "Dashboard, portal, and public-listing scrollable regions scroll without overflow on 375px (min-height:0 chain present)"
    - "The mobile bottom nav stays visible while content scrolls"
    - "LocatariosDesktop's two modals and ConfirmDialog use the .romma-modal-backdrop utility class"
    - "ConfirmDialog keeps its z-[100] escalation above other overlays"
    - "UnidadeDetailSheet (bottom sheet) is untouched"
  artifacts:
    - path: "src/components/ui/DashboardShell.js"
      provides: "minHeight:0 on scrollable <main> and its inner wrapper (inline-style convention)"
      contains: "minHeight: 0"
    - path: "src/app/portal/layout.js"
      provides: "min-h-0 on the scrollable <main>"
      contains: "min-h-0"
    - path: "src/components/features/UnidadesPublicas.js"
      provides: "min-h-0 on the flex-1 overflow-auto scroll div"
      contains: "min-h-0"
    - path: "src/components/features/LocatariosDesktop.js"
      provides: "romma-modal-backdrop on both modal overlays"
      contains: "romma-modal-backdrop"
    - path: "src/components/ui/ConfirmDialog.js"
      provides: "romma-modal-backdrop z-[100] on the dialog overlay"
      contains: "romma-modal-backdrop"
  key_links:
    - from: "modal JSX"
      to: ".romma-modal-backdrop (globals.css, plan 17-01)"
      via: "className references the utility added in plan 17-01"
      pattern: "romma-modal-backdrop"
---

<objective>
Apply the cross-cutting JSX retrofits that consume the CSS produced in plan 17-01: add the `min-height:0` flex-scroll fix to the three layout shells (dashboard, portal, public listing) so mobile 375px regions scroll without overflowing and the bottom nav stays visible (REFINO-03), and swap the two LocatariosDesktop modal backdrops + the ConfirmDialog backdrop to the `.romma-modal-backdrop` utility class so modals center in the full viewport on mobile (REFINO-04 JSX half).

Purpose: These are the surgical, low-risk edits (1-2 lines each) that turn the foundation CSS into observable behavior across every shell. The exact target lines and the inline-vs-Tailwind convention per file are pinned in 17-PATTERNS.md — follow each file's existing convention; never mix.
Output: Five edited files; no new files.

Depends on plan 17-01 (the `.romma-modal-backdrop` utility class must already exist in globals.css). Files do not overlap with plan 17-01 (globals.css) or 17-03 (migration/config).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-funda-o-tokens-mobile-modal-fixes-infra/17-RESEARCH.md
@.planning/phases/17-funda-o-tokens-mobile-modal-fixes-infra/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: min-height:0 scroll fix on the three layout shells (REFINO-03)</name>
  <read_first>
    - src/components/ui/DashboardShell.js (lines 31, 41, 45-49 — outer 100vh flex-col, content row flex:1 overflow:hidden, the scrollable `<main>` at line 45 and inner `<div>` maxWidth 1570px at line 46; this file uses INLINE `style={{}}` objects)
    - src/app/portal/layout.js (line 13 — `<main className="flex-1 overflow-auto">`; this file uses Tailwind classes)
    - src/components/features/UnidadesPublicas.js (line 123 — `<div className="flex-1 overflow-auto">` inside an `h-dvh flex-col`; Tailwind classes)
    - .planning/phases/17-funda-o-tokens-mobile-modal-fixes-infra/17-RESEARCH.md (Pitfall 2 + Code Examples scroll-fix snippets)
    - .planning/phases/17-funda-o-tokens-mobile-modal-fixes-infra/17-PATTERNS.md ("Inline Style vs Tailwind — Follow the File's Convention" table)
  </read_first>
  <action>
    Add the `min-height:0` flex-child fix to exactly the elements RESEARCH names, scoped to the shells (do NOT touch global html/body — public static pages must not change). (1) DashboardShell.js uses inline styles: add `minHeight: 0` to the scrollable `<main>` style object (currently `flex: 1, overflow: "auto", background: "var(--background)"`) AND to its immediate inner content `<div>` style object (currently `maxWidth: "1570px", margin: "0 auto", padding: "0 24px"`). (2) portal/layout.js uses Tailwind: change `<main className="flex-1 overflow-auto">` to `<main className="flex-1 overflow-auto min-h-0">`. (3) UnidadesPublicas.js uses Tailwind: change the `flex-1 overflow-auto` scroll `<div>` to add `min-h-0`. Do not alter outer shell heights (DashboardShell 100vh, portal h-screen, UnidadesPublicas h-dvh are already correct). Make no other edits.
  </action>
  <verify>
    <automated>test "$(grep -c 'minHeight: 0' src/components/ui/DashboardShell.js)" = 2 && grep -q 'flex-1 overflow-auto min-h-0' src/app/portal/layout.js && grep -q 'flex-1 overflow-auto min-h-0' src/components/features/UnidadesPublicas.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - DashboardShell.js contains exactly two `minHeight: 0` occurrences (on `<main>` and inner `<div>`).
    - portal/layout.js `<main>` className contains `min-h-0`.
    - UnidadesPublicas.js scroll `<div>` className contains `min-h-0`.
    - No edit to global html/body; outer shell heights unchanged.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>All three shells carry the min-height:0 fix on their scrollable region using each file's own style convention; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit modal backdrops to .romma-modal-backdrop (REFINO-04)</name>
  <read_first>
    - src/components/features/LocatariosDesktop.js (lines 240 and 337 — both identical: `className="fixed inset-0 z-50 bg-[oklch(0_0_0/0.7)] flex items-center justify-center"` with an `onClick` outside-click handler)
    - src/components/ui/ConfirmDialog.js (line 23 — `className="fixed inset-0 bg-[oklch(0_0_0/0.7)] flex items-center justify-center z-[100]"` with `onClick={onCancel}`)
    - src/components/features/UnidadeDetailSheet.js (line 9 — DO NOT TOUCH; `items-end` bottom sheet, distinct pattern)
    - .planning/phases/17-funda-o-tokens-mobile-modal-fixes-infra/17-PATTERNS.md (the two "Modal backdrop retrofit" sections — exact before/after)
    - src/app/globals.css (confirm `.romma-modal-backdrop` exists — produced by plan 17-01)
  </read_first>
  <action>
    Swap the repeated inline backdrop one-liners for the `.romma-modal-backdrop` utility (added in plan 17-01). (1) LocatariosDesktop.js: at BOTH occurrences (lines ~240 and ~337) replace `className="fixed inset-0 z-50 bg-[oklch(0_0_0/0.7)] flex items-center justify-center"` with `className="romma-modal-backdrop"`; leave each element's `onClick` outside-click handler unchanged. (2) ConfirmDialog.js (line ~23): replace `className="fixed inset-0 bg-[oklch(0_0_0/0.7)] flex items-center justify-center z-[100]"` with `className="romma-modal-backdrop z-[100]"` — KEEP the `z-[100]` Tailwind override because ConfirmDialog must sit ABOVE the LocatariosDesktop modals (utility supplies z-index:50; the override escalates it). Do NOT modify UnidadeDetailSheet.js (it is a bottom sheet with `items-end`). Change only the backdrop `className` strings; touch no other markup or handlers.
  </action>
  <verify>
    <automated>test "$(grep -c 'romma-modal-backdrop' src/components/features/LocatariosDesktop.js)" = 2 && grep -q 'romma-modal-backdrop z-\[100\]' src/components/ui/ConfirmDialog.js && ! grep -q 'romma-modal-backdrop' src/components/features/UnidadeDetailSheet.js && ! grep -q 'fixed inset-0 z-50 bg-\[oklch(0_0_0/0.7)\] flex items-center justify-center' src/components/features/LocatariosDesktop.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - LocatariosDesktop.js contains exactly two `romma-modal-backdrop` occurrences and zero of the old inline backdrop one-liner.
    - ConfirmDialog.js backdrop className is `romma-modal-backdrop z-[100]` (override preserved).
    - UnidadeDetailSheet.js contains no `romma-modal-backdrop` (bottom sheet untouched).
    - onClick handlers unchanged in all three.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Both LocatariosDesktop modals and ConfirmDialog use the shared backdrop utility (ConfirmDialog keeps z-[100]); the bottom sheet is untouched; build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0.
- `grep -c 'romma-modal-backdrop' src/components/features/LocatariosDesktop.js` returns 2; ConfirmDialog has `romma-modal-backdrop z-[100]`.
- DashboardShell has two `minHeight: 0`; portal/layout and UnidadesPublicas have `min-h-0` on their scroll regions.
- Manual (deferred to phase UAT): at 375px, dashboard/portal/`/unidades` scroll without overflow with bottom nav visible, and modals center full-viewport (REFINO-03/04 visual — see 17-VALIDATION.md Manual-Only table).
</verification>

<success_criteria>
- The three shells scroll correctly on mobile via the min-height:0 chain, scoped to shells not global html/body (REFINO-03).
- The center-aligned modals use the shared .romma-modal-backdrop utility; the bottom sheet remains distinct (REFINO-04).
- Build green; no unrelated edits.
</success_criteria>

<output>
Create `.planning/phases/17-funda-o-tokens-mobile-modal-fixes-infra/17-02-SUMMARY.md` when done.
</output>
