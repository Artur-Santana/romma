---
phase: 17-funda-o-tokens-mobile-modal-fixes-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - test/unit/globals/v15-tokens.test.js
autonomous: true
requirements: [REFINO-01, REFINO-02, REFINO-04, REFINO-05]
must_haves:
  truths:
    - "The 8 type tokens (--rt-metric/title/title-sm/section/subhead/body/data/label/meta) resolve via var() anywhere"
    - "The regular-density tokens (--rd-gutter..--rd-row-x) resolve via var() anywhere"
    - "The .r-* helper classes (type, panel, hover, scroll, dot) exist and reference real tokens"
    - "The .romma-modal-backdrop utility exists (position:fixed; inset:0; centered)"
    - ".romma-page animation no longer uses fill `both` and is guarded by @media print and prefers-reduced-motion"
    - "No [data-density] compact/comfy selectors were introduced (REFINO-F1 deferred)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Additive v1.5 token block, .r-* helpers, keyframes, modal backdrop utility, animation retrofit"
      contains: "--rt-metric"
    - path: "test/unit/globals/v15-tokens.test.js"
      provides: "Source-assertion test for token/class presence and absence of density variants"
      min_lines: 20
  key_links:
    - from: ".r-eyebrow.gold"
      to: "--highlight alias"
      via: "var(--highlight) resolved through new --highlight: var(--color-highlight) alias"
      pattern: "--highlight:\\s*var\\(--color-highlight\\)"
    - from: ".r-title / .r-metric / .r-section"
      to: "--font-display alias"
      via: "var(--font-display) resolved through new --font-display: var(--font-display-arch) alias"
      pattern: "--font-display:\\s*var\\(--font-display-arch\\)"
    - from: ".romma-page"
      to: "rFade keyframe"
      via: "animation switched from rommaFadeIn ...both to rFade (transform-only, no fill)"
      pattern: "\\.romma-page\\s*\\{\\s*animation:\\s*rFade"
---

<objective>
Inject the complete v1.5 design-refinement CSS into `src/app/globals.css` as a clearly delimited ADDITIVE section: the 8 type-scale tokens (`--rt-*`), the regular-density tokens (`--rd-*`), required duration/alias tokens, the `.r-*` helper classes, the motion keyframes, the `.romma-modal-backdrop` utility, and the animation retrofit on `.romma-page`. Also create a Vitest source-assertion test that locks token/class presence and forbids density-variant selectors.

This is the dependency root for the whole milestone (Phases 18-25 consume these tokens/classes). Implements REFINO-01 (type scale), REFINO-02 (regular density), and the CSS halves of REFINO-04 (`.romma-modal-backdrop` utility) and REFINO-05 (`.romma-page` animation retrofit). The JSX retrofits that consume the new class and the migration/infra are separate plans.

Purpose: One source of truth for type/density so screen fonts and spacing stop fighting; zero visual regression on screens not yet refactored (everything is additive — no existing rule is changed except the single `.romma-page` animation line, which is a documented bug fix).
Output: Updated `src/app/globals.css`; new `test/unit/globals/v15-tokens.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-funda-o-tokens-mobile-modal-fixes-infra/17-RESEARCH.md
@.planning/phases/17-funda-o-tokens-mobile-modal-fixes-infra/17-PATTERNS.md
@.planning/design/styles/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v1.5 token block + .r-* helpers + keyframes (REFINO-01, REFINO-02)</name>
  <read_first>
    - src/app/globals.css (FULL READ — confirm existing tokens: line 114 --indigo, 118 --warning, 128 --success, 19 --font-body, 129 --font-mono, 97 --font-display-arch, 13 --color-highlight, 105 --surface, 106 --surface-hi, 111 --border-3, 130 --ease-crisp, fg-1..5. Confirm section-delimiter comment style at lines 36-39 / 95-96.)
    - .planning/design/styles/app.css (token values 1:1 — lines 9-31 tokens, 71-124 helper classes, 93-98 keyframes)
    - .planning/phases/17-funda-o-tokens-mobile-modal-fixes-infra/17-PATTERNS.md (section "src/app/globals.css — Token block" — the exact filtered CSS to port)
  </read_first>
  <action>
    Append a NEW clearly-delimited section to the END of the `:root`/utility area of `src/app/globals.css` (additive — do not edit any existing rule in this task). Use the project section-delimiter comment style (`/* ── Title ───… */`). Add a brand-new `:root { }` block (CSS custom properties cascade additively; a second :root is correct) containing, copied 1:1 from `.planning/design/styles/app.css` lines 9-31: the 9 `--rt-*` tokens (metric 40px, title 32px, title-sm 24px, section 20px, subhead 16px, body 14px, data 14px, label 11px, meta 10px) and the 9 regular-density `--rd-*` tokens (gutter 32px, gutter-m 20px, page-y 28px, block 24px, block-sm 16px, panel 20px, cell 20px, row-y 12px, row-x 16px). In the SAME new :root block add four alias/duration tokens required by the ported helpers but absent from globals.css: `--dur-base: 220ms;`, `--dur-fast: 120ms;`, `--font-display: var(--font-display-arch);`, and `--highlight: var(--color-highlight);` (this last alias is REQUIRED — `.r-eyebrow.gold` references `var(--highlight)` which is NOT defined in globals.css today, only `--color-highlight`/`--ds-highlight` exist; without the alias gold eyebrows render with no color). Then port the `.r-*` helper classes EXACTLY as listed in 17-PATTERNS.md (`.r-metric/.r-title/.r-section/.r-subhead/.r-body/.r-data/.r-label/.r-meta/.r-eyebrow` + `.r-eyebrow.indigo/.warning/.gold`, `.r-panel/.r-divtop/.r-divrt`, hover affordances `.r-rowlink/.r-cell/.r-ghostbtn`, scrollbars `.r-scroll/.r-noscroll`, realtime `.r-dot`). Then add the keyframes `rFade, rPulse, rUnitOut, rBar, rSheetUp, rGrow` and the `.r-fade` class. DO NOT port from app.css: the prototype reset block (app.css lines 56-68 `* { box-sizing }`, `html/body/#root`, body `--neutral`/overflow:hidden), the `[data-density="compact"]`/`[data-density="comfy"]` blocks (lines 33-54 — REFINO-F1 DEFERRED, must not appear), or the `[data-accent="indigo"]` toggle (line 90). Keep all changes additive; introduce zero `[data-density]` selectors.
  </action>
  <verify>
    <automated>grep -c -- '--rt-metric\|--rt-title-sm\|--rd-gutter\|--rd-row-x\|--dur-base\|--font-display:\|--highlight:\|\.r-metric\|\.r-eyebrow\|\.r-dot\|@keyframes rFade' src/app/globals.css | grep -qv '^0$' && ! grep -q 'data-density' src/app/globals.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -- '--rt-metric: *40px' src/app/globals.css` matches (and all 8 other --rt-* present with app.css values).
    - `grep -- '--rd-gutter: *32px' src/app/globals.css` matches (and all 8 other --rd-* present).
    - `grep -- '--font-display: *var(--font-display-arch)' src/app/globals.css` matches.
    - `grep -- '--highlight: *var(--color-highlight)' src/app/globals.css` matches.
    - `grep -- '--dur-base: *220ms' src/app/globals.css` and `--dur-fast: *120ms` match.
    - `.r-metric`, `.r-eyebrow.gold`, `.r-dot`, `@keyframes rFade` all present.
    - `grep -q 'data-density' src/app/globals.css` returns NOTHING (compact/comfy not introduced — REFINO-F1 deferred).
    - `npm run build` exits 0 (no CSS parse error).
  </acceptance_criteria>
  <done>The 8 --rt-* + 9 --rd-* tokens, the alias/duration tokens, all .r-* helpers, and the keyframes exist in globals.css; no density-variant selectors; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add .romma-modal-backdrop utility + retrofit .romma-page animation (REFINO-04 CSS, REFINO-05 CSS)</name>
  <read_first>
    - src/app/globals.css (lines 336-350 — `@keyframes rommaFadeIn` and the BUG line 350 `.romma-page { animation: rommaFadeIn 320ms var(--ease-crisp) both; }`)
    - .planning/phases/17-funda-o-tokens-mobile-modal-fixes-infra/17-RESEARCH.md (Pitfall 1 — fill:both bug; Pitfall 3 — modal backdrop; Code Examples — `.r-fade` replacement and `romma-modal-backdrop`)
    - .planning/phases/17-funda-o-tokens-mobile-modal-fixes-infra/17-PATTERNS.md ("Modal backdrop utility to add" and "Animation retrofit — modify existing line 350")
  </read_first>
  <action>
    Two edits to `src/app/globals.css`. (1) Add the `.romma-modal-backdrop` utility class (a new delimited block): `position: fixed; inset: 0; z-index: 50; background: oklch(0 0 0 / 0.70); display: flex; align-items: center; justify-content: center;` — this consolidates the repeated inline backdrop pattern; the JSX swap happens in plan 17-02. (2) Retrofit the existing `.romma-page` rule (currently `animation: rommaFadeIn 320ms var(--ease-crisp) both;`): change it to `animation: rFade 320ms var(--ease-crisp);` — dropping the `both` fill-mode AND switching from the opacity-based `rommaFadeIn` to the transform-only `rFade` keyframe added in Task 1, so the element's base state is naturally visible (no permanent opacity:0 on print/projector/paused render). Then add safeguards: `@media (prefers-reduced-motion: reduce) { .r-fade, .romma-page { animation: none; } }` and `@media print { .r-fade, .romma-page { animation: none; } }` (if Task 1 already emitted the reduced-motion guard for `.r-fade`, extend it to also list `.romma-page` rather than duplicating). Keep `rommaFadeIn` keyframe defined (other code/siblings may reference it) — only the `.romma-page` usage changes. CSS-only: zero JSX changes in this task.
  </action>
  <verify>
    <automated>grep -Pzoq '\.romma-page\s*\{\s*animation:\s*rFade' src/app/globals.css && grep -q 'romma-modal-backdrop' src/app/globals.css && ! grep -q 'rommaFadeIn 320ms var(--ease-crisp) both' src/app/globals.css && grep -q '@media print' src/app/globals.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.romma-modal-backdrop` block present with `position: fixed`, `inset: 0`, `display: flex`, `align-items: center`, `justify-content: center`.
    - `.romma-page` rule now reads `animation: rFade 320ms var(--ease-crisp);` (no `both`).
    - `grep 'rommaFadeIn 320ms var(--ease-crisp) both' src/app/globals.css` returns NOTHING.
    - A `@media print` block names `.romma-page` (and `.r-fade`); a `prefers-reduced-motion` block names `.romma-page`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>romma-modal-backdrop utility exists; .romma-page animates via transform-only rFade with no fill:both and print + reduced-motion guards; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest source-assertion test locking tokens/classes and forbidding density variants</name>
  <read_first>
    - vitest.config.mjs (include glob `test/unit/**/*.test.js`, node environment, `passWithNoTests`)
    - test/unit/actions/ (any existing *.test.js — for import/assert style and `describe/it/expect` conventions)
    - src/app/globals.css (the file under assertion — written in Tasks 1-2)
  </read_first>
  <action>
    Create `test/unit/globals/v15-tokens.test.js`. It reads `src/app/globals.css` as a UTF-8 string (via `node:fs` `readFileSync` with a path resolved from `import.meta.url` or `process.cwd()`) and asserts with `expect(...).toMatch(...)` / regex: (a) each of the 9 `--rt-*` tokens is present with its app.css px value; (b) each of the 9 regular `--rd-*` tokens present with value; (c) `--dur-base: 220ms`, `--dur-fast: 120ms`, `--font-display: var(--font-display-arch)`, `--highlight: var(--color-highlight)` present; (d) `.romma-modal-backdrop`, `.r-metric`, `.r-eyebrow`, `.r-dot`, `@keyframes rFade` present; (e) NEGATIVE assertions — the string does NOT contain `data-density` and `.romma-page` does NOT contain `rommaFadeIn ... both` (assert `expect(css).not.toMatch(/data-density/)` and `expect(css).not.toMatch(/romma-page[\s\S]*?rommaFadeIn[^;]*both/)`). This is node-environment-safe (pure string read, no DOM). Runtime well under 60s.
  </action>
  <verify>
    <automated>npx vitest run test/unit/globals/v15-tokens.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/globals/v15-tokens.test.js` exits 0 with all assertions passing.
    - The test contains at least one `.not.toMatch(/data-density/)` negative assertion (REFINO-F1 guard) and one negative assertion forbidding the `both` fill-mode on `.romma-page`.
    - File lives under `test/unit/globals/` so the vitest include glob picks it up.
  </acceptance_criteria>
  <done>A passing Vitest test asserts presence of all v1.5 tokens/classes and the absence of density variants and the fill-both bug.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (CSS parses, no Next.js error).
- `npx vitest run test/unit/globals/v15-tokens.test.js` green.
- `grep -q 'data-density' src/app/globals.css` returns nothing (REFINO-F1 not introduced).
- Manual (deferred to phase UAT): a paused/print render of a `.romma-page` shows visible content (REFINO-05 visual).
</verification>

<success_criteria>
- The 8 type tokens and the regular-density tokens exist in globals.css and resolve via var() (REFINO-01, REFINO-02).
- `.romma-modal-backdrop` utility exists for plan 17-02 to consume (REFINO-04 CSS half).
- `.romma-page` no longer leaves opacity:0 as fill state and is print/reduced-motion guarded (REFINO-05 CSS half).
- All changes additive; no existing rule altered except the single documented `.romma-page` animation bug fix; build green.
</success_criteria>

<output>
Create `.planning/phases/17-funda-o-tokens-mobile-modal-fixes-infra/17-01-SUMMARY.md` when done.
</output>
