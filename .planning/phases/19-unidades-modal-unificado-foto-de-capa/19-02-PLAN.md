---
phase: 19-unidades-modal-unificado-foto-de-capa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/crud-unidades.spec.js
  - e2e/toast-unidades.spec.js
  - public/images/unidade-exemplo.jpg
autonomous: true
requirements: [UNID-01, UNID-02, UNID-03, UNID-05]
must_haves:
  truths:
    - "E2E specs target the new modal flow (button opens UnifiedUnidadeModal, not the inline form)"
    - "E2E specs cover métricas visibility (UNID-01), live search/filter (UNID-02), and ConfirmDialog-before-delete (UNID-05)"
    - "toast-unidades delete spec performs ConfirmDialog confirmation before asserting the toast"
    - "A static example cover-photo asset exists at /public/images/unidade-exemplo.jpg for the 'usar foto de exemplo' flow"
  artifacts:
    - path: "e2e/crud-unidades.spec.js"
      provides: "modal-flow CRUD specs + métricas/busca/ConfirmDialog specs"
      contains: "romma-modal-backdrop"
    - path: "e2e/toast-unidades.spec.js"
      provides: "delete spec with ConfirmDialog confirmation step"
      contains: "Remover Unidade"
    - path: "public/images/unidade-exemplo.jpg"
      provides: "static example cover photo asset"
  key_links:
    - from: "e2e/crud-unidades.spec.js"
      to: "UnifiedUnidadeModal (Plan 03) + Unidades refactor (Plan 04)"
      via: "selectors for .romma-modal-backdrop and placeholder 'Ex: Sala 1208'"
      pattern: "romma-modal-backdrop"
---

<objective>
Lay the Wave-0 test scaffolding and the static example asset that the rest of the phase verifies against. Update `e2e/crud-unidades.spec.js` and `e2e/toast-unidades.spec.js` selectors from the inline-form flow to the new `UnifiedUnidadeModal` flow, add specs for the new métricas bar (UNID-01), live search/filter (UNID-02), and the ConfirmDialog-before-delete gate (UNID-05), and add the `/public/images/unidade-exemplo.jpg` asset that the "usar foto de exemplo" feature (D-09) references.

Purpose: Nyquist coverage — the screen refactor (Plan 04) and modal (Plan 03) must have failing tests to make green. These specs will be RED until those plans land; that is expected (this plan creates the scaffold).
Output: Updated E2E specs + new static asset.

NOTE: These specs are written against the target UI from 19-UI-SPEC.md and 19-PATTERNS.md. They are expected to fail (RED) until Plans 03 and 04 complete — this is the Wave-0 scaffold, not a regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-unidades-modal-unificado-foto-de-capa/19-VALIDATION.md
@.planning/phases/19-unidades-modal-unificado-foto-de-capa/19-UI-SPEC.md
@.planning/phases/19-unidades-modal-unificado-foto-de-capa/19-PATTERNS.md
@e2e/crud-unidades.spec.js
@e2e/toast-unidades.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static example cover-photo asset</name>
  <files>public/images/unidade-exemplo.jpg</files>
  <read_first>
    - .planning/phases/19-unidades-modal-unificado-foto-de-capa/19-UI-SPEC.md (section "Campo de Foto de Capa" — exact asset name `unidade-exemplo.jpg`, displayed with grayscale filter)
    - .planning/phases/19-unidades-modal-unificado-foto-de-capa/19-CONTEXT.md (D-09 — example photo is a static /public asset saved directly to foto_url, never uploaded to Storage)
  </read_first>
  <action>
    Create the directory `public/images/` if absent and place a real JPEG at `public/images/unidade-exemplo.jpg` (per D-09 and UI-SPEC exact filename). The image is a generic building/interior placeholder; it is displayed with a `grayscale(0.3) contrast(1.1) brightness(0.7)` filter in the preview. Reuse an existing building image already in the repo if one exists under `public/` (check `public/` first); otherwise generate a small valid JPEG (e.g. a solid/gradient dark placeholder under ~80KB) via ImageMagick `convert` or a Node script writing a minimal valid JPEG. The asset MUST be a decodable JPEG (not a renamed text file) so `<img src="/images/unidade-exemplo.jpg">` renders.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const b=fs.readFileSync('public/images/unidade-exemplo.jpg'); if(b.length<100) throw new Error('asset too small'); if(!(b[0]===0xFF&&b[1]===0xD8)) throw new Error('not a JPEG (missing SOI marker)'); console.log('OK '+b.length+' bytes')"</automated>
  </verify>
  <acceptance_criteria>
    - File `public/images/unidade-exemplo.jpg` exists.
    - CLI assertion: first two bytes are `FF D8` (valid JPEG SOI marker) and file size > 100 bytes.
    - Path matches exactly the string `'/images/unidade-exemplo.jpg'` used by the modal's "usar foto de exemplo" handler in Plan 03.
  </acceptance_criteria>
  <done>A valid, decodable JPEG exists at public/images/unidade-exemplo.jpg.</done>
</task>

<task type="auto">
  <name>Task 2: Update crud-unidades.spec.js to modal flow + add métricas/busca/ConfirmDialog specs</name>
  <files>e2e/crud-unidades.spec.js</files>
  <read_first>
    - e2e/crud-unidades.spec.js (the file being modified — current inline-form selectors lines 40-65)
    - e2e/helpers.js (login() helper + test data conventions — preserve)
    - .planning/phases/19-unidades-modal-unificado-foto-de-capa/19-PATTERNS.md (section "e2e/crud-unidades.spec.js (update selectors)" — exact OLD→NEW selector mappings + new tests to add)
    - .planning/phases/19-unidades-modal-unificado-foto-de-capa/19-UI-SPEC.md (Copywriting Contract — exact button/placeholder/label strings)
  </read_first>
  <action>
    Preserve the file's imports, admin client setup, `test.describe` wrapper, `test.use` viewport, `beforeEach` login+navigate, `beforeAll`/`afterAll` cleanup, the `E2E-` data prefix, and the `login()` helper import (PATTERNS "File structure to preserve").
    Update the create flow: after clicking "Nova Unidade", assert `.romma-modal-backdrop` is visible, fill the nome field by placeholder `Ex: Sala 1208`, select the edificio via native `<select>` using `page.selectOption('select', ...)` (FSelect renders a native select — NOT a shadcn combobox; per PATTERNS Critical Note 5), fill area via the `type=number` input, then click submit "Criar Unidade".
    Update the edit flow: click the card's "Editar", assert modal visible, scope the nome input to `.romma-modal-backdrop input[placeholder="Ex: Sala 1208"]`, change value, click "Salvar Alterações".
    Update the delete flow: click "Remover" on the card, assert `.romma-modal-backdrop` visible (ConfirmDialog uses the same backdrop), then click "Remover Unidade" to confirm.
    Add three new tests using the exact UI-SPEC copy: (a) métricas visible — assert "Área total", "MRR realizado", "Potencial em aberto", "Valores ocultos" are visible; (b) busca — fill `Buscar unidade...` with `E2E-` and assert a `/resultado/i` count appears; (c) ConfirmDialog-before-delete — click "Remover", assert backdrop + "Remover unidade?" text visible, then click "Cancelar" and assert backdrop hidden (do not actually delete).
    Use the exact button/placeholder strings from the UI-SPEC Copywriting Contract — do not invent labels.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('e2e/crud-unidades.spec.js','utf8'); ['romma-modal-backdrop','Ex: Sala 1208','Salvar Alterações','Remover Unidade','Buscar unidade...','Remover unidade?','Área total'].forEach(t=>{ if(!s.includes(t)) throw new Error('missing selector/copy: '+t)}); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: spec references `.romma-modal-backdrop` for create, edit, and delete flows.
    - Source assertion: create/edit use placeholder `Ex: Sala 1208` and submit labels `Criar Unidade` / `Salvar Alterações`.
    - Source assertion: delete flow clicks `Remover Unidade` after the backdrop appears.
    - Source assertion: edificio selection uses `page.selectOption('select', ...)` (native select), not `getByRole('combobox')`.
    - Behavior assertion: three new tests exist — métricas visibility, live busca count, and ConfirmDialog-before-delete (with Cancelar path).
    - `npx playwright test e2e/crud-unidades.spec.js --list` enumerates the new tests without a parse error.
  </acceptance_criteria>
  <done>crud-unidades.spec.js targets the modal flow and includes métricas, busca, and ConfirmDialog specs.</done>
</task>

<task type="auto">
  <name>Task 3: Update toast-unidades.spec.js delete flow to confirm via ConfirmDialog</name>
  <files>e2e/toast-unidades.spec.js</files>
  <read_first>
    - e2e/toast-unidades.spec.js (the file being modified — current delete-then-toast at lines 77-91)
    - .planning/phases/19-unidades-modal-unificado-foto-de-capa/19-PATTERNS.md (section "e2e/toast-unidades.spec.js (update delete flow)" — exact selector change)
  </read_first>
  <action>
    In the delete test, after clicking the card's "Remover" button, insert the ConfirmDialog confirmation step: assert `.romma-modal-backdrop` is visible, then click "Remover Unidade". Keep the subsequent toast assertion `Unidade removida` (with its existing timeout). Do not alter the other toast tests (criar/editar) beyond what the new modal copy requires; if those tests assert create/edit toasts they should use "Unidade criada com sucesso" / "Unidade atualizada" per the UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('e2e/toast-unidades.spec.js','utf8'); if(!s.includes('Remover Unidade')) throw new Error('missing ConfirmDialog confirm'); if(!s.includes('Unidade removida')) throw new Error('missing remove toast'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the delete test clicks `Remover` then confirms via `Remover Unidade` after `.romma-modal-backdrop` appears.
    - Source assertion: the `Unidade removida` toast assertion is retained.
    - `npx playwright test e2e/toast-unidades.spec.js --list` enumerates tests without a parse error.
  </acceptance_criteria>
  <done>toast-unidades.spec.js confirms via ConfirmDialog before asserting the removal toast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → app under test | E2E specs drive the authenticated dashboard; no production trust boundary altered |
| static asset → public web | /public/images/unidade-exemplo.jpg is publicly served (intended — it is a demo placeholder) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Information Disclosure | public/images/unidade-exemplo.jpg | accept | generic placeholder image, no PII; intentionally public per D-09 |
| T-19-06 | Tampering | E2E test data | mitigate | tests use `E2E-` prefixed data + beforeAll/afterAll cleanup; no real tenant data touched |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | no new packages (RESEARCH Package Legitimacy Audit: none); ImageMagick/Node used only if already present — no install task |
</threat_model>

<verification>
- `npx playwright test e2e/crud-unidades.spec.js e2e/toast-unidades.spec.js --list` — parses, enumerates tests (live run will be RED until Plans 03/04 land).
- Example asset is a valid JPEG (SOI marker check passes).
- Grep gates in all three tasks pass.
</verification>

<success_criteria>
- E2E specs reference the modal flow (`.romma-modal-backdrop`, `Ex: Sala 1208`, `Salvar Alterações`, `Remover Unidade`).
- New specs exist for métricas, busca, and ConfirmDialog-before-delete.
- toast-unidades delete spec confirms via ConfirmDialog.
- `public/images/unidade-exemplo.jpg` is a valid JPEG.
</success_criteria>

<output>
Create `.planning/phases/19-unidades-modal-unificado-foto-de-capa/19-02-SUMMARY.md` when done.
</output>
