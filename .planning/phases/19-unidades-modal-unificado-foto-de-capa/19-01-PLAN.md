---
phase: 19-unidades-modal-unificado-foto-de-capa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/unidades.js
  - src/lib/queries-client.js
autonomous: true
requirements: [UNID-03, UNID-04, UNID-05]
must_haves:
  truths:
    - "criarUnidade returns the new unidade id so the browser can build the Storage upload path"
    - "editarUnidade persists foto_url when present in the form patch"
    - "deletarUnidade reads foto_url and best-effort removes the orphan Storage object before the DB delete, without blocking on cleanup failure"
    - "getUnidades returns foto_url so cards/modal can render the cover photo and cleanup can find the path"
  artifacts:
    - path: "src/actions/unidades.js"
      provides: "criar/editar/deletar with foto_url support + id return + Storage cleanup"
      contains: "select('id').single()"
    - path: "src/lib/queries-client.js"
      provides: "getUnidades SELECT including foto_url"
      contains: "foto_url"
  key_links:
    - from: "src/actions/unidades.js criarUnidade"
      to: "browser upload path construction"
      via: "return { status: 200, id }"
      pattern: "return \\{ status: 200, id"
    - from: "src/actions/unidades.js deletarUnidade"
      to: "supabaseAdmin.storage 'unidades-fotos'"
      via: "remove([foto_url]) best-effort before delete"
      pattern: "storage[\\s\\S]*remove"
---

<objective>
Extend the Unidades server-action + query layer to support cover photos end-to-end: `criarUnidade` returns the new `id` (so the browser can build the `{unidade_id}/{uuid}.{ext}` Storage path per D-08), `editarUnidade` persists `foto_url`, `deletarUnidade` performs best-effort Storage cleanup of the orphan object before the DB delete, and `getUnidades` includes `foto_url` in its SELECT.

Purpose: This is the data contract for the entire phase. The modal (Plan 03) and the screen refactor (Plan 04) consume `criarUnidade`'s `id` return and `getUnidades`'s `foto_url`. No binary ever transits the server — actions only handle the path string (per D-07).
Output: Modified `src/actions/unidades.js` and `src/lib/queries-client.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-unidades-modal-unificado-foto-de-capa/19-CONTEXT.md
@.planning/phases/19-unidades-modal-unificado-foto-de-capa/19-RESEARCH.md
@.planning/phases/19-unidades-modal-unificado-foto-de-capa/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend criarUnidade (return id) and editarUnidade (persist foto_url)</name>
  <files>src/actions/unidades.js</files>
  <read_first>
    - src/actions/unidades.js (the file being modified — current criar/editar at lines 18-66)
    - .planning/phases/19-unidades-modal-unificado-foto-de-capa/19-PATTERNS.md (section "src/actions/unidades.js (extend)" — exact destructuring + insert + patch changes)
    - .planning/phases/19-unidades-modal-unificado-foto-de-capa/19-RESEARCH.md (Pitfall 1 — why criarUnidade must return id; Pitfall 3 — editar must patch foto_url)
  </read_first>
  <action>
    In `criarUnidade` (per D-04 path-flow requirement / Pitfall 1): add `foto_url` to the form destructuring at the top of the function. Change the `insert(...)` call to include `foto_url: foto_url ?? null` and chain `.select('id').single()` so the row id is returned. On success return `{ status: 200, id: data.id }` instead of `{ status: 200 }`. Keep all existing validations (nome required, edificio_id UUID, area_m2/valor_mensal numeric, status enum, edificio ownership by proprietario_id) unchanged and ahead of the insert.
    In `editarUnidade` (per D-07): add `foto_url` to the form destructuring. Add a line `if (foto_url !== undefined) patch.foto_url = foto_url` to the patch object so the path string persists. Do NOT validate `foto_url` shape server-side beyond passing it through — the browser writes either a Storage path (`{uuid}/{uuid}.ext`) or a `/images/...` static path (D-09/D-10). Keep the existing ownership chain (unidade -> edificio.proprietario_id === user.id) unchanged.
    Preserve the project's single-quote style in this file (it already uses single quotes), `erroMessage` spelling, and the local `authGuard()`/`UUID_RE` declarations.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/actions/unidades.js','utf8'); if(!/\.select\('id'\)\.single\(\)/.test(s)) throw new Error('criarUnidade missing id return'); if(!/return \{ status: 200, id: data\.id \}/.test(s)) throw new Error('criarUnidade does not return id'); if(!/patch\.foto_url = foto_url/.test(s)) throw new Error('editarUnidade missing foto_url patch'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `criarUnidade` destructures `foto_url` from `form` and its insert includes `foto_url: foto_url ?? null`.
    - Source assertion: `criarUnidade` returns `{ status: 200, id: data.id }` via `.select('id').single()`.
    - Source assertion: `editarUnidade` destructures `foto_url` and the patch object gains `foto_url` only when `foto_url !== undefined`.
    - Behavior assertion: existing validations and the `edificio.proprietario_id === user.id` ownership checks are unchanged (still present in both functions).
    - `npx next lint --file src/actions/unidades.js` (or `npx eslint src/actions/unidades.js`) reports no new errors.
  </acceptance_criteria>
  <done>criarUnidade returns the created id and accepts foto_url; editarUnidade patches foto_url when provided; ownership chain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add best-effort Storage cleanup to deletarUnidade + foto_url to getUnidades SELECT</name>
  <files>src/actions/unidades.js, src/lib/queries-client.js</files>
  <read_first>
    - src/actions/unidades.js (deletarUnidade at lines 68-85)
    - src/lib/queries-client.js (getUnidades SELECT at line 6)
    - .planning/phases/19-unidades-modal-unificado-foto-de-capa/19-PATTERNS.md (sections "deletarUnidade extension" and "src/lib/queries-client.js (extend getUnidades)")
    - .planning/phases/19-unidades-modal-unificado-foto-de-capa/19-RESEARCH.md (Pattern 3 cleanup; Pitfall 2 getUnidades missing foto_url; Open Question 1 — cleanup canonical server-side via supabaseAdmin.storage)
  </read_first>
  <action>
    In `deletarUnidade` (per D-11): change the existing `unidade` fetch `.select('edificio_id')` to `.select('edificio_id, foto_url')`. After the `edificio.proprietario_id` ownership check passes and BEFORE the `supabaseAdmin.from('unidades').delete()` call, add best-effort Storage cleanup: when `unidade.foto_url` is truthy and does NOT start with `'/'` (Storage path, not a `/images/` static asset), call `supabaseAdmin.storage.from('unidades-fotos').remove([unidade.foto_url])` wrapped so a Storage failure cannot throw or block — chain `.catch(() => {})`. The DB delete must run regardless of cleanup outcome (best-effort). Keep return shapes `{ status: 200 }` / `{ status: 500, erroMessage }` unchanged.
    In `src/lib/queries-client.js` `getUnidades`: add `foto_url` to the end of the SELECT column string (currently `'id, edificio_id, nome, descricao, area_m2, valor_mensal, valor_visivel, status'`). Do not change the `?? []` null-safety convention at the call sites.
  </action>
  <verify>
    <automated>node -e "const a=require('fs').readFileSync('src/actions/unidades.js','utf8'); if(!/select\('edificio_id, foto_url'\)/.test(a)) throw new Error('deletarUnidade missing foto_url select'); if(!/storage[\s\S]*from\('unidades-fotos'\)[\s\S]*remove\(\[unidade\.foto_url\]\)/.test(a)) throw new Error('deletarUnidade missing storage cleanup'); const q=require('fs').readFileSync('src/lib/queries-client.js','utf8'); if(!/foto_url/.test(q)) throw new Error('getUnidades missing foto_url'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `deletarUnidade` selects `edificio_id, foto_url` for the existence/ownership fetch.
    - Source assertion: cleanup block guards on `unidade.foto_url && !unidade.foto_url.startsWith('/')` and calls `supabaseAdmin.storage.from('unidades-fotos').remove([unidade.foto_url]).catch(() => {})`.
    - Behavior assertion: the cleanup runs BEFORE the DB delete and a Storage error does not prevent the DB delete (no `return` between cleanup and delete on the failure path).
    - Source assertion: `getUnidades` SELECT string contains `foto_url`.
    - `npx eslint src/actions/unidades.js src/lib/queries-client.js` reports no new errors.
  </acceptance_criteria>
  <done>deletarUnidade cleans the orphan Storage object best-effort before deleting the row; getUnidades returns foto_url.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/client → Server Action | unidade form data (incl. foto_url path string) crosses; binary never crosses |
| Server Action → Supabase (service role) | supabaseAdmin bypasses RLS; ownership must be enforced in code |
| Server Action → Supabase Storage | supabaseAdmin.storage.remove on private bucket during delete cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Elevation of Privilege | criarUnidade / editarUnidade | mitigate | authGuard() (401/403) + `edificio.proprietario_id === user.id` chain retained ahead of every write (V2/V4) |
| T-19-02 | Elevation of Privilege | deletarUnidade | mitigate | ownership chain unidade→edificio.proprietario_id checked before any delete or Storage cleanup (V4) |
| T-19-03 | Tampering | editarUnidade foto_url patch | mitigate | action persists only the path string; binary never reaches server; UUID-validated id; foto_url written verbatim, no path interpolation into queries (V5) |
| T-19-04 | Information Disclosure | deletarUnidade cleanup | accept | cleanup uses service-role on a private bucket only after ownership verified; failure ignored (best-effort) per D-11 |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | no new packages this plan (RESEARCH Package Legitimacy Audit: none) — no install task present |
</threat_model>

<verification>
- `npx eslint src/actions/unidades.js src/lib/queries-client.js` — clean.
- Grep gates in the two task `<automated>` blocks all pass.
- No new npm dependencies introduced (package.json unchanged).
</verification>

<success_criteria>
- criarUnidade returns `{ status: 200, id }` and persists `foto_url`.
- editarUnidade patches `foto_url` when provided.
- deletarUnidade removes the orphan Storage object best-effort before the DB delete, ownership-gated, non-blocking on failure.
- getUnidades SELECT includes `foto_url`.
- Ownership chain (proprietario_id) unchanged in all three mutations.
</success_criteria>

<output>
Create `.planning/phases/19-unidades-modal-unificado-foto-de-capa/19-01-SUMMARY.md` when done.
</output>
