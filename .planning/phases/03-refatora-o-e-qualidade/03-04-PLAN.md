---
phase: 03-refatora-o-e-qualidade
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - package.json
  - package-lock.json
  - .planning/ROADMAP.md
autonomous: false
requirements: [REF-01, REF-02, REF-03, REF-04, DEPL-03]
user_setup: []
must_haves:
  truths:
    - "REF-01..04 confirmados via grep como já cumpridos (ou corrigidos se algum gate falhar)"
    - "npm run lint passa sem errors (apenas warnings no-img-element deferidos)"
    - "npm run build passa"
    - "Critério de npm audit de DEPL-03 é decidido pelo usuário (vulnerabilidades next.js sem fix no 16.x)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Critério DEPL-03 ajustado conforme decisão do usuário + exceção no-img-element documentada"
  key_links:
    - from: "npm run lint"
      to: "exit 0 (sem errors)"
      via: "fixes 03-01/03-02 commitados"
      pattern: "lint"
---

<objective>
Fechar a fase: auditoria-gate de REF-01..04 (confirmação via grep), validação final de lint+build, e decisão do usuário sobre o critério npm audit de DEPL-03 (vulnerabilidades de next.js 16.x sem fix disponível).

Purpose: Garantir que o codebase está "pronto para produção" segundo critério realista, sem silenciar a divergência de DEPL-03 que exige decisão de TCC.
Output: gates de auditoria executados; ROADMAP.md com critério DEPL-03 ajustado e exceção no-img-element documentada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-refatora-o-e-qualidade/03-CONTEXT.md
@.planning/phases/03-refatora-o-e-qualidade/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate de auditoria REF-01..04 (verificação via grep)</name>
  <files>src/app/dashboard/page.js, src/actions/, src/components/features/</files>
  <read_first>
    - .planning/phases/03-refatora-o-e-qualidade/03-RESEARCH.md (seção "Auditoria REF-01..04" — comandos de verificação)
    - .planning/phases/03-refatora-o-e-qualidade/03-CONTEXT.md (D-07 a D-10 — racional de cada item já cumprido)
  </read_first>
  <action>
    Executar os 4 gates de grep do RESEARCH e confirmar cumprimento. (REF-01) `grep -n "useEffect\|useState.*usuario" src/app/dashboard/page.js` — esperado: nenhum useEffect guard duplicado nem useState(null) de usuario não-consumido. (REF-02) `grep -rn "from.*@/lib/supabase'" src/components/ src/app/dashboard/` — esperado: nenhum Client Component importando supabase.js direto. (REF-03) `grep -rn "errorMessage" src/actions/` — esperado: zero matches (apenas erroMessage). (REF-04) `grep -n "const \[form" src/components/features/GestaoEdificios.js src/components/features/Unidades.js src/components/features/Locatarios.js` — esperado: form/formEdit já são objetos únicos. Se TODOS confirmarem cumprimento (esperado por D-07..D-10), marcar como done sem alterar código. Se ALGUM gate falhar inesperadamente, corrigir o item específico seguindo o padrão do CONVENTIONS.md (NÃO consolidar editandoId/erro/loading — são UI/data state legítimos por D-10) e re-rodar o gate.
  </action>
  <verify>
    <automated>grep -rn "errorMessage" src/actions/ | wc -l | grep -q '^0$' && grep -rEn "from ['\"]@/lib/supabase['\"]" src/components/ src/app/dashboard/ | wc -l | grep -q '^0$' && grep -lE "const \[form|const \[formEdit" src/components/features/GestaoEdificios.js src/components/features/Unidades.js src/components/features/Locatarios.js | wc -l | grep -q '^3$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "errorMessage" src/actions/` retorna zero matches (REF-03)
    - `grep -rEn "from ['\"]@/lib/supabase['\"]" src/components/ src/app/dashboard/` retorna zero matches (REF-02)
    - GestaoEdificios.js, Unidades.js e Locatarios.js cada um contém `const [form` ou `const [formEdit` (REF-04 — 3 arquivos)
    - dashboard/page.js sem useEffect guard duplicado nem useState(null) de usuario não-consumido (REF-01)
    - editandoId/erro/loading NÃO foram consolidados em form objects (D-10 preservado)
  </acceptance_criteria>
  <done>Os 4 gates de grep confirmam REF-01..04 cumpridos (ou corrigidos se algum falhou); D-10 preservado.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Qual critério de npm audit adotar para DEPL-03 no TCC, dado que as vulnerabilidades HIGH de next.js não têm fix disponível no 16.x estável?</decision>
  <context>
    RESEARCH (ACHADO CRÍTICO) verificou: `npm audit --omit=dev` sai com erro 1. Estado atual: 1 HIGH (next, múltiplas CVEs) + 2 MODERATE. `npm audit fix` upgrada next 16.2.4 → 16.2.6 (latest stable 16.x), mas 16.2.6 AINDA está no range vulnerável (fix só existe na linha 17.x). Após o fix as vulnerabilidades persistem (3 HIGH + 7 MODERATE). O critério DEPL-03 original ("npm audit sem vulnerabilidades alta/crítica") NÃO é atingível com Next.js 16.x. Esta é uma decisão de defesa de TCC — cabe ao usuário, não ao planner.
  </context>
  <options>
    <option id="option-a">
      <name>npm audit fix (16.2.4→16.2.6) + ajustar critério DEPL-03</name>
      <pros>Aplica todos os patches disponíveis; critério ajustado para "sem vulnerabilidades HIGH/CRITICAL exceto next.js sem fix no 16.x" reflete a realidade; defensável na banca</pros>
      <cons>Aceita conscientemente vulnerabilidades residuais de next.js; requer documentar a exceção no ROADMAP</cons>
    </option>
    <option id="option-b">
      <name>Não rodar npm audit fix; documentar estado atual como exceção conhecida</name>
      <pros>Sem mudança de dependências (zero risco de regressão antes da banca)</pros>
      <cons>Deixa de aplicar patches que existem (postcss/ws moderate); menos defensável</cons>
    </option>
    <option id="option-c">
      <name>npm audit fix + manter critério original como "falha conhecida"</name>
      <pros>Aplica patches; honesto sobre o critério não atingido</pros>
      <cons>DEPL-03 fica formalmente "não cumprido" no roadmap, o que pode confundir o status da fase</cons>
    </option>
  </options>
  <resume-signal>Selecione: option-a, option-b ou option-c</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Aplicar decisão DEPL-03, validar lint+build, documentar exceções no ROADMAP</name>
  <files>package.json, package-lock.json, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 3 Success Criteria — critério DEPL-03 a ajustar)
    - .planning/phases/03-refatora-o-e-qualidade/03-RESEARCH.md (seção DEPL-03 — texto exato do critério ajustado sugerido)
    - .planning/phases/03-refatora-o-e-qualidade/03-CONTEXT.md (D-02 — no-img-element deferido para Fase 4)
  </read_first>
  <action>
    Aplicar a decisão do checkpoint anterior: se option-a ou option-c, rodar `npm audit fix` (não `--force`); se option-b, não tocar dependências. Em todos os casos: rodar `npm run lint` e `npm run build` e confirmar que lint passa sem ERRORS (os 8 warnings `@next/next/no-img-element` em src/app/page.js são esperados e deferidos para Fase 4 por D-02 — NÃO corrigir aqui). Atualizar .planning/ROADMAP.md Phase 3 Success Criteria item 2 conforme a decisão: reescrever o critério npm audit para a formulação escolhida (ex. option-a: "sem vulnerabilidades HIGH/CRITICAL exceto as de next.js sem fix disponível no 16.x"), e adicionar nota documentando a exceção conhecida no-img-element (8 warnings em page.js, deferidos para VIS-01/Fase 4). NÃO marcar requirement IDs como Done no ROADMAP — isso é feito na verificação da fase, não aqui.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | grep -E "^ESLint: " | grep -q "0 errors" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` não reporta nenhum error (warnings no-img-element permitidos)
    - `npm run build` completa com sucesso
    - .planning/ROADMAP.md Phase 3 Success Criteria reflete a decisão do checkpoint (critério npm audit reescrito)
    - .planning/ROADMAP.md documenta a exceção no-img-element (8 warnings, deferida para Fase 4)
    - src/app/page.js NÃO foi modificado (no-img-element é fora de escopo D-02)
  </acceptance_criteria>
  <done>Decisão DEPL-03 aplicada; lint sem errors; build passa; ROADMAP documenta critério ajustado e exceção no-img-element.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supply chain (npm registry → package-lock) | npm audit fix altera versões de dependências instaladas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | vulnerabilidades next.js residuais (HIGH sem fix no 16.x) | accept | Documentado como exceção conhecida no ROADMAP por decisão explícita do usuário (checkpoint); fix só na linha 17.x, fora de escopo pré-banca |
| T-03-SC | Tampering | npm audit fix (mudança de package-lock) | mitigate | Sem novos pacotes instalados nesta fase (RESEARCH: Package Legitimacy Audit N/A); audit fix apenas atualiza versões dentro do range existente; build validado após o fix |
</threat_model>

<verification>
- Os 4 gates de grep REF-01..04 confirmam cumprimento
- `npm run lint` sem errors; `npm run build` passa
- ROADMAP atualizado com critério DEPL-03 ajustado e exceção no-img-element
</verification>

<success_criteria>
- REF-01..04 confirmados cumpridos
- lint sem errors, build passa
- Critério npm audit decidido pelo usuário e documentado no ROADMAP
</success_criteria>

<output>
Create `.planning/phases/03-refatora-o-e-qualidade/03-04-SUMMARY.md` when done
</output>
