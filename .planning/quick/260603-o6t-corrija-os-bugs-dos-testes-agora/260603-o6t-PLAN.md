---
phase: quick
plan: 260603-o6t
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/auth-confirm.spec.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "auth-confirm.spec.js executes without TypeError at waitForURL callback"
    - "Both tests 2.1 and 2.2 can evaluate redirect URL conditions correctly"
  artifacts:
    - path: e2e/auth-confirm.spec.js
      provides: Fixed waitForURL callbacks using url.href.includes()
  key_links: []
---

<objective>
Fix TypeError in auth-confirm.spec.js where waitForURL callback receives a URL object but calls .includes() directly on it (which is not a function on URL objects).

Purpose: Unblock CI pipeline — this is a deterministic one-line fix per occurrence.
Output: auth-confirm.spec.js with url.href.includes() replacing url.includes() on lines 6 and 13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix url.includes → url.href.includes in waitForURL callbacks</name>
  <files>e2e/auth-confirm.spec.js</files>
  <action>
    Playwright's waitForURL(callback) passes a URL object to the callback, not a string. The current code calls url.includes() which does not exist on URL objects, causing TypeError.

    Replace both occurrences on lines 6 and 13:

    Before: url => url.includes('/login') && url.includes('error=invite_invalid')
    After:  url => url.href.includes('/login') && url.href.includes('error=invite_invalid')

    This applies to both test 2.1 (no params) and test 2.2 (invalid token). No other changes needed.
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma && grep -n "url.href.includes" e2e/auth-confirm.spec.js | wc -l</automated>
  </verify>
  <done>Both waitForURL callbacks use url.href.includes() — grep shows 4 occurrences of url.href.includes (2 per callback × 2 tests). No url.includes( pattern remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → app | Tests consume app redirect behavior; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Information Disclosure | auth-confirm redirect params | accept | Error params are intentional, no sensitive data in URL |
</threat_model>

<verification>
Run: grep -n "url\.includes(" e2e/auth-confirm.spec.js
Expected: no matches (all occurrences replaced with url.href.includes)

Run: grep -n "url\.href\.includes" e2e/auth-confirm.spec.js
Expected: 4 matches (2 per test × 2 tests)
</verification>

<success_criteria>
- Zero occurrences of bare url.includes( in auth-confirm.spec.js
- Four occurrences of url.href.includes in auth-confirm.spec.js (lines 6 and 13, two conditions each)
- File is syntactically valid JavaScript
</success_criteria>

<output>
Create .planning/quick/260603-o6t-corrija-os-bugs-dos-testes-agora/260603-o6t-SUMMARY.md when done
</output>
