---
phase: 03-refatora-o-e-qualidade
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/LogoutButton.js
  - src/components/features/portal/PortalDashboard.js
autonomous: false
requirements: [DEPL-03]
user_setup: []
must_haves:
  truths:
    - "Locatário logado vê um botão 'Sair' no portal"
    - "Clicar em 'Sair' chama supabase.auth.signOut() e redireciona para /login"
    - "Botão mostra 'Saindo...' durante o signOut e mensagem de erro inline em falha"
  artifacts:
    - path: "src/components/ui/LogoutButton.js"
      provides: "Client Component de logout com signOut + router.push"
      contains: "signOut"
    - path: "src/components/features/portal/PortalDashboard.js"
      provides: "LogoutButton renderizado no header do portal"
      contains: "LogoutButton"
  key_links:
    - from: "src/components/ui/LogoutButton.js"
      to: "supabase.auth.signOut"
      via: "handleLogout no onClick"
      pattern: "signOut\\(\\)"
    - from: "src/components/features/portal/PortalDashboard.js"
      to: "LogoutButton"
      via: "import + render no header"
      pattern: "LogoutButton"
---

# Phase Goal (vertical slice — D-06)

**As a** Locatário, **I want to** sair do portal pelo botão "Sair", **so that** posso encerrar minha sessão e proteger meu acesso.

> Nota de modo: Phase 3 é majoritariamente refactor/qualidade. D-06 (logout) é a única funcionalidade genuinamente user-visible da fase — daí a única slice vertical real. Os demais planos (03-01 security, 03-02 lint, 03-04 audit) são quality gates invisíveis ao usuário e não admitem decomposição em slice vertical; divergência consciente de MVP_MODE para fase de refactor, conforme UI-SPEC ("exactly one new UI element").

<objective>
Adicionar o botão de logout ao portal do Locatário (D-06): novo Client Component LogoutButton.js renderizado no header do PortalDashboard.

Purpose: Locatário precisa encerrar a sessão; hoje não há saída do portal.
Output: src/components/ui/LogoutButton.js (novo) + PortalDashboard.js renderizando-o no header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-refatora-o-e-qualidade/03-CONTEXT.md
@.planning/phases/03-refatora-o-e-qualidade/03-PATTERNS.md
@.planning/phases/03-refatora-o-e-qualidade/03-UI-SPEC.md

<interfaces>
<!-- Padrão canônico (inverso do login) — login/page.js SignInForm -->
"use client"
import { useRouter } from "next/navigation"
import { createClient } from "@/lib/supabase-browser"
const supabase = createClient()
// handler: await supabase.auth.signInWithPassword(...); router.push(...)

<!-- UI-SPEC Interaction Contract para o botão -->
Label: "Sair" (NÃO usar Logout/Desconectar/Encerrar sessão)
Placement: inline no PortalDashboard.js, top-right do header, alinhado com a eyebrow row
Visual: style={{ all: "unset", cursor: "pointer" }} + classes font-mono text-[10px] text-fg-4 tracking-[1px] uppercase hover:text-fg-2 transition-colors
Loading: texto vira "Saindo..." durante signOut (useState boolean)
Error: em falha, texto inline "Erro ao sair. Recarregue a página." em cor --danger-fg, abaixo do botão
Confirmação: nenhuma (logout não é destrutivo de dados)
Interação: click → await supabase.auth.signOut() → router.push('/login')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar LogoutButton.js</name>
  <files>src/components/ui/LogoutButton.js</files>
  <read_first>
    - src/app/login/page.js (SignInForm — padrão "use client" + createClient + useRouter + supabase.auth.* + router.push, linhas 6-9 imports e 163-177 handler)
    - src/components/features/portal/PortalDashboard.js (verificar que já tem createClient no topo — linha 9; estilo de classes do header)
    - .planning/phases/03-refatora-o-e-qualidade/03-UI-SPEC.md (Interaction Contract — label, visual, loading, error)
  </read_first>
  <action>
    Criar src/components/ui/LogoutButton.js como Client Component (`"use client"` no topo). Imports: `useRouter` de `next/navigation`, `createClient` de `@/lib/supabase-browser`, `useState` de react. Instanciar `const supabase = createClient()` no escopo do módulo (mesmo padrão de login/page.js). Componente default export `LogoutButton`: estado `const [saindo, setSaindo] = useState(false)` e `const [erro, setErro] = useState(false)`. Handler `async function handleLogout()`: setSaindo(true); `const { error } = await supabase.auth.signOut()`; se error → setErro(true), setSaindo(false), return; senão `router.push('/login')`. Render: `<button>` com `style={{ all: "unset", cursor: "pointer" }}` e className `font-mono text-[10px] text-fg-4 tracking-[1px] uppercase hover:text-fg-2 transition-colors`, label `Saindo...` quando `saindo` else `Sair`, `onClick={handleLogout}`, `disabled={saindo}`. Abaixo do botão, render condicional quando `erro`: texto inline `Erro ao sair. Recarregue a página.` com cor danger (classe text-danger-fg ou style com var(--danger-fg)). NÃO usar Logout/Desconectar/Encerrar sessão como label.
  </action>
  <verify>
    <automated>grep -q '"use client"' src/components/ui/LogoutButton.js && grep -q "signOut()" src/components/ui/LogoutButton.js && grep -q "router.push('/login')" src/components/ui/LogoutButton.js && grep -q "Sair" src/components/ui/LogoutButton.js && ! grep -qE "Logout|Desconectar|Encerrar sessão" src/components/ui/LogoutButton.js && npx eslint src/components/ui/LogoutButton.js</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/LogoutButton.js existe com `"use client"` na primeira linha
    - contém `supabase.auth.signOut()` e `router.push('/login')`
    - contém label `Sair` e estado de loading `Saindo...`
    - grep `Logout|Desconectar|Encerrar sessão` retorna zero matches (terminologia UI-SPEC respeitada)
    - `npm run lint` não reporta errors em LogoutButton.js
  </acceptance_criteria>
  <done>LogoutButton.js criado: click faz signOut e redireciona para /login, com loading e erro inline.</done>
</task>

<task type="auto">
  <name>Task 2: Renderizar LogoutButton no header do PortalDashboard</name>
  <files>src/components/features/portal/PortalDashboard.js</files>
  <read_first>
    - src/components/features/portal/PortalDashboard.js (header block / eyebrow row onde o botão será alinhado top-right; já é 'use client')
    - src/components/ui/LogoutButton.js (componente criado na Task 1)
    - .planning/phases/03-refatora-o-e-qualidade/03-UI-SPEC.md (Placement: top-right do header, alinhado com eyebrow row)
  </read_first>
  <action>
    Em src/components/features/portal/PortalDashboard.js: importar `LogoutButton` de `@/components/ui/LogoutButton`. Renderizá-lo inline no header block do portal, no canto superior direito, alinhado com a eyebrow row (usar flex com justify-between no container do header ou posicionar o botão à direita do título/eyebrow). NÃO alterar copy dos estados existentes (empty state "Nenhum contrato ativo", error "Erro ao carregar dados" — manter verbatim). NÃO modificar a lógica de fetch/contrato existente. Apenas adicionar o import e o render do botão.
  </action>
  <verify>
    <automated>grep -q "LogoutButton" src/components/features/portal/PortalDashboard.js && grep -q "Nenhum contrato ativo" src/components/features/portal/PortalDashboard.js && npx eslint src/components/features/portal/PortalDashboard.js</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/portal/PortalDashboard.js importa LogoutButton de `@/components/ui/LogoutButton`
    - PortalDashboard.js renderiza `<LogoutButton` no header
    - copy verbatim preservada: `Nenhum contrato ativo` ainda presente
    - `npm run lint` não reporta errors em PortalDashboard.js
  </acceptance_criteria>
  <done>LogoutButton aparece no header do portal, top-right, sem alterar lógica/copy existentes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Botão "Sair" no header do portal do Locatário (LogoutButton.js + render em PortalDashboard.js). Click faz supabase.auth.signOut() e redireciona para /login.</what-built>
  <how-to-verify>
    1. `npm run dev` e fazer login como Locatário em /login (credenciais de um locatário convidado)
    2. No portal, confirmar que o botão "Sair" aparece no canto superior direito do header, em monospace minúsculo
    3. Clicar em "Sair" — confirmar que o texto muda para "Saindo..." brevemente e a página redireciona para /login
    4. Tentar voltar para /portal/dashboard pela URL — confirmar que o layout redireciona de volta para /login (sessão encerrada)
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva problemas</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (LogoutButton) → Supabase Auth | signOut limpa cookie de sessão via @supabase/ssr browser client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Spoofing | sessão não invalidada após logout | mitigate | D-06: signOut() limpa cookie via @supabase/ssr; router.push('/login') navega para rota nova sem reusar cache do layout; portal/layout.js já faz redirect('/login') quando !user (ASVS V2/V3) |
| T-03-06 | Information Disclosure | signOut hand-rolled | accept | Usar supabase.auth.signOut() nativo (não hand-roll); cookie clearing delegado ao SSR client |
</threat_model>

<verification>
- LogoutButton.js existe, é client, chama signOut + router.push('/login')
- PortalDashboard renderiza o botão; copy existente preservada
- Checkpoint humano confirma fluxo de logout funcional
</verification>

<success_criteria>
- Locatário vê e usa o botão "Sair" no portal
- signOut encerra a sessão e redireciona para /login
- Voltar à URL do portal após logout redireciona para /login
</success_criteria>

<output>
Create `.planning/phases/03-refatora-o-e-qualidade/03-03-SUMMARY.md` when done
</output>
