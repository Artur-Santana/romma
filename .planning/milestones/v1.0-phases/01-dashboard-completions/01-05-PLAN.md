---
phase: 01-dashboard-completions
plan: 05
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
files_modified:
  - src/components/features/Contratos.js
autonomous: true
requirements:
  - VIS-02

must_haves:
  truths:
    - "Contratos.js não contém inline styles exceto gridTemplateColumns COL"
    - "Formulário de criação de contrato usa shadcn Input e Select"
    - "Botões VER →, CANC, ENC usam shadcn Button variant ghost com tokens Tailwind corretos"
    - "Banner de erro no formulário usa text-danger-fg e bg-[var(--danger-bg2)]"
    - "Botão submit 'Criar Contrato' usa shadcn Button com cn() para estado loading"
    - "ConfirmDialog preservado para ações destrutivas"
  artifacts:
    - path: "src/components/features/Contratos.js"
      provides: "Componente de contratos migrado para Tailwind v4 + shadcn (maior arquivo)"
  key_links:
    - from: "src/components/features/Contratos.js"
      to: "src/components/ui/button.jsx"
      via: "import { Button }"
      pattern: "import.*Button.*components/ui/button"
    - from: "src/components/features/Contratos.js"
      to: "src/components/ui/select.jsx"
      via: "import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue }"
      pattern: "import.*Select.*components/ui/select"
---

<objective>
Migrar Contratos.js (~411 linhas, maior arquivo da fase) de inline styles para Tailwind v4 + shadcn. Executado após Plans 03+04 para poder usar os padrões validados.

Purpose: Entregar a tela de Contratos (/dashboard/contratos) com visual Obsidian Blueprint consistente.
Output: Contratos.js sem inline styles (exceto gridTemplateColumns COL), formulário com shadcn Input/Select, botões com shadcn Button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-dashboard-completions/01-CONTEXT.md
@.planning/phases/01-dashboard-completions/01-PATTERNS.md
@.planning/phases/01-dashboard-completions/01-03-SUMMARY.md
@.planning/phases/01-dashboard-completions/01-04-SUMMARY.md
</context>

<interfaces>
De src/components/ui/button.jsx:
- Button({ variant, size, className, disabled, onClick, type, children })

De src/components/ui/input.jsx:
- Input({ className, type, value, onChange, placeholder, required, ...props })

De src/components/ui/select.jsx:
- Select({ value, onValueChange, children })
- SelectTrigger({ className, children })
- SelectValue({ placeholder })
- SelectContent({ children })
- SelectItem({ value, children })

De src/lib/utils.js:
- cn(...classes)

Mapeamento de tokens para Contratos.js:
- var(--surface)    → bg-surface
- var(--surface-hi) → bg-surface-hi
- var(--border-3)   → border-border-3
- var(--fg-1)       → text-fg-1
- var(--fg-3)       → text-fg-3
- var(--fg-4)       → text-fg-4
- var(--indigo)     → bg-indigo / border-indigo / text-indigo
- var(--danger)     → NÃO MAPEADO — usar text-danger-fg
- var(--danger-bg2) → NÃO MAPEADO — usar bg-[var(--danger-bg2)] como arbitrary value
- var(--success)    → text-success

Exceção justificada (manter como style={{}}):
- gridTemplateColumns: COL = "100px 1.6fr 1.6fr 1fr 1fr 1.2fr 80px" — sem equivalente Tailwind
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrar helpers e container do formulário em Contratos.js</name>
  <files>src/components/features/Contratos.js</files>
  <read_first>
    - src/components/features/Contratos.js — ler integralmente: ver imports (~linhas 1-10), inputStyle (~linhas 20-29), actionBtnStyle (~linhas 31-36), HeaderCell (~linhas 38-50), container do formulário (~linhas 171-175), inputs do formulário (~linhas 185-240), select de locatário/unidade, banner de erro (~linhas 253-257), botão submit
    - .planning/phases/01-dashboard-completions/01-PATTERNS.md — seção "src/components/features/Contratos.js" integralmente
    - src/components/ui/select.jsx — ver como importar SelectContent, SelectItem, SelectTrigger, SelectValue
  </read_first>
  <action>
    Migrar a primeira metade do arquivo: imports, helpers e formulário de criação:

    1. IMPORTS — adicionar ao bloco existente:
       import { cn } from "@/lib/utils"
       import { Button } from "@/components/ui/button"
       import { Input } from "@/components/ui/input"
       import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"

    2. REMOVER variáveis de estilo — eliminar as constantes inputStyle e actionBtnStyle (linhas ~20-36). Essas serão substituídas por className inline nos elementos.

    3. HeaderCell — substituir style={{}} por:
       className="px-5 py-3 font-mono text-[10px] font-bold tracking-[1.4px] uppercase text-fg-4"

    4. CONTAINER DO FORMULÁRIO (~linhas 171-175):
       - style={{ border: "1px solid var(--indigo)", padding: 32, ... }} → className="border border-indigo p-8 mb-8 bg-surface"

    5. INPUTS DO FORMULÁRIO — substituir cada <input style={inputStyle}> por:
       <Input className="bg-surface-hi border-border-3 text-fg-1 font-mono text-[13px] rounded-none" value={...} onChange={...} placeholder={...} required={...} />

    6. SELECT DE LOCATÁRIO E UNIDADE — substituir <select> nativo por shadcn Select:
       <Select value={form.locatario_id} onValueChange={v => setForm({ ...form, locatario_id: v })}>
         <SelectTrigger className="bg-surface-hi border-border-3 text-fg-1 font-mono text-[13px] rounded-none">
           <SelectValue placeholder="Selecionar locatário..." />
         </SelectTrigger>
         <SelectContent>
           {locatarios.map(l => <SelectItem key={l.id} value={l.id}>{l.nome_razao_social}</SelectItem>)}
         </SelectContent>
       </Select>
       (mesma estrutura para select de unidade)

    7. BANNER DE ERRO (~linhas 253-257):
       - Substituir style={{}} por: className="bg-[var(--danger-bg2)] border border-[var(--danger-bg)] border-l-2 border-l-danger-fg px-4 py-3 font-mono text-[12px] text-danger-fg mb-4"
       - CRÍTICO: usar text-danger-fg (não text-danger — token não mapeado no @theme inline)

    8. BOTÃO SUBMIT "Criar Contrato":
       <Button type="submit" disabled={loading} className={cn("bg-indigo text-fg-1 font-body font-bold text-[12px] tracking-[1.2px] uppercase px-8 py-[14px] rounded-none", loading ? "opacity-60 cursor-not-allowed" : "cursor-pointer")}>
         {loading ? "Criando..." : "Criar Contrato"}
       </Button>

    Após esta task: grep -c "inputStyle\|actionBtnStyle" deve retornar 0.
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma && grep -c "inputStyle\|actionBtnStyle" src/components/features/Contratos.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "inputStyle\|actionBtnStyle" retorna 0 (variáveis de estilo eliminadas)
    - grep "Select" src/components/features/Contratos.js retorna pelo menos 5 linhas (shadcn Select em uso)
    - grep "Input" src/components/features/Contratos.js retorna pelo menos 3 linhas (shadcn Input em uso)
    - grep "var(--danger)\b" src/components/features/Contratos.js retorna 0
    - npm run build sem erros
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrar tabela e botões de ação de linha em Contratos.js</name>
  <files>src/components/features/Contratos.js</files>
  <read_first>
    - src/components/features/Contratos.js — estado após Task 1. Focar em: container de tabela (~linhas 291-292), linhas de tabela (~linhas 316-325), células de dados, botões VER →, CANC, ENC (~linhas próximas a 325-340)
    - .planning/phases/01-dashboard-completions/01-PATTERNS.md — seção "Container de tabela", "Linha de tabela", "Botões de ação de linha"
  </read_first>
  <action>
    Migrar a segunda metade do arquivo: tabela de contratos e botões de ação de linha:

    1. CONTAINER DA TABELA (~linhas 291-292):
       - Wrapper externo: className="border border-border-3 bg-surface mb-8"
       - Header row: manter style={{ gridTemplateColumns: COL }}, adicionar className="bg-[oklch(0.26_0_0)] border-b border-border-3"

    2. LINHAS DA TABELA (~linhas 316-325):
       - manter style={{ gridTemplateColumns: COL }}, adicionar:
         className={cn("items-center", i > 0 ? "border-t border-border-3" : "")}
       - Célula de padding: substituir style={{ padding: "16px 20px" }} por className="px-5 py-4"

    3. CÉLULAS DE DADOS — migrar cada style={{}} de texto:
       - Texto primário (nome locatário, etc.): className="font-body text-[13px] text-fg-1 font-semibold"
       - Texto secundário (unidade, edifício): className="font-mono text-[10px] text-fg-4"
       - Valores BRL: className="font-body text-[13px] text-fg-2"
       - Datas: className="font-mono text-[12px] text-fg-3" (ou text-warning se expirando)
       - Para data expirando: className={cn("font-mono text-[12px]", isExpiring ? "text-warning" : "text-fg-3")}

    4. BOTÕES DE AÇÃO DE LINHA (VER →, CANC, ENC):
       - VER →: Button variant="ghost" size="sm" className="font-mono text-[10px] text-fg-3 uppercase tracking-[1px] font-bold p-0 h-auto"
       - CANC (cancelar): Button variant="ghost" size="sm" className="font-mono text-[10px] text-danger-fg uppercase tracking-[1px] font-bold p-0 h-auto"
       - ENC (encerrar): Button variant="ghost" size="sm" className="font-mono text-[10px] text-danger-fg uppercase tracking-[1px] font-bold p-0 h-auto"
       - CRÍTICO: CANC e ENC usam text-danger-fg (não text-danger)

    5. QUAISQUER outros inline styles restantes — migrar usando tokens do mapeamento.

    MANTER apenas style={{ gridTemplateColumns: COL }} como única exceção justificada.
    Preservar ConfirmDialog para ações CANC e ENC — apenas migrar o estilo dos botões trigger.
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma && grep -c "style={{" src/components/features/Contratos.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "style={{" src/components/features/Contratos.js retorna 1 (somente gridTemplateColumns: COL)
    - grep "gridTemplateColumns: COL" src/components/features/Contratos.js confirma a única exceção
    - grep "var(--danger)\b" src/components/features/Contratos.js retorna 0
    - grep "text-danger-fg" src/components/features/Contratos.js retorna pelo menos 2 linhas (CANC e ENC)
    - grep "ConfirmDialog" src/components/features/Contratos.js retorna pelo menos 1 linha (preservado)
    - npm run build sem erros
    - Funcionalidade de criar contrato + cancelar/encerrar preservada (ConfirmDialog ainda funciona)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → Server Action | Criar/cancelar/encerrar contrato passam por Server Actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Ações de contrato (criar/cancelar/encerrar) | accept | Server Actions já têm authGuard() local. Migração de estilo não altera fluxo de autorização. |
| T-05-02 | Tampering | shadcn Select (locatario_id, unidade_id) | accept | Valores de Select são UUIDs validados no Server Action. Migração de UI não altera validação server-side. |
</threat_model>

<verification>
- grep -c "style={{" src/components/features/Contratos.js — 1
- grep "gridTemplateColumns: COL" — confirma exceção
- grep "var(--danger)\b" — 0
- grep "inputStyle\|actionBtnStyle" — 0
- grep "ConfirmDialog" — ao menos 1 (preservado)
- npm run build — sem erros
</verification>

<success_criteria>
1. Contratos.js com exatamente 1 inline style (gridTemplateColumns: COL)
2. Formulário de criação de contrato usando shadcn Input e Select
3. Botões de ação de linha usando shadcn Button (ghost)
4. Ações destrutivas (CANC, ENC) usam text-danger-fg
5. ConfirmDialog preservado para confirmações
6. npm run build sem erros
</success_criteria>

<output>
Criar `.planning/phases/01-dashboard-completions/01-05-SUMMARY.md` quando concluído
</output>
