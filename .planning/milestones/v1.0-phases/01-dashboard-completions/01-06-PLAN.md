---
phase: 01-dashboard-completions
plan: 06
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
files_modified:
  - src/components/features/Unidades.js
autonomous: true
requirements:
  - VIS-02

must_haves:
  truths:
    - "Unidades.js tem UI visual completa com Tailwind v4 + shadcn (antes era HTML puro sem estilo)"
    - "Formulário de criação de unidade usa shadcn Input, Select e checkbox customizado"
    - "Checkbox de valor_visivel usa cn() para estado checked/unchecked"
    - "Lista de unidades usa UnidadeCard existente (não reescrever)"
    - "Nenhum inline style em Unidades.js exceto gridTemplateColumns se aplicável"
    - "PageHeader com eyebrow 'U.LIST · UNIDADES' e título 'Unidades.'"
  artifacts:
    - path: "src/components/features/Unidades.js"
      provides: "Componente de unidades com UI completa em Tailwind v4 + shadcn"
  key_links:
    - from: "src/components/features/Unidades.js"
      to: "src/components/ui/button.jsx"
      via: "import { Button }"
      pattern: "import.*Button.*components/ui/button"
    - from: "src/components/features/Unidades.js"
      to: "src/components/ui/PageHeader.js"
      via: "import PageHeader"
      pattern: "import.*PageHeader"
---

<objective>
Construir UI completa para Unidades.js — o único componente que é HTML puro sem nenhum estilo (173 linhas de inputs e selects sem className). Este não é migração de inline styles, é construção de UI do zero usando os padrões estabelecidos.

Purpose: Entregar a tela de Unidades (/dashboard/unidades) com visual Obsidian Blueprint consistente, completando a migração VIS-02.
Output: Unidades.js com UI completa usando Tailwind v4 + shadcn Input/Select/Button, seguindo o padrão de Contratos.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-dashboard-completions/01-CONTEXT.md
@.planning/phases/01-dashboard-completions/01-PATTERNS.md
@.planning/phases/01-dashboard-completions/01-UI-SPEC.md
@.planning/phases/01-dashboard-completions/01-03-SUMMARY.md
@.planning/phases/01-dashboard-completions/01-04-SUMMARY.md
</context>

<interfaces>
De src/components/ui/PageHeader.js (verificar assinatura atual):
- Ler o arquivo antes de usar — assinatura pode ser { eyebrow, title, subtitle, cta }

De src/components/ui/UnidadeCard.js (verificar assinatura atual):
- Ler o arquivo antes de usar — props podem incluir { unidade, onEdit, onDelete, edificios }

De src/components/ui/button.jsx:
- Button({ variant, size, className, disabled, onClick, type, children })

De src/components/ui/input.jsx:
- Input({ className, type, value, onChange, placeholder, required, ...props })

De src/components/ui/select.jsx:
- Select, SelectContent, SelectItem, SelectTrigger, SelectValue

De src/lib/utils.js:
- cn(...classes)

Estrutura alvo de Unidades.js (de PATTERNS.md):
- Wrapper: className="romma-page p-12 bg-background min-h-full"
- PageHeader com eyebrow "U.LIST · UNIDADES", título "Unidades.", subtítulo com contadores
- Formulário colapsável com className="border border-indigo p-8 mb-8 bg-surface"
- Lista de UnidadeCard com className="flex flex-col gap-0 border border-border-3 bg-surface"

Tokens CSS para Unidades.js (mesmo mapeamento dos outros componentes):
- var(--danger) → NÃO MAPEADO — usar text-danger-fg
- var(--indigo) → bg-indigo / border-indigo / text-indigo
- var(--surface) → bg-surface
- var(--border-3) → border-border-3
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Construir UI de Unidades.js com Tailwind v4 + shadcn</name>
  <files>src/components/features/Unidades.js</files>
  <read_first>
    - src/components/features/Unidades.js — ler integralmente: ver estado atual (HTML puro sem estilo), lógica de CRUD existente (insertUnidade, deleteUnidade, fetchUnidades), estados (showForm, form, loading, erro), schema de form (nome, descricao, area_m2, valor_mensal, edificio_id, status, valor_visivel)
    - src/components/features/Contratos.js — referência de estrutura visual (formulário + tabela + ações) APÓS migração do Plan 05
    - src/components/ui/PageHeader.js — ler assinatura real (props aceitas)
    - src/components/ui/UnidadeCard.js — ler assinatura real (props aceitas)
    - .planning/phases/01-dashboard-completions/01-PATTERNS.md — seção "src/components/features/Unidades.js" e "Checkbox customizado para valor_visivel"
    - src/app/login/page.js — linhas 252-273 (padrão de checkbox customizado)
  </read_first>
  <action>
    Unidades.js é HTML puro sem estilo — construir a UI preservando toda a lógica existente. Não alterar: fetchUnidades, insertUnidade, deleteUnidade, estados, handlers de form.

    1. IMPORTS — adicionar ao bloco existente:
       import { cn } from "@/lib/utils"
       import { Button } from "@/components/ui/button"
       import { Input } from "@/components/ui/input"
       import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
       import PageHeader from "@/components/ui/PageHeader" (verificar path e assinatura)

    2. WRAPPER EXTERNO — adicionar className="romma-page p-12 bg-background min-h-full" ao container principal

    3. HEADER — usar PageHeader com:
       eyebrow="U.LIST · UNIDADES"
       title="Unidades."
       subtitle={`${disponiveis} disponíveis · ${alugadas} alugadas`} (calcular contadores de unidades.filter)
       cta={{ label: showForm ? "Fechar" : "Nova Unidade", code: showForm ? "×" : "U+", onClick: () => setShowForm(v => !v) }}
       (se PageHeader não aceitar essas props exatas, adaptar para o que aceitar — ler assinatura real primeiro)

    4. FORMULÁRIO DE CRIAÇÃO — envolver em:
       {showForm && (
         <div className="border border-indigo p-8 mb-8 bg-surface">
           <span className="eyebrow eyebrow--indigo mb-5 block">NOVA UNIDADE</span>
           <form onSubmit={insertUnidade}>
             <div className="grid grid-cols-2 gap-4 mb-4">
               {/* campos aqui */}
             </div>
           </form>
         </div>
       )}

    5. CAMPOS DO FORMULÁRIO:
       - edificio_id: shadcn Select com placeholder "Selecionar edifício..."
       - nome: Input type="text" placeholder="Nome da unidade"
       - descricao: Input type="text" placeholder="Descrição" (ou <textarea> se existir no HTML atual)
       - area_m2: Input type="number" placeholder="Área (m²)"
       - valor_mensal: Input type="number" placeholder="Valor mensal (R$)"
       - status: shadcn Select com options "disponivel" / "alugada"
       - valor_visivel: checkbox customizado conforme padrão de login/page.js (ver PATTERNS.md seção checkbox)

    6. CHECKBOX valor_visivel (padrão de login/page.js linhas 252-273):
       <div role="checkbox" aria-checked={form.valor_visivel} tabIndex={0}
         onClick={() => setForm({ ...form, valor_visivel: !form.valor_visivel })}
         onKeyDown={e => e.key === " " && setForm({ ...form, valor_visivel: !form.valor_visivel })}
         className="flex items-center gap-2 cursor-pointer">
         <div className={cn("w-4 h-4 border flex items-center justify-center shrink-0",
           form.valor_visivel ? "border-indigo bg-indigo" : "border-border-3 bg-transparent")}>
           {form.valor_visivel && <svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M1.5 5L4 7.5L8.5 2.5" stroke="white" strokeWidth="1.5" strokeLinecap="square" strokeLinejoin="miter" /></svg>}
         </div>
         <span className="font-mono text-[10px] text-fg-4 tracking-[1px] uppercase">Exibir valor publicamente</span>
       </div>

    7. BOTÃO SUBMIT:
       <Button type="submit" disabled={loading} className={cn("bg-indigo text-fg-1 font-body font-bold text-[12px] tracking-[1.2px] uppercase px-8 py-[14px] rounded-none mt-4", loading ? "opacity-60 cursor-not-allowed" : "cursor-pointer")}>
         {loading ? "Salvando..." : "Criar Unidade"}
       </Button>

    8. BANNER DE ERRO:
       {erro && <div className="bg-[var(--danger-bg2)] border-l-2 border-l-danger-fg px-4 py-3 font-mono text-[12px] text-danger-fg mb-4">{erro}</div>}

    9. LISTA DE UNIDADES — usar UnidadeCard existente:
       <div className="flex flex-col gap-0 border border-border-3 bg-surface">
         {unidades.length === 0 && <p className="px-5 py-4 font-mono text-[12px] text-fg-5">Nenhuma unidade cadastrada.</p>}
         {unidades.map(unidade => <UnidadeCard key={unidade.id} unidade={unidade} {/* demais props conforme assinatura real */} />)}
       </div>

    Preservar toda a lógica de negócio existente. Não alterar fetchUnidades, insertUnidade, deleteUnidade.
    Se UnidadeCard.js tiver inline styles ao ler o arquivo, não migrar agora — registrar para task de follow-up no SUMMARY.
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma && grep -c "style={{" src/components/features/Unidades.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "style={{" src/components/features/Unidades.js retorna 0 (nenhum inline style — arquivo era HTML puro, sem gridTemplateColumns)
    - grep "PageHeader\|eyebrow" src/components/features/Unidades.js retorna pelo menos 1 linha
    - grep "Select\b" src/components/features/Unidades.js retorna pelo menos 3 linhas (shadcn Select em uso)
    - grep "Input\b" src/components/features/Unidades.js retorna pelo menos 2 linhas (shadcn Input em uso)
    - grep "valor_visivel" src/components/features/Unidades.js retorna pelo menos 3 linhas (checkbox funcional)
    - npm run build sem erros
    - Funcionalidade de criar unidade preservada
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → Server Action | Inserir/deletar unidade passa por Server Actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Formulário de criação de unidade (valor_mensal, area_m2) | accept | Server Action valida tipos e auth. Migração de UI não altera validação server-side. |
| T-06-02 | Information Disclosure | valor_visivel checkbox | accept | Campo controla visibilidade pública do valor. Lógica preservada — checkbox afeta apenas form state, Server Action aplica. |
</threat_model>

<verification>
- grep -c "style={{" src/components/features/Unidades.js — 0
- grep "Select\b" src/components/features/Unidades.js — ao menos 3 linhas
- grep "Input\b" src/components/features/Unidades.js — ao menos 2 linhas
- grep "valor_visivel" — ao menos 3 linhas (checkbox funcional)
- npm run build — sem erros
</verification>

<success_criteria>
1. Unidades.js com UI visual completa (não mais HTML puro sem estilo)
2. Formulário de criação com shadcn Input, Select e checkbox customizado para valor_visivel
3. Lista de unidades renderiza UnidadeCard existente
4. PageHeader com eyebrow "U.LIST · UNIDADES" e título "Unidades."
5. Zero inline styles (arquivo não tinha gridTemplateColumns)
6. Funcionalidade de CRUD preservada
7. npm run build sem erros
</success_criteria>

<output>
Criar `.planning/phases/01-dashboard-completions/01-06-SUMMARY.md` quando concluído.

Após concluir este plano, executar verificação final da fase:
- npx playwright test tests/dashboard.spec.js --grep @smoke — todos passam
- grep -rn "style={{" src/components/features/ | grep -v "gridTemplateColumns" — 0 resultados
- npm run build — sem erros

Estes são os critérios de aceitação da Fase 1 completa.
</output>
