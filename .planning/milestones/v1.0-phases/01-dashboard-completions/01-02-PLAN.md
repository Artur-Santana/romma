---
phase: 01-dashboard-completions
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/dashboard/page.js
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - VIS-02

must_haves:
  truths:
    - "Tile 02 desktop exibe 'MRR' como label e fmtBRL(mrr) como valor principal"
    - "Tile 03 desktop exibe 'Receita Esperada' como label e fmtBRL(totalPendente) como valor principal"
    - "Mobile stats row 2: tile esquerdo mantém label 'MRR' (já correto); tile direito muda label 'Pendentes' → 'Receita Esperada', valor {parcelas.length} → {fmtBRL(totalPendente)}, sub {fmtBRL(totalPendente)} → {parcelas.length} parcela(s) em aberto"
    - "Banner de contratos vencendo usa Tailwind v4 (sem inline styles)"
    - "Nenhum inline style em dashboard/page.js exceto os gridTemplateColumns justificados"
  artifacts:
    - path: "src/app/dashboard/page.js"
      provides: "Dashboard Server Component migrado para Tailwind v4 com tiles corretos"
  key_links:
    - from: "src/app/dashboard/page.js"
      to: "src/lib/utils.js"
      via: "import { cn, fmtBRL, fmtData }"
      pattern: "import.*cn.*fmtBRL.*utils"
---

<objective>
Migrar src/app/dashboard/page.js de inline styles para Tailwind v4 e corrigir os tiles DASH-01 (MRR) e DASH-02 (Receita Esperada). Este plano faz os testes DASH-01 e DASH-02 criados no Plan 01 passarem (GREEN).

Purpose: Entregar as métricas financeiras corretas no dashboard e migrar o arquivo mais crítico da fase.
Output: dashboard/page.js sem inline styles (exceto gridTemplateColumns), tiles 02 e 03 com dados corretos, testes DASH-01 e DASH-02 passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-dashboard-completions/01-CONTEXT.md
@.planning/phases/01-dashboard-completions/01-RESEARCH.md
@.planning/phases/01-dashboard-completions/01-PATTERNS.md
@.planning/phases/01-dashboard-completions/01-01-SUMMARY.md
</context>

<interfaces>
De src/lib/utils.js (importações necessárias):
- cn(className, ...classes) — merge de classes Tailwind com tailwind-merge
- fmtBRL(value) — formata valor numérico como "R$ 1.234,56"
- fmtData(dateString) — formata data ISO como string legível

Mapeamento de tokens CSS → Tailwind (de globals.css @theme inline):
- var(--background)  → bg-background
- var(--surface)     → bg-surface
- var(--surface-hi)  → bg-surface-hi
- var(--fg-1)        → text-fg-1
- var(--fg-2)        → text-fg-2
- var(--fg-3)        → text-fg-3
- var(--fg-4)        → text-fg-4
- var(--fg-5)        → text-fg-5
- var(--indigo)      → text-indigo / bg-indigo / border-indigo
- var(--border-3)    → border-border-3
- var(--warning)     → text-warning / border-warning
- var(--warning-bg)  → bg-warning-bg
- var(--success)     → text-success
- var(--danger)      → NÃO MAPEADO — usar text-danger-fg (existe como --color-danger-fg)

Exceções justificadas ao D-01 (manter como style={{}}):
- gridTemplateColumns com valores complexos como "repeat(4, 1fr)", "2fr 1fr", "2.4fr 1fr 1fr 1.2fr", "1fr 1fr"
- --border-2 e --indigo-soft não estão no @theme inline — usar arbitrary value [var(--border-2)]

Estrutura real de dashboard/page.js (crítico para não errar):
- DESKTOP: usa array `metricas` mapeado em JSX. Tile idx "02" = MRR (label "Contratos Ativos" → "MRR"). Tile idx "03" = Receita (label "Parcelas Pendentes" → "Receita Esperada").
- MOBILE: NÃO usa array metricas. Usa HTML hardcoded em blocos separados:
  - Stats row 1 (~linhas 347-364): tile único "Edifícios" + tile único "Contratos" — não alterar
  - Stats row 2 (~linhas 366-380): dois tiles lado a lado, gridTemplateColumns "1fr 1fr"
    - Tile esquerdo: label "MRR" (já correto), valor `{mrr >= 1000 ? ...}` (já correto) — não alterar
    - Tile direito: label "Pendentes" → "Receita Esperada", valor `{parcelas.length}` → `{fmtBRL(totalPendente)}`, sub `{fmtBRL(totalPendente)}` → `{parcelas.length} parcela(s) em aberto`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Corrigir dados de DASH-01 e DASH-02 (desktop array + mobile hardcoded)</name>
  <files>src/app/dashboard/page.js</files>
  <read_first>
    - src/app/dashboard/page.js — ler integralmente (duas passagens distintas):
      1. Linhas 1-120: encontrar array `metricas` desktop. Identificar índices dos tiles "Contratos Ativos" (idx "02") e "Parcelas Pendentes" (idx "03").
      2. Linhas 340-410: encontrar o "Stats row 2" mobile (gridTemplateColumns "1fr 1fr"). Identificar o tile direito com label "Pendentes".
    - src/lib/utils.js — confirmar assinatura de fmtBRL(value) e cn()
    - .planning/phases/01-dashboard-completions/01-PATTERNS.md — seção "Mudanças de dados DASH-01 / DASH-02"
  </read_first>
  <behavior>
    - DASH-01 desktop: tile idx "02" label deve ser "MRR", value deve ser mrr >= 1000 ? `R$${(mrr/1000).toFixed(1)}k` : fmtBRL(mrr), sub deve ser `${ativos} contrato(s) ativo(s)`
    - DASH-02 desktop: tile idx "03" label deve ser "Receita Esperada", value deve ser fmtBRL(totalPendente), sub deve ser `${parcelas.length} parcela(s) em aberto`
    - DASH-02 mobile: tile direito do stats row 2 — label "Pendentes" → "Receita Esperada", valor {parcelas.length} → {fmtBRL(totalPendente)}, sub {fmtBRL(totalPendente)} → `${parcelas.length} parcela(s) em aberto`
    - DASH-01 mobile (tile esquerdo do stats row 2): NÃO alterar — label "MRR" e valor já estão corretos
  </behavior>
  <action>
    Duas edições distintas em dashboard/page.js:

    PARTE A — Array metricas desktop (ler linhas ~70-110 para localizar):
    - Tile idx "02": mudar label "Contratos Ativos" → "MRR", mudar value de `ativos` (contagem) para `mrr >= 1000 ? \`R$${(mrr/1000).toFixed(1)}k\` : fmtBRL(mrr)`, mudar sub de `${fmtBRL(mrr)} / mês` → `${ativos} contrato(s) ativo(s)`
    - Tile idx "03": mudar label "Parcelas Pendentes" → "Receita Esperada", mudar value de `parcelas.length` (contagem) para `fmtBRL(totalPendente)`, mudar sub de `${fmtBRL(totalPendente)}` → `${parcelas.length} parcela(s) em aberto`
    - NÃO alterar tiles idx "01" (Ocupação) e idx "04" (Vencendo em 7 dias)

    PARTE B — Stats row 2 mobile (hardcoded HTML, NÃO é array — ler linhas ~366-380):
    - Localizar o div com gridTemplateColumns "1fr 1fr" dentro da seção mobile
    - No tile direito (segundo filho do grid): mudar label "Pendentes" → "Receita Esperada", mudar `{parcelas.length}` → `{fmtBRL(totalPendente)}`, mudar sub `{fmtBRL(totalPendente)}` → `{parcelas.length} parcela(s) em aberto`
    - Tile esquerdo ("MRR"): não tocar — valor e label já corretos

    Adicionar import de fmtBRL ao bloco de imports se ainda não estiver presente.
    NÃO alterar fórmulas de cálculo de mrr, totalPendente, ativos — apenas o que é exibido.
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma && npx playwright test tests/dashboard.spec.js --grep "DASH-01|DASH-02" --reporter=list 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Testes DASH-01 e DASH-02 em tests/dashboard.spec.js passam (GREEN)
    - grep "Contratos Ativos\|Parcelas Pendentes" src/app/dashboard/page.js retorna 0 linhas (labels antigos removidos)
    - grep "Receita Esperada" src/app/dashboard/page.js retorna pelo menos 2 linhas (desktop array + mobile hardcoded)
    - grep "MRR" src/app/dashboard/page.js retorna pelo menos 2 linhas (desktop array + mobile hardcoded)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrar inline styles de dashboard/page.js para Tailwind v4</name>
  <files>src/app/dashboard/page.js</files>
  <read_first>
    - src/app/dashboard/page.js — estado atual após Task 1 (ver todos os style={{}} restantes)
    - src/app/login/page.js — referência canônica de Tailwind v4 no projeto (padrão cn(), classes de token)
    - .planning/phases/01-dashboard-completions/01-PATTERNS.md — seções "Padrão de tile", "Banner de alerta vencendo", "Padrão de header de página", "Responsividade"
  </read_first>
  <action>
    Migrar TODOS os inline styles de dashboard/page.js para Tailwind v4, seguindo as receitas do PATTERNS.md. Percorrer o arquivo de cima para baixo:

    1. WRAPPER DESKTOP — substituir className="romma-desktop-only" por className="hidden md:block romma-page"
    2. WRAPPER MOBILE — substituir className="romma-mobile-only" por className="flex flex-col h-screen md:hidden"
    3. CONTAINER EXTERNO — style={{ padding: "48px 48px 80px", background: ... }} → className="p-12 pb-20 bg-background min-h-full"
    4. HEADER — migrar todos os style={{}} do bloco de cabeçalho (h2 48px, operador label, contadores) usando classes font-display, font-mono, text-fg-1, text-fg-3, text-fg-4
    5. GRID DE TILES — manter style={{ gridTemplateColumns: "repeat(4, 1fr)" }} como exceção justificada. Migrar o restante: border → border-border-3
    6. CADA TILE — aplicar padrão cn() conforme PATTERNS.md: p-7, flex flex-col gap-2, relative, border-r border-border-3 (condicional para i < 3), bg-warning-bg (condicional para m.warn), text-[9px] text-fg-5 para idx, text-[11px] tracking-[1px] uppercase para label, text-[48px] font-display font-bold leading-none tracking-[-2.4px] para value, text-[11px] para sub
    7. BANNER VENCENDO desktop (linhas ~204-229) — migrar para: className="bg-warning-bg border-l-2 border-warning px-6 py-4 mb-8 flex justify-between items-center". Texto eyebrow: "ATENÇÃO · CONTRATOS A VENCER" com className="eyebrow eyebrow--warning mb-1". Botão "Renovar →": className="font-mono text-[12px] text-warning border border-warning px-4 py-2 tracking-[0.5px] shrink-0 ml-6 no-underline"
    8. SEÇÕES CONTRATOS RECENTES e PARCELAS PENDENTES desktop — manter style={{ gridTemplateColumns }} mas migrar background, border, padding, color para Tailwind
    9. QUICK ACTIONS desktop — manter style={{ gridTemplateColumns: "repeat(4, 1fr)" }}, migrar restante
    10. SEÇÃO MOBILE (linhas ~340-460):
        - Stats row 1 e row 2: manter style={{ gridTemplateColumns: "1fr 1fr" }} (exceção justificada), migrar todos os outros style={{}} (padding, flexDirection, gap, color, fontSize) para Tailwind
        - Banner vencendo mobile: style={{ background: "var(--warning-bg)", borderLeft: "2px solid var(--warning)" }} → className="bg-warning-bg border-l-2 border-warning px-5 py-[14px] mx-5 mb-6"
        - Contratos recentes mobile e demais blocos: migrar inline styles restantes

    REGRA CRÍTICA: o banner de vencimento (DASH-03) — não remover a lógica condicional `{vencendoContratos.length > 0 && (...)}` — apenas migrar o estilo.

    EXCEÇÕES JUSTIFICADAS (manter como style={{}}):
    - gridTemplateColumns com qualquer valor — sem equivalente Tailwind
    - --border-2 se usado: usar arbitrary value [var(--border-2)]
    - oklch(0.26 0 0) para header de tabela se usado: bg-[oklch(0.26_0_0)] como arbitrary value Tailwind

    APÓS migração: confirmar que nenhum style={{}} contém propriedades que têm token Tailwind disponível (verificar contra tabela de mapeamento em RESEARCH.md).
  </action>
  <verify>
    <automated>cd /home/artursantana/Code/romma && grep -n "style={{" src/app/dashboard/page.js | grep -v "gridTemplateColumns" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "style={{" src/app/dashboard/page.js | grep -v "gridTemplateColumns" | wc -l retorna 0 (todos os inline styles sem equivalente gridTemplateColumns foram migrados)
    - grep -c "gridTemplateColumns" src/app/dashboard/page.js retorna o número esperado de style={{}} com gridTemplateColumns (cada um justificado)
    - grep "var(--background)\|var(--surface)\|var(--fg-1)\|var(--fg-4)\|var(--warning)\|var(--indigo)" src/app/dashboard/page.js retorna 0 linhas (todos os tokens mapeados substituídos)
    - npx playwright test tests/dashboard.spec.js --grep @smoke passa (DASH-01, DASH-02, DASH-03 todos verdes)
    - npm run build sem erros
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → Supabase | dashboard/page.js é Server Component — dados já passam por RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | dashboard/page.js (mrr, totalPendente) | accept | Dados só acessíveis via RLS para proprietário autenticado. Migração de estilo não altera modelo de acesso. |
</threat_model>

<verification>
- npx playwright test tests/dashboard.spec.js --grep @smoke — todos passam
- grep -c "Contratos Ativos\|Parcelas Pendentes" src/app/dashboard/page.js — 0
- grep "MRR" src/app/dashboard/page.js — aparece em desktop (array metricas) e mobile (hardcoded)
- grep "Receita Esperada" src/app/dashboard/page.js — aparece em desktop (array metricas) e mobile (hardcoded)
- grep "style={{" src/app/dashboard/page.js | grep -v "gridTemplateColumns" — 0 linhas
- npm run build — sem erros
</verification>

<success_criteria>
1. Testes DASH-01 e DASH-02 passam: tile 02 mostra "MRR" em R$, tile 03 mostra "Receita Esperada" em R$
2. DASH-02 mobile: label "Pendentes" → "Receita Esperada", valor monetário correto
3. DASH-03 verificado: banner aparece quando há contratos vencendo, usa classes Tailwind (bg-warning-bg)
4. dashboard/page.js sem inline styles que têm equivalente Tailwind (apenas gridTemplateColumns permitidos)
5. npm run build sem erros após a migração
</success_criteria>

<output>
Criar `.planning/phases/01-dashboard-completions/01-02-SUMMARY.md` quando concluído
</output>
