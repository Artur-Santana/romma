---
phase: 04-polimento-visual-publico
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/UnidadeCard.js
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "UnidadeCard.js renderiza modo leitura com nome, referência (UN-XXXXXX), status badge, valor formatado e botões Editar/Remover"
    - "UnidadeCard.js renderiza modo edição inline com shadcn Input/Select/Button (nome, descrição, área, valor, status)"
    - "Botão 'Editar' abre modo edição; 'Salvar' confirma; 'Cancelar' descarta sem salvar"
    - "Zero inline styles de layout (exceto button reset canônico se necessário)"
    - "Arquivo não referencia JetBrains Mono ou quaisquer fontes literais"
  artifacts:
    - path: "src/components/ui/UnidadeCard.js"
      provides: "Card de unidade do dashboard com modo leitura e modo edição inline via shadcn"
      exports: ["default UnidadeCard"]
  key_links:
    - from: "src/components/features/Unidades.js"
      to: "src/components/ui/UnidadeCard.js"
      via: "import + props passadas do parent"
      pattern: "UnidadeCard"
    - from: "src/components/ui/UnidadeCard.js"
      to: "src/components/ui/button, input, select"
      via: "imports shadcn"
      pattern: "from '@/components/ui/button'"
---

<objective>
Reescrever `src/components/ui/UnidadeCard.js` com design system completo Tailwind v4 + shadcn. O plano 01-06 explicitamente adiou esta migração ("não migrar agora") — esta é a implementação definitiva.

**MVP Deviation:** Este plano cobre o leftover D-07/D-08 do dashboard, não o slice de visitante (ver 04-01 para o slice).

Purpose: UnidadeCard do dashboard estava como skeleton intencionalmente adiado. Entregar CRUD visual completo: modo leitura formatado + modo edição inline com Input/Select/Button shadcn.
Output: UnidadeCard.js reescrito; modo leitura e edição funcionais; props alinhadas com Unidades.js existente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-polimento-visual-p-blico/04-CONTEXT.md
@.planning/phases/04-polimento-visual-p-blico/04-RESEARCH.md
@.planning/phases/04-polimento-visual-p-blico/04-PATTERNS.md
@.planning/phases/04-polimento-visual-p-blico/04-UI-SPEC.md

<interfaces>
<!-- Contratos extraídos de PATTERNS.md e da codebase. -->

De src/components/features/Unidades.js (call site — ler antes de implementar):
  Passa props para UnidadeCard incluindo: unidade (objeto), editandoId, formEdit, onEditar, onSalvar, onDeletar, handlers de onChange
  O executor deve ler Unidades.js para ver exatamente quais props são passadas atualmente
  D-08 (Claude's Discretion): interface pode ser simplificada para objeto único { unidade, editandoId, formEdit, onEditar, onSalvar, onDeletar, onFormChange }
  OU manter props individuais compatíveis com Unidades.js sem alterar o parent — executor decide qual é mais limpa

shadcn components disponíveis (já instalados):
  import { Button } from '@/components/ui/button'
  import { Input } from '@/components/ui/input'
  import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'

De src/components/ui/StatusBadge.js:
  export default StatusBadge({ status }) — status: 'disponivel' | 'alugada'

De src/lib/utils.js:
  fmtBRL(value) → string formatada

Tipografia (UI-SPEC Phase 4):
  Eyebrow/label: font-mono text-[11px] tracking-[1px] uppercase
  Título card: font-body font-bold text-[22px] tracking-[-0.8px] text-fg-1
  Label de campo (maior): font-mono text-[10px] text-fg-4 tracking-[1px] uppercase

Modo leitura (padrão LocatariosDesktop.js linhas 97-150):
  Container: border-t border-border-3 px-5 py-4
  Ref eyebrow: font-mono text-[11px] text-fg-5 tracking-[1px] uppercase
  Nome unidade: font-display font-bold text-[18px] tracking-[-0.6px] text-fg-1
  Botões: Button variant="ghost" size="sm" com classes font-mono text-[10px] uppercase tracking-[0.5px] p-0 h-auto

Modo edição — campo padrão (Unidades.js linhas 146-155):
  <label className="flex flex-col gap-1.5">
    <span className="font-mono text-[10px] tracking-[1px] uppercase text-fg-4">LABEL</span>
    <Input type="text" value={...} onChange={...} className="h-9 rounded-none border-border-3 bg-surface text-fg-1" />
  </label>

Copywriting (UI-SPEC):
  Botão edição: "Editar"
  Botão delete: "Remover"
  Botão save: "Salvar"
  Botão cancel: "Cancelar"
  Erro: "Erro ao salvar unidade."
  Status field Select: opções 'disponivel' e 'alugada'

Design Constraints:
  Sharp corners: --radius: 0 → rounded-none em shadcn inputs/selects
  Sem confirmation dialog para "Remover" (UI-SPEC — fire handler diretamente, fora de escopo)
  Erro: text-danger-fg text-[11px] font-mono abaixo do form
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever UnidadeCard.js com modo leitura e modo edição inline (Tailwind v4 + shadcn)</name>
  <files>
    src/components/ui/UnidadeCard.js
  </files>
  <read_first>
    - src/components/ui/UnidadeCard.js — ler COMPLETO; ver as ~14 props individuais atuais e estrutura existente
    - src/components/features/Unidades.js — ler linhas 1-100 para ver como UnidadeCard é chamado e quais props são passadas (call site define a interface de props)
    - src/components/features/LocatariosDesktop.js — linhas 97-150 e 168-262 para padrão de linha de tabela e modal de edição
    - .planning/phases/04-polimento-visual-p-blico/04-PATTERNS.md — seção `src/components/ui/UnidadeCard.js` com os padrões exatos
    - .planning/phases/04-polimento-visual-p-blico/04-UI-SPEC.md — seção Component Inventory e Interaction Patterns
  </read_first>
  <action>
Ler Unidades.js primeiro para entender o call site e definir a interface de props. Então implementar a reescrita completa.

**Decisão de props (D-08):** Após ler Unidades.js, decidir entre:
- Opção A (objeto único): `{ unidade, editandoId, formEdit, onEditar, onSalvar, onDeletar, onFormChange }` — mais limpa, quebra compatibilidade com o call site atual e exige atualizar Unidades.js
- Opção B (props individuais compatíveis): manter a mesma assinatura que Unidades.js já passa — zero mudança no parent

Escolher a opção que minimiza mudanças no parent (Unidades.js). Se o parent já passa um objeto `formEdit`, preferir Opção A. Se o parent passa props individuais numerosas, preferir Opção B. Documentar a escolha no código com um comentário de uma linha.

**Modo leitura:**
- Container `border-t border-border-3 px-5 py-4`
- Eyebrow ref: `UN-${unidade.id.slice(0,6).toUpperCase()}` em `font-mono text-[11px] text-fg-5 tracking-[1px] uppercase`
- Nome: `font-body font-bold text-[22px] tracking-[-0.8px] text-fg-1` (ou `font-display` — usar o mesmo token que o resto do design system)
- Dados secundários: edifício, área, valor (`fmtBRL()`), status (`StatusBadge`)
- Botões na borda direita: `Button variant="ghost" size="sm"` com "Editar" (`text-fg-3`) e "Remover" (`text-danger-fg`)
- Remover fire diretamente `onDeletar(unidade.id)` sem confirmation dialog

**Modo edição (quando `editandoId === unidade.id`):**
- Campos com shadcn Input: nome, descricao, area_m2 (number), valor_mensal (number)
- Campo status: shadcn Select com opções "disponivel" e "alugada"
- Todos os inputs: `className="h-9 rounded-none border-border-3 bg-surface text-fg-1"`
- Labels: `font-mono text-[10px] tracking-[1px] uppercase text-fg-4`
- Botões: `Button variant="default"` para "Salvar" (bg-indigo), `Button variant="ghost"` para "Cancelar"
- Erro: exibir `erroMessage` abaixo do form em `text-danger-fg text-[11px] font-mono`
- Nota: o estado de erro (se houver) pode ser gerenciado no parent (Unidades.js) ou localmente — decidir conforme o padrão do call site

**Proibições:**
- Zero inline styles de layout (exceto `style={{ all: 'unset' }}` se houver botões que precisem de reset)
- `--radius: 0` já definido globalmente; usar `rounded-none` apenas se shadcn sobrescrever (verificar)
  </action>
  <verify>
    <automated>
      grep -v '^#' src/components/ui/UnidadeCard.js | grep -c 'style={{'
      # inline styles — se resultado > 0, verificar que cada um é o button reset canônico
      grep -c "from '@/components/ui/button'" src/components/ui/UnidadeCard.js
      # esperado: 1 (import shadcn Button presente)
      grep -c "from '@/components/ui/input'" src/components/ui/UnidadeCard.js
      # esperado: 1
      npm run lint 2>&amp;1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/UnidadeCard.js` exporta `default UnidadeCard`
    - Arquivo importa `Button` de `@/components/ui/button`, `Input` de `@/components/ui/input`, `Select` + variantes de `@/components/ui/select`
    - Arquivo importa `StatusBadge` de `@/components/ui/StatusBadge` e `fmtBRL` de `@/lib/utils`
    - Modo leitura: contém `UN-` + `toUpperCase()` (eyebrow de referência)
    - Modo leitura: contém texto "Editar" e "Remover" como botões de ação
    - Modo edição: contém `<Input` e `<Select` (shadcn)
    - Modo edição: contém texto "Salvar" e "Cancelar"
    - `grep -rn 'style={{' src/components/ui/UnidadeCard.js | grep -v "all: 'unset'" | wc -l` → 0
    - `npm run lint` não reporta erros para UnidadeCard.js
    - Decisão de interface de props (D-08) documentada com comentário no código
  </acceptance_criteria>
  <done>UnidadeCard.js reescrito com design system completo: modo leitura com dados formatados + modo edição inline shadcn; zero inline styles de layout; Tailwind v4 tokens aplicados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Dashboard (autenticado) → UnidadeCard | Apenas Proprietário autenticado acessa /dashboard/unidades |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Modo edição inline — submit de formEdit | accept | Estado de edição controlado por parent (Unidades.js); Server Action `editarUnidade` faz validação UUID + whitelist de campos no servidor; cliente não pode injetar campos extras que o server não ignora |
| T-04-03-02 | Spoofing | "Remover" sem confirmation | accept | Ação de deleção requer sessão autenticada de Proprietário; UI sem confirm dialog é aceitável para dashboard interno (fora de escopo adicionar AlertDialog nesta fase) |
</threat_model>

<verification>
Verificação do plano 04-03:

```bash
# Zero inline styles de layout
grep -rn 'style={{' src/components/ui/UnidadeCard.js | grep -v "all: 'unset'" | wc -l
# esperado: 0

# shadcn imports presentes
grep -c "from '@/components/ui/button'" src/components/ui/UnidadeCard.js
# esperado: 1

# Lint
npm run lint
```
</verification>

<success_criteria>
1. UnidadeCard.js reescrito em Tailwind v4 com design tokens Obsidian Blueprint
2. Modo leitura: ref UN-XXXXXX, nome, status badge, valor fmtBRL, botões Editar/Remover
3. Modo edição: shadcn Input (nome, descrição, área, valor) + Select (status) + botões Salvar/Cancelar
4. Zero inline styles de layout
5. npm run lint passa sem novos erros para o arquivo
6. Decisão D-08 (props interface) documentada com comentário no código
</success_criteria>

<output>
Criar `.planning/phases/04-polimento-visual-p-blico/04-03-SUMMARY.md` quando concluído.
</output>
