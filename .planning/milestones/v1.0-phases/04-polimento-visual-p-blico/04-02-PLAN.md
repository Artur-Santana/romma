---
phase: 04-polimento-visual-publico
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.js
  - src/app/globals.css
  - src/app/page.js
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "Nenhuma referência a JetBrains_Mono ou Public_Sans existe no codebase (src/)"
    - "globals.css usa var(--font-space-grotesk) nos dois pontos de --font-mono (linha @theme e linha :root)"
    - "Todos os 8 <img> nativos em src/app/page.js substituídos por <Image> do next/image"
    - "npm run lint e npm run build passam sem novos erros ou warnings"
  artifacts:
    - path: "src/app/layout.js"
      provides: "Carregamento de fontes sem JetBrains_Mono e sem Public_Sans (dead import)"
      contains: "Space_Grotesk, Hanken_Grotesk"
    - path: "src/app/globals.css"
      provides: "CSS vars --font-mono apontando para var(--font-space-grotesk) nos dois contextos (@theme e :root)"
      contains: "var(--font-space-grotesk), sans-serif"
    - path: "src/app/page.js"
      provides: "Landing page sem tags <img> nativas"
      contains: "from 'next/image'"
  key_links:
    - from: "src/app/layout.js"
      to: "src/app/globals.css"
      via: "CSS variable --font-jetbrains-mono removida; --font-mono redireciona para Space Grotesk"
      pattern: "font-space-grotesk"
---

<objective>
Dois cleanups cross-cutting da Fase 4: (1) remoção de JetBrains Mono e Public_Sans de layout.js + atualização de globals.css; (2) migração dos 8 `<img>` nativos em src/app/page.js para next/image.

**MVP Deviation:** Este plano cobre itens de limpeza D-01/D-02, não o slice de visitante (ver 04-01 para o slice). Os grep gates do Decision 2 ficam neste plano para a verificação de JetBrains.

Purpose: Limpar dívida técnica de fontes e warnings `@next/next/no-img-element` deferidos da Fase 3 (documentado em ROADMAP Fase 3 exceção conhecida). Necessário para build limpo (DEPL-03).
Output: layout.js sem dead imports, globals.css atualizado, page.js sem img nativas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-polimento-visual-p-blico/04-CONTEXT.md
@.planning/phases/04-polimento-visual-p-blico/04-RESEARCH.md
@.planning/phases/04-polimento-visual-p-blico/04-PATTERNS.md
@.planning/phases/04-polimento-visual-p-blico/04-UI-SPEC.md

<interfaces>
<!-- Padrões exatos extraídos do PATTERNS.md. -->

De src/app/layout.js (estado atual — ler antes de editar):
  Linha 1: import { Public_Sans, Space_Grotesk, Hanken_Grotesk, JetBrains_Mono } from "next/font/google";
  Linhas 15-18: bloco jetbrainsMono = JetBrains_Mono({ variable: "--font-jetbrains-mono", subsets: ["latin"] })
  Linha 34: className com ${jetbrainsMono.variable}
  → APÓS: manter apenas Space_Grotesk e Hanken_Grotesk no import; remover bloco jetbrainsMono; remover do className

De src/app/globals.css (estado atual — ler antes de editar):
  Linha 20 (@theme): --font-mono: var(--font-jetbrains-mono), monospace;
  Linha 129 (:root): --font-mono:     var(--font-jetbrains-mono), monospace;
  → APÓS: ambas → --font-mono: var(--font-space-grotesk), sans-serif;

De src/app/page.js — 8 ocorrências a migrar (per PATTERNS.md):
  Linha ~13: Detalhe_Arquitetonico.png (raster bg) → fill em container relative
  Linha ~52: Detalhe_Arquitetonico.png (raster) → fill em container relative
  Linha ~66: horizontal_divider.svg (SVG decorativo) → width/height + unoptimized
  Linha ~81: icon_qr_01.svg (SVG ícone, classe w-7) → width={28} height={28} unoptimized
  Linha ~96: icon_doc_02.svg (SVG ícone, classe w-5) → width={20} height={20} unoptimized
  Linha ~112: icon_conect_03.svg (SVG ícone) → width={28} height={28} unoptimized
  Linha ~128: icon_graph_04.svg (SVG ícone) → width={28} height={28} unoptimized
  Linha ~166: data_regional_demand_graph.png (raster gráfico) → fill em container relative

Padrão next/image para SVGs (Decision: usar unoptimized — Option A do PATTERNS.md):
  import Image from 'next/image'
  <Image src="/icon_qr_01.svg" alt="" width={28} height={28} unoptimized />

Padrão next/image para rasters com fill:
  container pai deve ter: position: relative (classe `relative`) + height definida
  <Image src="/Detalhe_Arquitetonico.png" alt="" fill className="object-cover opacity-20" sizes="100vw" priority />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover JetBrains Mono + Public_Sans de layout.js e atualizar globals.css</name>
  <files>
    src/app/layout.js
    src/app/globals.css
  </files>
  <read_first>
    - src/app/layout.js — ler COMPLETO para ver estrutura exata dos imports, variáveis de fonte e className do html
    - src/app/globals.css — ler linhas 1-135 para localizar linha 20 (@theme block) e linha 129 (:root block) com --font-mono
  </read_first>
  <action>
Executar grep gate antes de qualquer edição:
`grep -rn 'JetBrains\|jetbrains' src/ --include='*.js' --include='*.css' | wc -l`
Documentar resultado (esperado: >0 antes da remoção).

**src/app/layout.js — 3 mudanças cirúrgicas:**
1. Linha do import: remover `Public_Sans` e `JetBrains_Mono` do import de `next/font/google`. Manter `Space_Grotesk` e `Hanken_Grotesk`.
2. Remover o bloco completo de declaração `const jetbrainsMono = JetBrains_Mono({...})`.
3. Linha do `<html className=...>`: remover `${jetbrainsMono.variable}` do className. Manter `${HankenGrotesks.variable}` e `${spaceGrotesk.variable}`.

Não alterar mais nada em layout.js — verificar que a estrutura do componente, metadata, e outros imports permanecem intactos.

**src/app/globals.css — 2 substituições cirúrgicas:**
1. Localizar linha com `--font-mono: var(--font-jetbrains-mono), monospace;` no bloco `@theme` (próximo da linha 20). Substituir por: `--font-mono: var(--font-space-grotesk), sans-serif;`
2. Localizar linha com `--font-mono:     var(--font-jetbrains-mono), monospace;` no bloco `:root` (próximo da linha 129). Substituir por: `--font-mono:     var(--font-space-grotesk), sans-serif;` (preservar o espaçamento existente para manter formatação do arquivo).

Nenhuma outra linha de globals.css deve ser alterada.

Executar grep gate de verificação após edições:
`grep -rn 'JetBrains\|jetbrains' src/ --include='*.js' --include='*.css' | wc -l`
Esperado: 0.
  </action>
  <verify>
    <automated>
      grep -rn 'JetBrains\|jetbrains' src/ --include='*.js' --include='*.css' | wc -l
      # esperado: 0
      grep -rn 'Public_Sans\|public_sans' src/ --include='*.js' | wc -l
      # esperado: 0
      grep -c 'font-space-grotesk' src/app/globals.css
      # esperado: >= 2 (as duas substituições)
      npm run lint 2>&amp;1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'JetBrains\|jetbrains' src/ --include='*.js' --include='*.css'` → zero matches
    - `grep -rn 'Public_Sans' src/ --include='*.js'` → zero matches
    - `src/app/globals.css` contém `--font-mono: var(--font-space-grotesk), sans-serif` (sem `monospace` ou `jetbrains`)
    - `src/app/layout.js` contém `import { Space_Grotesk, Hanken_Grotesk } from "next/font/google"` (sem Public_Sans, sem JetBrains_Mono)
    - `src/app/layout.js` html className NÃO contém `jetbrainsMono.variable`
    - `npm run lint` não reporta erros relacionados a JetBrains ou undefined variable
  </acceptance_criteria>
  <done>layout.js e globals.css sem nenhuma referência a JetBrains Mono ou Public_Sans; --font-mono aponta para Space Grotesk.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar 8 tags &lt;img&gt; nativas em src/app/page.js para next/image</name>
  <files>
    src/app/page.js
  </files>
  <read_first>
    - src/app/page.js — ler COMPLETO; identificar as 8 tags `<img>` e o contexto (container pai, classes existentes, dimensões)
    - src/app/login/page.js — padrão canônico de next/image fill no projeto (ver LeftPanel)
    - .planning/phases/04-polimento-visual-p-blico/04-PATTERNS.md — seção `src/app/page.js` com tabela das 8 ocorrências e dimensões sugeridas
  </read_first>
  <action>
Executar grep gate antes de editar:
`grep -v '^#' src/app/page.js | grep -c '<img'`
Documentar resultado (esperado: 8).

Adicionar `import Image from 'next/image'` no topo de src/app/page.js (se não existir).

Migrar as 8 ocorrências conforme a tabela do PATTERNS.md (ler a seção `src/app/page.js` no arquivo de padrões antes de implementar para ter as dimensões exatas):

**Rasters (Detalhe_Arquitetonico.png e data_regional_demand_graph.png):**
- Verificar o container pai: precisa ter `position: relative` (classe `relative`) e `height` definida.
- Se o container já tem classes de altura, adicionar apenas `relative` se necessário.
- Substituir `<img src="..." className="...">` por `<Image src="..." alt="" fill className="object-cover ..." sizes="100vw" />`.
- Para Detalhe_Arquitetonico.png que é above the fold: adicionar `priority`.

**SVGs (icon_qr_01.svg, icon_doc_02.svg, icon_conect_03.svg, icon_graph_04.svg, horizontal_divider.svg):**
- Usar `<Image src="..." alt="" width={W} height={H} unoptimized />` onde W/H são as dimensões correspondentes às classes `w-7`/`w-5` (28px e 20px respectivamente).
- Para `horizontal_divider.svg`: verificar dimensões reais no container existente; usar width e height compatíveis com o layout atual.
- Preservar todas as classes Tailwind existentes no className da imagem.
- `unoptimized` é necessário em SVGs para evitar comportamento inesperado de sanitização do next/image.

Manter toda a estrutura do arquivo intacta — zero mudanças de layout, classes, ou lógica além das tags `<img>`.

Executar grep gate de verificação:
`grep -v '^#' src/app/page.js | grep -c '<img'`
Esperado: 0.
  </action>
  <verify>
    <automated>
      grep -v '^#' src/app/page.js | grep -c '&lt;img'
      # esperado: 0
      grep -c "from 'next/image'" src/app/page.js
      # esperado: 1
      npm run lint 2>&amp;1 | grep -i 'no-img-element' | wc -l
      # esperado: 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/app/page.js | grep -c '<img'` → 0
    - `grep -c "from 'next/image'" src/app/page.js` → 1 (import existe)
    - `npm run lint` não reporta nenhum warning `@next/next/no-img-element` para page.js
    - Todos os containers de imagens raster têm classe `relative` no pai imediato do `<Image fill>` (verificar manualmente no arquivo)
    - SVGs têm atributo `unoptimized` no `<Image>`
  </acceptance_criteria>
  <done>src/app/page.js sem tags &lt;img&gt; nativas; 8 ocorrências migradas para next/image; lint sem warnings no-img-element.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Servidor → cliente (layout.js) | Fonts carregadas via next/font — self-hosted pelo Next.js |
| Servidor → cliente (page.js) | Landing page estática; sem dados de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | SVGs com `unoptimized` em page.js | accept | SVGs são self-hosted sob /public/; sem upload de usuário; `unoptimized` apenas desabilita transformação de formato — não cria superfície nova de ataque |
| T-04-02-02 | Information Disclosure | next/font carregando Space_Grotesk | accept | Font carregada do Google Fonts via self-hosting automático do next/font; sem dados de usuário transmitidos para Google em produção |
</threat_model>

<verification>
Verificação completa do plano 04-02:

```bash
# Nenhuma referência a JetBrains no codebase
grep -rn 'JetBrains\|jetbrains' src/ --include='*.js' --include='*.css' | wc -l
# esperado: 0

# Nenhuma img nativa em page.js
grep -v '^#' src/app/page.js | grep -c '<img'
# esperado: 0

# globals.css atualizado nos dois pontos
grep -n 'font-jetbrains-mono' src/app/globals.css | wc -l
# esperado: 0

# Build limpo
npm run lint && npm run build
```
</verification>

<success_criteria>
1. Zero referências a JetBrains_Mono ou Public_Sans em todo src/
2. globals.css: --font-mono aponta para var(--font-space-grotesk) nas linhas @theme e :root
3. src/app/page.js: zero tags <img> nativas; import Image from 'next/image' presente
4. npm run lint: zero warnings @next/next/no-img-element para page.js e layout.js
5. npm run build: passa sem novos erros
</success_criteria>

<output>
Criar `.planning/phases/04-polimento-visual-p-blico/04-02-SUMMARY.md` quando concluído.
</output>
