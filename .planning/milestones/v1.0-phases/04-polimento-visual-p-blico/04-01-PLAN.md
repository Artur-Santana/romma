---
phase: 04-polimento-visual-publico
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/unidades/page.js
  - src/components/features/UnidadesPublicas.js
  - src/components/features/UnidadePublicaCard.js
  - src/components/features/UnidadeDetailSheet.js
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "Visitante acessa /unidades e vê lista de unidades disponíveis com nome, referência (UN-XXXXXX), edifício, área e valor mensal"
    - "Visitante clica em um card e vê o UnidadeDetailSheet com imagem placeholder (next/image), área, valor e botão 'Tenho interesse →'"
    - "Visitante vê dot de Realtime indicando atualização ao vivo"
    - "Página /unidades não contém nenhuma tag <img> nativa"
    - "Nenhum inline style de layout nos 3 novos feature components (exceto `style={{ all: 'unset' }}` canônico)"
  artifacts:
    - path: "src/app/unidades/page.js"
      provides: "Thin shell Server Component que importa UnidadesPublicas"
      contains: "import UnidadesPublicas from '@/components/features/UnidadesPublicas'"
    - path: "src/components/features/UnidadesPublicas.js"
      provides: "Client Component principal com data fetching, Realtime e composição dos subcomponentes"
      exports: ["default UnidadesPublicas"]
    - path: "src/components/features/UnidadePublicaCard.js"
      provides: "Card individual de unidade para listagem pública"
      exports: ["default UnidadePublicaCard"]
    - path: "src/components/features/UnidadeDetailSheet.js"
      provides: "Bottom sheet de detalhe de unidade — overlay fixed viewport"
      exports: ["default UnidadeDetailSheet"]
  key_links:
    - from: "src/app/unidades/page.js"
      to: "src/components/features/UnidadesPublicas.js"
      via: "import + render"
      pattern: "UnidadesPublicas"
    - from: "src/components/features/UnidadesPublicas.js"
      to: "src/lib/queries-client.js"
      via: "useEffect → getUnidades() + getEdificios()"
      pattern: "getUnidades|getEdificios"
    - from: "src/components/features/UnidadeDetailSheet.js"
      to: "next/image"
      via: "Image component com fill em container relative"
      pattern: "from 'next/image'"
---

## Phase Goal

**As a** visitante interessado em alugar uma unidade, **I want to** ver as unidades disponíveis numa página pública polida e rápida, **so that** eu reconheça o profissionalismo da Romma e clique em "Tenho interesse" com confiança.

<objective>
Slice vertical completo da página /unidades pública: reescreve `src/app/unidades/page.js` em thin shell + extrai 3 subcomponentes (`UnidadesPublicas`, `UnidadePublicaCard`, `UnidadeDetailSheet`) em Tailwind v4 com paleta Obsidian Blueprint e next/image para o placeholder.

**MVP Deviation (Decision 2):** O "primeiro task" usa grep gates como substituto do teste E2E com falha. Playwright está fora de escopo desta fase (Phase 5).

Purpose: Entrega o slice visitante da Fase 4 — a página pública que demonstra profissionalismo na banca.
Output: 3 novos feature components + page.js reescrito; zero tags `<img>` nativas em /unidades; design Obsidian Blueprint aplicado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-polimento-visual-p-blico/04-CONTEXT.md
@.planning/phases/04-polimento-visual-p-blico/04-RESEARCH.md
@.planning/phases/04-polimento-visual-p-blico/04-PATTERNS.md
@.planning/phases/04-polimento-visual-p-blico/04-UI-SPEC.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos da codebase via leitura do PATTERNS.md. -->

De src/lib/queries-client.js (existentes — não alterar):
  getUnidades() → Promise<Array<{id, nome, descricao, area_m2, valor_mensal, valor_visivel, status, edificio_id}>>
  getEdificios() → Promise<Array<{id, nome, endereco}>>

De src/components/ui/RealtimeDot.js:
  export default RealtimeDot — sem props obrigatórias

De src/components/ui/StatusBadge.js:
  export default StatusBadge({ status }) — status: 'disponivel' | 'alugada'

De src/lib/utils.js:
  fmtBRL(value) → string  (ex: "R$ 3.500,00")

De src/lib/supabase-browser.js:
  createClient() → SupabaseClient para Realtime

Padrão de Realtime (channel 'public-unidades'):
  .channel('public-unidades')
  .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'unidades' }, () => load())
  .on('postgres_changes', { event: 'DELETE', schema: 'public', table: 'unidades' }, () => load())
  .subscribe()
  return () => { supabase.removeChannel(channel) }

next/image com fill — asset existente (Claude's Discretion: reutilizar /Detalhe_Arquitetonico.png):
  import Image from 'next/image'
  Container: className="relative h-40 overflow-hidden border border-border-3"
  <Image src="/Detalhe_Arquitetonico.png" alt="" fill className="object-cover opacity-10" sizes="100vw" />

Tokens Tailwind v4 disponíveis em globals.css:
  bg-background, bg-surface, bg-surface-hi
  text-fg-1, text-fg-2, text-fg-3, text-fg-4, text-fg-5
  border-border-3, text-indigo, bg-indigo
  font-mono, font-body, font-display

Tipografia (UI-SPEC Phase 4):
  Eyebrow/label: font-mono text-[11px] tracking-[1px] uppercase
  Corpo: font-body text-[13px]
  Título card: font-body font-bold text-[22px] tracking-[-0.8px]
  Display: font-body font-bold text-[32px] tracking-[-1.6px]

Button reset canônico (exceção D-04 — CLAUDE.md):
  style={{ all: 'unset', cursor: 'pointer', display: 'block', width: '100%', boxSizing: 'border-box' }}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep gates (RED) + scaffolding dos 3 subcomponentes como exports vazios</name>
  <files>
    src/components/features/UnidadesPublicas.js
    src/components/features/UnidadePublicaCard.js
    src/components/features/UnidadeDetailSheet.js
  </files>
  <read_first>
    - src/app/unidades/page.js — estado atual completo; confirmar que existem tags `<img>` nativas (não deveria haver, mas confirmar o SVG inline que conta como placeholder)
    - src/components/features/portal/ContratoCard.js — referência canônica de card Tailwind v4
    - src/app/login/page.js — referência canônica de next/image + Tailwind v4
  </read_first>
  <action>
Registrar o estado inicial (grep gate = RED quando o slice ainda não existe) e criar os 3 arquivos de subcomponentes como exports vazios para garantir que as importações do próximo task não quebrem.

1. Executar grep gate de inline-styles na pasta: `grep -v '^#' src/app/unidades/page.js | grep -c 'style={{'` — documentar o resultado (esperado: >0 no arquivo atual; resultado confirma que o arquivo precisa ser reescrito).

2. Criar `src/components/features/UnidadesPublicas.js` com export default de função vazia + `'use client'`.

3. Criar `src/components/features/UnidadePublicaCard.js` com export default de função vazia + props `{ unidade, edificio, onSelect, isRemoving }`.

4. Criar `src/components/features/UnidadeDetailSheet.js` com export default de função vazia + props `{ unidade, edificio, onClose, onSimular }`.

Estes exports vazios permitem que o task seguinte importe os componentes sem erro de resolução de módulo durante o desenvolvimento incremental.
  </action>
  <verify>
    <automated>
      grep -l "export default" src/components/features/UnidadesPublicas.js src/components/features/UnidadePublicaCard.js src/components/features/UnidadeDetailSheet.js | wc -l
      # esperado: 3
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/components/features/UnidadesPublicas.js` existe com `'use client'` e `export default function UnidadesPublicas`
    - `src/components/features/UnidadePublicaCard.js` existe com `export default function UnidadePublicaCard`
    - `src/components/features/UnidadeDetailSheet.js` existe com `export default function UnidadeDetailSheet`
    - Grep gate registrado no terminal: `grep -v '^#' src/app/unidades/page.js | grep -c 'style={{'` — resultado documentado
  </acceptance_criteria>
  <done>3 arquivos existem com exports vazios; grep gate executado e resultado registrado.</done>
</task>

<task type="auto">
  <name>Task 2: Slice vertical completo — UnidadesPublicas + UnidadePublicaCard + UnidadeDetailSheet + page.js thin shell</name>
  <files>
    src/app/unidades/page.js
    src/components/features/UnidadesPublicas.js
    src/components/features/UnidadePublicaCard.js
    src/components/features/UnidadeDetailSheet.js
  </files>
  <read_first>
    - src/app/unidades/page.js — ler COMPLETO; é o arquivo fonte das lógicas a extrair (UnidadesPublicas linhas 203-504, UnitDetailSheet linhas 21-201, cards linhas 396-483)
    - src/components/features/portal/ContratoCard.js — padrão de card em Tailwind v4 (referência visual)
    - src/components/features/LocatariosDesktop.js — padrão de tipografia + botões Tailwind v4
    - src/components/features/Unidades.js — padrão de importações + Realtime + shadcn (linhas 1-12)
    - src/app/login/page.js — padrão de next/image com fill
    - .planning/phases/04-polimento-visual-p-blico/04-PATTERNS.md — padrões exatos para cada arquivo
    - .planning/phases/04-polimento-visual-p-blico/04-UI-SPEC.md — copywriting e tokens
  </read_first>
  <action>
Implementar o slice vertical completo em 4 arquivos. Todos os padrões exatos estão em PATTERNS.md — consultar antes de implementar cada arquivo.

**src/app/unidades/page.js — thin shell:**
Reescrever como Server Component puro importando `UnidadesPublicas` de `@/components/features/UnidadesPublicas`. Sem `'use client'`, sem props, sem estado. Padrão exato: ver PATTERNS.md seção `src/app/unidades/page.js`.

**src/components/features/UnidadesPublicas.js — Client Component principal:**
Implementar com `'use client'`. Importar: `{ useState, useEffect }`, `getUnidades` e `getEdificios` de `@/lib/queries-client`, `createClient` de `@/lib/supabase-browser`, `RealtimeDot` de `@/components/ui/RealtimeDot`, `UnidadePublicaCard`, `UnidadeDetailSheet`. Estado: `unidades`, `edificios`, `activeTab`, `selected`, `removedIds`, `removingId` — padrão exato em PATTERNS.md. Incluir Realtime subscription no useEffect com cleanup `removeChannel`. Renderizar: header com título "Unidades Disponíveis" + `<RealtimeDot />`, lista de `UnidadePublicaCard`, estado vazio ("Nenhuma unidade disponível"), `UnidadeDetailSheet` quando `selected !== null`. Paleta via tokens Tailwind v4 — zero inline styles de layout.

**src/components/features/UnidadePublicaCard.js:**
Props: `{ unidade, edificio, onSelect, isRemoving }`. Padrão de button reset canônico (`style={{ all: 'unset', ... }}`). Ref eyebrow: `UN-${unidade.id.slice(0,6).toUpperCase()}` — `font-mono text-[11px] tracking-[1px] uppercase`. Título: `font-body font-bold text-[22px] tracking-[-0.8px] text-fg-1`. Valor: mostrar `fmtBRL(unidade.valor_mensal) + '/mês'` quando `valor_visivel = true`, senão `"Valor sob consulta"`. Badge de status com `<StatusBadge status="disponivel" />`. CTA: `"Detalhes →"` em `text-indigo`. Gold (`text-highlight`) pode ser aplicado ao eyebrow de referência (UN-XXXXXX) — máximo um elemento por card, conforme UI-SPEC.

**src/components/features/UnidadeDetailSheet.js:**
Props: `{ unidade, edificio, onClose, onSimular }`. Overlay: `className="fixed inset-0 z-50 bg-[oklch(0_0_0/0.65)] flex items-end"` com `onClick={onClose}`. Sheet: `className="w-full max-h-[85dvh] overflow-auto bg-background border-t border-indigo px-5 pt-6 pb-8 flex flex-col gap-4"` com `onClick={e => e.stopPropagation()}`. Drag handle: `className="self-center w-8 h-[3px] bg-fg-5"`. Placeholder de imagem (D-03): container `className="relative h-40 overflow-hidden border border-border-3"` + `<Image src="/Detalhe_Arquitetonico.png" alt="" fill className="object-cover opacity-10" sizes="100vw" />` — import Image from 'next/image'. Copiar exato: botão primário "Tenho interesse →" (`bg-indigo`, `py-[14px]`, texto centrado), botão secundário "Fechar" (border variant), disclaimer demo verbatim: `"Demo · 'Tenho interesse' simula aluguel para fins de visualização"`. Padrão exato dos botões de ação em PATTERNS.md seção UnidadeDetailSheet.

**Proibições (D-04):**
- Zero inline styles de layout nos 3 novos components (exceto o `style={{ all: 'unset' }}` canônico de button reset)
- Usar apenas tokens Tailwind v4 (`bg-background`, `text-fg-1`, etc.) — sem hex literals em className (exceto `bg-[oklch(...)]` para o overlay que não tem token)
  </action>
  <verify>
    <automated>
      grep -v '^#' src/app/unidades/page.js | grep -c '&lt;img' || echo "0"
      # deve retornar 0 — sem tags img nativas na thin shell
      grep -rn 'style={{' src/components/features/UnidadesPublicas.js src/components/features/UnidadePublicaCard.js src/components/features/UnidadeDetailSheet.js | grep -v "all: 'unset'" | wc -l
      # deve retornar 0 — sem inline styles de layout (exceto button reset)
      npm run lint 2>&amp;1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/app/unidades/page.js` contém exatamente `import UnidadesPublicas from '@/components/features/UnidadesPublicas'` e nada mais de substancial (thin shell puro)
    - `src/components/features/UnidadesPublicas.js` contém `'use client'`, `getUnidades`, `getEdificios`, `createClient`, `removeChannel`, `UnidadePublicaCard`, `UnidadeDetailSheet`
    - `src/components/features/UnidadeDetailSheet.js` contém `from 'next/image'` e `"/Detalhe_Arquitetonico.png"`
    - `src/components/features/UnidadeDetailSheet.js` contém `"Tenho interesse →"` e `"Demo ·"` (disclaimer verbatim)
    - `grep -v '^#' src/app/unidades/page.js src/components/features/UnidadesPublicas.js src/components/features/UnidadePublicaCard.js src/components/features/UnidadeDetailSheet.js | grep -c '<img'` → 0
    - `grep -rn 'style={{' src/components/features/UnidadesPublicas.js src/components/features/UnidadePublicaCard.js src/components/features/UnidadeDetailSheet.js | grep -v "all: 'unset'" | wc -l` → 0
    - `npm run lint` passa sem novos erros relacionados a esses 4 arquivos
  </acceptance_criteria>
  <done>Slice vertical /unidades completo: thin shell + 3 subcomponentes em Tailwind v4 com Obsidian Blueprint, next/image para placeholder, Realtime preservado, zero img nativas, zero inline styles de layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| visitante → /unidades | Dados de unidades são somente leitura; visitante não autenticado |
| UnidadeDetailSheet → callback onSimular | Callback passa apenas unidade.id — não envia formulário para servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | getUnidades() query | accept | Query já é pública por design (RLS permite SELECT sem auth); dados exibidos são deliberadamente públicos |
| T-04-02 | Tampering | Realtime subscription 'public-unidades' | accept | Somente INSERT/DELETE ouvidos — sem escrita via Realtime; subscription é read-only |
| T-04-03 | Information Disclosure | next/image com SVG unoptimized | accept | SVGs são self-hosted sob /public/; zero upload de usuário; sem risco de XSS via SVG externo |
</threat_model>

<verification>
Verificação da fase completa (executar após ambos os tasks):

```bash
# Zero img nativas em /unidades
grep -rn '<img' src/app/unidades/ src/components/features/UnidadesPublicas.js src/components/features/UnidadePublicaCard.js src/components/features/UnidadeDetailSheet.js 2>/dev/null | wc -l
# esperado: 0

# Zero inline styles de layout nos novos components
grep -rn 'style={{' src/components/features/UnidadesPublicas.js src/components/features/UnidadePublicaCard.js src/components/features/UnidadeDetailSheet.js | grep -v "all: 'unset'" | wc -l
# esperado: 0

# Lint limpo
npm run lint
```
</verification>

<success_criteria>
1. /unidades é uma thin shell que importa UnidadesPublicas
2. UnidadesPublicas renderiza lista de UnidadePublicaCard + UnidadeDetailSheet ao clicar
3. UnidadeDetailSheet usa next/image com /Detalhe_Arquitetonico.png (fill em container relative h-40)
4. Zero tags `<img>` nativas nos 4 arquivos do plano
5. Zero inline styles de layout (exceto button reset canônico)
6. Paleta Obsidian Blueprint via tokens Tailwind v4: --indigo, --fg-*, --border-*, --surface*
7. Realtime subscription preservada (canal 'public-unidades', INSERT + DELETE, cleanup removeChannel)
8. `npm run lint` passa sem novos erros
</success_criteria>

<output>
Criar `.planning/phases/04-polimento-visual-p-blico/04-01-SUMMARY.md` quando concluído.
</output>
