---
phase: 06-deploy-final-e-demo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [scripts/seed-prod-demo.mjs]
autonomous: true
requirements: [DEMO-01]

must_haves:
  truths:
    - "Script seed-prod-demo.mjs popula a base de demo no Supabase de produção"
    - "Base contém 1-2 edifícios com múltiplas unidades"
    - "Base contém ao menos 1 contrato ativo com parcelas em status futura, pendente, paga e vencida"
    - "Base contém ao menos 1 locatário cadastrado"
    - "O script é idempotente — rodar novamente não duplica dados nem é destrutivo"
    - "O script NÃO contém o guard de URL de teste do e2e/seed.mjs"
  artifacts:
    - path: "scripts/seed-prod-demo.mjs"
      provides: "Seed idempotente de dados de demo na base de produção"
      min_lines: 60
      contains: "createClient"
  key_links:
    - from: "scripts/seed-prod-demo.mjs"
      to: "Supabase prod (NEXT_PUBLIC_SUPABASE_URL + SUPABASE_ROLE_KEY)"
      via: "createClient com service_role"
      pattern: "createClient\\("
---

<objective>
Criar `scripts/seed-prod-demo.mjs` — script idempotente que popula a base de demonstração no Supabase de produção: 1-2 edifícios, múltiplas unidades, ao menos 1 locatário, ao menos 1 contrato ativo com parcelas cobrindo todos os status (futura, pendente, paga, vencida). Implementa a "base pré-carregada" de D-04 (parte do DEMO-01).

Purpose: A demo da banca usa estratégia mix (D-04) — base pré-carregada + criação ao vivo de contrato. Este script monta a base. O e2e/seed.mjs aponta para Supabase local e tem guard de URL de teste; este script aponta intencionalmente para produção (D-04) e NÃO pode ter esse guard.
Output: scripts/seed-prod-demo.mjs executável via `node scripts/seed-prod-demo.mjs` lendo credenciais de prod do .env.local.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deploy-final-e-demo/06-CONTEXT.md
@.planning/phases/06-deploy-final-e-demo/06-PATTERNS.md
@scripts/seed-dev-data.mjs
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar scripts/seed-prod-demo.mjs (DEMO-01, D-04)</name>
  <files>scripts/seed-prod-demo.mjs</files>
  <read_first>
    - scripts/seed-dev-data.mjs (analógo A — conexão prod via .env.local, helper step(), verificação de existência para idempotência, resumo final; NÃO tem guard de URL)
    - e2e/seed.mjs (analógo B — cadeia FK edificio→unidade→locatario→contrato→parcelas, helper upsertUser com email_confirm; ATENÇÃO: NÃO copiar o guard de URL das linhas 156-160)
    - .planning/phases/06-deploy-final-e-demo/06-PATTERNS.md (excertos exatos dos dois analógos e aviso crítico sobre o guard)
    - CLAUDE.md §Schema e §Regras de Negócio (colunas e ENUMs de cada tabela; geração de parcelas)
  </read_first>
  <action>
    Criar scripts/seed-prod-demo.mjs seguindo o padrão de scripts/seed-dev-data.mjs: importar createClient de @supabase/supabase-js e config de dotenv (ambos já são dependências — sem instalação nova), carregar .env.local via `config({ path: resolve(process.cwd(), '.env.local') })`, ler NEXT_PUBLIC_SUPABASE_URL e SUPABASE_ROLE_KEY com guard de ausência (process.exit(1) se faltarem), criar client admin com `auth: { autoRefreshToken: false, persistSession: false }`. Reusar o helper step() de seed-dev-data.mjs para logging.

    NÃO incluir o guard de URL de teste das linhas 156-160 de e2e/seed.mjs (que aborta se a URL não contém 'test'/'local'/'127.0.0.1') — este script aponta INTENCIONALMENTE para a URL de produção (D-04, aviso crítico em 06-PATTERNS.md). O analógo correto de comportamento prod é seed-dev-data.mjs, que lê .env.local sem esse bloqueio.

    Idempotência (obrigatória — roda contra a base ao vivo da banca): antes de cada INSERT, verificar existência por chave natural (ex: edifício por nome, unidade por nome+edificio_id, contrato ativo por unidade_id como em seed-dev-data.mjs linhas 95-103) e pular se já existe, logando "skip". Nunca DELETE/TRUNCATE.

    Conteúdo da base (D-04, escopo modesto — DEMO-02 é v2, fora de escopo): criar 1-2 edifícios; em cada edifício, múltiplas unidades (mix de disponivel e alugada, com valor_visivel true/false para demonstrar "Consulte o Proprietário"); ao menos 1 locatário via upsertUser (padrão de e2e/seed.mjs linhas 10-21, com email_confirm:true para login funcionar) + insert em locatarios; ao menos 1 contrato ativo (status 'ativo', data_fim distante para não disparar alerta DASH-03 indevidamente); parcelas desse contrato cobrindo os 4 status — paga (com data_pagamento), pendente, vencida, futura — seguindo a estrutura de e2e/seed.mjs linhas 119-147 (datas relativas a hoje conforme regras de CLAUDE.md). Respeitar a constraint de no máx 1 contrato ativo por unidade_id.

    Encerrar com resumo final (padrão seed-dev-data.mjs linhas 184-199): contar parcelas por status e contratos ativos. Adicionar comentário de cabeçalho documentando uso (`node scripts/seed-prod-demo.mjs`) e que aponta para produção.
  </action>
  <verify>
    <automated>node --check scripts/seed-prod-demo.mjs && grep -v '^#' scripts/seed-prod-demo.mjs | grep -c "createClient"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/seed-prod-demo.mjs` retorna sem erro de sintaxe
    - O arquivo NÃO contém a string `127.0.0.1` nem o padrão de abort `URL de Supabase não parece ser de teste` (guard de teste ausente)
    - O arquivo contém `email_confirm` (locatário logável) e `auth: { autoRefreshToken: false, persistSession: false }` (client admin)
    - O arquivo cria registros para edificios, unidades, locatarios, contratos e parcelas (grep por cada `.from('<tabela>')`)
    - O arquivo contém verificação de existência antes de insert (padrão `.maybeSingle()` ou `select` + branch de skip) provando idempotência
    - As 4 inserções de parcela cobrem status 'paga', 'pendente', 'vencida', 'futura'
  </acceptance_criteria>
  <done>scripts/seed-prod-demo.mjs existe, passa node --check, é idempotente, não tem guard de teste, e produz a base de demo D-04 completa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script local → Supabase prod | service_role bypassa RLS e escreve direto na base de produção da banca |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | seed-prod-demo.mjs escreve na base ao vivo | mitigate | Idempotência obrigatória (verificação de existência antes de insert); nenhum DELETE/TRUNCATE; nunca sobrescreve dados existentes |
| T-06-05 | Information disclosure | SUPABASE_ROLE_KEY lido pelo script | accept | Lido de .env.local local (server-only, fora do bundle); script roda só na máquina do dev, nunca no client |
| T-06-SC | Tampering | npm installs | mitigate | Sem instalação de pacotes — @supabase/supabase-js (^2.99.2) e dotenv (^17.4.2) já são dependências do projeto |
</threat_model>

<verification>
- `node --check scripts/seed-prod-demo.mjs` passa
- Ausência do guard de URL de teste confirmada via grep
- Cadeia FK completa e mix de status de parcela confirmados via grep
</verification>

<success_criteria>
- DEMO-01 (parte base): script idempotente popula base de demo de produção com edifícios, unidades, locatário, contrato ativo e parcelas em todos os 4 status (D-04)
</success_criteria>

<output>
Create `.planning/phases/06-deploy-final-e-demo/06-02-SUMMARY.md` when done
</output>
