---
phase: 07-ajustes-finais-pre-banca
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/OwnerSidebar.js
  - e2e/dashboard-smoke.spec.js
autonomous: true
requirements: [UX-01, UX-03]
must_haves:
  truths:
    - "Proprietário vê um botão 'Sair' no footer do sidebar do dashboard"
    - "Clicar em 'Sair' executa signOut e redireciona para /login"
    - "O link '→ Acessar como Locatário' não existe mais no sidebar"
    - "O link '→ Ver Página Pública' continua presente no sidebar"
  artifacts:
    - path: "src/components/ui/OwnerSidebar.js"
      provides: "Sidebar do proprietário com logout e sem link inútil"
      contains: "LogoutButton"
    - path: "e2e/dashboard-smoke.spec.js"
      provides: "Smoke test validando presença de Sair e ausência de Acessar como Locatário"
  key_links:
    - from: "src/components/ui/OwnerSidebar.js"
      to: "src/components/ui/LogoutButton.js"
      via: "import e render no footer"
      pattern: "LogoutButton"
---

<objective>
Adicionar o botão de logout no sidebar do dashboard do Proprietário (UX-01) e remover o link inútil "→ Acessar como Locatário" (UX-03). Ambas as mudanças são no mesmo arquivo (`OwnerSidebar.js`), por isso compõem um único plano (não podem rodar em paralelo).

Slice vertical: edição do componente sidebar (UI) → validação por smoke test E2E. Após este plano, o Proprietário consegue sair do dashboard pela própria sidebar, e não há mais um link que apenas redireciona de volta (o proxy já bloqueia proprietário em `/portal`).

Purpose: Atualmente o Proprietário não tem como fazer logout pelo dashboard, e o link "Acessar como Locatário" é inútil (proxy.js redireciona proprietário de `/portal` de volta para `/dashboard`).
Output: `OwnerSidebar.js` com `<LogoutButton />` e sem o link de locatário; `e2e/dashboard-smoke.spec.js` estendido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ajustes-finais-pre-banca/07-CONTEXT.md
@.planning/phases/07-ajustes-finais-pre-banca/07-PATTERNS.md
@.planning/phases/07-ajustes-finais-pre-banca/07-UI-SPEC.md
@src/components/ui/OwnerSidebar.js
@src/components/ui/LogoutButton.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender smoke test (RED) — Sair presente, Acessar como Locatário ausente</name>
  <read_first>
    - e2e/dashboard-smoke.spec.js (arquivo a estender; já faz login como PROPRIETARIO e navega para /dashboard)
    - e2e/helpers.js (função `login(page, ...)`)
    - e2e/fixtures.js (`PROPRIETARIO`)
  </read_first>
  <action>
    Estender `e2e/dashboard-smoke.spec.js` com dois novos testes dentro do `test.describe('Dashboard smoke')` existente, reutilizando o `beforeEach` que já faz login. Test UX-01: após estar em `/dashboard`, asseverar que o botão com texto `Sair` está visível no sidebar — usar `expect(page.getByText('Sair')).toBeVisible()`. Test UX-03: asseverar que o link `→ Acessar como Locatário` NÃO existe — usar `expect(page.getByText('Acessar como Locatário')).toHaveCount(0)` (ou `.not.toBeVisible()`). Não duplicar o `beforeEach`. Seguir o estilo dos testes existentes no arquivo (import de @playwright/test, login, PROPRIETARIO). Estes testes rodam em RED agora (UX-03: link ainda existe; UX-01: botão Sair ainda não foi adicionado) e passam após a Task 2.
  </action>
  <verify>
    <automated>npx playwright test e2e/dashboard-smoke.spec.js 2>&1 | grep -qE "fail" && echo "RED confirmado" || echo "ATENCAO: passou antes da implementacao"</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/dashboard-smoke.spec.js` contém um teste asseverando `Sair` visível
    - Contém um teste asseverando `Acessar como Locatário` com count 0 / não visível
    - Os novos testes reutilizam o `beforeEach` existente (sem duplicar login)
    - Pelo menos o teste UX-03 FALHA nesta etapa (link ainda presente — estado RED)
  </acceptance_criteria>
  <done>Smoke test estendido e em RED.</done>
</task>

<task type="auto">
  <name>Task 2: OwnerSidebar — adicionar LogoutButton (UX-01) e remover link de locatário (UX-03)</name>
  <read_first>
    - src/components/ui/OwnerSidebar.js (arquivo a modificar; footer nas linhas 71-89; bloco do Link `/portal` nas linhas 78-83; bloco do email nas linhas 84-88)
    - src/components/ui/LogoutButton.js (componente a reutilizar SEM modificações; já faz `signOut()` → `router.push('/login')`; visual `font-mono text-[10px] text-fg-4`)
    - .planning/phases/07-ajustes-finais-pre-banca/07-UI-SPEC.md (seção "UX-01 — LogoutButton no OwnerSidebar": posicionamento após o `{email && <span>}`, dentro do `flex flex-col gap-[10px]`)
  </read_first>
  <action>
    Modificar `src/components/ui/OwnerSidebar.js`:

    (A) **UX-03 (D-11):** REMOVER o bloco completo `<Link href="/portal" ...>→ Acessar como Locatário</Link>` (linhas 78-83). Sem link substituto. O link `→ Ver Página Pública` (href="/") logo acima permanece intacto.

    (B) **UX-01 (D-06, D-07):** ADICIONAR `import LogoutButton from "@/components/ui/LogoutButton"` no bloco de imports (após a linha 7). RENDERIZAR `<LogoutButton />` no footer, DEPOIS do bloco `{email && <span>...</span>}`, dentro do `<div className="border-t ... flex flex-col gap-[10px]">` existente. NÃO modificar o componente `LogoutButton.js` — comportamento `signOut()` → `router.push("/login")` (D-07) já está implementado. Manter o `gap-[10px]` herdado do footer (exceção documentada no 07-UI-SPEC.md — não introduzir novos valores de espaçamento).
  </action>
  <verify>
    <automated>npx playwright test e2e/dashboard-smoke.spec.js && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'href="/portal"' src/components/ui/OwnerSidebar.js` retorna 0 (o `<Link>` para /portal foi removido — gate sobre o atributo href do elemento, não sobre texto/comentário)
    - O teste E2E (`e2e/dashboard-smoke.spec.js`) assevera count 0 de "Acessar como Locatário" no DOM renderizado — fonte de verdade para ausência (imune a comentários no código)
    - `grep -c "Ver Página Pública" src/components/ui/OwnerSidebar.js` retorna 1 (link público preservado)
    - `grep -c "LogoutButton" src/components/ui/OwnerSidebar.js` retorna ≥ 2 (import + render)
    - `src/components/ui/LogoutButton.js` permanece sem alterações
    - `e2e/dashboard-smoke.spec.js` PASSA (GREEN): Sair visível, Acessar como Locatário ausente
    - `npm run build` sem erros
  </acceptance_criteria>
  <done>Sidebar tem botão Sair funcional e não tem mais o link inútil. Smoke test em GREEN, build limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase Auth | Ação de logout (signOut) cruza para o servidor de auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Repudiation | LogoutButton (logout) | mitigate | `supabase.auth.signOut()` (já existente, testado no portal) limpa a sessão server-side; sem hand-roll de limpeza de cookie |
| T-07-07 | Information Disclosure | OwnerSidebar (remoção de link) | accept | Remoção de link não altera controle de acesso — `proxy.js` já bloqueia proprietário em `/portal`; mudança é puramente de UX |
| T-07-SC | Tampering | npm/shadcn installs | mitigate | Nenhum pacote novo neste plano — reutiliza `LogoutButton.js` existente |
</threat_model>

<verification>
- `npx playwright test e2e/dashboard-smoke.spec.js` passa (incluindo os 2 novos testes)
- `npm run build` sem erros
- Verificação visual: logar como proprietário, ver botão "Sair" no footer do sidebar, clicar e confirmar redirect para /login
</verification>

<success_criteria>
- Proprietário tem botão de logout no sidebar que redireciona para `/login` (UX-01, critério 2 da fase)
- Link "Acessar como Locatário" removido do sidebar (UX-03, critério 4 da fase)
- Link "Ver Página Pública" preservado
</success_criteria>

<output>
Create `.planning/phases/07-ajustes-finais-pre-banca/07-02-SUMMARY.md` when done
</output>
