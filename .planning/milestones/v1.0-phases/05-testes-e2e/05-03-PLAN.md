---
phase: 05-testes-e2e
plan: 03
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - e2e/parcelas.spec.js
autonomous: true
requirements:
  - TEST-02
tags:
  - testing
  - e2e
  - playwright
  - parcelas
  - edge-function

must_haves:
  truths:
    - "Após criar contrato via UI (handleCriarContrato → gerarParcelas automático), navegar para /dashboard/contratos/[id] exibe parcelas geradas pela Edge Function"
    - "Spec marca parcela como paga via UI e confirma transição de status para 'paga'"
    - "Spec é independente do seed principal — cria sua própria cadeia FK no beforeAll via supabaseAdmin com prefixo E2E-"
  artifacts:
    - path: "e2e/parcelas.spec.js"
      provides: "Cobertura TEST-02 — ciclo de Parcelas via Edge Function"
      contains: "test.describe('TEST-02"
      min_lines: 80
  key_links:
    - from: "e2e/parcelas.spec.js"
      to: "Edge Function gerar-parcelas"
      via: "criação de contrato via UI dispara handleCriarContrato → gerarParcelas"
      pattern: "Criar Contrato"
    - from: "e2e/parcelas.spec.js"
      to: "/dashboard/contratos/[id]"
      via: "page.goto após capturar contrato_id via supabaseAdmin"
      pattern: "/dashboard/contratos/"
---

<objective>
Slice vertical de TEST-02: spec dedicado `e2e/parcelas.spec.js` que valida o ciclo completo de Parcelas — geração via Edge Function `gerar-parcelas` (disparada automaticamente por `handleCriarContrato`) e mudança de status ao marcar parcela como paga via UI.

User story: **As a** Proprietário, **I want to** ter cobertura automatizada do ciclo de parcelas (geração + marcar como paga), **so that** eu posso confiar que a Edge Function `gerar-parcelas` e a transição `pendente → paga` continuam funcionando após qualquer refatoração.

Purpose: Validar a integração com a Edge Function — único spec que exercita esse ponto de integração. Sem ele, regressões na EF ou no `marcarParcelaComoPaga` passam silenciosamente.
Output: `e2e/parcelas.spec.js` verde, com setup próprio via supabaseAdmin (D-06) e timeout estendido para chamadas EF (15s).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-testes-e2e/05-CONTEXT.md
@.planning/phases/05-testes-e2e/05-RESEARCH.md
@.planning/phases/05-testes-e2e/05-PATTERNS.md
@.planning/phases/05-testes-e2e/05-01-SUMMARY.md
@CLAUDE.md
@e2e/dashboard.spec.js
@e2e/seed.mjs
@e2e/helpers.js
@e2e/fixtures.js
@src/components/features/Contratos.js
@src/components/features/Parcelas.js

<interfaces>
<!-- Contratos chave. -->

handleCriarContrato (Contratos.js):
- Após `criarContrato` retornar status 200, chama `gerarParcelas(contratoId)` automaticamente
- gerarParcelas faz POST para http://127.0.0.1:54321/functions/v1/gerar-parcelas
- NÃO existe botão "Gerar Parcelas" na UI — verificação é por navegação para /dashboard/contratos/[id]

/dashboard/contratos/[id] (Parcelas.js):
- Lista parcelas com colunas: numero, data_fechamento, data_vencimento, data_pagamento, status
- Status possíveis: 'futura', 'pendente', 'paga', 'vencida'
- Botão "Marcar Paga" aparece apenas em linhas com status 'pendente' ou 'vencida'

supabaseAdmin setup (padrão de seed.mjs linhas 1-12):
- import { createClient } from '@supabase/supabase-js'
- import { config } from 'dotenv'; config({ path: '.env.test' })
- admin = createClient(NEXT_PUBLIC_SUPABASE_URL, SUPABASE_ROLE_KEY, { auth: { autoRefreshToken: false, persistSession: false } })

Pré-condição de ambiente (RESEARCH pitfall 3):
- `supabase functions serve` DEVE estar rodando antes da suíte
- Sem isso, criar contrato pela UI falha com erro 404/connection refused na chamada da EF
- Spec não pode iniciar o serviço — apenas documentar como pré-condição no SUMMARY

Pattern de assertion com timeout estendido:
- await expect(locator).toBeVisible({ timeout: 15_000 }) — EF é mais lenta que DB direto
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spec parcelas.spec.js — geração via EF + marcar paga</name>
  <files>e2e/parcelas.spec.js</files>
  <read_first>
    - e2e/seed.mjs (padrão supabaseAdmin + insert FK chain, linhas 1-90)
    - e2e/dashboard.spec.js (estrutura básica de spec: imports, beforeEach login)
    - .planning/phases/05-testes-e2e/05-RESEARCH.md (Selector Map — Parcelas; Pattern 1 — beforeAll com supabaseAdmin; Pitfall 3 — EF requer serve)
    - .planning/phases/05-testes-e2e/05-PATTERNS.md (seção `e2e/parcelas.spec.js`)
    - src/components/features/Parcelas.js (confirmar exato texto do botão "Marcar Paga" e estrutura da tabela)
    - src/components/features/Contratos.js (confirmar que handleCriarContrato chama gerarParcelas)
  </read_first>
  <behavior>
    - beforeAll cria cadeia via admin: edificio 'E2E-Edifício Parcelas', unidade 'E2E-Sala Parcelas' (status disponivel, sem contrato), auth user e2e-parcelas@test.romma.local (email_confirm: true), locatario 'E2E-Locatário Parcelas' (usuario_id = auth user). NÃO cria contrato no beforeAll — o teste faz isso pela UI para disparar gerarParcelas.
    - Teste 1 "gera parcelas via Edge Function ao criar contrato": login Proprietário, navega para /dashboard/contratos, clica "Novo Contrato", seleciona via combobox o locatário e a unidade E2E-, preenche data_inicio e data_fim, clica "Criar Contrato". Aguarda 'E2E-Locatário Parcelas' aparecer na tabela com timeout 15_000. Captura contratoId via supabaseAdmin (select por locatario_id). Navega para `/dashboard/contratos/${contratoId}` e verifica que pelo menos uma parcela existe (`getByText('futura').or(getByText('pendente'))` visível em 15s).
    - Teste 2 "marca parcela como paga": no mesmo contratoId, garante via admin que existe pelo menos uma parcela com status 'pendente' (atualizar parcela 1 para `status='pendente'`, `data_fechamento=ontem`, `data_vencimento=hoje` se necessário, ou criar uma diretamente via admin). Recarrega `/dashboard/contratos/${contratoId}`. Clica no primeiro botão "Marcar Paga" da página. Verifica que 'paga' fica visível em 10s.
    - afterAll: deleta em cascata parcelas → contratos → unidades → edificios → locatarios criados pelo spec, e deleta auth user e2e-parcelas@test.romma.local. (Teardown global por prefixo é fallback.)
  </behavior>
  <action>Criar `e2e/parcelas.spec.js`. Imports: `{ test, expect }` de `@playwright/test`, `login` de `./helpers.js`, `PROPRIETARIO` de `./fixtures.js`, `createClient` de `@supabase/supabase-js`, `config` de `dotenv`. Após imports, chamar `config({ path: '.env.test' })` e declarar const `admin = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_ROLE_KEY, { auth: { autoRefreshToken: false, persistSession: false } })`. Envelope `test.describe('TEST-02 — Parcelas', () => { test.use({ viewport: { width: 1440, height: 900 } }); ... })`. Variáveis no escopo do describe para guardar IDs: `let edificioId, unidadeId, locatarioId, authUserId, contratoId`. `test.beforeAll` cria a cadeia FK conforme behavior (padrão de seed.mjs com `.select().single()` e `if (err) throw err` após cada insert; criar auth user via `admin.auth.admin.createUser({ email: 'e2e-parcelas@test.romma.local', password: 'Test1234!', email_confirm: true })`). `test.beforeEach` faz login + `waitForURL('**/dashboard', { timeout: 15_000 })`. Dois testes conforme behavior — capturar `contratoId` via `admin.from('contratos').select('id').eq('locatario_id', locatarioId).order('created_at', { ascending: false }).limit(1).single()` após criar contrato pela UI. Usar combobox click pattern para Locatário (combobox[0]) e Unidade (combobox[1]); datas via `page.locator('input[type="date"]').nth(0).fill('2026-06-01')` e `.nth(1).fill('2027-06-01')`. Para o teste de "Marcar Paga", garantir parcela pendente via `admin.from('parcelas').update({ status: 'pendente', data_fechamento: ontemISO, data_vencimento: hojeISO, data_pagamento: null }).eq('contrato_id', contratoId).eq('numero', 1)` antes do reload. `test.afterAll` faz cascata FK conforme behavior. Documentar no comentário do topo do arquivo a pré-condição `supabase functions serve` (pitfall 3 RESEARCH).</action>
  <verify>
    <automated>npx playwright test e2e/parcelas.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `e2e/parcelas.spec.js` existe
    - Contém `test.describe('TEST-02` no topo
    - Contém `test.beforeAll` que cria edifício, unidade (status disponivel), auth user com email `e2e-parcelas@test.romma.local`, e locatário — todos prefixados `E2E-`
    - Contém `test.afterAll` com cascata FK na ordem parcelas → contratos → unidades → edificios → locatarios → auth user
    - Contém pelo menos um `getByRole('button', { name: 'Criar Contrato' })`
    - Contém pelo menos um `getByRole('button', { name: 'Marcar Paga' })`
    - Contém ao menos uma assertion com `timeout: 15_000` (operação que envolve EF)
    - Contém comentário documentando pré-condição `supabase functions serve`
    - NÃO contém botão fictício "Gerar Parcelas" — assertion verifica parcelas existentes via navegação direta
    - `npx playwright test e2e/parcelas.spec.js` passa 100% (2 testes verdes) com `supabase functions serve` ativo
    - Pós-execução: `select count(*) from contratos where locatario_id in (select id from locatarios where nome_razao_social like 'E2E-%')` retorna 0
  </acceptance_criteria>
  <done>Spec verde demonstra: (1) criar contrato pela UI gera parcelas via EF automaticamente, (2) "Marcar Paga" muda status para `paga`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spec → Edge Function gerar-parcelas | Disparada indiretamente via Server Action criarContrato → gerarParcelas (legacy JWT) |
| Spec → Supabase admin API | Setup/teardown e captura de contratoId via SUPABASE_ROLE_KEY |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Denial of Service | EF não disponível causa flake difícil de diagnosticar | mitigate | Documentar pré-condição `supabase functions serve` em comentário do spec e no SUMMARY; usar timeout 15s para assertions que dependem da EF |
| T-05-08 | Tampering | UPDATE em parcelas via admin bypassa RLS | accept | Operação intencional para garantir setup determinístico (mesma estratégia do seed); ambiente local apenas |
| T-05-09 | Information Disclosure | contratoId em URL | accept | UUID v4 — não enumerável; ambiente local |
| T-05-SC | Tampering | npm installs | accept | Esta wave não instala pacotes |
</threat_model>

<verification>
- `supabase functions serve gerar-parcelas` rodando em paralelo ao `npx playwright test`
- `npx playwright test e2e/parcelas.spec.js` passa 100%
- Suíte completa (`npx playwright test`) continua verde com este spec adicionado
- Pós-execução: nenhum registro `E2E-`/`e2e-parcelas` persiste no banco
</verification>

<success_criteria>
1. TEST-02 coberto: criar contrato via UI → parcelas aparecem em `/dashboard/contratos/[id]` (geração via EF validada implicitamente)
2. Transição `pendente → paga` validada via UI ("Marcar Paga" muda status)
3. Spec independente do seed principal — cria/destrói sua própria cadeia FK
4. Pré-condição `supabase functions serve` documentada
</success_criteria>

<output>
Criar `.planning/phases/05-testes-e2e/05-03-SUMMARY.md` ao concluir, registrando: comandos exatos para reproduzir (incluindo `supabase functions serve`), tempo médio do spec, e quaisquer ajustes em seletores de Parcelas.js detectados durante implementação.
</output>
