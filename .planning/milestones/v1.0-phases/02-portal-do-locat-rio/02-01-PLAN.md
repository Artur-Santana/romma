---
phase: 02-portal-do-locat-rio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/seed.mjs
  - e2e/global-teardown.js
  - e2e/portal.spec.js
  - e2e/auth-redirect.spec.js
  - playwright.config.js
autonomous: true
requirements:
  - TEST-03
  - PORT-01
  - PORT-02
  - PORT-03
user_setup: []

must_haves:
  truths:
    - "Suite Playwright contém spec dedicado para o Portal do Locatário (e2e/portal.spec.js)"
    - "Seed cria edifício+unidade+locatário+contrato+parcelas (paga/pendente/vencida) vinculados ao usuário locatario@test.romma.local"
    - "Teardown remove dados de domínio na ordem FK correta (parcelas → contratos → locatarios → unidades → edificios)"
    - "Teste auth-redirect 1.2 espera /portal/dashboard em vez de /"
    - "Antes do código de Fase 2 existir, portal.spec.js executa em RED (failing) consistente com TDD outside-in"
  artifacts:
    - path: "e2e/portal.spec.js"
      provides: "Specs PORT-01, PORT-02, PORT-03"
      contains: "PORT-01"
    - path: "e2e/global-teardown.js"
      provides: "Cleanup FK-aware do seed do locatário"
      exports: ["default"]
    - path: "playwright.config.js"
      provides: "Wiring globalTeardown"
      contains: "globalTeardown"
    - path: "e2e/seed.mjs"
      provides: "Seed de domínio do locatário"
      contains: "locatario@test.romma.local"
  key_links:
    - from: "playwright.config.js"
      to: "e2e/global-teardown.js"
      via: "globalTeardown property"
      pattern: "globalTeardown:.*global-teardown"
    - from: "e2e/portal.spec.js"
      to: "e2e/helpers.js + e2e/fixtures.js"
      via: "import { login } / LOCATARIO"
      pattern: "from ['\"]\\./(helpers|fixtures)"
    - from: "e2e/seed.mjs"
      to: "auth.users (locatario@test.romma.local)"
      via: "admin.from('locatarios').upsert({ usuario_id: ... })"
      pattern: "locatario@test.romma.local"
---

<objective>
Estabelecer a base de validação E2E para a Fase 2 antes de qualquer implementação de produção: testes Playwright para PORT-01/02/03 no estado RED, seed de dados expandido com a cadeia FK do locatário (edifício → unidade → locatário → contrato → parcelas), teardown FK-aware, e correção do auth-redirect existente que será quebrado por D-01.

Purpose: Outside-in TDD — as três specs em RED guiam o desenvolvimento das slices verticais nas Waves 2 e 3. Sem isso não há sinal verde objetivo.
Output: 1 spec novo (portal), 1 teardown novo, 3 arquivos modificados (seed, config, auth-redirect).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-portal-do-locat-rio/02-CONTEXT.md
@.planning/phases/02-portal-do-locat-rio/02-RESEARCH.md
@.planning/phases/02-portal-do-locat-rio/02-PATTERNS.md
@.planning/phases/02-portal-do-locat-rio/02-UI-SPEC.md

<interfaces>
Helpers e fixtures existentes (reutilizar — não modificar):

From e2e/helpers.js:
- export async function login(page, credentials): preenche form de /login e submete

From e2e/fixtures.js:
- export const LOCATARIO = { email: 'locatario@test.romma.local', password: 'Test1234!' }
- export const PROPRIETARIO já existe para os outros testes

From e2e/seed.mjs (atual):
- admin client criado com SUPABASE_ROLE_KEY
- upsertUser(email, password) → cria/retorna user em auth.users com email_confirm:true
- seed() já chama upsertUser para PROPRIETARIO; vamos adicionar locatário e cadeia FK

Schema relevante (do CLAUDE.md):
- edificios(nome, endereco)
- unidades(edificio_id, nome, area_m2, valor_mensal, valor_visivel, status)
- locatarios(usuario_id, nome_razao_social, tipo, documento, email, telefone, status_convite)
- contratos(unidade_id, locatario_id, data_inicio, data_fim, status)
- parcelas(contrato_id, numero, data_fechamento, data_vencimento, data_pagamento, status)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expandir e2e/seed.mjs com cadeia FK do locatário (D-08)</name>
  <files>e2e/seed.mjs</files>
  <read_first>
    - e2e/seed.mjs (estado atual — manter admin client e upsertUser intactos)
    - e2e/fixtures.js (referência das credenciais LOCATARIO)
    - .planning/phases/02-portal-do-locat-rio/02-RESEARCH.md (Pattern 5, linhas 332–376 — código de referência)
    - .planning/phases/02-portal-do-locat-rio/02-PATTERNS.md (seção e2e/seed.mjs — nota sobre status_convite)
    - CLAUDE.md (schema de cada tabela — campos obrigatórios)
  </read_first>
  <action>
    Após a chamada existente que cria o usuário PROPRIETARIO via `upsertUser`, adicionar bloco que: (1) chama `await upsertUser('locatario@test.romma.local', 'Test1234!')` salvando em `locatarioUser`; (2) insere com `admin.from('edificios').insert({ nome: 'Edifício Teste E2E', endereco: 'Rua Teste, 1' }).select().single()`; (3) insere unidade com `edificio_id` da etapa anterior, `nome: 'Sala 101'`, `area_m2: 40`, `valor_mensal: 2500`, `valor_visivel: true`, `status: 'alugada'`; (4) faz `upsert` em `locatarios` com `{ usuario_id: locatarioUser.id, nome_razao_social: 'Locatário Teste', tipo: 'pf', documento: '12345678901', email: locatarioUser.email, telefone: '11999999999', status_convite: 'aceito' }` e `onConflict: 'usuario_id'`; (5) insere contrato ativo com `data_inicio = hoje`, `data_fim = hoje + 365 dias`, `status: 'ativo'`; (6) insere 3 parcelas no mesmo `contrato_id`: numero 1 paga (data_pagamento = ontem), numero 2 vencida (data_vencimento = ontem, data_pagamento = null), numero 3 pendente (data_fechamento = hoje, data_vencimento = hoje + 7 dias, data_pagamento = null). Cada `.insert()` deve checar `error` e fazer `throw` se houver. Incluir `status_convite: 'aceito'` no upsert do locatário (precaução vs RLS — ver Pitfall 6 do RESEARCH). Ordem de inserção é obrigatória: edifício → unidade → locatário → contrato → parcelas. NÃO criar parcela com `status: 'futura'` (contradiz PORT-03 e seed design).
  </action>
  <verify>
    <automated>node e2e/seed.mjs &amp;&amp; node --env-file=.env.test -e "import('@supabase/supabase-js').then(async ({createClient})=>{const a=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_ROLE_KEY);const {data}=await a.from('parcelas').select('status').in('status',['paga','pendente','vencida']);console.log(JSON.stringify(data));}"</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/seed.mjs` contains the string `locatario@test.romma.local`
    - `e2e/seed.mjs` contains the string `status_convite: 'aceito'` (or `status_convite:"aceito"`)
    - `e2e/seed.mjs` contains the string `Sala 101`
    - `e2e/seed.mjs` contains `from('edificios')`, `from('unidades')`, `from('locatarios')`, `from('contratos')`, `from('parcelas')` (all five)
    - `e2e/seed.mjs` does NOT insert any parcela with `status: 'futura'` (grep -v '^//' e2e/seed.mjs | grep -c "status: 'futura'" returns 0)
    - Running `node e2e/seed.mjs` exits 0 and creates a contrato `ativo` for the locatario user
    - After seed runs, querying parcelas where contrato_id = seeded contrato returns exactly 3 rows with statuses {paga, pendente, vencida}
  </acceptance_criteria>
  <done>Seed expandido cria toda a cadeia FK necessária para os testes do portal, com mix correto de status de parcelas.</done>
</task>

<task type="auto">
  <name>Task 2: Criar e2e/global-teardown.js e wire em playwright.config.js (D-09)</name>
  <files>e2e/global-teardown.js, playwright.config.js</files>
  <read_first>
    - e2e/seed.mjs (referência do bootstrap do admin client — linhas 1–10)
    - playwright.config.js (estado atual — onde adicionar globalTeardown)
    - .planning/phases/02-portal-do-locat-rio/02-RESEARCH.md (Pattern 5, linhas 378–421 — código completo do teardown)
    - .planning/phases/02-portal-do-locat-rio/02-PATTERNS.md (seção global-teardown.js)
  </read_first>
  <action>
    Criar `e2e/global-teardown.js` como ES module com `export default async function globalTeardown()`. Bootstrap idêntico ao do `seed.mjs`: importar `createClient` de `@supabase/supabase-js`, importar `config` de `dotenv`, chamar `config({ path: '.env.test' })`, criar `admin` com `NEXT_PUBLIC_SUPABASE_URL` e `SUPABASE_ROLE_KEY` e `{ auth: { autoRefreshToken: false, persistSession: false } }`. Lógica do teardown: (1) chamar `admin.auth.admin.listUsers()` e localizar `locatarioUser` com email `locatario@test.romma.local` — se não encontrar, `return` cedo; (2) `select id from locatarios where usuario_id = locatarioUser.id` com `.maybeSingle()` — se null, return; (3) buscar `contratos.id` onde `locatario_id = locatario.id`; (4) se houver `contratoIds.length`, deletar `parcelas` com `.in('contrato_id', contratoIds)` e depois `contratos` com `.in('id', contratoIds)`; (5) deletar `locatarios` com `.eq('id', locatario.id)`; (6) `select id, edificio_id from unidades where nome = 'Sala 101'` — se houver, deletar `unidades` e depois `edificios` (eq edificio_id da unidade). Ordem obrigatória: parcelas → contratos → locatarios → unidades → edificios (FK constraints, Pitfall 3 do RESEARCH). Em `playwright.config.js`, adicionar nova chave `globalTeardown: './e2e/global-teardown.js'` imediatamente após a chave `globalSetup: './e2e/global-setup.js'`. NÃO deletar o user em `auth.users` (D-09).
  </action>
  <verify>
    <automated>node -e "import('./e2e/global-teardown.js').then(m => m.default()).then(() => console.log('OK')).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/global-teardown.js` exists and exports a default async function
    - `e2e/global-teardown.js` contains `from('parcelas')`, `from('contratos')`, `from('locatarios')`, `from('unidades')`, `from('edificios')` in this textual order (verify with `grep -n "from(" e2e/global-teardown.js` shows parcelas before contratos before locatarios before unidades before edificios)
    - `e2e/global-teardown.js` does NOT contain `auth.admin.deleteUser` (auth users preserved per D-09)
    - `playwright.config.js` contains the string `globalTeardown:` and the value references `./e2e/global-teardown.js`
    - Running the teardown immediately after seed leaves zero rows in parcelas/contratos for the locatario user and zero rows in unidades with nome='Sala 101'
  </acceptance_criteria>
  <done>Teardown FK-aware é executado pelo Playwright após a suite, limpando todo o estado criado pelo seed sem violar foreign keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar e2e/portal.spec.js (RED) + atualizar auth-redirect.spec.js teste 1.2</name>
  <files>e2e/portal.spec.js, e2e/auth-redirect.spec.js</files>
  <read_first>
    - e2e/auth-redirect.spec.js (estado atual, especialmente o teste "1.2" — linha 16 a ser alterada)
    - e2e/dashboard.spec.js (referência de beforeEach + login pattern)
    - e2e/helpers.js (assinatura do helper login)
    - e2e/fixtures.js (LOCATARIO export)
    - .planning/phases/02-portal-do-locat-rio/02-UI-SPEC.md (textos canônicos: "PORTAL DO LOCATÁRIO", "SEU CONTRATO", "CONTRATO ATIVO", "HISTÓRICO DE PARCELAS", "Sala 101")
    - .planning/phases/02-portal-do-locat-rio/02-PATTERNS.md (seção e2e/portal.spec.js — imports, beforeEach, waitForURL patterns)
  </read_first>
  <behavior>
    - PORT-01: login com credenciais LOCATARIO redireciona para URL terminando em /portal/dashboard
    - PORT-02: após login, página exibe o nome da unidade "Sala 101" e valor "R$ 2.500" e o status "ativo"
    - PORT-03: ParcelsTable exibe pelo menos uma parcela com status "paga", uma "pendente" e uma "vencida"; nenhuma parcela com status "futura" é renderizada (asserção: a string "futura" NÃO aparece dentro da região da tabela de parcelas)
    - auth-redirect 1.2: o redirect de não-proprietário agora termina em /portal/dashboard, não em /
  </behavior>
  <action>
    Criar `e2e/portal.spec.js`. Imports: `import { test, expect } from '@playwright/test'`, `import { login } from './helpers.js'`, `import { LOCATARIO } from './fixtures.js'`. Estruturar como `test.describe('Portal do Locatário', () => { ... })` com três testes nomeados exatamente `'PORT-01: locatário loga e é redirecionado para /portal/dashboard'`, `'PORT-02: locatário visualiza contrato ativo (unidade, valor, status)'`, `'PORT-03: ParcelsTable exibe paga/pendente/vencida e omite futura'`. Para PORT-02 e PORT-03, usar `test.beforeEach` que chama `await login(page, LOCATARIO)` e `await page.waitForURL('**/portal/dashboard', { timeout: 15_000 })`. Asserções: PORT-01 → `expect(page.url()).toContain('/portal/dashboard')`; PORT-02 → `await expect(page.getByText('Sala 101')).toBeVisible({ timeout: 10_000 })`, `await expect(page.getByText(/R\$\s*2\.?500/)).toBeVisible()`, `await expect(page.getByText(/ativo/i)).toBeVisible()`; PORT-03 → localizar a região da tabela via `page.getByRole('region', { name: /HISTÓRICO DE PARCELAS/i })` OU `page.locator('[data-testid="parcelas-table"]')` (executor escolhe — adicionar testid no componente em Plan 03 se necessário), depois `await expect(parcelasRegion.getByText(/paga/i)).toBeVisible()`, mesmo para `pendente` e `vencida`, e `await expect(parcelasRegion.getByText(/futura/i)).toHaveCount(0)`. Em `e2e/auth-redirect.spec.js`, localizar o teste cujo nome contém "1.2" (não-proprietário loga e é redirecionado) e substituir a linha 16 `await page.waitForURL('http://localhost:3000/', { timeout: 10_000 })` por `await page.waitForURL('**/portal/dashboard', { timeout: 10_000 })`, e a linha seguinte `expect(page.url()).toBe('http://localhost:3000/')` por `expect(page.url()).toContain('/portal/dashboard')`. Não tocar nos demais testes do arquivo. NOTA: este task PRODUZ testes que falham — isso é esperado (RED phase, Waves 2/3 transformam em GREEN).
  </action>
  <verify>
    <automated>npx playwright test e2e/portal.spec.js --list 2>&amp;1 | grep -E "PORT-0[123]" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/portal.spec.js` exists
    - `npx playwright test e2e/portal.spec.js --list` lists exactly 3 tests with names containing 'PORT-01', 'PORT-02', 'PORT-03'
    - `e2e/portal.spec.js` contains `from './helpers.js'` and `from './fixtures.js'` (no inline credentials)
    - `e2e/portal.spec.js` contains the literal `'**/portal/dashboard'`
    - `e2e/auth-redirect.spec.js` no longer contains the string `'http://localhost:3000/'` followed by waitForURL on the 1.2 test (grep `waitForURL.*localhost:3000/'` returns 0 inside the 1.2 block)
    - `e2e/auth-redirect.spec.js` contains the string `'**/portal/dashboard'`
    - Running `npx playwright test e2e/portal.spec.js` after this task and before Plans 02/03 land FAILS (RED phase) — exit code non-zero, expected
  </acceptance_criteria>
  <done>Specs PORT-01/02/03 declarados em RED; auth-redirect.spec.js teste 1.2 atualizado para a nova URL de Locatário. Suite roda e falha de forma controlada — sinal verde objetivo está definido para Waves 2 e 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed/teardown → Supabase (admin) | Service role key bypassa RLS — apenas em scripts E2E |
| .env.test → process.env | Credenciais carregadas de arquivo local; nunca commitado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | seed.mjs / global-teardown.js | mitigate | SUPABASE_ROLE_KEY lido de .env.test (gitignored); nunca embutido em commits; scripts só executados localmente / em CI controlado |
| T-02-02 | Tampering | seed expandido | accept | Dados de teste; conflito improvável pois `onConflict: 'usuario_id'` no upsert do locatário e `nome = 'Sala 101'` único no ambiente de teste |
| T-02-03 | Elevation of Privilege | global-teardown.js | mitigate | Teardown NÃO deleta auth.users (D-09); apenas dados de domínio filtrados pelo usuario_id do locatário de teste, evitando wipe acidental de outros dados |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma instalação de pacote nova nesta fase (RESEARCH § Package Legitimacy Audit) — não aplicável |
</threat_model>

<verification>
- `node e2e/seed.mjs` exits 0
- Após seed: query SQL via admin retorna exatamente 1 contrato ativo + 3 parcelas (statuses {paga, pendente, vencida}) para o usuario_id do locatário de teste
- `npx playwright test e2e/portal.spec.js --list` lista PORT-01, PORT-02, PORT-03
- `npx playwright test e2e/portal.spec.js` falha (esperado — RED phase)
- `node -e "import('./e2e/global-teardown.js').then(m => m.default())"` exits 0 e remove o estado criado pelo seed
</verification>

<success_criteria>
- 3 testes Playwright declarados para PORT-01/02/03 que falham no estado atual (sem implementação) e passarão após Waves 2 e 3
- Seed cria toda a cadeia FK do locatário; teardown remove sem violar FK
- auth-redirect.spec.js 1.2 ajustado para a nova rota
- TEST-03 infraestrutura completa; nenhum código de produção tocado nesta wave
</success_criteria>

<output>
Create `.planning/phases/02-portal-do-locat-rio/02-01-SUMMARY.md` when done
</output>
