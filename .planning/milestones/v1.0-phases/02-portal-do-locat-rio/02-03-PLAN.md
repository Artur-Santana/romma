---
phase: 02-portal-do-locat-rio
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/lib/queries-client.js
  - src/components/features/portal/PortalDashboard.js
  - src/components/features/portal/ContratoCard.js
  - src/components/features/portal/ParcelsTable.js
autonomous: true
requirements:
  - PORT-02
  - PORT-03
  - VIS-03
  - TEST-03
user_setup: []

must_haves:
  truths:
    - "Locatário logado vê dados do seu contrato ativo (nome da unidade, valor_mensal formatado em BRL, data_inicio e data_fim formatadas, status)"
    - "Locatário vê histórico de parcelas com colunas: número, vencimento, pagamento, status"
    - "Parcelas com status 'futura' NÃO aparecem na tabela (filtro feito na query)"
    - "Locatário sem contrato ativo vê empty state 'Nenhum contrato ativo' (não crash)"
    - "Locatário com contrato sem parcelas visíveis vê empty state 'Nenhuma parcela registrada'"
    - "Erro de fetch exibe banner de erro em vez de crash"
    - "Nenhum arquivo novo/modificado nesta wave contém inline styles (D-11)"
    - "Após esta wave, PORT-02 e PORT-03 ficam GREEN e a suite completa do portal passa"
  artifacts:
    - path: "src/lib/queries-client.js"
      provides: "getContratoAtivoByLocatario + getParcelasPortal"
      contains: "getContratoAtivoByLocatario"
    - path: "src/components/features/portal/ContratoCard.js"
      provides: "Cartão visual do contrato ativo"
      exports: ["default"]
    - path: "src/components/features/portal/ParcelsTable.js"
      provides: "Tabela de parcelas read-only com StatusBadge"
      exports: ["default"]
    - path: "src/components/features/portal/PortalDashboard.js"
      provides: "Wire-up: fetch chain + estados (loading/empty/error/data) + render dos sub-componentes"
      contains: "getContratoAtivoByLocatario"
  key_links:
    - from: "PortalDashboard.js"
      to: "queries-client.js"
      via: "getLocatarioByUserId → getContratoAtivoByLocatario → getParcelasPortal"
      pattern: "getContratoAtivoByLocatario|getParcelasPortal"
    - from: "ParcelsTable.js"
      to: "src/components/ui/StatusBadge"
      via: "import default"
      pattern: "import StatusBadge"
    - from: "ContratoCard.js"
      to: "src/lib/utils.js (fmtBRL, fmtData)"
      via: "named imports"
      pattern: "fmtBRL|fmtData"
    - from: "getParcelasPortal"
      to: "Supabase parcelas table"
      via: ".neq('status', 'futura')"
      pattern: "neq\\(['\"]status['\"]\\s*,\\s*['\"]futura"
---

<objective>
Encher a slice do portal com dados reais: adicionar as duas queries novas (D-06, D-07), criar ContratoCard e ParcelsTable conforme UI-SPEC, e transformar o shell do PortalDashboard (criado em Plan 02) em um Client Component que faz a cadeia user → locatario → contrato → parcelas e renderiza loading/empty/error/data states.

Purpose: PORT-02 e PORT-03 GREEN. Encerra o ciclo MVP da Fase 2 — após esta wave a fase está implementada e a suite Playwright do portal passa integralmente.
Output: 1 query file expandido (queries-client.js), 1 component file expandido (PortalDashboard.js), 2 components novos (ContratoCard, ParcelsTable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-portal-do-locat-rio/02-CONTEXT.md
@.planning/phases/02-portal-do-locat-rio/02-RESEARCH.md
@.planning/phases/02-portal-do-locat-rio/02-PATTERNS.md
@.planning/phases/02-portal-do-locat-rio/02-UI-SPEC.md
@src/lib/queries-client.js
@src/components/features/Parcelas.js

<interfaces>
Utilitários existentes (não modificar):

From src/lib/utils.js:
- export function fmtBRL(v): number → "R$ 2.500" (lida com null → "R$ 0")
- export function fmtData(d): isoDate → "01 jan. 2026" (lida com null → "—")

From src/components/ui/StatusBadge.js:
- export default function StatusBadge({ status }): suporta "paga", "pendente", "vencida", "ativo", "encerrado", "cancelado" (entre outros). Internamente usa inline styles — não é arquivo novo/modificado, D-11 não aplica.

From src/lib/queries-client.js (existente — não alterar):
- export async function getLocatarioByUserId(userId): retorna { id, usuario_id, nome_razao_social, ... } via maybeSingle()

Shape esperado de getContratoAtivoByLocatario(locatarioId):
- { id, data_inicio, data_fim, status, observacoes, unidades: { nome, valor_mensal } } | null

Shape esperado de getParcelasPortal(contratoId):
- Array<{ id, numero, data_vencimento, data_pagamento, status }> (status ∈ {paga, pendente, vencida}; nunca 'futura'; ordem DESC por data_vencimento)

Copy canônico (UI-SPEC):
- ContratoCard eyebrow: "CONTRATO ATIVO"
- ParcelsTable eyebrow: "HISTÓRICO DE PARCELAS"
- Empty (sem contrato): heading "Nenhum contrato ativo" / body "Você não possui contrato ativo. Entre em contato com o proprietário."
- Empty (sem parcelas): "Nenhuma parcela registrada para este contrato."
- Error: heading "Erro ao carregar dados" / body "Não foi possível buscar seu contrato. Tente recarregar a página."
- Colunas ParcelsTable (UI-SPEC): #, Vencimento, Pagamento, Status (read-only — sem coluna Ação)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar queries getContratoAtivoByLocatario + getParcelasPortal em queries-client.js (D-06, D-07)</name>
  <files>src/lib/queries-client.js</files>
  <read_first>
    - src/lib/queries-client.js (estado atual completo — modelo das funções existentes; localizar onde adicionar; NÃO alterar getContratosByLocatario nem getParcelasByContrato)
    - .planning/phases/02-portal-do-locat-rio/02-PATTERNS.md (seção queries-client.js — código de referência)
    - .planning/phases/02-portal-do-locat-rio/02-RESEARCH.md (Pattern 4)
    - .planning/phases/02-portal-do-locat-rio/02-CONTEXT.md (D-06, D-07 + Claude's Discretion sobre maybeSingle)
  </read_first>
  <behavior>
    - getContratoAtivoByLocatario(locatarioId): retorna o único contrato com status='ativo' do locatário, joined com unidades(nome, valor_mensal); retorna null se não existir (não lança erro)
    - getParcelasPortal(contratoId): retorna array de parcelas do contrato com status != 'futura', ordenadas por data_vencimento DESC; retorna [] (não null/undefined) se não houver
  </behavior>
  <action>
    Em `src/lib/queries-client.js`, adicionar duas exports nomeadas novas SEM alterar nenhuma função existente: (1) `getContratoAtivoByLocatario(locatarioId)` que faz `supabase.from('contratos').select('id, data_inicio, data_fim, status, observacoes, unidades(nome, valor_mensal)').eq('locatario_id', locatarioId).eq('status', 'ativo').maybeSingle()` e retorna `data`. Usar `maybeSingle()`, NUNCA `single()` (Pitfall em CONTEXT/RESEARCH). (2) `getParcelasPortal(contratoId)` que faz `supabase.from('parcelas').select('id, numero, data_vencimento, data_pagamento, status').eq('contrato_id', contratoId).neq('status', 'futura').order('data_vencimento', { ascending: false })` e retorna `data ?? []` (null safety conforme CLAUDE.md). NÃO alterar a importação de `supabase` no topo do arquivo (deve ser a mesma que as outras funções já usam). NÃO criar versões alternativas das funções existentes.
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./src/lib/queries-client.js').then(m => { console.log(typeof m.getContratoAtivoByLocatario, typeof m.getParcelasPortal); process.exit(0); }).catch(() => process.exit(1))"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/queries-client.js` contains `export async function getContratoAtivoByLocatario(locatarioId)`
    - `src/lib/queries-client.js` contains `export async function getParcelasPortal(contratoId)`
    - `src/lib/queries-client.js` contains `unidades(nome, valor_mensal)` (the join select string)
    - `src/lib/queries-client.js` contains `.eq('status', 'ativo')` and `.maybeSingle()` (in the getContratoAtivoByLocatario body)
    - `src/lib/queries-client.js` contains `.neq('status', 'futura')` (in getParcelasPortal body)
    - `src/lib/queries-client.js` contains `.order('data_vencimento', { ascending: false })` (in getParcelasPortal body)
    - `src/lib/queries-client.js` does NOT contain `.single()` newly added (existing single() calls preserved; new code uses maybeSingle)
    - `getContratosByLocatario` and `getParcelasByContrato` signatures and bodies remain unchanged (`git diff src/lib/queries-client.js` shows only additions, not modifications to those functions)
  </acceptance_criteria>
  <done>Duas queries novas exportadas, seguindo padrão das existentes, sem regressão.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar ContratoCard.js e ParcelsTable.js (D-11)</name>
  <files>src/components/features/portal/ContratoCard.js, src/components/features/portal/ParcelsTable.js</files>
  <read_first>
    - src/components/features/Parcelas.js (referência de estrutura de tabela com grid e StatusBadge — linhas 96–161; ATENÇÃO: NÃO copiar `style={gridStyle}` — D-11)
    - src/app/dashboard/page.js (referência de metric tile + grid de dados — linhas 183–196, 251–265)
    - src/components/ui/StatusBadge.js (assinatura — `<StatusBadge status="..." />`)
    - src/lib/utils.js (fmtBRL, fmtData)
    - .planning/phases/02-portal-do-locat-rio/02-UI-SPEC.md (contrato visual autoritativo — copy, paleta, eyebrow)
    - .planning/phases/02-portal-do-locat-rio/02-PATTERNS.md (seções ContratoCard.js + ParcelsTable.js — código de referência completo)
    - .planning/phases/02-portal-do-locat-rio/02-CONTEXT.md (D-11)
  </read_first>
  <behavior>
    - ContratoCard recebe prop `contrato` (shape definido em interfaces) e renderiza: eyebrow "CONTRATO ATIVO", nome da unidade (contrato.unidades.nome), valor mensal formatado via fmtBRL(contrato.unidades.valor_mensal), data_inicio e data_fim via fmtData, e <StatusBadge status={contrato.status} />
    - ParcelsTable recebe prop `parcelas` (array) e renderiza: eyebrow "HISTÓRICO DE PARCELAS", header row (#, Vencimento, Pagamento, Status), uma row por parcela com StatusBadge na última coluna; empty state quando parcelas.length === 0
    - Ambos componentes usam Tailwind v4 exclusivo, ZERO inline styles
    - ParcelsTable usa `data-testid="parcelas-table"` (ou role/aria-label "HISTÓRICO DE PARCELAS") no wrapper externo para o spec PORT-03 encontrar a região
  </behavior>
  <action>
    **(a) ContratoCard.js**: Declarar `'use client'` na linha 1 (recebe props mas é renderizado dentro de Client Component — `'use client'` é seguro mantê-lo). Imports: `import { fmtBRL, fmtData } from "@/lib/utils"`, `import StatusBadge from "@/components/ui/StatusBadge"`. Função default `ContratoCard({ contrato })`. Renderizar wrapper `<section className="border border-border-3 bg-surface p-7">` contendo: eyebrow `<span className="eyebrow eyebrow--indigo">CONTRATO ATIVO</span>`, depois um grid 2-col (`grid grid-cols-2 gap-6 mt-4`) com labels mono + valores display. Labels: "UNIDADE" → contrato.unidades?.nome ?? "—"; "VALOR MENSAL" → fmtBRL(contrato.unidades?.valor_mensal); "INÍCIO" → fmtData(contrato.data_inicio); "FIM" → fmtData(contrato.data_fim); "STATUS" → `<StatusBadge status={contrato.status} />`. Cada par label/valor: label `className="font-mono text-[11px] text-fg-4 tracking-[1px] uppercase block"`, valor `className="font-display font-bold text-[24px] leading-tight text-fg-1 block mt-1"`. ZERO `style={...}`. **(b) ParcelsTable.js**: Declarar `'use client'`. Imports: `import StatusBadge from "@/components/ui/StatusBadge"`, `import { fmtData } from "@/lib/utils"`, `import { cn } from "@/lib/utils"` (cn existe — verificar utils.js; se não existir, omitir e usar template literal com classes condicionais). Função default `ParcelsTable({ parcelas })`. Wrapper externo `<section data-testid="parcelas-table" aria-label="HISTÓRICO DE PARCELAS" className="mt-10">` contendo: eyebrow `<span className="eyebrow eyebrow--indigo">HISTÓRICO DE PARCELAS</span>`, depois container `<div className="mt-4 border border-border-3 bg-surface">`. Header row: `<div className="grid grid-cols-[60px_1fr_1fr_1.2fr] border-b border-border-3 bg-[var(--card)]">` com 4 cells header pattern `<div className="px-5 py-3 font-mono text-[10px] font-bold tracking-[1.4px] uppercase text-fg-4">#</div>` para `#`, `Vencimento`, `Pagamento`, `Status`. Loop em `parcelas`: cada row `<div key={parcela.id} className="grid grid-cols-[60px_1fr_1fr_1.2fr] items-center border-t border-border-3 first:border-t-0">` com 4 cells: numero com `String(parcela.numero).padStart(2, "0")` em `font-mono text-[12px] text-fg-2 font-bold`; vencimento com `fmtData(parcela.data_vencimento)` em `font-mono text-[11px]` + cor condicional (`text-danger-fg` quando `parcela.status === 'vencida'`, senão `text-fg-3`); pagamento com `parcela.data_pagamento ? fmtData(parcela.data_pagamento) : "—"` em `font-mono text-[11px]` + cor condicional (`text-success` quando data_pagamento existe, senão `text-fg-5`); status com `<StatusBadge status={parcela.status} />`. Empty state: `parcelas.length === 0 && <div className="px-5 py-12 text-center font-mono text-[12px] text-fg-4 tracking-[0.5px]">Nenhuma parcela registrada para este contrato.</div>`. ZERO `style={...}`. NÃO usar a constante `COL` nem `gridStyle` (anti-pattern em Parcelas.js — Pattern Map).
  </action>
  <verify>
    <automated>grep -L 'style={' src/components/features/portal/ContratoCard.js src/components/features/portal/ParcelsTable.js | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/features/portal/ContratoCard.js` exists and contains `'use client'`, `fmtBRL`, `fmtData`, `StatusBadge`, and the literal `CONTRATO ATIVO`
    - `src/components/features/portal/ContratoCard.js` does NOT contain `style={` (grep returns 0)
    - `src/components/features/portal/ParcelsTable.js` exists and contains `data-testid="parcelas-table"`, the literal `HISTÓRICO DE PARCELAS`, `StatusBadge`, and `grid-cols-[60px_1fr_1fr_1.2fr]`
    - `src/components/features/portal/ParcelsTable.js` does NOT contain `style={` (grep returns 0)
    - `src/components/features/portal/ParcelsTable.js` does NOT contain `gridStyle` or `const COL =` (anti-pattern not copied from Parcelas.js)
    - `src/components/features/portal/ParcelsTable.js` contains `Nenhuma parcela registrada` (empty state copy)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>ContratoCard e ParcelsTable renderizam dados via props com paleta/tipografia Obsidian Blueprint e zero inline styles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire PortalDashboard.js — fetch chain + estados loading/empty/error/data</name>
  <files>src/components/features/portal/PortalDashboard.js</files>
  <read_first>
    - src/components/features/portal/PortalDashboard.js (estado atual — shell criado em Plan 02)
    - src/components/features/Parcelas.js (referência de useEffect + fetch chain + error banner — linhas 22–47, 90–93)
    - src/lib/queries-client.js (após Task 1 deste plano — assinaturas getLocatarioByUserId, getContratoAtivoByLocatario, getParcelasPortal)
    - src/lib/supabase-browser.js (createClient)
    - .planning/phases/02-portal-do-locat-rio/02-PATTERNS.md (seção PortalDashboard.js — código de referência)
    - .planning/phases/02-portal-do-locat-rio/02-RESEARCH.md (Pattern 3)
    - .planning/phases/02-portal-do-locat-rio/02-UI-SPEC.md (copy dos empty/error states)
  </read_first>
  <behavior>
    - Ao montar, chama supabase.auth.getUser() → getLocatarioByUserId → getContratoAtivoByLocatario → getParcelasPortal
    - Estado loading → exibe "Carregando..." (placeholder simples; skeletons full ficam para polimento)
    - Estado sem locatario OU sem contrato ativo → exibe empty state "Nenhum contrato ativo" / "Você não possui contrato ativo. Entre em contato com o proprietário."
    - Estado erro → exibe error banner "Erro ao carregar dados" / "Não foi possível buscar seu contrato. Tente recarregar a página."
    - Estado dados ok → renderiza <ContratoCard contrato={contrato} /> + <ParcelsTable parcelas={parcelas} />
    - Header da página (eyebrow + título "Seu Contrato.") permanece visível em todos os estados
  </behavior>
  <action>
    Reescrever (não acrescentar) `src/components/features/portal/PortalDashboard.js` mantendo a primeira linha `'use client'`. Imports: `import { useEffect, useState } from "react"`, `import { createClient } from "@/lib/supabase-browser"`, `import { getLocatarioByUserId, getContratoAtivoByLocatario, getParcelasPortal } from "@/lib/queries-client"`, `import ContratoCard from "./ContratoCard"`, `import ParcelsTable from "./ParcelsTable"`. Após imports, `const supabase = createClient()`. Default export `function PortalDashboard()` com estados: `locatario` (null), `contrato` (null), `parcelas` ([]), `loading` (true), `erro` (null — convenção do projeto usa "erro" não "error", CLAUDE.md). useEffect uma vez com fetchData async: try → `const { data: { user } } = await supabase.auth.getUser(); if (!user) return;` → `const loc = await getLocatarioByUserId(user.id); setLocatario(loc); if (!loc) { setLoading(false); return; }` → `const ct = await getContratoAtivoByLocatario(loc.id); setContrato(ct); if (!ct) { setLoading(false); return; }` → `const parc = await getParcelasPortal(ct.id); setParcelas(parc);` → catch `setErro(e.message ?? 'Erro desconhecido')` → finally `setLoading(false)`. Render: wrapper raiz `<div className="romma-page bg-background min-h-full px-12 pt-12 pb-20">` contendo: header (`<span className="eyebrow eyebrow--indigo">PORTAL DO LOCATÁRIO</span>` + `<h1 className="font-display font-bold text-[48px] leading-none tracking-[-2.4px] text-fg-1 m-0">Seu Contrato.</h1>` + `<p className="font-mono text-[11px] text-fg-4 mt-2">Acesso restrito — contrato e histórico de parcelas.</p>`) e abaixo a área condicional: se `erro` → banner `<div className="px-4 py-[10px] mt-8 bg-[var(--danger-bg2)] border border-danger-fg font-mono text-[12px] text-danger-fg"><strong>Erro ao carregar dados</strong><br />Não foi possível buscar seu contrato. Tente recarregar a página.</div>`; senão se `loading` → `<div className="mt-8 font-mono text-[12px] text-fg-4">Carregando...</div>`; senão se `!contrato` → empty state `<div className="mt-8"><span className="eyebrow eyebrow--indigo">PORTAL DO LOCATÁRIO</span><h2 className="font-display font-bold text-[28px] text-fg-1 mt-2">Nenhum contrato ativo</h2><p className="font-mono text-[12px] text-fg-4 mt-2">Você não possui contrato ativo. Entre em contato com o proprietário.</p></div>`; senão → fragment com `<div className="mt-8"><ContratoCard contrato={contrato} /></div>` e `<ParcelsTable parcelas={parcelas} />`. ZERO inline styles. NÃO importar `supabaseAdmin`. NÃO importar `@/lib/supabase` legacy — apenas `@/lib/supabase-browser` (REF-02 alignment).
  </action>
  <verify>
    <automated>npx playwright test e2e/portal.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/features/portal/PortalDashboard.js` starts with `'use client'`
    - File contains `import { createClient } from "@/lib/supabase-browser"`
    - File contains `getLocatarioByUserId`, `getContratoAtivoByLocatario`, `getParcelasPortal` (all three imported from `@/lib/queries-client`)
    - File contains `import ContratoCard from "./ContratoCard"` and `import ParcelsTable from "./ParcelsTable"`
    - File contains `const [erro, setErro]` (convention `erro` not `error`)
    - File does NOT contain `style={` (grep returns 0)
    - File does NOT contain `from '@/lib/supabaseAdmin'` or `from "@/lib/supabaseAdmin"`
    - File does NOT contain `from '@/lib/supabase'` (uses supabase-browser only — REF-02 alignment)
    - File contains the literal `Nenhum contrato ativo`
    - File contains the literal `Erro ao carregar dados`
    - `npx playwright test e2e/portal.spec.js` PASSES all three tests (PORT-01, PORT-02, PORT-03)
    - `npx playwright test e2e/auth-redirect.spec.js` PASSES (no regression)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>PortalDashboard busca dados via cadeia de queries e renderiza ContratoCard + ParcelsTable; todos os estados (loading/empty/error/data) cobertos; PORT-02 e PORT-03 GREEN; TEST-03 completo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase (anon) | Client Component usa anon key via supabase-browser; RLS server-side filtra dados |
| Locatário A ←→ Locatário B | RLS deve impedir Locatário A de ver dados de B |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Information Disclosure | getContratoAtivoByLocatario | mitigate | Query filtra `eq('locatario_id', locatarioId)`; o `locatarioId` é derivado server-side via `getLocatarioByUserId(auth.uid())` no Client Component — locatário não pode forjar id de outro. RLS no Supabase é a defesa primária. |
| T-02-09 | Information Disclosure | getParcelasPortal | mitigate | Query filtra `eq('contrato_id', contratoId)` onde `contratoId` veio de query anterior já filtrada por locatário do usuário autenticado. RLS no Supabase é a defesa primária. |
| T-02-10 | Tampering | Portal expõe mutação acidental | mitigate | Nenhum botão / Server Action é renderizado nos componentes do portal. Portal é estritamente read-only (CONTEXT.md). Acceptance criteria de Plan 03 verifica ausência de `<button>` de ação destrutiva nos novos componentes. |
| T-02-11 | Information Disclosure | parcela `futura` em response | mitigate | `.neq('status', 'futura')` filtra na query (D-07). Status futura nunca atinge o componente — defesa em profundidade contra dump acidental. |
| T-02-12 | Elevation of Privilege | supabaseAdmin em Client Component | mitigate | Acceptance criteria proíbe import de supabaseAdmin; review de código detecta em `grep` (acceptance criteria explícita). |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma instalação de pacote nova (RESEARCH § Package Legitimacy Audit) — não aplicável |
</threat_model>

<verification>
- `npx playwright test e2e/portal.spec.js` — todos os 3 testes (PORT-01, PORT-02, PORT-03) PASSAM
- `npx playwright test e2e/auth-redirect.spec.js` — todos os testes passam (nenhuma regressão; 1.2 já corrigido no Plan 01)
- `npx playwright test` — suite completa do projeto passa (não introduzir regressão em dashboard.spec.js, server-actions.spec.js, auth-session.spec.js)
- `npm run build` exits 0
- Visit manual /portal/dashboard como Locatário: vê eyebrow, título, ContratoCard com unidade/valor/status, ParcelsTable com mix paga/pendente/vencida (3 rows) e nenhuma futura
- `grep -r "style={" src/components/features/portal/` retorna 0
- `grep -r "supabaseAdmin" src/components/features/portal/` retorna 0
</verification>

<success_criteria>
- PORT-02 GREEN
- PORT-03 GREEN
- TEST-03 GREEN (suite portal.spec.js completa)
- VIS-03 atendido — todos os novos componentes em Tailwind v4 com paleta Obsidian Blueprint e copy do UI-SPEC
- Phase 2 funcionalmente completa
</success_criteria>

<output>
Create `.planning/phases/02-portal-do-locat-rio/02-03-SUMMARY.md` when done
</output>
