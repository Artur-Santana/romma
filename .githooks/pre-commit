#!/bin/sh
branch=$(git symbolic-ref --short HEAD 2>/dev/null)
if [ "$branch" = "main" ]; then
  echo "❌ Commits diretos em main bloqueados."
  echo "   Crie uma branch: git checkout -b <nome-da-branch>"
  exit 1
fi
